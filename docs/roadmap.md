# CBT — Roadmap

> Where the dashboard is going. Looser than a handoff. Items here graduate to numbered handoffs in `docs/handoffs/` when they sharpen up. Polish lives in `docs/ui-plan.md`, not here.

## Framing question

**WTF is going on in Congress?**

That's what someone opens this dashboard to answer. The product positioning isn't "track bills" or "summarize legislation." It's "make sense of what's happening." Every theme below earns its place by serving that question.

This started as a personal sense-making project. The author is figuring out what's actually happening in Congress, building the tools they wished existed, then opening those tools to anyone else asking the same question. The dashboard prioritizes comprehension over coverage. Most existing trackers fail on that axis: they show every bill and help with none.

What answering it in 30 seconds looks like, as the home page the dashboard is building toward:

- Three sentences at the top summarizing the week, generated from the underlying data
- Stage funnel showing where bills are pooling right now (the bottleneck of the moment)
- Topic mix showing what's getting worked on versus ignored
- Top 5 movers (advanced this week), top 5 stalls (went quiet), top 5 in the news
- One historical anchor: pace versus the 118th at the same point in the term

The current home page (page 1 of 157 feed rows) buries the answer six clicks deep. Reorienting around this question gives the home-page redesign a concrete target instead of guessing what the dashboard summary surface should hold.

## Lenses on the framing question

Specific questions an analyst would ask. Each is a sub-cut of "wtf is going on." Add and edit freely; the doc gets sharper as this list grows.

### Noise vs signal

Filtering the noise is what makes the framing question answerable in the first place. Sub-questions, all unlocked by ceremonial classification:

1. What's the noise rate, by chamber, by month?
2. Who makes the noise (highest ratio of ceremonial to substantive bills)?
3. Is the 119th noisier than the 118th?
4. Does noise correlate with party, seniority, swing-district status, or seat-up year?
5. Are some topics noisier than others (health awareness weeks, defense recognition resolutions)?

Cuts 1, 2, and 5 ship as analyst views the day `is_ceremonial` lands in the table. Cuts 3 and 4 need historical or member-level data first.

### Other lenses (seeded, to expand)

6. Which sponsors recycled bills from the 118th and how many landed this time?
7. What's the freshman class focused on?
8. Who introduces the most bills that go nowhere?
9. Which committees are graveyards versus paths to floor?
10. Which bills are getting press right now and from whom?
11. What's the shape of activity over time (introductions per week, enactments per month, deaths per quarter)?
12. Which sponsors are up for reelection next cycle, and what's their pass rate?
13. Which coalitions does each member belong to (caucus memberships, union endorsements, advocacy alignments), and how does that align with their voting and sponsorship behavior?

## Site assessment

Snapshot from a fresh look at https://cbt-chi-silk.vercel.app on 2026-05-12.

What's working: six views established (feed, `/sponsors`, `/stale`, `/changes`, `/president`, `/watchlist`). 15,677 bills synced, daily cron landing at 02:48 MT, stable pipeline. Topic taxonomy renders cleanly as compact tags, stage indicators give immediate progress signal. The terminal aesthetic is consistent across pages.

What's noisy or missing:

- **Ceremonial pollution is loud.** Visual count of page 1: ~14 ceremonial rows out of ~100. Mother's Day, Cinco de Mayo, 200 years of Peru diplomacy, "National Day of Reason," AAPI Mental Health Day, Glioblastoma Awareness Day, Tardive Dyskinesia Awareness Week, National Nurses Week, Eliot L. Engel Post Office renaming, Roy A. Rutherford Air Traffic Control Tower naming, Martinsville Missile land speed record recognition, "National Small Business Week," "National Postpartum Awareness Week for Communities of Color," "Arthritis Awareness Month." Every analyst stat downstream is skewed by this.
- **Zero visualizations.** Pure list views. No topic-distribution charts, no time-series, no funnel, no comparison overlays.
- **No landing moment.** Home page is the feed. There's no "what should I know right now" summary; the framing question gets no answer surface.
- **No external context.** Bills exist in isolation. Nothing surfaces which got coverage, which are being whipped, which are tied to active news cycles.
- **No historical comparisons.** Can't see this Congress versus the 118th at a glance.

## Architecture

Lives in `SKILL.md` as the information architecture rule (snapshot → hub → sub-pages). Themes below reference that rule rather than redefining it.

## Themes

### 1. Bill signal: noise classification + similarity

Direct service to the noise lens, prerequisite for the framing question, highest leverage on the list. Without this, every downstream cut is polluted.

Ceremonial classifier: one additional Gemini classification at summarize time, stored as `is_ceremonial` boolean, hidden by default with a toggle. Folds into the existing summarize call so marginal cost is near zero (rough estimate ~$4 to backfill the 15,677 corpus, then cents per month going forward). Four of the five noise sub-questions become analyst views the day this boolean lands.

Similarity clustering: bills with near-identical structure share title templates and action patterns. v1 is rule-based regex on titles ("Resolution honoring the memory of ___", "Resolution designating ___ as National ___ Day", "To rename the ___ Post Office located at ___"). Becomes the "similar bills" sub-page link off the bill hub, plus a standalone duplicate-shaped-bills view that's useful on its own (shows the post-office-renaming champion, the awareness-week king). Embedding-based v2 deferred until regex proves insufficient.

Sequence: ceremonial first (one handoff, ~1 day). Similarity v1 second (one handoff, ~1 day).

### 2. Home page redesign

Promoted from a sequencing step to a theme. The current home is the feed; the future home is the answer surface for the framing question. The 30-second sketch at the top of this doc is the brief.

This blocks reports and graphs. Both want a dashboard surface that doesn't exist yet, and shipping either one before the home page redesign means rework.

Open questions: keep the feed as a sub-route (`/feed`) or push it lower on the new home? Pagination there or full-bleed list? How much LLM-generated prose at the top before it stops feeling honest and starts feeling like a generated newsletter?

### 3. Weekly reports

A report is a packaged answer to the framing question, generated on a cadence and saved as a snapshot. Different from a live view: it's a moment in time, has commentary, and is shareable as a URL or file.

Two cuts on day one:

Bill changes: what's new this week, what died (no action for some chamber-appropriate window), what advanced (stage transitions), what got revived. Aggregated counts plus a list of the most notable individual movements. Most of this is already computable from existing data; `/changes` is a real-time version of the same thing.

Most talked about: which bills got the most external attention (news mentions, social signal, press releases). Depends on the news signal theme below. Without that data, this cut can't ship.

Open scope before sizing: storage (MDX files in the repo versus a `reports` table), trigger (cron-generated Mondays versus on-demand), authoring (pure LLM versus template with LLM commentary versus hand-written with data injected), format (read-on-site versus downloadable). The Substack draft is effectively the manual prototype.

### 4. News signal: breaking news + media attention

External data ingestion. Pull bill mentions from news sources, tag them to specific bill IDs, store with timestamps. Two surfaces:

Breaking news: real-time ticker or banner showing bills that got coverage in the last 24–48 hours. The closest the dashboard gets to a Bloomberg-style headline feed.

Media attention column: per-bill count of recent mentions, shown on the feed row and in the expanded view. Surfaces which bills are quietly getting traction.

Source options: GDELT (free, comprehensive, noisy), NewsAPI (paid, cleaner, tighter limits), or direct RSS from Politico, The Hill, Punchbowl, Roll Call (controllable, fewer surprises, more setup). The matching is the hard part, not the ingestion. Bill IDs are matchable but most coverage references the marketing title or sponsor name. Plan on iteration.

Sequence: pick a source. Build the sync. Run match accuracy against a hand-labeled sample of 100 bills. Ship breaking-news first (lower bar for usefulness). Media attention column second once matching tightens.

### 5. Graphs and visualizations

The dashboard has no visual idiom yet. Adding graphs is a design decision before it's an implementation task. Pick the idiom once, apply it across views.

Candidate first charts, ordered by analyst payoff: stage funnel on the home page (single bar chart, surfaces the bottleneck of the moment), topic distribution faceted by chamber, activity over time stacked by topic, sponsor productivity scatter (volume versus pass rate) on `/sponsors`, 119th versus 118th laws-enacted comparison on reports.

Library question: D3 directly, Recharts, Observable Plot, or hand-rolled SVG. The terminal aesthetic argues for thin styling and tight control, pushing toward Observable Plot or hand-rolled SVG over Recharts' defaults.

Sequence: pick the library. Ship the stage funnel as the first chart on the new home page. Iterate. Apply the same component pattern to later charts.

### 6. Member legislative depth

Applies the architecture pattern (snapshot → hub → sub-pages) to the member entity. Hub is `/sponsors/[bioguideId]`, thesis is "what this person works on in Congress." Voting record, sponsored bills, committee assignments, badges. Race coverage lives at theme 7's surface and connects back via a header tag, not a hub section. Snapshot lives in the row dropdown on `/sponsors` and the sponsor expansion in the bill feed (couple of badges, current top donor, last notable vote, "view full profile" button).

External data, each a sync pipeline of its own: member bio and committees (Congress.gov `/member/{bioguideId}`, free, already in your auth), endorsements and affiliations (caucus rosters mostly public, union and advocacy endorsements scattered across org sites and press releases), donors (FEC bulk or OpenSecrets API), stock trades (Capitol Trades, Unusual Whales; confirm sources), district demographics (Census API).

Don't batch these. Each is roughly the scope of the original Congress.gov sync.

#### Header indicators (links out, not embedded sections)

Two flags belong in the page header next to the name and party, both clickable to theme 7's race surface:

- "Seat up 2026" (or whichever cycle) when applicable
- "Active campaign" when the member is currently in a contested cycle

These do not get sections on the deep page. Race detail lives on `/race/[id]`; the header indicator is the bridge.

#### Endorsements and badges

The smallest piece in this theme but the most legible at a glance. Powers lens 13 and turns member names on the feed into coalition signals ("this is a DSA-endorsed, Squad-aligned Dem" versus "this is a Freedom Caucus Republican").

Sourcing tiers, easiest to hardest:

1. **Caucus memberships.** Mostly public rosters: Progressive Caucus, New Democrat Coalition, Problem Solvers, Republican Study Committee, the identity caucuses (CBC, CHC, CAPAC). Freedom Caucus is semi-secret but tracked by journalists. Hand-enter the initial set if no clean API exists; refresh quarterly.
2. **Union endorsements.** AFL-CIO, Teamsters, SEIU, UAW, IBEW, NEA. Each org publishes their cycle endorsements; sync would scrape or hand-enter per cycle.
3. **Advocacy alignments.** DSA, Justice Democrats, Indivisible (left); Club for Growth, Heritage Action, Susan B. Anthony List (right); Sierra Club, Planned Parenthood, NRA (issue). Most scattered and per-cycle.

UI: badge component sits in the page header next to the name (top 2–3 by priority) and renders the full set in a dedicated row on `/sponsors/[bioguideId]`. A 10-badge member should not crash the feed-row layout.

Schema sketch: separate `affiliations` table keyed by `bioguideId`, with `org`, `category` (caucus, union, advocacy), `source_url`, `last_verified`. Don't fold into the bills schema.

#### Order

Member bio first (free, unblocks `/sponsors/[bioguideId]` detail pages and the seat-up header indicator). Caucus badges second (small, public, immediate visual payoff). Donors third (largest analytical payoff). Stock trades and broader endorsements after that.

### 7. Races

Separate surface from member depth so neither overcrowds the other. The bridge is the header indicator on the member page; the destination is `/race/[id]` (likely a state-district-cycle pattern like `/race/CO-08-2026`).

A race page answers: when is this seat up, who's running, what do the ratings say, what's the fundraising picture, what does the district look like. Electoral context for the legislator, not a competing thesis to "what they're doing in Congress."

External data:

- Race ratings: Cook, Sabato, Inside Elections (likely manual unless one has an API; quarterly refresh)
- Candidate roster: Ballotpedia or FEC filings
- Fundraising: FEC, can layer on the donor pipeline from theme 6
- District demographics: Census API
- Polling: 538 successor, or curated; harder to source cleanly

MVP race page: rating, seat-up year, top three candidates, link back to incumbent's bio. Ship that with mostly-manual data, layer automated sources in later.

Open question: which races warrant pages? All 435 House + 33–34 Senate seats per cycle is a lot. Tiered approach: full pages for competitive races (any rating not "safe"), stubs for safe seats showing just incumbent and cycle.

### 8. CCBT parity

CCBT lags CBT by every feature that shipped after the fork: `/stale`, `/changes`, `/president`, sponsor depth, sort dropdown, fluid layout, header rename. The longer the gap, the harder the port.

Three options: inline porting (every CBT handoff that ships, follow with the CCBT equivalent in the next session), batched porting (quarterly sweep), or selective porting (only features that pass a "this matters at the state level too" filter). Doing nothing is worse than any of these.

### 9. UI polish

Lives in `docs/ui-plan.md`. Not duplicated here. Pull from there when you want a polish session.

## Sequencing

Working order, subject to your priorities:

1. **Ceremonial classifier.** Cheap, immediately visible, unblocks every downstream stat including the home page summary.
2. **Similarity v1.** Builds on ceremonial. Same regex-on-title shape.
3. **Home page redesign.** Scoping conversation first, then handoff. Sketch the layout, decide what blocks live on it, decide what happens to the feed (sub-route or pushed down).
4. **First graph (stage funnel).** Picks the visual idiom. Lives on the new home page as the first concrete answer to the framing question.
5. **Reports format scoping.** Chat, not a handoff. Decide MDX versus DB, cron versus on-demand, authoring style.
6. **First weekly report (bill changes cut).** Ship with the "most talked about" section as a placeholder.
7. **News sync pipeline.** Pick a source, build, validate matching.
8. **Breaking news view + media attention column.**
9. **Most talked about cut goes live on weekly reports.**
10. **Member bio sync.** Unblocks `/sponsors/[bioguideId]` detail pages plus the "seat up" / "active campaign" header indicators.
11. **Caucus membership data + badge component.** Smallest, most public, biggest immediate visual payoff. Powers lens 13. Schema design here determines how union and advocacy endorsements layer on later.
12. **Race surface MVP.** `/race/[id]` stubs with rating, seat-up year, candidate roster, link back to incumbent. Header indicator from step 10 needs a destination; this is it.
13. **One member-depth pipeline (donors or stocks).** Pick by analytical value.
14. **CCBT port batch.**

That's roughly six months of work at a CBT pace if you ship 2–4 handoffs a week. Don't treat it as a plan. Treat it as one defensible order.

## What this doc isn't

- Not a deadline-bound plan. Order is logical, not calendar.
- Not a feature checklist. Items here are themes; handoffs are checklist work.
- Not where polish lives. `docs/ui-plan.md` owns those.
- Not a contract. Cross things off, reorder, kill items as the dashboard tells you what it actually needs.

## Status

> **As of HO 519 · 2026-07-25.** This block is the **source of truth**. Future updates edit this block, not memory or off-repo notes. *(**HO 519 — the 2nd full re-derivation** (the deferred "next full audit"), read-only §0–§5 against live prod `5681322`. **Structurally green:** sync fresh (17,027 bills == `bills_fts`), 14 crons healthy, the four fixed loops holding in steady state (summarize 406, weekly-report 407, `/api/sync` trailing-step 480/481 at 7% post-fix timeout, trades 483 p90 well clear), cold-plan audit clean (0 unexpected BAD of 50) — plus **9 post-404 queries spot-EXPLAIN'd by hand** to close the frozen-`cold-start-audit-332` coverage gap (the LDA / amendments / nominations / news-rail pillars postdate its inventory), every one riding its intended index. **Two ratified bar changes:** **News signal CLOSED → 98** (the "obs layer joinable" label under-sold a now richly-surfaced pillar — `/news` route + `?member=` + topic rail + member rail, race→news, member→news, the dashboard NEW badge), and a **new combined bar `Lobbying / amendments / nominations` = 92** (the 435–463 "extend downstream along the bill spine" arc, v1-complete across data + aggregate + detail + cross-links). **Overall ~98 → ~97**, recomputed from the reconciled 9 bars. **One data-integrity item cleared this pass:** the SC Graham seat change (HO 520) — replacing HO 404's now-fixed "P1 OK-Senate still live" line (that was resolved at 411/412; the new integrity finding this pass was SC, now also fixed via a routine roster re-sync, zero code). "Also" notes now run through **HO 520**.)*

**Overall: ~98%.** Recomputed from the reconciled **9** bars (96·100·97·98·98·100·99·98·92 ≈ 97.6) — nudged up from ~97 as **Member depth closed 99 → 100** (HO 526, the participation arc; see provenance). The HO 519 re-derivation had eased it to ~97 when News became a number (CLOSED→98) and the new 92 pillar bar joined the average. CCBT (the Colorado sister project) is downstream port work, **not** a CBT roadmap theme — excluded from this figure.

| Theme | % |
|---|---|
| Foundation | 96 |
| Home | 100 |
| Visualizations | 97 |
| Weekly reports | 98 |
| News signal | 98 |
| Member depth | 100 |
| Races | 99 |
| Committee activity / hearings | 98 |
| Lobbying / amendments / nominations | 92 |

**Baseline provenance.** Foundation (96) / Member depth (98) were **re-derived against live prod state in HO 404** (the full audit) — no longer the off-repo-through-HO-221 baseline. Foundation confirmed at 96 (plumbing healthy; two cron regressions logged as loops, not baked in); Member depth moved 99 → 98 (comprehensive member surfaces, minus the unbuilt member→news + the 463/530 by_size gap). See the "Re-derivation (HO 404)" note below for the evidence. **Home (98→99)** and **Weekly reports (95→96)** were bumped this pass for the committee-activity / hearings arc (the /dashboard-v2 On the Hill band touches Home; the weekly-report COMMITTEE ACTIVITY section touches Weekly reports). **Committee activity / hearings (98, new this pass)** is **effectively complete** — the full 263–269 arc shipped; the open remainder is the two banked items below. **HO 519 re-derivation (#2):** the canonical tracked corpus grew 15,075 → **16,657** (`getCorpusStats(true)`); **News signal was promoted CLOSED → 98** (a now richly-surfaced pillar — `/news` route + rails + race/member hubs — not an excluded thread; `98` because the residue is gated/inert, not active gaps); a **new combined bar `Lobbying / amendments / nominations` was minted at 92** (the 435–463 downstream arc, v1-complete data + aggregate + detail + cross-links, with real banked gaps); every other bar **held** (the 113 HOs since 404 were chrome/infra/enabling, self-declared no-bar-move and confirmed in aggregate by the §1/§2 green audit); and the **SC Graham data-integrity item was cleared** (HO 520, roster re-sync + `backfill:races`, zero code). Overall eased ~98 → ~97 across the 9 reconciled bars. **Post-audit bar move (HO 526):** **Member depth 99 → 100** for the participation arc (B1 chamber-median context, HO 523 + B2 career-votes rollup, HO 525); the **"external scorecards"** half of the old Member-depth hold is **reframed** from a hold to **optional breadth** — USCPR already ships on the hub, and the HO 522 probe established broader scorecards are a maintenance-appetite choice, not a capability gap (VoteSmart NO-GO on access; LCV / Heritage viable-but-unbuilt). This is **one tracked bar move on top of the HO 519 re-derivation, not a new audit** — the anchor stays "As of HO 519", and this clause resolves any apparent table-vs-anchor drift. *(Prior pass: Home 95→98 + Visualizations 90→95 for the HO 230–234 design arc; Races 98 reflects HO 222–226.)*

**Shipped recently (HO 263–269, the committee-activity / hearings arc — effectively complete):** committee-meetings data layer (263) · standalone `/hearings` with grouped list + two-week Mon–Fri calendar (264 / 265) · `/dashboard-v2` On the Hill band (266, corrected onto v2 in 269 so it survives the `/` swap) · committee-detail + bill-hub meeting embeds reusing the list row (267) · weekly-report COMMITTEE ACTIVITY section, fallback variant — markup blocks at current stage, no VIA tags (268). *(HO 230–234 prior: the dashboard design pass; HO 222–226: /races LIST + district maps/modals.)*

The "Also (…)" changelog is append-only and the pointer is frozen-per-block. Close an arc by adding a new block — which carries its own `Also notes now run through HO N` — never by editing a prior block's clause or its pointer. Each block freezes its `run through HO N` at append time, so the "running pointer" is simply whichever block is last: every distinct `run through HO N` appears exactly once, monotonic. A doc sweep therefore adds a pointer via its new block and touches no earlier one — a sweep's grep gate expects the previous block's pointer to still read 1, not 0. Mis-bumping a prior block both duplicates a pointer and violates the append-only rule (the HO 518 §1b near-miss; the HO 418/429 precedent).

**Also (HO 270–272), v2 refinements:** the `/dashboard-v2` races strip became a `HEARINGS | RACES` tabbed box with hearings the default tab (270/271) — superseding the standalone On the Hill band — plus a live `MOVES n` badge on the RACES tab (per-card `MOVED · <lean>`; `NEW` dark until the news→race join, 272). Refinements to the already-counted v2 Home + Races surfaces — **no theme-% change** (Home 99 / Races 98 unchanged).

**Also (HO 273–275), v2 coherence sweep — Home redesign now build-complete + coherent.** The `/dashboard-v2` surface was reconciled against the mock (`docs/design/dashboard-2col.html`): hearing meeting titles cleaned at render (273); race-card Kalshi/Polymarket odds party-normalized via the full candidate roster + battlefield/card-dot color reconciled to the incumbent party + MARKETS/SIGNALS tape labels pinned outside the marquee + a ±8% implausible-move tape guard (274); doc sweep (275). All v2 surfaces — hearings, races, tape, feed — now match the mock. **Home held at 99, not bumped to 100:** the residual 1% is the v2→`/` swap, now gated **solely on the `/dashboard-v2` cold-start 500** (backlog OPEN LOOPS, BLOCKER — the HO 238 fan-out cold-abort `/` would inherit on the swap), **not** on any remaining coherence gap. No other theme-% change.

**Also (HO 277–280), outage-fixing arc — swap blocker CLEARED.** The `/dashboard-v2` "cold-start 500" turned out to be the **gated-aggregate mis-plan** class (a summary-gated fat-`bills` aggregate the statless Turso planner drops onto `idx_bills_is_ceremonial`, slow even warm), the same root cause behind live 500s on `/members` (277) and `/bills` (279). All fixed with additive covering/partial indexes + gated `INDEXED BY` hints (280 doc sweep). **Cold-verified:** `/members` 0.885s, `/dashboard-v2` 5× cold 200, `/bills` 0.705s first-hit-after-idle. **Home stays at 99** (the swap hasn't shipped), but its residual 1% — the v2→`/` swap — is now **UNBLOCKED and actionable** (backlog QUEUED), no longer blocked on the cold-start 500. Home → 100 when the swap ships. No other theme-% change.

**Also (HO 281–294), v2 polish — the B-series tweaks + the B2 tape arc.** Header type scale (281: title 26 / nav 16 / active amber underline, ≥701px), the v2 HEARINGS|RACES tabbed-box height/overlay fix (282), the WeeklyBand WoW deltas + a fourth HEARINGS metric + per-metric hover popovers (283/286), the chip-family additive tokens + shared `<SourceTag>`/`<MicroTag>` (287), and the **B2 markets-tape arc** (288–294): MARKETS gained five equities (NVDA/AAPL/MSFT/GOOGL/LMT) + CPI/UNEMP (MO badges); the second strip became **ODDS** — prediction-markets-only (shutdown · Fed-cut · recession, all dual-source K/P); counter-scroll + edge-fade; a curated policy-hook hover line; and the hover box portaled + re-anchored to pop over the ticker. Out-of-band: the weekly-report cron made reliable via a daily catch-up (284/285). All of this is v2 polish on the already-counted Home surface plus the shared WeeklyBand/header (both render on `/` and `/dashboard-v2`) — **no theme-% change; Home held at 99** pending the still-unshipped v2→`/` swap (the residual 1%, backlog QUEUED). The existing id/topic/stage/party chip migration is queued into a **B6 arc** (chip-family spec; 287 shipped only the additive half).

**Also (HO 296–303), v2 feature-complete — the B6 feed-row arc + the B2 tape update.** The **B6 arc (296–300)** rebuilt the `/dashboard-v2` feed as one shared `V2FeedList` row across MOVERS / TOP STALLS / NEW THIS WEEK (metric the only per-tab swap): collapsed rowhead with a sans title (297), the 6-node stage bar with a data-driven ping/park/complete current node (298), the expanded RELATED block (NEWS + omit-when-empty HEARINGS; ODDS dark — no bill↔market data, 299), and the sponsor hover card + meta column (300, which pushed the full sponsor enrichment onto the three v2 feed queries). This is the **first `--sans` use in CBT** (feed prose; mono stays for chrome). The **B2 tape update (301–303)** added a 4th ODDS pair **FED CUT SEP** (302, via new `kalshiEvent`/`polyMonth` symbol pins) and the **richer four-part hover box** + the symbol-amber/value-white tape convention (303); the 301 re-probe reaffirmed the data walls and banked a US-DEFAULT pair as the only honest debt-risk fill. **With this the v2 dashboard is FEATURE-COMPLETE** — no remaining v2 build work, only the swap. **Home held at 99**, not 100: the residual 1% is the still-unshipped **v2→`/` swap** (backlog QUEUED, now READY — open decision: replace `/` outright vs keep an archive route). No other theme-% change.

**Also (HO 311–314), the v2→`/` swap + the markets/reports arc — Home redesign SHIPPED.** **HO 311** made the v2 redesign the live home page: `/` now renders the DashboardV2Header + HEARINGS|RACES box + WeeklyBand + 49/51 body with the summary-gated count; the old `/` is preserved unlinked at `/dashboard-classic`, and `/dashboard-v2` permanently redirects to `/`. The shared stage funnel / topic treemap render in a new opt-in **`staticMode`** at `/` (read-only, no `?stage=`/`?topics=` rebasing — the page takes no `searchParams`); `/dashboard-classic` keeps full click-to-filter via a **`basePath`** prop. **This ships the Home redesign theme → Home 99 → 100.** The residual click-to-filter-parity-vs-static decision and the `/dashboard-classic` sunset are banked (backlog OPEN LOOPS), not blockers. Out-of-band, same arc: the **markets tape freeze** was diagnosed (313) as GitHub Actions scheduled-cron delivery (not a fetch failure) and fixed (314) with a reliable **Vercel daily cron floor** at 21:30 UTC; and the **reports-index-stale** report (312) closed as a **no-op** — it self-resolved when the HO 285 catch-up landed June 8 and revalidated, both bug hypotheses refuted by code. **Overall ~97 → ~98** with Home complete. No other theme-% change.

**Also (HO 316–317), the bill-row standardization arc — collapsed + expanded, both surfaces, COMPLETE.** **316** unified the topic chip into the shared bordered **`TopicChips`** (the dashboard feed + every `BillRow` surface; the lone borderless holdout is `/bill/[id]`). **317** unified the **expanded panel** into the shared **`BillExpandPanel`** (containing the one shared **`BillStageBar`**), rendered identically on the dashboard `/` and `/bills`, differing only in trigger — dashboard hover (`:hover`/`:focus-within`, box grows inline) vs `/bills` click — built to `docs/design/expanded-panel-unified.html`; the `/bills` collapsed title also moved to the sans face. **No theme-% change** — this polishes/unifies already-counted surfaces (Home is at 100; the `/bills` row touches the off-repo Foundation baseline, not re-derived). Banked from the arc: the rich HEARING slot, the dead-CSS sweep, touch-expand, and the `/bill/[id]` chip migration (backlog). No other theme-% change.

**Also (HO 319–321), the dashboard-row arc — interaction + row polish, COMPLETE.** **319** reverted the dashboard expand from the HO 317 hover model back to **click** (it was transient + dead on touch) — both surfaces now expand on click via `useSingleOpenPanel`. **320** restored the **distributions click-to-filter** on `/` (ported from `/dashboard-classic`, deferred at the HO 311 swap as `staticMode`): clicking a STAGE bar / TOPIC tile rebases the gated distributions + MOVERS + BREAKING, with `ActiveFilterStrip` — closing the HO 311 open loop. **321** made the collapsed feed row **two lines** (title over sponsor + `[party-state]` + chips), matching `/bills`. **No theme-% change** — all three polish the already-at-100 Home surface; `/dashboard-classic` untouched. No other theme-% change.

**Also (HO 328), the Members + Committees merge — one surface.** `/members` became a two-pane browser: a committee rail (the former `/committees` index — that route now `redirect("/members")`) + the member list, the chamber filter driving both panes, click-a-committee scoping the roster, per-member topic-mix bars (`MemberTopicBar`), and an UPCOMING HEARINGS rail group. The HO 152/197 productivity scatters were dropped (`getSponsorProductivity` + `SponsorProductivityRow` deleted, HO 330); a new `idx_bills_sponsor_topics` covering index serves the topic-mix bar. **Member depth 90 → 93** — the committee index, member roster, and per-member committee membership now read from one consolidated surface, with committees folded in from the retired standalone index. The member-expand cold-start 500 is under separate diagnosis (HO 329, fix pending). No other theme-% change.

**Also (HO 333), the Electoral consolidation — Races + Primaries → one surface.** `/races` + `/primaries` collapsed into a single **`/electoral`** surface (both 308-redirect in): the competitive map on top + a NEW primary-calendar **timeline band** below it (`PrimaryTimeline` + the `ElectoralBoard` wrapper), wired so clicking a timeline date paints that date's voting states amber on the map (selections accumulate / drop-on-reclick / CLEAR ALL; hover previews an outline). Sen+House COMBINED per date; data via the new **uncached `getPrimaryCalendar(2026)`** (no primaries cache tag exists, matches `getDashboardPrimaries`). The electoral GroupTabs sub-nav (HO 173) is retired; the nav item is relabeled **Electoral**. **State-click → the HO 225 district modal was KEPT** (Corey's call — a deliberate deviation from the handoff's "read-only map," preserving the district drill + HO 236/237 metro panels). The HO 226 primaries recency-map + its modal/scrubber are superseded by the timeline (`PrimariesMap`/`PrimaryDistrictModal`/`RacesMap` left unreferenced — backlog open loop). **Races 98 → 99.** No other theme-% change.

**Also (HO 346–352), the Patterns / Trends / Stale / Reports refinement arc.** **346** reframed the `/patterns` meta line as **LEGISLATIVE PATTERNS** with a new blurb; **347** replaced the `PatternBubbleSVG` bubble cluster with ranked horizontal bars (`PatternBars`, length = bill count, color = % past committee) that also absorbed the old ALL PATTERNS table; **348** added the collapsible **Filler Watch** strip (CEREMONIAL · NON-BINDING) between the meta line and the blurb, every number live-interpolated; **349** cut the redundant TOTAL INTRODUCTIONS line chart from `/trends` (the stacked-by-topic chart already carries that envelope). **350** reframed `/stale` as a **stage-led** surface — default PAST COMMITTEE (`floor`/`other_chamber`/`president`, enacted excluded), sorted legislative-stage-DESC then furthest-action-first, atop the 60-day floor — plus a curated procedural-housekeeping filter (frozen opening-week IDs + an `electing members%` live pattern + a blocklist) with an INCLUDE PROCEDURAL escape hatch and a committee-backlog footer; the ship build's 73s cold query was fixed in `339f8fc` (a CASE `ORDER BY` defeating the date index — oddities). **351** added a **filler-share bar** to the dashboard `WeeklyBand` (broad `is_ceremonial=1 OR cluster IN (four)` definition). **352** added a **stage-movements ladder** by destination stage to the weekly report. **Visualizations 95 → 97** (the `/patterns` bars + filler strip + `/trends` trim), **Foundation 95 → 96** (the `/stale` reframe), **Weekly reports 96 → 97** (the report stage ladder); Home held at 100 (the WeeklyBand filler bar renders on the already-complete home). **Overall holds at ~98** — these are within-theme refinements; Member depth (93) anchors the remainder. No other theme-% change.

**Also (HO 358–361), the Reports section arc + the multi-user landing — arc COMPLETE.** **358** added the weekly report's **Floor votes** section (after the 352 stage ladder): named decisive votes (bold verb · chamber · linkified bare-ID bill or `nominee → position` · margin · contested party split), procedural/amendment votes collapsed, recess states handled. **359** made **`/president` a real in-surface page** (its own `HeaderBar` + feed sub-nav + `StageLegend` + `BillRowList`, active tab marked) instead of the HO 151 `/bills?stage=president` redirect, so CHANGES/PRESIDENT/REPORTS all render in-surface. **360** added the **outcome-verb color rule** to `ReportMarkdown` (exact-text match, render-only — `content_md` stays plain bold). **Weekly reports 97 → 98** for the floor-votes section + color rule (the 352 ladder was already counted last pass — not double-counted; 359 is reports-surface chrome). Separately, **361** shipped **B1 — the `/welcome` split-layout landing** (cookie redirect from `/`, no middleware; live MOVERS/tape/readout reusing `/`'s cached queries; `signIn("github")`), **closing the multi-user arc (A1+A2+B1)**. That auth/landing arc (355/356/361) is **net-new direction, not one of the tracked themes** — no theme bar minted for it. **Overall holds at ~98** — within-theme reports refinements; Member depth (93) still anchors the remainder. No other theme-% change.

**Also (HO 363–365), the B2 tape restyle + the B5 week-strip redesign — dashboard polish.** **363** restyled the v2 two-tape stack (MARKETS/ODDS strip restyle + an inter-instrument pipe divider + even K/P pair spacing). The **B5 arc (364 probe + 365 build)** refreshed the `WeeklyBand`: a **middot-separated** strip with a dated `<MON DD> report →` link (most recent finalized report) + FILLER folded in-run (365 1/2, SHA `5524852`), and a **rich per-metric hover card** (`WeeklyBandMetricCard` — sparkline + breakdowns, replacing the flat HO 284 popover) backed by five new helpers + the canonical `MEETING_TYPE_BUCKETS` 9→3 map (365 2/2, SHA `eda88bc`). Window semantics are **trailing-7d**, not calendar week-to-date (a mock-inferred premise caught in plan mode — oddities). **No theme-% change — Home is already at 100** (these polish the shipped home redesign + the shared `WeeklyBand`/tape, both rendering on `/` and `/dashboard-v2`); the doc-sweep handoff's "bump the home-redesign %" had no headroom to bump. No other theme-% change. *(Flagged for their own pass, not folded here: the HO 364 brand rename — already reflected in SKILL's brand note — and the HO 366/367 `/hearings` + `/members` header consolidations.)*

**Also (HO 373–376), the B2 hover/tape arc — fit-and-finish on the markets tape.** **373** probed `market_ticks` history for a hover sparkline (read-only; settled the roll-stability/monthly-cohort/dead-symbol premises). **374** added a **7d sparkline + 1W delta** to the tape hover (gated ≥2 points / a 7d anchor; the monthly CPI/UNEMP cohort suppressed; odds spark reads the Kalshi series only). **375** fixed the hover-card clipping by switching to the shared `computeTooltipPosition` (measure + four-edge clamp + `preferBelow` to clear the masthead — HO 294's hardcoded 160px/grow-upward was the bug) and added the odds **`closes <MON DD>`** freshness line (the resolution date was already persisted as `market_ticks.market_date`, so render-only — no new store; replaced the redundant "prediction market" clause). **376** shuffles each scrolling strip's order once on mount (independent per strip, hydration-safe) and slowed the marquee ~1.75× (`SCROLL_SPEED_PX_S` 42→24). **No theme-% change — Home is at 100**; all four polish the already-shipped markets tape (renders on `/` and `/dashboard-classic`). A manual prod eyeball is owed (BotID blocks headless hover — backlog OPEN LOOPS). No other theme-% change.

**Also (HO 379, 382–386), the stage-correctness arc + the smoke harness — no theme-% change.** **379** stood up a Playwright route crawler (`e2e/smoke.spec.ts`, prod-default: per-page 200 / no-failed-request / no-console-error + targeted interactions), which caught a prod **filtered-view 500** hidden behind the `ct_seen` gate. **381/382** fixed it — not a logic throw but an unbounded corpus-wide scan on the index-driven filtered stage/topic distributions + the giant-`committee`-bucket feed (date-drive at `FEED_DATE_DRIVE_MIN`=2000). **383** made `computeStage(latest_action_text)` the deterministic **sole authority** for `stage`, written independent of summarization (so a stage advance no longer hides behind the summary-gated home bar), + presentment-queue priority + sync-side transition logging. **384** a one-off corrective stage backfill; **385** extended `computeStage` coverage rules + a 37-case unit suite under the locked floor-rung definition; **386** stripped the dead LLM `stage` output (summarize contract now `{topics, is_ceremonial}`). This is **correctness + test infrastructure, not new theme surface** — **Foundation held at 96** (the block's rubric bumps for shipped theme surface, not bug-fixes/harnesses; "don't invent movement"), **overall holds at ~98**. Residual loops banked/watched (CI-wiring, the ~75-bill `floor→committee` downward correction, the B-sync transition-logging soak, the FEED_DATE_DRIVE margin — backlog). No other theme-% change.

**Also (HO 388–390), the stocks-and-money arc — Member depth surfaced.** **388** probed the stocks/money sources: FMP congressional-trading is **healthy** (HO 70 rebuilt onto `/stable/{senate,house}-latest`, 548 rows, 0 unmatched, bioguide-keyed — NOT dead), while a donor **industry** rollup has no free classified source (banked). **389** shipped the corpus-wide **`/trades`** index — a `stock_trades` recency feed reusing `TradeRow` + a most-traded-tickers rollup, `?member=<bioguide>` scoping to one member, breadcrumb nested under Members. **390** added the **donor small/large-dollar split** (FEC Schedule A `by_size`, off the cached `fec_candidate_id`) beside `MemberFundraisingLine` on the member hub, riding `sync:fec` with **no new cron slot** (shipped `3e57740`, prod-verified, 463/524 populated). **Member depth 93 → 98** — the member surface now carries a dedicated trades index + a donor-composition cut. The **OpenSecrets-style employer/industry rollup stays banked** (no free classified replacement; would need our own classifier). No other theme-% change.

**Also (HO 392–395), the PAC-IE direction feature + the observation layer.** **392–393 (Races surface):** a **`PAC SPENDING`** line on race surfaces showing who a pro-Israel super PAC (UDP `C00799031`, AIPAC's affiliated super PAC; labeled "AIPAC SUPER PAC · via FEC") is **backing / opposing** per seat — **direction only**. Dollars are deliberately NOT rendered in-app (FEC Schedule E has no clean dollar source — the ~5× F24/F3X double-count, banked in oddities `fd8eb7c`); each direction **deep-links to the live FEC IE browser** instead. New `pac_ie_spending` table + manual `sync:pac-ie`. Within-theme Races overlay — **no theme-% change**. **394–395 (News signal):** the **news-as-observations pilot adopted + live** (`4fd9c9d`). `observations` + `observation_entities` are CBT's **entity-resolved news layer**: the news cron dual-writes one entity-resolved `observation` per fetched article alongside the unchanged `news_mentions` write, resolving people/orgs to `bioguide_id`/`system_code`/caucus-slug deterministically (LLM proposes names, code resolves IDs). Gate C closed on a deployed tick (+15 obs, 45.8s, 0 dual-write errors). **News signal was already CLOSED — no % to bump**; the layer's payoff is that **member→news and race→news are now buildable** (the store exists; `observation_entities` joins to `races` via `incumbent_bioguide_id`, EXPLAINs clean) — graduating race→news from "the wall every surface hits" to a queued surface (backlog). Several envelope fields (reliability/credibility/cluster_id/supersedes/schema_version) ride dormant as reserved constants. No other theme-% change.

**Also (HO 402), the member ID crosswalk — enabling infrastructure.** Two additive tables from unitedstates/congress-legislators (`member_ids` scalar external IDs + flattened `member_fec_ids`) now bridge `bioguide_id` → **ICPSR** (Voteview, ~60% native), **wikipedia_title** (pageviews, ~full), **GovTrack/LIS/OpenSecrets/Ballotpedia** titles, and the **authoritative FEC candidate-ID array** (money). One YAML sync (`sync:crosswalk`), run right after `sync:members`, gated on known bioguides so it can never invent a member; one read-only coverage diagnostic. Live coverage: 537/537 current matched (100%), icpsr 59.6%, wikipedia 99.8%, 605 distinct FEC candidate IDs. **ID crosswalk landed — Voteview/pageviews/FEC-money bridges unblocked.** It's **plumbing, not user-facing completion**, so **Member depth 98 → 99** (a single point; the annotation carries the weight — the surfaces that consume these bridges are still to build). Actionable output banked: 3 fuzzy-vs-authoritative `fec_candidate_id` disagreements (Gillen/Ivey/Self) feed a future `sync-fec` switch; `member_social` deferred (no consumer) — both in backlog. No other theme-% change.

**Also (HO 398), race→news on the race hub — within-theme Races add.** `5f26ee9` shipped `getRaceNews(incumbentBioguideId)` reading the **observation layer** (`observation_entities` join on `entity_type='person' AND entity_value=incumbent_bioguide_id` → `observations`), rendered as a NEWS section on `/race/[id]` (`RaceHubBody` + `RaceNewsRow`). This is the **first consumer of the HO 394 observation layer's entity join** — the payoff the layer was built for. **Scope is an honest v1:** race-hub page only (gated `!preview`, not the dashboard drawer), **incumbent-only** (by bioguide, so a challenger surfaces only if already a sitting member), open seats get the empty state, and **no member-hub news surface** (`getMemberNews` doesn't exist). So it **partially** discharges the banked "race→news / member→news surface": race→news (hub, incumbent) is DONE; member→news is still unbuilt (backlog). Within-theme Races overlay powered by the already-CLOSED news layer — **no theme-% change; Races held at 99** (incumbent-only v1 doesn't complete the theme). Live coverage at audit: 15 of the 2026 incumbents have ≥1 resolved person-observation, 70 observations / 65 entity rows total.

**Re-derivation (HO 404), the full audit against live prod state.** Read-only sweep (deploy SHA `7749de5` confirmed live; §1–§3 numbers in the HO 404 handoff). **Foundation held at 96:** plumbing is structurally sound — bills sync fresh (`MAX(update_date)` = 2026-07-02), `bills_fts` fully populated (16,623 == corpus), every forced `INDEXED BY` present, cold-start audit **0 unexpected BAD of 50**, all seven latency-watch routes 200 (warm 0.25–2.5s, `/changes` 0.25–0.76s well under its 8.5s fuse). The two blemishes are **operational cron regressions, not structural gaps**, so they're logged as backlog loops rather than dropping the bar: `/api/cron/summarize` intermittently timing out with the summary backlog **grown 1,011 → 1,374**, and `/api/cron/weekly-report` **erroring on its 06-29 run** leaving reports 2 weeks stale (latest = week-of Jun 15–21). **Member depth 99 → 98:** the member surfaces are comprehensive and live — hub (bio/committees/votes/badges/palestine/trades/donor-split), `/members` roster (552/537), `/trades` index, the HO 402 crosswalk (member_ids 537/537, member_fec_ids 605) — but two real gaps keep it off a near-complete score: **member→news is unbuilt** (HO 398 shipped race→news only) and the **FEC by_size split is 463/530** (the HO 390 rate-limited ~67 never self-healed). Canonical corpus counts settled this pass: "tracked" = `getCorpusStats(true)` = `(is_ceremonial=0 OR IS NULL) AND summary IS NOT NULL` ≈ **15,075**; the old five-number masthead incoherence is resolved (inner-page counts removed HO 323/326; only sunset `/dashboard-classic` still shows the ungated ~16,449). **Overall recomputed from the reconciled bars: ~98% holds** (96/100/97/98/98/99/98). No other theme-% change.

**Also (HO 405–413), the cron-reliability fixes + the senator-integrity arc — correctness, no theme-% change.** The two operational cron regressions the HO 404 anchor logged as backlog loops (not baked into Foundation's bar) both **shipped and cold-verified**, so **Foundation's HO 404 cron fuse is defused — the bar holds at 96** (never docked; the fuse only threatened a future dock if left unfixed): **`/api/cron/summarize`** doom-loop fixed (HO 405 probe / 406 fix, `82fb6bc` — partial `idx_bills_summarize_queue` forced `INDEXED BY` + a C=5 worker pool; one real prod tick 80 summarized/0 fail, the 1,374 backlog drained to 7), and **`/api/cron/weekly-report`** `gatherReportData` misplanners fixed (HO 407, `33cc707` — six `INDEXED BY` hints, the 4th appearance of the stateless-planner class; erroring cron #559 → #673 success 13.5s, stale weeks regenerated + catch-up hardened with a machine-readable `report-catchup-failed:` health field). Separately, the **senator-year integrity arc landed** (HO 408 `d234531` · 410 audit · 411 `db9d910` · 412 `a3a2a18`): the HO 404 anchor's "P1 still live" (`S-OK-2026` → Lankford, plus the `2031` impossible-year smell) **is discharged** — the underlying systemic drift it flagged (18/100 current senators on a wrong-anchor `senateTermStart`+6 derivation) root-fixed by deriving year-pairs from `members.senate_class` (residue-mod-6, `now`-relative), with the S-OK card corrected to appointee Armstrong, two phantom race rows deleted, 17 stale future-cycle cards self-healed by the `backfill:races` upsert, and a standing integrity audit committed as the regression guard. Per the block's rubric this is a **correctness fix, not new theme surface** (line: "bumps for shipped theme surface, not bug-fixes/harnesses") — so **Member depth held at 98 and Races held at 99**; the 17 corrected cards don't complete either theme, and the still-owed member→news + 463/530 FEC by_size gaps are what keep Member depth off a near-complete score. **Also notes now run through HO 413.** No theme-% change.

**Also (HO 414), member→news on the member hub — one of the two Member-depth-98 holds closed.** `3e7d1f1` shipped `getMemberNews(bioguideId)` reading the **same observation layer** as HO 398's race→news (`observation_entities` `person` join → `observations`), keyed on the member's **own** bioguide (no incumbent hop), rendered as a **`News · in the press`** section on `/members/[bioguideId]` (inline server fetch in the existing `Promise.all` + reused `RaceNewsRow`; the member page has no shared body/drawer, so **no `MemberHubBody` split** — the handoff's assumed split was moot). **Second consumer of the HO 394 observation layer** (race→news was the first, HO 398). Cold-EXPLAINed to the 398 covering-index path (`GROUP BY o.obs_id` didn't replan → no `INDEXED BY`); the new `member-news` cache tag is flushed by the news cron beside `race-news` **and** allowlisted on `/api/revalidate` (bogus-tag 400 control confirmed the allowlist entry genuinely took). Prod-verified: `/members/J000299` renders the 4-row section, low-profile members get the empty state. **Member depth held at 98, NOT bumped to 99:** this closes only **one** of the HO 404 audit's two Member-depth holds — member→news is now built, but the **FEC `by_size` split remains** (463/530 at the audit; the ~67 rate-limited members need a **deliberate** `sync:fec` run, not passive self-heal — backlog OPEN LOOP). The 98→99 bump waits until `by_size` is confirmed populated too; don't bump off member→news alone. **Also notes now run through HO 414.** No theme-% change.

**Also (HO 415–416), the FEC member-fundraising completion arc — Member depth 98 → 99.** The HO 404 audit's two Member-depth holds are now **both discharged** (member→news shipped at 414; the FEC `by_size` gap closed here), so **Member depth 98 → 99**. **HO 415 (`c36f0a2`), the by_size cycle-key fix:** `fetchFecBySize` hard-coded `cycle=2026`, but `schedule_a/by_size/by_candidate` keys to the candidate's **election cycle** — so off-cycle senators (Class 1/3, up 2028/2030) returned empty at 2026, which is the entire "463/530 coverage gap" (never rate-limiting, a keying bug). Fixed with `nextElectionYear ?? CYCLE` (HO 411's class-derived, specials-aware column — one source of truth); by_size **464 → 530**, `bysize_empty=0`. This answered the standing **HO 390** question — **there is no genuine by_size floor**; the whole "empty-for-valid-ID" bucket was the bug, 100% recovered. Carried the **rate-hardening** the 60/hr `K0Ue` key forced (cap-55 clean-exit / 1100ms pacing / `skipped_fresh` resume cursor). **HO 416 (`62b7f8e`), the `totals_failed` `candidate_id` sweep:** 2 departed members (Grijalva, Greene) dropped via an `is_current=1` filter on `fetchPendingMembers`; **8 House→Senate 2026 switchers** re-resolved to live Senate ids via a new `data/fec-candidate-overrides.json` (the 2nd resolver-consulted `data/` override after the specials file); Marshall re-resolved through the same override (a stale/live scoring-tie case); Zinke + Barrasso left as named no-action residuals (unfiled / upstream FEC gap). A **"Senate 2026 campaign"** tag on the fundraising line is **computed** from resolved id being S-prefix while `chamber='house'`. by_size **530 → 539**. Per the block's rubric these **complete the audit's two named Member-depth holds** (surface completion, not a bug-fix harness), so the bump is earned; the deferred member-scorecard arc (career vote participation + external scorecards) stays banked as the path to 100 — **not** closed here. **Also notes now run through HO 416.** No other theme-% change.

**Also (HO 418), housekeeping — no theme-% change.** Struck the stale metro-zoom panels (spec-3 Phase 2) entry from the Banked/unbuilt line: it shipped at HO 236/237 (backlog DONE) but the banked entry was never pruned, which had been feeding a stale "next feature = metro-zoom" pointer across session wraps. No feature shipped, no bar moved. **Also notes now run through HO 418.**

**Also (HO 419), the Voteview ideology data layer — enabling data, no bar move.** `da2745d` shipped the **member DW-NOMINATE layer**: a new `member_ideology` table (bioguide PK, `icpsr`, `nominate_dim1/2` + the Nokken-Poole per-congress variant, `number_of_votes`, `conditional`) populated from Voteview's `HS119_members.csv` by a manual **`sync:ideology`** (run after `sync:members` → `sync:crosswalk`, members-gated so it can't invent a member, max-`number_of_votes` dedup for a bioguide's multiple 119th rows, idempotent `ON CONFLICT` upsert), plus a shared `scripts/voteview-source.ts` fetch/parse helper (vendored quote-aware CSV parser — the `bioname` comma trap) and a read-only coverage diagnostic. It bridges **`bioguide_id` → DW-NOMINATE**: `nominate_dim1` is the headline economic left/right axis, `nokken_poole_dim1/2` the per-congress variant. **The join is on Voteview's native `bioguide_id`, not the HO 402 crosswalk's ICPSR bridge** — the "~60% via ICPSR" premise was measuring the wrong join and is retired (oddities). Coverage is essentially complete for current members who've cast a roll call (552 scored / 1 NULL-dim1 / 0 conditional). **No theme-% change — this is enabling data with no user-facing surface, and Member depth is already at 99** (100 stays reserved for the deferred member-scorecard arc). The **first ideology surface** — a member-hub ideology line, an ideology axis on the `SponsorProductivityScatter`, or chamber/party medians — is deferred to a follow-on (backlog QUEUED). It also surfaced a `members.party` data bug (Kiley reads `I`, is a Republican — backlog OPEN LOOP). **Also notes now run through HO 419.**

**Also (HO 421–422), the first ideology surface + the Kiley party finding — no bar move.** **HO 421 (`99de3c1`)** shipped the **first ideology surface** off the HO 419 `member_ideology` layer: a compact **DW-NOMINATE axis on the member hub** (`getMemberIdeology` + `MemberIdeology`), placed with the voting-record stats. It's a fixed **−1..+1** rail (comparable across members, not chamber-scaled) with the member's party-colored marker, both chamber party **medians computed app-side**, `dim1`/`dim2` numerics, and an empty state for the 1 member with too few votes. The axis is **vertically banded** — party medians as ticks above the rail (with a contrasting core), the member's identity as a caret below it, a 2% domain inset for near-edge markers — so a member sitting on their own party median stays legible (the on-median collision). **HO 422** is this doc sweep plus a **correction**: the HO 420 "Kiley party bug" is **NOT a bug** — Kevin Kiley (CA-03) genuinely **switched Republican → Independent on 2026-03-09** (both Congress.gov `partyHistory` and congress-legislators `party_affiliations` confirm), while **caucusing Republican**; `members.party='I'` is correct, and Voteview's `party_code=200` (R) follows **caucus, not registration** (oddities). So no code fix landed — the loop closes as diagnosed, and a small follow-up to teach the ideology diagnostic the caucus-vs-registration distinction is queued (backlog). **Member depth held at 99** (Corey's call): the ideology axis rides as enabling surface; **100 stays reserved for the member-scorecard arc.**

**Also (HO 424–425), the two chamber-ideology surfaces — no bar move.** Two more surfaces off the HO 419 `member_ideology` layer, both live, no new tables. **HO 424 (`127b798`), the dashboard chamber polarization band:** a new `getPolarizationBand()` (all four chamber-party `dim1` medians independent of any member — `getMemberIdeology` is member-chamber-scoped and returns only a pair, so it couldn't be reused) feeds a `PolarizationBand` server component placed full-width between `RacesBoxTabs` and `WeeklyBand` (the battlefield moved inside `RacesBoxTabs`' non-default tab at HO 270/271, so "below the battlefield" resolved to this always-visible slot). It extends the HO 421 positioned-tick rail (**divs + CSS**, not SVG) to chamber-aggregate scale: two chamber rails (HOUSE/SENATE) on a fixed −1..+1 domain (the 2% inset), D and R median ticks, an amber gap line spanning them, the gap magnitude as the hero. **HO 425 (`910f5c4`), the `/members` polarization dotplot strip:** a new `getIdeologyStrip()` (the full scored population, one cache entry) feeds `IdeologyStrip`, a client-island SVG Wilkinson dotplot (`binw 0.02`, party-colored, strict-D/R median ticks pinned to the band's method inline since a client island can't import the server `median()`, empty-center annotation, hover name + dim1 + hub, click-through), ~180px at the top of `/members`, server-scoped to the chamber toggle only. It's the **third live hand-rolled SVG chart** (`BillsTimeSeries`, `CommitteeActivityChart`, `IdeologyStrip`); the over-time chart (below, data-gated) is the fourth, where the shared scatter scaffold gets extracted. **The finding:** both chamber gaps read **0.92** on dim1 (House and Senate) — the "Senate is the calmer chamber" assumption doesn't hold on real data (oddities); cross-checked band-vs-strip, 0 `member_ideology.chamber`-vs-`members.chamber` mismatches across 552 rows, coverage 551 scored / 1 unscored. **Member depth held at 99** — these ride as enabling ideology surface, **100 stays reserved for the member-scorecard arc**; the **over-time chart (ship 3) remains, data-gated** on a per-Congress D/R chamber-medians sync (backlog). **Also notes now run through HO 426.**

**Also (HO 427–428), the polarization-over-time chart — the ideology arc CLOSED.** The third and last cut off the HO 419 `member_ideology` layer, so the three-surface arc (band 424, dotplot 425, over-time 428) is complete. **HO 427 (`e0601dc`)** shipped the data layer: a `polarization_history` table + a manual **`sync:polarization-history`** aggregating Voteview's full `HSall_members.csv` to per-Congress/per-chamber D/R `dim1` medians (caucus, `party_code` 100/200; 190 rows, 95 Congresses/chamber, two-party from the 34th/1855) + a read-only coverage/cross-check diagnostic + a `voteview-source.ts` extension for the full HSall fetch. **HO 428-A (`83f0952`)** extracted the shared SVG scaffold to **`components/svg/`** (`ChartFrame`/`SvgAxisX`/`SvgGridY`/`SvgLegend`) — the standing "extract at the fourth chart" trigger fired — and retrofitted `BillsTimeSeries` + `CommitteeActivityChart` onto it (byte-identical render verified); `IdeologyStrip`, the client-island dotplot, is the deliberate holdout. **HO 428-B (`11f2122`)** shipped `PolarizationOverTime` on `/members` (collapsed by default, above the dotplot strip). Per HO 427's "let the real curve set the narrative": the historical **line runs to the 118th** (caucus) with the **current dot read live from `getPolarizationBand`** (registration) — the two agree to three decimals (0.92/0.92) so the caucus/registration seam is invisible but real (not line-connected across 2023→2025); the **midcentury low is computed per chamber — House 1947, Senate 1943 (gaps 0.50/0.47), not the mockup's shared 1961**; no prose caption. **No theme-% change** — enabling ideology surfaces; **Member depth holds at 99**, 100 reserved for the member-scorecard arc. **Also notes now run through HO 428.**

**Also (HO 430), the ideology-arc tail — correctness/refactor, no theme-% change.** Two small isolated cleanups retiring the last two ideology-arc follow-ups (both had ridden in backlog QUEUED since HO 429), same **correctness-not-new-surface** bucket as the HO 405–413 arc — no bar move. **HO 430a (`6329a0f`), the `median()` extraction:** the four byte-identical inline copies (server `lib/queries`, the `IdeologyStrip` client island, the sync + diagnostic scripts) collapsed into one zero-import leaf module **`lib/median.ts`** — canonical definition, four importers, so nobody re-inlines it; a pure refactor, the 119th history-vs-band cross-check held at 0.925/0.918 delta 0.000. **HO 430b (`d061284`), the ideology diagnostic caucus fix:** `ideology-coverage-419.ts` now applies the **neither-match invariant** (flag only when Voteview's letter matches neither registration nor caucus) instead of a registration-only equality check, suppressing the structural caucus-vs-registration gap while still catching a genuine sync mismatch. **Premise correction:** the backlog claimed King, Sanders, *and* Kiley tripped the diagnostic; the live run showed only Kiley did (King/Sanders code Voteview 328→I and already agree with `members.party='I'`) — the HO 422 caucus-vs-registration distinction confirmed on live data (a new oddities entry records the premise correction — the old "all three flagged" read was wrong). Diagnostic-only; no `members`/`sync` touch (Kiley's `I` is correct, HO 422 NOT A BUG). Docs: HO 431. **Also notes now run through HO 430.**

**Also (HO 433), the cron-cadence pass on Vercel Pro — infrastructure, no theme-% change.** CBT moved Hobby → Pro, retiring the **once/day cron cap** (Hobby rejected any sub-daily schedule at deploy, so the ten hand-staggered 09:00–21:30 UTC slots were a scheduling artifact, not a design). `vercel.json` cadences reset to what each cron actually wants: **`/api/cron/summarize` `0 13`→`*/10`** — continuous drain, which **discharges the HO 404 summarize backlog loop**: summarize ran once/day, so on any day inflow exceeded one 60s drain the queue grew (1,011 → 1,374 at the HO 404 audit); HO 405/406 made a single tick self-heal but daily cadence let it re-grow — at `*/10` the drain is continuous and the loop can't re-form. Also **`/api/cron/news` `0 14`→`*/30`**, **`/api/sync` `0 9`→`0 */6`**, **`/api/cron/committees` `30 11`→`0 */12`**; votes / rating-history / primaries / race-ratings / weekly-report unchanged (correctly daily / weekly / event-driven); **`maxDuration: 60` untouched everywhere** (frequency replaces per-run volume, so no run needs more than 60s). Two logic fixes shipped **ahead of** the cadence flip in history (config must not deploy before them): a **summarize overlap lock** (`lib/cron-lock.ts` — an atomic `dashboard_state` advisory claim, self-healing on a 120s TTL; Vercel Cron is best-effort *at-least-once* and the drain isn't overlap-safe, so two concurrent invocations would double-spend Gemini and double-write the append-only `stage_transitions` log), and **news dedup-before-LLM** (skip the Gemini matcher for any article whose deterministic `obs_id` already exists — `storeObservation` freezes entities on first insert, so re-matching a seen article at a tighter cadence was pure token waste). Dedup **live-verified** on `cbt-csu-j3`: an immediate second news tick dropped `llmCalls` 23→0 with `llm_skipped_seen` 55/55, wall-clock 43s→17s. **Markets + per-seat Kalshi/Polymarket odds deliberately stay on GitHub Actions** (markets-tick 30m, markets-daily 4h, kalshi-tick 2h); the Vercel `30 21` markets floor was a non-firing HO 314 artifact, left as-is. **Migrating those three workflows GitHub → Vercel is flagged as its own follow-up HO** — a *reliability* play (HO 313 measured GitHub dropping ~20% of scheduled slots), **not a cadence change**, needing query-path / FRED-gating / workflow-retirement care to avoid double-writes. **No theme-% change** — cron infrastructure, no user-facing surface. Docs: this handoff renumbered 432 → 433 (HO 432 is the race-news badge/popover). **Also notes now run through HO 433.**

**Also (HO 434–435), the LDA lobbying pillar — data layer shipped, no theme-% change.** The roadmap's "best next pillar" call landed as an additive data layer (source probe 434 → build 435). **Source:** the lda.gov API (the legacy `lda.senate.gov` host is sunsetting ~2026-06/07; built against `lda.gov`). **HO 434** probed it GO on a partial bill-number join; **HO 435** (`39f6e54` data + `72c41ee` cron) built three additive tables — `lda_filings` / `lda_activities` / `lda_activity_bills` — ingesting current-Congress LD-2 quarterly reports via a resumable **DB-frontier** sync (`sync:lda`; resume = `MAX(dt_posted)` per `(filing_year, filing_type)`, no stored cursor) on a **daily-300s** `/api/cron/lda`. **Corpus:** 108,707 filings / 233,499 activities / 173,885 bill-links / 5,165 registrants / 26,920 clients. **Join reality:** 99.3% of parsed IDs resolve, but only **22.8% of activities carry a bill number** and **25.4% of filings have ≥1 joined bill** — so issue-code (BUD/HCR/TAX/DEF top) is the spine, the bill join the overlay. A 40-row residual (0.037%) on the closed 2025 quarters is accepted DRF count-vs-enumerable drift, not a gap (oddities). **No theme-% change** — enabling data with no user-facing surface; the LDA surface is HO 437 (bill-keyed vs issue-code-first + placement, Corey's call). Banked: a Congress.gov title-enrichment sync (to catch named-not-numbered bills — NDAA / appropriations — since `bills` has no `short_title`) and LD-203 contributions for a future money surface. Docs: HO 436. **Also notes now run through HO 435.**

**Also (HO 437), the LDA lobbying surface — issue-code-first, no theme-% change.** The HO 435 data layer's surface (Corey's call: issue-code-first over bill-keyed). Shipped + prod-verified across `2b34058` (query layer + rollup) · `d6c7d2c` (`/lobbying` surface) · `3851b85` (feed OFFSET fix); live SHA `3851b85`. **`/lobbying`** is a new top-level nav item and a **standalone analytical surface** (the `/patterns` class — an aggregate lens, not an entity hub): a stat readout + issue-area **ranked bars** (native LDA `general_issue_code` labels, NOT crosswalked to the 24 CBT topics — lossy) + a **per-issue drill** (top clients / top firms / recent) + a corpus-wide **recent-filings feed**. **The query-layer story is load-bearing:** request-time aggregation is **non-viable** on this Turso tier for these table sizes (whole-table aggregates row-fetch 30–90s+ cold; even a bounded per-code drill >25s — oddities). So stats + issue-bars + the **full per-code drill** are **precomputed** by the daily LDA cron into a `dashboard_state` blob (`lda_lobbying_rollup`) via full-table **sequential** reads + in-memory JS aggregation (~186s, under the 220s cron budget gate), served O(1); only the corpus recent-filings feed is **live** (an `idx_lda_filings_dt_posted` walk clamped to the 1,000 most-recent — `MAX_FEED_PAGES=40` — so a tail-of-table OFFSET can't 500). A **live-per-code drill was tried and abandoned** (>25s cold); a **covering-index detour was tried and fully reverted** (`migrate.ts` net-zero, no prod schema change). **No theme-% change** — an enabling surface on a new pillar. **Banked:** HO 439 (bill-keyed lobbying on `/bill/[id]`, reuses `lda_activity_bills`) + the CBT-topic crosswalk + a corpus-wide top-firms leaderboard + named-not-numbered bill recovery (needs the Congress.gov title-enrichment sync). Docs: HO 438. **Also notes now run through HO 437.**

**Also (HO 439–440), bill-keyed lobbying on `/bill/[id]` — the LDA arc's second fork, no theme-% change.** The other fork of the HO 435 data layer (the HO 437 block banked this as "HO 439"; **439 became the read-only cost probe, 440 the surface** — the probe took its own number, HO 434→435 precedent). **HO 439 (`42ff9c0`), the cost probe:** timed the real per-bill seek+hydrate shape cold against the worst case. **Verdict: live is non-viable for the fat tail** — worst-case bill (`119-hr-1`) **44.5s cold** (4.5× the 10s `getDb` cap), and the cost is entirely the random `lda_filings` row-fetch (seek 461ms, aggregate 30ms — the HO 340/437 shape confirmed one more time). Fan-out is a **fat tail with an extreme outlier**: 9,179 bills linked, p50 6 / p90 27 / p99 159 / max 8,048. Diagnostic committed as `scripts/diagnostic/lda-bill-cost-439.ts` (sibling to `lda-coverage-435.ts`). **HO 440 (`9a540b2` surface · `ab1505a` data+cron · `7b8f0d5` refactor), the hybrid surface:** **precompute the ≥150-filing tail, live-serve the rest.** `computeLdaRollup` refactored to share its sequential three-table read (`readLdaTables` → `computeIssueRollup` [issue rollup, **byte-identical to prod** verified] + `computeBillDrill`), so the per-bill drill piggybacks the pass already running — near-free. Qualifying bills (**96 at ≥150**, 559 KB) written to a **second `dashboard_state` blob** (`lda_bill_drill`) by the same budget-gated cron step; `getBillLobbying(billId)` reads it O(1), **falls through to a live `getDb` drill** for below-threshold/zero-link bills (worst below-threshold bill live 2.3s, 7.7s margin), **try/catch → null** so a freshly-heavy bill can never 500 the page. A LOBBYING section on `/bill/[id]` (after Hearings, before Summary) reuses `FilingRow` + the extracted `LobbyingMiniBars`; zero-link bills omit it. Ranks top firms/clients by **distinct filings** (consistent with the `/lobbying` per-code drill). Prod-verified across all three `getBillLobbying` branches at `42ff9c0` (blob `119-hr-1`, live `119-hr-2794`, zero-link `119-hres-1420` — 200, section absent). **Still banked:** the CBT-topic crosswalk + the corpus-wide top-firms leaderboard (both carried from HO 437; untouched here). **No theme-% change** — a secondary signal on an existing pillar. Docs: HO 441. **Also notes now run through HO 440.**

**Also (HO 442), the corpus-wide top-firms leaderboard — the LDA arc's last banked surface, no theme-% change.** The third and final item banked in the HO 437 block (bill-keyed → HO 439–440; the CBT-topic crosswalk stays banked; this closes top-firms). Shipped + prod-verified across `b64e1c7` (data + cron/CLI) · `69644fd` (surface); live SHA `69644fd`. **No cost probe** — unlike every prior `lda_*` surface (434→435, 439→440), this introduced no new cold query: `computeTopFirms` is a **third consumer of the shared `readLdaTables` pass** (beside `computeIssueRollup` + `computeBillDrill`), one in-memory grouping over the already-loaded `filings` map, so the build/probe split didn't apply — it rode the ~96s pass already running. Written to a **third `dashboard_state` blob** (`lda_top_firms`) in the same budget-gated cron step under the single `revalidateTag("lda")`; the CLI (`npm run lda:rollup`) mirrors it, so local regen writes all three blobs. **Definition:** ranks **registrants** (the lobbying shops — consistent with `topFirms` everywhere else) by **distinct filings** (the HO 435 house rule), name-tiebroken; per firm carries distinct clients, bill-linked filing count, and their single most-cited `general_issue_code`. **Top 25** (one constant `TOP_FIRMS_N`, store == render). **Surface:** a **TOP FIRMS section on `/lobbying`** (new Section 3, between the issue-drill two-column and the recent-filings feed) served O(1) from `getTopFirms()` (null before the first rollup → section cleanly omitted); the separate blob key keeps the 286KB `/lobbying` rollup read lean (the HO 440 rationale). Mobile: the Top-issue + Bill-linked columns collapse below ~640px (Tailwind, no `globals.css` coupling). **Two deliberate omissions, recorded so they aren't re-litigated:** (a) **no dollar ranking** — LD-2 income/expenses are partial + income-vs-expense asymmetric across registrant types, so a summed-dollar leaderboard would mislead; dollars are the banked **LD-203** money surface; (b) **native issue-code labels, NOT crosswalked to the 24 CBT topics** — that crosswalk is the other still-banked item. **The finding:** rank-1 is **Cornerstone Government Affairs**, #2 Brownstein Hyatt — a filing-**volume** ranking is deliberately distinct from a revenue ranking (Cornerstone runs a huge book of small clients → files a mountain of LD-2s without top-grossing), which is exactly the signal this cut adds over the dollar rankings. Invariants at rollup: **25 firms of 5,175 registrants**, 0 `billLinked > filings`, 0 null `topIssueCode`; the Section-1 readout `stats.registrants` and the leaderboard `totalRegistrants` both render **5,175** (structurally guaranteed — same `readLdaTables` filings map — so no masthead incoherence). **No migration, no new cron slot, no new nav item.** **No theme-% change** — an enabling/aggregate surface on the LDA pillar (the `/patterns` class), matching HO 437/439–440. Docs: HO 443. **Also notes now run through HO 442.**

**Also (HO 444), the CBT-topic crosswalk on `/lobbying` — the LDA arc's last banked surface, no theme-% change.** The final item banked in the HO 437 block (bill-keyed → 439–440; top-firms → 442; this closes the crosswalk), so the **HO 437 four-item fork is now fully discharged — nothing from it stays banked.** Shipped + prod-verified across `8f7d7bd` (data) · `549d81e` (surface); live SHA `549d81e`. **Two calls shaped it. Build, not probe:** unlike every prior `lda_*` surface (434→435, 439→440), it introduced no new cold query — `computeTopicCrosswalk` is a **fourth consumer of the shared `readLdaTables` pass** (beside `computeIssueRollup`/`computeBillDrill`/`computeTopFirms`), one in-memory re-bucketing of the already-loaded `uuidsByCode` sets, so the build/probe split didn't apply (the HO 442 precedent). **`/lobbying` section, not a topic hub:** there is no topic entity route (topics are a filter/facet only), so cross-linking lobbying onto a topic page has no home — the self-contained surface is a **BY-CBT-TOPIC section on `/lobbying`** (new Section 3, between the issue two-column and TOP FIRMS; TOP FIRMS → 4, feed → 5), which does **not** reopen the HO 437/442 native-labels decision (that was about not *replacing* the native `general_issue_code` bars; this sits alongside them, disclosing the lossiness by keeping both, and its payoff is expressing lobbying in the same 24-topic vocabulary as the dashboard's bill distribution). **The map** (`lib/lda-issue-topic-map.ts`, a typed `Record<string, Topic>` — not `enums.ts`, not JSON, so the per-code judgment comments type-check): **79 LDA codes → 23 live CBT topics**; `elections` legitimately empty (no lobbying issue code maps there — an honest gap, not forced); **`other` = 21** industry/profession/contested codes (comprehension-over-coverage, the `TOPIC_ALIASES` posture). Two contested calls resolved on review: **CPT (Copyright/Patent) → `technology`** and **FAM (Family/Abortion/Adoption) → `healthcare`** (the closest single home for a single-valued map; the abuse-vs-industry asymmetry — ALC→healthcare as the *abuse* code, TOB→other as the *industry* code — is deliberate, see oddities). **The multi-code property:** a filing carries multiple issue codes, so it lands in multiple topic buckets — topic-filings sum **213,780 > corpus 110,055**, NOT a partition (the same property the native issue bars have; the section header discloses it, never "share of filings"). Invariants: 23 topics, 0 unmapped (structural — `?? "other"` catch-all), the HO 442 `5,175`-registrant readout holds (same `readLdaTables` map). **No cost probe, no migration, no new cron slot, no new nav item** — the fourth blob (`lda_topic_crosswalk`) rides the same budget-gated cron step under the one `revalidateTag("lda")`. **No theme-% change** — an enabling/aggregate surface on the LDA pillar (the `/patterns` class), matching HO 437/439–440/442. Docs: HO 445. **Also notes now run through HO 444.**

**Also (HO 446–448), the amendments pillar — probe → data layer → first surface, no theme-% change.** The roadmap's "cheapest core-deepening move" call (amendments over nominations: dual-attaches to the bill spine via `amendedBill` and the member spine via `sponsors`, reuses the Congress.gov sync wholesale, fills the floor-stage visibility gap; nominations sequenced later on a weaker join). Three HOs, probe-first as ever. **HO 446 (`508656e`), the source probe — GO:** 6,788 amendments in the 119th (SAMDT 6,560 / HAMDT 228 / SUAMDT 0), `amendedBill` present and resolving 100% to tracked bills, rich `latestAction` disposition vocab. The structural finding that killed a speculative table: sub-amendments carry BOTH `amendedBill` AND `amendedAmendment`, and the API resolves the ultimate bill transitively — so lineage is a nullable **self-FK**, not an `amendment_bills` link table, and `amended_bill_id` is a plain resolved column. **HO 447 (`667cc33` data + `cecff19`/`c5575b2`/`08394e6` fixes), the data layer:** an additive `amendments` table (house-style `amendment_type`/`amendment_number`, **loose links not enforced FKs** — `cecff19`, correct for self-referential bulk-load ordering), a resumable DB-frontier sync (`sync:amendments`, `MAX(update_date)` ascending sweep, list→detail hydration, deadline-gated `/api/cron/amendments` at 07:00 UTC, unbounded CLI backfill) mirroring `sync:lda`, and a read-only coverage diagnostic. Two resume corrections landed in-arc: **resume on the list item's `updateDate`, not the detail's** (`c5575b2`), and a **`--repair` mode** (`08394e6`) that enumerates live-vs-stored and fills the handful of holes the unstable-`updateDate`-order pagination skips (the ascending frontier can't refill an interior gap — oddities). Backfill ran whole: `--repair` exits `complete` at 6,788 == live `pagination.count`. **HO 448 (`13670f2`), the first surface — a bill-hub AMENDMENTS section on `/bill/[id]`:** the per-bill floor fight, placed **between Hearings and Lobbying** (committee → floor → outside-influence order). **No cost probe, no precompute blob** — unlike every `lda_*` surface, and deliberately so: amendments is **bills-scale** (~6,800 rows total, smaller than `119-hr-1`'s LDA filing set alone at 8,048), so a per-bill seek behind `idx_amendments_bill` hydrates instantly even for the worst magnet; `getBillAmendments` is a plain live `unstable_cache` query (tag `amendments`, try/catch → null so a bad read hides the section instead of 500-ing the page), no `dashboard_state` involvement. Each row: label, party-colored sponsor (members join, plain-text fallback for committee/manager amendments with no bioguide), purpose, a **display-only disposition dot** (green/red/neutral keyword-scanned from top-level `latest_action_text` via the `--vote-yea`/`--vote-nay` palette — NOT the deferred status model), sub-amendment `↳ amends` note; ordered by `COALESCE(latest_action_date, submitted_date) DESC`, capped at 60 + "N more" for magnets. **The finding:** `latest_action_text` is present on only **8.6%** of amendments (447's "~35%" was the probe's stale frontier read — oddities), because a couple of budget vote-a-rama magnets dominate the filed-not-acted corpus — `119-sconres-7` alone carries **1,131 amendments (17% of all 6,788)**, only 24 with a top-level disposition. That 8.6% is the concrete argument for the deferred actions-walk status model (the vote-a-rama outcomes live in the per-amendment `actions` sub-resource the sync skips). Prod-verified across magnet (`119-sconres-7`, 1,131 / capped 60), small (`119-sjres-98`, 8 full), zero (`119-hconres-1`, section absent), and sub-amendment lineage (`119-s-1318`, `↳ amends SAMDT 5814/5816/5817`). **No theme-% change** — the floor-stage gap is now visible on the bill hub, but the outcome-classification (status model) and the cross-bill aggregate lens (`/amendments`) that a bump would ride are both deferred, so this is a v1 surface on a new pillar, matching the LDA-arc precedent (437 / 439–440 / 442 / 444 all held). **Banked (QUEUED):** the standalone `/amendments` aggregate surface, a member-hub sponsored-amendments section, and the persisted status/disposition model. Docs: HO 449. **Also notes now run through HO 448.**

**Also (HO 450), the member-hub sponsored-amendments section — the second amendments fork, no theme-% change.** One of the three forks banked in the HO 446–448 block (bill-hub → 448; this ships the member-hub; the standalone `/amendments` aggregate + the persisted status model stay QUEUED). Shipped + prod-verified; live SHA `ea686a8`. **The inverse projection of the HO 448 bill-hub section:** `getMemberAmendments(bioguideId)` seeks `idx_amendments_sponsor` (where 448 seeks `idx_amendments_bill`) and keys each row on the **amended bill** — linked to `/bill/[id]` via `formatBillId`, with the bill title — rather than the sponsor, since the sponsor is fixed (it's their page). A new `MemberAmendmentRow` (a bare row the page maps — the member-page idiom, not self-boxing like the bill page's `BillAmendments`); the disposition dot + module-level `deriveDisposition` are reused, no new palette. **No probe, no blob, no cron/migration** — same bills-scale reasoning as 448: a per-member seek hydrates instantly, and the heaviest amender (Bennet `B001267` at **271** amendments) served live, capped at 60 + "N more". Placed as the **authoring pair** — immediately after Sponsored bills, before Committees (bills + amendments = the two forms of member-authored legislative text) — and hidden when empty (most House members sponsor none), the `{scorecard ? …}` conditional-section idiom. Prod-verified across heavy/capped (Bennet, 271 → 60 + 211 more), small/full (Moreno `M001242`, 6), zero/absent (Aderholt `A000055`, section omitted), and dots + sub-amendment lineage (Paul `P000603`, 1 yea / 7 nay / 35 muted, `↳ amends`). **No theme-% change** — the second v1 surface on the amendments pillar; the outcome-classification (status model) + the cross-bill aggregate lens (`/amendments`) that a bump would ride both stay deferred, matching 448 and the LDA-arc precedent. Docs: HO 451. **Also notes now run through HO 450.**

**Also (HO 452), the actions-walk disposition probe — NO-GO, the status-model fork retired, no theme-% change.** The third fork banked in the HO 446–448 block (bill-hub → 448; member-hub → 450; this probes and **closes** the persisted status/disposition model). Probe committed `67ed564`, read-only, no build followed. **The question:** the display-only disposition dot (HO 448/450) colors only the **8.6%** of amendments with a top-level `latest_action_text`; the persisted-enum model would walk each amendment's `/actions` sub-resource to recover dispositions for the silent 91% — but that ~doubles the backfill (~6,800 → ~13,600 requests, ~85 → ~195 min), so it's only worth it if `/actions` actually carries those dispositions. **The finding: it doesn't — 0% recovery.** A stratified 155-amendment walk (`scripts/diagnostic/amendments-actions-probe-452.ts`, 0 errors / 0 throttles) found that no-top-level-action == **genuinely filed-never-acted**: both the general silent-91% stratum and the decisive `119-sconres-7` vote-a-rama stratum returned **100% submit/propose-only** `/actions` lists (0% terminal disposition), with an **empty residual** (the starter classifier left zero terminal actions unmatched — the 0% is a real absence, not a classifier miss). A vote-a-rama files ~1,131 amendments and votes on ~24; those ~24 already carry a top-level action, and the rest are never called up, so there's nothing in `/actions` to recover. **The top-level scan is the disposition ceiling** — a persisted enum would only persist what render already computes, for a doubled backfill and no coverage gain (oddities). So the status model closes NO-GO; the display-only dot stays as the complete-for-what's-available surface, not a stopgap. **One capability banked, orthogonal to dispositions:** `/actions` carries **`recordedVotes`** ({chamber, rollNumber, url, date}) — a clean amendment → roll-call-vote link (backlog QUEUED), the source for a future "how did each member vote on this amendment" surface, scoped on its own merits (it'd still pay the `/actions` walk cost, but for vote links, not dispositions). Payload also confirmed structured (`type` / 13 distinct `actionCode`s), though the text is already unambiguous. **No theme-% change** — a probe that retired a fork; no surface. This leaves the amendments pillar at **two live surfaces** (bill-hub, member-hub) with **one QUEUED fork** remaining (the `/amendments` aggregate) plus the new vote-linkage lead. Docs: HO 453. **Also notes now run through HO 452.**

**Also (HO 454–456), the nominations pillar — probe → data layer → surface, GO where amendments' status model was NO-GO, no theme-% change.** The other half of the amendments-vs-nominations fork (picked after amendments closed at 453). Nominations have **no bill spine and no member-sponsor** (the president nominates, the Senate confirms) — the "weaker join" the fork flagged, probe-confirmed: nominees carry no bioguide, so the pillar is standalone. Three HOs, probe-first. **HO 454 (`b27a1d2`), the source probe — GO:** 1,884 PNs in the 119th; both decision cuts came back favorable. **Civilian volume is real** — 833 PNs (judges, cabinet, ambassadors, US Attorneys), 44% by PN count, not drowned by the military bulk (1,051 PNs but ~25k nominees at ~24.5/PN; military dominates the *nominee* axis ~30:1, so it must be **collapsed** — one row per list PN, never exploded). **The disposition contrast — the reason nominations clears where amendments didn't:** `latest_action_text` is **100%** present (vs amendments' 8.6%), clean deterministic vocab along the confirmation pipeline (Received → Referred → Hearings → Reported → Calendar → Confirmed / Returned / Withdrawn) — a **persisted** status model is viable (oddities: the amendments NO-GO does NOT generalize). Two catches a naive build misses: the **`(pn_number, part_number)` composite key** (citations are `PN{n}-{part}`; the base-part detail is a *different* record) and the list-vs-detail `updateDate` divergence (same frontier hazard as amendments). Organization is a clean 84-value agency facet; `recordedVotes` absent (the roll-call is in the action text); committee referral present but the civilian rate unmeasured (a confound — deferred re-measure). **HO 455 (`ebadee3` data + `e175fd7`/`f15608b` gate fixes), the data layer:** an additive **list-only** `nominations` table (every core-surface field is list-level, so no detail fetch — which sidesteps the part-specific-detail wrinkle), keyed on the composite, military collapsed to single rows, with a **persisted `disposition`** computed at sync by `computeNominationDisposition` (the differentiator); `committee_system_code`/`nominee_count` present-but-NULL (deferred, forward-compatible). A resumable frontier sync (`sync:nominations`, `MAX(update_date)` ascending, inclusive boundary, deadline-gated `/api/cron/nominations` at 09:00) mirroring `sync:amendments` but cheaper (list-only, ~34s backfill). **The completion gate got a real fix in-arc:** nominations exposed a latent bug the amendments corpus never hit — `--repair` gated on `stored >= pagination.count`, but `pagination.count` **overcounts** the distinct-enumerable set (5 byte-identical duplicate list entries in the feed, 0.27% — the LDA-40 drift), so a correctly-deduping store can never reach it; the gate now keys on **full-enumeration `missing.length === 0`** (`e175fd7`), the diagnostic's landed-clean line likewise (`f15608b`). Corpus: **1,879 distinct**, provably whole. **HO 456 (`754cf0e` disposition tune + `febaaec` surface), the surface — standalone `/nominations`:** civilian-focused (830), leading with the **disposition strip** (60.5% confirmed — the status-model headline) + the 84-agency facet (State/Justice/The Judiciary/…), a filtered list with chips + pagination. **Live aggregation, no rollup blob** — 1,879 rows (smaller than bills), so `getNominationsSummary`/`getNominations` `GROUP BY` live (the bills-scale call, not the `lda_*` precompute one); no `MAX_FEED_PAGES` clamp at 830 rows. Military excluded and **disclosed** in the count line (the LDA-style honesty). Nav item `◈ Nominations` (standalone key, no sub-nav, mirrors Lobbying). The disposition tune (`754cf0e`) fixed the one residual — a reconsider-tabled action reading `received` is a finalized **confirmation** — and surfaced another gotcha (oddities): **`--backfill` resets offset, not frontier**, so a persisted derived column can't be recomputed by re-running the sync on a populated DB (it only re-touches the `MAX(update_date)` delta); the corpus-wide recompute needed **truncate + re-backfill** (safe here only because the table is list-only and API-reproducible). Prod-verified across the disposition strip, both facets filtering, deep pages (no tail 500), and the disclosure line. **No theme-% change** — a new-pillar v1 surface, matching the LDA/amendments precedent; the deferred committee cross-link + detail hydration are what a bump would ride. **Banked:** the `/committees/[id]` nominations cross-link (+ part-specific detail + civilian referral re-measure) in QUEUED; the military view + `/nomination/[id]` detail in BANKED. Docs: HO 457. **Also notes now run through HO 456.**

**Also (HO 458–459), the nominations committee cross-link — the deferred detail-only join, shipped, no theme-% change.** The one fork the HO 454–456 pillar deferred (list-only `/nominations` shipped; `committee_system_code` sat NULL because committee referral is detail-only). Probe-first as ever. **HO 458 (`a0cab49`), the detail probe — GO:** the cross-link rides a cold query never measured at civilian scale on the *correct* record, and the HO 454 civilian referral number was **confounded** — it read the base `/nomination/{c}/{n}`, which for a multi-part PN is a **stub** (`committees` absent). The probe settled the shape: **multi-part is load-bearing** (786/830 = 94.7% of civilian rows live inside multi-part PNs, not a tail); each part-row's stored `raw_json.url` resolves to a **distinct record with its own per-part committee** (PN730-1 African Development Bank vs -2 Federal Labor Relations Authority); `committees` is a sub-resource `{count, url}` everywhere (a 2nd fetch when `count > 0`). Re-measured civilian-only on the right record: **98% referral presence / 100% resolution to tracked `committees` / 0 unresolved codes** (no `sync:committees` gap), effectively 1:1 (a 6/150 dual-referral tail). Cost ~1.98 fetches/row → ~277–500s for 830 rows, over the 300s route → CLI backfill + a bounded cron delta. **HO 459 (`b783d39` data + `fd5e256` surface), the build:** hydrate `committee_system_code` per civilian part-row by following the stored `url` (2 fetches when referred, storing the **first**/primary committee — the dual-referral secondary dropped, disclosed, no link table). A **`committee_hydrated_at` sentinel + partial index** (`idx_nominations_hydrate ON nominations(pn_number) WHERE is_military = 0 AND committee_hydrated_at IS NULL`, the HO 406 summarize-queue pattern) is the two-phase-populate key: every processed row is stamped (incl. the ~2% with no committee) so it exits the queue and incremental re-runs only touch the NULL delta. A `--hydrate` CLI mode does the one-time 830-row populate; a **bounded cron step** (sync deadline 45s → hydrate 55s, cap 40/tick) trickle-hydrates the delta after. **The list sync needs no change** — `buildNominationStatement`'s `ON CONFLICT DO UPDATE SET` already omits `committee_system_code`, so a bulk-refresh re-upsert preserves the hydration stamp (adding the sentinel to the SET clause would re-null every hydrated row — oddities). Initial populate: **830 processed / 817 withCommittee / 13 no-committee / 0 unresolved / 22 dual-dropped / remaining 0** (one transient 15s fetch-timeout on `PN379-07` left `remaining=1`; a resumable 2nd `--hydrate` pass drained just that row — the sentinel-queue working as designed). **Surface:** `getCommitteeNominations(systemCode)` + a `?committee=` filter on `getNominations` (bidirectional), and a **full-width "Nominations referred" section on `/committee/[systemCode]`** (after Hearings; `NominationRow` reused, disposition-badged, header `{total} referred · {confirmed} confirmed`) — **omitted when total is 0**, so House/joint committees (which confirm nothing) show no section, no "0 referred" noise; a "see all → `/nominations?committee=…`" foot when over the 25-cap. Prod-verified: `/committee/ssfr00` (Foreign Relations, 201 referred / 100 confirmed), `ssju00` (Judiciary, 187 / 118 — the filtered `?committee=ssju00` list count matches exactly, join correct), `hsju00` (House Judiciary, section omitted). **Scope held:** `nominee_count` stays deferred/NULL (military-only, no consumer — folded into the banked military-view item), primary-only referral (no `nomination_committees` table), no `/nomination/[id]`, no new nav/cron. **No theme-% change** — a cross-link deepening an existing pillar (the deferred join it was waiting on is now built, but it's a secondary lens, not a new theme), matching the LDA/amendments precedent. The nominations pillar now sits at **two surfaces + a bidirectional committee link**. Docs: HO 460. **Also notes now run through HO 459.**

**Also (HO 461), the `/amendments` aggregate — the amendments pillar feature-complete, no theme-% change.** The last QUEUED amendments fork (bill-hub → 448; member-hub → 450; the actions-walk status model retired NO-GO → 452; this ships the standalone aggregate), so the **HO 446–448 three-fork pillar is now fully discharged** — nothing from it stays banked. Shipped + prod-verified across `6050b41` (query layer + cost gate + index) · `e96f0ab` (surface + nav); live SHA `e96f0ab`. **`/amendments`** is a new top-level nav item and a **standalone analytical surface** (the `/lobbying` + `/nominations` class — an aggregate lens, not an entity hub): a status hero + most-amended-bills + top-amenders ranked cuts + a filtered feed. **Build, not probe, but with an inline cold-cost gate** — unlike the prior amendments surfaces this carried one genuinely unmeasured cut: `disposition` is **display-only-derived** (HO 452 killed the persisted column), so the corpus disposition breakdown SCANs `latest_action_text` (unindexed), the one non-covered query where the two ranked cuts ride `idx_amendments_bill`/`idx_amendments_sponsor`. So a Step-0 gate (`scripts/diagnostic/amendments-aggregate-cost-461.ts`) measured the three unbounded cuts cold **before** committing live-vs-blob: the disposition scan was **4065ms** (SCAN + `raw_json` bloat), the two GROUP BYs **90ms/138ms** (already index-only, never touched the table). The fix cleared it: a **partial covering index `idx_amendments_acted`** (`ON amendments(latest_action_text) WHERE latest_action_text IS NOT NULL`) took the disposition scan **4065ms → 40ms** (SEARCH USING COVERING INDEX), a 100× cut — so **live confirmed, no rollup blob, bills-scale-live like `/nominations`.** The index is a pure performance fix (the HO 277/340 covering-aggregate class), **NOT** a status model — HO 452 stays closed (it persists nothing new; it just makes the already-computed display-only scan index-only). **The query layer:** `getAmendmentsSummary()` composes `total` / `acted` / `agreed`·`failed`·`otherActed` (the covered disposition scan mapped through the module-level `deriveDisposition`) / `filedOnly` = total − acted, `byChamber` (SAMDT+SUAMDT→senate, HAMDT→house), and `topBills`/`topSponsors` (top-15 aggregate + `IN`-join hydration, re-sorted by count); `getAmendments({type?, disposition?, bill?, page})` is the bounded paginated feed (coarse `acted`/`filed` disposition filter — fine-grained agreed/failed stays on the per-row dot, since the JS `deriveDisposition` keyword logic would drift a SQL LIKE mirror). Both live `unstable_cache`, tag `amendments`, `try/catch → null`. **The surface:** a 5-section `/amendments` (header/blurb → status hero bar → most-amended bills → top amenders → filtered feed with chips + pagination), a bare `AmendmentRow` carrying **both** the bill link and the sponsor (the corpus feed fixes neither), and a `Δ Amendments` nav entry (standalone key, mirrors `$ Lobbying` / `◈ Nominations`). **The finding — the "floor almost never votes" headline:** 6,788 filed · **586 acted (8.6%)** · 6,202 awaiting floor action; **6,560 Senate / 228 House** (the House barely amends on the floor; the Senate does via vote-a-rama); `119-sconres-7` alone = **1,131 (17%)** at the top of most-amended; Bennet (`B001267`) = **271** the top amender — so the status bar's muted `filedOnly` segment dominates, which is the point. Prod-verified across the shape: the hero readout matches the data to the digit, `?bill=119-sconres-7` → 1,131 + pill, `?disposition=acted` → 586, `?type=HAMDT` → 228 / `?type=SAMDT` → 6,560, deep `?page=20` + combined filters HTTP 200 (bounded feed, no tail 500), nav tab lit. **No theme-% change** — a v1 aggregate lens on an existing pillar, matching 448/450 and the LDA-arc precedent (437/439–440/442/444 all held). **Banked (QUEUED):** the fine-grained disposition list filter, the `dispositionColor`/`deriveDisposition` extraction to a shared module (deferred to keep the feature commit off two working components — the two-commit refactor-separate rule), and the HO 452 `recordedVotes` amendment→roll-call-vote lead (orthogonal, still QUEUED). Docs: HO 462. **Also notes now run through HO 461.**

**Also (HO 463), the `/lobbying` topic-crosswalk click-through — the HO 444 crosswalk made interactive, no theme-% change.** The BY-CBT-TOPIC bars (HO 444) were non-interactive in v1; HO 463 (`2bdbe15`) turns each into a `<button>` that expands inline to its constituent LDA issue codes, each chip a `<Link>` into the **existing** Section 2 `?issue=` drill — scrolling it into view via a plain `#lobby-drill` hash (Next 15.5 honors it on soft-nav; no `scroll={false}`/`onClick` workaround needed). **No chip can land on a blank:** `drill[code]` is built uncapped over every corpus code (`computeIssueRollup` loops all of `uuidsByCode`), so every constituent code has a real drill. Page-side grouping of `rollup.issues` keys on the **same `topicForCode`** `computeTopicCrosswalk` uses, so the chips align exactly with the bar totals — no double-count seam. `TopicCrosswalk` became a `"use client"` island (instant expand; client state persists across the `?issue=` soft-nav, so the open topic survives a chip click). **No new data layer / blob / query / cron / migration / nav** — a UI pass over data already in the page. **No theme-% change** — a v2 interaction refinement on the LDA `/lobbying` surface (the `/patterns` class), matching the LDA-arc precedent. Docs: HO 464. **Also notes now run through HO 463.**

**Also (HO 466–468), the chip-family existing-chip migration — the HO 287 deferral closed, no theme-% change.** The normalization HO 287 scope-guarded out (its additive half shipped the `--kalshi`/`--poly` tokens + `SourceTag`/`MicroTag`; the existing id/stage/party chips were "used in many divergent ways" and split to this arc; topic already unified at HO 316). Audit-first as HO 287 mandated: **HO 465** (read-only, no commit) catalogued every ID/stage/party rendering + consumer and framed three decisions; the builds landed one commit per leg. **HO 466 (`2706d23`), the ID leg — Decision 1: keep chamber-tint.** `BillIdRail` (vertical) + `.bill-chip` (horizontal) stay chamber-colored (`--rail-house`/`--rail-senate`) per the HO 125 rationale (chamber-of-origin is a *structural* marker, not partisan — amberizing it would erase the at-a-glance chamber cue across ~11 BillRow surfaces + TOP STALLS). `.v2f-id` already matched the HO 287 tier-1 spec exactly (bordered `--accent-amber`, 11px/600, 2px radius), so it became the source of a shared **`BillIdChip` + `.bill-id-chip`** primitive that absorbs only the **non-chamber** id surfaces: the dashboard feed (`.v2f-id`), weekly band (`.weekly-band-id` green→amber), and hearings list + calendar (`.hearing-bill-line .bill-id`, `.hcal-card-billid`) — id text unified through `formatBillId`. **Two documented exceptions left as amber text** (the "compact multi-id list ≠ single row-anchor" line): `SponsorExpandedPanel`'s 14px card bill-list (an 11px chip would misalign the baseline row) and `.hearing-chip` (the compact "·N more" multi-id glance strip). **One behavior change:** the feed id was a plain `<span>` inside the HO 319 click-to-expand row (clicking it expanded the row); as a `<Link>` it now deep-links to `/bill/[id]` (with `stopPropagation` so the row still expands from anywhere else) — consistent with the id linking on every other surface. **HO 467 (`03efc6e`), the stage leg — Decision 2: `StageIndicator` stays exempt.** `StagePillStrip`/`.stage-pill` → tier-2 spec: **10px** (was 11), active (current) pill border = its stage color at **50% alpha** (`color-mix(in srgb, currentColor 50%, transparent)`), history pill → explicit **`--text-dim` text + `--border-strong` border** (dropped the old `opacity: 0.6` dimmed-stage-color); one shared-component edit flows to every BillRow surface. `StageIndicator` (hearings + `/bill/[id]`) untouched — its `▸/▸▸/▸▸▸▸` arrow-prefix encodes stage progression at a glance, which a flat pill loses. **HO 468 (`9327517`), the party leg — Decision 3: tint stays ON (pure refactor, no visible change).** Party color is the feed's core at-a-glance signal, so the spec's "tint off by default" was declined; the leg is a source-of-truth consolidation only. The ~8-way `partyColor` fork collapsed to **one canonical helper** in `lib/race-colors.ts` (widened to `string` + **inline**-normalized rather than importing `normalizePartyVariant`, which lives in `queries.ts` behind `next/cache` — the HO 425/430 client-leaf boundary, and `race-colors.ts` is imported by client race components); the feed (`.v2f-subline-bracket`) + `/members` (`.mc-brk`) inline brackets route through `PartyTag` (gained a `className` passthrough), `SponsorExpandedPanel`/`SponsorHoverName`/`MemberHeader` color-converged (markup untouched — `MemberHeader`'s dash-joined identity meta line is not the `[P-ST]` idiom, exempt). **The null-case lesson (Corey caught it pre-commit):** widening the helper's domain risked collapsing `null → I` (independent purple); the fix keeps two categories distinct — **`I`/`ID` → independent purple, `null`/any-unrecognized-code → `--text-dim` gray** (verified `partyColor(null) === partyColor("L") === --text-dim`; the sibling purple path verified live on `[I-CA]`/`[I-VT]`). No visible change: `[D-CT]` renders byte-identical everywhere; the null `[?-ST]` gray path is function-proven (no null-party row exists in the corpus to render live). Accepted micro-edge: `SponsorHoverName`'s null fallback shifts `--text-muted → --text-dim` (a ~never-fired path). **The B6 chip-family arc is now complete** — topic (316) + ID (466) + stage (467) + party (468) — closing the HO 287 deferral; nothing from it stays QUEUED. **No theme-% change** — a visual-normalization arc over existing surfaces, no new data/blob/query/cron/nav. Docs: HO 469. **Also notes now run through HO 468.**

**Also (HO 470 / 432), race→news on the dashboard — the NEW badge lit, no theme-% change.** The HO 272 RACES-tab `NEW` badge (wired but dark since the news→race join was absent) is now live off the HO 394 observation layer. Provenance twist worth recording: the feature **shipped as commit `35bf8d5`, git-labeled HO 432** — a parallel session had built the complete implementation from an earlier "badge + drawer" handoff (HO 432) and left it **uncommitted** in the tree; the HO 470 session (a fresh badge-only respec) found it, verified it clean (`7cbcd37` is an ancestor of HEAD, diff purely additive over HO 466/467, tsc/build green), shipped it, then **dropped HO 470's own redundant spec** (a local gitignored file, just `rm`'d). **What lit:** `RacesBoxTabs` gained a `newsSet` registry beside `movedSet` (`newCount = newsSet.size`, replacing the hardcoded `0`); a per-card `RaceNewIndicator` (mirror of `RaceMovedIndicator`) registers when its latest-news date postdates the browser's `cbt:racesLastView` stamp, rendering a red `.rc-new` NEWS pip; opening RACES stamps `now` and clears both badges. **No new query** — the badge reads the existing **`getRaceNews(incumbent, 3)`** (threaded via `hubs[i].news[0].observedAt`), so the HO 414 "fold `getRaceNews`+`getMemberNews` into `getObservationNews` on the 3rd consumer" debt **stays deferred** (this adds callers, not a third projection). **The variant reality (corrects a premise stale across BOTH handoffs):** live `/` renders `<CompetitiveRacesBlock variant="v2" />` — whole-`<Link>` `RaceCard`s with **no popover** — so HO 432's second half (race news in the competitive **popover**: `RaceHubBody preview` + a hover-bridge) renders **only on the default variant = `/dashboard-classic`** (the sunset route), **not** the live dashboard. So the live dashboard's race→news is the **NEW badge only**; the news content lives on the `/race/[id]` hub (HO 398) every card links to. Both HO 432's author and the executing session had asserted "the dashboard has a race-news popover" by reading the popover branch in `CompetitiveRacesStrip` without checking the variant the caller passes — recorded in oddities. **The finding:** at ship, **3 of 4 featured incumbents carry ≥1 person-observation** (Ossoff / Collins / Kean; Nunn 0) → the badge lights **"New 3"** today; CDP-verified the pips + badge render and clear on RACES open (NJ-07 co-renders MOVED+NEWS, IA-03 correctly bare). **This discharges the QUEUED "HO 272 NEW badge + race→news drawer" item in full** — badge lit; the "drawer" resolved as a scope finding (no live drawer exists; popover-news shipped on classic); non-incumbent challengers stay unjoinable (no bioguide — the expected HO 398 coverage edge). **No theme-% change — Races held at 99** (a within-theme dashboard signal, matching the HO 398/414 race/member→news v1s). Docs: HO 471. **Also notes now run through HO 470.**

**Also (HO 475), the markets/kalshi cron migration GitHub Actions → Vercel Pro — infrastructure, no theme-% change.** The follow-up HO 433 flagged ("migrating those three workflows GitHub → Vercel … its own follow-up HO"), now shipped + prod-verified across `1c16646` (recon diagnostic, HO 474) · `f3fb87a` (Phase 1 — Vercel crons armed) · `945486f` (Phase 3 — workflows retired); live SHA `945486f`. **The three GitHub-Actions market crons (`markets-tick` `0,30 13-21 * * 1-5` fmp / `markets-daily` `0 */4` bare full-roster / `kalshi-tick` `15 */2`) are now Vercel Pro crons** in `vercel.json`: the intraday **fmp tick** ported as a query-string entry `/api/cron/markets?source=fmp` (the 7 latency-sensitive equity/index symbols SPX/NDQ/NVDA/AAPL/MSFT/GOOGL/LMT), the **bare full-roster** run **repurposing the dormant HO 314 `30 21` floor** (schedule changed in place `30 21 * * *` → `0 */4 * * *` — the every-4h bare superset IS the markets-daily replacement, so no separate floor remained to reconcile), and **kalshi** ported direct to `/api/cron/kalshi` `15 */2`; the three `.github/workflows/*.yml` deleted. **Probe-first as ever:** the HO 474 recon (read-only, committed diagnostic `scripts/diagnostic/markets-cadence-474.ts`) cleared the cutover — writes are benign under overlap (`market_ticks` is a plain append, the "harmless charting fuel" the daily workflow always tolerated; `kalshi_odds` is `ON CONFLICT(race_id) DO UPDATE`, idempotent), auth is green (`CRON_SECRET` proven in Vercel Production — the existing Vercel crons succeed and the repurposed floor already authorized), bloat is a non-issue (3,773 `market_ticks` rows / 51 days, no retention step) — so it shipped as a **clean single cutover** (arm → 1-day overlap watch → retire), not a zero-gap dance. **Reliability, not cadence** (the HO 433 framing): HO 313 measured GitHub dropping ~20% of scheduled slots, re-measured in the recon (the weekday `0,30 13-21` fmp histogram read **17h:12 / 15h:3 / 16h:3 / 18h:3 / 19h:2** where ~10/hr was due). **The attribution method (oddities):** `cron_runs` can't distinguish a Vercel invocation from the still-firing GitHub twin (it records route/status/payload, not method/host/UA), so all three were confirmed as distinct **Vercel-cron GETs** off the deployment host — fmp 17:00:44 (7 sym, no POLY), kalshi 18:15:29, bare 20:00:26 (18 sym + the POLY-SHUTDOWN ghost) — using **GET-vs-POST** and **200-implies-Bearer** (the route 401s without `CRON_SECRET`, so only Vercel-with-secret gets a 200) as the discriminators; the 20:00 window showed the **fmp + bare split 18s apart** (7 vs 18 symbols), proving the two `/api/cron/markets` entries register as distinct Vercel crons (a query-string cron keys on the full path string). **Deferred:** the FRED once-daily gate (the bare 4h run re-fetches the 4 `source:"fred"` symbols ~6×/day, ~20 redundant same-value append rows/day — negligible, and a real gate needs a route insert-guard, so skipped to keep the cutover pure-config; revisit only on a future cadence bump). **Banked:** the **markets-watchdog** pairing (a `cron_runs` health check asserting markets/kalshi fired in-window and the only chronic failure is the known POLY-SHUTDOWN ghost — now more valuable with the reliable Vercel floor) and a one-time **~200-row dead-symbol prune** (frozen Stooq-era rows — DOW/GOLD/BTC/VIX/DXY/… — inert in `market_ticks`, not in `MARKET_SYMBOLS`; pure cosmetics). **This discharges the HO 433 "migration flagged as its own follow-up HO" and closes the "Intraday-reliable markets upgrade" OPEN LOOP** (the HO 314 floor is now the 4h bare run; the "free hosted cron" the loop wanted is Vercel Pro's native cron). **No theme-% change** — cron infrastructure, no user-facing surface. Docs: HO 476. **Also notes now run through HO 475.**

**Also (HO 477), the cron-health watchdog — infrastructure, no theme-% change.** The banked "Markets watchdog" (from the HO 475 block), generalized and shipped as a fleet-wide health surface; live SHA `92baba0`. **The load-bearing finding:** a cron that silently *stops firing* is **invisible to Vercel's own monitoring** — no invocation means no function error to alert on — so the HO 314→475 floor *bounds* markets staleness but can't *detect* a total trigger failure; the only thing that catches it is an external "is the data fresh?" **pull**. So rather than a markets-only check, `lib/cron-health.ts` is a **14-route `CRON_ROUTES` registry + `computeCronHealth()`** (single source of truth) feeding two consumers: a new **`app/api/health` GET** returning **200 healthy / 503 unhealthy** (the pull surface an external uptime monitor watches — the actual push-alert layer, Corey's out-of-repo step), and the **refreshed HO 135 diagnostic** (its stale 7-route pre-433 list retired, now reading the same registry). **The liveness rule is the subtle part (oddities):** unhealthy = **no `cron_runs` row within the route's window** (the real "stopped firing" signal) OR the most-recent run is `error`/`orphaned` — **`timeout` counts as alive**, because HO 139's soft-timeout-resume means a `timeout` row proves the cron *fired and executed* (the pre-build probe caught this: `/api/sync` reads `timeout` with bills ~1h fresh and last clean success ~37h back — a success-age check would false-alarm on every soft-timeout route). **Markets is the one exception, keyed on data not the row (oddities):** because the route logs one `route="/api/cron/markets"` for both the bare + fmp crons AND carries a permanent POLY-SHUTDOWN chronicErr every bare run, `computeCronHealth` judges markets by **`MAX(market_ticks.ticked_at)`** (the literal "no `market_ticks` write in N hours" the backlog asked for) — unhealthy iff ticks > ~9h stale OR the markets run is error/orphaned; a success-with-ghost is healthy, the chronicErr string never consulted. Thresholds ≈ 2× cadence + grace (`summarize` 30m … `sync` 13h … 26h daily, weekly at a flat 9d `windowOnly`). The endpoint is **`force-dynamic`/`no-store`, unauthed, not `wrapCronRoute`-wrapped** (a monitor, not a cron — it never logs itself into `cron_runs`) and **503s on a check failure** (never a misleading 200). Prod-verified: **200 / `healthy:true` / all 14 routes fresh**, `/api/sync` + `/api/sync-votes` alive via recent-`timeout` rows, markets alive via tick-freshness with the ghost ignored. **This discharges the "Markets watchdog" OPEN LOOP.** **Two refinements banked:** fmp-specific freshness (the shared `route` string means fmp stopping while bare fires isn't row-detectable — needs payload inspection) and window-aware thresholds for the weekly routes (flat 9d catches a total stop; precise next-fire needs cron-expression parsing). **One finding filed as a new OPEN LOOP (not this HO):** `/api/sync` hit the 55s soft cap on ~6 of its last 7 ticks — the sync step commits (bills fresh) but a trailing step (dashboard-lead/trades) overruns; the watchdog correctly reads it alive, but it's the HO 407 / 405–406 step-overruns-the-wall class and wants its own probe. **No theme-% change** — cron infrastructure, no user-facing surface. Docs: HO 478. **Also notes now run through HO 477.**

**Also (HO 480), the `/api/sync` lead-query hint sweep — a correctness/operational fix, no theme-% change.** The `/api/sync trailing-step timeout` OPEN LOOP the HO 477 block filed, now probed (HO 479, read-only) and fixed (HO 480, hint-only); live SHA `b440b82`. **The load-bearing correction:** the overrun was mis-diagnosed by its symptom. HO 479's probe read the prod telemetry — 32 of 40 `/api/sync` ticks timed out at the 55s soft cap (a standing ~80%, not a wobble) — and cold-timed the suspects, refuting its own stated hypothesis (Gemini latency): the trailing cost was the HO 405/407 **cold-DB-stall**, not a slow external call. `gatherLeadData`'s transitions query, lured by the `is_ceremonial=0 OR IS NULL` predicate into a `MULTI-INDEX OR` over the low-selectivity `idx_bills_is_ceremonial` (~the whole 16.8k corpus) + a `TEMP B-TREE` sort, **ignored the perfect `idx_bills_stage_changed_at`** and cold-stalled past the 10s `boundedFetch` wall — retrying to 10+10s and *failing* the (non-fatal) lead step. **The finding worse than the timeout:** because the lead step was *failing* (not merely slow), the dashboard lead had been **silently going stale on every real-delta tick**, not only on the ticks that tripped the 55s cap. HO 480 swept the function 407 missed — `INDEXED BY idx_bills_stage_changed_at` on `transRs`/`topicRs`, `INDEXED BY idx_bills_introduced_date` on `introRs` (all cold-EXPLAIN-verified to a single sorted range, the residual `json_each` GROUP BY/ORDER BY temp-btrees inherent per the 407 Q6 precedent) — collapsing cold `generateDashboardLead()` from the 14–20s stall band to ~1–3s (warm 806ms), no `[db] timeout, retrying`. **The wall math:** `sync (~30, budget-pinned) + lead (~1–3) + trades (2.4–7.5) ≈ 33–40s`, comfortably under the 55s cap — route finalizes `success`, lead stops silently failing. Close criterion met. **Two items filed, not folded:** (a) the `wrapCronRoute` **instrumentation gap** — timeout rows carry no per-step timings because the finalize catch-branch writes `{ok,elapsedMs,error,status}`, not the handler's closure `timings`; real but a fleet-wide 14-cron contract change, and unnecessary once the route stops timing out (its success-row timings record normally) → its own HO; (b) the 4th lead read `queryEnactedThisWeek` mis-plans identically but is a shared helper → new OPEN LOOP (HO 481). **Banked, not today's cause:** FMP `AbortController` / split-trades — FMP `-latest` `402`s past page 0 so `maxPagesPerChamber:3` is a no-op, the GETs never hung, trades' 2.4–7.5s is ~200 sequential `INSERT OR IGNORE` round-trips. **No theme-% change** — an operational cron fix, no user-facing surface (the correctness gain: the lead is regenerated, not silently stale). Docs: HO 480. **Also notes now run through HO 480.**

**Also (HO 481), the `queryEnactedThisWeek` enacted-helper hint — the 5th sibling — no theme-% change.** The `queryEnactedThisWeek cold-stall` OPEN LOOP the HO 480 sweep filed, now closed; live SHA `9c19557`. HO 480 hinted three of `gatherLeadData`'s four reads and flagged the fourth — the shared HO 232 helper `queryEnactedThisWeek` (`lib/enacted-this-week.ts`), consumed by the lead step AND the ENACTED THIS WEEK banner + week-over-week delta — as needing its own scoping (a hint in a shared helper has blast radius). Probe-then-fix, hint-first: cold-EXPLAIN confirmed both callers (`queryEnactedThisWeek` + `queryEnactedPriorWeekCount`) mis-plan identically (`MULTI-INDEX OR` over `idx_bills_dash_stage` + temp-btree), and `INDEXED BY idx_bills_enacted` (Option A) collapsed both to `SCAN bills USING INDEX idx_bills_enacted` — cold 617ms / 106ms, identical rows/count — so the Option B new partial index (`idx_bills_enacted_changed`) was unnecessary, no migration. **The partial index qualifies** because it's `WHERE stage='enacted'` and the query carries `AND stage='enacted'`; forcing it bounds the scan to the enacted subset by construction. **Corrected premise:** HO 335 recorded this helper as riding `idx_bills_enacted` cleanly — but that index keys `(congress, latest_action_date)`, has no `stage_changed_at`, so it can serve the `stage='enacted'` seek but not the window filter or `ORDER BY stage_changed_at DESC`, leaving a residual in-memory sort (trivial over the few-hundred-row enacted set). **This closes the known OR-lure instances in the enacted/lead cluster (the last flagged sibling); the standing guard against new ones stays the HO 332/340 cold-start audit.** **No theme-% change** — a correctness/hygiene fix, no user-facing surface. Docs: HO 481. **Also notes now run through HO 481.**

**Also (HO 482–487), the `/lobbying` members-style redesign + the trades batch-fix verification — a chrome restructure + an operational verification, no theme-% change.** Two arcs cleared in one sweep. **Numbering:** HO 484 was drafted as the trades doc sweep but never issued; its content is folded here, so a future session should not hunt for a missing `484` file. This sweep is **487**. **(1) HO 482/483 — trades batch fix, VERIFIED against prod.** The `ingestChamber` DB writes in `lib/trades-ingest.ts` (~200 sequential `db.execute` `INSERT OR IGNORE` round-trips) were collapsed into one `db.batch(statements, "write")` (`ded2cf8`, shipped 2026-07-19 17:28). **The load-bearing correction on the verification itself:** the fix-time claim on record — "warm trades wall dropped 5–11.5s → 320ms" — is **not observable in `cron_runs`**, and the 14-day pull that was supposed to confirm it **straddles two mid-window fixes**, so the pooled figures are a trap (the naive full-window read shows a 63% `/api/sync` timeout rate + a 52s "breach", both fix-straddle artifacts, not current state). Split at the fix boundaries the story is clean and *confirms the prior arc*: **(a)** the route-timeout picture validates HO 480/481 (07-18 14:32) — **PRE-fix 79% timeout (34/43), p50 elapsed 55.2s; POST-fix 14% (2/14), p50 41.3s** — the route now finalizes `success` under the 55s soft cap on ~6 of 7 ticks, matching the documented ~1-in-5 baseline, the residual cap-hits being the by-design graceful reap (resumable sync continues next tick), not a regression. **(b)** the trades step validates HO 483 in *direction* — `payload.timings.trades` **PRE-483 p50 5.5s (n=11) → POST-483 p50 3.1s, min 1.9s (n=9)** — but the metric is the **whole `ingestTrades` step** (FMP page fetches for up to 3 pages × 2 chambers + name matching + the batch write), FMP-network-dominated, so the isolated 320ms DB-write win is muted inside a ~2–3s network floor and cannot be read off cron_runs directly. **A logged number is a claim, not a fact:** the 320ms is a real fix-time micro-measurement of the DB portion, but the coarse step metric is the only thing prod exposes and it only shows a consistent-direction p50 drop. **Re-probe triggers (banked: 52s route breach, or trades p90 > ~13s):** neither is crossed by post-fix steady state — post-fix route p50 41.3s (occasional cap-hits are the soft-cap-by-design, not a runaway), trades p90 11.5s under the 13s line (one pre-483 run touched 13.4s). **00:00-UTC contention oddity (banked at 483):** faint and inconclusive — success-only elapsed puts the midnight slot slowest (p50 46.5s vs 34–41s elsewhere), but n=5, it spans the fix boundary, and it is **not** trades-driven (midnight trades p50 3.3s is mid-pack); don't act on it. Verification script committed as `scripts/diagnostic/cron-runs-sync-verify-487.ts` (read-only). **(2) HO 485 — LDA filing-expand cost + shape probe, verdict GO.** The `/lobbying` inline row-expand runs a live per-click single-filing read; the probe cold-timed the worst case (`SEARCH lda_activities USING INDEX sqlite_autoindex_lda_activities_1 (filing_uuid=?)`, a PK-prefix seek — the **inverse of the HO 439 per-bill random-fetch trap that measured 44.5s**) at **75ms cold** (31-activity omnibus filing), typical **29–85ms**. Committed as `scripts/diagnostic/lda-filing-expand-cost-485.ts` (`3ed533c`); the durable LDA shape numbers it produced live in oddities. Note the follow-on `0614a66` typecheck guard — the probe pushed at `3ed533c` with a broken `npm run typecheck` (a `noUncheckedIndexedAccess` gap in the `pct` helper), undetected until the next session (oddities). **(3) HO 486 — the `/lobbying` members-style redesign.** Two commits: `2bc1a26` (chrome — an issue rail + filings content pane, scope via `?issue=`) and `0765aaf` (expandable rows + per-activity panel, carrying the `.lob-content .mc-row` A-regression fix and the orphaned-`IssueDrill` removal). One new query, `getFilingActivities`. **The deliberate divergence from `/members`:** scoping an issue serves the **precomputed `drill.recent` sample**, not a complete per-issue browse, and the pager stays on the **unscoped corpus feed only** — because a live per-code filings feed is the **HO 437 abandoned query (>25s cold)**, the thing the scoped-view divergence exists to work around (backlog). Prod-verified live at `0765aaf` (rail `ISSUES · 79` VOL-sorted, `AGE / REGISTRANT → CLIENT / ISSUES / BILLS` header, row carets, pager clamped `Page 1 of 40`, crosswalk + firms leaderboard below); headless verification is BotID-blocked (backlog). **No theme-% change** — a restructure of an existing surface + operational verification, no new theme surface (matching the LDA-arc precedent where 437 / 439–440 / 442 / 444 all held). Docs: HO 487. **Also notes now run through HO 487.**

**Also (HO 488–490), the first CI wiring + the #418 hydration arc — infrastructure + a correctness fix, no theme-% change.** Three HOs, and the arc closes on itself: **488 built the gate, 489 found the cause, 490 used the gate to make the cause unrepeatable.** **(1) HO 488 — CI wiring (`3aa677c`).** The **first CI in the repo** — there was none after the HO 475 Vercel-cron cutover retired the three GitHub Actions market workflows (and with them the whole `.github/` tree). `.github/workflows/ci.yml`: a single `checks` job running **typecheck + build** on `pull_request` and `push:main`, Node 20, `concurrency` with `cancel-in-progress`. **Secretless** — probed first: `next build` succeeds with `TURSO_*` and `GEMINI_API_KEY` unset because every DB-backed route is dynamic (`ƒ`), so no `env:` block and no repo secrets are needed. **Lint deliberately omitted** — no ESLint dependency or config exists in the repo, and wiring one fresh is its own arc (backlog), so forcing `next lint` would have produced a wall of pre-existing errors and blocked CI from landing clean. Verified two ways: a deliberate type error tripped `tsc` (TS2322, exit 2), and the Actions log was confirmed to show **real step execution, not skipped steps** (a green check on a skip-everything run is not a pass). No branch protection wired, so a red CI blocks nothing mechanically — it's the standing signal, not a hard gate. **(2) HO 489 — hydration probe (`c7d75f1`).** Root-caused the intermittent React **#418** on `/bills`, `/changes`, `/` (surfaced by the 488 CI's own smoke run): **client feed rows** (`BillRow`/`StagePillStrip`, `V2FeedList`, the expand panels) render a `Date.now()`-relative age at render time, so the SSR instant and the hydration instant land in different minute/hour/day buckets and React discards the server HTML. **Two plausible hypotheses killed** (oddities): it is *not* the `LAST SYNC` / HO 183 cycling stamp (`CyclingTimestamp`+`zone-cycle.ts` are hydration-safe by construction), and `/dashboard-v2` is a **`permanentRedirect("/")`, not a static page** — the failure attributed to it is really the home page after the 308, so the "static-staleness" theory was moot. Committed `scripts/diagnostic/hydration-probe-489.ts` — the forced-straddle harness (advance only the client's `Date.now()` before hydration). **(3) HO 490 — hydration fix (`8114ffc`, 34 files, +282/−75).** `formatRelativeAge` / `formatRelativeAgeLong` / `daysSince` now take a **required `nowMs`**; each page computes one `nowMs = Date.now()` and prop-drills it to the rows — no context provider, no default. **Required-with-no-default was the whole point:** a default would compile every existing call site untouched and let the bug walk back in silently; required means `tsc` rejects any implicit-clock caller, and **CI runs `tsc` (488)**, so the bug *class* is closed rather than the instance. The HO's 6-client-component sketch undercounted — the sweep actually reached ~14 pages + ~8 server wrappers (tsc's missing-arg errors drove completeness). Acceptance test = the 489 probe under forced straddle: **0 hydration errors across 6 loads** where it previously reproduced #418 every time. Also carries a **load-bearing force-dynamic invariant** (oddities): ages are now frozen server-side, so a route ever prerendered or given `revalidate` would produce a *silently wrong* age with no warning. **No theme-% change** — CI infrastructure + a correctness fix, no user-facing surface. Docs: HO 491. **Also notes now run through HO 491.**

**Also (HO 492–493), the `/lobbying` height compaction + feed-size tune — chrome + one constant, no theme-% change.** The `/lobbying` page ran ~4,897px tall (@1280w); two HOs took it to ~1,369px, all layout — no data-layer, no queries, no row internals. **(1) HO 492 — height compaction (`7564423`, 5 files).** Three chrome changes: the 79-code issue **rail bounded** (`.lob-rail-scroll`, 520px scroll + 31px header, internal scroll) so it stops setting the pane height with ~900px of dead whitespace beside it; **crosswalk + firms side-by-side** (`.lob-secs` 2-col grid, single-column ≤900px) instead of two full-width panels each wasting half their width; **both sections bounded** (`.lob-sec-scroll`, 340px, crosswalk topic-expansion scrolls inside the bound rather than clipping). Page **4,897 → 1,777px unscoped / 1,369px scoped**. All rules scoped to `.lob-*` (element-owned) so the shared `.mc-pane`/`.mc-rail`/`.mc-content` and `/members`/`/bill/[id]` stay byte-identical (globals.css diff purely additive). **The regression the bound introduced, and its fix** (oddities): bounding a list with a selected state broke selection visibility — scoping an issue low in the 79-item list re-rendered with it selected but the rail scrolled to top; fixed with `scrollIntoView({ block: "nearest" })` in `IssueRailRow` (scrolls the rail's overflow container only, not the window — verified on a deep `?issue=MON` pick). Rail bound chosen by **measuring** (scoped content ~553px, unscoped feed was the taller floor), not the mockup's number. **(2) HO 493 — feed page size tuned to the rail floor (`2fcb2a8`).** After 492 the bounded rail is the page-height floor (~551px), so any feed at/under that gives the same page height and the oversized feed only added dead scroll. `PAGE_SIZE` **25 → 13** (file-local to `app/lobbying/page.tsx`; `/bills` uses `FEED_PAGE_SIZE`, amendments/nominations have their own — zero blast radius). Unscoped now **1,369px**, section headers start ~887px (was ~1,250). **13 not 14** — the measured largest fit was 14 (17px slack) but feed rows are variable height (multi-bill-chip filings wrap ~33px), so 13's ~50px slack absorbs one wrapped row where 14 absorbs none (oddities: measured slack is a property of that day's feed, not the constant). **`MAX_FEED_PAGES` left at 40 deliberately:** reachable depth drops 1,000 → 520 filings while page 40's OFFSET falls to ~507 from ~975, so the deep-OFFSET clamp gets *cheaper*; holding depth would mean ~77 pages, worse. **No theme-% change** — a chrome restructure + one tuning constant on an existing surface (matching the LDA-arc precedent). Docs: HO 494. **Process observation (not a change):** the SKILL `/lobbying` entry has now gone stale twice in three arcs (HO 491 refresh, stale again by 492/493), both times because a shipped layout change didn't carry its own doc update — the cost of the code/docs-split convention, which is otherwise correct. Flagged, not acted on; if it recurs a third time the convention needs revisiting. **Also notes now run through HO 494.**

**Also (HO 495–496), the `/bills` two-pane redesign — probe → build, PARTIAL verdict, no theme-% change.** Third and final application of the `.mc-*` two-pane pattern (after committees on `/members` and LDA issue codes on `/lobbying`), so the pattern now spans all three category surfaces: committees, issue codes, CBT topics. **(1) HO 495 — filtered topic-distribution cost probe (`0d21148`, read-only diagnostic).** The rail's per-topic counts rebase on the other active filters — the query shape that produced the HO 382 `/?topics=` 500. Verdict **PARTIAL**: the bounded dims (stage/chamber/ceremonial) rebase cheaply (stage-present combos ~35ms; worst bounded — chamber/ceremonial alone — ~150–600ms, no costlier than the query already live on `/`), but **`q` is excluded** — it gives *zero* scan-narrowing (the query GROUP BYs all 24 topics regardless, so broad "act" at 29k matches and narrow "quantum" at 74 cost the same full-corpus walk) and every distinct search string would be its own unbounded cache key. No new index needed — the two HO 382 partial covering indexes suffice, and the stage-absent `INDEXED BY` hint stays mandatory or the planner regresses to the MULTI-INDEX-OR misplanner. **(2) HO 496 — the build, two commits.** `getBillTopicRailCounts` (`e84f7c5`) is a **new sibling**, deliberately NOT an extension of `getTopicDistribution` (widening that helper's two dashboard callers would silently reshape the treemap — the HO 486 riding-regression shape); it rebases on the bounded tuple only, zero-pads to 24 topics, and is self-excluding (never applies the topic clause). Then the two-pane chrome (`d789c20`): the 24-chip topic row becomes a **multi-select** `TopicRailRow` rail (the one divergence from the two single-scope rails — `?topics=` OR semantics preserved), the stage legend + party key fold into a keystrip, bill rows + client single-open expansion are unchanged, NEWS mode is untouched, and the CSS is purely additive under `.bl-*` so `/members`/`/lobbying`/`/bill/[id]` stay byte-identical. Live-verified: OR grows the count (HLTH+TAX 3,873 > HLTH 2,778), a stage filter rebases the rail, `q` leaves it unchanged, the three other pages render byte-identical. **No theme-% change** — a browse-surface restructure on an existing pillar. Docs: HO 497. **Also notes now run through HO 497.**

**Also (HO 499–501), the electoral-orphan sweep + the `/news` route extraction — no theme-% change.** **HO 499 (`2f6636c`), the HO 333 dead-component sweep:** deleted the four electoral orphans the Races+Primaries→Electoral consolidation (HO 333) left unreferenced — `RacesMap`, `PrimariesMap`, `PrimaryDistrictModal`, `PrimaryScrubber` — with the build compiling every electoral route as the safety net (a dangling import fails the build). **HO 500 (probe, read-only), the `/news` extraction scoping:** the premise going in was a greenfield route build; it wasn't. `app/news/page.tsx` **already existed as a redirect stub pointing INTO the thing it was meant to replace** (`/bills?mode=news`), so the real work was **inverting an existing arrow**, not laying a new one. Probe verdict: `NewsView` moves cleanly and there are **zero shared-component signature changes** — the HO 486 riding-regression pattern did *not* fire (the only edited component, `NewsFilters`, is single-consumer, confirmed by grep). Recommended: drop the cross-carry (option a), invert the redirect carrying `?bill=`, keep `mode` as a legacy alias. **HO 501 (two commits), the build:** `05ee430` adds an optional `basePath` prop to `NewsFilters` (additive, default `/bills`, byte-identical behavior) as a separable revert boundary; `ffdf243` is the atomic extraction — `/news` renders the feed (was the stub), carries **only** its five news params (`source`/`topic`/`window`/`bill`/`signal` + `page`), the eight-BILLS-param cross-carry is **dropped**, the redirect is **inverted** (`/bills?mode=news` → exactly one 307 → `/news`; `/news` itself does 0 redirects — the two-way-redirect brick avoided, curl-verified both directions), `mode` kept as a one-`if` legacy alias, the ⚡ `MediaAttentionCell` chip repointed to `/news?bill=`, and `breadcrumb` + `pathToNavKey` given `/news` lines. **`/bills` no longer carries ~10 params across two modes — the backlog's standing "`?mode=` is doing a route's job" observation is discharged.** (STEP 0 note: the `fit-finish.spec.ts:614` `/news` test PASSES against the new routes — its body only asserts loads-clean, never the redirect — so it's a title-rename, tracked as a separate code commit, not folded into these docs.) Docs: HO 502. **Also notes now run through HO 502.**

**Also (HO 503–504), the Phase-2 CI arc — a loop closed by replacing its own criterion, no theme-% change.** The Playwright-in-CI loop, carried since Jun 30 (HO 379), closed **backwards**: sent to build a fixture, it came back arguing against one. **HO 503 (probe, read-only, no commit) — verdict: don't build the fixture.** It was sent to cost the seeded local libSQL fixture the backlog had preferred since HO 491 (libSQL is SQLite, so a fixture removes the secrets question rather than managing it). Two results worth keeping though the verdict mooted them: `file:` works through `getDb()` **with zero `lib/db.ts` change** (the local sqlite path ignores the injected `boundedFetch` + the undefined `authToken`, so HO 238's HTTP timeout/retry semantics are provably untouched), and `migrate.ts` runs clean against `file:` (52 tables, FTS5 populated). Bucket count across both specs, 73 tests: **A 41 · B 28 · C 2 · D 2 · E 0** — 95% nominally fixture-servable. **The verdict turned on none of that.** It turned on the **guard inversion** (oddities): the specs guard with `if (await x.count()) … test.skip`, so an under-seeded fixture doesn't fail — it *passes*, exercising nothing. Green that proves nothing is worse than the red-blindness the fixture existed to cure; and pre-merge protection buys minutes in a single-committer flow that already diff-gates + prod-verifies every change, against a hundreds-of-row artifact maintained forever versus a 132-statement schema. **HO 504 (build, two commits) — the loop closed the other way.** `fc42872` (spec hardening) + `482cd55` (workflow). `.github/workflows/e2e-prod.yml`: an unattended prod crawl, `deployment_status` (gated `state=success` + `environment=Production`) + daily `0 15 * * *` + `workflow_dispatch`, running `e2e/smoke.spec.ts --grep-invert "@nonci" --retries=2`, `timeout-minutes: 20`, report artifact on failure, global concurrency `cancel-in-progress`. Deliberately a **separate workflow file, not a job in `ci.yml`** — it never runs on the PR path, so it can't become a required check that fails to report under the `paths-ignore`/branch-protection landmine `ci.yml` documents. **Step 0 (verified live, not assumed):** Vercel emits GitHub `deployment_status(state=success, env=Production)` to this repo, so post-deploy coverage is real; the event's `target_url` is the protected per-deploy alias, so the crawl runs against the stable prod domain instead, and the ~2min of checkout + `npm ci` + browser install is what makes that safe against the alias-swap race (the workflow comment says so as a constraint on future edits). Spec hardening: `isKnownNoise` extracted to `e2e/console-noise.ts` (single-source, both specs import it), the race-drawer test retargeted to `/electoral` with its skip-guard **converted to a hard assertion** (the cartogram is static topojson, so zero cells = regression), the bill-row selector re-anchored on the `role=button` + `aria-expanded` toggle contract, and the markets-hover case tagged `@nonci`. **Trigger coverage stated honestly:** `deployment_status` PROVEN (run `30040913867`, job ran-not-skipped, green 2m39s), `workflow_dispatch` PROVEN (run `30041280979`, exercises the `if:` fall-through branch `schedule` shares), **`schedule` UNPROVEN until its first fire** (can't be forced — tracked in backlog). **Two hardenings resolved, one retired:** #3 (bill-presence selector) shipped — but the backlog's description of it was **wrong on both counts** (it claimed an `<a href>` selector broken by `<li role=button>` rows; the actual code was a `.v2f-row` class selector against a `<div role="button">`); #2 (console allowlist) shipped as the shared extraction; **#1 retired as moot, not done** — "freeze the marquee so the hover lands" was specified before BotID was understood, and prod withholds the tape from headless Chrome entirely, so freezing a non-rendering animation fixes nothing. **No theme-% change** — test infrastructure, no user-facing surface (the HO 379 / 382–386 harness precedent); Foundation holds at 96. **Also notes now run through HO 504.**

**Also (HO 507, `eaf79a4`), the `/bill/[id]` detail-page redesign — panel-family hero + sectioned layout, no theme-% change.** The dedicated bill page had been *outclassed by its own hover preview*: the feed expand panel (HO 317) rendered a colored `BillStageBar` where the page showed a plain `StageIndicator`, carried RELATED NEWS the page lacked **entirely** — even though the ⚡ media-attention chip links *here* — and surfaced the summary up top, while the page buried it **6th down** on the one surface whose whole job is "WTF is this bill." So it was rebuilt as **the panel's big sibling**: a bordered hero reusing the shared `BillStageBar` + the `.bxp-body` 1.35fr/1fr grid (summary prose + RELATED NEWS via `getNewsForBill(bill.id, 5)` — the one added read, the exact query `/api/bill/[id]/panel` already runs — on the left, the boxed `.bxp-metabox` on the right), then member-hub section shells below: **COMMITTEES | HEARINGS** side-by-side via `.bdp-pair` (single-column ≤900px, **lone survivor renders full-width**), **AMENDMENTS** with the agreed/failed tallies folded into the header + a **340px scroll bound** (so `119-sconres-7`'s magnet render stops setting page height), **LOBBYING** with filings/clients folded up, and a one-row footer with a no-JS `<details>` raw-JSON toggle. **Four condensation moves** on the HO 492 fold-into-header precedent: the standalone `LATEST ACTION` section collapsed into the metabox, the amendment + lobbying stat lines pulled up into their shell headers, the footer squeezed to a single row. **Containment held** (the HO 486/492/496 additive-scope pattern): zero edits to the shared `.bxp-*`/`.v2f-bar` rules, every deviation additive under a new `.bdp` wrapper — the feed expand panel verified **byte-identical**. **`TopicTags` deleted** — `/bill/[id]` was its last consumer, closing the HO 316 holdout, so the B6 chip family (topic 316 · ID 466 · stage 467 · party 468) is now genuinely complete rather than "complete except one page"; `TopicChips` carries the metabox topics. The point-lookup `getBillById` gained **three plain `bills` columns** (`sponsor_bioguide_id`, `cosponsor_count`, `stage_changed_at`) — *not* because "the type lied": they're **optional** on the shared `FeedBill` row shape, so tsc could never have flagged the omitted SELECT (the check it can't run — oddities HO 508). And a **display-only** sponsor strip (`sponsorDisplayName()`, page-only) drops the `[P-ST]` embedded in `sponsor_name` so the colored `PartyTag` isn't double-printed — the query keeps the embedded form, the `BillAmendments` precedent. **No theme-% change** — a chrome restructure on an existing surface (the HO 486 / 492–493 / 495–496 browse-redesign precedent, every one of which held its bar); with this landing, **all three category browse surfaces (`/members` 328, `/lobbying` 486, `/bills` 496) plus the detail page now share one design vocabulary — the arc that opened at HO 328 is complete.** **Numbering — the 504 → 507 pointer jump skips no blocks:** HO 505 was the doc sweep *for* 503–504 (its content *is* that block, plus the SKILL testing-section refresh at `2c453af`), and HO 506 was docs-only (`22fd265` — backlog + oddities for the CA-cert pre-flight; no shipped surface, so no roadmap block was owed) — the HO 487 precedent for the skipped 484. Docs: HO 508. **Also notes now run through HO 507.**

**Also (HO 509–510), the shared record box — two detail pages onto one tabbed shell, no theme-% change.** The member hub was the last detail surface still in the pre-328 idiom — four unboxed strips stacked above the first section (`MemberHeader` / `MemberStats` / `MemberAffiliations` / `MemberFundraisingLine`, all floating on `--bg-base`), the DW-NOMINATE ideology axis buried *inside* the Voting-record section three sections down, and six full-width sections stacking to ~3,400px with two of them empty states ("No disclosed trades", "No recent news") on a typical House member; `/bill/[id]` had the same stack four sections deep. Both converged on one new `components/RecordTabs.tsx`. **HO 509 (`8538660`) — the component + the member hub.** A `"use client"` island on the `ActivityTabs` (HO 132/249) contract: holds **only** tab state, takes pre-rendered `ReactNode`s as props so the server page keeps every fetch; all panels mount with `hidden` (unmounting would drop an open `BillRowList` row on every swap); `tabs.length === 0 → null`. The hero became one bordered card in three bands — identity + a five-figure inline stat run (band 1), `MemberIdeology` **promoted out of Voting record** to the page's spine (band 2), and the metabox **unrolled sideways** into a `data-cells` N-up that collapses to 2-up/1-up when a cell is absent (band 3). Six sections → one box: Bills · Votes · Committees · Amendments · Press · Trades, plus a conditional **Scorecard** tab (Senate Democrats only — genuinely inapplicable, not merely zero). **Two components deleted**, both grep-confirmed single-consumer: `MemberAffiliations` (it re-printed the badges `MemberHeader`'s meta line already carried) and `MemberStats` (absorbed into the band-1 run). **The 509 follow-up, three fixes before the push:** `.rec-body` went from a fixed `430px` to `min-height: 260px; max-height: 430px` — **HO 244's no-jump rationale doesn't transfer**, because 244 protected content *below* its panel while the record box is the last element before the footer, so a swap moves only the footer; a bounded shift beat pervasive dead space on the majority of tabs. Internal out-links (`/bills?sponsor=`, `/trades?member=`) had regressed to plain `<a>` and were put back on `next/link` (external keeps `target="_blank"`). And the **election readout was printing twice** — `MemberHeader`'s chip *and* band 3's cell — so it consolidated into band 3, which also **fixed a live bug**: band 3 checked only `nextElectionYear != null` and rendered a stale "Next election 2024" where `MemberHeader` had correctly branched to "Former member." `MemberHeader` lost its election-chip block, its `rating` prop, and its **local `ratingColor()`** — the one carrying a "keep in sync with `RatingChip`" warning, so that duplicate map is discharged; the page imports the shared `lib/race-colors` one. **HO 510 (`d22fe66`) — the second consumer.** `/bill/[id]`'s four sections became the same box: Committees · Hearings · Amendments · Lobbying in **fixed legislative-process order** (referral → hearings → floor amendments → outside money, the order the sections already sat in). One additive prop, **`initialKey`**, opens the heaviest tab without the strip ever reordering — so `119-s-4784` opens on Amendments (872) and a one-referral bill opens on Committees. **The hero was not touched:** HO 507's containment rule held, no `.bxp-*` line moved, feed expand panel byte-identical. **Retired:** the page-local `SectionShell`, `.bdp-pair` (+ its `> section` rule and ≤900px query), `.bdp-amend-scroll` and `.bdp-out` — ~40 lines of `.bdp` CSS, three weeks after HO 507 landed it; the HO 507 stat lines that had been folded into section headers moved down one level into `.rec-note` sticky strips. **The two empty-state policies are opposite and both correct** — the arc's most transferable decision: member tabs render at zero and dim (`TRADES (0)` is a fact about a person), bill tabs are **omitted** at zero, so a bill with none of the four gets **no box at all**, preserving HO 507's section-omission exactly (see oddities). **`key={entity}` on both call sites** — `key={bill.id}`, `key={member.bioguideId}` — added as insurance against a same-instance route reconcile stranding the lazy-`useState` tab index, not a demonstrated bug (see oddities). **No theme-% change** — a chrome restructure on existing surfaces (the HO 486 / 492–493 / 495–496 / 507 precedent); it **extends** the HO 328 vocabulary arc the 507 block called complete rather than contradicting it — the browse surfaces converged on `.mc-*`, and the two detail pages have now converged on a second shared shell. Docs: HO 511. **Also notes now run through HO 510.**

**Also (HO 512, `af3ab90`), `?member=` on `/news` + the Press-tab out-link — no theme-% change.** The record box (HO 509) gave Bills + Trades out-links but left Press with nowhere to go — `/news` (HO 501) carried `source`/`topic`/`window`/`bill`/`signal` only. Three files, one commit: `/news` gains a `?member=` mode, and the member-hub Press tab gains `View press coverage →`. **The mechanism correction is the load-bearing content.** The backlog's close-path asserted `getNewsFeed` "already joins `observation_entities`" — **it doesn't**: `getNewsFeed` drives `news_mentions m INNER JOIN bills b` and never touches the observation tables. A person-filter bolted on there would have returned only articles that both name the member *and* matched a bill — a strict subset of what the Press tab previews (most of a member's coverage has no bill match), so the out-link would have landed on **fewer** rows than the tab it came from. So member mode is a **branch to the observation layer** — `getMemberNews`, the exact query the Press tab already renders — not a filter on the mention feed, and it ships **zero `lib/queries.ts` changes** (the HO 501 route-split precedent: the honest fix was structural, not another param on the existing query; see oddities). **Precedence:** when `member` parses it owns the view — `bill`/`source`/`topic`/`window`/`signal`/`page` are ignored and not carried, and the pill's ✕ returns to bare `/news`; `getMember` null (valid-shape but unknown bioguide) renders member mode with the raw id + empty state (never a 404), and a parse-fail falls through to the unscoped feed. **The filter-bar asymmetry, stated as a rule so it isn't "fixed" later:** member mode early-branches `NewsFilters` to render *only* the member pill — observation rows carry no source/topic/window/signal dims, so dead chips would imply filtering that isn't happening; bill mode keeps its chips because bill-scoped rows are still `news_mentions`. Both correct. **The cap asymmetry, disclosed:** the tab previews `getMemberNews(…, 8)`, the out-link serves `NEWS_MEMBER_CAP = 50` (file-local, the HO 493 precedent), so the out-link deliberately lands on *more* rows than the tab — the opposite direction from Bills/Trades (whose out-links exist because their counts exceed the shown rows). Hence **no count in the label**: `count: news.length` is a cap, not a total, and "View all N" would be a lie. **Not done (recorded so it isn't re-litigated):** no `getObservationNews` fold — the HO 414 debt triggers on a 3rd *projection*, and this is a 3rd *caller* of the existing helper at a raised limit (the HO 470 "adds callers, not a third projection" line); no pagination, no total-count query, no `getNewsFeed` change of any kind; the `/news` source-spine two-pane stays a separate QUEUED follow-on (this added a mode, not the rail). **No theme-% change** — a cross-link on existing surfaces (the HO 509–510 precedent); it **discharges** the backlog's Press-out-link item in full. Docs: HO 513. **Also notes now run through HO 512.**

**Also (HO 514–515), the `/news` topic rail — the 4th and final `.mc-*` two-pane, no theme-% change.** The `.mc-*` two-pane pattern now covers **every browse surface**: committees (`/members` 328) → LDA issue codes (`/lobbying` 486) → bill topics (`/bills` 496) → **news topics (`/news` 515)** — the arc that opened at HO 328 is complete across browse surfaces (the detail pages converged separately on `RecordTabs`, HO 509–510). **The respec is the block's real content:** the banked item named **SOURCES** as the spine, but `NEWS_SOURCES` has **three** entries against 24 / 79 / ~200 on the shipped rails — three rows is the SOURCE chip row relocated, not a spine. So the rail shipped on the **TOPIC** axis instead, and the SOURCES idea is **dead on cardinality, not deferred**. **HO 514 (`f7a3de4`), the probe — GO, live-servable, hint mandatory:** unhinted bare **2231ms** cold (the MULTI-INDEX OR off `idx_bills_is_ceremonial` — the HO 332/479–481 drive-order class firing on a *fourth* surface), `signal=breaking` 943ms, vs **29–64ms hinted** across every combination; when `source` is set the planner picks `idx_news_mentions_source` itself but hinting there costs nothing (31–33ms), so **one always-hinted query** beat a conditional. No new index. Corpus **re-measured 227 → 726 rows / 439 distinct articles** (3.2×, not order-of-magnitude, so the live call stands — the stale 227 lived in 4 places incl. 2 code comments, corrected across HO 516's two commits). **HO 515, two commits:** `a2c825d` — `getNewsTopicRailCounts`, a **new sibling** of `getNewsFeed` (not an extension — the HO 496 `getBillTopicRailCounts` reasoning), self-excluding on `topic`, rebased on source/window/signal, `unstable_cache` tag `news-breaking` (no new cache wiring), `try/catch → null`, 24 topics zero-padded VOL-desc; `8bb9618` — the `.mc-pane nw-pane` chrome. **Two calls recorded as rules:** (a) **the rail counts DISTINCT ARTICLES, not mentions** — the feed's own total is distinct-articles (HO 133), so a mentions-counting rail would print a number the feed contradicts on click (the HO 442 structural-coherence discipline); enforced by extracting the dedup expression to one module-level **`NEWS_ARTICLE_KEY_SQL`** both consume, so they *can't* drift (the `getNewsFeed` refactor was pure substitution, feed verified unchanged; live: `?topic=government_operations` → rail 43 === feed 43). (b) **single-select rail**, a new `NewsTopicRailRow` on the `/members` + `/lobbying` `.mc-crow.is-sel` idiom — **not** a lift of `/bills`' `TopicRailRow`, whose multi-select checkbox is the documented *divergence* (plural `?topics=` OR) against `/news`'s singular `?topic=`; widening the `/bills` component with a mode flag was rejected as the HO 486/496 riding-regression shape. **Three modes, stated as contract:** default → rail renders; `?bill=` → no rail (one bill is a single-topic slice); `?member=` → no rail (different table — HO 512's mode comes out byte-identical, `hideTopics` left off so its chip row returns). The hidden rail in member mode is a **deliberate seam Axis B reopens**, not an oversight. **CSS additive under `.nw-*`** — diff-confirmed no shared `.mc-`/`.lob-`/`.bl-`/`.bxp-`/`.bdp-` rule edited, so `/members` / `/lobbying` / `/bills` are byte-identical (the containment rule has now held through 486 / 492 / 496 / 507 / 515). **Axis B, banked with its constraint (the forward-looking part):** an **"IN THE NEWS" member group as a SECOND group in this same rail**, off the observation layer — probe cardinality **126 bioguides ≥1 obs, 39 ≥3, 10 ≥10**, head genuinely newsy (Graham 49 · Thune 29 · Collins 21 · McConnell 20), **100% of resolved bioguides hit a live `members` row**, rollup 34ms off `idx_obs_entities_type_value`. **The load-bearing constraint: the member group needs its OWN window, decoupled from `?window=`** — at 24h the head collapses to 6 members (0 at ≥3), so rebasing it on the page window empties it; and the observation layer is only ~30d old (HO 394 pilot), so 30d ≈ all-time *today* and that equivalence will stop holding. **No theme-% change** — a browse-surface restructure on an existing pillar (the 486 / 492–493 / 495–496 / 507 precedent). Docs: HO 516. **Also notes now run through HO 515.**

**Also (HO 517, `a71a1d4` query · `f1b1167` chrome), the `/news` "IN THE NEWS" member rail group — the Axis B seam closed, no theme-% change.** The forward item the HO 514–515 block banked (a second rail group off the observation layer), built with **no probe step** — HO 514's cardinality/cost numbers stood up unmodified, the payoff of measuring both axes in one probe. The `?member=` seam HO 515 left deliberately open (member mode rendered no rail) is now closed: the `.nw-rail` carries **two groups** (TOPICS + IN THE NEWS) inside its one bounded `.nw-rail-scroll`, and member mode is a first-class rail selection rather than a deep-link-only view. **The query (`a71a1d4`):** `getNewsMemberRailCounts()` off `observation_entities` (`entity_type='person'`) → `observations`, `INNER JOIN members` for name+party, ranked desc + name-tiebroken, returns `{ rows, total }`; EXPLAIN-confirmed it drives `idx_obs_entities_type_value`, `unstable_cache` tag `member-news` (already flushed by the news cron + allowlisted on `/api/revalidate`, HO 414 — no new wiring), `try/catch → null` hiding **only this group** (TOPICS + feed still render). Plus `getNewsMemberRailPin(bioguide)`, an **uncached point-lookup** for the pin — a bounded `activeBioguide` arg on the counts query would make every visited member its own cache key, so the separate lookup keeps the counts cache clean. `NEWS_MEMBER_RAIL_WINDOW_DAYS = 30` is exported so the header label and the query can't drift. **Four design calls, all verified live:** (1) **fixed 30d window, disclosed** — the member group does NOT rebase on `source`/`window`/`signal` (header `IN THE NEWS · 30d`); 30d not "all-time" because the observation layer is ~30d old (HO 394 pilot) so they're equal today but the label stays correct as the corpus accrues (the HO 456 disclose-the-exclusion posture) — verified Graham holds 49 at `?window=24`. (2) **units match** — HO 394 writes one observation per fetched article, so both groups count **articles** (the HO 442 coherence that lets two count columns share one rail; recorded in a comment). (3) **one selection across both groups** — the two groups drive different panes (topic → mention feed, member → observation feed) so they can't co-activate; `NewsTopicRailRow.scope()` now also clears `member` and selecting a member clears `topic` (topic↔member mutually clear, exactly one lit row spanning both groups). (4) **top 15, total disclosed** (`… · 15 of 126`), with the active member **pinned** at the group top when outside the shown set — the one path the rail can't reach from itself (the HO 512 Press-tab out-link can send any of ~535 members here), so without it a low-coverage arrival renders a rail with nothing lit: the HO 492 selection-invisibility failure through a different door. **The zero-count pin holds:** a pinned member with 0 recent observations still renders, lit, count 0 (the pane below is showing their coverage, so the rail must agree they're the active scope). **Chrome (`f1b1167`):** a new single-select `NewsMemberRailRow` (party dot via `lib/race-colors`, not a local map — HO 468; surname-forward + count, no bar), both groups sharing the one `.nw-rail-scroll` (no nested scroll), `scrollIntoView({block:"nearest"})` covering the member group too; CSS **additive under `.nw-*`** (a `.nw-rail .mc-crow.nw-mrow` grid override + dot + pin hairline + group-header border) — diff-confirmed no shared `.mc-`/`.lob-`/`.bl-`/`.bxp-`/`.bdp-` rule touched, the **sixth** consecutive test of the containment rule (486 / 492 / 496 / 507 / 515 / 517). **Mode contract now:** default → both groups selectable; `?topic=` → topic lit, mention feed; **`?member=` → rail renders** (the 515 seam closed — TOPICS shows bare counts, IN THE NEWS shows the member lit/pinned), the HO 512 observation-feed pane below unchanged; `?bill=` → still no rail, byte-identical. Prod-verified across bare (15 rows, Graham head), `member=Lawler` (rank ~30, injected+lit, `nw-mrow-pin`×1, observation feed), `member=Aderholt` (0 recent obs, pinned+lit, count 0 — the call-4 zero case), the window-independence check, and `?bill=` unchanged; `tsc` clean, `verify:deploy` served `f1b1167` === HEAD. **No new nav, migration, cron, or index; the 35 unjoinable person rows (no bioguide) stay excluded (the HO 398 challenger edge); SOURCES stays dead on cardinality (HO 514).** **No theme-% change** — a browse-surface add on an existing pillar (the 486 / 492–493 / 495–496 / 507 / 514–515 precedent). With this, the `.mc-*` two-pane arc that opened at HO 328 is complete across every browse surface **and** the `/news` rail now carries the member axis the observation layer was built to enable. Docs: HO 518. **Also notes now run through HO 517.**

**Also (HO 519–520), the 2nd full re-derivation + the SC seat-change fix.** **HO 519 (read-only audit, §0–§5 against `5681322`):** the deferred "next full audit," done as re-derivation #2. **Structurally green** — sync fresh (`MAX(update_date)` today, `bills` 17,027 == `bills_fts`), tracked corpus **16,657** (`getCorpusStats(true)`, up from 15,075 at HO 404), summarize queue drained to 4; `/api/health` 200 with **all 14 crons fresh**; the **four fixed loops holding** in steady state — summarize (406) not re-grown, weekly-report (407) present + catch-up healthy, `/api/sync` trailing-step (480/481) at **77% → 7%** post-fix timeout (the residual ~55s reaping is the by-design HO 477 soft-cap, not failure), trades (483) step p90 **10,530 → 5,600ms** (clear of the ~13s re-probe line); cold-plan audit **0 unexpected BAD of 50**. **Cold-plan gap-closer:** `cold-start-audit-332`'s SQL inventory predates the LDA / amendments / nominations / news-rail pillars, so its "0 BAD" is blind to them — the audit **hand-EXPLAIN'd 9 post-404 queries** (the `*RailCounts` trio, the amendments disposition/aggregate scans, the nominations GROUP BYs, the `getBillLobbying` seek), all riding their intended index or covering index (extending the script is a **standing observation**, not owed here). **Two ratified bar decisions:** **News signal CLOSED → 98** — the "obs layer joinable" label under-sold a now-full pillar (`/news` route 501 + `?member=` 512 + topic rail 515 + member rail 517, race→news 398, member→news 414, dashboard NEW badge 470); `98` not higher because the residue is gated/inert (the observation envelope grading/cluster/lineage fields, the policy-hook→bill/topic linkage, the the_hill budget soak), not active gaps. **New combined bar `Lobbying / amendments / nominations` = 92** — one bar (the 435–463 "extend downstream along the bill spine" arc; the `/patterns`-class aggregate+drill shape; each individually thinner surface than Member/Races; naming matches the `Committee activity / hearings` slash style); `92` = v1-complete across all three (data + aggregate + detail + cross-links) with real banked gaps (LD-203 money surface, military-noms view + `/nomination/[id]`, amendment→roll-call `recordedVotes` linkage, the `/lobbying` fbar features + description clamp). **Overall ~98 → ~97**, recomputed from the reconciled **9** bars; **all other bars held** (Foundation 96 / Home 100 / Visualizations 97 / Weekly reports 98 / Member depth 99 / Races 99 / Committee activity / hearings 98). **HO 520 (SC seat-change fix — data-only, zero commits):** Sen. Lindsey Graham (`G000359`, R-SC, class 2) **died** (served 2003–2026); Gov. McMaster appointed **Darline Graham (`G000608`)** as caretaker until 2027-01-03 — **verified against governor.sc.gov** (the HO 422 Kiley verify-before-fix discipline: a logged "wrong-incumbent" was a real post-cutoff event, not bad yaml). CBT's stale roster (`G000359` still `is_current=1`, `G000608` absent) + the `S-SC-2026` incumbent were fixed by the routine **`sync:members` → `sync:crosswalk` → `backfill:races`** (the HO 402 Gallagher-staleness class recurred), the **HO 410 audit re-ran fully green** (§1/§2/§4 all 0/0), and the live `/electoral` + `/race/S-SC-2026` now render Darline Graham — **zero code, zero commits**. The **Aug-11 special-primary re-ingest is parked** (SC 2026 became an open special — special primary Aug 11, runoff Aug 25, general Nov 3; the field's still forming, so the stored June-9 regular primary re-ingests once results exist — the "re-poll as the field settles" pattern). Docs: HO 521. **Also notes now run through HO 520.**

**Also (HO 522–525), the member-scorecard participation arc — B1 chamber-context + B2 career rollup, Member depth 99 → 100.** **HO 522 (source probe):** participation **GO**; the "VoteSmart" idea was **NO-GO on *access* not join** — the icpsr↔votesmart join was already solved in `member_ids.votesmart`, but the SmartVote/VoteSmart API is retired behind a Cloudflare/ToS wall, so it's an access dead-end; **broader external scorecards reframed from a hold to optional breadth** (LCV CSV-export + Heritage bioguide-keyed URLs both viable-but-unbuilt, ADA dropped stale, USCPR already ships as one instance). **HO 523 (B1, `2b8d62b`):** the 119th missed-rate was **already on the hero** — the delta was chamber-median **context** beside it (`getChamberParticipationContext`, a cached all-member distribution, **no new table**); **floor F=50** over the population for both the median and rank-eligibility (floored only 3 departed members, clips zero current). Median shipped in the hero; a bare attendance **rank was computed-then-dropped** (delegate-inflated + unused — the drop-unused-field call). **HO 524 (cost probe):** streaming ingest **GO** — 52 files 94→119, ~356MB, flat ~130MB peak (the accumulator is 536 members × a few ints, so memory doesn't grow with range; stream per-file, discard, next). **HO 525 (B2, `bf63b23`):** `member_career_votes` rollup via manual `sync:career-votes` (truncate-then-recompute off Voteview's per-Congress votes files, current-only via an `is_current=1` join), the career stat on the hub beside the 119th (`Missed · career · N Congresses`); cross-checks passed (Collins 0/10,062 since 1997, Grassley 1.09%/26C, Fetterman 13.1%), Graham (no icpsr) → graceful null. **Two data findings worth the oddities ledger:** (1) Voteview `cast_code` **7/8 = Present = participation**, so the correct split is **cast = 1–8 / miss = 9 / not-seated = 0-or-absent** — the coarse {0,1–6,9} model the HO 524 handoff carried was **wrong**, caught only by parsing real rows; (2) **non-voting delegates carry structurally-inflated miss-rates** (can't vote on final passage), so a *rank* surface needs a delegate-eligibility carve-out while a member's *own-rate* display keeps it (B1/B2 precedent). **Member depth 99 → 100** — the substantive participation gap shipped end-to-end, the "external scorecards" half reframed optional (see provenance). No new nav / cron / migration beyond the one rollup table; the probe + cost scripts stay untracked. Docs: HO 526. **Also notes now run through HO 525.**

**Also (HO 527), the `/members` participation dotplot — the participation arc's population cut, no bar move.** The population twin of the member-hub participation display (B1 523 + B2 525), built as `IdeologyStrip`'s sibling — the exact member-hub→`/members`-dotplot progression the ideology arc took (hub axis 421 → dotplot 425). Shipped + prod-verified; live SHA `7fa729c`. **`getParticipationStrip()`** (`lib/queries.ts`) is a new sibling of `getIdeologyStrip` (NOT an extension of `getChamberParticipationContext`, which returns only per-chamber medians): every floored current member (`is_current=1`, `HAVING total >= PARTICIPATION_FLOOR`, metric `not_voting/total` — the **identical B1 population**, so the strip's client-computed chamber medians echo the hub context by construction) plus an `is_delegate` flag; small-table population read, no `INDEXED BY`/no new index (the `getIdeologyStrip`/band regime), `unstable_cache` tag `votes`/3600. **`ParticipationStrip`** (`components/ParticipationStrip.tsx`) is `IdeologyStrip` cloned with the metric deltas: domain **0..CAP=30** (locked from STEP 0's non-delegate max 29.02, not a mockup number), 0.5pp Wilkinson bins, dots **party-colored** (so high-miss clustering by party reads — roadmap lens #4), median ticks on the **CHAMBER split** (HSE/SEN, neutral amber — NOT party, the meaningful split since the House votes far more than the Senate), no empty-center annotation (participation is unimodal-skewed). Placed **open directly below** `IdeologyStrip`, rescoped by the same chamber toggle only (`partDots` mirrors `stripDots`); CSS **additive under `.part-strip-*`**, `IdeologyStrip` byte-identical (the containment rule's next test). **The delegate carve-out is the load-bearing detail (the HO 523/525 banked constraint, now discharged for the dotplot):** non-voting delegates are excluded from both cloud and median via the territorial predicate `chamber='house' AND state IN ('DC','AS','GU','MP','PR','VI')` (there is no delegate flag — the 6-code set catches exactly the 6 non-voting members), with the excluded count disclosed in the footer (the LDA/nominations disclose-the-exclusion posture). **The STEP-0 finding corrected a premise (oddities):** the carve-out is **population-correctness, not outlier-trimming** — only 2 of 6 delegates (Radewagen 82%, Plaskett 50%) are extreme outliers; the real high-miss tail is a **voting** member (Frederica Wilson 29% > 4 of the 6 delegates), which is what sets CAP. So delegates are dropped because they're structurally ineligible on final passage, NOT because they're the high scores. Prod-verified: HSE med 1.9% / SEN med 2.1% (= the B1 medians), chamber toggle rescopes (ALL 2 ticks 530/6 · HOUSE 1 tick 431/6 · SENATE 1 tick 99/0), delegates absent from the cloud, hover→hub. **Member depth holds at 100** — an enabling surface, no bar move (the HO 425 dotplot precedent, which also held). **Banked from the arc, still unbuilt:** a participation *sort/rank* on the roster (the carve-out constraint still binds it) + a career-chamber-median context (cheap from `member_career_votes`, deliberately unbuilt to avoid a 4-stat hero). Docs: HO 528. **Also notes now run through HO 527.**

**Also (HO 529–530), amendment roll-call votes on the bill hub — the Senate half of the banked `recordedVotes` gap, query-only, no theme-% change.** The HO 452 `recordedVotes` lead (the capability that block banked when the amendments status model closed NO-GO), built probe-first. **HO 529 (`e0131cf`, read-only probe) retired the backlog's premise.** The banked item assumed a member-level "how did each member vote on this amendment" surface needs an **`/actions` walk** (~2× the amendments backfill) to recover each voted amendment's roll-call number. The probe found that **half wrong and the handoff's own candidate join dead**, payoff intact via two different keys per chamber (the "a backlog line's stated mechanism is a claim — probe it" rule, HO 512/514 class): **(1) `votes.amendment_designation` is dead for amendments** — it only holds nomination/treaty designations (the syncs tag amendment votes to the underlying bill or to nothing), 0/431 join. **(2) Senate is FREE (GO-CHEAP):** Senate amendment votes carry the number in `votes.question` (`On the Amendment S.Amdt. 14 to …`), a regex parse joins **114 SAMDT, 100% on tracked bills, `member_votes` present** (99–100/vote) — query-only, zero API, zero backfill, retiring the walk premise for the Senate. **(3) House needs the walk (GO-WALK, linkage-only):** House vote questions are generic (`On Agreeing to the Amendment`, no number, only `bill_id` — can't disambiguate which amendment), so a text parse can't do the House; the `/actions.recordedVotes.rollNumber` walk resolves **100% to existing `votes` rows** (BLOCKED-ON-SYNC refuted — the roll calls are all ingested), so the House is a bounded *linkage* backfill (store amendment↔`vote.id`), NOT a member-vote backfill. **(4) The buildable surface is small but clean** — ~114 SAMDT free + the rare HAMDTs recoverable via the walk (the HO 461 "the floor almost never votes" finding reinforced: 17/20 sampled HAMDTs never got a recorded vote), all on tracked bills, full member positions. **HO 530 (`1f9f748`) ships the Senate half — query-only.** `getBillAmendmentVotes(billId)` is a **separate** cached query (the HO 496/512 new-sibling discipline — `getBillAmendments` byte-identical; the regex parse can't live in its SQL): pulls the bill's SAMDT amendments, parses matching Senate votes from `votes.question`, attaches the tally (on the `votes` row directly — `yea_count`/`nay_count`/`result`) + the **D/R/I party split** from `member_votes` (a bounded per-vote aggregate), returns `Map<amendmentId, AmendmentVote[]>` (a **list** — 3 amendments corpus-wide carry a table+substantive pair). Rendered as a compact vote line under acted amendments in the `/bill/[id]` AMENDMENTS section — `Rejected 45–55 · D 41-4 · R 2-51 · I 2-0` (party-colored via the shared `partyColor`, HO 468) — and the display-only disposition dot **becomes authoritative from `votes.result` where a vote exists** (via the module `deriveDisposition`, no drift), keeping the keyword scan only for the no-vote majority. Byte-identical where no vote (~99% of rows — the absence is the signal, the HO 527 omit-don't-disclose-per-row call). **No cost probe** — 529 settled feasibility, the set is tiny (~114 rows corpus-wide); a straight build. Prod-verified on `119-hr-1` (the reconciliation bill's Senate vote-a-rama): 13 vote lines + 39 party-split fragments, the split summing to the tally in every sampled case (`119-hr-1`/`119-s-2`/`119-sconres-33`, e.g. a 50–50 VP-tiebreak reading R 50-3 party-line). **Two records for the ledger:** the **two amendment-row components are distinct** — the bill-hub row is an internal function inside `BillAmendments.tsx`; `components/AmendmentRow.tsx` is the *separate* `/amendments` corpus-feed row (HO 461) — so the HO 530 handoff's pathspec conflated them and the edit correctly landed in `BillAmendments.tsx` only (a SKILL note now, so future handoffs pathspec right); and **`votes.id` is a TEXT composite** (`senate-119-1-3`), not an integer. **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** this ships the **Senate half** of the 519-named `recordedVotes` gap, but the **House linkage walk and the full per-member roll-call drill both stay banked**, so the gap is partially—not fully—discharged (a v1 cut on an existing pillar, the LDA/amendments precedent every prior surface held). **Banked, no fixed HO number** (they take the next free number when built — the HO 418 don't-pre-number lesson; the "531" the HO 530 handoff pinned to the House walk was premature since this sweep is 531): the **House amendment→vote linkage walk** (GO-WALK, build-ready, no further probe — a bounded `/actions` walk over HAMDTs-with-a-recorded-vote storing a persisted `amendment↔vote.id` link + a cron delta step), the **full per-member roll-call drill** (v1 ships the partisan *shape*; the who-voted-how list is the heavier v2 expand, `member_votes` has the data), and the **`/amendments` aggregate "voted" cut** (once both chambers' links exist). Docs: HO 531. **Also notes now run through HO 530.**

**Also (HO 532–533), amendment votes across both chambers + a latent-500 fix — the `recordedVotes` gap closed, no theme-% change.** The House tail HO 530/531 banked (GO-WALK, no probe — HO 529 §C confirmed 100% of `recordedVotes.rollNumber` resolve to existing `votes` rows), plus the cache-serialization bug the build surfaced. **HO 532 (`3c4c72c`), the House linkage walk.** The House has no cheap key (generic `On Agreeing to the Amendment` question, no number), so the amendment→vote link is recovered from each HAMDT's `/actions.recordedVotes` — a **linkage** backfill (`amendment_id ↔ vote_id`), NOT a member-vote one (positions already in the DB). A new **`amendment_votes`** link table (composite PK for the table+substantive list case), an **`amendment_vote_walked_at`** sentinel on `amendments` (sync-invisible — the amendments-sync `ON CONFLICT SET` names every column so it's never clobbered, the HO 459 pattern verified by reading the clause), and a partial `idx_amendments_hamdt` walk-queue index. `sync:amendment-votes --walk` (`lib/amendment-votes-walk.ts`) fetches each HAMDT's `/actions`, **constructs** the deterministic `house-{congress}-{session}-{rollNumber}` vote_id (House `votes.id` is composable — no lookup), upserts the link, stamps the sentinel (the queue-exit; the re-walk predicate keys on `update_date > walked_at`, so a HAMDT that *gains* a vote after its first walk is caught — the live-corpus edge the nominations immutable-committee case didn't have). A **bounded cron walk step** rides after the sync in `/api/cron/amendments` (240s/280s carve, cap 40/tick, non-fatal). The read is the **same `getBillAmendmentVotes`** (Step 1 widened to `IN ('SAMDT','HAMDT')`, a new Step 2b joins `amendment_votes → votes`; the party-split, tally, and render are chamber-agnostic and reused). Backfill: **267 HAMDT walked, 92 links / 90 amendments, 0 fetch errors** (`119-hamdt-5 → house-119-1-32`, Failed 182–226, 432 member_votes — the HO 529 §C case confirmed). **HO 533 (`85d6660`), two fixes, one blocking the other.** (1) **The latent prod 500 — the bigger find:** `getBillAmendmentVotes` returned a **`Map`**, but `unstable_cache` serializes its return and a Map round-trips to `{}` on a **cache HIT** — so `.get` was gone and `BillAmendments` threw `votes.get is not a function`, 500-ing **every amendment-bearing `/bill/[id]` for ~59 of every 60 minutes since HO 530 first shipped.** It hid because every "verified live" check landed in the cache-**miss** window right after each deploy (three deploys, three misses). Fixed by returning a plain **`Record<amendmentId, AmendmentVote[]>`** (serializes cleanly), consumer reads `votes[a.id]`. (2) **HO 533's own ask — the zero-split guard:** `VoteLine` now suppresses the D/R/I party-split fragment when it's entirely zero (the `member_votes`-lag case — the newest roll calls' member detail trails the vote-list sync, e.g. `house-119-2-269`), so those rows show the tally alone and self-heal on the next votes sync rather than printing a false `D 0-0 · R 0-0` (the "wrong is worse than absent" HO 448 disposition-dot principle). The Map fix was a **hard prerequisite** — the guard lives inside `VoteLine`, which never rendered while `BillAmendments` threw first, so both shipped in one commit. Prod-verified on the cache-**hit** path (3 consecutive hits): `119-hr-8800` 200 (was 500), 19 tallies / 18 splits (lagging row suppressed); `119-hr-1` (Senate) unchanged at 13; a repo-wide ~90-function audit confirmed `getBillAmendmentVotes` was the **only** Map/Set/Date-returning cached function. **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** the House walk is the *linkage completion* of the same 519 `recordedVotes` gap (both chambers now render amendment votes), not a new surface, and the 500 fix is a correctness fix — recorded honestly as a regression that **shipped at HO 530 and lived latent for three deploys**, not a bar event. **Two process lessons (oddities):** never return a `Map`/`Set`/`Date`/class from an `unstable_cache` function (plain objects/arrays/primitives only — they round-trip to `{}`/`[]` on a hit, and tsc can't catch it); and **cache-miss verification right after a deploy does not exercise the cache-hit path** — hit it 2–3 times (or wait out the revalidate window) before calling a cached surface verified. **Banked, now both cleaner** (the House links unblocked them): the full per-member roll-call drill (both chambers ship the partisan *shape*; the who-voted-how list is the v2 expand) and the `/amendments` aggregate "voted" cut. Docs: HO 534. **Also notes now run through HO 533.**

**Also (HO 535), the participation *sort* on the /members roster — the ranked-list twin of the 527 dotplot, no theme-% change.** The banked participation sort/rank (the constraint the HO 523/525 carve-out was written for) — the "who misses the most votes" ranked list, complementing the HO 527 dotplot's distribution. Shipped `b565c13`. Adds a **MISSED** segment beside VOLUME / PASS RATE that orders the roster by each member's **119th missed-vote rate** (`SUM(not_voting)/COUNT(*)` over `member_votes`, `HAVING COUNT(*) >= PARTICIPATION_FLOOR=50` — the identical dotplot/B1 metric). **No new column — the shared `.mc-row` grid stays untouched:** `.mc-row` backs both `/members` AND `/lobbying`'s `FilingRow` (the filing rows fell back to the members grid), so widening it would reshape the lobbying rows; instead the existing **RATE column repurposes** to the missed-rate when the missed sort is active (amber vs passrate's green, `—` for null, the header swaps RATE→MISSED), leaving `.mc-row`/`.mc-r-num`/`/lobbying` byte-identical. **The delegate carve-out adapts HO 527 to a roster:** the dotplot *drops* the 6 non-voting delegates; a list can't drop members, so they're carved from the **ranking** (rate exposed NULL via the territorial predicate `chamber='house' AND state IN ('DC','AS','GU','MP','PR','VI')` → sort last + `—`, disclosed "non-voting delegates unranked") but kept in the list — same population-correctness principle, different mechanism. Threaded into **both** `getMembersRanked` (full list) and `getCommitteeRoster` (scoped view) via a shared `participationAggCte` + a shared `MISSED_CARVE_EXPR`, both gaining a **`votes` cache tag** (they read `member_votes` now, else they'd only refresh on the 86400s backstop — the `getChamberParticipationContext` precedent). **One spec bug the probe caught (oddities):** the handoff ordered by the bare `missed_pct` column, but SQLite resolves an unqualified column *inside an ORDER BY expression* against the input tables (`part_agg`'s raw rate), NOT the carved SELECT alias — so delegates (Radewagen 82%, Plaskett 50%) sorted to the top while displaying `—`; fixed by repeating the carve in the ORDER BY via the shared `MISSED_CARVE_EXPR`, re-probe confirmed. **No probe HO** (the missed-rate is the dotplot's own cheap ~536-row aggregate), but the build's throwaway probe verified the ranking: **TOP by MISSED = Wilson 29.0% → Hunt 24.6% → Kean 22.8%** (all voting members, matching the HO 527 dotplot tail), **BOTTOM = the 6 delegates + below-floor Graham showing `—`**. **No theme-% change — an enabling surface on `/members`; Member depth holds at 100** (the HO 425/527 dotplot precedent). **Banked:** a **career missed-rate sort** (its NULL-coverage set differs — the reason the dotplot's career toggle is banked) and the **career chamber-median context** on the hub (still the deliberate 4-stat-hero hold). Docs: HO 536. **Also notes now run through HO 535.**

**Also (HO 537), the `/amendments` "voted" cut — the last banked piece of the `recordedVotes` gap, no theme-% change.** HO 530/532/533 rendered amendment votes on the bill hub; the corpus surface still described the whole pillar through `latest_action_text` and couldn't say how many amendments actually drew a recorded floor vote. Shipped as four commits + a duplication fix, gated by a Step-0 cost/correctness probe. **STEP 0 (`amendments-voted-cost-537.ts`, `19a412e`):** the HO 461 inline cost-gate — 8 measurements. The equivalence gate (materializer set === live parse, per bill) PASSED; feed EXISTS EXPLAIN clean (composite-PK seek, no new index); containment clean (voted ⊆ acted, 0 with NULL action); tiebreak inert (0 same-date pairs). **Measurement 8 — a completeness detour that changed the design:** `votes` is append-only, so HO 529's "114 SAMDT" couldn't have fallen to the 97 the anchored matcher finds: of **161** senate votes that mention "Amdt", **97 are up-or-down** ("On the Amendment"), **64 are procedural** (`On the Motion to Table / Cloture Motion / Decision of the Chair S.Amdt. N`, + 50 budget-waiver `Amdt. No. N`). The pre-authorized "widen to capture the resolving residual" was **REJECTED on the data**: resolving an S.Amdt number is necessary, not sufficient — a procedural vote's yea/nay is on the MOTION, not the amendment (a `Motion to Table Failed` leaves the amendment ALIVE; `119-samdt-2310`/`4421` were WITHDRAWN, `119-samdt-2360` was ADOPTED 51–50 separately), and 3 of the 14 resolving residual already carry an anchored decisive vote a widen would double-line. **The anchored matcher is correct BY DESIGN** — it isolates the one class whose tally is renderable as support for the amendment. So: no widen, build as specced (oddities). **C1 (data, `92454f5`):** materialize the Senate links into `amendment_votes`, making it the single symmetric source of truth across chambers (House was HO 532's walk). Pure DB recompute (no API) — one `db.batch` delete-then-insert scoped by a `votes.chamber='senate'` join (not an id prefix), returning a real `changed` delta so the cron flushes `votes` only when the set moves (the recompute always rewrites, so row-count can't gate honestly). The Senate parser + LIKE moved to a **leaf `lib/amendment-vote-key.ts`** so the request-time parse and the sync-time materializer share ONE matcher (the `NEWS_ARTICLE_KEY_SQL`/`MISSED_CARVE_EXPR` anti-drift shape; leaf-safe so the `scripts/` materializer never drags `next/cache`); `getBillAmendmentVotes`' Senate path is byte-identical. A `--senate` CLI mode + a cron materialize step riding **before** the House walk (cheap-first, so a starved walk can't staleness the Senate links). **C2 (refactor, `acde5de`):** `VoteLine`/`voteVerb`/`dispositionColor` extracted to a shared `components/AmendmentVoteLine.tsx` (the corpus feed is the third consumer); byte-identical move verified by character-diff + `tsc` + build. **C3 (surface, `cb551c4`):** `getAmendmentsSummary` voted block (canonical/latest vote per amendment via `ROW_NUMBER` → `deriveDisposition`, the `list[0]` hub rule); `getAmendments` gains `voted` as a third disposition (EXISTS over resolved links) + page-scoped vote hydration (`hydratePageAmendmentVotes`, both chambers off the materialized table, party split FETCHED so the HO 533 zero-split guard reads true, plain `AmendmentVote[]` so `unstable_cache` round-trips clean); `AmendmentRow` renders the shared `VoteLine` with an authoritative-from-`votes.result` dot; the page gains a recorded-votes readout (authoritative agreed/failed), a `Voted` chip, a blurb chamber-contrast, and the exclusion-disclosure line. **Both cached fns gained the `votes` tag** — they read `votes`/`member_votes` now, so tags must follow the tables a query READS, not the pillar it belongs to (the HO 535 precedent; oddities). **Duplication fix (`c35837e`, own commit):** the C3 blurb repeated "187 drew a recorded floor vote" + "97 Senate · 90 House" verbatim with the readout; blurb now asserts qualitatively (claim), the readout owns the numbers (evidence) — the HO 509 double-print precedent. **Prod-verified on the cache-hit path (corpus at this snapshot 6,971 filed · 625 acted — HO 461's 6,788 was its own snapshot, left as written):** voted **187** (`42 agreed / 145 failed / 0 other`, `97 Senate / 90 House`); `?disposition=voted` 187 / `&type=HAMDT` 90 / `&type=SAMDT` 97; bare feed the full corpus (not 0/187 — the empty-`IN` guard holds); and the **bill-hub regression gate held exactly** — `119-hr-1` 13 lines, `119-hr-8800` 19 tallies / 18 splits — proving the leaf swap + `VoteLine` extraction didn't regress the hub. **Correction of the HO 529 reading (not a retro-edit of its block):** the "114 SAMDT" it recorded was procedural-inclusive; the genuine up-or-down count is **97** (oddities). **No theme-% change — a v1 cut on an existing pillar (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 all held their bar); the combined `Lobbying / amendments / nominations` bar holds at 92:** this ships the aggregate "voted" cut both 519 and 530/533 banked, discharging the `recordedVotes` gap's *aggregate* piece, but the **full per-member roll-call drill stays banked — this HO does NOT discharge it** (both chambers ship only the partisan shape; the who-voted-how list is the v2 expand) and a **procedural-outcome surface is newly banked** (needs a motion-aware model — invert tallies, distinguish cloture from adoption, suppress when an anchored vote exists). **Numbering — the 535 → 537 pointer jump skips no block:** HO 536 was the doc sweep *for* HO 535 (docs-only sweeps are owed no roadmap block — the HO 506 precedent), so a future reader should not hunt for a missing 536 block. Docs: HO 538. **Also notes now run through HO 537.**

**Also (HO 539–540), the `/vote/[id]` roll-call entity page + two residual cleanups — no theme-% change.** **HO 539 (residuals), briefly.** The `/amendments` blurb stopped asserting "near-evenly" (a hardcoded judgment on interpolated numbers — the HO 538 WATCH) and now states the contrast as **two percentages** — filings 96% Senate against recorded votes 52% Senate — guarded on `voted > 0`: self-updating prose with no adjective to rot, and no repeat of the readout's counts (which is why restoring the raw numbers wasn't the fix — it'd reopen the HO 509 double-print). Commit `588bed0`. Separately `df155de` wrote the **review-ref route** into SKILL and widened its scope beyond SKILL edits to **append-only roadmap blocks** (read-before-land text). **HO 540, the substance.** The banked per-member roll-call drill, built as a **page** not an expand-in-place: the fork was A (expand on `/bill/[id]`) vs B (a `/vote/[id]` entity page), B chosen. The argument is the HO 501 one — a roll call is an entity CBT already stores (`votes` at 1,513 rows with full positions in `member_votes`) whose data surfaced only as a fragment of other pages (a tally on the member hub, a split under an amendment). The corroborating find: **`getVotesByBill` existed, cached, with ZERO consumers** — the query for a bill's own passage votes had been written and never wired, the shape of a missing entity page, not a missing section. Three commits: **`41df8ed`** query layer (three NEW siblings — `getVoteById` reusing `VOTE_SELECT`/`rowToVote`, `getVoteMemberPositions`, `getVoteAmendmentContext` — never widening the shared SELECT), **`60d8c77`** the page + `VotePositionList` (flat sectioned, not `RecordTabs`), **`068dc7e`** inbound links (the tally → `/vote/[voteId]` on BOTH `/bill/[id]` and the `/amendments` feed via the shared `AmendmentVoteLine`; the member-hub roll number → `/vote/[id]`) + breadcrumb (`Legislation \ Votes`) + `pathToNavKey → "feed"`. **Step 0 (`vote-detail-cost-540.ts`):** House worst case is a **439-row list at 694ms cold** on the `member_votes` PK seek + the members PK join, so no index warranted; **4** votes carry a tally with zero positions; **890 of 1,513** votes carry `bill_id`, 623 don't, and **97** of those resolve via the `amendment_votes` link table. **Three decisions for the permanent record:** (1) **amendment context is fetched UNCONDITIONALLY, not as a null-`bill_id` fallback** — the Step-0 split showed a null-gate would show context on the 97 Senate amendment votes (no `bill_id`) and hide it on the 90 House ones (carry `bill_id`), the exact chamber asymmetry HO 537 spent its arc removing, reappearing in a different column (the HO 529 question-text asymmetry showing up again in `bill_id` presence). (2) **the party split is structural, not incidental** — `member_votes.bioguide_id` is deliberately not FK'd to `members`, so an unmatched row is expected; `getBillAmendmentVotes` INNER JOINs + skips unresolvable parties while the page LEFT JOINs, so the page applies the SAME skip-unless-D/R/I rule and the two agree regardless of join type (unmatched members still render — bioguide id, dim, unlinked, the `BillAmendments` sponsor idiom — because they did vote). (3) **two empty states, not one** — tally>0 with no positions is **lag** (4 votes, newest `house-119-2-269`, self-heals next votes sync); tally=0 with no positions is a **by-name/procedural** vote (`house-119-1-2`, Election of the Speaker) where nothing is pending and nothing ever arrives, and the zero tally is suppressed ("0–0" on a Speaker election is meaningless). **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** this discharges the per-member roll-call drill banked at HO 530/533, but it's a v1 entity surface (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 / 537 all held). **`/vote/[id]` is CROSS-CUTTING** — it serves bills, amendments, nominations AND the member hub — so it doesn't sit cleanly under any one bar; whether roll calls warrant their own line is for the next full re-derivation, not this sweep. Docs: HO 541. **Also notes now run through HO 540.**

**Also (HO 542), the `/bill/[id]` VOTES tab — the bill's own passage/procedural roll calls, no theme-% change.** The HO 541 sweep flagged `getVotesByBill` as **written, cached, and UNCONSUMED** — `/bill/[id]` rendered its *amendments'* votes (HO 530/532/533) but never the bill's *own* passage / motion-to-recommit / cloture / motion-to-commit roll calls, because the query for them had been written and never wired (the same orphan HO 540 read as the shape of a missing entity page). HO 542 wires it into the HO 510 `RecordTabs` box as a VOTES tab, each row → `/vote/[id]`. **STEP 0 (`bill-votes-overlap-542.ts`, `630833c`):** the one real unknown was overlap with the Amendments tab — `getVotesByBill` returns *every* vote carrying the bill's `bill_id`, and House amendment votes carry `bill_id` (HO 532), so some of its rows are already shown under Amendments. Read-only measurement: **890** bill_id-carrying votes across **467** bills; **92** are amendment votes (**all 92 House-linked via `amendment_votes`; 0 Senate-pattern**) → **798 own votes across all 467 bills** (p50 **1** · p90 **2** · max **24**), overlap concentrated exactly on amendment-heavy bills (`119-hr-8800`: 19 of 21; `119-hr-1`: 0 of 24). **The finding that shaped the build:** the exclusion set is **House-link only** — Senate amendment votes are question-matched and **don't carry `bill_id`** (the HO 540 bill_id-asymmetry, the HO 529 question-text asymmetry once more), so they never enter `getVotesByBill` and the belt-and-suspenders Senate-pattern check the handoff pre-authorized is a provable no-op (0 leak). So the exclusion is a plain `Set<string>` over the already-fetched `amendmentVotes` map values — no `lib/amendment-vote-key.ts` import in the page (oddities). **The build (`61cacd2`):** `getVotesByBill(bill.id)` added to the page `Promise.all`; `ownVotes = billVotes.filter(v => !amendmentVoteIds.has(v.id))`; a NEW `components/BillVoteRow.tsx` (server summary row — tally · date · question+result · `#{rollCall}` → `/vote/[id]`; **NOT** `MemberVoteRow` [member-scoped chip] nor `VotePositionList` [the heavy per-member breakdown] — the bill hub has no per-member context; the "new sibling, don't widen" discipline), with the **HO 540 tally-suppression** on an all-zero roll call (a by-name/procedural vote where "0–0" is meaningless). The tab inserted **between amendments and lobbying** (passage = the culminating floor action on the whole bill; amendments act on parts, lobbying is orthogonal-last), **omit-at-zero** like every other bill-page tab, `initialKey` untouched (own-vote counts are small, so Votes never steals the initial tab on a heavy bill; a passage-only bill opens on Votes). CSS **additive under `.bvote-*`** — no `.vote-row`/`.rec-*`/`.mc-*`/`.bxp-*`/`.bdp-*` edit; one deliberate deviation from `MemberVoteRow` — the roll# stays visible on mobile because here it's the *sole* link to the vote page. **Discharges the backlog QUEUED "bill-hub passage-votes section" (HO 418)** — see DONE. **No theme-% change — a v1 cut wiring an existing orphaned query onto an existing pillar (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 / 537 / 540 all held); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Prod-verified on the cache-hit path:** `119-hr-8800` Votes **2** (passage + motion to recommit) with Amendments still **19** (21 unique `/vote/` links = 2 own + 19 amendment — no double-count, Amendments byte-identical); `119-hr-5371` Votes **21**; `119-hr-3034` (introduced-never-voted) **no VOTES tab**. **Owed eyeball (WATCH):** the tally-suppression (`bvote-none`) path is tsc-verified but **unexercised** — none of the three samples had a by-name own-vote — render-confirm on the next one (the HO 540 empty-state WATCH posture). Docs: HO 542 (this sweep — backlog DONE + WATCH, oddities, the SKILL `getVotesByBill` reconciliation). **Also notes now run through HO 542.**

**Also (HO 543–544), the `/lobbying` corpus fbar's sort segment (RECENT ⇄ VOLUME) + bill-linked toggle — the first of the HO 486 fbar cuts, no theme-% change.** The HO 486 chrome restructure deferred the fbar's real filters (each needs a query, not markup); HO 544 ships two of the three (sort + bill-linked), gated by a Step-0 cost/shape probe. **HO 543 (`aabc8a8`, read-only probe).** Priced the sort × filter over the corpus feed and read GO — but on the **cheapest component**, which is exactly the trap the build then walked into (oddities). **HO 544 (`e640a0d`).** `getRecentFilings` gains `sort` (`"recent"` default / `"volume"` = per-filing activity count, "meatiest filings") × `billLinked`. **The default path (recent + unfiltered) is the byte-identical pre-544 SQL** — same `idx_lda_filings_dt_posted` ordered walk, so the untouched common case can't regress. **The mid-build catch (the load-bearing find):** the 543 probe timed VOLUME as a bare `GROUP BY filing_uuid` over `lda_activities` (index-only, **340–440ms**) — but the shipped feed has to join back to `lda_filings` to hydrate the row, and the naive shape (LEFT JOIN from `lda_filings`, order by the joined count) planned an **AUTOMATIC COVERING INDEX on the materialized agg → ~18s cold** (a build-time EXPLAIN caught it). The fix **drives from the agg (INNER JOIN)**, PK-seeking into `lda_filings` — the ~295 zero-activity filings the INNER JOIN drops have `activity_count 0`, sort dead-last past the 40-page clamp, so the visible feed is identical to a LEFT JOIN (a `do NOT "fix" this to a LEFT JOIN` comment guards it). The `dt_posted` hint is **dropped** on VOLUME (order is by count, not date) and **kept** on recent+linked. **The required addition on review — the HO 440/448 guard:** the read + hydrate are wrapped **try/catch → `{items:[], page, pageSize}`**. VOLUME's ~7s worst cold miss sits close to the 10s `DB_REQUEST_TIMEOUT_MS` wall (the 543 probe saw remote Turso vary up to ~5×), and this is the ONE live LDA read on `/lobbying` — an abort would 500 the WHOLE page (rollup/rail/crosswalk/firms), not just the feed, so it degrades to an empty feed instead (the same guard every LDA surface has carried since 440; commented so it isn't stripped as defensive noise). Controls are **unscoped-feed-only** (carried across pagination + expand) and **hidden when scoped** (`?issue=` serves the precomputed `drill.recent` sample — the HO 437 >25s per-issue wall — which can't be re-sorted live). **Prod-verified on the cache-hit path (2× per URL):** all 4 combos 200 (VOLUME cold **7.08s** → warm 1.83s — right at the WATCH figure; volume+linked 2.84s → 0.36s); unscoped shows the VOLUME control, `?issue=TAX` hides it; `?page=999` clamps the feed byte-identical to page 40 (only the echoed `?page=` in the expand `toggleHref` differs). **No theme-% change — a v1 filter cut on the existing LDA pillar (437/439–440/442/444/486 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Two records for the ledger (docs, HO 544 this sweep):** the **oddities lesson** — *a probe that measures an aggregate in isolation has not measured the feed query that joins back to hydrate rows; cost-probe the shape you'll actually ship, joins included, not its cheapest component* (a sibling of the HO 340/437 "clean EXPLAIN says nothing about cold latency" entry — cross-referenced); and the **WATCH** — VOLUME cold-miss headroom (~7s vs the 10s wall, corpus grew 108,707 → 112,411 this arc, sort×linked took the cache-key surface ~40 → ~160 keys so cold misses hit ~4× more often), **promote to a fix if it ever aborts** (the fix is a precomputed `activity_count` column on `lda_filings` — the 543 probe's noted optional upgrade, turning VOLUME into a ~45ms indexed walk, and the natural home if search wants the same). **The third fbar cut — registrant/client SEARCH — stays QUEUED as its own FTS follow-on** (a `LIKE` over 112k rows full-scans; wants an `lda_filings` FTS5 index, the `bills_fts` shape). Docs: HO 544 (this sweep — SKILL `getRecentFilings` + `/lobbying` fbar, oddities cost-probe lesson, backlog VOLUME WATCH + the fbar-cut QUEUED update). **Also notes now run through HO 544.**

**Also (HO 545), the `/lobbying` expand-panel description line-clamp + SHOW MORE — clears the banked QUEUED line-clamp item, no theme-% change.** The LD-2 free text rode a fixed `max-height:140px` scroll box (buries the long tail: p99≈1.8k / max 24.4k chars, HO 485). HO 545 (`54d2fd3`) clamps it to 4 lines closed with an explicit **SHOW MORE / SHOW LESS** reveal; the open state is a **bounded 420px scroll** so even the 24k monster can't stretch the HO 492/493-tuned page. The toggle rides a dedicated `<button>` in a small client island (`FilingDescription`, the `FillerWatchStrip` HO 348 precedent — the SOLE client boundary out of the server `FilingExpandPanel`), **NOT** `<details>/<summary>`: these are descriptions researchers copy, and select-to-copy toggling the panel is an active regression. CSS additive under `.lob-exp-desc*` only — no `.lob-*`/`.mc-*` layout, `PAGE_SIZE`, rail-bound, or `.lob-sec-scroll` retune (the three HO 492/493 numbers are tuned against each other and stayed put). **The verification find (the load-bearing one, oddities):** the handoff flagged the risk that `white-space: pre-wrap` might *defeat* the clamp — it did NOT (CDP: closed box `clientH:88` = exactly 4 lines, `scrollH:6280`, clipped; the clamp engages with pre-wrap). The REAL risk was the **inverse** — pre-wrap makes the 4-line clamp count *hard newlines* as lines, and CDP-measuring the column at **56 chars/line → 4 lines ≈ 224 chars** proved the handoff's ~280-char threshold + bare-clamped-below-threshold would **silently clip** a below-threshold description (a 224–280-char text, or one with a mid-text break, wrapping past 4 lines with no reveal control). Fix: **below threshold renders UNCLAMPED (the `--open` state), not the bare clamped base** — clip-proof regardless of column width or break placement; short text never reaches the 420px bound, so it renders identically to a short filing today. The char threshold (now 250) is thereby demoted to a purely cosmetic "when to OFFER collapse" line, not a correctness dependency (a hard-newline count >4 also triggers the affordance). **Prod-verified on the cache-hit path (2× per URL, matching the local CDP exactly):** the 31-activity / 21,633-char monster (`cd77f510…`, on volume page 1) renders **27 clamped-with-button + 4 short `--open` boxes** — both branches on one filing; SHOW MORE → a bounded 418px scroll (page grew **+330px, not ~6000**), SHOW LESS restores; per-activity toggles are independent; short descriptions show fully with no button and no clip. The collapsed default `/lobbying` page (no `?expanded=`) is untouched — the CSS lives only inside the expand panel. **No theme-% change — UI polish on the existing LDA pillar (the 437/440/442/444/486/544 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** Docs: HO 545 (this sweep — roadmap block + backlog QUEUED→DONE + the pre-wrap/hard-newline clamp oddity). **Also notes now run through HO 545.**

**Also (HO 546–547), the `/lobbying` registrant/client search — the LAST HO 486 fbar cut, FTS probed + rejected, no theme-% change.** The final open fbar control. **HO 546 (`debb9dd`, read-only probe) retired the backlog's premise.** The close criterion assumed search needed an `lda_filings` FTS5 mirror of `bills_fts` — the probe found that **wrong on measurement** (the "a backlog line's stated mechanism is a claim — probe it" rule, HO 512/529 class): LDA names avg **25 chars**, so a `%term%` substring scan of 112k rows is **~700ms** (the `bills_fts` premise was scanning long `title`+`summary`, which doesn't transfer to short names). NULL-id/name share is **0%**, distinct registrants 5,183 / clients 27,115 → FTS would **22×-over-index** the repeated names AND add a delete+reinsert trigger to the `FLUSH_AT=100`-tuned sync write path — **all cost, zero benefit. FTS REJECTED; the fallback if `%term%` ever climbs is a distinct-name lookup table, NOT FTS** (recorded so it isn't re-proposed). **HO 547 (`5c73643`) ships Fork B leanest — a live `LIKE`, no new DB object at all.** Two amendments to the probe's rec, both load-bearing: **(1) substring `%term%`, not prefix** — "Boeing" must match "The Boeing Company" (client names lead with articles, firms with surnames); prefix would read as broken. **(2) a REQUIRED pre-build VOLUME×search measurement** — the probe only priced RECENT; VOLUME is the agg-driven join carrying the HO 544 ~7s WATCH. Measured cold: **VOLUME+search 7,792ms** (a TEMP B-TREE over the whole materialized agg — the LIKE defeats the LIMIT short-circuit, and search is *usually served cold* since `q` makes cache keys unbounded) vs **RECENT+search 34–697ms** (a plain `SCAN` under the forced `idx_lda_filings_dt_posted`, no MULTI-INDEX-OR — the OR-cold-stall class HOs 405/407/479/480 did NOT bite because the name columns are unindexed). So **search FORCES RECENT** — `getRecentFilings` overrides `sort→recent` when `q` is set, and the UI disables the VOLUME segment (a page-local disabled variant; the shared `SegmentedToggle` is not widened). **Build:** `getRecentFilings` gains `q?` (substring, % / _ escaped via `ESCAPE '\'`); the **default path (recent, unfiltered, no q) stays byte-identical**; a searched set has no blob total, so the pager rides a **live `COUNT(*)`** with the same predicate (≤67ms), run in **parallel (`Promise.all`) inside the HO 440/448 guard**; `MAX_FEED_PAGES=40` clamp kept; the fbar carries a `form` GET (`?q=`, server-driven — matches the page's `<Link>` idiom) beside the HO 544 controls, carried across pagination + expand, hidden when scoped, count line shows the term, zero matches → `No filings match "term"`. CSS additive under `.lob-fbar-*`. **Prod cache-hit verified (2×):** default byte-identical; `boeing`→results **via substring**; zero-match + literal `%`/`_`→`No filings match` (**escaped**, matched nothing not everything); search+linked composes; search+`sort=volume`→forced-recent (VOLUME disabled); `?issue=`→box hidden + q ignored; page-999→clamped — all sub-second. **No theme-% change — a v1 filter cut on the existing LDA pillar (437/440/442/444/486/544 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** This **fully closes the HO 486 fbar-cuts item** (sort + bill-linked + search all shipped). Docs: HO 547 (this sweep — roadmap block + backlog fbar-entry struck to DONE + FTS-rejected recorded + the search-forces-RECENT / short-names-≠-bills-LIKE oddity + SKILL `getRecentFilings`/`/lobbying`). **Also notes now run through HO 547.**

**Also (HO 548), harness coverage extension — a cache-hit detector + interaction coverage for five post-472 arcs, no theme-% change.** Test-only, two commits (`88f669f` smoke · `b38f530` fit-finish); findings-only, zero fixes — the HO 472 posture. **Two gaps closed.** **(1) The crawl never exercised a cache HIT.** `smoke.spec.ts` did one `goto` per route and `e2e-prod.yml` triggers on `deployment_status`, so every unattended prod crawl ran inside the post-deploy cache-*miss* window — structurally unable to see the HO 533 failure class (a cached `unstable_cache` value that round-trips wrong — a `Map`→`{}` — and 500s only on the hit). Each route is now hit **twice** (fresh `goto`, not `reload`); collectors are marked after hit 1 and the second hit asserted on the slice past the mark (attribution), one log line carrying both (`hit1=200 … | hit2=…`); hit-2 document status is asserted **hard**. `/vote/[id]` (HO 540, previously in NO `ROUTES` list) added. **(2) Five interactive arcs had no coverage since HO 472.** `fit-finish.spec.ts` gained six areas with provenance: **RecordTabs** (509/510 — tablist, mount-all-panels-carry-`hidden`, scroll-reset, open-row-survives-swap, and the opposite empty-state policy the two consumers deliberately disagree on: member **dims** `data-empty`, bill **omits**), **`/vote/[id]`** (540 — reached via the bill VOTES-tab link *and* cold direct-nav), **the no-double-count invariant** (542 — every `/vote/` link unique page-wide; never-voted bill omits the VOTES tab), **`/lobbying` fbar** (544/547 — sort/linked rebase+compose, search-forces-RECENT with VOLUME inert, clear, scoped-hides), **FilingDescription** (545 — SHOW MORE toggle, the 24k-char monster's page growth bounded <600px, per-activity independence), **`/news` rails** (515/517 — single selection spanning the topic + member groups). **Five seeds, picked from prod Turso** (query per seed in the ship report): `VOTE`=`senate-119-2-207` (Senate roll call w/ positions + a bill link — nomination votes rejected since the `/bill/` back-link needs `vote.billId`), `VOTE_BILL`=`119-hr-8800` (2 own + 19 amendment = 21 unique `/vote/`), `NOVOTE_BILL`=`119-hr-3034`, `TABS_MEMBER`=`A000055` (amendments=0 + trades=0 → dim tabs), `FILING_UUID`=`cd77f510…` (31 activities, 27 clamped + 4 short). **STEP 0 falsification (HO 549, nothing committed) — the detector was proven able to fail before any doc recorded it as protection:** re-injecting the exact HO 533 defect (hand-applied on HEAD — `getBillAmendmentVotes` returns a `Map`, `BillAmendments` reads `.get`) and running the smoke crawl against a local `next start` produced **`[bill-detail] hit1=200 … | hit2=500 … bad=1 console=1 pageErr=1`** on `119-hr-8800` — the detector fired; the scratch edits were reverted, tree clean. That is what makes 548's clean sweep an instrument reading rather than a structural green. **Findings: NONE — stated as a real result, not an absence:** all 36 routes swept `hit1===hit2===200` (zero divergence), and every one of the six interaction invariants held. **One design detail worth keeping:** the bill-hub scroll-reset is **guarded-and-logged, not skipped** — its highest-count panel fits the 430px body so there's no scroll to reset (the member hub, whose Bills panel overflows, exercises it). An earlier `expect.soft(scrolled>0)` correctly surfaced the vacuous case but reds a *committed* test permanently, the wrong shape; the guard-plus-`console.log` keeps the non-exercise visible on stdout (not a silent HO-503 skip) while staying green. **No theme-% change — harness work on already-counted surfaces; no bar moves.** Docs: HO 549 (STEP 0 + this sweep — roadmap block + backlog fbar-line-struck + 548 tombstone + doubled-crawl WATCH + the extended cache-hit oddity + SKILL double-hit convention). **Also notes now run through HO 548.**

**Also (HO 550–552), the House amendment-vote mislink fix (P1) + the motion-aware-surface GO probe — a correctness fix + a probe on an existing pillar, no theme-% change.** **HO 550 (`9383b9f`, read-only probe)** priced the banked HO 537 M8 surface (the 64 Senate roll calls that touch an amendment without being its up-or-down vote) across six measurements — and its **M5, run first, found a P1 live on prod**: `lib/amendment-votes-walk.ts` (House, HO 532) links **every** `recordedVote` off a HAMDT's `/actions` with no question filter (the mirror of the Senate materializer's anchored `SENATE_AMDT_Q`), so two amendments — `119-hamdt-40`→`house-119-1-187`, `119-hamdt-175`→`house-119-2-122` — were rendering an **"On Ordering the Previous Question"** procedural tally through the amendment-framed `VoteLine` as the amendment's own outcome, green agreed dot and all from `deriveDisposition("Passed")`. Ordering the previous question ends debate; it decides *whether* the House votes, not what — wrong on the number and wrong on the outcome. Per the handoff, the fix outranked the surface. **HO 551 (`c03f1b7`) fixed it — predicate derived from the population, not the two exceptions.** STEP 0 enumerated **all 92 House-linked `amendment_votes` questions**: 90 decisive (**89** "On Agreeing to the Amendment" + **1** ", as Modified") + **2** procedural — vocabulary CLOSED, so the gate is `HOUSE_AMDT_QUESTION_LIKE = "On Agreeing to the Amendment%"` (the `%` covers the modified/amended suffixes), added to `lib/amendment-vote-key.ts` **next to the Senate one** (the leaf module exists precisely so the two chambers can't drift — and the asymmetry survived *because* the House had no counterpart there). The walk constructs `vote_id` from the payload (no question text in hand), so the filter rides a **JOIN to `votes` at link time** in the INSERT's LIKE — one SQL authority, no code-side copy. The two live bad rows were deleted one-time, **scoped to the enumerated procedural set** (exactly 2, dry-run confirmed); both offenders keep their real "On Agreeing to the Amendment" vote, and the `119-hr-8800` 19-amendment control stayed byte-identical (the over-filter check). **HO 552 (`20770e3`) closed a self-inflicted edge the 551 gate opened.** The gate collapses "votes row not synced yet" and "synced, wrong question" to the same no-link, but they need OPPOSITE handling: a wrong-question miss is *settled*, an ABSENT votes row is *pending* — and 551's own header comment falsely claimed the absent case "self-heals on the update_date re-walk," which the queue predicate (`update_date > amendment_vote_walked_at`) **cannot deliver** (a just-voted HAMDT already spent its `update_date` bump getting queued this run, so stamping it drops the decisive link **permanently**). Fix: an existence `SELECT id FROM votes WHERE id IN (…)` before the link loop; if any constructed `voteId` is absent the HAMDT is **left UNSTAMPED to retry** (`deferred++`, `continue`) — the same non-stamp discipline `fetchErrors` already used — and the comment was corrected. Existence-check chosen over the handoff-suggested `SELECT id+question` deliberately, to keep decisiveness the single SQL LIKE (no second predicate copy). **STEP 0 falsification (HO 553, nothing committed):** the `deferred` branch had never executed (both 552 verify runs saw `deferred=0`), so — the HO 549 discipline — it was proven able to fire before any doc recorded it as protection. A scratch perturbation of the constructed `voteId` produced `deferred=1`, HAMDT unstamped, `remaining=1` **pinned across 3 runs** (~175ms / one `/actions` GET each — the permanent-gap signature); restoring the code, the **retry completed** (`walked=1`, stamped, decisive `house-119-1-188` link restored). Scratch reverted, tree clean, nothing committed. **Call: permanent retry is acceptable, no bound** — ~228 HAMDTs, one GET per stuck row per run is negligible, and the only alternative (stamp after N attempts) reintroduces the exact permanent-drop this fix removed; the pinned `deferred` counter makes a stuck row visible instead of silent. **The probe's motion-aware-surface verdict is GO (banked, not built here):** vocabulary CLOSED at 4 classes (table 9 / cloture 4 / chair 1 / waiver 50, **tail 0**), **100% number resolution** (64/64; the waiver form's `Amdt. No. N` confirmed the SAME Senate `S.Amdt` namespace by 42/43 sponsor-name match, not a parallel numbering), **~55 fate-bearing lines** after setting aside the 4 already-anchored (M3: **60** would gain a line they don't have today) and 5 orthogonal cloture/chair, **polarity validated 54/56 (96%)** — and against **FULL coverage** (64/64 residual amendments carry `latest_action_text`, because a procedural floor vote always generates an action, NOT the corpus-wide ~8.6% that would make a high rate on tiny N meaningless), the 2 misses both "Motion to Table Failed" edge cases (`samdt-3946/3947`, "fell when SA … fell"). Cold ~162ms (351ms at probe time; remote Turso varies). **One unreconciled arithmetic smell recorded for the build handoff, not papered over:** M4's exclusion math is `64 − 5 orthogonal − 2 withdrawn = 57` expected clean pairs but N came back **56** — an off-by-one that doesn't move the 96% verdict but a build shouldn't inherit it silently. The surface stays a separate scoped build (invert table/waiver polarity, render cloture/chair as orthogonal not a fate, suppress the 4 already-anchored). **No theme-% change — a correctness fix plus a probe on the existing amendments pillar (the 437/439–440/442/444/448/450/461/537/540/542 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** Docs: HO 553 (this sweep — roadmap block + backlog P1 tombstone + motion-aware BANKED→QUEUED + the `deferred` transient-vs-pinned WATCH + oddities the stamp-on-exit-converts-transient-to-permanent rule + the HO 542 chamber-asymmetry M5 extension + SKILL the walk gate / `deferred` / non-stamp discipline). **Also notes now run through HO 552.**

**Also (HO 554–555), the motion-model STEP 0 + the `deriveDisposition` kill-vocabulary widening — a probe + a correctness fix on the amendments pillar, no theme-% change.** The motion-aware surface (GO'd HO 550, QUEUED) got its STEP 0 — and the probe's biggest find wasn't about the surface: it falsified the framing that the disposition dot was already correct on the procedural residual, spawning HO 555 as a prerequisite fix that reshapes what the build still owes. **HO 554 (`be0fc4d`, read-only probe), four measurements.** M1 resolved the HO 550 off-by-one the 553 block said the build shouldn't inherit silently: the 57-expected-vs-56-measured clean-pair gap is `119-samdt-3849`, whose actual-side text "rendered moot" sits outside the probe's actualFate validation vocabulary — a **validation-coverage gap, not a render gap** (polarity holds; the row simply couldn't be scored). M2: motion-vs-actual DISAGREE=0 (no polarity halt) — but **DOT-OTHER=53/64**: on 83% of residual rows the motion implies a clean fate the muted prod `other` dot was missing, falsifying the §0 "the dot is already correct, the surface adds the vote line" framing. M3: motions-per-amendment MAX=1 — no multi-motion render ambiguity. M4: the residual select rides `idx_votes_chamber_date` (31ms/64 rows; the split fetch 253ms/297 rows) — trivially live-servable. **HO 555 (probe `c180054` + fix `2ce1d6c`), the widening — measured, not guessed.** The probe clustered the FULL `other` bucket (115/625 acted rows, top-30 normalized shapes hand-tagged) rather than patching the 3 examples M2 surfaced: the candidate rescues **80** — "ruled out of order by the chair" (47; the point-of-order kill after a failed budget waiver; anchored to the full phrase, not bare "out of order") + word-boundaried "fell" (fell with its parent / when cloture was invoked / when tabled). **Deliberately NOT added, still `other`:** withdrawn (the amendment survived the vote and was pulled later — genuinely neither), rendered moot (describes the cloture motion's fate, not the amendment's — the M1 row), cloture-not-invoked (orthogonal procedure); "stricken" is not a kill token. M2 measured **zero collision** with the 4-string `votes.result` vocabulary, so **one shared function holds** across both input families — no split. **The zero-flip gate HALTed on exactly one row, and the flip was accepted as a correction:** `119-samdt-3963` moves agreed→failed — the OLD dot was WRONG there (its text is "SA 3963 fell when …" with a trailing "agreed to" belonging to the subordinate clause, which the old classifier read), so the failed-family-FIRST ordering is now load-bearing for the subordinate-clause case too, not just "not agreed to" (oddities). **Corpus dots move: agreed 337→336 · failed 173→254 · otherActed 115→35** (corpus other 6465→6385); **the voted block is untouched (0 flips)** — the `?disposition=` coarse filter and the voted EXISTS bypass `deriveDisposition` entirely (M4 grep), so the fix moves dots + summary buckets only, never filter semantics. An **M5 watch instrument** was added to the probe post-fix — agreed-classified rows containing `when` — and reads **0**: SA 3963 was alone, no residual mis-read family (→ WATCH). **The verification produced its own lesson (oddities — the HO 533 rule sharpened):** the Vercel Data Cache survives deploys (HO 312), so a shipped VALUE change does not land on the first prod read — the first hit serves the STALE pre-deploy value and only triggers the SWR regen. "Hit 2–3 times" is a serialization-shape test; for a value change the rule is **confirm the served SHA first (`/api/version`), then hit until the value CONVERGES to the expected number and holds** — at 555 the lighter per-bill dot converged before the heavier `/amendments` summary. Local cache-hit double-hit first (hero 336/254/35 + 42/145; ruled-out-of-order rows red on `/bill` and `/members`; control `119-hr-8800` unchanged), then prod convergence. **The motion-aware build's value proposition is RESHAPED, not retired (QUEUED updated):** the dot now already carries the fate on the rescued rows, so what the build still owes is the **motion-framed vote line** (the tally + inverted polarity + verb — a tabling-agreed tally rendered as the kill it is), cloture/chair rendered as ORTHOGONAL not a fate, and suppression of the 4 already-anchored — the vote-line rendering was always the surface; the dot-correction half is done. **No theme-% change — a probe + a correctness fix on the existing amendments pillar (the 550–552 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Numbering — the 552 → 554 pointer jump skips no block:** HO 553 was the doc sweep for 550–552 (docs-only sweeps are owed no roadmap block — the HO 506/536 precedent). Docs: HO 556 (this sweep — roadmap block + backlog QUEUED-reshape/DONE/WATCH + oddities ×2 + SKILL verification-convention amendment + `deriveDisposition` vocabulary note). **Also notes now run through HO 555.**

**Also (HO 557), the motion-aware Senate amendment-outcome surface — the HO 537 M8 banked surface, shipped with its model corrected mid-build; no theme-% change.** The 64 Senate roll calls that touch an amendment without being its up-or-down vote (Motion to Table · Cloture · Decision of the Chair · budget waiver) now render a **motion line** — the motion, its tally, the party split, a drill link to `/vote/[id]`, and where the motion decides, the amendment's fate. Live at `bf820bd`; **60 amendments gain a line they didn't have** — 55 of them carrying a "→ amendment killed" clause, the rest tally-only (undecided or orthogonal), with the 4 anchored suppressed. **Anchored-among-killed is 0, and structurally so:** an amendment killed by a tabling motion or a failed waiver dies at that moment and never gets its own up-or-down vote, so every anchored amendment is one the motion left pending or only touched orthogonally. Suppression therefore never hides a fate clause — it only ever drops a tally-only line. **The arc's real content is that the STEP 0 gate fired and the model was wrong.** HO 554's M2 tripwire (dot vs implied fate, DISAGREE=0) had been measured against the **pre-555** classifier, and the probe pins a verbatim copy of `deriveDisposition` — so HO 557's first act (`7798997`, discharging the HO 556 WATCH on its first opportunity) was reconciling that copy to the shipped `2ce1d6c` version and re-running. **DISAGREE went 0→2**, both rows the exact pair HO 550's polarity validation had missed (`119-samdt-3946`/`3947`, "Motion to Table Failed" → "SA N fell when SA N-1 fell"): the tabling motion failed, so the model said *survived*; the amendment later fell with its parent, so the HO 555-widened dot said *killed*. **Both readings were individually true — the model was answering a different question than the dot.** `impliedFate` described the amendment's status **at the motion**; the dot describes its **outcome**. Those coincide in one direction only, and the arc had been reading a label coincidence ("survived" ≡ "agreed") as agreement. **The fix narrowed the model rather than patching the two rows** (`d213cb7`): `impliedFate` → **`motionFate(cls, outcome): "killed" | "undecided" | "orthogonal"`** — there is **no positive fate**. Tabling **agreed** → killed; waiver **failed** → killed; the opposite outcome of each is **undecided** (not tabled ≠ adopted; cleared the point of order ≠ agreed to), and cloture/chair are orthogonal always. Both fate-bearing classes are **kill mechanisms that decide in one direction only**. The rejected alternative is worth recording: suppressing the fate clause whenever it disagreed with the dot would have coupled the motion model to `deriveDisposition`'s vocabulary — so a future dot widening would silently change motion-line behavior — and would have hidden the modelling error instead of fixing it. **The re-gate is the contradiction shape that remains possible:** HALT if any row has `dot=agreed AND fate=killed`; measured **0** (`killed=55 · undecided=4 · orthogonal=5 · dot-agreed-in-killed=0`), and undecided/orthogonal rows assert nothing so they can't contradict by construction. **The build, three commits.** `568d58d` — a new leaf **`lib/amendment-motion-key.ts`** beside `amendment-vote-key.ts` (untouched), carrying the four anchored classifiers verbatim from the probe (table 9 / cloture 4 / chair 1 / waiver 50, **tail 0**, `null` for the tail so it's droppable by type), the number parse (waiver's `Amdt. No. N` = the same Senate `S.Amdt` namespace, HO 550's 42/43 sponsor match), and `motionFate`. **The rename is a type-safety device, not cosmetics:** `"agreed" | "failed"` is structurally assignable to `AmendmentVote["disposition"]`, so tsc would have allowed an implied fate into `dispositionColor`/`voteVerb` and printed "Agreed" on an amendment that merely wasn't tabled. `7027d10` — **`getSenateAmendmentMotions()`**, a corpus-wide **argument-less** cached sibling (64 rows total, so one cache key serves both consumers), two statements only, both HO 554 M4-measured: the residual select on `idx_votes_chamber_date` (**31ms/64**, no new index, no hint) and the `member_votes ⋈ members` split (**253ms/297**). It drops the probe's 64 per-row existence lookups (an unresolvable key is simply never read) and keys on **`votes.congress` per row**, not a `MAX(congress)` — multi-congress safe by construction. Distinct `AmendmentMotion` type, plain `Record` (HO 533), `try/catch → {}`, tags `["amendments","votes"]` (the `amendments` tag is consumer-coherence, not a FROM clause — noted in comment). `b40b462` — **`components/AmendmentMotionLine.tsx`**, a new sibling of `AmendmentVoteLine` (not a widened `VoteLine`, whose verbs are amendment-framed and wrong here), reusing the shared `partyColor` and the **HO 533 all-zero split suppression**; wired into `/bill/[id]` (the inner row in `BillAmendments.tsx` — *not* `AmendmentRow.tsx`, the HO 530 pathspec note) and the `/amendments` feed **page-scoped in `app/amendments/page.tsx`**, so `getAmendments` is untouched. **Suppression costs nothing and makes the two lines mutually exclusive:** the motion line renders only when the amendment has no anchored decisive vote, a check both sites already hold in hand — so `VoteLine` and `MotionLine` can never stack, and there is no ordering question. **No `+N earlier` counter** — M3 measured 1:1, so it would be structurally unreachable code documented as protection (the HO 549 discipline); the render maps the list instead, so a future second motion appears as a second line rather than hiding. **The dot never moves** — HO 555 already fixed it for these rows, and this HO adds evidence, not classification. **One measured over-claim, shipped deliberately (oddities + WATCH):** `motionFate(waiver, failed) = killed`, but M6 (`70a23ae`, added mid-build on the ruling) split the class **48 "ruled out of order" / 1 "withdrawn" (`119-samdt-5806`) / 0 neither** — so 5806 renders "→ amendment killed" against an action text saying it was *withdrawn*, contradicting HO 555's deliberate decision that withdrawn is neither. A 1-row over-claim against 48 correct ones doesn't justify narrowing, and the obvious fix (a token check on action text) is the dot-coupling the gate already rejected. **Numbering — the 555 → 557 pointer jump skips no block:** HO 556 was the doc sweep for 554–555. **No theme-% change — a v1 line on the existing amendments pillar (the 437/439–440/442/444/448/450/461/537/540/542/550–552 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** This **discharges the HO 537 M8 / HO 550 QUEUED surface in full**. Docs: HO 558. **Also notes now run through HO 557.**

**Also (HO 559–561), the primaries arc — a banked pair closed on measurement (neither built) + the SC special-primary ingestion path; no theme-% change.** *Nothing here was built to the spec that opened it:* the banked display item targeted code HO 333 had already orphaned, its re-poll twin described a mechanism that already existed, and the real hazard — a scrape silently substituting one contest's results for another's — was found by reading the sync path, not by either measurement. **HO 559 (`6db8fd0`, read-only probe) took the two-year banked pair and closed both without a build.** *Display half — moot, not built:* `buildPrimariesCartogram`, `PrimaryDistrictCard`, `PrimaryRow`, `primariesFill`, `PrimariesLegend` are all ORPHAN; `PrimaryMapCard` and the `variant === "primaries"` branches live only inside `CartogramShell`'s primaries fork, which never executes because the sole `<CartogramShell>` (`ElectoralBoard.tsx:103`) passes `variant="races"`. The item's build target was unreachable code, and `PrimaryTimeline` (`:122`, `c.date <= todayISO`) already ships the date-driven model the item asked for. M3 confirmed **no user-visible wrongness survived HO 333**: `/race/[id]` renders `getRunoffsForRace` (runoff-round only), the member chip is date-only. *Re-poll half — already satisfied:* the cursor wraps forever and each unit delete-rebuilds, so "never re-polled" was false — M5(a) measured all **431** resulted contests rewritten after their date (1–7d=5 · 8–30d=129 · 31–90d=147 · 90+d=150, none ≤0d). **The measured cycle is 44.6 days** (471 units, mean 10.56 advanced/tick, 14/45 budget-stops), which HO 560 C3 halved rather than answering with a priority queue. **The population number was decomposing two different bugs.** Past-by-date 482→**612**, voted-by-results 407→**431**, gap 75→**181** — but **143** of that is zero-candidate (roster never populated) and only **38** is the item's actual target, has-roster-no-shares, none older than 90 days; the 143 is now its own backlog entry. **HO 560 — the hazard the probe didn't look for.** `syncSenateCandidates` delete-rebuilds `senate-{ST}-2026-{D,R,open}` without reading which contest the scraped votebox describes, and `syncCalendar` writes only 270toWin's state-uniform date — so Ballotpedia rebuilding a seat's page around a later contest would have produced **a row dated June 9 carrying the August field, June's results gone, no error anywhere**. Three commits: `87a4366` seeds `senate-SC-2026-special-{D,R}` (Aug 11, runoff Aug 25, `race_id=S-SC-2026`, **`election_round='primary'`** so it flows to `getPrimaryCalendar` → `PrimaryTimeline` as a new Aug 11 tick rather than needing a third round vocabulary every consumer would learn); `aa9093a` freezes **settled** rows (past-dated AND already carrying a share) against the rewrite — narrow on purpose so past-dated-but-shareless rows stay open (the exact mid-count population the re-poll item existed for), with `settledSkipped` surfaced and `reingest-primary-slate.ts` as the correction escape hatch; `65d0692` takes the cron to twice daily. **Freeze blast radius: 431 rows** (senate 42 / house 389). *Amendment before land:* the seed loader's empty-roster path was fixed to leave a roster **untouched** rather than clobber it (oddities). **HO 561 — the design that couldn't fire, and what replaced it.** The obvious route keyed off which URL served, since `scrapeSenateCandidates` already falls back to `senateSpecialPageUrl` and the success path preserves the winner on `result.url` — **but it never fires for SC**: the standard SC page returns 200 with the June regular, so the fallback is never reached, and S2 then found **0 of 35** standard pages missing (including FL and OH, whose specials that fallback was written for). So the special page is fetched **in addition**, gated on a seeded `-special-` row existing for that state, which closes the FL/OH regression surface by construction rather than by care. `5122a79` adds `scrapeSenateSpecialCandidates` to the scrape module (additive; the FL/OH fallback untouched) + the opt-in pass, with `buildSenateMatcher` and `isSettled` extracted so base and special writes make the identical call; `d521145` adds the dated trigger — seeded specials within **±7 days** of primary or runoff date and carrying no shares get scraped ahead of the cursor, cap 3 states/tick, cursor never advanced, budget shared from `DEADLINE_MS` (a cursor position cannot serve a fixed election date; at twice-daily ~14 attempts across each window). *Amendment before land:* the special fetch was moved into the scrape module so one copy of the Ballotpedia URL/UA/timeout semantics exists by construction (oddities). **It ships un-run:** Ballotpedia's SC special page was 404 at both 560 and 561, so C1's hit branch is unexercised and the path has only ever run its miss branch — WATCH-tracked with a scheduled proof date (~Aug 13). **No theme-% change — a correctness arc plus an ingestion path on the existing electoral pillar; Races holds at 99.** **Numbering — the 557 → 561 pointer jump skips no block:** HO 558 was the doc sweep for 557, HO 562 is this sweep (the HO 506/536/553 precedent). Docs: HO 562. **Also notes now run through HO 561.**

**Also (HO 563–564), the House roster-erasure arc — a live P1 found by a queued investigation, then fixed and repaired; no theme-% change.** *The queued "143 zero-candidate rows" investigation found a live data-loss loop, and the two backlog items it was meant to separate turned out to be one bug seen from both ends.* **HO 563 (`921e752`, read-only probe)** was sent to split three candidate causes for the 143 and came back with a fourth thing that outranked the question. **M1:** 143 rows (senate 1 / house 142) across 96 districts — whole-state failures (OH 15/15 · AZ 9/9 · NE 3/3 · ME 2/2 · ND/SD) alongside scattered ones (NY 24 of 26 · NC 8 of 14 · CA 8 of 52 · AL 6 of 7), with `updated_at` well after `primary_date`, so **not un-visited**. **M2, the deciding measurement:** a live scrape of all 96 → **ok=66 · no_section=13 · no_candidates=17 · no_page=0 · special=0** — not one bucket but two: two-thirds parse fine live while sitting empty in the database, the signature of a roster deleted and not yet rewritten, and the zero `no_page`/`special` **refuted the handoff's own §1 premise** that those statuses made these rows (neither reaches the write block). **M3, the finding that reordered everything:** of 40 sampled resulted House districts, **12 (30%) return `no_section` live** (AL-01 · AR-02 · CA-09 · GA-01 · NV-01 · NY-01 · OK-01 · OR-01 · PA-01 · KY-02 · MD-02 · MS-02), every one holding results, every one one cursor visit from the fall-through's delete-then-insert-nothing. **M4:** `.cache/` is uncommitted, so `readCachedHtml` returns null in production and M2/M3 are live Ballotpedia, not a local artifact. **M5:** cursor mid-sweep at house offset 288 of 435, the M1 rows written 2026-06-18 … 07-21 — visited recently and empty anyway. **The mechanism:** `syncHouseDistricts` sent `special` and `no_page` to `continue`, but `no_section` and `no_candidates` **fell through into the write block**, whose per-contest delete-then-insert then ran with an empty roster — a district whose page stopped parsing had its roster, and its results, deleted and replaced with nothing. **HO 560's guard could not have caught it:** that predicate refuses a *rewrite* on a settled row (substitution); this is *erasure*, and a wiped row has no share left to protect — which is why the queued House guard was the wrong shape before the probe ran. **The cadence interaction, as measured rather than assumed — a correction to this block's own first draft.** It originally asserted HO 560 C3 doubled the erasure rate and ran that way for about a day; **that was wrong, and the correction is the more useful record.** C3 (`65d0692`) landed **2026-07-29T18:14:38Z**, after that day's only 12:00 tick, so the first 00:00 slot was not due until 2026-07-30T00:00; before it arrived the cron was paused from the Vercel dashboard for the classifier outage, and the 564 fix went live roughly 71 minutes ahead of that slot. **Hour-00 ticks since C3 = 2, both post-fix** (the scheduled 00:00:47Z tick, which timed out, plus the 00:38:47Z manual confirmation tick) — and **0 ever ran the erasure code**. So **C3 was a latent amplification closed before it took effect**, and the erasure ran at the original once-daily rate for its entire life — which reframes the repair: **+124 rostered contests and +21 carrying a share reflect erosion across many daily cycles, not one accelerated day.** The methodological half is the reusable one: the doubling was asserted from a schedule *string* without checking whether the schedule had ever *run*, and a roadmap block is append-only, so the error would have been permanent — the claim-verification rule applied to the planning layer's own framing. The dashboard pause is worth recording on its own merits (no `vercel.json` change in either direction): these losses are the durable kind, because a row is erased *precisely because* its page fails to parse and the repair works by re-scraping, so the rows destroyed are exactly the ones re-scraping cannot recover. The cadence **stayed at twice daily** after the fix — halving the rate of a fixed bug buys nothing. **HO 564 (`154d779` C1 · `95fdd10` C2).** The rule shipped as *a sync must never convert absence of evidence into deletion*: `no_section` joins `special`/`no_page` and skips the write block (a missing anchor after three retries is a parse failure, not evidence a contest exists), `no_candidates` stays on the row-creating path (that page did parse), and the per-contest delete runs **only when the incoming roster is non-empty**, on both chambers. **Senate was widened in on structure, not measurement** — its status fork already skipped non-`ok`, but the empty-per-contest-roster path on a good parse was the same edge, sized by STEP 0 S3 at **28 rows** outside the HO 560 freeze. `rosterDeletesRefused` surfaces on both summaries and into `cron_runs.payload`, so the guard is visible rather than silent. **Falsified before it was recorded as protection** (the HO 549/553 discipline), on MD-05 with 27 resulted candidates: forced `no_section` left the roster byte-identical 27→27 with the skip logged; forced empty-`ok` returned `rosterDeletesRefused` naming both contests. Scratch reverted, tree clean, nothing committed. **Repair, measured:** House candidate rows **1,911 → 2,294** (+383), contests with a roster **594 → 718** (+124), contests carrying a share **389 → 410** (+21 — result sets recovered, not just rosters), zero-candidate **143 → 73**; refusals during the repair run NE 44 · S 34 · MW 1 · W 7. **The residual 73 is decomposed, not stuck:** 40 rows are a legitimately uncontested party whose sibling contest is rostered, and 32 rows across 16 districts are genuinely non-parsing (AL-02 · AR-01/03 · ME-01/02 · NC-02 · NY-02/05/08/16/18/20/22/26 · UT-04); the handoff's "~30" estimate mapped to the second group only, and the fix repopulated everything that parses while fabricating nothing that doesn't. **The confirmed post-re-enable tick** is `cron_runs` id 6042 (2026-07-30T00:38:47.600Z, success, unit house, cursor 332→344): `rosterDeletesRefused: []` — the field is **live in the prod payload**, which is what confirms the *wiring*, and its empty value means all 12 districts on that pass parsed cleanly so the guard had nothing to refuse. **An empty counter is not evidence the guard works** — the MD-05 falsification above is that evidence, and the two must not be conflated; likewise the presence-of-field discriminator is invalid on `timeout` rows (id 6035 has no finalized payload), so the pre/post-fix attribution rests on deploy timing, not on the field's absence. **No theme-% change — a correctness fix on the existing electoral pillar; Races holds at 99.** **Numbering — the 561 → 564 pointer jump skips no block:** HO 562 was the sweep for 559–561, HO 565 is this sweep (the HO 506/536/553/558 precedent). Docs: HO 565. **Also notes now run through HO 564.**

**Also (HO 566–568), the delete-rebuild erosion arc — the deliberate version of the 563/564 accident; no theme-% change.** The erosion at 563/564 was found by a probe aimed at something else, so the next idle slot aimed one on purpose: a grep of the delete-rebuild shape across sync code picked three targets, and **a read-only probe measured all of them before anything was scoped.** The map it returned inverted the fear — the STANDING wipe (committees, every roster revisited every 12h) had **zero victims**, the real damage sat frozen on the WINDOWED paths, and the derived recompute held its contract exactly. **HO 566 (`0c62a81`, read-only probe), five measurements.** **M1:** 236 committees (53 main / 183 sub), 10 zero-member — all select/joint/commission bodies **ABSENT from the roster source entirely**, so they never enter the write loop; source parsed 230, **0** source-rostered-but-DB-empty, **0** trailing-by-≥3, uniform 1.7h roster age. **M2:** 1,516 votes, **4** with a positive tally and ZERO `member_votes` (`house-119-2-253/254/269` · `senate-119-2-146`), the **1–4 partial bucket EMPTY in both chambers**, surpluses 0 — **the deficit shape did the diagnosing**: benign per-member skips produce partials, an inflow race produces all-or-nothing zeros. **M3:** both vote syncs **WINDOWED** (House watermark `MAX(vote_date)`, L354/L388; Senate `MAX(roll_call)` per session, L361/L373) — thin/empty risk is first-write-only, then frozen forever. **M4:** Senate `amendment_votes` computed 97 == materialized 97, drift 0/0, unmatched 0, residual 64 = the pinned anchors — the HO 537 pure-recompute contract's first external check. **M5** on the record: 2,259/2,624 zero-bill meetings = expected sparsity. Classification: **committees STANDING-zero-victims · member_votes WINDOWED with 4 frozen first-writes · amendment_votes INERT.** **HO 567 (diag `26879b5` · C1 `547d358` · C2 `f6aab1b`), the member_votes self-heal.** STEP 0 gated the cause before any build code: all four returned **populated rosters upstream with tallies matching the stored ones exactly** — a transient-publication race (the tally publishes before the member positions), not permanently memberless rolls. The design fork was decided for the **bounded self-heal pass over holding the vote row**: tallies stay live, permanent loss becomes bounded lag, and repair + fix are one mechanism (the pass's first run drained the 4 — no backfill script). Predicate is **zero-roster only** (`tally > 0 AND NOT EXISTS member_votes`, newest-first, `HEAL_CAP=10`/chamber): partials measured 0, a structural partial (a Senate resolver miss) would spin under a deficit predicate, and the zero-roster predicate is **self-limiting by construction** — a vote that heals even partially exits the set. It reuses the per-vote write unchanged; permanent retry per the HO 553 posture; `memberVotesHealed` / `memberVotesHealPending` into `cron_runs.payload` (live at id 6056). **Both branches proven** (the 549/553 discipline): a scratch-forced empty roster on `269` landed `pending=[269]` with the row unchanged while `253`/`254` healed live around it, scratch reverted clean, re-run healed it. Drain: all four to full tally, zero residual; the 566 probe re-ran as the **regression instrument** — M2(a) 0/0 (senate 871→873 = two new fully-populated votes synced during the drain). **The participation distortion, per the §0.4 read (not from memory):** a zero-roster vote **deflates the 119th participation denominator** — `getParticipationStrip` / `participationAggCte` / `getChamberParticipationContext` all compute `not_voting / COUNT(*)` over the member's OWN `member_votes` rows, so a vote with no rows is absent from every member's count and the missed-rate is measured over fewer votes than occurred; it does NOT inflate any individual MISSED (no member has a row to count as a miss). The career path (`member_career_votes`, Voteview cast-codes) is a different source and unaffected. **Render payoff:** `senate-119-2-146` is the **Kim Amdt No. 5545 budget waiver** — its HO 557 motion-line party split was all-zero → HO 533-suppressed; it now renders **44 D + 2 I yea / 53 R nay / 1 D nv**, and the waiver-failed → amendment-killed clause carries a real split. **HO 568 (`38ea8b0`), the committees delete gate.** The 564 rule enforced on the standing wipe — this time **the analogy WAS measured before porting** (566 M1 sized the same shape). The DELETE is pushed only when the **insertable count** (bioguide-bearing entries, NOT raw array length — the B falsification's point) is > 0; a refusal is **reported only when it protects existing rows** (the rostered/unrostered fork keeps `rosterDeletesRefused` signal-bearing at `[]`; with the 10 empties source-absent, the silent-skip branch is purely defensive today). The one-`db.batch` shape is preserved (HO 143). Falsified both variants on `hsag00` (53 rows byte-identical under an empty array AND insertless entries, code in `rosterDeletesRefused`; control `hsju00` rewrote normally; natural run restored, `[]`). Dissolution trade accepted: a genuinely emptied roster lingers at last membership until a manual one-off. **Two files, adjudicated:** the committees route **hand-picks** payload fields (unlike the primaries route's whole-result pass-through), so surfacing the field took one line in `app/api/cron/committees/route.ts` — guard #1's "one file" was inconsistent with the handoff's own §5.2 payload gate, so Code resolved toward the gate and flagged it; accepted, and the forwarding-shape difference is now a SKILL note. Payload live at id 6059. **No theme-% change across the arc** — a probe plus hardening on existing pillars; no bar moved. **Numbering — the 564 → 568 pointer jump skips no block:** HO 565 was the sweep for 563–564, HO 569 is this sweep (the HO 506/536/553/558/562 precedent). Docs: HO 569. **Also notes now run through HO 568.**

**Also (HO 570), the cleanup arc — six deferred "next-touch" cleanups discharged in one pass, six clean-revert commits; no theme-% change. The block's content is not the deletion count — it's that STEP 0 fired and the target list was wrong.** Each item had been deferred to "the next touch" of a file nobody was touching; this was that touch. **STEP 0 saved two live surfaces the backlog had marked dead.** The backlog named `.v2f-sponsor*` dead — but `.v2f-sponsor-last` / `--plain` are **live** (`V2FeedList.tsx:123,130`), and the `.v2f-sc-*` sponsor-card block sitting *immediately beside* the "these are dead" comment is **live**, rendered by `BillExpandPanel.tsx:224` and revealed by `.bxp-sponsor:hover .v2f-sc-card` (`globals.css:4456`). A prefix sweep would have silently killed the sponsor hover card HO 472 verified; the comment naming the dead set was itself the vector, so **C1 rewrote it rather than deleting under it**. **C1 (`ef36547`)** — 20 dead selectors to zero, `app/globals.css` **9,713 → 9,269 (−444)**, braces balanced 1468/1468: the HO 317 private-Expand orphans, the HO 306 `.hill-band*` family, the HO 366 `/hearings` chrome, the HO 306 list-view filter bar, the HO 367 `.mc-fbar-title`. **Flagged deviation:** C1 also dropped `.hearings-filter-count` + `.hearings-filter-opt.is-active*` (0 consumers) beyond the handoff's literal list, and said so. **C2 (`29a7e66`)** — `getMembersRankedCount` + 8 `lib/hearings` exports (+ the internal `HEARING_TYPES_SET`). Two kept, **and the handoff was wrong about one:** `HEARING_BADGES` produces the live `HearingBadge` type, and **`weekdayOfKey` is live via `mondayOfKey` inside the module** — the planning grep excluded `lib/hearings.ts` from its own consumer search and missed it (the same class of error as the glob; Code caught it and refused the delete). **C3 (`95cf48e`)** — the dup key was **`:96 key={id}` over `billIds`**, not the `:82` issue-code map the backlog named: **82 filings in the scoped drill blob carry duplicate bill IDs because the precomputed `readLdaTables` path has no `SELECT DISTINCT` while the live `hydrateFilings` path does — and the HO 440 distinct-filings ranking is unaffected**, because `computeBillDrill` inverts `billsByUuid` through a `Set<string>` (`lda-rollup.ts:459–464`) so a bill repeated inside one filing contributes its uuid once; the duplication only ever escaped through the `FilingSummary.billIds` display field (`:482`), which is where it was fixed (deduped both arrays). Retired the allowlist entry; `MissingSecret` kept. **C4 (`c229145`)** — `verify-deploy.ts` `process.exit` → `process.exitCode` (live-proven: `verify:deploy` exited 0), the `sync-fec.ts:4` stale "1000/hr" comment corrected to the real 60/hr tier, the dead `getSponsorStates` probe that had produced the HO 404 audit's **false SLOW: 34,656ms**. **C5 (`33dfb3e`)** — the amendments repair gate moved from `stored >= pagination.count` to full enumeration (`missing.length === 0`), matching the nominations fix at `e175fd7`; `RepairResult` now carries `distinctLive` + `duplicates`. **Falsified, so recorded as proven protection not a WATCH:** a scratch fetch monkeypatch served the real 6,975-id stored set plus one duplicate with `pagination.count=6976`; the real `repairAmendments` returned `duplicates:1, complete:true` in **pass 1**, where the old gate (`6975 >= 6976`) would have looped to `maxPasses` with `complete:false`. Harness deleted, tree clean, nothing committed from it. **C6 (`af8378e`)** — the president predicate-wait (`waitForURL(/[?&]stage=president/)` replacing the 1.5s fixed wait; the `toContain` assertion kept; the crawl settle at `:162-163` and the sites at `:275`/`:308` untouched) plus an explicit `/races` → `/electoral` landing assertion. **Code's correction, carried:** the "HO 502 pattern" the HO 570 handoff told it to extend **does not exist in the file** — `/news` and `/president` became real routes, so there was no per-route `redirectedFrom()` assertion to copy (the claim came from the backlog). **The convention problem, stated as a finding:** the ledger's header rule (a backlog line's mechanism is a claim) notes two wrong in three HOs and says a third makes it a convention problem — this arc produced **two more genuine wrong-mechanism claims** (the `.v2f-sponsor*` glob, the HO 502 redirect pattern) plus stale line numbers in three places, so it is past the threshold; promoted to a real backlog item rather than another tally mark. **No theme-% change** — cleanup on existing surfaces, no bar moves, no new surface, no migration, no cron change. **Numbering — the 568 → 570 pointer jump skips no block:** HO 569 was the doc sweep for 566–568, and docs-only sweeps are owed no roadmap block (the HO 506/536/553/558/562/565 precedent). Docs: HO 571. **Also notes now run through HO 570.**

**Also (HO 572), the `#lobby-drill` anchor restored + the schedule-trigger answer — a correctness fix on an existing surface plus a read-only check; no theme-% change.** **The fix (`290e239`, two files).** `TopicCrosswalk`'s code chips link to `/lobbying?issue=…#lobby-drill`, but the HO 486 redesign had removed the target element — so a click dropped the reader at page top. The id is restored on the **stable `.mc-content lob-content` pane container** (`app/lobbying/page.tsx:416`), **not** the scoped `.mc-ctx` block: a well-formed-but-unknown `?issue=` resolves `selectedDrill = null` and falls back to unscoped (`:117`), so the scoped block doesn't render and an id inside it would dangle again through a narrower door. A comment at the id records the placement so nobody "corrects" it onto the scoped block. **Why restore rather than drop the fragment** (the fork HO 571 filed with two options): the crosswalk renders in `.lob-secs` **below** the pane (`:493` vs `:416`, the HO 492 side-by-side layout), so a reader clicking a code chip is at the bottom of the page with ~400px of chrome (masthead + two-row nav + readout + blurb + fbar) between them and the scoped answer they asked for; dropping the fragment would have made the test green **by deleting the behavior instead of restoring it** — the same locate-the-reader's-selection principle already applied at HO 492 (`scrollIntoView({block:"nearest"})` when a bounded rail re-rendered with its selection out of sight) and HO 517 (the rail pin when a deep-linked member landed on a rail with nothing lit). **The assertion promoted soft → hard** (`e2e/fit-finish.spec.ts:470`, the HO 473 precedent: soft while a finding is open, hard once fixed). The STEP 0 diagnostic is the interesting half: the failure was **not** a soft red — `drill.evaluate(...)` ran against a zero-element locator and rejected *before* `expect.soft` was reached, which is why it surfaced as a hard failure and why the `codeChip.count()` guard above it was passing (chip present, target missing). Suite **1 failed → 0 failed (41 passed, 1 skipped)**. **Verified:** `inView` true at 1280×720 and the motivating short viewport; `?issue=ZZZZ#lobby-drill` → 200 with the scoped block absent, the anchor present, zero console errors, confirmed on prod; unscoped `/lobbying` unchanged (~1,380px against the HO 492/493 ~1,369 figure — data-dependent, an id adds no layout). **STEP 0(a) — the `schedule` trigger fires.** Two runs — 2026-07-29 (failure) and 2026-07-30 (success) — both landing **~85 minutes after the `0 15 * * *` slot** (recorded so a future reader doesn't treat a 16:2x run as anomalous). Latest green → the **Jul-23 open loop closes, and with it the whole Phase-2 Playwright-in-CI loop**, whose only surviving thread this was. Breakdown over the last 30: `deployment_status` 14 success / 12 skipped / 2 failure; `schedule` 1 success / 1 failure. **The finding — UNCHARACTERIZED.** An intermittent `pageErr=1` (uncaught page error) on **`/`** and **`/?stage=other_chamber`** in the `smoke.spec.ts` route crawl accounts for the 07-29 schedule red and both `deployment_status` reds; it sometimes recovers on retry #1 and sometimes fails both hits. **Ruled out:** the HO 473 multi-child SVG `<title>` hydration class — all four `<title>` sites (`PrimaryTimeline:145`, `BillsTimeSeries:123`, `CommitteeActivityChart:145`, `RaceDistrictModal:140`) carry a single template-literal child at `290e239`, so that fix held; and the global-`concurrency` cancellation WATCH (these are `failure`, not `cancelled`). **Untested hypothesis, flagged as such:** the two failing URLs differ only by a filter that changes the feed, so the shared dashboard chrome is the suspect surface, and its time-derived render inputs (the HO 183 cycling stamp, relative ages, the hearings three-hour `watchState` window, the tape) are the shape that mismatches only when a render straddles a boundary — **not to be written up as "probably the known #418 class"** (the grep above falsifies that specific mechanism, and scoping a probe on it would repeat the wrong-premise failure HO 570/571 spent two sweeps documenting). Its own probe HO. **No theme-% change** — a correctness fix on an existing surface plus a read-only check, no bar moves. **Numbering — the 570 → 572 pointer jump skips no block:** HO 571 was the doc sweep for 570 (docs-only sweeps are owed no roadmap block, the HO 506/536/553/558/562/565/569 precedent). Docs: HO 573. **Also notes now run through HO 572.**

**Also (HO 574), the `pageErr` probe — the artifact answered it, the design paid, and the failing set is still moving; no theme-% change.** **The design nearly didn't pay.** The probe was built **artifact-first** against a **14-day retention clock**: all four failed-run report artifacts were still live, and the captured `pageErr` message was **identical across all four runs** — a single cause. It is **React #418, `args[]=HTML`** — the hydration text-content mismatch variant. The message had been captured all along (`smoke.spec.ts:129` pushes `err.message`) but only the **count** was ever printed (`:200`), so the diagnostic had existed for weeks yet was reachable only through an expiring artifact. **Route/hit breakdown:** run `30506188415` (`/?stage=committee`, `/?stage=other_chamber`, hit-2); `30510430349` (`/`, `/?stage=other_chamber`, hit-1 + hit-2); `30470412500` (the 07-29 schedule — `/`, four `?stage=` variants, `/president`, both hits); `30572418753` (five `?stage=` variants + `/members`, hit-2). **It fires on hit-1 as well as hit-2, which rules out the HO 533 cache-serialization class.** **H1 falsified.** `/president` and `/members` fired, and both are non-tape inner pages (HO 323/326 stripped the tape; no `MarketsTape` in `HeaderBar`), so the HO 572 "tape-bearing set" observation does not hold — killed on the artifact route pattern rather than a local test, the stronger evidence. **The block's real lesson — the firing set has been re-characterised twice, from growing samples.** Two runs supported "tape-bearing"; four supported "the relative-age feed surfaces," after two non-tape pages fired. **Neither characterization was wrong on its data; both were premature** — the tell is that the set *grew* with sampling (2 routes → 6 → 6 including a non-tape inner page) rather than converging. **The current characterization is also provisional, and the fix HO must not scope around the observed route set until it stops growing.** The confound is still live: those pages are also the heaviest client-island pages, so "relative-age" and "most islands" remain perfectly confounded. **H2 supported, component unidentified.** Time-derived hydration is the shape that fits (#418 by message, both hits, sometimes recovering on retry), but it is a **new** cause, not the HO 473 `<title>` mechanism — re-confirmed falsified, all four `<title>` sites single-child at `290e239`; nearest prior art is the HO 489/490 relative-age `nowMs` arc, a candidate the fix HO *starts* from, not a conclusion this one reached. **Not reproducible locally** — two full local smoke crawls, route-crawl `pageErr=0` throughout; recorded as a **result, not a gap** (the handoff pre-authorised it — an unreproduced intermittent with the instrument shipped is a complete outcome). **C1 (`57395b9`, `e2e/smoke.spec.ts` only):** a second log line, emitted only when non-empty, carrying the actual messages whitespace-collapsed + truncated to 200 chars each, `pageErr` first. **Falsified before recorded as working** (the HO 549/553 rule): a scratch client-only throw in `CyclingTimestamp` produced a real `pageerror` through the unchanged capture and the new line printed it, console messages cut at 200; scratch reverted, nothing committed. **Handoff premise corrected (a doc error, not a code one):** the HO 574 handoff said `attachCollectors`/`logClean`/`assertClean`/`settle` are shared between the specs — **they are not.** `fit-finish.spec.ts:56` defines its own `attachCollectors` (and its own `GATE_COOKIE` `beforeEach`); it imports only `isKnownNoise` from `console-noise.ts`. The claim came from SKILL `:1913` (corrected in the HO 575 SKILL commit), so C1 is smoke-only and fit-finish is provably unaffected (re-confirmed 41/0). **No theme-% change** — a read-only probe plus test instrumentation, no user-facing surface (the HO 379 / 382–386 / 503–504 / 548 harness precedent). **Numbering — the 572 → 574 pointer jump skips no block:** HO 573 was the doc sweep for 572. Docs: HO 575. **Also notes now run through HO 574.**

**Also (HO 576–577), the primaries data arc — the member-hub primary chip corrected + the house-date self-referential clobber fixed and repaired; no theme-% change.** Two code arcs on one subject; 577 came directly out of 576's STEP 0. **HO 576 — `getPrimaryForRace` over structured columns (`e3cf396`).** The member-hub chip reconstructed the id `senate-${state}-2026-${party}` literally, which **structurally could not match a `-special-` row**, so SC's Aug 11 senate special was invisible and the chip showed a stale June 9; replaced with a query over structured columns. **STEP 0 earned itself twice.** M3a **proved the handoff's own proposed query wrong** — with no `district` restriction it leaked per-district house rows and would have "fixed" 127 nulls with arbitrary district dates. And it surfaced the design question the backlog line hid: the caller passes `district = null` for House **and** Senate members by design, so "prefer the soonest future contest" hands a House member a Senate-only special that isn't theirs. **The approval was self-contradictory, and Code named the contradiction rather than picking one and proceeding:** special-exclusion, a delta matching M3b's 2 keys, and no caller change could not all hold simultaneously for a chamber-blind function. The resolution was a **`memberChamber` argument** — named that way, not `chamber` (which reads as row-selection) or `includeSpecial` (which would put the policy in the caller) — with **no `p.chamber = ?` in the WHERE**, so no House chip was emptied. The rule, as written into the code comment: **a Senate special is a Senate-only contest, so it is never a valid proxy for a House member's primary in any state** — the senate-row-as-proxy convention works because regular Senate and House primaries share a date, and a special is exactly the case where that coincidence breaks. Derivable, not tuned to SC. Special-test is the id substring (no column marks it), isolated in one named predicate flagged as the single site to change if an `is_special` column ever lands. **Verified delta: SC's two senators only** — 532 unchanged, 0 newly populated, 0 lost chips.

**HO 577 — the house-date clobber. The defect class, not the symptom: a table must not derive its writes from an unfiltered aggregate over itself.** `calByState` was `MAX(primary_date) GROUP BY state` with no filters, so it computed "the latest date on any row this state owns," not "this state's primary date." Specials and runoffs won by construction — and once a house row absorbed the wrong date it was `election_round='primary'` and **won MAX itself on the next tick: the loop was already self-sustaining.** Measured (M1a–M1c): **3 states — LA, GA, SC — 48 house rows**, cause tally **special=2 · runoff=3 · house=48** — the house bucket dominating is the finding, the special/runoff were the injection sources but no longer the active cause. Excluding only special and runoff was therefore **provably insufficient**: **`district IS NULL` is load-bearing because it severs the self-referential class rather than narrowing it** — every row that can feed back carries a district. **M7 forced the shape:** 15 states own house rows but have no clean statewide senate row (AZ CA CT HI IN MD MO ND NV NY PA UT VT WA WI) — exactly HO 209's 35-state gate, with no stale pre-gate rows surviving — so a bare `district IS NULL` filter would have nulled their dates, and the write instead **COALESCEs to preserve** the existing value. **The consequence is recorded, not buried:** those 15 states' house dates are now **frozen with no refresh path** — strictly better than circular and the right call, but new latent staleness, which gets its own WATCH. Part 1 resolved **without calendar-threading** — the cron walks a cursor and the House slice already reads the statewide date from the DB's senate rows (`syncCalendar` seeds them from 270toWin), so the fix lives entirely inside the House sync and the scrape stays out of the per-tick path. **LA is a different failure mode entirely and its facts are recorded here because nothing in the repo can rederive them:** Governor Landry's **EO 26-038 (2026-04-30)** suspended Louisiana's U.S. House primaries only, following SCOTUS in *Louisiana v. Callais* striking the congressional map as a racial gerrymander; per the FEC, **neither the May 16 Senate primary nor the June 27 Senate runoff was affected**, so `senate-LA-2026-*` remains correct and a valid `calByState` source. The order ran "until July 15, 2026 or such time as determined by the Legislature" — **that date has passed**, the Legislature approved a new map in late May, and LA may have reverted to the jungle primary for U.S. House — so **no date is correct for the LA house rows, and "suspended" is itself a claim with an expiry.** LA modeled **postponed, not cancelled**: `primary_date`/`runoff_date` NULLed via a `HOUSE_PRIMARY_SUSPENDED={LA}` carve-out, **`primary_type` left alone** — that column is a *type* (open/closed/jungle), not a status; encoding `suspended` there would break precisely when LA needs `jungle`, and it's the same column where SC's `open` is correct. **The repair (Stage 2, `--apply`):** date-only, **GA 28 → 05-19, SC 14 → 06-09, LA 6 → NULL**, correct dates **derived from the clean statewide row, not hardcoded**, both externally verified against the public calendar — GA's clobber was the June 16 runoff, SC's was the Aug 11 special. Date-only because GA's 28 rows are settled and the tick's roster delete-rebuild would churn decided results; verified **48/48 converged, GA roster `80→80` / `28→28` untouched**. **The review caught two amendments before land:** every SELECT and UPDATE carries `id NOT LIKE '%-special-%'` so a house special can't be swept, and the GA roster check captures a fingerprint **before and after** rather than an after-only count that reads the same either way. **Ordering was the whole risk.** GA's 28 rows are settled, so a guard landing before the repair would have frozen them at the wrong date — **Part 1 → repair → falsification → guard, across two deploys because they cannot ship together.** The House `isSettled` guard (parity with the Senate path ~`:707`) is **untriggered the moment it lands**, and an untriggered guard reads identically to a broken one — so the falsification drove the **real** `syncHouseDistricts` against a settled GA-01 and proved it fires: `settledSkipped` names both ids, `primary_date` and the roster's `updated_at` both unchanged (the delete-rebuild never ran). **The interlude:** prod went down mid-arc on an **account-wide Turso rows-read block (~3h, no code fix)** — STEP 0 could not run and was committed **UNRUN** rather than validated against fixtures (fixtures would have produced numbers that looked like findings and weren't); attribution afterward put the burn on **CBT, not psephos** (psephos peaked ~1.7M and was declining; CBT stepped ~10× around Jul 25 and plateaued near 85–90M/day). Live at **`3391850`** (Part 1 `2e8cbbe` · repair `eb88959` · guard `3391850`), two-stage fast-forward through a review ref. **No theme-% change** — a correctness fix and a data repair on existing surfaces. **Numbering — the 574 → 577 jump skips no block:** HO 575 was the doc sweep for 574, and 576–577 are covered by this one block. Docs: HO 578. **Also notes now run through HO 577.**

**Also (HO 579–580), two cron-hygiene fixes on the trades + LDA paths — no theme-% change (a log-noise correction and a cron-topology fix, no user-facing surface).** **HO 579 — the FMP `page` 402 is a plan boundary, not an error (`54f0590`).** `/api/sync` had logged `[sync] trades error: FMP house-latest page 1 failed: 402` every tick; confirmed by hand against the live endpoint — page 0 returns data, page 1 returns the 402 — because the account is on FMP's **Free plan, which permits `page=0` only**. **It's an entitlement gate, not a rate limit:** the FMP usage meter read **0/250 calls and 0 MB/512 MB** while the 402s fired, so nothing was misconfigured or over quota. `lib/fmp.ts` gained `FmpPlanBoundaryError` + `isFmpPlanBoundary`, keyed on **402 AND the page-parameter text** (not 402 alone, so a payment failure on any other parameter stays a real error — proven both ways: a synthetic limit-parameter body returns false, and a forced 404 still lands in `errors` with `isFmpPlanBoundary=false`); `lib/trades-ingest.ts` records `planCappedAtPage` on the result instead of pushing to `errors`, and `/api/sync` carries `tradesPlanCap` per chamber. **The design call worth recording:** `maxPagesPerChamber` was deliberately **not** capped at the call sites (3 cron / 20 manual) — hardcoding `1` would work today and quietly cap a future paid plan, whereas detecting the boundary means an upgrade needs no code change. Nothing is lost operationally (the `-latest` endpoints return newest-first, so page 0 still captures every new disclosure; only per-tick backfill depth is forgone). **The honest gap:** the post-deploy prod tick was warm (`inserted=0` → short-circuit before page 1), so it exercised the clean stop but not the `planCappedAtPage=1` recording — proven locally against the real 402 and self-closing into a log line on the next tick that reaches page 1 (WATCH). **HO 580 — the LDA rollup and sync were competing for one budget (`e459fe9`).** The rollup started only if `msLeft >= ROLLUP_RESERVE_MS (220_000)` against a 300s ceiling, so **the sync had to finish within 80s**; the first authenticated run made **259 `lda.gov` calls at ~1s each** (~259s), starving the rollup by more than 3× the margin. **The inversion is the finding:** the rollup ran only when the sync had nothing to do and was starved precisely when new filings arrived — `route.ts:45-48` accepted "a day stale" but the real behaviour was stale indefinitely until a hand-run `npm run lda:rollup` (last refresh 114,048ms; 129,105 filings / 277,703 activities). Neither component was wrong; the coupling was. **Change 1:** a dedicated **`/api/cron/lda-rollup` at `0 22 * * *`** (the previously-banked standalone slot, now built), with the rollup block + `ROLLUP_RESERVE_MS` + `ROUTE_CEILING_MS` removed from the sync route; the new route can `revalidateTag("lda")` which `scripts/rollup-lda.ts` cannot (Next-runtime only), so the surface no longer waits on a TTL, and the manual script stays the backfill hatch. **Slot correction recorded:** the handoff proposed `0 10`, which is **taken by `/api/sync-votes`** — the instruction to verify against `vercel.json` rather than trust the handoff is what caught it; `0 22` is clear of every daily cron, markets (`0 */4`, last 20:00), the weekday `?source=fmp` pass (`0,30 13-21`, last 21:30), and kalshi (`15 */2`). **Change 2:** the deadline check at `lda-sync.ts:411` ran *before* `fetchPage` while the 429 retry sleep happened *inside* it — a check passing at 279s then a `retry-after=39` sleep landed at 318s and the function was SIGKILLed instead of exiting with `deadlineHit` (the observed 504). `fetchPage` now returns **`null`** when the sleep would cross `deadlineMs`, and the caller treats it as `deadlineHit`, **not** `fetchError` (different destinations — reporting it as an error would convert a clean pause into recorded failure); `onThrottle` still fires so `throttled429` keeps counting the real 429; backfill passes `deadlineMs: undefined`, leaving the branch inert. **Exactly one `return null` exists in the fetch path**, so no other condition can be misread as a deadline pause. **The falsification was two-sided** (why it's recorded): BAIL (`retry-after=60`, deadline ~35s out) → bailed, `deadlineHit=true`, `fetchErrors=0`, elapsed 31s against the ~91s a real sleep would have produced; CONTROL (`retry-after=2`, deadline far) → slept, proceeded, 16 pages — a guard that always bailed would pass a bail-only test and be badly wrong, and elapsed time is physical evidence rather than a reported flag. First runs after deploy: sync at 21:38 (`pages=19`, no rollup line at all), rollup at 21:39 (`rollup ok in 20219ms`). **Numbering — the 577 → 580 jump skips no block:** HO 578 was the sweep for 576–577, and 579–580 are covered by this one block. Docs: HO 581. **Also notes now run through HO 580.**

**Banked / unbuilt:** **strong VIA-markup report attribution** (blocked, not deferred — `stage_changed_at` history began 2026-05-11, 52 committee→floor moves all-time, no markup-date alignment → 0 clean joins; HO 268 Gate A; revisit once the history matures) · **MARKUPS column on the reports index stat strip** (queued from 268 — needs a new persisted column + migration + backfill) · **MOVERS from→to display + hop-count sort** (gated on `stage_transitions` accrual, planted 2026-06-11) · ~~metro-zoom panels (spec-3 Phase 2)~~ (**SHIPPED HO 236/237** — dense-state CA/TX metro insets + leader lines; see backlog DONE. Extending the per-state config to more dense states is a fresh scoped item, not this banked one.) · ~~**race→news / member→news surface**~~ (**both halves SHIPPED** — race→news hub HO 398, member→news hub HO 414, off the HO 394 observation layer's `observation_entities` entity join; residual drawer/challengers + the dark HO 272 `NEW` badge tracked in backlog QUEUED) · **observation inert fields** (grading / cluster / lineage — activate when a second collector or a real consumer arrives) · **the_hill budget-starvation soak** (watch a few news ticks that politico runs long — confirm the_hill isn't systematically skipped; fix is per-feed budget carving, not a timeout raise) · primaries-map results-coloring (recency-only today) · rating-history sparkline (logging live since HO 220; chart awaits weeks of data).

**Owed eyeball:** **hearings LIVE state** — the ● LIVE badge (v2 band callout + `/hearings` list/calendar glyph) was never exercised against a real streaming meeting (0 live in every build sample); confirm it renders correctly the next time a committee meeting is actually live.
