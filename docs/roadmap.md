# CBT — Roadmap

> Where the dashboard is going. Looser than a handoff. Items here graduate to numbered handoffs in `docs/handoffs/` when they sharpen up. Polish lives in `docs/ui-plan.md`, not here.

## Framing question

**WTF is going on in Congress?**

That's what someone opens this dashboard to answer. The product positioning isn't "track bills" or "summarize legislation." It's "make sense of what's happening." Every theme below earns its place by serving that question.

This started as a personal sense-making project. The author is figuring out what's actually happening in Congress, building the tools they wished existed, then opening those tools to anyone else asking the same question. The dashboard prioritizes comprehension over coverage. Most existing trackers fail on that axis: they show every bill and help with none.

What answering it in 30 seconds looks like, as the home page the dashboard is building toward:

- Three sentences at the top summarizing the week, generated from the underlying data
- Stage funnel showing where bills are pooling right now (the bottleneck of the moment)
- Topic mix showing what's getting worked on versus ignored
- Top 5 movers (advanced this week), top 5 stalls (went quiet), top 5 in the news
- One historical anchor: pace versus the 118th at the same point in the term

The current home page (page 1 of 157 feed rows) buries the answer six clicks deep. Reorienting around this question gives the home-page redesign a concrete target instead of guessing what the dashboard summary surface should hold.

## Lenses on the framing question

Specific questions an analyst would ask. Each is a sub-cut of "wtf is going on." Add and edit freely; the doc gets sharper as this list grows.

### Noise vs signal

Filtering the noise is what makes the framing question answerable in the first place. Sub-questions, all unlocked by ceremonial classification:

1. What's the noise rate, by chamber, by month?
2. Who makes the noise (highest ratio of ceremonial to substantive bills)?
3. Is the 119th noisier than the 118th?
4. Does noise correlate with party, seniority, swing-district status, or seat-up year?
5. Are some topics noisier than others (health awareness weeks, defense recognition resolutions)?

Cuts 1, 2, and 5 ship as analyst views the day `is_ceremonial` lands in the table. Cuts 3 and 4 need historical or member-level data first.

### Other lenses (seeded, to expand)

6. Which sponsors recycled bills from the 118th and how many landed this time?
7. What's the freshman class focused on?
8. Who introduces the most bills that go nowhere?
9. Which committees are graveyards versus paths to floor?
10. Which bills are getting press right now and from whom?
11. What's the shape of activity over time (introductions per week, enactments per month, deaths per quarter)?
12. Which sponsors are up for reelection next cycle, and what's their pass rate?
13. Which coalitions does each member belong to (caucus memberships, union endorsements, advocacy alignments), and how does that align with their voting and sponsorship behavior?

## Site assessment

Snapshot from a fresh look at https://cbt-chi-silk.vercel.app on 2026-05-12.

What's working: six views established (feed, `/sponsors`, `/stale`, `/changes`, `/president`, `/watchlist`). 15,677 bills synced, daily cron landing at 02:48 MT, stable pipeline. Topic taxonomy renders cleanly as compact tags, stage indicators give immediate progress signal. The terminal aesthetic is consistent across pages.

What's noisy or missing:

- **Ceremonial pollution is loud.** Visual count of page 1: ~14 ceremonial rows out of ~100. Mother's Day, Cinco de Mayo, 200 years of Peru diplomacy, "National Day of Reason," AAPI Mental Health Day, Glioblastoma Awareness Day, Tardive Dyskinesia Awareness Week, National Nurses Week, Eliot L. Engel Post Office renaming, Roy A. Rutherford Air Traffic Control Tower naming, Martinsville Missile land speed record recognition, "National Small Business Week," "National Postpartum Awareness Week for Communities of Color," "Arthritis Awareness Month." Every analyst stat downstream is skewed by this.
- **Zero visualizations.** Pure list views. No topic-distribution charts, no time-series, no funnel, no comparison overlays.
- **No landing moment.** Home page is the feed. There's no "what should I know right now" summary; the framing question gets no answer surface.
- **No external context.** Bills exist in isolation. Nothing surfaces which got coverage, which are being whipped, which are tied to active news cycles.
- **No historical comparisons.** Can't see this Congress versus the 118th at a glance.

## Architecture

Lives in `SKILL.md` as the information architecture rule (snapshot → hub → sub-pages). Themes below reference that rule rather than redefining it.

## Themes

### 1. Bill signal: noise classification + similarity

Direct service to the noise lens, prerequisite for the framing question, highest leverage on the list. Without this, every downstream cut is polluted.

Ceremonial classifier: one additional Gemini classification at summarize time, stored as `is_ceremonial` boolean, hidden by default with a toggle. Folds into the existing summarize call so marginal cost is near zero (rough estimate ~$4 to backfill the 15,677 corpus, then cents per month going forward). Four of the five noise sub-questions become analyst views the day this boolean lands.

Similarity clustering: bills with near-identical structure share title templates and action patterns. v1 is rule-based regex on titles ("Resolution honoring the memory of ___", "Resolution designating ___ as National ___ Day", "To rename the ___ Post Office located at ___"). Becomes the "similar bills" sub-page link off the bill hub, plus a standalone duplicate-shaped-bills view that's useful on its own (shows the post-office-renaming champion, the awareness-week king). Embedding-based v2 deferred until regex proves insufficient.

Sequence: ceremonial first (one handoff, ~1 day). Similarity v1 second (one handoff, ~1 day).

### 2. Home page redesign

Promoted from a sequencing step to a theme. The current home is the feed; the future home is the answer surface for the framing question. The 30-second sketch at the top of this doc is the brief.

This blocks reports and graphs. Both want a dashboard surface that doesn't exist yet, and shipping either one before the home page redesign means rework.

Open questions: keep the feed as a sub-route (`/feed`) or push it lower on the new home? Pagination there or full-bleed list? How much LLM-generated prose at the top before it stops feeling honest and starts feeling like a generated newsletter?

### 3. Weekly reports

A report is a packaged answer to the framing question, generated on a cadence and saved as a snapshot. Different from a live view: it's a moment in time, has commentary, and is shareable as a URL or file.

Two cuts on day one:

Bill changes: what's new this week, what died (no action for some chamber-appropriate window), what advanced (stage transitions), what got revived. Aggregated counts plus a list of the most notable individual movements. Most of this is already computable from existing data; `/changes` is a real-time version of the same thing.

Most talked about: which bills got the most external attention (news mentions, social signal, press releases). Depends on the news signal theme below. Without that data, this cut can't ship.

Open scope before sizing: storage (MDX files in the repo versus a `reports` table), trigger (cron-generated Mondays versus on-demand), authoring (pure LLM versus template with LLM commentary versus hand-written with data injected), format (read-on-site versus downloadable). The Substack draft is effectively the manual prototype.

### 4. News signal: breaking news + media attention

External data ingestion. Pull bill mentions from news sources, tag them to specific bill IDs, store with timestamps. Two surfaces:

Breaking news: real-time ticker or banner showing bills that got coverage in the last 24–48 hours. The closest the dashboard gets to a Bloomberg-style headline feed.

Media attention column: per-bill count of recent mentions, shown on the feed row and in the expanded view. Surfaces which bills are quietly getting traction.

Source options: GDELT (free, comprehensive, noisy), NewsAPI (paid, cleaner, tighter limits), or direct RSS from Politico, The Hill, Punchbowl, Roll Call (controllable, fewer surprises, more setup). The matching is the hard part, not the ingestion. Bill IDs are matchable but most coverage references the marketing title or sponsor name. Plan on iteration.

Sequence: pick a source. Build the sync. Run match accuracy against a hand-labeled sample of 100 bills. Ship breaking-news first (lower bar for usefulness). Media attention column second once matching tightens.

### 5. Graphs and visualizations

The dashboard has no visual idiom yet. Adding graphs is a design decision before it's an implementation task. Pick the idiom once, apply it across views.

Candidate first charts, ordered by analyst payoff: stage funnel on the home page (single bar chart, surfaces the bottleneck of the moment), topic distribution faceted by chamber, activity over time stacked by topic, sponsor productivity scatter (volume versus pass rate) on `/sponsors`, 119th versus 118th laws-enacted comparison on reports.

Library question: D3 directly, Recharts, Observable Plot, or hand-rolled SVG. The terminal aesthetic argues for thin styling and tight control, pushing toward Observable Plot or hand-rolled SVG over Recharts' defaults.

Sequence: pick the library. Ship the stage funnel as the first chart on the new home page. Iterate. Apply the same component pattern to later charts.

### 6. Member legislative depth

Applies the architecture pattern (snapshot → hub → sub-pages) to the member entity. Hub is `/sponsors/[bioguideId]`, thesis is "what this person works on in Congress." Voting record, sponsored bills, committee assignments, badges. Race coverage lives at theme 7's surface and connects back via a header tag, not a hub section. Snapshot lives in the row dropdown on `/sponsors` and the sponsor expansion in the bill feed (couple of badges, current top donor, last notable vote, "view full profile" button).

External data, each a sync pipeline of its own: member bio and committees (Congress.gov `/member/{bioguideId}`, free, already in your auth), endorsements and affiliations (caucus rosters mostly public, union and advocacy endorsements scattered across org sites and press releases), donors (FEC bulk or OpenSecrets API), stock trades (Capitol Trades, Unusual Whales; confirm sources), district demographics (Census API).

Don't batch these. Each is roughly the scope of the original Congress.gov sync.

#### Header indicators (links out, not embedded sections)

Two flags belong in the page header next to the name and party, both clickable to theme 7's race surface:

- "Seat up 2026" (or whichever cycle) when applicable
- "Active campaign" when the member is currently in a contested cycle

These do not get sections on the deep page. Race detail lives on `/race/[id]`; the header indicator is the bridge.

#### Endorsements and badges

The smallest piece in this theme but the most legible at a glance. Powers lens 13 and turns member names on the feed into coalition signals ("this is a DSA-endorsed, Squad-aligned Dem" versus "this is a Freedom Caucus Republican").

Sourcing tiers, easiest to hardest:

1. **Caucus memberships.** Mostly public rosters: Progressive Caucus, New Democrat Coalition, Problem Solvers, Republican Study Committee, the identity caucuses (CBC, CHC, CAPAC). Freedom Caucus is semi-secret but tracked by journalists. Hand-enter the initial set if no clean API exists; refresh quarterly.
2. **Union endorsements.** AFL-CIO, Teamsters, SEIU, UAW, IBEW, NEA. Each org publishes their cycle endorsements; sync would scrape or hand-enter per cycle.
3. **Advocacy alignments.** DSA, Justice Democrats, Indivisible (left); Club for Growth, Heritage Action, Susan B. Anthony List (right); Sierra Club, Planned Parenthood, NRA (issue). Most scattered and per-cycle.

UI: badge component sits in the page header next to the name (top 2–3 by priority) and renders the full set in a dedicated row on `/sponsors/[bioguideId]`. A 10-badge member should not crash the feed-row layout.

Schema sketch: separate `affiliations` table keyed by `bioguideId`, with `org`, `category` (caucus, union, advocacy), `source_url`, `last_verified`. Don't fold into the bills schema.

#### Order

Member bio first (free, unblocks `/sponsors/[bioguideId]` detail pages and the seat-up header indicator). Caucus badges second (small, public, immediate visual payoff). Donors third (largest analytical payoff). Stock trades and broader endorsements after that.

### 7. Races

Separate surface from member depth so neither overcrowds the other. The bridge is the header indicator on the member page; the destination is `/race/[id]` (likely a state-district-cycle pattern like `/race/CO-08-2026`).

A race page answers: when is this seat up, who's running, what do the ratings say, what's the fundraising picture, what does the district look like. Electoral context for the legislator, not a competing thesis to "what they're doing in Congress."

External data:

- Race ratings: Cook, Sabato, Inside Elections (likely manual unless one has an API; quarterly refresh)
- Candidate roster: Ballotpedia or FEC filings
- Fundraising: FEC, can layer on the donor pipeline from theme 6
- District demographics: Census API
- Polling: 538 successor, or curated; harder to source cleanly

MVP race page: rating, seat-up year, top three candidates, link back to incumbent's bio. Ship that with mostly-manual data, layer automated sources in later.

Open question: which races warrant pages? All 435 House + 33–34 Senate seats per cycle is a lot. Tiered approach: full pages for competitive races (any rating not "safe"), stubs for safe seats showing just incumbent and cycle.

### 8. CCBT parity

CCBT lags CBT by every feature that shipped after the fork: `/stale`, `/changes`, `/president`, sponsor depth, sort dropdown, fluid layout, header rename. The longer the gap, the harder the port.

Three options: inline porting (every CBT handoff that ships, follow with the CCBT equivalent in the next session), batched porting (quarterly sweep), or selective porting (only features that pass a "this matters at the state level too" filter). Doing nothing is worse than any of these.

### 9. UI polish

Lives in `docs/ui-plan.md`. Not duplicated here. Pull from there when you want a polish session.

## Sequencing

Working order, subject to your priorities:

1. **Ceremonial classifier.** Cheap, immediately visible, unblocks every downstream stat including the home page summary.
2. **Similarity v1.** Builds on ceremonial. Same regex-on-title shape.
3. **Home page redesign.** Scoping conversation first, then handoff. Sketch the layout, decide what blocks live on it, decide what happens to the feed (sub-route or pushed down).
4. **First graph (stage funnel).** Picks the visual idiom. Lives on the new home page as the first concrete answer to the framing question.
5. **Reports format scoping.** Chat, not a handoff. Decide MDX versus DB, cron versus on-demand, authoring style.
6. **First weekly report (bill changes cut).** Ship with the "most talked about" section as a placeholder.
7. **News sync pipeline.** Pick a source, build, validate matching.
8. **Breaking news view + media attention column.**
9. **Most talked about cut goes live on weekly reports.**
10. **Member bio sync.** Unblocks `/sponsors/[bioguideId]` detail pages plus the "seat up" / "active campaign" header indicators.
11. **Caucus membership data + badge component.** Smallest, most public, biggest immediate visual payoff. Powers lens 13. Schema design here determines how union and advocacy endorsements layer on later.
12. **Race surface MVP.** `/race/[id]` stubs with rating, seat-up year, candidate roster, link back to incumbent. Header indicator from step 10 needs a destination; this is it.
13. **One member-depth pipeline (donors or stocks).** Pick by analytical value.
14. **CCBT port batch.**

That's roughly six months of work at a CBT pace if you ship 2–4 handoffs a week. Don't treat it as a plan. Treat it as one defensible order.

## What this doc isn't

- Not a deadline-bound plan. Order is logical, not calendar.
- Not a feature checklist. Items here are themes; handoffs are checklist work.
- Not where polish lives. `docs/ui-plan.md` owns those.
- Not a contract. Cross things off, reorder, kill items as the dashboard tells you what it actually needs.

## Status

> **As of HO 519 · 2026-07-25.** This block is the **source of truth**. Future updates edit this block, not memory or off-repo notes. *(**HO 519 — the 2nd full re-derivation** (the deferred "next full audit"), read-only §0–§5 against live prod `5681322`. **Structurally green:** sync fresh (17,027 bills == `bills_fts`), 14 crons healthy, the four fixed loops holding in steady state (summarize 406, weekly-report 407, `/api/sync` trailing-step 480/481 at 7% post-fix timeout, trades 483 p90 well clear), cold-plan audit clean (0 unexpected BAD of 50) — plus **9 post-404 queries spot-EXPLAIN'd by hand** to close the frozen-`cold-start-audit-332` coverage gap (the LDA / amendments / nominations / news-rail pillars postdate its inventory), every one riding its intended index. **Two ratified bar changes:** **News signal CLOSED → 98** (the "obs layer joinable" label under-sold a now richly-surfaced pillar — `/news` route + `?member=` + topic rail + member rail, race→news, member→news, the dashboard NEW badge), and a **new combined bar `Lobbying / amendments / nominations` = 92** (the 435–463 "extend downstream along the bill spine" arc, v1-complete across data + aggregate + detail + cross-links). **Overall ~98 → ~97**, recomputed from the reconciled 9 bars. **One data-integrity item cleared this pass:** the SC Graham seat change (HO 520) — replacing HO 404's now-fixed "P1 OK-Senate still live" line (that was resolved at 411/412; the new integrity finding this pass was SC, now also fixed via a routine roster re-sync, zero code). "Also" notes now run through **HO 520**.)*

**Overall: ~98%.** Recomputed from the reconciled **9** bars (96·100·97·98·98·100·99·98·92 ≈ 97.6) — nudged up from ~97 as **Member depth closed 99 → 100** (HO 526, the participation arc; see provenance). The HO 519 re-derivation had eased it to ~97 when News became a number (CLOSED→98) and the new 92 pillar bar joined the average. CCBT (the Colorado sister project) is downstream port work, **not** a CBT roadmap theme — excluded from this figure.

| Theme | % |
|---|---|
| Foundation | 96 |
| Home | 100 |
| Visualizations | 97 |
| Weekly reports | 98 |
| News signal | 98 |
| Member depth | 100 |
| Races | 99 |
| Committee activity / hearings | 98 |
| Lobbying / amendments / nominations | 92 |

**Baseline provenance.** Foundation (96) / Member depth (98) were **re-derived against live prod state in HO 404** (the full audit) — no longer the off-repo-through-HO-221 baseline. Foundation confirmed at 96 (plumbing healthy; two cron regressions logged as loops, not baked in); Member depth moved 99 → 98 (comprehensive member surfaces, minus the unbuilt member→news + the 463/530 by_size gap). See the "Re-derivation (HO 404)" note below for the evidence. **Home (98→99)** and **Weekly reports (95→96)** were bumped this pass for the committee-activity / hearings arc (the /dashboard-v2 On the Hill band touches Home; the weekly-report COMMITTEE ACTIVITY section touches Weekly reports). **Committee activity / hearings (98, new this pass)** is **effectively complete** — the full 263–269 arc shipped; the open remainder is the two banked items below. **HO 519 re-derivation (#2):** the canonical tracked corpus grew 15,075 → **16,657** (`getCorpusStats(true)`); **News signal was promoted CLOSED → 98** (a now richly-surfaced pillar — `/news` route + rails + race/member hubs — not an excluded thread; `98` because the residue is gated/inert, not active gaps); a **new combined bar `Lobbying / amendments / nominations` was minted at 92** (the 435–463 downstream arc, v1-complete data + aggregate + detail + cross-links, with real banked gaps); every other bar **held** (the 113 HOs since 404 were chrome/infra/enabling, self-declared no-bar-move and confirmed in aggregate by the §1/§2 green audit); and the **SC Graham data-integrity item was cleared** (HO 520, roster re-sync + `backfill:races`, zero code). Overall eased ~98 → ~97 across the 9 reconciled bars. **Post-audit bar move (HO 526):** **Member depth 99 → 100** for the participation arc (B1 chamber-median context, HO 523 + B2 career-votes rollup, HO 525); the **"external scorecards"** half of the old Member-depth hold is **reframed** from a hold to **optional breadth** — USCPR already ships on the hub, and the HO 522 probe established broader scorecards are a maintenance-appetite choice, not a capability gap (VoteSmart NO-GO on access; LCV / Heritage viable-but-unbuilt). This is **one tracked bar move on top of the HO 519 re-derivation, not a new audit** — the anchor stays "As of HO 519", and this clause resolves any apparent table-vs-anchor drift. *(Prior pass: Home 95→98 + Visualizations 90→95 for the HO 230–234 design arc; Races 98 reflects HO 222–226.)*

**Shipped recently (HO 263–269, the committee-activity / hearings arc — effectively complete):** committee-meetings data layer (263) · standalone `/hearings` with grouped list + two-week Mon–Fri calendar (264 / 265) · `/dashboard-v2` On the Hill band (266, corrected onto v2 in 269 so it survives the `/` swap) · committee-detail + bill-hub meeting embeds reusing the list row (267) · weekly-report COMMITTEE ACTIVITY section, fallback variant — markup blocks at current stage, no VIA tags (268). *(HO 230–234 prior: the dashboard design pass; HO 222–226: /races LIST + district maps/modals.)*

The "Also (…)" changelog is append-only and the pointer is frozen-per-block. Close an arc by adding a new block — which carries its own `Also notes now run through HO N` — never by editing a prior block's clause or its pointer. Each block freezes its `run through HO N` at append time, so the "running pointer" is simply whichever block is last: every distinct `run through HO N` appears exactly once, monotonic. A doc sweep therefore adds a pointer via its new block and touches no earlier one — a sweep's grep gate expects the previous block's pointer to still read 1, not 0. Mis-bumping a prior block both duplicates a pointer and violates the append-only rule (the HO 518 §1b near-miss; the HO 418/429 precedent). **A CORRECTION block is the one exception and carries NO pointer** — it corrects earlier blocks rather than closing an arc, so there is no new N to run through, and appending one with a pointer duplicates the N the block above already froze (HO 597 did exactly that; the fix was to strike the clause from the correction block, which is legitimate because a block appended in the same session by the same HO is not settled history — the prohibition protects *prior arcs'* blocks). State the decline explicitly (*"No pointer advance — … the pointer stands at HO N from the block above"*) so a later reader can tell a correction block from one that forgot its pointer.

**Also (HO 270–272), v2 refinements:** the `/dashboard-v2` races strip became a `HEARINGS | RACES` tabbed box with hearings the default tab (270/271) — superseding the standalone On the Hill band — plus a live `MOVES n` badge on the RACES tab (per-card `MOVED · <lean>`; `NEW` dark until the news→race join, 272). Refinements to the already-counted v2 Home + Races surfaces — **no theme-% change** (Home 99 / Races 98 unchanged).

**Also (HO 273–275), v2 coherence sweep — Home redesign now build-complete + coherent.** The `/dashboard-v2` surface was reconciled against the mock (`docs/design/dashboard-2col.html`): hearing meeting titles cleaned at render (273); race-card Kalshi/Polymarket odds party-normalized via the full candidate roster + battlefield/card-dot color reconciled to the incumbent party + MARKETS/SIGNALS tape labels pinned outside the marquee + a ±8% implausible-move tape guard (274); doc sweep (275). All v2 surfaces — hearings, races, tape, feed — now match the mock. **Home held at 99, not bumped to 100:** the residual 1% is the v2→`/` swap, now gated **solely on the `/dashboard-v2` cold-start 500** (backlog OPEN LOOPS, BLOCKER — the HO 238 fan-out cold-abort `/` would inherit on the swap), **not** on any remaining coherence gap. No other theme-% change.

**Also (HO 277–280), outage-fixing arc — swap blocker CLEARED.** The `/dashboard-v2` "cold-start 500" turned out to be the **gated-aggregate mis-plan** class (a summary-gated fat-`bills` aggregate the statless Turso planner drops onto `idx_bills_is_ceremonial`, slow even warm), the same root cause behind live 500s on `/members` (277) and `/bills` (279). All fixed with additive covering/partial indexes + gated `INDEXED BY` hints (280 doc sweep). **Cold-verified:** `/members` 0.885s, `/dashboard-v2` 5× cold 200, `/bills` 0.705s first-hit-after-idle. **Home stays at 99** (the swap hasn't shipped), but its residual 1% — the v2→`/` swap — is now **UNBLOCKED and actionable** (backlog QUEUED), no longer blocked on the cold-start 500. Home → 100 when the swap ships. No other theme-% change.

**Also (HO 281–294), v2 polish — the B-series tweaks + the B2 tape arc.** Header type scale (281: title 26 / nav 16 / active amber underline, ≥701px), the v2 HEARINGS|RACES tabbed-box height/overlay fix (282), the WeeklyBand WoW deltas + a fourth HEARINGS metric + per-metric hover popovers (283/286), the chip-family additive tokens + shared `<SourceTag>`/`<MicroTag>` (287), and the **B2 markets-tape arc** (288–294): MARKETS gained five equities (NVDA/AAPL/MSFT/GOOGL/LMT) + CPI/UNEMP (MO badges); the second strip became **ODDS** — prediction-markets-only (shutdown · Fed-cut · recession, all dual-source K/P); counter-scroll + edge-fade; a curated policy-hook hover line; and the hover box portaled + re-anchored to pop over the ticker. Out-of-band: the weekly-report cron made reliable via a daily catch-up (284/285). All of this is v2 polish on the already-counted Home surface plus the shared WeeklyBand/header (both render on `/` and `/dashboard-v2`) — **no theme-% change; Home held at 99** pending the still-unshipped v2→`/` swap (the residual 1%, backlog QUEUED). The existing id/topic/stage/party chip migration is queued into a **B6 arc** (chip-family spec; 287 shipped only the additive half).

**Also (HO 296–303), v2 feature-complete — the B6 feed-row arc + the B2 tape update.** The **B6 arc (296–300)** rebuilt the `/dashboard-v2` feed as one shared `V2FeedList` row across MOVERS / TOP STALLS / NEW THIS WEEK (metric the only per-tab swap): collapsed rowhead with a sans title (297), the 6-node stage bar with a data-driven ping/park/complete current node (298), the expanded RELATED block (NEWS + omit-when-empty HEARINGS; ODDS dark — no bill↔market data, 299), and the sponsor hover card + meta column (300, which pushed the full sponsor enrichment onto the three v2 feed queries). This is the **first `--sans` use in CBT** (feed prose; mono stays for chrome). The **B2 tape update (301–303)** added a 4th ODDS pair **FED CUT SEP** (302, via new `kalshiEvent`/`polyMonth` symbol pins) and the **richer four-part hover box** + the symbol-amber/value-white tape convention (303); the 301 re-probe reaffirmed the data walls and banked a US-DEFAULT pair as the only honest debt-risk fill. **With this the v2 dashboard is FEATURE-COMPLETE** — no remaining v2 build work, only the swap. **Home held at 99**, not 100: the residual 1% is the still-unshipped **v2→`/` swap** (backlog QUEUED, now READY — open decision: replace `/` outright vs keep an archive route). No other theme-% change.

**Also (HO 311–314), the v2→`/` swap + the markets/reports arc — Home redesign SHIPPED.** **HO 311** made the v2 redesign the live home page: `/` now renders the DashboardV2Header + HEARINGS|RACES box + WeeklyBand + 49/51 body with the summary-gated count; the old `/` is preserved unlinked at `/dashboard-classic`, and `/dashboard-v2` permanently redirects to `/`. The shared stage funnel / topic treemap render in a new opt-in **`staticMode`** at `/` (read-only, no `?stage=`/`?topics=` rebasing — the page takes no `searchParams`); `/dashboard-classic` keeps full click-to-filter via a **`basePath`** prop. **This ships the Home redesign theme → Home 99 → 100.** The residual click-to-filter-parity-vs-static decision and the `/dashboard-classic` sunset are banked (backlog OPEN LOOPS), not blockers. Out-of-band, same arc: the **markets tape freeze** was diagnosed (313) as GitHub Actions scheduled-cron delivery (not a fetch failure) and fixed (314) with a reliable **Vercel daily cron floor** at 21:30 UTC; and the **reports-index-stale** report (312) closed as a **no-op** — it self-resolved when the HO 285 catch-up landed June 8 and revalidated, both bug hypotheses refuted by code. **Overall ~97 → ~98** with Home complete. No other theme-% change.

**Also (HO 316–317), the bill-row standardization arc — collapsed + expanded, both surfaces, COMPLETE.** **316** unified the topic chip into the shared bordered **`TopicChips`** (the dashboard feed + every `BillRow` surface; the lone borderless holdout is `/bill/[id]`). **317** unified the **expanded panel** into the shared **`BillExpandPanel`** (containing the one shared **`BillStageBar`**), rendered identically on the dashboard `/` and `/bills`, differing only in trigger — dashboard hover (`:hover`/`:focus-within`, box grows inline) vs `/bills` click — built to `docs/design/expanded-panel-unified.html`; the `/bills` collapsed title also moved to the sans face. **No theme-% change** — this polishes/unifies already-counted surfaces (Home is at 100; the `/bills` row touches the off-repo Foundation baseline, not re-derived). Banked from the arc: the rich HEARING slot, the dead-CSS sweep, touch-expand, and the `/bill/[id]` chip migration (backlog). No other theme-% change.

**Also (HO 319–321), the dashboard-row arc — interaction + row polish, COMPLETE.** **319** reverted the dashboard expand from the HO 317 hover model back to **click** (it was transient + dead on touch) — both surfaces now expand on click via `useSingleOpenPanel`. **320** restored the **distributions click-to-filter** on `/` (ported from `/dashboard-classic`, deferred at the HO 311 swap as `staticMode`): clicking a STAGE bar / TOPIC tile rebases the gated distributions + MOVERS + BREAKING, with `ActiveFilterStrip` — closing the HO 311 open loop. **321** made the collapsed feed row **two lines** (title over sponsor + `[party-state]` + chips), matching `/bills`. **No theme-% change** — all three polish the already-at-100 Home surface; `/dashboard-classic` untouched. No other theme-% change.

**Also (HO 328), the Members + Committees merge — one surface.** `/members` became a two-pane browser: a committee rail (the former `/committees` index — that route now `redirect("/members")`) + the member list, the chamber filter driving both panes, click-a-committee scoping the roster, per-member topic-mix bars (`MemberTopicBar`), and an UPCOMING HEARINGS rail group. The HO 152/197 productivity scatters were dropped (`getSponsorProductivity` + `SponsorProductivityRow` deleted, HO 330); a new `idx_bills_sponsor_topics` covering index serves the topic-mix bar. **Member depth 90 → 93** — the committee index, member roster, and per-member committee membership now read from one consolidated surface, with committees folded in from the retired standalone index. The member-expand cold-start 500 is under separate diagnosis (HO 329, fix pending). No other theme-% change.

**Also (HO 333), the Electoral consolidation — Races + Primaries → one surface.** `/races` + `/primaries` collapsed into a single **`/electoral`** surface (both 308-redirect in): the competitive map on top + a NEW primary-calendar **timeline band** below it (`PrimaryTimeline` + the `ElectoralBoard` wrapper), wired so clicking a timeline date paints that date's voting states amber on the map (selections accumulate / drop-on-reclick / CLEAR ALL; hover previews an outline). Sen+House COMBINED per date; data via the new **uncached `getPrimaryCalendar(2026)`** (no primaries cache tag exists, matches `getDashboardPrimaries`). The electoral GroupTabs sub-nav (HO 173) is retired; the nav item is relabeled **Electoral**. **State-click → the HO 225 district modal was KEPT** (Corey's call — a deliberate deviation from the handoff's "read-only map," preserving the district drill + HO 236/237 metro panels). The HO 226 primaries recency-map + its modal/scrubber are superseded by the timeline (`PrimariesMap`/`PrimaryDistrictModal`/`RacesMap` left unreferenced — backlog open loop). **Races 98 → 99.** No other theme-% change.

**Also (HO 346–352), the Patterns / Trends / Stale / Reports refinement arc.** **346** reframed the `/patterns` meta line as **LEGISLATIVE PATTERNS** with a new blurb; **347** replaced the `PatternBubbleSVG` bubble cluster with ranked horizontal bars (`PatternBars`, length = bill count, color = % past committee) that also absorbed the old ALL PATTERNS table; **348** added the collapsible **Filler Watch** strip (CEREMONIAL · NON-BINDING) between the meta line and the blurb, every number live-interpolated; **349** cut the redundant TOTAL INTRODUCTIONS line chart from `/trends` (the stacked-by-topic chart already carries that envelope). **350** reframed `/stale` as a **stage-led** surface — default PAST COMMITTEE (`floor`/`other_chamber`/`president`, enacted excluded), sorted legislative-stage-DESC then furthest-action-first, atop the 60-day floor — plus a curated procedural-housekeeping filter (frozen opening-week IDs + an `electing members%` live pattern + a blocklist) with an INCLUDE PROCEDURAL escape hatch and a committee-backlog footer; the ship build's 73s cold query was fixed in `339f8fc` (a CASE `ORDER BY` defeating the date index — oddities). **351** added a **filler-share bar** to the dashboard `WeeklyBand` (broad `is_ceremonial=1 OR cluster IN (four)` definition). **352** added a **stage-movements ladder** by destination stage to the weekly report. **Visualizations 95 → 97** (the `/patterns` bars + filler strip + `/trends` trim), **Foundation 95 → 96** (the `/stale` reframe), **Weekly reports 96 → 97** (the report stage ladder); Home held at 100 (the WeeklyBand filler bar renders on the already-complete home). **Overall holds at ~98** — these are within-theme refinements; Member depth (93) anchors the remainder. No other theme-% change.

**Also (HO 358–361), the Reports section arc + the multi-user landing — arc COMPLETE.** **358** added the weekly report's **Floor votes** section (after the 352 stage ladder): named decisive votes (bold verb · chamber · linkified bare-ID bill or `nominee → position` · margin · contested party split), procedural/amendment votes collapsed, recess states handled. **359** made **`/president` a real in-surface page** (its own `HeaderBar` + feed sub-nav + `StageLegend` + `BillRowList`, active tab marked) instead of the HO 151 `/bills?stage=president` redirect, so CHANGES/PRESIDENT/REPORTS all render in-surface. **360** added the **outcome-verb color rule** to `ReportMarkdown` (exact-text match, render-only — `content_md` stays plain bold). **Weekly reports 97 → 98** for the floor-votes section + color rule (the 352 ladder was already counted last pass — not double-counted; 359 is reports-surface chrome). Separately, **361** shipped **B1 — the `/welcome` split-layout landing** (cookie redirect from `/`, no middleware; live MOVERS/tape/readout reusing `/`'s cached queries; `signIn("github")`), **closing the multi-user arc (A1+A2+B1)**. That auth/landing arc (355/356/361) is **net-new direction, not one of the tracked themes** — no theme bar minted for it. **Overall holds at ~98** — within-theme reports refinements; Member depth (93) still anchors the remainder. No other theme-% change.

**Also (HO 363–365), the B2 tape restyle + the B5 week-strip redesign — dashboard polish.** **363** restyled the v2 two-tape stack (MARKETS/ODDS strip restyle + an inter-instrument pipe divider + even K/P pair spacing). The **B5 arc (364 probe + 365 build)** refreshed the `WeeklyBand`: a **middot-separated** strip with a dated `<MON DD> report →` link (most recent finalized report) + FILLER folded in-run (365 1/2, SHA `5524852`), and a **rich per-metric hover card** (`WeeklyBandMetricCard` — sparkline + breakdowns, replacing the flat HO 284 popover) backed by five new helpers + the canonical `MEETING_TYPE_BUCKETS` 9→3 map (365 2/2, SHA `eda88bc`). Window semantics are **trailing-7d**, not calendar week-to-date (a mock-inferred premise caught in plan mode — oddities). **No theme-% change — Home is already at 100** (these polish the shipped home redesign + the shared `WeeklyBand`/tape, both rendering on `/` and `/dashboard-v2`); the doc-sweep handoff's "bump the home-redesign %" had no headroom to bump. No other theme-% change. *(Flagged for their own pass, not folded here: the HO 364 brand rename — already reflected in SKILL's brand note — and the HO 366/367 `/hearings` + `/members` header consolidations.)*

**Also (HO 373–376), the B2 hover/tape arc — fit-and-finish on the markets tape.** **373** probed `market_ticks` history for a hover sparkline (read-only; settled the roll-stability/monthly-cohort/dead-symbol premises). **374** added a **7d sparkline + 1W delta** to the tape hover (gated ≥2 points / a 7d anchor; the monthly CPI/UNEMP cohort suppressed; odds spark reads the Kalshi series only). **375** fixed the hover-card clipping by switching to the shared `computeTooltipPosition` (measure + four-edge clamp + `preferBelow` to clear the masthead — HO 294's hardcoded 160px/grow-upward was the bug) and added the odds **`closes <MON DD>`** freshness line (the resolution date was already persisted as `market_ticks.market_date`, so render-only — no new store; replaced the redundant "prediction market" clause). **376** shuffles each scrolling strip's order once on mount (independent per strip, hydration-safe) and slowed the marquee ~1.75× (`SCROLL_SPEED_PX_S` 42→24). **No theme-% change — Home is at 100**; all four polish the already-shipped markets tape (renders on `/` and `/dashboard-classic`). A manual prod eyeball is owed (BotID blocks headless hover — backlog OPEN LOOPS). No other theme-% change.

**Also (HO 379, 382–386), the stage-correctness arc + the smoke harness — no theme-% change.** **379** stood up a Playwright route crawler (`e2e/smoke.spec.ts`, prod-default: per-page 200 / no-failed-request / no-console-error + targeted interactions), which caught a prod **filtered-view 500** hidden behind the `ct_seen` gate. **381/382** fixed it — not a logic throw but an unbounded corpus-wide scan on the index-driven filtered stage/topic distributions + the giant-`committee`-bucket feed (date-drive at `FEED_DATE_DRIVE_MIN`=2000). **383** made `computeStage(latest_action_text)` the deterministic **sole authority** for `stage`, written independent of summarization (so a stage advance no longer hides behind the summary-gated home bar), + presentment-queue priority + sync-side transition logging. **384** a one-off corrective stage backfill; **385** extended `computeStage` coverage rules + a 37-case unit suite under the locked floor-rung definition; **386** stripped the dead LLM `stage` output (summarize contract now `{topics, is_ceremonial}`). This is **correctness + test infrastructure, not new theme surface** — **Foundation held at 96** (the block's rubric bumps for shipped theme surface, not bug-fixes/harnesses; "don't invent movement"), **overall holds at ~98**. Residual loops banked/watched (CI-wiring, the ~75-bill `floor→committee` downward correction, the B-sync transition-logging soak, the FEED_DATE_DRIVE margin — backlog). No other theme-% change.

**Also (HO 388–390), the stocks-and-money arc — Member depth surfaced.** **388** probed the stocks/money sources: FMP congressional-trading is **healthy** (HO 70 rebuilt onto `/stable/{senate,house}-latest`, 548 rows, 0 unmatched, bioguide-keyed — NOT dead), while a donor **industry** rollup has no free classified source (banked). **389** shipped the corpus-wide **`/trades`** index — a `stock_trades` recency feed reusing `TradeRow` + a most-traded-tickers rollup, `?member=<bioguide>` scoping to one member, breadcrumb nested under Members. **390** added the **donor small/large-dollar split** (FEC Schedule A `by_size`, off the cached `fec_candidate_id`) beside `MemberFundraisingLine` on the member hub, riding `sync:fec` with **no new cron slot** (shipped `3e57740`, prod-verified, 463/524 populated). **Member depth 93 → 98** — the member surface now carries a dedicated trades index + a donor-composition cut. The **OpenSecrets-style employer/industry rollup stays banked** (no free classified replacement; would need our own classifier). No other theme-% change.

**Also (HO 392–395), the PAC-IE direction feature + the observation layer.** **392–393 (Races surface):** a **`PAC SPENDING`** line on race surfaces showing who a pro-Israel super PAC (UDP `C00799031`, AIPAC's affiliated super PAC; labeled "AIPAC SUPER PAC · via FEC") is **backing / opposing** per seat — **direction only**. Dollars are deliberately NOT rendered in-app (FEC Schedule E has no clean dollar source — the ~5× F24/F3X double-count, banked in oddities `fd8eb7c`); each direction **deep-links to the live FEC IE browser** instead. New `pac_ie_spending` table + manual `sync:pac-ie`. Within-theme Races overlay — **no theme-% change**. **394–395 (News signal):** the **news-as-observations pilot adopted + live** (`4fd9c9d`). `observations` + `observation_entities` are CBT's **entity-resolved news layer**: the news cron dual-writes one entity-resolved `observation` per fetched article alongside the unchanged `news_mentions` write, resolving people/orgs to `bioguide_id`/`system_code`/caucus-slug deterministically (LLM proposes names, code resolves IDs). Gate C closed on a deployed tick (+15 obs, 45.8s, 0 dual-write errors). **News signal was already CLOSED — no % to bump**; the layer's payoff is that **member→news and race→news are now buildable** (the store exists; `observation_entities` joins to `races` via `incumbent_bioguide_id`, EXPLAINs clean) — graduating race→news from "the wall every surface hits" to a queued surface (backlog). Several envelope fields (reliability/credibility/cluster_id/supersedes/schema_version) ride dormant as reserved constants. No other theme-% change.

**Also (HO 402), the member ID crosswalk — enabling infrastructure.** Two additive tables from unitedstates/congress-legislators (`member_ids` scalar external IDs + flattened `member_fec_ids`) now bridge `bioguide_id` → **ICPSR** (Voteview, ~60% native), **wikipedia_title** (pageviews, ~full), **GovTrack/LIS/OpenSecrets/Ballotpedia** titles, and the **authoritative FEC candidate-ID array** (money). One YAML sync (`sync:crosswalk`), run right after `sync:members`, gated on known bioguides so it can never invent a member; one read-only coverage diagnostic. Live coverage: 537/537 current matched (100%), icpsr 59.6%, wikipedia 99.8%, 605 distinct FEC candidate IDs. **ID crosswalk landed — Voteview/pageviews/FEC-money bridges unblocked.** It's **plumbing, not user-facing completion**, so **Member depth 98 → 99** (a single point; the annotation carries the weight — the surfaces that consume these bridges are still to build). Actionable output banked: 3 fuzzy-vs-authoritative `fec_candidate_id` disagreements (Gillen/Ivey/Self) feed a future `sync-fec` switch; `member_social` deferred (no consumer) — both in backlog. No other theme-% change.

**Also (HO 398), race→news on the race hub — within-theme Races add.** `5f26ee9` shipped `getRaceNews(incumbentBioguideId)` reading the **observation layer** (`observation_entities` join on `entity_type='person' AND entity_value=incumbent_bioguide_id` → `observations`), rendered as a NEWS section on `/race/[id]` (`RaceHubBody` + `RaceNewsRow`). This is the **first consumer of the HO 394 observation layer's entity join** — the payoff the layer was built for. **Scope is an honest v1:** race-hub page only (gated `!preview`, not the dashboard drawer), **incumbent-only** (by bioguide, so a challenger surfaces only if already a sitting member), open seats get the empty state, and **no member-hub news surface** (`getMemberNews` doesn't exist). So it **partially** discharges the banked "race→news / member→news surface": race→news (hub, incumbent) is DONE; member→news is still unbuilt (backlog). Within-theme Races overlay powered by the already-CLOSED news layer — **no theme-% change; Races held at 99** (incumbent-only v1 doesn't complete the theme). Live coverage at audit: 15 of the 2026 incumbents have ≥1 resolved person-observation, 70 observations / 65 entity rows total.

**Re-derivation (HO 404), the full audit against live prod state.** Read-only sweep (deploy SHA `7749de5` confirmed live; §1–§3 numbers in the HO 404 handoff). **Foundation held at 96:** plumbing is structurally sound — bills sync fresh (`MAX(update_date)` = 2026-07-02), `bills_fts` fully populated (16,623 == corpus), every forced `INDEXED BY` present, cold-start audit **0 unexpected BAD of 50**, all seven latency-watch routes 200 (warm 0.25–2.5s, `/changes` 0.25–0.76s well under its 8.5s fuse). The two blemishes are **operational cron regressions, not structural gaps**, so they're logged as backlog loops rather than dropping the bar: `/api/cron/summarize` intermittently timing out with the summary backlog **grown 1,011 → 1,374**, and `/api/cron/weekly-report` **erroring on its 06-29 run** leaving reports 2 weeks stale (latest = week-of Jun 15–21). **Member depth 99 → 98:** the member surfaces are comprehensive and live — hub (bio/committees/votes/badges/palestine/trades/donor-split), `/members` roster (552/537), `/trades` index, the HO 402 crosswalk (member_ids 537/537, member_fec_ids 605) — but two real gaps keep it off a near-complete score: **member→news is unbuilt** (HO 398 shipped race→news only) and the **FEC by_size split is 463/530** (the HO 390 rate-limited ~67 never self-healed). Canonical corpus counts settled this pass: "tracked" = `getCorpusStats(true)` = `(is_ceremonial=0 OR IS NULL) AND summary IS NOT NULL` ≈ **15,075**; the old five-number masthead incoherence is resolved (inner-page counts removed HO 323/326; only sunset `/dashboard-classic` still shows the ungated ~16,449). **Overall recomputed from the reconciled bars: ~98% holds** (96/100/97/98/98/99/98). No other theme-% change.

**Also (HO 405–413), the cron-reliability fixes + the senator-integrity arc — correctness, no theme-% change.** The two operational cron regressions the HO 404 anchor logged as backlog loops (not baked into Foundation's bar) both **shipped and cold-verified**, so **Foundation's HO 404 cron fuse is defused — the bar holds at 96** (never docked; the fuse only threatened a future dock if left unfixed): **`/api/cron/summarize`** doom-loop fixed (HO 405 probe / 406 fix, `82fb6bc` — partial `idx_bills_summarize_queue` forced `INDEXED BY` + a C=5 worker pool; one real prod tick 80 summarized/0 fail, the 1,374 backlog drained to 7), and **`/api/cron/weekly-report`** `gatherReportData` misplanners fixed (HO 407, `33cc707` — six `INDEXED BY` hints, the 4th appearance of the stateless-planner class; erroring cron #559 → #673 success 13.5s, stale weeks regenerated + catch-up hardened with a machine-readable `report-catchup-failed:` health field). Separately, the **senator-year integrity arc landed** (HO 408 `d234531` · 410 audit · 411 `db9d910` · 412 `a3a2a18`): the HO 404 anchor's "P1 still live" (`S-OK-2026` → Lankford, plus the `2031` impossible-year smell) **is discharged** — the underlying systemic drift it flagged (18/100 current senators on a wrong-anchor `senateTermStart`+6 derivation) root-fixed by deriving year-pairs from `members.senate_class` (residue-mod-6, `now`-relative), with the S-OK card corrected to appointee Armstrong, two phantom race rows deleted, 17 stale future-cycle cards self-healed by the `backfill:races` upsert, and a standing integrity audit committed as the regression guard. Per the block's rubric this is a **correctness fix, not new theme surface** (line: "bumps for shipped theme surface, not bug-fixes/harnesses") — so **Member depth held at 98 and Races held at 99**; the 17 corrected cards don't complete either theme, and the still-owed member→news + 463/530 FEC by_size gaps are what keep Member depth off a near-complete score. **Also notes now run through HO 413.** No theme-% change.

**Also (HO 414), member→news on the member hub — one of the two Member-depth-98 holds closed.** `3e7d1f1` shipped `getMemberNews(bioguideId)` reading the **same observation layer** as HO 398's race→news (`observation_entities` `person` join → `observations`), keyed on the member's **own** bioguide (no incumbent hop), rendered as a **`News · in the press`** section on `/members/[bioguideId]` (inline server fetch in the existing `Promise.all` + reused `RaceNewsRow`; the member page has no shared body/drawer, so **no `MemberHubBody` split** — the handoff's assumed split was moot). **Second consumer of the HO 394 observation layer** (race→news was the first, HO 398). Cold-EXPLAINed to the 398 covering-index path (`GROUP BY o.obs_id` didn't replan → no `INDEXED BY`); the new `member-news` cache tag is flushed by the news cron beside `race-news` **and** allowlisted on `/api/revalidate` (bogus-tag 400 control confirmed the allowlist entry genuinely took). Prod-verified: `/members/J000299` renders the 4-row section, low-profile members get the empty state. **Member depth held at 98, NOT bumped to 99:** this closes only **one** of the HO 404 audit's two Member-depth holds — member→news is now built, but the **FEC `by_size` split remains** (463/530 at the audit; the ~67 rate-limited members need a **deliberate** `sync:fec` run, not passive self-heal — backlog OPEN LOOP). The 98→99 bump waits until `by_size` is confirmed populated too; don't bump off member→news alone. **Also notes now run through HO 414.** No theme-% change.

**Also (HO 415–416), the FEC member-fundraising completion arc — Member depth 98 → 99.** The HO 404 audit's two Member-depth holds are now **both discharged** (member→news shipped at 414; the FEC `by_size` gap closed here), so **Member depth 98 → 99**. **HO 415 (`c36f0a2`), the by_size cycle-key fix:** `fetchFecBySize` hard-coded `cycle=2026`, but `schedule_a/by_size/by_candidate` keys to the candidate's **election cycle** — so off-cycle senators (Class 1/3, up 2028/2030) returned empty at 2026, which is the entire "463/530 coverage gap" (never rate-limiting, a keying bug). Fixed with `nextElectionYear ?? CYCLE` (HO 411's class-derived, specials-aware column — one source of truth); by_size **464 → 530**, `bysize_empty=0`. This answered the standing **HO 390** question — **there is no genuine by_size floor**; the whole "empty-for-valid-ID" bucket was the bug, 100% recovered. Carried the **rate-hardening** the 60/hr `K0Ue` key forced (cap-55 clean-exit / 1100ms pacing / `skipped_fresh` resume cursor). **HO 416 (`62b7f8e`), the `totals_failed` `candidate_id` sweep:** 2 departed members (Grijalva, Greene) dropped via an `is_current=1` filter on `fetchPendingMembers`; **8 House→Senate 2026 switchers** re-resolved to live Senate ids via a new `data/fec-candidate-overrides.json` (the 2nd resolver-consulted `data/` override after the specials file); Marshall re-resolved through the same override (a stale/live scoring-tie case); Zinke + Barrasso left as named no-action residuals (unfiled / upstream FEC gap). A **"Senate 2026 campaign"** tag on the fundraising line is **computed** from resolved id being S-prefix while `chamber='house'`. by_size **530 → 539**. Per the block's rubric these **complete the audit's two named Member-depth holds** (surface completion, not a bug-fix harness), so the bump is earned; the deferred member-scorecard arc (career vote participation + external scorecards) stays banked as the path to 100 — **not** closed here. **Also notes now run through HO 416.** No other theme-% change.

**Also (HO 418), housekeeping — no theme-% change.** Struck the stale metro-zoom panels (spec-3 Phase 2) entry from the Banked/unbuilt line: it shipped at HO 236/237 (backlog DONE) but the banked entry was never pruned, which had been feeding a stale "next feature = metro-zoom" pointer across session wraps. No feature shipped, no bar moved. **Also notes now run through HO 418.**

**Also (HO 419), the Voteview ideology data layer — enabling data, no bar move.** `da2745d` shipped the **member DW-NOMINATE layer**: a new `member_ideology` table (bioguide PK, `icpsr`, `nominate_dim1/2` + the Nokken-Poole per-congress variant, `number_of_votes`, `conditional`) populated from Voteview's `HS119_members.csv` by a manual **`sync:ideology`** (run after `sync:members` → `sync:crosswalk`, members-gated so it can't invent a member, max-`number_of_votes` dedup for a bioguide's multiple 119th rows, idempotent `ON CONFLICT` upsert), plus a shared `scripts/voteview-source.ts` fetch/parse helper (vendored quote-aware CSV parser — the `bioname` comma trap) and a read-only coverage diagnostic. It bridges **`bioguide_id` → DW-NOMINATE**: `nominate_dim1` is the headline economic left/right axis, `nokken_poole_dim1/2` the per-congress variant. **The join is on Voteview's native `bioguide_id`, not the HO 402 crosswalk's ICPSR bridge** — the "~60% via ICPSR" premise was measuring the wrong join and is retired (oddities). Coverage is essentially complete for current members who've cast a roll call (552 scored / 1 NULL-dim1 / 0 conditional). **No theme-% change — this is enabling data with no user-facing surface, and Member depth is already at 99** (100 stays reserved for the deferred member-scorecard arc). The **first ideology surface** — a member-hub ideology line, an ideology axis on the `SponsorProductivityScatter`, or chamber/party medians — is deferred to a follow-on (backlog QUEUED). It also surfaced a `members.party` data bug (Kiley reads `I`, is a Republican — backlog OPEN LOOP). **Also notes now run through HO 419.**

**Also (HO 421–422), the first ideology surface + the Kiley party finding — no bar move.** **HO 421 (`99de3c1`)** shipped the **first ideology surface** off the HO 419 `member_ideology` layer: a compact **DW-NOMINATE axis on the member hub** (`getMemberIdeology` + `MemberIdeology`), placed with the voting-record stats. It's a fixed **−1..+1** rail (comparable across members, not chamber-scaled) with the member's party-colored marker, both chamber party **medians computed app-side**, `dim1`/`dim2` numerics, and an empty state for the 1 member with too few votes. The axis is **vertically banded** — party medians as ticks above the rail (with a contrasting core), the member's identity as a caret below it, a 2% domain inset for near-edge markers — so a member sitting on their own party median stays legible (the on-median collision). **HO 422** is this doc sweep plus a **correction**: the HO 420 "Kiley party bug" is **NOT a bug** — Kevin Kiley (CA-03) genuinely **switched Republican → Independent on 2026-03-09** (both Congress.gov `partyHistory` and congress-legislators `party_affiliations` confirm), while **caucusing Republican**; `members.party='I'` is correct, and Voteview's `party_code=200` (R) follows **caucus, not registration** (oddities). So no code fix landed — the loop closes as diagnosed, and a small follow-up to teach the ideology diagnostic the caucus-vs-registration distinction is queued (backlog). **Member depth held at 99** (Corey's call): the ideology axis rides as enabling surface; **100 stays reserved for the member-scorecard arc.**

**Also (HO 424–425), the two chamber-ideology surfaces — no bar move.** Two more surfaces off the HO 419 `member_ideology` layer, both live, no new tables. **HO 424 (`127b798`), the dashboard chamber polarization band:** a new `getPolarizationBand()` (all four chamber-party `dim1` medians independent of any member — `getMemberIdeology` is member-chamber-scoped and returns only a pair, so it couldn't be reused) feeds a `PolarizationBand` server component placed full-width between `RacesBoxTabs` and `WeeklyBand` (the battlefield moved inside `RacesBoxTabs`' non-default tab at HO 270/271, so "below the battlefield" resolved to this always-visible slot). It extends the HO 421 positioned-tick rail (**divs + CSS**, not SVG) to chamber-aggregate scale: two chamber rails (HOUSE/SENATE) on a fixed −1..+1 domain (the 2% inset), D and R median ticks, an amber gap line spanning them, the gap magnitude as the hero. **HO 425 (`910f5c4`), the `/members` polarization dotplot strip:** a new `getIdeologyStrip()` (the full scored population, one cache entry) feeds `IdeologyStrip`, a client-island SVG Wilkinson dotplot (`binw 0.02`, party-colored, strict-D/R median ticks pinned to the band's method inline since a client island can't import the server `median()`, empty-center annotation, hover name + dim1 + hub, click-through), ~180px at the top of `/members`, server-scoped to the chamber toggle only. It's the **third live hand-rolled SVG chart** (`BillsTimeSeries`, `CommitteeActivityChart`, `IdeologyStrip`); the over-time chart (below, data-gated) is the fourth, where the shared scatter scaffold gets extracted. **The finding:** both chamber gaps read **0.92** on dim1 (House and Senate) — the "Senate is the calmer chamber" assumption doesn't hold on real data (oddities); cross-checked band-vs-strip, 0 `member_ideology.chamber`-vs-`members.chamber` mismatches across 552 rows, coverage 551 scored / 1 unscored. **Member depth held at 99** — these ride as enabling ideology surface, **100 stays reserved for the member-scorecard arc**; the **over-time chart (ship 3) remains, data-gated** on a per-Congress D/R chamber-medians sync (backlog). **Also notes now run through HO 426.**

**Also (HO 427–428), the polarization-over-time chart — the ideology arc CLOSED.** The third and last cut off the HO 419 `member_ideology` layer, so the three-surface arc (band 424, dotplot 425, over-time 428) is complete. **HO 427 (`e0601dc`)** shipped the data layer: a `polarization_history` table + a manual **`sync:polarization-history`** aggregating Voteview's full `HSall_members.csv` to per-Congress/per-chamber D/R `dim1` medians (caucus, `party_code` 100/200; 190 rows, 95 Congresses/chamber, two-party from the 34th/1855) + a read-only coverage/cross-check diagnostic + a `voteview-source.ts` extension for the full HSall fetch. **HO 428-A (`83f0952`)** extracted the shared SVG scaffold to **`components/svg/`** (`ChartFrame`/`SvgAxisX`/`SvgGridY`/`SvgLegend`) — the standing "extract at the fourth chart" trigger fired — and retrofitted `BillsTimeSeries` + `CommitteeActivityChart` onto it (byte-identical render verified); `IdeologyStrip`, the client-island dotplot, is the deliberate holdout. **HO 428-B (`11f2122`)** shipped `PolarizationOverTime` on `/members` (collapsed by default, above the dotplot strip). Per HO 427's "let the real curve set the narrative": the historical **line runs to the 118th** (caucus) with the **current dot read live from `getPolarizationBand`** (registration) — the two agree to three decimals (0.92/0.92) so the caucus/registration seam is invisible but real (not line-connected across 2023→2025); the **midcentury low is computed per chamber — House 1947, Senate 1943 (gaps 0.50/0.47), not the mockup's shared 1961**; no prose caption. **No theme-% change** — enabling ideology surfaces; **Member depth holds at 99**, 100 reserved for the member-scorecard arc. **Also notes now run through HO 428.**

**Also (HO 430), the ideology-arc tail — correctness/refactor, no theme-% change.** Two small isolated cleanups retiring the last two ideology-arc follow-ups (both had ridden in backlog QUEUED since HO 429), same **correctness-not-new-surface** bucket as the HO 405–413 arc — no bar move. **HO 430a (`6329a0f`), the `median()` extraction:** the four byte-identical inline copies (server `lib/queries`, the `IdeologyStrip` client island, the sync + diagnostic scripts) collapsed into one zero-import leaf module **`lib/median.ts`** — canonical definition, four importers, so nobody re-inlines it; a pure refactor, the 119th history-vs-band cross-check held at 0.925/0.918 delta 0.000. **HO 430b (`d061284`), the ideology diagnostic caucus fix:** `ideology-coverage-419.ts` now applies the **neither-match invariant** (flag only when Voteview's letter matches neither registration nor caucus) instead of a registration-only equality check, suppressing the structural caucus-vs-registration gap while still catching a genuine sync mismatch. **Premise correction:** the backlog claimed King, Sanders, *and* Kiley tripped the diagnostic; the live run showed only Kiley did (King/Sanders code Voteview 328→I and already agree with `members.party='I'`) — the HO 422 caucus-vs-registration distinction confirmed on live data (a new oddities entry records the premise correction — the old "all three flagged" read was wrong). Diagnostic-only; no `members`/`sync` touch (Kiley's `I` is correct, HO 422 NOT A BUG). Docs: HO 431. **Also notes now run through HO 430.**

**Also (HO 433), the cron-cadence pass on Vercel Pro — infrastructure, no theme-% change.** CBT moved Hobby → Pro, retiring the **once/day cron cap** (Hobby rejected any sub-daily schedule at deploy, so the ten hand-staggered 09:00–21:30 UTC slots were a scheduling artifact, not a design). `vercel.json` cadences reset to what each cron actually wants: **`/api/cron/summarize` `0 13`→`*/10`** — continuous drain, which **discharges the HO 404 summarize backlog loop**: summarize ran once/day, so on any day inflow exceeded one 60s drain the queue grew (1,011 → 1,374 at the HO 404 audit); HO 405/406 made a single tick self-heal but daily cadence let it re-grow — at `*/10` the drain is continuous and the loop can't re-form. Also **`/api/cron/news` `0 14`→`*/30`**, **`/api/sync` `0 9`→`0 */6`**, **`/api/cron/committees` `30 11`→`0 */12`**; votes / rating-history / primaries / race-ratings / weekly-report unchanged (correctly daily / weekly / event-driven); **`maxDuration: 60` untouched everywhere** (frequency replaces per-run volume, so no run needs more than 60s). Two logic fixes shipped **ahead of** the cadence flip in history (config must not deploy before them): a **summarize overlap lock** (`lib/cron-lock.ts` — an atomic `dashboard_state` advisory claim, self-healing on a 120s TTL; Vercel Cron is best-effort *at-least-once* and the drain isn't overlap-safe, so two concurrent invocations would double-spend Gemini and double-write the append-only `stage_transitions` log), and **news dedup-before-LLM** (skip the Gemini matcher for any article whose deterministic `obs_id` already exists — `storeObservation` freezes entities on first insert, so re-matching a seen article at a tighter cadence was pure token waste). Dedup **live-verified** on `cbt-csu-j3`: an immediate second news tick dropped `llmCalls` 23→0 with `llm_skipped_seen` 55/55, wall-clock 43s→17s. **Markets + per-seat Kalshi/Polymarket odds deliberately stay on GitHub Actions** (markets-tick 30m, markets-daily 4h, kalshi-tick 2h); the Vercel `30 21` markets floor was a non-firing HO 314 artifact, left as-is. **Migrating those three workflows GitHub → Vercel is flagged as its own follow-up HO** — a *reliability* play (HO 313 measured GitHub dropping ~20% of scheduled slots), **not a cadence change**, needing query-path / FRED-gating / workflow-retirement care to avoid double-writes. **No theme-% change** — cron infrastructure, no user-facing surface. Docs: this handoff renumbered 432 → 433 (HO 432 is the race-news badge/popover). **Also notes now run through HO 433.**

**Also (HO 434–435), the LDA lobbying pillar — data layer shipped, no theme-% change.** The roadmap's "best next pillar" call landed as an additive data layer (source probe 434 → build 435). **Source:** the lda.gov API (the legacy `lda.senate.gov` host is sunsetting ~2026-06/07; built against `lda.gov`). **HO 434** probed it GO on a partial bill-number join; **HO 435** (`39f6e54` data + `72c41ee` cron) built three additive tables — `lda_filings` / `lda_activities` / `lda_activity_bills` — ingesting current-Congress LD-2 quarterly reports via a resumable **DB-frontier** sync (`sync:lda`; resume = `MAX(dt_posted)` per `(filing_year, filing_type)`, no stored cursor) on a **daily-300s** `/api/cron/lda`. **Corpus:** 108,707 filings / 233,499 activities / 173,885 bill-links / 5,165 registrants / 26,920 clients. **Join reality:** 99.3% of parsed IDs resolve, but only **22.8% of activities carry a bill number** and **25.4% of filings have ≥1 joined bill** — so issue-code (BUD/HCR/TAX/DEF top) is the spine, the bill join the overlay. A 40-row residual (0.037%) on the closed 2025 quarters is accepted DRF count-vs-enumerable drift, not a gap (oddities). **No theme-% change** — enabling data with no user-facing surface; the LDA surface is HO 437 (bill-keyed vs issue-code-first + placement, Corey's call). Banked: a Congress.gov title-enrichment sync (to catch named-not-numbered bills — NDAA / appropriations — since `bills` has no `short_title`) and LD-203 contributions for a future money surface. Docs: HO 436. **Also notes now run through HO 435.**

**Also (HO 437), the LDA lobbying surface — issue-code-first, no theme-% change.** The HO 435 data layer's surface (Corey's call: issue-code-first over bill-keyed). Shipped + prod-verified across `2b34058` (query layer + rollup) · `d6c7d2c` (`/lobbying` surface) · `3851b85` (feed OFFSET fix); live SHA `3851b85`. **`/lobbying`** is a new top-level nav item and a **standalone analytical surface** (the `/patterns` class — an aggregate lens, not an entity hub): a stat readout + issue-area **ranked bars** (native LDA `general_issue_code` labels, NOT crosswalked to the 24 CBT topics — lossy) + a **per-issue drill** (top clients / top firms / recent) + a corpus-wide **recent-filings feed**. **The query-layer story is load-bearing:** request-time aggregation is **non-viable** on this Turso tier for these table sizes (whole-table aggregates row-fetch 30–90s+ cold; even a bounded per-code drill >25s — oddities). So stats + issue-bars + the **full per-code drill** are **precomputed** by the daily LDA cron into a `dashboard_state` blob (`lda_lobbying_rollup`) via full-table **sequential** reads + in-memory JS aggregation (~186s, under the 220s cron budget gate), served O(1); only the corpus recent-filings feed is **live** (an `idx_lda_filings_dt_posted` walk clamped to the 1,000 most-recent — `MAX_FEED_PAGES=40` — so a tail-of-table OFFSET can't 500). A **live-per-code drill was tried and abandoned** (>25s cold); a **covering-index detour was tried and fully reverted** (`migrate.ts` net-zero, no prod schema change). **No theme-% change** — an enabling surface on a new pillar. **Banked:** HO 439 (bill-keyed lobbying on `/bill/[id]`, reuses `lda_activity_bills`) + the CBT-topic crosswalk + a corpus-wide top-firms leaderboard + named-not-numbered bill recovery (needs the Congress.gov title-enrichment sync). Docs: HO 438. **Also notes now run through HO 437.**

**Also (HO 439–440), bill-keyed lobbying on `/bill/[id]` — the LDA arc's second fork, no theme-% change.** The other fork of the HO 435 data layer (the HO 437 block banked this as "HO 439"; **439 became the read-only cost probe, 440 the surface** — the probe took its own number, HO 434→435 precedent). **HO 439 (`42ff9c0`), the cost probe:** timed the real per-bill seek+hydrate shape cold against the worst case. **Verdict: live is non-viable for the fat tail** — worst-case bill (`119-hr-1`) **44.5s cold** (4.5× the 10s `getDb` cap), and the cost is entirely the random `lda_filings` row-fetch (seek 461ms, aggregate 30ms — the HO 340/437 shape confirmed one more time). Fan-out is a **fat tail with an extreme outlier**: 9,179 bills linked, p50 6 / p90 27 / p99 159 / max 8,048. Diagnostic committed as `scripts/diagnostic/lda-bill-cost-439.ts` (sibling to `lda-coverage-435.ts`). **HO 440 (`9a540b2` surface · `ab1505a` data+cron · `7b8f0d5` refactor), the hybrid surface:** **precompute the ≥150-filing tail, live-serve the rest.** `computeLdaRollup` refactored to share its sequential three-table read (`readLdaTables` → `computeIssueRollup` [issue rollup, **byte-identical to prod** verified] + `computeBillDrill`), so the per-bill drill piggybacks the pass already running — near-free. Qualifying bills (**96 at ≥150**, 559 KB) written to a **second `dashboard_state` blob** (`lda_bill_drill`) by the same budget-gated cron step; `getBillLobbying(billId)` reads it O(1), **falls through to a live `getDb` drill** for below-threshold/zero-link bills (worst below-threshold bill live 2.3s, 7.7s margin), **try/catch → null** so a freshly-heavy bill can never 500 the page. A LOBBYING section on `/bill/[id]` (after Hearings, before Summary) reuses `FilingRow` + the extracted `LobbyingMiniBars`; zero-link bills omit it. Ranks top firms/clients by **distinct filings** (consistent with the `/lobbying` per-code drill). Prod-verified across all three `getBillLobbying` branches at `42ff9c0` (blob `119-hr-1`, live `119-hr-2794`, zero-link `119-hres-1420` — 200, section absent). **Still banked:** the CBT-topic crosswalk + the corpus-wide top-firms leaderboard (both carried from HO 437; untouched here). **No theme-% change** — a secondary signal on an existing pillar. Docs: HO 441. **Also notes now run through HO 440.**

**Also (HO 442), the corpus-wide top-firms leaderboard — the LDA arc's last banked surface, no theme-% change.** The third and final item banked in the HO 437 block (bill-keyed → HO 439–440; the CBT-topic crosswalk stays banked; this closes top-firms). Shipped + prod-verified across `b64e1c7` (data + cron/CLI) · `69644fd` (surface); live SHA `69644fd`. **No cost probe** — unlike every prior `lda_*` surface (434→435, 439→440), this introduced no new cold query: `computeTopFirms` is a **third consumer of the shared `readLdaTables` pass** (beside `computeIssueRollup` + `computeBillDrill`), one in-memory grouping over the already-loaded `filings` map, so the build/probe split didn't apply — it rode the ~96s pass already running. Written to a **third `dashboard_state` blob** (`lda_top_firms`) in the same budget-gated cron step under the single `revalidateTag("lda")`; the CLI (`npm run lda:rollup`) mirrors it, so local regen writes all three blobs. **Definition:** ranks **registrants** (the lobbying shops — consistent with `topFirms` everywhere else) by **distinct filings** (the HO 435 house rule), name-tiebroken; per firm carries distinct clients, bill-linked filing count, and their single most-cited `general_issue_code`. **Top 25** (one constant `TOP_FIRMS_N`, store == render). **Surface:** a **TOP FIRMS section on `/lobbying`** (new Section 3, between the issue-drill two-column and the recent-filings feed) served O(1) from `getTopFirms()` (null before the first rollup → section cleanly omitted); the separate blob key keeps the 286KB `/lobbying` rollup read lean (the HO 440 rationale). Mobile: the Top-issue + Bill-linked columns collapse below ~640px (Tailwind, no `globals.css` coupling). **Two deliberate omissions, recorded so they aren't re-litigated:** (a) **no dollar ranking** — LD-2 income/expenses are partial + income-vs-expense asymmetric across registrant types, so a summed-dollar leaderboard would mislead; dollars are the banked **LD-203** money surface; (b) **native issue-code labels, NOT crosswalked to the 24 CBT topics** — that crosswalk is the other still-banked item. **The finding:** rank-1 is **Cornerstone Government Affairs**, #2 Brownstein Hyatt — a filing-**volume** ranking is deliberately distinct from a revenue ranking (Cornerstone runs a huge book of small clients → files a mountain of LD-2s without top-grossing), which is exactly the signal this cut adds over the dollar rankings. Invariants at rollup: **25 firms of 5,175 registrants**, 0 `billLinked > filings`, 0 null `topIssueCode`; the Section-1 readout `stats.registrants` and the leaderboard `totalRegistrants` both render **5,175** (structurally guaranteed — same `readLdaTables` filings map — so no masthead incoherence). **No migration, no new cron slot, no new nav item.** **No theme-% change** — an enabling/aggregate surface on the LDA pillar (the `/patterns` class), matching HO 437/439–440. Docs: HO 443. **Also notes now run through HO 442.**

**Also (HO 444), the CBT-topic crosswalk on `/lobbying` — the LDA arc's last banked surface, no theme-% change.** The final item banked in the HO 437 block (bill-keyed → 439–440; top-firms → 442; this closes the crosswalk), so the **HO 437 four-item fork is now fully discharged — nothing from it stays banked.** Shipped + prod-verified across `8f7d7bd` (data) · `549d81e` (surface); live SHA `549d81e`. **Two calls shaped it. Build, not probe:** unlike every prior `lda_*` surface (434→435, 439→440), it introduced no new cold query — `computeTopicCrosswalk` is a **fourth consumer of the shared `readLdaTables` pass** (beside `computeIssueRollup`/`computeBillDrill`/`computeTopFirms`), one in-memory re-bucketing of the already-loaded `uuidsByCode` sets, so the build/probe split didn't apply (the HO 442 precedent). **`/lobbying` section, not a topic hub:** there is no topic entity route (topics are a filter/facet only), so cross-linking lobbying onto a topic page has no home — the self-contained surface is a **BY-CBT-TOPIC section on `/lobbying`** (new Section 3, between the issue two-column and TOP FIRMS; TOP FIRMS → 4, feed → 5), which does **not** reopen the HO 437/442 native-labels decision (that was about not *replacing* the native `general_issue_code` bars; this sits alongside them, disclosing the lossiness by keeping both, and its payoff is expressing lobbying in the same 24-topic vocabulary as the dashboard's bill distribution). **The map** (`lib/lda-issue-topic-map.ts`, a typed `Record<string, Topic>` — not `enums.ts`, not JSON, so the per-code judgment comments type-check): **79 LDA codes → 23 live CBT topics**; `elections` legitimately empty (no lobbying issue code maps there — an honest gap, not forced); **`other` = 21** industry/profession/contested codes (comprehension-over-coverage, the `TOPIC_ALIASES` posture). Two contested calls resolved on review: **CPT (Copyright/Patent) → `technology`** and **FAM (Family/Abortion/Adoption) → `healthcare`** (the closest single home for a single-valued map; the abuse-vs-industry asymmetry — ALC→healthcare as the *abuse* code, TOB→other as the *industry* code — is deliberate, see oddities). **The multi-code property:** a filing carries multiple issue codes, so it lands in multiple topic buckets — topic-filings sum **213,780 > corpus 110,055**, NOT a partition (the same property the native issue bars have; the section header discloses it, never "share of filings"). Invariants: 23 topics, 0 unmapped (structural — `?? "other"` catch-all), the HO 442 `5,175`-registrant readout holds (same `readLdaTables` map). **No cost probe, no migration, no new cron slot, no new nav item** — the fourth blob (`lda_topic_crosswalk`) rides the same budget-gated cron step under the one `revalidateTag("lda")`. **No theme-% change** — an enabling/aggregate surface on the LDA pillar (the `/patterns` class), matching HO 437/439–440/442. Docs: HO 445. **Also notes now run through HO 444.**

**Also (HO 446–448), the amendments pillar — probe → data layer → first surface, no theme-% change.** The roadmap's "cheapest core-deepening move" call (amendments over nominations: dual-attaches to the bill spine via `amendedBill` and the member spine via `sponsors`, reuses the Congress.gov sync wholesale, fills the floor-stage visibility gap; nominations sequenced later on a weaker join). Three HOs, probe-first as ever. **HO 446 (`508656e`), the source probe — GO:** 6,788 amendments in the 119th (SAMDT 6,560 / HAMDT 228 / SUAMDT 0), `amendedBill` present and resolving 100% to tracked bills, rich `latestAction` disposition vocab. The structural finding that killed a speculative table: sub-amendments carry BOTH `amendedBill` AND `amendedAmendment`, and the API resolves the ultimate bill transitively — so lineage is a nullable **self-FK**, not an `amendment_bills` link table, and `amended_bill_id` is a plain resolved column. **HO 447 (`667cc33` data + `cecff19`/`c5575b2`/`08394e6` fixes), the data layer:** an additive `amendments` table (house-style `amendment_type`/`amendment_number`, **loose links not enforced FKs** — `cecff19`, correct for self-referential bulk-load ordering), a resumable DB-frontier sync (`sync:amendments`, `MAX(update_date)` ascending sweep, list→detail hydration, deadline-gated `/api/cron/amendments` at 07:00 UTC, unbounded CLI backfill) mirroring `sync:lda`, and a read-only coverage diagnostic. Two resume corrections landed in-arc: **resume on the list item's `updateDate`, not the detail's** (`c5575b2`), and a **`--repair` mode** (`08394e6`) that enumerates live-vs-stored and fills the handful of holes the unstable-`updateDate`-order pagination skips (the ascending frontier can't refill an interior gap — oddities). Backfill ran whole: `--repair` exits `complete` at 6,788 == live `pagination.count`. **HO 448 (`13670f2`), the first surface — a bill-hub AMENDMENTS section on `/bill/[id]`:** the per-bill floor fight, placed **between Hearings and Lobbying** (committee → floor → outside-influence order). **No cost probe, no precompute blob** — unlike every `lda_*` surface, and deliberately so: amendments is **bills-scale** (~6,800 rows total, smaller than `119-hr-1`'s LDA filing set alone at 8,048), so a per-bill seek behind `idx_amendments_bill` hydrates instantly even for the worst magnet; `getBillAmendments` is a plain live `unstable_cache` query (tag `amendments`, try/catch → null so a bad read hides the section instead of 500-ing the page), no `dashboard_state` involvement. Each row: label, party-colored sponsor (members join, plain-text fallback for committee/manager amendments with no bioguide), purpose, a **display-only disposition dot** (green/red/neutral keyword-scanned from top-level `latest_action_text` via the `--vote-yea`/`--vote-nay` palette — NOT the deferred status model), sub-amendment `↳ amends` note; ordered by `COALESCE(latest_action_date, submitted_date) DESC`, capped at 60 + "N more" for magnets. **The finding:** `latest_action_text` is present on only **8.6%** of amendments (447's "~35%" was the probe's stale frontier read — oddities), because a couple of budget vote-a-rama magnets dominate the filed-not-acted corpus — `119-sconres-7` alone carries **1,131 amendments (17% of all 6,788)**, only 24 with a top-level disposition. That 8.6% is the concrete argument for the deferred actions-walk status model (the vote-a-rama outcomes live in the per-amendment `actions` sub-resource the sync skips). Prod-verified across magnet (`119-sconres-7`, 1,131 / capped 60), small (`119-sjres-98`, 8 full), zero (`119-hconres-1`, section absent), and sub-amendment lineage (`119-s-1318`, `↳ amends SAMDT 5814/5816/5817`). **No theme-% change** — the floor-stage gap is now visible on the bill hub, but the outcome-classification (status model) and the cross-bill aggregate lens (`/amendments`) that a bump would ride are both deferred, so this is a v1 surface on a new pillar, matching the LDA-arc precedent (437 / 439–440 / 442 / 444 all held). **Banked (QUEUED):** the standalone `/amendments` aggregate surface, a member-hub sponsored-amendments section, and the persisted status/disposition model. Docs: HO 449. **Also notes now run through HO 448.**

**Also (HO 450), the member-hub sponsored-amendments section — the second amendments fork, no theme-% change.** One of the three forks banked in the HO 446–448 block (bill-hub → 448; this ships the member-hub; the standalone `/amendments` aggregate + the persisted status model stay QUEUED). Shipped + prod-verified; live SHA `ea686a8`. **The inverse projection of the HO 448 bill-hub section:** `getMemberAmendments(bioguideId)` seeks `idx_amendments_sponsor` (where 448 seeks `idx_amendments_bill`) and keys each row on the **amended bill** — linked to `/bill/[id]` via `formatBillId`, with the bill title — rather than the sponsor, since the sponsor is fixed (it's their page). A new `MemberAmendmentRow` (a bare row the page maps — the member-page idiom, not self-boxing like the bill page's `BillAmendments`); the disposition dot + module-level `deriveDisposition` are reused, no new palette. **No probe, no blob, no cron/migration** — same bills-scale reasoning as 448: a per-member seek hydrates instantly, and the heaviest amender (Bennet `B001267` at **271** amendments) served live, capped at 60 + "N more". Placed as the **authoring pair** — immediately after Sponsored bills, before Committees (bills + amendments = the two forms of member-authored legislative text) — and hidden when empty (most House members sponsor none), the `{scorecard ? …}` conditional-section idiom. Prod-verified across heavy/capped (Bennet, 271 → 60 + 211 more), small/full (Moreno `M001242`, 6), zero/absent (Aderholt `A000055`, section omitted), and dots + sub-amendment lineage (Paul `P000603`, 1 yea / 7 nay / 35 muted, `↳ amends`). **No theme-% change** — the second v1 surface on the amendments pillar; the outcome-classification (status model) + the cross-bill aggregate lens (`/amendments`) that a bump would ride both stay deferred, matching 448 and the LDA-arc precedent. Docs: HO 451. **Also notes now run through HO 450.**

**Also (HO 452), the actions-walk disposition probe — NO-GO, the status-model fork retired, no theme-% change.** The third fork banked in the HO 446–448 block (bill-hub → 448; member-hub → 450; this probes and **closes** the persisted status/disposition model). Probe committed `67ed564`, read-only, no build followed. **The question:** the display-only disposition dot (HO 448/450) colors only the **8.6%** of amendments with a top-level `latest_action_text`; the persisted-enum model would walk each amendment's `/actions` sub-resource to recover dispositions for the silent 91% — but that ~doubles the backfill (~6,800 → ~13,600 requests, ~85 → ~195 min), so it's only worth it if `/actions` actually carries those dispositions. **The finding: it doesn't — 0% recovery.** A stratified 155-amendment walk (`scripts/diagnostic/amendments-actions-probe-452.ts`, 0 errors / 0 throttles) found that no-top-level-action == **genuinely filed-never-acted**: both the general silent-91% stratum and the decisive `119-sconres-7` vote-a-rama stratum returned **100% submit/propose-only** `/actions` lists (0% terminal disposition), with an **empty residual** (the starter classifier left zero terminal actions unmatched — the 0% is a real absence, not a classifier miss). A vote-a-rama files ~1,131 amendments and votes on ~24; those ~24 already carry a top-level action, and the rest are never called up, so there's nothing in `/actions` to recover. **The top-level scan is the disposition ceiling** — a persisted enum would only persist what render already computes, for a doubled backfill and no coverage gain (oddities). So the status model closes NO-GO; the display-only dot stays as the complete-for-what's-available surface, not a stopgap. **One capability banked, orthogonal to dispositions:** `/actions` carries **`recordedVotes`** ({chamber, rollNumber, url, date}) — a clean amendment → roll-call-vote link (backlog QUEUED), the source for a future "how did each member vote on this amendment" surface, scoped on its own merits (it'd still pay the `/actions` walk cost, but for vote links, not dispositions). Payload also confirmed structured (`type` / 13 distinct `actionCode`s), though the text is already unambiguous. **No theme-% change** — a probe that retired a fork; no surface. This leaves the amendments pillar at **two live surfaces** (bill-hub, member-hub) with **one QUEUED fork** remaining (the `/amendments` aggregate) plus the new vote-linkage lead. Docs: HO 453. **Also notes now run through HO 452.**

**Also (HO 454–456), the nominations pillar — probe → data layer → surface, GO where amendments' status model was NO-GO, no theme-% change.** The other half of the amendments-vs-nominations fork (picked after amendments closed at 453). Nominations have **no bill spine and no member-sponsor** (the president nominates, the Senate confirms) — the "weaker join" the fork flagged, probe-confirmed: nominees carry no bioguide, so the pillar is standalone. Three HOs, probe-first. **HO 454 (`b27a1d2`), the source probe — GO:** 1,884 PNs in the 119th; both decision cuts came back favorable. **Civilian volume is real** — 833 PNs (judges, cabinet, ambassadors, US Attorneys), 44% by PN count, not drowned by the military bulk (1,051 PNs but ~25k nominees at ~24.5/PN; military dominates the *nominee* axis ~30:1, so it must be **collapsed** — one row per list PN, never exploded). **The disposition contrast — the reason nominations clears where amendments didn't:** `latest_action_text` is **100%** present (vs amendments' 8.6%), clean deterministic vocab along the confirmation pipeline (Received → Referred → Hearings → Reported → Calendar → Confirmed / Returned / Withdrawn) — a **persisted** status model is viable (oddities: the amendments NO-GO does NOT generalize). Two catches a naive build misses: the **`(pn_number, part_number)` composite key** (citations are `PN{n}-{part}`; the base-part detail is a *different* record) and the list-vs-detail `updateDate` divergence (same frontier hazard as amendments). Organization is a clean 84-value agency facet; `recordedVotes` absent (the roll-call is in the action text); committee referral present but the civilian rate unmeasured (a confound — deferred re-measure). **HO 455 (`ebadee3` data + `e175fd7`/`f15608b` gate fixes), the data layer:** an additive **list-only** `nominations` table (every core-surface field is list-level, so no detail fetch — which sidesteps the part-specific-detail wrinkle), keyed on the composite, military collapsed to single rows, with a **persisted `disposition`** computed at sync by `computeNominationDisposition` (the differentiator); `committee_system_code`/`nominee_count` present-but-NULL (deferred, forward-compatible). A resumable frontier sync (`sync:nominations`, `MAX(update_date)` ascending, inclusive boundary, deadline-gated `/api/cron/nominations` at 09:00) mirroring `sync:amendments` but cheaper (list-only, ~34s backfill). **The completion gate got a real fix in-arc:** nominations exposed a latent bug the amendments corpus never hit — `--repair` gated on `stored >= pagination.count`, but `pagination.count` **overcounts** the distinct-enumerable set (5 byte-identical duplicate list entries in the feed, 0.27% — the LDA-40 drift), so a correctly-deduping store can never reach it; the gate now keys on **full-enumeration `missing.length === 0`** (`e175fd7`), the diagnostic's landed-clean line likewise (`f15608b`). Corpus: **1,879 distinct**, provably whole. **HO 456 (`754cf0e` disposition tune + `febaaec` surface), the surface — standalone `/nominations`:** civilian-focused (830), leading with the **disposition strip** (60.5% confirmed — the status-model headline) + the 84-agency facet (State/Justice/The Judiciary/…), a filtered list with chips + pagination. **Live aggregation, no rollup blob** — 1,879 rows (smaller than bills), so `getNominationsSummary`/`getNominations` `GROUP BY` live (the bills-scale call, not the `lda_*` precompute one); no `MAX_FEED_PAGES` clamp at 830 rows. Military excluded and **disclosed** in the count line (the LDA-style honesty). Nav item `◈ Nominations` (standalone key, no sub-nav, mirrors Lobbying). The disposition tune (`754cf0e`) fixed the one residual — a reconsider-tabled action reading `received` is a finalized **confirmation** — and surfaced another gotcha (oddities): **`--backfill` resets offset, not frontier**, so a persisted derived column can't be recomputed by re-running the sync on a populated DB (it only re-touches the `MAX(update_date)` delta); the corpus-wide recompute needed **truncate + re-backfill** (safe here only because the table is list-only and API-reproducible). Prod-verified across the disposition strip, both facets filtering, deep pages (no tail 500), and the disclosure line. **No theme-% change** — a new-pillar v1 surface, matching the LDA/amendments precedent; the deferred committee cross-link + detail hydration are what a bump would ride. **Banked:** the `/committees/[id]` nominations cross-link (+ part-specific detail + civilian referral re-measure) in QUEUED; the military view + `/nomination/[id]` detail in BANKED. Docs: HO 457. **Also notes now run through HO 456.**

**Also (HO 458–459), the nominations committee cross-link — the deferred detail-only join, shipped, no theme-% change.** The one fork the HO 454–456 pillar deferred (list-only `/nominations` shipped; `committee_system_code` sat NULL because committee referral is detail-only). Probe-first as ever. **HO 458 (`a0cab49`), the detail probe — GO:** the cross-link rides a cold query never measured at civilian scale on the *correct* record, and the HO 454 civilian referral number was **confounded** — it read the base `/nomination/{c}/{n}`, which for a multi-part PN is a **stub** (`committees` absent). The probe settled the shape: **multi-part is load-bearing** (786/830 = 94.7% of civilian rows live inside multi-part PNs, not a tail); each part-row's stored `raw_json.url` resolves to a **distinct record with its own per-part committee** (PN730-1 African Development Bank vs -2 Federal Labor Relations Authority); `committees` is a sub-resource `{count, url}` everywhere (a 2nd fetch when `count > 0`). Re-measured civilian-only on the right record: **98% referral presence / 100% resolution to tracked `committees` / 0 unresolved codes** (no `sync:committees` gap), effectively 1:1 (a 6/150 dual-referral tail). Cost ~1.98 fetches/row → ~277–500s for 830 rows, over the 300s route → CLI backfill + a bounded cron delta. **HO 459 (`b783d39` data + `fd5e256` surface), the build:** hydrate `committee_system_code` per civilian part-row by following the stored `url` (2 fetches when referred, storing the **first**/primary committee — the dual-referral secondary dropped, disclosed, no link table). A **`committee_hydrated_at` sentinel + partial index** (`idx_nominations_hydrate ON nominations(pn_number) WHERE is_military = 0 AND committee_hydrated_at IS NULL`, the HO 406 summarize-queue pattern) is the two-phase-populate key: every processed row is stamped (incl. the ~2% with no committee) so it exits the queue and incremental re-runs only touch the NULL delta. A `--hydrate` CLI mode does the one-time 830-row populate; a **bounded cron step** (sync deadline 45s → hydrate 55s, cap 40/tick) trickle-hydrates the delta after. **The list sync needs no change** — `buildNominationStatement`'s `ON CONFLICT DO UPDATE SET` already omits `committee_system_code`, so a bulk-refresh re-upsert preserves the hydration stamp (adding the sentinel to the SET clause would re-null every hydrated row — oddities). Initial populate: **830 processed / 817 withCommittee / 13 no-committee / 0 unresolved / 22 dual-dropped / remaining 0** (one transient 15s fetch-timeout on `PN379-07` left `remaining=1`; a resumable 2nd `--hydrate` pass drained just that row — the sentinel-queue working as designed). **Surface:** `getCommitteeNominations(systemCode)` + a `?committee=` filter on `getNominations` (bidirectional), and a **full-width "Nominations referred" section on `/committee/[systemCode]`** (after Hearings; `NominationRow` reused, disposition-badged, header `{total} referred · {confirmed} confirmed`) — **omitted when total is 0**, so House/joint committees (which confirm nothing) show no section, no "0 referred" noise; a "see all → `/nominations?committee=…`" foot when over the 25-cap. Prod-verified: `/committee/ssfr00` (Foreign Relations, 201 referred / 100 confirmed), `ssju00` (Judiciary, 187 / 118 — the filtered `?committee=ssju00` list count matches exactly, join correct), `hsju00` (House Judiciary, section omitted). **Scope held:** `nominee_count` stays deferred/NULL (military-only, no consumer — folded into the banked military-view item), primary-only referral (no `nomination_committees` table), no `/nomination/[id]`, no new nav/cron. **No theme-% change** — a cross-link deepening an existing pillar (the deferred join it was waiting on is now built, but it's a secondary lens, not a new theme), matching the LDA/amendments precedent. The nominations pillar now sits at **two surfaces + a bidirectional committee link**. Docs: HO 460. **Also notes now run through HO 459.**

**Also (HO 461), the `/amendments` aggregate — the amendments pillar feature-complete, no theme-% change.** The last QUEUED amendments fork (bill-hub → 448; member-hub → 450; the actions-walk status model retired NO-GO → 452; this ships the standalone aggregate), so the **HO 446–448 three-fork pillar is now fully discharged** — nothing from it stays banked. Shipped + prod-verified across `6050b41` (query layer + cost gate + index) · `e96f0ab` (surface + nav); live SHA `e96f0ab`. **`/amendments`** is a new top-level nav item and a **standalone analytical surface** (the `/lobbying` + `/nominations` class — an aggregate lens, not an entity hub): a status hero + most-amended-bills + top-amenders ranked cuts + a filtered feed. **Build, not probe, but with an inline cold-cost gate** — unlike the prior amendments surfaces this carried one genuinely unmeasured cut: `disposition` is **display-only-derived** (HO 452 killed the persisted column), so the corpus disposition breakdown SCANs `latest_action_text` (unindexed), the one non-covered query where the two ranked cuts ride `idx_amendments_bill`/`idx_amendments_sponsor`. So a Step-0 gate (`scripts/diagnostic/amendments-aggregate-cost-461.ts`) measured the three unbounded cuts cold **before** committing live-vs-blob: the disposition scan was **4065ms** (SCAN + `raw_json` bloat), the two GROUP BYs **90ms/138ms** (already index-only, never touched the table). The fix cleared it: a **partial covering index `idx_amendments_acted`** (`ON amendments(latest_action_text) WHERE latest_action_text IS NOT NULL`) took the disposition scan **4065ms → 40ms** (SEARCH USING COVERING INDEX), a 100× cut — so **live confirmed, no rollup blob, bills-scale-live like `/nominations`.** The index is a pure performance fix (the HO 277/340 covering-aggregate class), **NOT** a status model — HO 452 stays closed (it persists nothing new; it just makes the already-computed display-only scan index-only). **The query layer:** `getAmendmentsSummary()` composes `total` / `acted` / `agreed`·`failed`·`otherActed` (the covered disposition scan mapped through the module-level `deriveDisposition`) / `filedOnly` = total − acted, `byChamber` (SAMDT+SUAMDT→senate, HAMDT→house), and `topBills`/`topSponsors` (top-15 aggregate + `IN`-join hydration, re-sorted by count); `getAmendments({type?, disposition?, bill?, page})` is the bounded paginated feed (coarse `acted`/`filed` disposition filter — fine-grained agreed/failed stays on the per-row dot, since the JS `deriveDisposition` keyword logic would drift a SQL LIKE mirror). Both live `unstable_cache`, tag `amendments`, `try/catch → null`. **The surface:** a 5-section `/amendments` (header/blurb → status hero bar → most-amended bills → top amenders → filtered feed with chips + pagination), a bare `AmendmentRow` carrying **both** the bill link and the sponsor (the corpus feed fixes neither), and a `Δ Amendments` nav entry (standalone key, mirrors `$ Lobbying` / `◈ Nominations`). **The finding — the "floor almost never votes" headline:** 6,788 filed · **586 acted (8.6%)** · 6,202 awaiting floor action; **6,560 Senate / 228 House** (the House barely amends on the floor; the Senate does via vote-a-rama); `119-sconres-7` alone = **1,131 (17%)** at the top of most-amended; Bennet (`B001267`) = **271** the top amender — so the status bar's muted `filedOnly` segment dominates, which is the point. Prod-verified across the shape: the hero readout matches the data to the digit, `?bill=119-sconres-7` → 1,131 + pill, `?disposition=acted` → 586, `?type=HAMDT` → 228 / `?type=SAMDT` → 6,560, deep `?page=20` + combined filters HTTP 200 (bounded feed, no tail 500), nav tab lit. **No theme-% change** — a v1 aggregate lens on an existing pillar, matching 448/450 and the LDA-arc precedent (437/439–440/442/444 all held). **Banked (QUEUED):** the fine-grained disposition list filter, the `dispositionColor`/`deriveDisposition` extraction to a shared module (deferred to keep the feature commit off two working components — the two-commit refactor-separate rule), and the HO 452 `recordedVotes` amendment→roll-call-vote lead (orthogonal, still QUEUED). Docs: HO 462. **Also notes now run through HO 461.**

**Also (HO 463), the `/lobbying` topic-crosswalk click-through — the HO 444 crosswalk made interactive, no theme-% change.** The BY-CBT-TOPIC bars (HO 444) were non-interactive in v1; HO 463 (`2bdbe15`) turns each into a `<button>` that expands inline to its constituent LDA issue codes, each chip a `<Link>` into the **existing** Section 2 `?issue=` drill — scrolling it into view via a plain `#lobby-drill` hash (Next 15.5 honors it on soft-nav; no `scroll={false}`/`onClick` workaround needed). **No chip can land on a blank:** `drill[code]` is built uncapped over every corpus code (`computeIssueRollup` loops all of `uuidsByCode`), so every constituent code has a real drill. Page-side grouping of `rollup.issues` keys on the **same `topicForCode`** `computeTopicCrosswalk` uses, so the chips align exactly with the bar totals — no double-count seam. `TopicCrosswalk` became a `"use client"` island (instant expand; client state persists across the `?issue=` soft-nav, so the open topic survives a chip click). **No new data layer / blob / query / cron / migration / nav** — a UI pass over data already in the page. **No theme-% change** — a v2 interaction refinement on the LDA `/lobbying` surface (the `/patterns` class), matching the LDA-arc precedent. Docs: HO 464. **Also notes now run through HO 463.**

**Also (HO 466–468), the chip-family existing-chip migration — the HO 287 deferral closed, no theme-% change.** The normalization HO 287 scope-guarded out (its additive half shipped the `--kalshi`/`--poly` tokens + `SourceTag`/`MicroTag`; the existing id/stage/party chips were "used in many divergent ways" and split to this arc; topic already unified at HO 316). Audit-first as HO 287 mandated: **HO 465** (read-only, no commit) catalogued every ID/stage/party rendering + consumer and framed three decisions; the builds landed one commit per leg. **HO 466 (`2706d23`), the ID leg — Decision 1: keep chamber-tint.** `BillIdRail` (vertical) + `.bill-chip` (horizontal) stay chamber-colored (`--rail-house`/`--rail-senate`) per the HO 125 rationale (chamber-of-origin is a *structural* marker, not partisan — amberizing it would erase the at-a-glance chamber cue across ~11 BillRow surfaces + TOP STALLS). `.v2f-id` already matched the HO 287 tier-1 spec exactly (bordered `--accent-amber`, 11px/600, 2px radius), so it became the source of a shared **`BillIdChip` + `.bill-id-chip`** primitive that absorbs only the **non-chamber** id surfaces: the dashboard feed (`.v2f-id`), weekly band (`.weekly-band-id` green→amber), and hearings list + calendar (`.hearing-bill-line .bill-id`, `.hcal-card-billid`) — id text unified through `formatBillId`. **Two documented exceptions left as amber text** (the "compact multi-id list ≠ single row-anchor" line): `SponsorExpandedPanel`'s 14px card bill-list (an 11px chip would misalign the baseline row) and `.hearing-chip` (the compact "·N more" multi-id glance strip). **One behavior change:** the feed id was a plain `<span>` inside the HO 319 click-to-expand row (clicking it expanded the row); as a `<Link>` it now deep-links to `/bill/[id]` (with `stopPropagation` so the row still expands from anywhere else) — consistent with the id linking on every other surface. **HO 467 (`03efc6e`), the stage leg — Decision 2: `StageIndicator` stays exempt.** `StagePillStrip`/`.stage-pill` → tier-2 spec: **10px** (was 11), active (current) pill border = its stage color at **50% alpha** (`color-mix(in srgb, currentColor 50%, transparent)`), history pill → explicit **`--text-dim` text + `--border-strong` border** (dropped the old `opacity: 0.6` dimmed-stage-color); one shared-component edit flows to every BillRow surface. `StageIndicator` (hearings + `/bill/[id]`) untouched — its `▸/▸▸/▸▸▸▸` arrow-prefix encodes stage progression at a glance, which a flat pill loses. **HO 468 (`9327517`), the party leg — Decision 3: tint stays ON (pure refactor, no visible change).** Party color is the feed's core at-a-glance signal, so the spec's "tint off by default" was declined; the leg is a source-of-truth consolidation only. The ~8-way `partyColor` fork collapsed to **one canonical helper** in `lib/race-colors.ts` (widened to `string` + **inline**-normalized rather than importing `normalizePartyVariant`, which lives in `queries.ts` behind `next/cache` — the HO 425/430 client-leaf boundary, and `race-colors.ts` is imported by client race components); the feed (`.v2f-subline-bracket`) + `/members` (`.mc-brk`) inline brackets route through `PartyTag` (gained a `className` passthrough), `SponsorExpandedPanel`/`SponsorHoverName`/`MemberHeader` color-converged (markup untouched — `MemberHeader`'s dash-joined identity meta line is not the `[P-ST]` idiom, exempt). **The null-case lesson (Corey caught it pre-commit):** widening the helper's domain risked collapsing `null → I` (independent purple); the fix keeps two categories distinct — **`I`/`ID` → independent purple, `null`/any-unrecognized-code → `--text-dim` gray** (verified `partyColor(null) === partyColor("L") === --text-dim`; the sibling purple path verified live on `[I-CA]`/`[I-VT]`). No visible change: `[D-CT]` renders byte-identical everywhere; the null `[?-ST]` gray path is function-proven (no null-party row exists in the corpus to render live). Accepted micro-edge: `SponsorHoverName`'s null fallback shifts `--text-muted → --text-dim` (a ~never-fired path). **The B6 chip-family arc is now complete** — topic (316) + ID (466) + stage (467) + party (468) — closing the HO 287 deferral; nothing from it stays QUEUED. **No theme-% change** — a visual-normalization arc over existing surfaces, no new data/blob/query/cron/nav. Docs: HO 469. **Also notes now run through HO 468.**

**Also (HO 470 / 432), race→news on the dashboard — the NEW badge lit, no theme-% change.** The HO 272 RACES-tab `NEW` badge (wired but dark since the news→race join was absent) is now live off the HO 394 observation layer. Provenance twist worth recording: the feature **shipped as commit `35bf8d5`, git-labeled HO 432** — a parallel session had built the complete implementation from an earlier "badge + drawer" handoff (HO 432) and left it **uncommitted** in the tree; the HO 470 session (a fresh badge-only respec) found it, verified it clean (`7cbcd37` is an ancestor of HEAD, diff purely additive over HO 466/467, tsc/build green), shipped it, then **dropped HO 470's own redundant spec** (a local gitignored file, just `rm`'d). **What lit:** `RacesBoxTabs` gained a `newsSet` registry beside `movedSet` (`newCount = newsSet.size`, replacing the hardcoded `0`); a per-card `RaceNewIndicator` (mirror of `RaceMovedIndicator`) registers when its latest-news date postdates the browser's `cbt:racesLastView` stamp, rendering a red `.rc-new` NEWS pip; opening RACES stamps `now` and clears both badges. **No new query** — the badge reads the existing **`getRaceNews(incumbent, 3)`** (threaded via `hubs[i].news[0].observedAt`), so the HO 414 "fold `getRaceNews`+`getMemberNews` into `getObservationNews` on the 3rd consumer" debt **stays deferred** (this adds callers, not a third projection). **The variant reality (corrects a premise stale across BOTH handoffs):** live `/` renders `<CompetitiveRacesBlock variant="v2" />` — whole-`<Link>` `RaceCard`s with **no popover** — so HO 432's second half (race news in the competitive **popover**: `RaceHubBody preview` + a hover-bridge) renders **only on the default variant = `/dashboard-classic`** (the sunset route), **not** the live dashboard. So the live dashboard's race→news is the **NEW badge only**; the news content lives on the `/race/[id]` hub (HO 398) every card links to. Both HO 432's author and the executing session had asserted "the dashboard has a race-news popover" by reading the popover branch in `CompetitiveRacesStrip` without checking the variant the caller passes — recorded in oddities. **The finding:** at ship, **3 of 4 featured incumbents carry ≥1 person-observation** (Ossoff / Collins / Kean; Nunn 0) → the badge lights **"New 3"** today; CDP-verified the pips + badge render and clear on RACES open (NJ-07 co-renders MOVED+NEWS, IA-03 correctly bare). **This discharges the QUEUED "HO 272 NEW badge + race→news drawer" item in full** — badge lit; the "drawer" resolved as a scope finding (no live drawer exists; popover-news shipped on classic); non-incumbent challengers stay unjoinable (no bioguide — the expected HO 398 coverage edge). **No theme-% change — Races held at 99** (a within-theme dashboard signal, matching the HO 398/414 race/member→news v1s). Docs: HO 471. **Also notes now run through HO 470.**

**Also (HO 475), the markets/kalshi cron migration GitHub Actions → Vercel Pro — infrastructure, no theme-% change.** The follow-up HO 433 flagged ("migrating those three workflows GitHub → Vercel … its own follow-up HO"), now shipped + prod-verified across `1c16646` (recon diagnostic, HO 474) · `f3fb87a` (Phase 1 — Vercel crons armed) · `945486f` (Phase 3 — workflows retired); live SHA `945486f`. **The three GitHub-Actions market crons (`markets-tick` `0,30 13-21 * * 1-5` fmp / `markets-daily` `0 */4` bare full-roster / `kalshi-tick` `15 */2`) are now Vercel Pro crons** in `vercel.json`: the intraday **fmp tick** ported as a query-string entry `/api/cron/markets?source=fmp` (the 7 latency-sensitive equity/index symbols SPX/NDQ/NVDA/AAPL/MSFT/GOOGL/LMT), the **bare full-roster** run **repurposing the dormant HO 314 `30 21` floor** (schedule changed in place `30 21 * * *` → `0 */4 * * *` — the every-4h bare superset IS the markets-daily replacement, so no separate floor remained to reconcile), and **kalshi** ported direct to `/api/cron/kalshi` `15 */2`; the three `.github/workflows/*.yml` deleted. **Probe-first as ever:** the HO 474 recon (read-only, committed diagnostic `scripts/diagnostic/markets-cadence-474.ts`) cleared the cutover — writes are benign under overlap (`market_ticks` is a plain append, the "harmless charting fuel" the daily workflow always tolerated; `kalshi_odds` is `ON CONFLICT(race_id) DO UPDATE`, idempotent), auth is green (`CRON_SECRET` proven in Vercel Production — the existing Vercel crons succeed and the repurposed floor already authorized), bloat is a non-issue (3,773 `market_ticks` rows / 51 days, no retention step) — so it shipped as a **clean single cutover** (arm → 1-day overlap watch → retire), not a zero-gap dance. **Reliability, not cadence** (the HO 433 framing): HO 313 measured GitHub dropping ~20% of scheduled slots, re-measured in the recon (the weekday `0,30 13-21` fmp histogram read **17h:12 / 15h:3 / 16h:3 / 18h:3 / 19h:2** where ~10/hr was due). **The attribution method (oddities):** `cron_runs` can't distinguish a Vercel invocation from the still-firing GitHub twin (it records route/status/payload, not method/host/UA), so all three were confirmed as distinct **Vercel-cron GETs** off the deployment host — fmp 17:00:44 (7 sym, no POLY), kalshi 18:15:29, bare 20:00:26 (18 sym + the POLY-SHUTDOWN ghost) — using **GET-vs-POST** and **200-implies-Bearer** (the route 401s without `CRON_SECRET`, so only Vercel-with-secret gets a 200) as the discriminators; the 20:00 window showed the **fmp + bare split 18s apart** (7 vs 18 symbols), proving the two `/api/cron/markets` entries register as distinct Vercel crons (a query-string cron keys on the full path string). **Deferred:** the FRED once-daily gate (the bare 4h run re-fetches the 4 `source:"fred"` symbols ~6×/day, ~20 redundant same-value append rows/day — negligible, and a real gate needs a route insert-guard, so skipped to keep the cutover pure-config; revisit only on a future cadence bump). **Banked:** the **markets-watchdog** pairing (a `cron_runs` health check asserting markets/kalshi fired in-window and the only chronic failure is the known POLY-SHUTDOWN ghost — now more valuable with the reliable Vercel floor) and a one-time **~200-row dead-symbol prune** (frozen Stooq-era rows — DOW/GOLD/BTC/VIX/DXY/… — inert in `market_ticks`, not in `MARKET_SYMBOLS`; pure cosmetics). **This discharges the HO 433 "migration flagged as its own follow-up HO" and closes the "Intraday-reliable markets upgrade" OPEN LOOP** (the HO 314 floor is now the 4h bare run; the "free hosted cron" the loop wanted is Vercel Pro's native cron). **No theme-% change** — cron infrastructure, no user-facing surface. Docs: HO 476. **Also notes now run through HO 475.**

**Also (HO 477), the cron-health watchdog — infrastructure, no theme-% change.** The banked "Markets watchdog" (from the HO 475 block), generalized and shipped as a fleet-wide health surface; live SHA `92baba0`. **The load-bearing finding:** a cron that silently *stops firing* is **invisible to Vercel's own monitoring** — no invocation means no function error to alert on — so the HO 314→475 floor *bounds* markets staleness but can't *detect* a total trigger failure; the only thing that catches it is an external "is the data fresh?" **pull**. So rather than a markets-only check, `lib/cron-health.ts` is a **14-route `CRON_ROUTES` registry + `computeCronHealth()`** (single source of truth) feeding two consumers: a new **`app/api/health` GET** returning **200 healthy / 503 unhealthy** (the pull surface an external uptime monitor watches — the actual push-alert layer, Corey's out-of-repo step), and the **refreshed HO 135 diagnostic** (its stale 7-route pre-433 list retired, now reading the same registry). **The liveness rule is the subtle part (oddities):** unhealthy = **no `cron_runs` row within the route's window** (the real "stopped firing" signal) OR the most-recent run is `error`/`orphaned` — **`timeout` counts as alive**, because HO 139's soft-timeout-resume means a `timeout` row proves the cron *fired and executed* (the pre-build probe caught this: `/api/sync` reads `timeout` with bills ~1h fresh and last clean success ~37h back — a success-age check would false-alarm on every soft-timeout route). **Markets is the one exception, keyed on data not the row (oddities):** because the route logs one `route="/api/cron/markets"` for both the bare + fmp crons AND carries a permanent POLY-SHUTDOWN chronicErr every bare run, `computeCronHealth` judges markets by **`MAX(market_ticks.ticked_at)`** (the literal "no `market_ticks` write in N hours" the backlog asked for) — unhealthy iff ticks > ~9h stale OR the markets run is error/orphaned; a success-with-ghost is healthy, the chronicErr string never consulted. Thresholds ≈ 2× cadence + grace (`summarize` 30m … `sync` 13h … 26h daily, weekly at a flat 9d `windowOnly`). The endpoint is **`force-dynamic`/`no-store`, unauthed, not `wrapCronRoute`-wrapped** (a monitor, not a cron — it never logs itself into `cron_runs`) and **503s on a check failure** (never a misleading 200). Prod-verified: **200 / `healthy:true` / all 14 routes fresh**, `/api/sync` + `/api/sync-votes` alive via recent-`timeout` rows, markets alive via tick-freshness with the ghost ignored. **This discharges the "Markets watchdog" OPEN LOOP.** **Two refinements banked:** fmp-specific freshness (the shared `route` string means fmp stopping while bare fires isn't row-detectable — needs payload inspection) and window-aware thresholds for the weekly routes (flat 9d catches a total stop; precise next-fire needs cron-expression parsing). **One finding filed as a new OPEN LOOP (not this HO):** `/api/sync` hit the 55s soft cap on ~6 of its last 7 ticks — the sync step commits (bills fresh) but a trailing step (dashboard-lead/trades) overruns; the watchdog correctly reads it alive, but it's the HO 407 / 405–406 step-overruns-the-wall class and wants its own probe. **No theme-% change** — cron infrastructure, no user-facing surface. Docs: HO 478. **Also notes now run through HO 477.**

**Also (HO 480), the `/api/sync` lead-query hint sweep — a correctness/operational fix, no theme-% change.** The `/api/sync trailing-step timeout` OPEN LOOP the HO 477 block filed, now probed (HO 479, read-only) and fixed (HO 480, hint-only); live SHA `b440b82`. **The load-bearing correction:** the overrun was mis-diagnosed by its symptom. HO 479's probe read the prod telemetry — 32 of 40 `/api/sync` ticks timed out at the 55s soft cap (a standing ~80%, not a wobble) — and cold-timed the suspects, refuting its own stated hypothesis (Gemini latency): the trailing cost was the HO 405/407 **cold-DB-stall**, not a slow external call. `gatherLeadData`'s transitions query, lured by the `is_ceremonial=0 OR IS NULL` predicate into a `MULTI-INDEX OR` over the low-selectivity `idx_bills_is_ceremonial` (~the whole 16.8k corpus) + a `TEMP B-TREE` sort, **ignored the perfect `idx_bills_stage_changed_at`** and cold-stalled past the 10s `boundedFetch` wall — retrying to 10+10s and *failing* the (non-fatal) lead step. **The finding worse than the timeout:** because the lead step was *failing* (not merely slow), the dashboard lead had been **silently going stale on every real-delta tick**, not only on the ticks that tripped the 55s cap. HO 480 swept the function 407 missed — `INDEXED BY idx_bills_stage_changed_at` on `transRs`/`topicRs`, `INDEXED BY idx_bills_introduced_date` on `introRs` (all cold-EXPLAIN-verified to a single sorted range, the residual `json_each` GROUP BY/ORDER BY temp-btrees inherent per the 407 Q6 precedent) — collapsing cold `generateDashboardLead()` from the 14–20s stall band to ~1–3s (warm 806ms), no `[db] timeout, retrying`. **The wall math:** `sync (~30, budget-pinned) + lead (~1–3) + trades (2.4–7.5) ≈ 33–40s`, comfortably under the 55s cap — route finalizes `success`, lead stops silently failing. Close criterion met. **Two items filed, not folded:** (a) the `wrapCronRoute` **instrumentation gap** — timeout rows carry no per-step timings because the finalize catch-branch writes `{ok,elapsedMs,error,status}`, not the handler's closure `timings`; real but a fleet-wide 14-cron contract change, and unnecessary once the route stops timing out (its success-row timings record normally) → its own HO; (b) the 4th lead read `queryEnactedThisWeek` mis-plans identically but is a shared helper → new OPEN LOOP (HO 481). **Banked, not today's cause:** FMP `AbortController` / split-trades — FMP `-latest` `402`s past page 0 so `maxPagesPerChamber:3` is a no-op, the GETs never hung, trades' 2.4–7.5s is ~200 sequential `INSERT OR IGNORE` round-trips. **No theme-% change** — an operational cron fix, no user-facing surface (the correctness gain: the lead is regenerated, not silently stale). Docs: HO 480. **Also notes now run through HO 480.**

**Also (HO 481), the `queryEnactedThisWeek` enacted-helper hint — the 5th sibling — no theme-% change.** The `queryEnactedThisWeek cold-stall` OPEN LOOP the HO 480 sweep filed, now closed; live SHA `9c19557`. HO 480 hinted three of `gatherLeadData`'s four reads and flagged the fourth — the shared HO 232 helper `queryEnactedThisWeek` (`lib/enacted-this-week.ts`), consumed by the lead step AND the ENACTED THIS WEEK banner + week-over-week delta — as needing its own scoping (a hint in a shared helper has blast radius). Probe-then-fix, hint-first: cold-EXPLAIN confirmed both callers (`queryEnactedThisWeek` + `queryEnactedPriorWeekCount`) mis-plan identically (`MULTI-INDEX OR` over `idx_bills_dash_stage` + temp-btree), and `INDEXED BY idx_bills_enacted` (Option A) collapsed both to `SCAN bills USING INDEX idx_bills_enacted` — cold 617ms / 106ms, identical rows/count — so the Option B new partial index (`idx_bills_enacted_changed`) was unnecessary, no migration. **The partial index qualifies** because it's `WHERE stage='enacted'` and the query carries `AND stage='enacted'`; forcing it bounds the scan to the enacted subset by construction. **Corrected premise:** HO 335 recorded this helper as riding `idx_bills_enacted` cleanly — but that index keys `(congress, latest_action_date)`, has no `stage_changed_at`, so it can serve the `stage='enacted'` seek but not the window filter or `ORDER BY stage_changed_at DESC`, leaving a residual in-memory sort (trivial over the few-hundred-row enacted set). **This closes the known OR-lure instances in the enacted/lead cluster (the last flagged sibling); the standing guard against new ones stays the HO 332/340 cold-start audit.** **No theme-% change** — a correctness/hygiene fix, no user-facing surface. Docs: HO 481. **Also notes now run through HO 481.**

**Also (HO 482–487), the `/lobbying` members-style redesign + the trades batch-fix verification — a chrome restructure + an operational verification, no theme-% change.** Two arcs cleared in one sweep. **Numbering:** HO 484 was drafted as the trades doc sweep but never issued; its content is folded here, so a future session should not hunt for a missing `484` file. This sweep is **487**. **(1) HO 482/483 — trades batch fix, VERIFIED against prod.** The `ingestChamber` DB writes in `lib/trades-ingest.ts` (~200 sequential `db.execute` `INSERT OR IGNORE` round-trips) were collapsed into one `db.batch(statements, "write")` (`ded2cf8`, shipped 2026-07-19 17:28). **The load-bearing correction on the verification itself:** the fix-time claim on record — "warm trades wall dropped 5–11.5s → 320ms" — is **not observable in `cron_runs`**, and the 14-day pull that was supposed to confirm it **straddles two mid-window fixes**, so the pooled figures are a trap (the naive full-window read shows a 63% `/api/sync` timeout rate + a 52s "breach", both fix-straddle artifacts, not current state). Split at the fix boundaries the story is clean and *confirms the prior arc*: **(a)** the route-timeout picture validates HO 480/481 (07-18 14:32) — **PRE-fix 79% timeout (34/43), p50 elapsed 55.2s; POST-fix 14% (2/14), p50 41.3s** — the route now finalizes `success` under the 55s soft cap on ~6 of 7 ticks, matching the documented ~1-in-5 baseline, the residual cap-hits being the by-design graceful reap (resumable sync continues next tick), not a regression. **(b)** the trades step validates HO 483 in *direction* — `payload.timings.trades` **PRE-483 p50 5.5s (n=11) → POST-483 p50 3.1s, min 1.9s (n=9)** — but the metric is the **whole `ingestTrades` step** (FMP page fetches for up to 3 pages × 2 chambers + name matching + the batch write), FMP-network-dominated, so the isolated 320ms DB-write win is muted inside a ~2–3s network floor and cannot be read off cron_runs directly. **A logged number is a claim, not a fact:** the 320ms is a real fix-time micro-measurement of the DB portion, but the coarse step metric is the only thing prod exposes and it only shows a consistent-direction p50 drop. **Re-probe triggers (banked: 52s route breach, or trades p90 > ~13s):** neither is crossed by post-fix steady state — post-fix route p50 41.3s (occasional cap-hits are the soft-cap-by-design, not a runaway), trades p90 11.5s under the 13s line (one pre-483 run touched 13.4s). **00:00-UTC contention oddity (banked at 483):** faint and inconclusive — success-only elapsed puts the midnight slot slowest (p50 46.5s vs 34–41s elsewhere), but n=5, it spans the fix boundary, and it is **not** trades-driven (midnight trades p50 3.3s is mid-pack); don't act on it. Verification script committed as `scripts/diagnostic/cron-runs-sync-verify-487.ts` (read-only). **(2) HO 485 — LDA filing-expand cost + shape probe, verdict GO.** The `/lobbying` inline row-expand runs a live per-click single-filing read; the probe cold-timed the worst case (`SEARCH lda_activities USING INDEX sqlite_autoindex_lda_activities_1 (filing_uuid=?)`, a PK-prefix seek — the **inverse of the HO 439 per-bill random-fetch trap that measured 44.5s**) at **75ms cold** (31-activity omnibus filing), typical **29–85ms**. Committed as `scripts/diagnostic/lda-filing-expand-cost-485.ts` (`3ed533c`); the durable LDA shape numbers it produced live in oddities. Note the follow-on `0614a66` typecheck guard — the probe pushed at `3ed533c` with a broken `npm run typecheck` (a `noUncheckedIndexedAccess` gap in the `pct` helper), undetected until the next session (oddities). **(3) HO 486 — the `/lobbying` members-style redesign.** Two commits: `2bc1a26` (chrome — an issue rail + filings content pane, scope via `?issue=`) and `0765aaf` (expandable rows + per-activity panel, carrying the `.lob-content .mc-row` A-regression fix and the orphaned-`IssueDrill` removal). One new query, `getFilingActivities`. **The deliberate divergence from `/members`:** scoping an issue serves the **precomputed `drill.recent` sample**, not a complete per-issue browse, and the pager stays on the **unscoped corpus feed only** — because a live per-code filings feed is the **HO 437 abandoned query (>25s cold)**, the thing the scoped-view divergence exists to work around (backlog). Prod-verified live at `0765aaf` (rail `ISSUES · 79` VOL-sorted, `AGE / REGISTRANT → CLIENT / ISSUES / BILLS` header, row carets, pager clamped `Page 1 of 40`, crosswalk + firms leaderboard below); headless verification is BotID-blocked (backlog). **No theme-% change** — a restructure of an existing surface + operational verification, no new theme surface (matching the LDA-arc precedent where 437 / 439–440 / 442 / 444 all held). Docs: HO 487. **Also notes now run through HO 487.**

**Also (HO 488–490), the first CI wiring + the #418 hydration arc — infrastructure + a correctness fix, no theme-% change.** Three HOs, and the arc closes on itself: **488 built the gate, 489 found the cause, 490 used the gate to make the cause unrepeatable.** **(1) HO 488 — CI wiring (`3aa677c`).** The **first CI in the repo** — there was none after the HO 475 Vercel-cron cutover retired the three GitHub Actions market workflows (and with them the whole `.github/` tree). `.github/workflows/ci.yml`: a single `checks` job running **typecheck + build** on `pull_request` and `push:main`, Node 20, `concurrency` with `cancel-in-progress`. **Secretless** — probed first: `next build` succeeds with `TURSO_*` and `GEMINI_API_KEY` unset because every DB-backed route is dynamic (`ƒ`), so no `env:` block and no repo secrets are needed. **Lint deliberately omitted** — no ESLint dependency or config exists in the repo, and wiring one fresh is its own arc (backlog), so forcing `next lint` would have produced a wall of pre-existing errors and blocked CI from landing clean. Verified two ways: a deliberate type error tripped `tsc` (TS2322, exit 2), and the Actions log was confirmed to show **real step execution, not skipped steps** (a green check on a skip-everything run is not a pass). No branch protection wired, so a red CI blocks nothing mechanically — it's the standing signal, not a hard gate. **(2) HO 489 — hydration probe (`c7d75f1`).** Root-caused the intermittent React **#418** on `/bills`, `/changes`, `/` (surfaced by the 488 CI's own smoke run): **client feed rows** (`BillRow`/`StagePillStrip`, `V2FeedList`, the expand panels) render a `Date.now()`-relative age at render time, so the SSR instant and the hydration instant land in different minute/hour/day buckets and React discards the server HTML. **Two plausible hypotheses killed** (oddities): it is *not* the `LAST SYNC` / HO 183 cycling stamp (`CyclingTimestamp`+`zone-cycle.ts` are hydration-safe by construction), and `/dashboard-v2` is a **`permanentRedirect("/")`, not a static page** — the failure attributed to it is really the home page after the 308, so the "static-staleness" theory was moot. Committed `scripts/diagnostic/hydration-probe-489.ts` — the forced-straddle harness (advance only the client's `Date.now()` before hydration). **(3) HO 490 — hydration fix (`8114ffc`, 34 files, +282/−75).** `formatRelativeAge` / `formatRelativeAgeLong` / `daysSince` now take a **required `nowMs`**; each page computes one `nowMs = Date.now()` and prop-drills it to the rows — no context provider, no default. **Required-with-no-default was the whole point:** a default would compile every existing call site untouched and let the bug walk back in silently; required means `tsc` rejects any implicit-clock caller, and **CI runs `tsc` (488)**, so the bug *class* is closed rather than the instance. The HO's 6-client-component sketch undercounted — the sweep actually reached ~14 pages + ~8 server wrappers (tsc's missing-arg errors drove completeness). Acceptance test = the 489 probe under forced straddle: **0 hydration errors across 6 loads** where it previously reproduced #418 every time. Also carries a **load-bearing force-dynamic invariant** (oddities): ages are now frozen server-side, so a route ever prerendered or given `revalidate` would produce a *silently wrong* age with no warning. **No theme-% change** — CI infrastructure + a correctness fix, no user-facing surface. Docs: HO 491. **Also notes now run through HO 491.**

**Also (HO 492–493), the `/lobbying` height compaction + feed-size tune — chrome + one constant, no theme-% change.** The `/lobbying` page ran ~4,897px tall (@1280w); two HOs took it to ~1,369px, all layout — no data-layer, no queries, no row internals. **(1) HO 492 — height compaction (`7564423`, 5 files).** Three chrome changes: the 79-code issue **rail bounded** (`.lob-rail-scroll`, 520px scroll + 31px header, internal scroll) so it stops setting the pane height with ~900px of dead whitespace beside it; **crosswalk + firms side-by-side** (`.lob-secs` 2-col grid, single-column ≤900px) instead of two full-width panels each wasting half their width; **both sections bounded** (`.lob-sec-scroll`, 340px, crosswalk topic-expansion scrolls inside the bound rather than clipping). Page **4,897 → 1,777px unscoped / 1,369px scoped**. All rules scoped to `.lob-*` (element-owned) so the shared `.mc-pane`/`.mc-rail`/`.mc-content` and `/members`/`/bill/[id]` stay byte-identical (globals.css diff purely additive). **The regression the bound introduced, and its fix** (oddities): bounding a list with a selected state broke selection visibility — scoping an issue low in the 79-item list re-rendered with it selected but the rail scrolled to top; fixed with `scrollIntoView({ block: "nearest" })` in `IssueRailRow` (scrolls the rail's overflow container only, not the window — verified on a deep `?issue=MON` pick). Rail bound chosen by **measuring** (scoped content ~553px, unscoped feed was the taller floor), not the mockup's number. **(2) HO 493 — feed page size tuned to the rail floor (`2fcb2a8`).** After 492 the bounded rail is the page-height floor (~551px), so any feed at/under that gives the same page height and the oversized feed only added dead scroll. `PAGE_SIZE` **25 → 13** (file-local to `app/lobbying/page.tsx`; `/bills` uses `FEED_PAGE_SIZE`, amendments/nominations have their own — zero blast radius). Unscoped now **1,369px**, section headers start ~887px (was ~1,250). **13 not 14** — the measured largest fit was 14 (17px slack) but feed rows are variable height (multi-bill-chip filings wrap ~33px), so 13's ~50px slack absorbs one wrapped row where 14 absorbs none (oddities: measured slack is a property of that day's feed, not the constant). **`MAX_FEED_PAGES` left at 40 deliberately:** reachable depth drops 1,000 → 520 filings while page 40's OFFSET falls to ~507 from ~975, so the deep-OFFSET clamp gets *cheaper*; holding depth would mean ~77 pages, worse. **No theme-% change** — a chrome restructure + one tuning constant on an existing surface (matching the LDA-arc precedent). Docs: HO 494. **Process observation (not a change):** the SKILL `/lobbying` entry has now gone stale twice in three arcs (HO 491 refresh, stale again by 492/493), both times because a shipped layout change didn't carry its own doc update — the cost of the code/docs-split convention, which is otherwise correct. Flagged, not acted on; if it recurs a third time the convention needs revisiting. **Also notes now run through HO 494.**

**Also (HO 495–496), the `/bills` two-pane redesign — probe → build, PARTIAL verdict, no theme-% change.** Third and final application of the `.mc-*` two-pane pattern (after committees on `/members` and LDA issue codes on `/lobbying`), so the pattern now spans all three category surfaces: committees, issue codes, CBT topics. **(1) HO 495 — filtered topic-distribution cost probe (`0d21148`, read-only diagnostic).** The rail's per-topic counts rebase on the other active filters — the query shape that produced the HO 382 `/?topics=` 500. Verdict **PARTIAL**: the bounded dims (stage/chamber/ceremonial) rebase cheaply (stage-present combos ~35ms; worst bounded — chamber/ceremonial alone — ~150–600ms, no costlier than the query already live on `/`), but **`q` is excluded** — it gives *zero* scan-narrowing (the query GROUP BYs all 24 topics regardless, so broad "act" at 29k matches and narrow "quantum" at 74 cost the same full-corpus walk) and every distinct search string would be its own unbounded cache key. No new index needed — the two HO 382 partial covering indexes suffice, and the stage-absent `INDEXED BY` hint stays mandatory or the planner regresses to the MULTI-INDEX-OR misplanner. **(2) HO 496 — the build, two commits.** `getBillTopicRailCounts` (`e84f7c5`) is a **new sibling**, deliberately NOT an extension of `getTopicDistribution` (widening that helper's two dashboard callers would silently reshape the treemap — the HO 486 riding-regression shape); it rebases on the bounded tuple only, zero-pads to 24 topics, and is self-excluding (never applies the topic clause). Then the two-pane chrome (`d789c20`): the 24-chip topic row becomes a **multi-select** `TopicRailRow` rail (the one divergence from the two single-scope rails — `?topics=` OR semantics preserved), the stage legend + party key fold into a keystrip, bill rows + client single-open expansion are unchanged, NEWS mode is untouched, and the CSS is purely additive under `.bl-*` so `/members`/`/lobbying`/`/bill/[id]` stay byte-identical. Live-verified: OR grows the count (HLTH+TAX 3,873 > HLTH 2,778), a stage filter rebases the rail, `q` leaves it unchanged, the three other pages render byte-identical. **No theme-% change** — a browse-surface restructure on an existing pillar. Docs: HO 497. **Also notes now run through HO 497.**

**Also (HO 499–501), the electoral-orphan sweep + the `/news` route extraction — no theme-% change.** **HO 499 (`2f6636c`), the HO 333 dead-component sweep:** deleted the four electoral orphans the Races+Primaries→Electoral consolidation (HO 333) left unreferenced — `RacesMap`, `PrimariesMap`, `PrimaryDistrictModal`, `PrimaryScrubber` — with the build compiling every electoral route as the safety net (a dangling import fails the build). **HO 500 (probe, read-only), the `/news` extraction scoping:** the premise going in was a greenfield route build; it wasn't. `app/news/page.tsx` **already existed as a redirect stub pointing INTO the thing it was meant to replace** (`/bills?mode=news`), so the real work was **inverting an existing arrow**, not laying a new one. Probe verdict: `NewsView` moves cleanly and there are **zero shared-component signature changes** — the HO 486 riding-regression pattern did *not* fire (the only edited component, `NewsFilters`, is single-consumer, confirmed by grep). Recommended: drop the cross-carry (option a), invert the redirect carrying `?bill=`, keep `mode` as a legacy alias. **HO 501 (two commits), the build:** `05ee430` adds an optional `basePath` prop to `NewsFilters` (additive, default `/bills`, byte-identical behavior) as a separable revert boundary; `ffdf243` is the atomic extraction — `/news` renders the feed (was the stub), carries **only** its five news params (`source`/`topic`/`window`/`bill`/`signal` + `page`), the eight-BILLS-param cross-carry is **dropped**, the redirect is **inverted** (`/bills?mode=news` → exactly one 307 → `/news`; `/news` itself does 0 redirects — the two-way-redirect brick avoided, curl-verified both directions), `mode` kept as a one-`if` legacy alias, the ⚡ `MediaAttentionCell` chip repointed to `/news?bill=`, and `breadcrumb` + `pathToNavKey` given `/news` lines. **`/bills` no longer carries ~10 params across two modes — the backlog's standing "`?mode=` is doing a route's job" observation is discharged.** (STEP 0 note: the `fit-finish.spec.ts:614` `/news` test PASSES against the new routes — its body only asserts loads-clean, never the redirect — so it's a title-rename, tracked as a separate code commit, not folded into these docs.) Docs: HO 502. **Also notes now run through HO 502.**

**Also (HO 503–504), the Phase-2 CI arc — a loop closed by replacing its own criterion, no theme-% change.** The Playwright-in-CI loop, carried since Jun 30 (HO 379), closed **backwards**: sent to build a fixture, it came back arguing against one. **HO 503 (probe, read-only, no commit) — verdict: don't build the fixture.** It was sent to cost the seeded local libSQL fixture the backlog had preferred since HO 491 (libSQL is SQLite, so a fixture removes the secrets question rather than managing it). Two results worth keeping though the verdict mooted them: `file:` works through `getDb()` **with zero `lib/db.ts` change** (the local sqlite path ignores the injected `boundedFetch` + the undefined `authToken`, so HO 238's HTTP timeout/retry semantics are provably untouched), and `migrate.ts` runs clean against `file:` (52 tables, FTS5 populated). Bucket count across both specs, 73 tests: **A 41 · B 28 · C 2 · D 2 · E 0** — 95% nominally fixture-servable. **The verdict turned on none of that.** It turned on the **guard inversion** (oddities): the specs guard with `if (await x.count()) … test.skip`, so an under-seeded fixture doesn't fail — it *passes*, exercising nothing. Green that proves nothing is worse than the red-blindness the fixture existed to cure; and pre-merge protection buys minutes in a single-committer flow that already diff-gates + prod-verifies every change, against a hundreds-of-row artifact maintained forever versus a 132-statement schema. **HO 504 (build, two commits) — the loop closed the other way.** `fc42872` (spec hardening) + `482cd55` (workflow). `.github/workflows/e2e-prod.yml`: an unattended prod crawl, `deployment_status` (gated `state=success` + `environment=Production`) + daily `0 15 * * *` + `workflow_dispatch`, running `e2e/smoke.spec.ts --grep-invert "@nonci" --retries=2`, `timeout-minutes: 20`, report artifact on failure, global concurrency `cancel-in-progress`. Deliberately a **separate workflow file, not a job in `ci.yml`** — it never runs on the PR path, so it can't become a required check that fails to report under the `paths-ignore`/branch-protection landmine `ci.yml` documents. **Step 0 (verified live, not assumed):** Vercel emits GitHub `deployment_status(state=success, env=Production)` to this repo, so post-deploy coverage is real; the event's `target_url` is the protected per-deploy alias, so the crawl runs against the stable prod domain instead, and the ~2min of checkout + `npm ci` + browser install is what makes that safe against the alias-swap race (the workflow comment says so as a constraint on future edits). Spec hardening: `isKnownNoise` extracted to `e2e/console-noise.ts` (single-source, both specs import it), the race-drawer test retargeted to `/electoral` with its skip-guard **converted to a hard assertion** (the cartogram is static topojson, so zero cells = regression), the bill-row selector re-anchored on the `role=button` + `aria-expanded` toggle contract, and the markets-hover case tagged `@nonci`. **Trigger coverage stated honestly:** `deployment_status` PROVEN (run `30040913867`, job ran-not-skipped, green 2m39s), `workflow_dispatch` PROVEN (run `30041280979`, exercises the `if:` fall-through branch `schedule` shares), **`schedule` UNPROVEN until its first fire** (can't be forced — tracked in backlog). **Two hardenings resolved, one retired:** #3 (bill-presence selector) shipped — but the backlog's description of it was **wrong on both counts** (it claimed an `<a href>` selector broken by `<li role=button>` rows; the actual code was a `.v2f-row` class selector against a `<div role="button">`); #2 (console allowlist) shipped as the shared extraction; **#1 retired as moot, not done** — "freeze the marquee so the hover lands" was specified before BotID was understood, and prod withholds the tape from headless Chrome entirely, so freezing a non-rendering animation fixes nothing. **No theme-% change** — test infrastructure, no user-facing surface (the HO 379 / 382–386 harness precedent); Foundation holds at 96. **Also notes now run through HO 504.**

**Also (HO 507, `eaf79a4`), the `/bill/[id]` detail-page redesign — panel-family hero + sectioned layout, no theme-% change.** The dedicated bill page had been *outclassed by its own hover preview*: the feed expand panel (HO 317) rendered a colored `BillStageBar` where the page showed a plain `StageIndicator`, carried RELATED NEWS the page lacked **entirely** — even though the ⚡ media-attention chip links *here* — and surfaced the summary up top, while the page buried it **6th down** on the one surface whose whole job is "WTF is this bill." So it was rebuilt as **the panel's big sibling**: a bordered hero reusing the shared `BillStageBar` + the `.bxp-body` 1.35fr/1fr grid (summary prose + RELATED NEWS via `getNewsForBill(bill.id, 5)` — the one added read, the exact query `/api/bill/[id]/panel` already runs — on the left, the boxed `.bxp-metabox` on the right), then member-hub section shells below: **COMMITTEES | HEARINGS** side-by-side via `.bdp-pair` (single-column ≤900px, **lone survivor renders full-width**), **AMENDMENTS** with the agreed/failed tallies folded into the header + a **340px scroll bound** (so `119-sconres-7`'s magnet render stops setting page height), **LOBBYING** with filings/clients folded up, and a one-row footer with a no-JS `<details>` raw-JSON toggle. **Four condensation moves** on the HO 492 fold-into-header precedent: the standalone `LATEST ACTION` section collapsed into the metabox, the amendment + lobbying stat lines pulled up into their shell headers, the footer squeezed to a single row. **Containment held** (the HO 486/492/496 additive-scope pattern): zero edits to the shared `.bxp-*`/`.v2f-bar` rules, every deviation additive under a new `.bdp` wrapper — the feed expand panel verified **byte-identical**. **`TopicTags` deleted** — `/bill/[id]` was its last consumer, closing the HO 316 holdout, so the B6 chip family (topic 316 · ID 466 · stage 467 · party 468) is now genuinely complete rather than "complete except one page"; `TopicChips` carries the metabox topics. The point-lookup `getBillById` gained **three plain `bills` columns** (`sponsor_bioguide_id`, `cosponsor_count`, `stage_changed_at`) — *not* because "the type lied": they're **optional** on the shared `FeedBill` row shape, so tsc could never have flagged the omitted SELECT (the check it can't run — oddities HO 508). And a **display-only** sponsor strip (`sponsorDisplayName()`, page-only) drops the `[P-ST]` embedded in `sponsor_name` so the colored `PartyTag` isn't double-printed — the query keeps the embedded form, the `BillAmendments` precedent. **No theme-% change** — a chrome restructure on an existing surface (the HO 486 / 492–493 / 495–496 browse-redesign precedent, every one of which held its bar); with this landing, **all three category browse surfaces (`/members` 328, `/lobbying` 486, `/bills` 496) plus the detail page now share one design vocabulary — the arc that opened at HO 328 is complete.** **Numbering — the 504 → 507 pointer jump skips no blocks:** HO 505 was the doc sweep *for* 503–504 (its content *is* that block, plus the SKILL testing-section refresh at `2c453af`), and HO 506 was docs-only (`22fd265` — backlog + oddities for the CA-cert pre-flight; no shipped surface, so no roadmap block was owed) — the HO 487 precedent for the skipped 484. Docs: HO 508. **Also notes now run through HO 507.**

**Also (HO 509–510), the shared record box — two detail pages onto one tabbed shell, no theme-% change.** The member hub was the last detail surface still in the pre-328 idiom — four unboxed strips stacked above the first section (`MemberHeader` / `MemberStats` / `MemberAffiliations` / `MemberFundraisingLine`, all floating on `--bg-base`), the DW-NOMINATE ideology axis buried *inside* the Voting-record section three sections down, and six full-width sections stacking to ~3,400px with two of them empty states ("No disclosed trades", "No recent news") on a typical House member; `/bill/[id]` had the same stack four sections deep. Both converged on one new `components/RecordTabs.tsx`. **HO 509 (`8538660`) — the component + the member hub.** A `"use client"` island on the `ActivityTabs` (HO 132/249) contract: holds **only** tab state, takes pre-rendered `ReactNode`s as props so the server page keeps every fetch; all panels mount with `hidden` (unmounting would drop an open `BillRowList` row on every swap); `tabs.length === 0 → null`. The hero became one bordered card in three bands — identity + a five-figure inline stat run (band 1), `MemberIdeology` **promoted out of Voting record** to the page's spine (band 2), and the metabox **unrolled sideways** into a `data-cells` N-up that collapses to 2-up/1-up when a cell is absent (band 3). Six sections → one box: Bills · Votes · Committees · Amendments · Press · Trades, plus a conditional **Scorecard** tab (Senate Democrats only — genuinely inapplicable, not merely zero). **Two components deleted**, both grep-confirmed single-consumer: `MemberAffiliations` (it re-printed the badges `MemberHeader`'s meta line already carried) and `MemberStats` (absorbed into the band-1 run). **The 509 follow-up, three fixes before the push:** `.rec-body` went from a fixed `430px` to `min-height: 260px; max-height: 430px` — **HO 244's no-jump rationale doesn't transfer**, because 244 protected content *below* its panel while the record box is the last element before the footer, so a swap moves only the footer; a bounded shift beat pervasive dead space on the majority of tabs. Internal out-links (`/bills?sponsor=`, `/trades?member=`) had regressed to plain `<a>` and were put back on `next/link` (external keeps `target="_blank"`). And the **election readout was printing twice** — `MemberHeader`'s chip *and* band 3's cell — so it consolidated into band 3, which also **fixed a live bug**: band 3 checked only `nextElectionYear != null` and rendered a stale "Next election 2024" where `MemberHeader` had correctly branched to "Former member." `MemberHeader` lost its election-chip block, its `rating` prop, and its **local `ratingColor()`** — the one carrying a "keep in sync with `RatingChip`" warning, so that duplicate map is discharged; the page imports the shared `lib/race-colors` one. **HO 510 (`d22fe66`) — the second consumer.** `/bill/[id]`'s four sections became the same box: Committees · Hearings · Amendments · Lobbying in **fixed legislative-process order** (referral → hearings → floor amendments → outside money, the order the sections already sat in). One additive prop, **`initialKey`**, opens the heaviest tab without the strip ever reordering — so `119-s-4784` opens on Amendments (872) and a one-referral bill opens on Committees. **The hero was not touched:** HO 507's containment rule held, no `.bxp-*` line moved, feed expand panel byte-identical. **Retired:** the page-local `SectionShell`, `.bdp-pair` (+ its `> section` rule and ≤900px query), `.bdp-amend-scroll` and `.bdp-out` — ~40 lines of `.bdp` CSS, three weeks after HO 507 landed it; the HO 507 stat lines that had been folded into section headers moved down one level into `.rec-note` sticky strips. **The two empty-state policies are opposite and both correct** — the arc's most transferable decision: member tabs render at zero and dim (`TRADES (0)` is a fact about a person), bill tabs are **omitted** at zero, so a bill with none of the four gets **no box at all**, preserving HO 507's section-omission exactly (see oddities). **`key={entity}` on both call sites** — `key={bill.id}`, `key={member.bioguideId}` — added as insurance against a same-instance route reconcile stranding the lazy-`useState` tab index, not a demonstrated bug (see oddities). **No theme-% change** — a chrome restructure on existing surfaces (the HO 486 / 492–493 / 495–496 / 507 precedent); it **extends** the HO 328 vocabulary arc the 507 block called complete rather than contradicting it — the browse surfaces converged on `.mc-*`, and the two detail pages have now converged on a second shared shell. Docs: HO 511. **Also notes now run through HO 510.**

**Also (HO 512, `af3ab90`), `?member=` on `/news` + the Press-tab out-link — no theme-% change.** The record box (HO 509) gave Bills + Trades out-links but left Press with nowhere to go — `/news` (HO 501) carried `source`/`topic`/`window`/`bill`/`signal` only. Three files, one commit: `/news` gains a `?member=` mode, and the member-hub Press tab gains `View press coverage →`. **The mechanism correction is the load-bearing content.** The backlog's close-path asserted `getNewsFeed` "already joins `observation_entities`" — **it doesn't**: `getNewsFeed` drives `news_mentions m INNER JOIN bills b` and never touches the observation tables. A person-filter bolted on there would have returned only articles that both name the member *and* matched a bill — a strict subset of what the Press tab previews (most of a member's coverage has no bill match), so the out-link would have landed on **fewer** rows than the tab it came from. So member mode is a **branch to the observation layer** — `getMemberNews`, the exact query the Press tab already renders — not a filter on the mention feed, and it ships **zero `lib/queries.ts` changes** (the HO 501 route-split precedent: the honest fix was structural, not another param on the existing query; see oddities). **Precedence:** when `member` parses it owns the view — `bill`/`source`/`topic`/`window`/`signal`/`page` are ignored and not carried, and the pill's ✕ returns to bare `/news`; `getMember` null (valid-shape but unknown bioguide) renders member mode with the raw id + empty state (never a 404), and a parse-fail falls through to the unscoped feed. **The filter-bar asymmetry, stated as a rule so it isn't "fixed" later:** member mode early-branches `NewsFilters` to render *only* the member pill — observation rows carry no source/topic/window/signal dims, so dead chips would imply filtering that isn't happening; bill mode keeps its chips because bill-scoped rows are still `news_mentions`. Both correct. **The cap asymmetry, disclosed:** the tab previews `getMemberNews(…, 8)`, the out-link serves `NEWS_MEMBER_CAP = 50` (file-local, the HO 493 precedent), so the out-link deliberately lands on *more* rows than the tab — the opposite direction from Bills/Trades (whose out-links exist because their counts exceed the shown rows). Hence **no count in the label**: `count: news.length` is a cap, not a total, and "View all N" would be a lie. **Not done (recorded so it isn't re-litigated):** no `getObservationNews` fold — the HO 414 debt triggers on a 3rd *projection*, and this is a 3rd *caller* of the existing helper at a raised limit (the HO 470 "adds callers, not a third projection" line); no pagination, no total-count query, no `getNewsFeed` change of any kind; the `/news` source-spine two-pane stays a separate QUEUED follow-on (this added a mode, not the rail). **No theme-% change** — a cross-link on existing surfaces (the HO 509–510 precedent); it **discharges** the backlog's Press-out-link item in full. Docs: HO 513. **Also notes now run through HO 512.**

**Also (HO 514–515), the `/news` topic rail — the 4th and final `.mc-*` two-pane, no theme-% change.** The `.mc-*` two-pane pattern now covers **every browse surface**: committees (`/members` 328) → LDA issue codes (`/lobbying` 486) → bill topics (`/bills` 496) → **news topics (`/news` 515)** — the arc that opened at HO 328 is complete across browse surfaces (the detail pages converged separately on `RecordTabs`, HO 509–510). **The respec is the block's real content:** the banked item named **SOURCES** as the spine, but `NEWS_SOURCES` has **three** entries against 24 / 79 / ~200 on the shipped rails — three rows is the SOURCE chip row relocated, not a spine. So the rail shipped on the **TOPIC** axis instead, and the SOURCES idea is **dead on cardinality, not deferred**. **HO 514 (`f7a3de4`), the probe — GO, live-servable, hint mandatory:** unhinted bare **2231ms** cold (the MULTI-INDEX OR off `idx_bills_is_ceremonial` — the HO 332/479–481 drive-order class firing on a *fourth* surface), `signal=breaking` 943ms, vs **29–64ms hinted** across every combination; when `source` is set the planner picks `idx_news_mentions_source` itself but hinting there costs nothing (31–33ms), so **one always-hinted query** beat a conditional. No new index. Corpus **re-measured 227 → 726 rows / 439 distinct articles** (3.2×, not order-of-magnitude, so the live call stands — the stale 227 lived in 4 places incl. 2 code comments, corrected across HO 516's two commits). **HO 515, two commits:** `a2c825d` — `getNewsTopicRailCounts`, a **new sibling** of `getNewsFeed` (not an extension — the HO 496 `getBillTopicRailCounts` reasoning), self-excluding on `topic`, rebased on source/window/signal, `unstable_cache` tag `news-breaking` (no new cache wiring), `try/catch → null`, 24 topics zero-padded VOL-desc; `8bb9618` — the `.mc-pane nw-pane` chrome. **Two calls recorded as rules:** (a) **the rail counts DISTINCT ARTICLES, not mentions** — the feed's own total is distinct-articles (HO 133), so a mentions-counting rail would print a number the feed contradicts on click (the HO 442 structural-coherence discipline); enforced by extracting the dedup expression to one module-level **`NEWS_ARTICLE_KEY_SQL`** both consume, so they *can't* drift (the `getNewsFeed` refactor was pure substitution, feed verified unchanged; live: `?topic=government_operations` → rail 43 === feed 43). (b) **single-select rail**, a new `NewsTopicRailRow` on the `/members` + `/lobbying` `.mc-crow.is-sel` idiom — **not** a lift of `/bills`' `TopicRailRow`, whose multi-select checkbox is the documented *divergence* (plural `?topics=` OR) against `/news`'s singular `?topic=`; widening the `/bills` component with a mode flag was rejected as the HO 486/496 riding-regression shape. **Three modes, stated as contract:** default → rail renders; `?bill=` → no rail (one bill is a single-topic slice); `?member=` → no rail (different table — HO 512's mode comes out byte-identical, `hideTopics` left off so its chip row returns). The hidden rail in member mode is a **deliberate seam Axis B reopens**, not an oversight. **CSS additive under `.nw-*`** — diff-confirmed no shared `.mc-`/`.lob-`/`.bl-`/`.bxp-`/`.bdp-` rule edited, so `/members` / `/lobbying` / `/bills` are byte-identical (the containment rule has now held through 486 / 492 / 496 / 507 / 515). **Axis B, banked with its constraint (the forward-looking part):** an **"IN THE NEWS" member group as a SECOND group in this same rail**, off the observation layer — probe cardinality **126 bioguides ≥1 obs, 39 ≥3, 10 ≥10**, head genuinely newsy (Graham 49 · Thune 29 · Collins 21 · McConnell 20), **100% of resolved bioguides hit a live `members` row**, rollup 34ms off `idx_obs_entities_type_value`. **The load-bearing constraint: the member group needs its OWN window, decoupled from `?window=`** — at 24h the head collapses to 6 members (0 at ≥3), so rebasing it on the page window empties it; and the observation layer is only ~30d old (HO 394 pilot), so 30d ≈ all-time *today* and that equivalence will stop holding. **No theme-% change** — a browse-surface restructure on an existing pillar (the 486 / 492–493 / 495–496 / 507 precedent). Docs: HO 516. **Also notes now run through HO 515.**

**Also (HO 517, `a71a1d4` query · `f1b1167` chrome), the `/news` "IN THE NEWS" member rail group — the Axis B seam closed, no theme-% change.** The forward item the HO 514–515 block banked (a second rail group off the observation layer), built with **no probe step** — HO 514's cardinality/cost numbers stood up unmodified, the payoff of measuring both axes in one probe. The `?member=` seam HO 515 left deliberately open (member mode rendered no rail) is now closed: the `.nw-rail` carries **two groups** (TOPICS + IN THE NEWS) inside its one bounded `.nw-rail-scroll`, and member mode is a first-class rail selection rather than a deep-link-only view. **The query (`a71a1d4`):** `getNewsMemberRailCounts()` off `observation_entities` (`entity_type='person'`) → `observations`, `INNER JOIN members` for name+party, ranked desc + name-tiebroken, returns `{ rows, total }`; EXPLAIN-confirmed it drives `idx_obs_entities_type_value`, `unstable_cache` tag `member-news` (already flushed by the news cron + allowlisted on `/api/revalidate`, HO 414 — no new wiring), `try/catch → null` hiding **only this group** (TOPICS + feed still render). Plus `getNewsMemberRailPin(bioguide)`, an **uncached point-lookup** for the pin — a bounded `activeBioguide` arg on the counts query would make every visited member its own cache key, so the separate lookup keeps the counts cache clean. `NEWS_MEMBER_RAIL_WINDOW_DAYS = 30` is exported so the header label and the query can't drift. **Four design calls, all verified live:** (1) **fixed 30d window, disclosed** — the member group does NOT rebase on `source`/`window`/`signal` (header `IN THE NEWS · 30d`); 30d not "all-time" because the observation layer is ~30d old (HO 394 pilot) so they're equal today but the label stays correct as the corpus accrues (the HO 456 disclose-the-exclusion posture) — verified Graham holds 49 at `?window=24`. (2) **units match** — HO 394 writes one observation per fetched article, so both groups count **articles** (the HO 442 coherence that lets two count columns share one rail; recorded in a comment). (3) **one selection across both groups** — the two groups drive different panes (topic → mention feed, member → observation feed) so they can't co-activate; `NewsTopicRailRow.scope()` now also clears `member` and selecting a member clears `topic` (topic↔member mutually clear, exactly one lit row spanning both groups). (4) **top 15, total disclosed** (`… · 15 of 126`), with the active member **pinned** at the group top when outside the shown set — the one path the rail can't reach from itself (the HO 512 Press-tab out-link can send any of ~535 members here), so without it a low-coverage arrival renders a rail with nothing lit: the HO 492 selection-invisibility failure through a different door. **The zero-count pin holds:** a pinned member with 0 recent observations still renders, lit, count 0 (the pane below is showing their coverage, so the rail must agree they're the active scope). **Chrome (`f1b1167`):** a new single-select `NewsMemberRailRow` (party dot via `lib/race-colors`, not a local map — HO 468; surname-forward + count, no bar), both groups sharing the one `.nw-rail-scroll` (no nested scroll), `scrollIntoView({block:"nearest"})` covering the member group too; CSS **additive under `.nw-*`** (a `.nw-rail .mc-crow.nw-mrow` grid override + dot + pin hairline + group-header border) — diff-confirmed no shared `.mc-`/`.lob-`/`.bl-`/`.bxp-`/`.bdp-` rule touched, the **sixth** consecutive test of the containment rule (486 / 492 / 496 / 507 / 515 / 517). **Mode contract now:** default → both groups selectable; `?topic=` → topic lit, mention feed; **`?member=` → rail renders** (the 515 seam closed — TOPICS shows bare counts, IN THE NEWS shows the member lit/pinned), the HO 512 observation-feed pane below unchanged; `?bill=` → still no rail, byte-identical. Prod-verified across bare (15 rows, Graham head), `member=Lawler` (rank ~30, injected+lit, `nw-mrow-pin`×1, observation feed), `member=Aderholt` (0 recent obs, pinned+lit, count 0 — the call-4 zero case), the window-independence check, and `?bill=` unchanged; `tsc` clean, `verify:deploy` served `f1b1167` === HEAD. **No new nav, migration, cron, or index; the 35 unjoinable person rows (no bioguide) stay excluded (the HO 398 challenger edge); SOURCES stays dead on cardinality (HO 514).** **No theme-% change** — a browse-surface add on an existing pillar (the 486 / 492–493 / 495–496 / 507 / 514–515 precedent). With this, the `.mc-*` two-pane arc that opened at HO 328 is complete across every browse surface **and** the `/news` rail now carries the member axis the observation layer was built to enable. Docs: HO 518. **Also notes now run through HO 517.**

**Also (HO 519–520), the 2nd full re-derivation + the SC seat-change fix.** **HO 519 (read-only audit, §0–§5 against `5681322`):** the deferred "next full audit," done as re-derivation #2. **Structurally green** — sync fresh (`MAX(update_date)` today, `bills` 17,027 == `bills_fts`), tracked corpus **16,657** (`getCorpusStats(true)`, up from 15,075 at HO 404), summarize queue drained to 4; `/api/health` 200 with **all 14 crons fresh**; the **four fixed loops holding** in steady state — summarize (406) not re-grown, weekly-report (407) present + catch-up healthy, `/api/sync` trailing-step (480/481) at **77% → 7%** post-fix timeout (the residual ~55s reaping is the by-design HO 477 soft-cap, not failure), trades (483) step p90 **10,530 → 5,600ms** (clear of the ~13s re-probe line); cold-plan audit **0 unexpected BAD of 50**. **Cold-plan gap-closer:** `cold-start-audit-332`'s SQL inventory predates the LDA / amendments / nominations / news-rail pillars, so its "0 BAD" is blind to them — the audit **hand-EXPLAIN'd 9 post-404 queries** (the `*RailCounts` trio, the amendments disposition/aggregate scans, the nominations GROUP BYs, the `getBillLobbying` seek), all riding their intended index or covering index (extending the script is a **standing observation**, not owed here). **Two ratified bar decisions:** **News signal CLOSED → 98** — the "obs layer joinable" label under-sold a now-full pillar (`/news` route 501 + `?member=` 512 + topic rail 515 + member rail 517, race→news 398, member→news 414, dashboard NEW badge 470); `98` not higher because the residue is gated/inert (the observation envelope grading/cluster/lineage fields, the policy-hook→bill/topic linkage, the the_hill budget soak), not active gaps. **New combined bar `Lobbying / amendments / nominations` = 92** — one bar (the 435–463 "extend downstream along the bill spine" arc; the `/patterns`-class aggregate+drill shape; each individually thinner surface than Member/Races; naming matches the `Committee activity / hearings` slash style); `92` = v1-complete across all three (data + aggregate + detail + cross-links) with real banked gaps (LD-203 money surface, military-noms view + `/nomination/[id]`, amendment→roll-call `recordedVotes` linkage, the `/lobbying` fbar features + description clamp). **Overall ~98 → ~97**, recomputed from the reconciled **9** bars; **all other bars held** (Foundation 96 / Home 100 / Visualizations 97 / Weekly reports 98 / Member depth 99 / Races 99 / Committee activity / hearings 98). **HO 520 (SC seat-change fix — data-only, zero commits):** Sen. Lindsey Graham (`G000359`, R-SC, class 2) **died** (served 2003–2026); Gov. McMaster appointed **Darline Graham (`G000608`)** as caretaker until 2027-01-03 — **verified against governor.sc.gov** (the HO 422 Kiley verify-before-fix discipline: a logged "wrong-incumbent" was a real post-cutoff event, not bad yaml). CBT's stale roster (`G000359` still `is_current=1`, `G000608` absent) + the `S-SC-2026` incumbent were fixed by the routine **`sync:members` → `sync:crosswalk` → `backfill:races`** (the HO 402 Gallagher-staleness class recurred), the **HO 410 audit re-ran fully green** (§1/§2/§4 all 0/0), and the live `/electoral` + `/race/S-SC-2026` now render Darline Graham — **zero code, zero commits**. The **Aug-11 special-primary re-ingest is parked** (SC 2026 became an open special — special primary Aug 11, runoff Aug 25, general Nov 3; the field's still forming, so the stored June-9 regular primary re-ingests once results exist — the "re-poll as the field settles" pattern). Docs: HO 521. **Also notes now run through HO 520.**

**Also (HO 522–525), the member-scorecard participation arc — B1 chamber-context + B2 career rollup, Member depth 99 → 100.** **HO 522 (source probe):** participation **GO**; the "VoteSmart" idea was **NO-GO on *access* not join** — the icpsr↔votesmart join was already solved in `member_ids.votesmart`, but the SmartVote/VoteSmart API is retired behind a Cloudflare/ToS wall, so it's an access dead-end; **broader external scorecards reframed from a hold to optional breadth** (LCV CSV-export + Heritage bioguide-keyed URLs both viable-but-unbuilt, ADA dropped stale, USCPR already ships as one instance). **HO 523 (B1, `2b8d62b`):** the 119th missed-rate was **already on the hero** — the delta was chamber-median **context** beside it (`getChamberParticipationContext`, a cached all-member distribution, **no new table**); **floor F=50** over the population for both the median and rank-eligibility (floored only 3 departed members, clips zero current). Median shipped in the hero; a bare attendance **rank was computed-then-dropped** (delegate-inflated + unused — the drop-unused-field call). **HO 524 (cost probe):** streaming ingest **GO** — 52 files 94→119, ~356MB, flat ~130MB peak (the accumulator is 536 members × a few ints, so memory doesn't grow with range; stream per-file, discard, next). **HO 525 (B2, `bf63b23`):** `member_career_votes` rollup via manual `sync:career-votes` (truncate-then-recompute off Voteview's per-Congress votes files, current-only via an `is_current=1` join), the career stat on the hub beside the 119th (`Missed · career · N Congresses`); cross-checks passed (Collins 0/10,062 since 1997, Grassley 1.09%/26C, Fetterman 13.1%), Graham (no icpsr) → graceful null. **Two data findings worth the oddities ledger:** (1) Voteview `cast_code` **7/8 = Present = participation**, so the correct split is **cast = 1–8 / miss = 9 / not-seated = 0-or-absent** — the coarse {0,1–6,9} model the HO 524 handoff carried was **wrong**, caught only by parsing real rows; (2) **non-voting delegates carry structurally-inflated miss-rates** (can't vote on final passage), so a *rank* surface needs a delegate-eligibility carve-out while a member's *own-rate* display keeps it (B1/B2 precedent). **Member depth 99 → 100** — the substantive participation gap shipped end-to-end, the "external scorecards" half reframed optional (see provenance). No new nav / cron / migration beyond the one rollup table; the probe + cost scripts stay untracked. Docs: HO 526. **Also notes now run through HO 525.**

**Also (HO 527), the `/members` participation dotplot — the participation arc's population cut, no bar move.** The population twin of the member-hub participation display (B1 523 + B2 525), built as `IdeologyStrip`'s sibling — the exact member-hub→`/members`-dotplot progression the ideology arc took (hub axis 421 → dotplot 425). Shipped + prod-verified; live SHA `7fa729c`. **`getParticipationStrip()`** (`lib/queries.ts`) is a new sibling of `getIdeologyStrip` (NOT an extension of `getChamberParticipationContext`, which returns only per-chamber medians): every floored current member (`is_current=1`, `HAVING total >= PARTICIPATION_FLOOR`, metric `not_voting/total` — the **identical B1 population**, so the strip's client-computed chamber medians echo the hub context by construction) plus an `is_delegate` flag; small-table population read, no `INDEXED BY`/no new index (the `getIdeologyStrip`/band regime), `unstable_cache` tag `votes`/3600. **`ParticipationStrip`** (`components/ParticipationStrip.tsx`) is `IdeologyStrip` cloned with the metric deltas: domain **0..CAP=30** (locked from STEP 0's non-delegate max 29.02, not a mockup number), 0.5pp Wilkinson bins, dots **party-colored** (so high-miss clustering by party reads — roadmap lens #4), median ticks on the **CHAMBER split** (HSE/SEN, neutral amber — NOT party, the meaningful split since the House votes far more than the Senate), no empty-center annotation (participation is unimodal-skewed). Placed **open directly below** `IdeologyStrip`, rescoped by the same chamber toggle only (`partDots` mirrors `stripDots`); CSS **additive under `.part-strip-*`**, `IdeologyStrip` byte-identical (the containment rule's next test). **The delegate carve-out is the load-bearing detail (the HO 523/525 banked constraint, now discharged for the dotplot):** non-voting delegates are excluded from both cloud and median via the territorial predicate `chamber='house' AND state IN ('DC','AS','GU','MP','PR','VI')` (there is no delegate flag — the 6-code set catches exactly the 6 non-voting members), with the excluded count disclosed in the footer (the LDA/nominations disclose-the-exclusion posture). **The STEP-0 finding corrected a premise (oddities):** the carve-out is **population-correctness, not outlier-trimming** — only 2 of 6 delegates (Radewagen 82%, Plaskett 50%) are extreme outliers; the real high-miss tail is a **voting** member (Frederica Wilson 29% > 4 of the 6 delegates), which is what sets CAP. So delegates are dropped because they're structurally ineligible on final passage, NOT because they're the high scores. Prod-verified: HSE med 1.9% / SEN med 2.1% (= the B1 medians), chamber toggle rescopes (ALL 2 ticks 530/6 · HOUSE 1 tick 431/6 · SENATE 1 tick 99/0), delegates absent from the cloud, hover→hub. **Member depth holds at 100** — an enabling surface, no bar move (the HO 425 dotplot precedent, which also held). **Banked from the arc, still unbuilt:** a participation *sort/rank* on the roster (the carve-out constraint still binds it) + a career-chamber-median context (cheap from `member_career_votes`, deliberately unbuilt to avoid a 4-stat hero). Docs: HO 528. **Also notes now run through HO 527.**

**Also (HO 529–530), amendment roll-call votes on the bill hub — the Senate half of the banked `recordedVotes` gap, query-only, no theme-% change.** The HO 452 `recordedVotes` lead (the capability that block banked when the amendments status model closed NO-GO), built probe-first. **HO 529 (`e0131cf`, read-only probe) retired the backlog's premise.** The banked item assumed a member-level "how did each member vote on this amendment" surface needs an **`/actions` walk** (~2× the amendments backfill) to recover each voted amendment's roll-call number. The probe found that **half wrong and the handoff's own candidate join dead**, payoff intact via two different keys per chamber (the "a backlog line's stated mechanism is a claim — probe it" rule, HO 512/514 class): **(1) `votes.amendment_designation` is dead for amendments** — it only holds nomination/treaty designations (the syncs tag amendment votes to the underlying bill or to nothing), 0/431 join. **(2) Senate is FREE (GO-CHEAP):** Senate amendment votes carry the number in `votes.question` (`On the Amendment S.Amdt. 14 to …`), a regex parse joins **114 SAMDT, 100% on tracked bills, `member_votes` present** (99–100/vote) — query-only, zero API, zero backfill, retiring the walk premise for the Senate. **(3) House needs the walk (GO-WALK, linkage-only):** House vote questions are generic (`On Agreeing to the Amendment`, no number, only `bill_id` — can't disambiguate which amendment), so a text parse can't do the House; the `/actions.recordedVotes.rollNumber` walk resolves **100% to existing `votes` rows** (BLOCKED-ON-SYNC refuted — the roll calls are all ingested), so the House is a bounded *linkage* backfill (store amendment↔`vote.id`), NOT a member-vote backfill. **(4) The buildable surface is small but clean** — ~114 SAMDT free + the rare HAMDTs recoverable via the walk (the HO 461 "the floor almost never votes" finding reinforced: 17/20 sampled HAMDTs never got a recorded vote), all on tracked bills, full member positions. **HO 530 (`1f9f748`) ships the Senate half — query-only.** `getBillAmendmentVotes(billId)` is a **separate** cached query (the HO 496/512 new-sibling discipline — `getBillAmendments` byte-identical; the regex parse can't live in its SQL): pulls the bill's SAMDT amendments, parses matching Senate votes from `votes.question`, attaches the tally (on the `votes` row directly — `yea_count`/`nay_count`/`result`) + the **D/R/I party split** from `member_votes` (a bounded per-vote aggregate), returns `Map<amendmentId, AmendmentVote[]>` (a **list** — 3 amendments corpus-wide carry a table+substantive pair). Rendered as a compact vote line under acted amendments in the `/bill/[id]` AMENDMENTS section — `Rejected 45–55 · D 41-4 · R 2-51 · I 2-0` (party-colored via the shared `partyColor`, HO 468) — and the display-only disposition dot **becomes authoritative from `votes.result` where a vote exists** (via the module `deriveDisposition`, no drift), keeping the keyword scan only for the no-vote majority. Byte-identical where no vote (~99% of rows — the absence is the signal, the HO 527 omit-don't-disclose-per-row call). **No cost probe** — 529 settled feasibility, the set is tiny (~114 rows corpus-wide); a straight build. Prod-verified on `119-hr-1` (the reconciliation bill's Senate vote-a-rama): 13 vote lines + 39 party-split fragments, the split summing to the tally in every sampled case (`119-hr-1`/`119-s-2`/`119-sconres-33`, e.g. a 50–50 VP-tiebreak reading R 50-3 party-line). **Two records for the ledger:** the **two amendment-row components are distinct** — the bill-hub row is an internal function inside `BillAmendments.tsx`; `components/AmendmentRow.tsx` is the *separate* `/amendments` corpus-feed row (HO 461) — so the HO 530 handoff's pathspec conflated them and the edit correctly landed in `BillAmendments.tsx` only (a SKILL note now, so future handoffs pathspec right); and **`votes.id` is a TEXT composite** (`senate-119-1-3`), not an integer. **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** this ships the **Senate half** of the 519-named `recordedVotes` gap, but the **House linkage walk and the full per-member roll-call drill both stay banked**, so the gap is partially—not fully—discharged (a v1 cut on an existing pillar, the LDA/amendments precedent every prior surface held). **Banked, no fixed HO number** (they take the next free number when built — the HO 418 don't-pre-number lesson; the "531" the HO 530 handoff pinned to the House walk was premature since this sweep is 531): the **House amendment→vote linkage walk** (GO-WALK, build-ready, no further probe — a bounded `/actions` walk over HAMDTs-with-a-recorded-vote storing a persisted `amendment↔vote.id` link + a cron delta step), the **full per-member roll-call drill** (v1 ships the partisan *shape*; the who-voted-how list is the heavier v2 expand, `member_votes` has the data), and the **`/amendments` aggregate "voted" cut** (once both chambers' links exist). Docs: HO 531. **Also notes now run through HO 530.**

**Also (HO 532–533), amendment votes across both chambers + a latent-500 fix — the `recordedVotes` gap closed, no theme-% change.** The House tail HO 530/531 banked (GO-WALK, no probe — HO 529 §C confirmed 100% of `recordedVotes.rollNumber` resolve to existing `votes` rows), plus the cache-serialization bug the build surfaced. **HO 532 (`3c4c72c`), the House linkage walk.** The House has no cheap key (generic `On Agreeing to the Amendment` question, no number), so the amendment→vote link is recovered from each HAMDT's `/actions.recordedVotes` — a **linkage** backfill (`amendment_id ↔ vote_id`), NOT a member-vote one (positions already in the DB). A new **`amendment_votes`** link table (composite PK for the table+substantive list case), an **`amendment_vote_walked_at`** sentinel on `amendments` (sync-invisible — the amendments-sync `ON CONFLICT SET` names every column so it's never clobbered, the HO 459 pattern verified by reading the clause), and a partial `idx_amendments_hamdt` walk-queue index. `sync:amendment-votes --walk` (`lib/amendment-votes-walk.ts`) fetches each HAMDT's `/actions`, **constructs** the deterministic `house-{congress}-{session}-{rollNumber}` vote_id (House `votes.id` is composable — no lookup), upserts the link, stamps the sentinel (the queue-exit; the re-walk predicate keys on `update_date > walked_at`, so a HAMDT that *gains* a vote after its first walk is caught — the live-corpus edge the nominations immutable-committee case didn't have). A **bounded cron walk step** rides after the sync in `/api/cron/amendments` (240s/280s carve, cap 40/tick, non-fatal). The read is the **same `getBillAmendmentVotes`** (Step 1 widened to `IN ('SAMDT','HAMDT')`, a new Step 2b joins `amendment_votes → votes`; the party-split, tally, and render are chamber-agnostic and reused). Backfill: **267 HAMDT walked, 92 links / 90 amendments, 0 fetch errors** (`119-hamdt-5 → house-119-1-32`, Failed 182–226, 432 member_votes — the HO 529 §C case confirmed). **HO 533 (`85d6660`), two fixes, one blocking the other.** (1) **The latent prod 500 — the bigger find:** `getBillAmendmentVotes` returned a **`Map`**, but `unstable_cache` serializes its return and a Map round-trips to `{}` on a **cache HIT** — so `.get` was gone and `BillAmendments` threw `votes.get is not a function`, 500-ing **every amendment-bearing `/bill/[id]` for ~59 of every 60 minutes since HO 530 first shipped.** It hid because every "verified live" check landed in the cache-**miss** window right after each deploy (three deploys, three misses). Fixed by returning a plain **`Record<amendmentId, AmendmentVote[]>`** (serializes cleanly), consumer reads `votes[a.id]`. (2) **HO 533's own ask — the zero-split guard:** `VoteLine` now suppresses the D/R/I party-split fragment when it's entirely zero (the `member_votes`-lag case — the newest roll calls' member detail trails the vote-list sync, e.g. `house-119-2-269`), so those rows show the tally alone and self-heal on the next votes sync rather than printing a false `D 0-0 · R 0-0` (the "wrong is worse than absent" HO 448 disposition-dot principle). The Map fix was a **hard prerequisite** — the guard lives inside `VoteLine`, which never rendered while `BillAmendments` threw first, so both shipped in one commit. Prod-verified on the cache-**hit** path (3 consecutive hits): `119-hr-8800` 200 (was 500), 19 tallies / 18 splits (lagging row suppressed); `119-hr-1` (Senate) unchanged at 13; a repo-wide ~90-function audit confirmed `getBillAmendmentVotes` was the **only** Map/Set/Date-returning cached function. **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** the House walk is the *linkage completion* of the same 519 `recordedVotes` gap (both chambers now render amendment votes), not a new surface, and the 500 fix is a correctness fix — recorded honestly as a regression that **shipped at HO 530 and lived latent for three deploys**, not a bar event. **Two process lessons (oddities):** never return a `Map`/`Set`/`Date`/class from an `unstable_cache` function (plain objects/arrays/primitives only — they round-trip to `{}`/`[]` on a hit, and tsc can't catch it); and **cache-miss verification right after a deploy does not exercise the cache-hit path** — hit it 2–3 times (or wait out the revalidate window) before calling a cached surface verified. **Banked, now both cleaner** (the House links unblocked them): the full per-member roll-call drill (both chambers ship the partisan *shape*; the who-voted-how list is the v2 expand) and the `/amendments` aggregate "voted" cut. Docs: HO 534. **Also notes now run through HO 533.**

**Also (HO 535), the participation *sort* on the /members roster — the ranked-list twin of the 527 dotplot, no theme-% change.** The banked participation sort/rank (the constraint the HO 523/525 carve-out was written for) — the "who misses the most votes" ranked list, complementing the HO 527 dotplot's distribution. Shipped `b565c13`. Adds a **MISSED** segment beside VOLUME / PASS RATE that orders the roster by each member's **119th missed-vote rate** (`SUM(not_voting)/COUNT(*)` over `member_votes`, `HAVING COUNT(*) >= PARTICIPATION_FLOOR=50` — the identical dotplot/B1 metric). **No new column — the shared `.mc-row` grid stays untouched:** `.mc-row` backs both `/members` AND `/lobbying`'s `FilingRow` (the filing rows fell back to the members grid), so widening it would reshape the lobbying rows; instead the existing **RATE column repurposes** to the missed-rate when the missed sort is active (amber vs passrate's green, `—` for null, the header swaps RATE→MISSED), leaving `.mc-row`/`.mc-r-num`/`/lobbying` byte-identical. **The delegate carve-out adapts HO 527 to a roster:** the dotplot *drops* the 6 non-voting delegates; a list can't drop members, so they're carved from the **ranking** (rate exposed NULL via the territorial predicate `chamber='house' AND state IN ('DC','AS','GU','MP','PR','VI')` → sort last + `—`, disclosed "non-voting delegates unranked") but kept in the list — same population-correctness principle, different mechanism. Threaded into **both** `getMembersRanked` (full list) and `getCommitteeRoster` (scoped view) via a shared `participationAggCte` + a shared `MISSED_CARVE_EXPR`, both gaining a **`votes` cache tag** (they read `member_votes` now, else they'd only refresh on the 86400s backstop — the `getChamberParticipationContext` precedent). **One spec bug the probe caught (oddities):** the handoff ordered by the bare `missed_pct` column, but SQLite resolves an unqualified column *inside an ORDER BY expression* against the input tables (`part_agg`'s raw rate), NOT the carved SELECT alias — so delegates (Radewagen 82%, Plaskett 50%) sorted to the top while displaying `—`; fixed by repeating the carve in the ORDER BY via the shared `MISSED_CARVE_EXPR`, re-probe confirmed. **No probe HO** (the missed-rate is the dotplot's own cheap ~536-row aggregate), but the build's throwaway probe verified the ranking: **TOP by MISSED = Wilson 29.0% → Hunt 24.6% → Kean 22.8%** (all voting members, matching the HO 527 dotplot tail), **BOTTOM = the 6 delegates + below-floor Graham showing `—`**. **No theme-% change — an enabling surface on `/members`; Member depth holds at 100** (the HO 425/527 dotplot precedent). **Banked:** a **career missed-rate sort** (its NULL-coverage set differs — the reason the dotplot's career toggle is banked) and the **career chamber-median context** on the hub (still the deliberate 4-stat-hero hold). Docs: HO 536. **Also notes now run through HO 535.**

**Also (HO 537), the `/amendments` "voted" cut — the last banked piece of the `recordedVotes` gap, no theme-% change.** HO 530/532/533 rendered amendment votes on the bill hub; the corpus surface still described the whole pillar through `latest_action_text` and couldn't say how many amendments actually drew a recorded floor vote. Shipped as four commits + a duplication fix, gated by a Step-0 cost/correctness probe. **STEP 0 (`amendments-voted-cost-537.ts`, `19a412e`):** the HO 461 inline cost-gate — 8 measurements. The equivalence gate (materializer set === live parse, per bill) PASSED; feed EXISTS EXPLAIN clean (composite-PK seek, no new index); containment clean (voted ⊆ acted, 0 with NULL action); tiebreak inert (0 same-date pairs). **Measurement 8 — a completeness detour that changed the design:** `votes` is append-only, so HO 529's "114 SAMDT" couldn't have fallen to the 97 the anchored matcher finds: of **161** senate votes that mention "Amdt", **97 are up-or-down** ("On the Amendment"), **64 are procedural** (`On the Motion to Table / Cloture Motion / Decision of the Chair S.Amdt. N`, + 50 budget-waiver `Amdt. No. N`). The pre-authorized "widen to capture the resolving residual" was **REJECTED on the data**: resolving an S.Amdt number is necessary, not sufficient — a procedural vote's yea/nay is on the MOTION, not the amendment (a `Motion to Table Failed` leaves the amendment ALIVE; `119-samdt-2310`/`4421` were WITHDRAWN, `119-samdt-2360` was ADOPTED 51–50 separately), and 3 of the 14 resolving residual already carry an anchored decisive vote a widen would double-line. **The anchored matcher is correct BY DESIGN** — it isolates the one class whose tally is renderable as support for the amendment. So: no widen, build as specced (oddities). **C1 (data, `92454f5`):** materialize the Senate links into `amendment_votes`, making it the single symmetric source of truth across chambers (House was HO 532's walk). Pure DB recompute (no API) — one `db.batch` delete-then-insert scoped by a `votes.chamber='senate'` join (not an id prefix), returning a real `changed` delta so the cron flushes `votes` only when the set moves (the recompute always rewrites, so row-count can't gate honestly). The Senate parser + LIKE moved to a **leaf `lib/amendment-vote-key.ts`** so the request-time parse and the sync-time materializer share ONE matcher (the `NEWS_ARTICLE_KEY_SQL`/`MISSED_CARVE_EXPR` anti-drift shape; leaf-safe so the `scripts/` materializer never drags `next/cache`); `getBillAmendmentVotes`' Senate path is byte-identical. A `--senate` CLI mode + a cron materialize step riding **before** the House walk (cheap-first, so a starved walk can't staleness the Senate links). **C2 (refactor, `acde5de`):** `VoteLine`/`voteVerb`/`dispositionColor` extracted to a shared `components/AmendmentVoteLine.tsx` (the corpus feed is the third consumer); byte-identical move verified by character-diff + `tsc` + build. **C3 (surface, `cb551c4`):** `getAmendmentsSummary` voted block (canonical/latest vote per amendment via `ROW_NUMBER` → `deriveDisposition`, the `list[0]` hub rule); `getAmendments` gains `voted` as a third disposition (EXISTS over resolved links) + page-scoped vote hydration (`hydratePageAmendmentVotes`, both chambers off the materialized table, party split FETCHED so the HO 533 zero-split guard reads true, plain `AmendmentVote[]` so `unstable_cache` round-trips clean); `AmendmentRow` renders the shared `VoteLine` with an authoritative-from-`votes.result` dot; the page gains a recorded-votes readout (authoritative agreed/failed), a `Voted` chip, a blurb chamber-contrast, and the exclusion-disclosure line. **Both cached fns gained the `votes` tag** — they read `votes`/`member_votes` now, so tags must follow the tables a query READS, not the pillar it belongs to (the HO 535 precedent; oddities). **Duplication fix (`c35837e`, own commit):** the C3 blurb repeated "187 drew a recorded floor vote" + "97 Senate · 90 House" verbatim with the readout; blurb now asserts qualitatively (claim), the readout owns the numbers (evidence) — the HO 509 double-print precedent. **Prod-verified on the cache-hit path (corpus at this snapshot 6,971 filed · 625 acted — HO 461's 6,788 was its own snapshot, left as written):** voted **187** (`42 agreed / 145 failed / 0 other`, `97 Senate / 90 House`); `?disposition=voted` 187 / `&type=HAMDT` 90 / `&type=SAMDT` 97; bare feed the full corpus (not 0/187 — the empty-`IN` guard holds); and the **bill-hub regression gate held exactly** — `119-hr-1` 13 lines, `119-hr-8800` 19 tallies / 18 splits — proving the leaf swap + `VoteLine` extraction didn't regress the hub. **Correction of the HO 529 reading (not a retro-edit of its block):** the "114 SAMDT" it recorded was procedural-inclusive; the genuine up-or-down count is **97** (oddities). **No theme-% change — a v1 cut on an existing pillar (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 all held their bar); the combined `Lobbying / amendments / nominations` bar holds at 92:** this ships the aggregate "voted" cut both 519 and 530/533 banked, discharging the `recordedVotes` gap's *aggregate* piece, but the **full per-member roll-call drill stays banked — this HO does NOT discharge it** (both chambers ship only the partisan shape; the who-voted-how list is the v2 expand) and a **procedural-outcome surface is newly banked** (needs a motion-aware model — invert tallies, distinguish cloture from adoption, suppress when an anchored vote exists). **Numbering — the 535 → 537 pointer jump skips no block:** HO 536 was the doc sweep *for* HO 535 (docs-only sweeps are owed no roadmap block — the HO 506 precedent), so a future reader should not hunt for a missing 536 block. Docs: HO 538. **Also notes now run through HO 537.**

**Also (HO 539–540), the `/vote/[id]` roll-call entity page + two residual cleanups — no theme-% change.** **HO 539 (residuals), briefly.** The `/amendments` blurb stopped asserting "near-evenly" (a hardcoded judgment on interpolated numbers — the HO 538 WATCH) and now states the contrast as **two percentages** — filings 96% Senate against recorded votes 52% Senate — guarded on `voted > 0`: self-updating prose with no adjective to rot, and no repeat of the readout's counts (which is why restoring the raw numbers wasn't the fix — it'd reopen the HO 509 double-print). Commit `588bed0`. Separately `df155de` wrote the **review-ref route** into SKILL and widened its scope beyond SKILL edits to **append-only roadmap blocks** (read-before-land text). **HO 540, the substance.** The banked per-member roll-call drill, built as a **page** not an expand-in-place: the fork was A (expand on `/bill/[id]`) vs B (a `/vote/[id]` entity page), B chosen. The argument is the HO 501 one — a roll call is an entity CBT already stores (`votes` at 1,513 rows with full positions in `member_votes`) whose data surfaced only as a fragment of other pages (a tally on the member hub, a split under an amendment). The corroborating find: **`getVotesByBill` existed, cached, with ZERO consumers** — the query for a bill's own passage votes had been written and never wired, the shape of a missing entity page, not a missing section. Three commits: **`41df8ed`** query layer (three NEW siblings — `getVoteById` reusing `VOTE_SELECT`/`rowToVote`, `getVoteMemberPositions`, `getVoteAmendmentContext` — never widening the shared SELECT), **`60d8c77`** the page + `VotePositionList` (flat sectioned, not `RecordTabs`), **`068dc7e`** inbound links (the tally → `/vote/[voteId]` on BOTH `/bill/[id]` and the `/amendments` feed via the shared `AmendmentVoteLine`; the member-hub roll number → `/vote/[id]`) + breadcrumb (`Legislation \ Votes`) + `pathToNavKey → "feed"`. **Step 0 (`vote-detail-cost-540.ts`):** House worst case is a **439-row list at 694ms cold** on the `member_votes` PK seek + the members PK join, so no index warranted; **4** votes carry a tally with zero positions; **890 of 1,513** votes carry `bill_id`, 623 don't, and **97** of those resolve via the `amendment_votes` link table. **Three decisions for the permanent record:** (1) **amendment context is fetched UNCONDITIONALLY, not as a null-`bill_id` fallback** — the Step-0 split showed a null-gate would show context on the 97 Senate amendment votes (no `bill_id`) and hide it on the 90 House ones (carry `bill_id`), the exact chamber asymmetry HO 537 spent its arc removing, reappearing in a different column (the HO 529 question-text asymmetry showing up again in `bill_id` presence). (2) **the party split is structural, not incidental** — `member_votes.bioguide_id` is deliberately not FK'd to `members`, so an unmatched row is expected; `getBillAmendmentVotes` INNER JOINs + skips unresolvable parties while the page LEFT JOINs, so the page applies the SAME skip-unless-D/R/I rule and the two agree regardless of join type (unmatched members still render — bioguide id, dim, unlinked, the `BillAmendments` sponsor idiom — because they did vote). (3) **two empty states, not one** — tally>0 with no positions is **lag** (4 votes, newest `house-119-2-269`, self-heals next votes sync); tally=0 with no positions is a **by-name/procedural** vote (`house-119-1-2`, Election of the Speaker) where nothing is pending and nothing ever arrives, and the zero tally is suppressed ("0–0" on a Speaker election is meaningless). **No theme-% change — the combined `Lobbying / amendments / nominations` bar holds at 92:** this discharges the per-member roll-call drill banked at HO 530/533, but it's a v1 entity surface (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 / 537 all held). **`/vote/[id]` is CROSS-CUTTING** — it serves bills, amendments, nominations AND the member hub — so it doesn't sit cleanly under any one bar; whether roll calls warrant their own line is for the next full re-derivation, not this sweep. Docs: HO 541. **Also notes now run through HO 540.**

**Also (HO 542), the `/bill/[id]` VOTES tab — the bill's own passage/procedural roll calls, no theme-% change.** The HO 541 sweep flagged `getVotesByBill` as **written, cached, and UNCONSUMED** — `/bill/[id]` rendered its *amendments'* votes (HO 530/532/533) but never the bill's *own* passage / motion-to-recommit / cloture / motion-to-commit roll calls, because the query for them had been written and never wired (the same orphan HO 540 read as the shape of a missing entity page). HO 542 wires it into the HO 510 `RecordTabs` box as a VOTES tab, each row → `/vote/[id]`. **STEP 0 (`bill-votes-overlap-542.ts`, `630833c`):** the one real unknown was overlap with the Amendments tab — `getVotesByBill` returns *every* vote carrying the bill's `bill_id`, and House amendment votes carry `bill_id` (HO 532), so some of its rows are already shown under Amendments. Read-only measurement: **890** bill_id-carrying votes across **467** bills; **92** are amendment votes (**all 92 House-linked via `amendment_votes`; 0 Senate-pattern**) → **798 own votes across all 467 bills** (p50 **1** · p90 **2** · max **24**), overlap concentrated exactly on amendment-heavy bills (`119-hr-8800`: 19 of 21; `119-hr-1`: 0 of 24). **The finding that shaped the build:** the exclusion set is **House-link only** — Senate amendment votes are question-matched and **don't carry `bill_id`** (the HO 540 bill_id-asymmetry, the HO 529 question-text asymmetry once more), so they never enter `getVotesByBill` and the belt-and-suspenders Senate-pattern check the handoff pre-authorized is a provable no-op (0 leak). So the exclusion is a plain `Set<string>` over the already-fetched `amendmentVotes` map values — no `lib/amendment-vote-key.ts` import in the page (oddities). **The build (`61cacd2`):** `getVotesByBill(bill.id)` added to the page `Promise.all`; `ownVotes = billVotes.filter(v => !amendmentVoteIds.has(v.id))`; a NEW `components/BillVoteRow.tsx` (server summary row — tally · date · question+result · `#{rollCall}` → `/vote/[id]`; **NOT** `MemberVoteRow` [member-scoped chip] nor `VotePositionList` [the heavy per-member breakdown] — the bill hub has no per-member context; the "new sibling, don't widen" discipline), with the **HO 540 tally-suppression** on an all-zero roll call (a by-name/procedural vote where "0–0" is meaningless). The tab inserted **between amendments and lobbying** (passage = the culminating floor action on the whole bill; amendments act on parts, lobbying is orthogonal-last), **omit-at-zero** like every other bill-page tab, `initialKey` untouched (own-vote counts are small, so Votes never steals the initial tab on a heavy bill; a passage-only bill opens on Votes). CSS **additive under `.bvote-*`** — no `.vote-row`/`.rec-*`/`.mc-*`/`.bxp-*`/`.bdp-*` edit; one deliberate deviation from `MemberVoteRow` — the roll# stays visible on mobile because here it's the *sole* link to the vote page. **Discharges the backlog QUEUED "bill-hub passage-votes section" (HO 418)** — see DONE. **No theme-% change — a v1 cut wiring an existing orphaned query onto an existing pillar (the LDA/amendments precedent where 437 / 439–440 / 442 / 444 / 448 / 450 / 461 / 537 / 540 all held); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Prod-verified on the cache-hit path:** `119-hr-8800` Votes **2** (passage + motion to recommit) with Amendments still **19** (21 unique `/vote/` links = 2 own + 19 amendment — no double-count, Amendments byte-identical); `119-hr-5371` Votes **21**; `119-hr-3034` (introduced-never-voted) **no VOTES tab**. **Owed eyeball (WATCH):** the tally-suppression (`bvote-none`) path is tsc-verified but **unexercised** — none of the three samples had a by-name own-vote — render-confirm on the next one (the HO 540 empty-state WATCH posture). Docs: HO 542 (this sweep — backlog DONE + WATCH, oddities, the SKILL `getVotesByBill` reconciliation). **Also notes now run through HO 542.**

**Also (HO 543–544), the `/lobbying` corpus fbar's sort segment (RECENT ⇄ VOLUME) + bill-linked toggle — the first of the HO 486 fbar cuts, no theme-% change.** The HO 486 chrome restructure deferred the fbar's real filters (each needs a query, not markup); HO 544 ships two of the three (sort + bill-linked), gated by a Step-0 cost/shape probe. **HO 543 (`aabc8a8`, read-only probe).** Priced the sort × filter over the corpus feed and read GO — but on the **cheapest component**, which is exactly the trap the build then walked into (oddities). **HO 544 (`e640a0d`).** `getRecentFilings` gains `sort` (`"recent"` default / `"volume"` = per-filing activity count, "meatiest filings") × `billLinked`. **The default path (recent + unfiltered) is the byte-identical pre-544 SQL** — same `idx_lda_filings_dt_posted` ordered walk, so the untouched common case can't regress. **The mid-build catch (the load-bearing find):** the 543 probe timed VOLUME as a bare `GROUP BY filing_uuid` over `lda_activities` (index-only, **340–440ms**) — but the shipped feed has to join back to `lda_filings` to hydrate the row, and the naive shape (LEFT JOIN from `lda_filings`, order by the joined count) planned an **AUTOMATIC COVERING INDEX on the materialized agg → ~18s cold** (a build-time EXPLAIN caught it). The fix **drives from the agg (INNER JOIN)**, PK-seeking into `lda_filings` — the ~295 zero-activity filings the INNER JOIN drops have `activity_count 0`, sort dead-last past the 40-page clamp, so the visible feed is identical to a LEFT JOIN (a `do NOT "fix" this to a LEFT JOIN` comment guards it). The `dt_posted` hint is **dropped** on VOLUME (order is by count, not date) and **kept** on recent+linked. **The required addition on review — the HO 440/448 guard:** the read + hydrate are wrapped **try/catch → `{items:[], page, pageSize}`**. VOLUME's ~7s worst cold miss sits close to the 10s `DB_REQUEST_TIMEOUT_MS` wall (the 543 probe saw remote Turso vary up to ~5×), and this is the ONE live LDA read on `/lobbying` — an abort would 500 the WHOLE page (rollup/rail/crosswalk/firms), not just the feed, so it degrades to an empty feed instead (the same guard every LDA surface has carried since 440; commented so it isn't stripped as defensive noise). Controls are **unscoped-feed-only** (carried across pagination + expand) and **hidden when scoped** (`?issue=` serves the precomputed `drill.recent` sample — the HO 437 >25s per-issue wall — which can't be re-sorted live). **Prod-verified on the cache-hit path (2× per URL):** all 4 combos 200 (VOLUME cold **7.08s** → warm 1.83s — right at the WATCH figure; volume+linked 2.84s → 0.36s); unscoped shows the VOLUME control, `?issue=TAX` hides it; `?page=999` clamps the feed byte-identical to page 40 (only the echoed `?page=` in the expand `toggleHref` differs). **No theme-% change — a v1 filter cut on the existing LDA pillar (437/439–440/442/444/486 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Two records for the ledger (docs, HO 544 this sweep):** the **oddities lesson** — *a probe that measures an aggregate in isolation has not measured the feed query that joins back to hydrate rows; cost-probe the shape you'll actually ship, joins included, not its cheapest component* (a sibling of the HO 340/437 "clean EXPLAIN says nothing about cold latency" entry — cross-referenced); and the **WATCH** — VOLUME cold-miss headroom (~7s vs the 10s wall, corpus grew 108,707 → 112,411 this arc, sort×linked took the cache-key surface ~40 → ~160 keys so cold misses hit ~4× more often), **promote to a fix if it ever aborts** (the fix is a precomputed `activity_count` column on `lda_filings` — the 543 probe's noted optional upgrade, turning VOLUME into a ~45ms indexed walk, and the natural home if search wants the same). **The third fbar cut — registrant/client SEARCH — stays QUEUED as its own FTS follow-on** (a `LIKE` over 112k rows full-scans; wants an `lda_filings` FTS5 index, the `bills_fts` shape). Docs: HO 544 (this sweep — SKILL `getRecentFilings` + `/lobbying` fbar, oddities cost-probe lesson, backlog VOLUME WATCH + the fbar-cut QUEUED update). **Also notes now run through HO 544.**

**Also (HO 545), the `/lobbying` expand-panel description line-clamp + SHOW MORE — clears the banked QUEUED line-clamp item, no theme-% change.** The LD-2 free text rode a fixed `max-height:140px` scroll box (buries the long tail: p99≈1.8k / max 24.4k chars, HO 485). HO 545 (`54d2fd3`) clamps it to 4 lines closed with an explicit **SHOW MORE / SHOW LESS** reveal; the open state is a **bounded 420px scroll** so even the 24k monster can't stretch the HO 492/493-tuned page. The toggle rides a dedicated `<button>` in a small client island (`FilingDescription`, the `FillerWatchStrip` HO 348 precedent — the SOLE client boundary out of the server `FilingExpandPanel`), **NOT** `<details>/<summary>`: these are descriptions researchers copy, and select-to-copy toggling the panel is an active regression. CSS additive under `.lob-exp-desc*` only — no `.lob-*`/`.mc-*` layout, `PAGE_SIZE`, rail-bound, or `.lob-sec-scroll` retune (the three HO 492/493 numbers are tuned against each other and stayed put). **The verification find (the load-bearing one, oddities):** the handoff flagged the risk that `white-space: pre-wrap` might *defeat* the clamp — it did NOT (CDP: closed box `clientH:88` = exactly 4 lines, `scrollH:6280`, clipped; the clamp engages with pre-wrap). The REAL risk was the **inverse** — pre-wrap makes the 4-line clamp count *hard newlines* as lines, and CDP-measuring the column at **56 chars/line → 4 lines ≈ 224 chars** proved the handoff's ~280-char threshold + bare-clamped-below-threshold would **silently clip** a below-threshold description (a 224–280-char text, or one with a mid-text break, wrapping past 4 lines with no reveal control). Fix: **below threshold renders UNCLAMPED (the `--open` state), not the bare clamped base** — clip-proof regardless of column width or break placement; short text never reaches the 420px bound, so it renders identically to a short filing today. The char threshold (now 250) is thereby demoted to a purely cosmetic "when to OFFER collapse" line, not a correctness dependency (a hard-newline count >4 also triggers the affordance). **Prod-verified on the cache-hit path (2× per URL, matching the local CDP exactly):** the 31-activity / 21,633-char monster (`cd77f510…`, on volume page 1) renders **27 clamped-with-button + 4 short `--open` boxes** — both branches on one filing; SHOW MORE → a bounded 418px scroll (page grew **+330px, not ~6000**), SHOW LESS restores; per-activity toggles are independent; short descriptions show fully with no button and no clip. The collapsed default `/lobbying` page (no `?expanded=`) is untouched — the CSS lives only inside the expand panel. **No theme-% change — UI polish on the existing LDA pillar (the 437/440/442/444/486/544 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** Docs: HO 545 (this sweep — roadmap block + backlog QUEUED→DONE + the pre-wrap/hard-newline clamp oddity). **Also notes now run through HO 545.**

**Also (HO 546–547), the `/lobbying` registrant/client search — the LAST HO 486 fbar cut, FTS probed + rejected, no theme-% change.** The final open fbar control. **HO 546 (`debb9dd`, read-only probe) retired the backlog's premise.** The close criterion assumed search needed an `lda_filings` FTS5 mirror of `bills_fts` — the probe found that **wrong on measurement** (the "a backlog line's stated mechanism is a claim — probe it" rule, HO 512/529 class): LDA names avg **25 chars**, so a `%term%` substring scan of 112k rows is **~700ms** (the `bills_fts` premise was scanning long `title`+`summary`, which doesn't transfer to short names). NULL-id/name share is **0%**, distinct registrants 5,183 / clients 27,115 → FTS would **22×-over-index** the repeated names AND add a delete+reinsert trigger to the `FLUSH_AT=100`-tuned sync write path — **all cost, zero benefit. FTS REJECTED; the fallback if `%term%` ever climbs is a distinct-name lookup table, NOT FTS** (recorded so it isn't re-proposed). **HO 547 (`5c73643`) ships Fork B leanest — a live `LIKE`, no new DB object at all.** Two amendments to the probe's rec, both load-bearing: **(1) substring `%term%`, not prefix** — "Boeing" must match "The Boeing Company" (client names lead with articles, firms with surnames); prefix would read as broken. **(2) a REQUIRED pre-build VOLUME×search measurement** — the probe only priced RECENT; VOLUME is the agg-driven join carrying the HO 544 ~7s WATCH. Measured cold: **VOLUME+search 7,792ms** (a TEMP B-TREE over the whole materialized agg — the LIKE defeats the LIMIT short-circuit, and search is *usually served cold* since `q` makes cache keys unbounded) vs **RECENT+search 34–697ms** (a plain `SCAN` under the forced `idx_lda_filings_dt_posted`, no MULTI-INDEX-OR — the OR-cold-stall class HOs 405/407/479/480 did NOT bite because the name columns are unindexed). So **search FORCES RECENT** — `getRecentFilings` overrides `sort→recent` when `q` is set, and the UI disables the VOLUME segment (a page-local disabled variant; the shared `SegmentedToggle` is not widened). **Build:** `getRecentFilings` gains `q?` (substring, % / _ escaped via `ESCAPE '\'`); the **default path (recent, unfiltered, no q) stays byte-identical**; a searched set has no blob total, so the pager rides a **live `COUNT(*)`** with the same predicate (≤67ms), run in **parallel (`Promise.all`) inside the HO 440/448 guard**; `MAX_FEED_PAGES=40` clamp kept; the fbar carries a `form` GET (`?q=`, server-driven — matches the page's `<Link>` idiom) beside the HO 544 controls, carried across pagination + expand, hidden when scoped, count line shows the term, zero matches → `No filings match "term"`. CSS additive under `.lob-fbar-*`. **Prod cache-hit verified (2×):** default byte-identical; `boeing`→results **via substring**; zero-match + literal `%`/`_`→`No filings match` (**escaped**, matched nothing not everything); search+linked composes; search+`sort=volume`→forced-recent (VOLUME disabled); `?issue=`→box hidden + q ignored; page-999→clamped — all sub-second. **No theme-% change — a v1 filter cut on the existing LDA pillar (437/440/442/444/486/544 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** This **fully closes the HO 486 fbar-cuts item** (sort + bill-linked + search all shipped). Docs: HO 547 (this sweep — roadmap block + backlog fbar-entry struck to DONE + FTS-rejected recorded + the search-forces-RECENT / short-names-≠-bills-LIKE oddity + SKILL `getRecentFilings`/`/lobbying`). **Also notes now run through HO 547.**

**Also (HO 548), harness coverage extension — a cache-hit detector + interaction coverage for five post-472 arcs, no theme-% change.** Test-only, two commits (`88f669f` smoke · `b38f530` fit-finish); findings-only, zero fixes — the HO 472 posture. **Two gaps closed.** **(1) The crawl never exercised a cache HIT.** `smoke.spec.ts` did one `goto` per route and `e2e-prod.yml` triggers on `deployment_status`, so every unattended prod crawl ran inside the post-deploy cache-*miss* window — structurally unable to see the HO 533 failure class (a cached `unstable_cache` value that round-trips wrong — a `Map`→`{}` — and 500s only on the hit). Each route is now hit **twice** (fresh `goto`, not `reload`); collectors are marked after hit 1 and the second hit asserted on the slice past the mark (attribution), one log line carrying both (`hit1=200 … | hit2=…`); hit-2 document status is asserted **hard**. `/vote/[id]` (HO 540, previously in NO `ROUTES` list) added. **(2) Five interactive arcs had no coverage since HO 472.** `fit-finish.spec.ts` gained six areas with provenance: **RecordTabs** (509/510 — tablist, mount-all-panels-carry-`hidden`, scroll-reset, open-row-survives-swap, and the opposite empty-state policy the two consumers deliberately disagree on: member **dims** `data-empty`, bill **omits**), **`/vote/[id]`** (540 — reached via the bill VOTES-tab link *and* cold direct-nav), **the no-double-count invariant** (542 — every `/vote/` link unique page-wide; never-voted bill omits the VOTES tab), **`/lobbying` fbar** (544/547 — sort/linked rebase+compose, search-forces-RECENT with VOLUME inert, clear, scoped-hides), **FilingDescription** (545 — SHOW MORE toggle, the 24k-char monster's page growth bounded <600px, per-activity independence), **`/news` rails** (515/517 — single selection spanning the topic + member groups). **Five seeds, picked from prod Turso** (query per seed in the ship report): `VOTE`=`senate-119-2-207` (Senate roll call w/ positions + a bill link — nomination votes rejected since the `/bill/` back-link needs `vote.billId`), `VOTE_BILL`=`119-hr-8800` (2 own + 19 amendment = 21 unique `/vote/`), `NOVOTE_BILL`=`119-hr-3034`, `TABS_MEMBER`=`A000055` (amendments=0 + trades=0 → dim tabs), `FILING_UUID`=`cd77f510…` (31 activities, 27 clamped + 4 short). **STEP 0 falsification (HO 549, nothing committed) — the detector was proven able to fail before any doc recorded it as protection:** re-injecting the exact HO 533 defect (hand-applied on HEAD — `getBillAmendmentVotes` returns a `Map`, `BillAmendments` reads `.get`) and running the smoke crawl against a local `next start` produced **`[bill-detail] hit1=200 … | hit2=500 … bad=1 console=1 pageErr=1`** on `119-hr-8800` — the detector fired; the scratch edits were reverted, tree clean. That is what makes 548's clean sweep an instrument reading rather than a structural green. **Findings: NONE — stated as a real result, not an absence:** all 36 routes swept `hit1===hit2===200` (zero divergence), and every one of the six interaction invariants held. **One design detail worth keeping:** the bill-hub scroll-reset is **guarded-and-logged, not skipped** — its highest-count panel fits the 430px body so there's no scroll to reset (the member hub, whose Bills panel overflows, exercises it). An earlier `expect.soft(scrolled>0)` correctly surfaced the vacuous case but reds a *committed* test permanently, the wrong shape; the guard-plus-`console.log` keeps the non-exercise visible on stdout (not a silent HO-503 skip) while staying green. **No theme-% change — harness work on already-counted surfaces; no bar moves.** Docs: HO 549 (STEP 0 + this sweep — roadmap block + backlog fbar-line-struck + 548 tombstone + doubled-crawl WATCH + the extended cache-hit oddity + SKILL double-hit convention). **Also notes now run through HO 548.**

**Also (HO 550–552), the House amendment-vote mislink fix (P1) + the motion-aware-surface GO probe — a correctness fix + a probe on an existing pillar, no theme-% change.** **HO 550 (`9383b9f`, read-only probe)** priced the banked HO 537 M8 surface (the 64 Senate roll calls that touch an amendment without being its up-or-down vote) across six measurements — and its **M5, run first, found a P1 live on prod**: `lib/amendment-votes-walk.ts` (House, HO 532) links **every** `recordedVote` off a HAMDT's `/actions` with no question filter (the mirror of the Senate materializer's anchored `SENATE_AMDT_Q`), so two amendments — `119-hamdt-40`→`house-119-1-187`, `119-hamdt-175`→`house-119-2-122` — were rendering an **"On Ordering the Previous Question"** procedural tally through the amendment-framed `VoteLine` as the amendment's own outcome, green agreed dot and all from `deriveDisposition("Passed")`. Ordering the previous question ends debate; it decides *whether* the House votes, not what — wrong on the number and wrong on the outcome. Per the handoff, the fix outranked the surface. **HO 551 (`c03f1b7`) fixed it — predicate derived from the population, not the two exceptions.** STEP 0 enumerated **all 92 House-linked `amendment_votes` questions**: 90 decisive (**89** "On Agreeing to the Amendment" + **1** ", as Modified") + **2** procedural — vocabulary CLOSED, so the gate is `HOUSE_AMDT_QUESTION_LIKE = "On Agreeing to the Amendment%"` (the `%` covers the modified/amended suffixes), added to `lib/amendment-vote-key.ts` **next to the Senate one** (the leaf module exists precisely so the two chambers can't drift — and the asymmetry survived *because* the House had no counterpart there). The walk constructs `vote_id` from the payload (no question text in hand), so the filter rides a **JOIN to `votes` at link time** in the INSERT's LIKE — one SQL authority, no code-side copy. The two live bad rows were deleted one-time, **scoped to the enumerated procedural set** (exactly 2, dry-run confirmed); both offenders keep their real "On Agreeing to the Amendment" vote, and the `119-hr-8800` 19-amendment control stayed byte-identical (the over-filter check). **HO 552 (`20770e3`) closed a self-inflicted edge the 551 gate opened.** The gate collapses "votes row not synced yet" and "synced, wrong question" to the same no-link, but they need OPPOSITE handling: a wrong-question miss is *settled*, an ABSENT votes row is *pending* — and 551's own header comment falsely claimed the absent case "self-heals on the update_date re-walk," which the queue predicate (`update_date > amendment_vote_walked_at`) **cannot deliver** (a just-voted HAMDT already spent its `update_date` bump getting queued this run, so stamping it drops the decisive link **permanently**). Fix: an existence `SELECT id FROM votes WHERE id IN (…)` before the link loop; if any constructed `voteId` is absent the HAMDT is **left UNSTAMPED to retry** (`deferred++`, `continue`) — the same non-stamp discipline `fetchErrors` already used — and the comment was corrected. Existence-check chosen over the handoff-suggested `SELECT id+question` deliberately, to keep decisiveness the single SQL LIKE (no second predicate copy). **STEP 0 falsification (HO 553, nothing committed):** the `deferred` branch had never executed (both 552 verify runs saw `deferred=0`), so — the HO 549 discipline — it was proven able to fire before any doc recorded it as protection. A scratch perturbation of the constructed `voteId` produced `deferred=1`, HAMDT unstamped, `remaining=1` **pinned across 3 runs** (~175ms / one `/actions` GET each — the permanent-gap signature); restoring the code, the **retry completed** (`walked=1`, stamped, decisive `house-119-1-188` link restored). Scratch reverted, tree clean, nothing committed. **Call: permanent retry is acceptable, no bound** — ~228 HAMDTs, one GET per stuck row per run is negligible, and the only alternative (stamp after N attempts) reintroduces the exact permanent-drop this fix removed; the pinned `deferred` counter makes a stuck row visible instead of silent. **The probe's motion-aware-surface verdict is GO (banked, not built here):** vocabulary CLOSED at 4 classes (table 9 / cloture 4 / chair 1 / waiver 50, **tail 0**), **100% number resolution** (64/64; the waiver form's `Amdt. No. N` confirmed the SAME Senate `S.Amdt` namespace by 42/43 sponsor-name match, not a parallel numbering), **~55 fate-bearing lines** after setting aside the 4 already-anchored (M3: **60** would gain a line they don't have today) and 5 orthogonal cloture/chair, **polarity validated 54/56 (96%)** — and against **FULL coverage** (64/64 residual amendments carry `latest_action_text`, because a procedural floor vote always generates an action, NOT the corpus-wide ~8.6% that would make a high rate on tiny N meaningless), the 2 misses both "Motion to Table Failed" edge cases (`samdt-3946/3947`, "fell when SA … fell"). Cold ~162ms (351ms at probe time; remote Turso varies). **One unreconciled arithmetic smell recorded for the build handoff, not papered over:** M4's exclusion math is `64 − 5 orthogonal − 2 withdrawn = 57` expected clean pairs but N came back **56** — an off-by-one that doesn't move the 96% verdict but a build shouldn't inherit it silently. The surface stays a separate scoped build (invert table/waiver polarity, render cloture/chair as orthogonal not a fate, suppress the 4 already-anchored). **No theme-% change — a correctness fix plus a probe on the existing amendments pillar (the 437/439–440/442/444/448/450/461/537/540/542 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** Docs: HO 553 (this sweep — roadmap block + backlog P1 tombstone + motion-aware BANKED→QUEUED + the `deferred` transient-vs-pinned WATCH + oddities the stamp-on-exit-converts-transient-to-permanent rule + the HO 542 chamber-asymmetry M5 extension + SKILL the walk gate / `deferred` / non-stamp discipline). **Also notes now run through HO 552.**

**Also (HO 554–555), the motion-model STEP 0 + the `deriveDisposition` kill-vocabulary widening — a probe + a correctness fix on the amendments pillar, no theme-% change.** The motion-aware surface (GO'd HO 550, QUEUED) got its STEP 0 — and the probe's biggest find wasn't about the surface: it falsified the framing that the disposition dot was already correct on the procedural residual, spawning HO 555 as a prerequisite fix that reshapes what the build still owes. **HO 554 (`be0fc4d`, read-only probe), four measurements.** M1 resolved the HO 550 off-by-one the 553 block said the build shouldn't inherit silently: the 57-expected-vs-56-measured clean-pair gap is `119-samdt-3849`, whose actual-side text "rendered moot" sits outside the probe's actualFate validation vocabulary — a **validation-coverage gap, not a render gap** (polarity holds; the row simply couldn't be scored). M2: motion-vs-actual DISAGREE=0 (no polarity halt) — but **DOT-OTHER=53/64**: on 83% of residual rows the motion implies a clean fate the muted prod `other` dot was missing, falsifying the §0 "the dot is already correct, the surface adds the vote line" framing. M3: motions-per-amendment MAX=1 — no multi-motion render ambiguity. M4: the residual select rides `idx_votes_chamber_date` (31ms/64 rows; the split fetch 253ms/297 rows) — trivially live-servable. **HO 555 (probe `c180054` + fix `2ce1d6c`), the widening — measured, not guessed.** The probe clustered the FULL `other` bucket (115/625 acted rows, top-30 normalized shapes hand-tagged) rather than patching the 3 examples M2 surfaced: the candidate rescues **80** — "ruled out of order by the chair" (47; the point-of-order kill after a failed budget waiver; anchored to the full phrase, not bare "out of order") + word-boundaried "fell" (fell with its parent / when cloture was invoked / when tabled). **Deliberately NOT added, still `other`:** withdrawn (the amendment survived the vote and was pulled later — genuinely neither), rendered moot (describes the cloture motion's fate, not the amendment's — the M1 row), cloture-not-invoked (orthogonal procedure); "stricken" is not a kill token. M2 measured **zero collision** with the 4-string `votes.result` vocabulary, so **one shared function holds** across both input families — no split. **The zero-flip gate HALTed on exactly one row, and the flip was accepted as a correction:** `119-samdt-3963` moves agreed→failed — the OLD dot was WRONG there (its text is "SA 3963 fell when …" with a trailing "agreed to" belonging to the subordinate clause, which the old classifier read), so the failed-family-FIRST ordering is now load-bearing for the subordinate-clause case too, not just "not agreed to" (oddities). **Corpus dots move: agreed 337→336 · failed 173→254 · otherActed 115→35** (corpus other 6465→6385); **the voted block is untouched (0 flips)** — the `?disposition=` coarse filter and the voted EXISTS bypass `deriveDisposition` entirely (M4 grep), so the fix moves dots + summary buckets only, never filter semantics. An **M5 watch instrument** was added to the probe post-fix — agreed-classified rows containing `when` — and reads **0**: SA 3963 was alone, no residual mis-read family (→ WATCH). **The verification produced its own lesson (oddities — the HO 533 rule sharpened):** the Vercel Data Cache survives deploys (HO 312), so a shipped VALUE change does not land on the first prod read — the first hit serves the STALE pre-deploy value and only triggers the SWR regen. "Hit 2–3 times" is a serialization-shape test; for a value change the rule is **confirm the served SHA first (`/api/version`), then hit until the value CONVERGES to the expected number and holds** — at 555 the lighter per-bill dot converged before the heavier `/amendments` summary. Local cache-hit double-hit first (hero 336/254/35 + 42/145; ruled-out-of-order rows red on `/bill` and `/members`; control `119-hr-8800` unchanged), then prod convergence. **The motion-aware build's value proposition is RESHAPED, not retired (QUEUED updated):** the dot now already carries the fate on the rescued rows, so what the build still owes is the **motion-framed vote line** (the tally + inverted polarity + verb — a tabling-agreed tally rendered as the kill it is), cloture/chair rendered as ORTHOGONAL not a fate, and suppression of the 4 already-anchored — the vote-line rendering was always the surface; the dot-correction half is done. **No theme-% change — a probe + a correctness fix on the existing amendments pillar (the 550–552 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** **Numbering — the 552 → 554 pointer jump skips no block:** HO 553 was the doc sweep for 550–552 (docs-only sweeps are owed no roadmap block — the HO 506/536 precedent). Docs: HO 556 (this sweep — roadmap block + backlog QUEUED-reshape/DONE/WATCH + oddities ×2 + SKILL verification-convention amendment + `deriveDisposition` vocabulary note). **Also notes now run through HO 555.**

**Also (HO 557), the motion-aware Senate amendment-outcome surface — the HO 537 M8 banked surface, shipped with its model corrected mid-build; no theme-% change.** The 64 Senate roll calls that touch an amendment without being its up-or-down vote (Motion to Table · Cloture · Decision of the Chair · budget waiver) now render a **motion line** — the motion, its tally, the party split, a drill link to `/vote/[id]`, and where the motion decides, the amendment's fate. Live at `bf820bd`; **60 amendments gain a line they didn't have** — 55 of them carrying a "→ amendment killed" clause, the rest tally-only (undecided or orthogonal), with the 4 anchored suppressed. **Anchored-among-killed is 0, and structurally so:** an amendment killed by a tabling motion or a failed waiver dies at that moment and never gets its own up-or-down vote, so every anchored amendment is one the motion left pending or only touched orthogonally. Suppression therefore never hides a fate clause — it only ever drops a tally-only line. **The arc's real content is that the STEP 0 gate fired and the model was wrong.** HO 554's M2 tripwire (dot vs implied fate, DISAGREE=0) had been measured against the **pre-555** classifier, and the probe pins a verbatim copy of `deriveDisposition` — so HO 557's first act (`7798997`, discharging the HO 556 WATCH on its first opportunity) was reconciling that copy to the shipped `2ce1d6c` version and re-running. **DISAGREE went 0→2**, both rows the exact pair HO 550's polarity validation had missed (`119-samdt-3946`/`3947`, "Motion to Table Failed" → "SA N fell when SA N-1 fell"): the tabling motion failed, so the model said *survived*; the amendment later fell with its parent, so the HO 555-widened dot said *killed*. **Both readings were individually true — the model was answering a different question than the dot.** `impliedFate` described the amendment's status **at the motion**; the dot describes its **outcome**. Those coincide in one direction only, and the arc had been reading a label coincidence ("survived" ≡ "agreed") as agreement. **The fix narrowed the model rather than patching the two rows** (`d213cb7`): `impliedFate` → **`motionFate(cls, outcome): "killed" | "undecided" | "orthogonal"`** — there is **no positive fate**. Tabling **agreed** → killed; waiver **failed** → killed; the opposite outcome of each is **undecided** (not tabled ≠ adopted; cleared the point of order ≠ agreed to), and cloture/chair are orthogonal always. Both fate-bearing classes are **kill mechanisms that decide in one direction only**. The rejected alternative is worth recording: suppressing the fate clause whenever it disagreed with the dot would have coupled the motion model to `deriveDisposition`'s vocabulary — so a future dot widening would silently change motion-line behavior — and would have hidden the modelling error instead of fixing it. **The re-gate is the contradiction shape that remains possible:** HALT if any row has `dot=agreed AND fate=killed`; measured **0** (`killed=55 · undecided=4 · orthogonal=5 · dot-agreed-in-killed=0`), and undecided/orthogonal rows assert nothing so they can't contradict by construction. **The build, three commits.** `568d58d` — a new leaf **`lib/amendment-motion-key.ts`** beside `amendment-vote-key.ts` (untouched), carrying the four anchored classifiers verbatim from the probe (table 9 / cloture 4 / chair 1 / waiver 50, **tail 0**, `null` for the tail so it's droppable by type), the number parse (waiver's `Amdt. No. N` = the same Senate `S.Amdt` namespace, HO 550's 42/43 sponsor match), and `motionFate`. **The rename is a type-safety device, not cosmetics:** `"agreed" | "failed"` is structurally assignable to `AmendmentVote["disposition"]`, so tsc would have allowed an implied fate into `dispositionColor`/`voteVerb` and printed "Agreed" on an amendment that merely wasn't tabled. `7027d10` — **`getSenateAmendmentMotions()`**, a corpus-wide **argument-less** cached sibling (64 rows total, so one cache key serves both consumers), two statements only, both HO 554 M4-measured: the residual select on `idx_votes_chamber_date` (**31ms/64**, no new index, no hint) and the `member_votes ⋈ members` split (**253ms/297**). It drops the probe's 64 per-row existence lookups (an unresolvable key is simply never read) and keys on **`votes.congress` per row**, not a `MAX(congress)` — multi-congress safe by construction. Distinct `AmendmentMotion` type, plain `Record` (HO 533), `try/catch → {}`, tags `["amendments","votes"]` (the `amendments` tag is consumer-coherence, not a FROM clause — noted in comment). `b40b462` — **`components/AmendmentMotionLine.tsx`**, a new sibling of `AmendmentVoteLine` (not a widened `VoteLine`, whose verbs are amendment-framed and wrong here), reusing the shared `partyColor` and the **HO 533 all-zero split suppression**; wired into `/bill/[id]` (the inner row in `BillAmendments.tsx` — *not* `AmendmentRow.tsx`, the HO 530 pathspec note) and the `/amendments` feed **page-scoped in `app/amendments/page.tsx`**, so `getAmendments` is untouched. **Suppression costs nothing and makes the two lines mutually exclusive:** the motion line renders only when the amendment has no anchored decisive vote, a check both sites already hold in hand — so `VoteLine` and `MotionLine` can never stack, and there is no ordering question. **No `+N earlier` counter** — M3 measured 1:1, so it would be structurally unreachable code documented as protection (the HO 549 discipline); the render maps the list instead, so a future second motion appears as a second line rather than hiding. **The dot never moves** — HO 555 already fixed it for these rows, and this HO adds evidence, not classification. **One measured over-claim, shipped deliberately (oddities + WATCH):** `motionFate(waiver, failed) = killed`, but M6 (`70a23ae`, added mid-build on the ruling) split the class **48 "ruled out of order" / 1 "withdrawn" (`119-samdt-5806`) / 0 neither** — so 5806 renders "→ amendment killed" against an action text saying it was *withdrawn*, contradicting HO 555's deliberate decision that withdrawn is neither. A 1-row over-claim against 48 correct ones doesn't justify narrowing, and the obvious fix (a token check on action text) is the dot-coupling the gate already rejected. **Numbering — the 555 → 557 pointer jump skips no block:** HO 556 was the doc sweep for 554–555. **No theme-% change — a v1 line on the existing amendments pillar (the 437/439–440/442/444/448/450/461/537/540/542/550–552 precedent); the combined `Lobbying / amendments / nominations` bar holds at 92.** This **discharges the HO 537 M8 / HO 550 QUEUED surface in full**. Docs: HO 558. **Also notes now run through HO 557.**

**Also (HO 559–561), the primaries arc — a banked pair closed on measurement (neither built) + the SC special-primary ingestion path; no theme-% change.** *Nothing here was built to the spec that opened it:* the banked display item targeted code HO 333 had already orphaned, its re-poll twin described a mechanism that already existed, and the real hazard — a scrape silently substituting one contest's results for another's — was found by reading the sync path, not by either measurement. **HO 559 (`6db8fd0`, read-only probe) took the two-year banked pair and closed both without a build.** *Display half — moot, not built:* `buildPrimariesCartogram`, `PrimaryDistrictCard`, `PrimaryRow`, `primariesFill`, `PrimariesLegend` are all ORPHAN; `PrimaryMapCard` and the `variant === "primaries"` branches live only inside `CartogramShell`'s primaries fork, which never executes because the sole `<CartogramShell>` (`ElectoralBoard.tsx:103`) passes `variant="races"`. The item's build target was unreachable code, and `PrimaryTimeline` (`:122`, `c.date <= todayISO`) already ships the date-driven model the item asked for. M3 confirmed **no user-visible wrongness survived HO 333**: `/race/[id]` renders `getRunoffsForRace` (runoff-round only), the member chip is date-only. *Re-poll half — already satisfied:* the cursor wraps forever and each unit delete-rebuilds, so "never re-polled" was false — M5(a) measured all **431** resulted contests rewritten after their date (1–7d=5 · 8–30d=129 · 31–90d=147 · 90+d=150, none ≤0d). **The measured cycle is 44.6 days** (471 units, mean 10.56 advanced/tick, 14/45 budget-stops), which HO 560 C3 halved rather than answering with a priority queue. **The population number was decomposing two different bugs.** Past-by-date 482→**612**, voted-by-results 407→**431**, gap 75→**181** — but **143** of that is zero-candidate (roster never populated) and only **38** is the item's actual target, has-roster-no-shares, none older than 90 days; the 143 is now its own backlog entry. **HO 560 — the hazard the probe didn't look for.** `syncSenateCandidates` delete-rebuilds `senate-{ST}-2026-{D,R,open}` without reading which contest the scraped votebox describes, and `syncCalendar` writes only 270toWin's state-uniform date — so Ballotpedia rebuilding a seat's page around a later contest would have produced **a row dated June 9 carrying the August field, June's results gone, no error anywhere**. Three commits: `87a4366` seeds `senate-SC-2026-special-{D,R}` (Aug 11, runoff Aug 25, `race_id=S-SC-2026`, **`election_round='primary'`** so it flows to `getPrimaryCalendar` → `PrimaryTimeline` as a new Aug 11 tick rather than needing a third round vocabulary every consumer would learn); `aa9093a` freezes **settled** rows (past-dated AND already carrying a share) against the rewrite — narrow on purpose so past-dated-but-shareless rows stay open (the exact mid-count population the re-poll item existed for), with `settledSkipped` surfaced and `reingest-primary-slate.ts` as the correction escape hatch; `65d0692` takes the cron to twice daily. **Freeze blast radius: 431 rows** (senate 42 / house 389). *Amendment before land:* the seed loader's empty-roster path was fixed to leave a roster **untouched** rather than clobber it (oddities). **HO 561 — the design that couldn't fire, and what replaced it.** The obvious route keyed off which URL served, since `scrapeSenateCandidates` already falls back to `senateSpecialPageUrl` and the success path preserves the winner on `result.url` — **but it never fires for SC**: the standard SC page returns 200 with the June regular, so the fallback is never reached, and S2 then found **0 of 35** standard pages missing (including FL and OH, whose specials that fallback was written for). So the special page is fetched **in addition**, gated on a seeded `-special-` row existing for that state, which closes the FL/OH regression surface by construction rather than by care. `5122a79` adds `scrapeSenateSpecialCandidates` to the scrape module (additive; the FL/OH fallback untouched) + the opt-in pass, with `buildSenateMatcher` and `isSettled` extracted so base and special writes make the identical call; `d521145` adds the dated trigger — seeded specials within **±7 days** of primary or runoff date and carrying no shares get scraped ahead of the cursor, cap 3 states/tick, cursor never advanced, budget shared from `DEADLINE_MS` (a cursor position cannot serve a fixed election date; at twice-daily ~14 attempts across each window). *Amendment before land:* the special fetch was moved into the scrape module so one copy of the Ballotpedia URL/UA/timeout semantics exists by construction (oddities). **It ships un-run:** Ballotpedia's SC special page was 404 at both 560 and 561, so C1's hit branch is unexercised and the path has only ever run its miss branch — WATCH-tracked with a scheduled proof date (~Aug 13). **No theme-% change — a correctness arc plus an ingestion path on the existing electoral pillar; Races holds at 99.** **Numbering — the 557 → 561 pointer jump skips no block:** HO 558 was the doc sweep for 557, HO 562 is this sweep (the HO 506/536/553 precedent). Docs: HO 562. **Also notes now run through HO 561.**

**Also (HO 563–564), the House roster-erasure arc — a live P1 found by a queued investigation, then fixed and repaired; no theme-% change.** *The queued "143 zero-candidate rows" investigation found a live data-loss loop, and the two backlog items it was meant to separate turned out to be one bug seen from both ends.* **HO 563 (`921e752`, read-only probe)** was sent to split three candidate causes for the 143 and came back with a fourth thing that outranked the question. **M1:** 143 rows (senate 1 / house 142) across 96 districts — whole-state failures (OH 15/15 · AZ 9/9 · NE 3/3 · ME 2/2 · ND/SD) alongside scattered ones (NY 24 of 26 · NC 8 of 14 · CA 8 of 52 · AL 6 of 7), with `updated_at` well after `primary_date`, so **not un-visited**. **M2, the deciding measurement:** a live scrape of all 96 → **ok=66 · no_section=13 · no_candidates=17 · no_page=0 · special=0** — not one bucket but two: two-thirds parse fine live while sitting empty in the database, the signature of a roster deleted and not yet rewritten, and the zero `no_page`/`special` **refuted the handoff's own §1 premise** that those statuses made these rows (neither reaches the write block). **M3, the finding that reordered everything:** of 40 sampled resulted House districts, **12 (30%) return `no_section` live** (AL-01 · AR-02 · CA-09 · GA-01 · NV-01 · NY-01 · OK-01 · OR-01 · PA-01 · KY-02 · MD-02 · MS-02), every one holding results, every one one cursor visit from the fall-through's delete-then-insert-nothing. **M4:** `.cache/` is uncommitted, so `readCachedHtml` returns null in production and M2/M3 are live Ballotpedia, not a local artifact. **M5:** cursor mid-sweep at house offset 288 of 435, the M1 rows written 2026-06-18 … 07-21 — visited recently and empty anyway. **The mechanism:** `syncHouseDistricts` sent `special` and `no_page` to `continue`, but `no_section` and `no_candidates` **fell through into the write block**, whose per-contest delete-then-insert then ran with an empty roster — a district whose page stopped parsing had its roster, and its results, deleted and replaced with nothing. **HO 560's guard could not have caught it:** that predicate refuses a *rewrite* on a settled row (substitution); this is *erasure*, and a wiped row has no share left to protect — which is why the queued House guard was the wrong shape before the probe ran. **The cadence interaction, as measured rather than assumed — a correction to this block's own first draft.** It originally asserted HO 560 C3 doubled the erasure rate and ran that way for about a day; **that was wrong, and the correction is the more useful record.** C3 (`65d0692`) landed **2026-07-29T18:14:38Z**, after that day's only 12:00 tick, so the first 00:00 slot was not due until 2026-07-30T00:00; before it arrived the cron was paused from the Vercel dashboard for the classifier outage, and the 564 fix went live roughly 71 minutes ahead of that slot. **Hour-00 ticks since C3 = 2, both post-fix** (the scheduled 00:00:47Z tick, which timed out, plus the 00:38:47Z manual confirmation tick) — and **0 ever ran the erasure code**. So **C3 was a latent amplification closed before it took effect**, and the erasure ran at the original once-daily rate for its entire life — which reframes the repair: **+124 rostered contests and +21 carrying a share reflect erosion across many daily cycles, not one accelerated day.** The methodological half is the reusable one: the doubling was asserted from a schedule *string* without checking whether the schedule had ever *run*, and a roadmap block is append-only, so the error would have been permanent — the claim-verification rule applied to the planning layer's own framing. The dashboard pause is worth recording on its own merits (no `vercel.json` change in either direction): these losses are the durable kind, because a row is erased *precisely because* its page fails to parse and the repair works by re-scraping, so the rows destroyed are exactly the ones re-scraping cannot recover. The cadence **stayed at twice daily** after the fix — halving the rate of a fixed bug buys nothing. **HO 564 (`154d779` C1 · `95fdd10` C2).** The rule shipped as *a sync must never convert absence of evidence into deletion*: `no_section` joins `special`/`no_page` and skips the write block (a missing anchor after three retries is a parse failure, not evidence a contest exists), `no_candidates` stays on the row-creating path (that page did parse), and the per-contest delete runs **only when the incoming roster is non-empty**, on both chambers. **Senate was widened in on structure, not measurement** — its status fork already skipped non-`ok`, but the empty-per-contest-roster path on a good parse was the same edge, sized by STEP 0 S3 at **28 rows** outside the HO 560 freeze. `rosterDeletesRefused` surfaces on both summaries and into `cron_runs.payload`, so the guard is visible rather than silent. **Falsified before it was recorded as protection** (the HO 549/553 discipline), on MD-05 with 27 resulted candidates: forced `no_section` left the roster byte-identical 27→27 with the skip logged; forced empty-`ok` returned `rosterDeletesRefused` naming both contests. Scratch reverted, tree clean, nothing committed. **Repair, measured:** House candidate rows **1,911 → 2,294** (+383), contests with a roster **594 → 718** (+124), contests carrying a share **389 → 410** (+21 — result sets recovered, not just rosters), zero-candidate **143 → 73**; refusals during the repair run NE 44 · S 34 · MW 1 · W 7. **The residual 73 is decomposed, not stuck:** 40 rows are a legitimately uncontested party whose sibling contest is rostered, and 32 rows across 16 districts are genuinely non-parsing (AL-02 · AR-01/03 · ME-01/02 · NC-02 · NY-02/05/08/16/18/20/22/26 · UT-04); the handoff's "~30" estimate mapped to the second group only, and the fix repopulated everything that parses while fabricating nothing that doesn't. **The confirmed post-re-enable tick** is `cron_runs` id 6042 (2026-07-30T00:38:47.600Z, success, unit house, cursor 332→344): `rosterDeletesRefused: []` — the field is **live in the prod payload**, which is what confirms the *wiring*, and its empty value means all 12 districts on that pass parsed cleanly so the guard had nothing to refuse. **An empty counter is not evidence the guard works** — the MD-05 falsification above is that evidence, and the two must not be conflated; likewise the presence-of-field discriminator is invalid on `timeout` rows (id 6035 has no finalized payload), so the pre/post-fix attribution rests on deploy timing, not on the field's absence. **No theme-% change — a correctness fix on the existing electoral pillar; Races holds at 99.** **Numbering — the 561 → 564 pointer jump skips no block:** HO 562 was the sweep for 559–561, HO 565 is this sweep (the HO 506/536/553/558 precedent). Docs: HO 565. **Also notes now run through HO 564.**

**Also (HO 566–568), the delete-rebuild erosion arc — the deliberate version of the 563/564 accident; no theme-% change.** The erosion at 563/564 was found by a probe aimed at something else, so the next idle slot aimed one on purpose: a grep of the delete-rebuild shape across sync code picked three targets, and **a read-only probe measured all of them before anything was scoped.** The map it returned inverted the fear — the STANDING wipe (committees, every roster revisited every 12h) had **zero victims**, the real damage sat frozen on the WINDOWED paths, and the derived recompute held its contract exactly. **HO 566 (`0c62a81`, read-only probe), five measurements.** **M1:** 236 committees (53 main / 183 sub), 10 zero-member — all select/joint/commission bodies **ABSENT from the roster source entirely**, so they never enter the write loop; source parsed 230, **0** source-rostered-but-DB-empty, **0** trailing-by-≥3, uniform 1.7h roster age. **M2:** 1,516 votes, **4** with a positive tally and ZERO `member_votes` (`house-119-2-253/254/269` · `senate-119-2-146`), the **1–4 partial bucket EMPTY in both chambers**, surpluses 0 — **the deficit shape did the diagnosing**: benign per-member skips produce partials, an inflow race produces all-or-nothing zeros. **M3:** both vote syncs **WINDOWED** (House watermark `MAX(vote_date)`, L354/L388; Senate `MAX(roll_call)` per session, L361/L373) — thin/empty risk is first-write-only, then frozen forever. **M4:** Senate `amendment_votes` computed 97 == materialized 97, drift 0/0, unmatched 0, residual 64 = the pinned anchors — the HO 537 pure-recompute contract's first external check. **M5** on the record: 2,259/2,624 zero-bill meetings = expected sparsity. Classification: **committees STANDING-zero-victims · member_votes WINDOWED with 4 frozen first-writes · amendment_votes INERT.** **HO 567 (diag `26879b5` · C1 `547d358` · C2 `f6aab1b`), the member_votes self-heal.** STEP 0 gated the cause before any build code: all four returned **populated rosters upstream with tallies matching the stored ones exactly** — a transient-publication race (the tally publishes before the member positions), not permanently memberless rolls. The design fork was decided for the **bounded self-heal pass over holding the vote row**: tallies stay live, permanent loss becomes bounded lag, and repair + fix are one mechanism (the pass's first run drained the 4 — no backfill script). Predicate is **zero-roster only** (`tally > 0 AND NOT EXISTS member_votes`, newest-first, `HEAL_CAP=10`/chamber): partials measured 0, a structural partial (a Senate resolver miss) would spin under a deficit predicate, and the zero-roster predicate is **self-limiting by construction** — a vote that heals even partially exits the set. It reuses the per-vote write unchanged; permanent retry per the HO 553 posture; `memberVotesHealed` / `memberVotesHealPending` into `cron_runs.payload` (live at id 6056). **Both branches proven** (the 549/553 discipline): a scratch-forced empty roster on `269` landed `pending=[269]` with the row unchanged while `253`/`254` healed live around it, scratch reverted clean, re-run healed it. Drain: all four to full tally, zero residual; the 566 probe re-ran as the **regression instrument** — M2(a) 0/0 (senate 871→873 = two new fully-populated votes synced during the drain). **The participation distortion, per the §0.4 read (not from memory):** a zero-roster vote **deflates the 119th participation denominator** — `getParticipationStrip` / `participationAggCte` / `getChamberParticipationContext` all compute `not_voting / COUNT(*)` over the member's OWN `member_votes` rows, so a vote with no rows is absent from every member's count and the missed-rate is measured over fewer votes than occurred; it does NOT inflate any individual MISSED (no member has a row to count as a miss). The career path (`member_career_votes`, Voteview cast-codes) is a different source and unaffected. **Render payoff:** `senate-119-2-146` is the **Kim Amdt No. 5545 budget waiver** — its HO 557 motion-line party split was all-zero → HO 533-suppressed; it now renders **44 D + 2 I yea / 53 R nay / 1 D nv**, and the waiver-failed → amendment-killed clause carries a real split. **HO 568 (`38ea8b0`), the committees delete gate.** The 564 rule enforced on the standing wipe — this time **the analogy WAS measured before porting** (566 M1 sized the same shape). The DELETE is pushed only when the **insertable count** (bioguide-bearing entries, NOT raw array length — the B falsification's point) is > 0; a refusal is **reported only when it protects existing rows** (the rostered/unrostered fork keeps `rosterDeletesRefused` signal-bearing at `[]`; with the 10 empties source-absent, the silent-skip branch is purely defensive today). The one-`db.batch` shape is preserved (HO 143). Falsified both variants on `hsag00` (53 rows byte-identical under an empty array AND insertless entries, code in `rosterDeletesRefused`; control `hsju00` rewrote normally; natural run restored, `[]`). Dissolution trade accepted: a genuinely emptied roster lingers at last membership until a manual one-off. **Two files, adjudicated:** the committees route **hand-picks** payload fields (unlike the primaries route's whole-result pass-through), so surfacing the field took one line in `app/api/cron/committees/route.ts` — guard #1's "one file" was inconsistent with the handoff's own §5.2 payload gate, so Code resolved toward the gate and flagged it; accepted, and the forwarding-shape difference is now a SKILL note. Payload live at id 6059. **No theme-% change across the arc** — a probe plus hardening on existing pillars; no bar moved. **Numbering — the 564 → 568 pointer jump skips no block:** HO 565 was the sweep for 563–564, HO 569 is this sweep (the HO 506/536/553/558/562 precedent). Docs: HO 569. **Also notes now run through HO 568.**

**Also (HO 570), the cleanup arc — six deferred "next-touch" cleanups discharged in one pass, six clean-revert commits; no theme-% change. The block's content is not the deletion count — it's that STEP 0 fired and the target list was wrong.** Each item had been deferred to "the next touch" of a file nobody was touching; this was that touch. **STEP 0 saved two live surfaces the backlog had marked dead.** The backlog named `.v2f-sponsor*` dead — but `.v2f-sponsor-last` / `--plain` are **live** (`V2FeedList.tsx:123,130`), and the `.v2f-sc-*` sponsor-card block sitting *immediately beside* the "these are dead" comment is **live**, rendered by `BillExpandPanel.tsx:224` and revealed by `.bxp-sponsor:hover .v2f-sc-card` (`globals.css:4456`). A prefix sweep would have silently killed the sponsor hover card HO 472 verified; the comment naming the dead set was itself the vector, so **C1 rewrote it rather than deleting under it**. **C1 (`ef36547`)** — 20 dead selectors to zero, `app/globals.css` **9,713 → 9,269 (−444)**, braces balanced 1468/1468: the HO 317 private-Expand orphans, the HO 306 `.hill-band*` family, the HO 366 `/hearings` chrome, the HO 306 list-view filter bar, the HO 367 `.mc-fbar-title`. **Flagged deviation:** C1 also dropped `.hearings-filter-count` + `.hearings-filter-opt.is-active*` (0 consumers) beyond the handoff's literal list, and said so. **C2 (`29a7e66`)** — `getMembersRankedCount` + 8 `lib/hearings` exports (+ the internal `HEARING_TYPES_SET`). Two kept, **and the handoff was wrong about one:** `HEARING_BADGES` produces the live `HearingBadge` type, and **`weekdayOfKey` is live via `mondayOfKey` inside the module** — the planning grep excluded `lib/hearings.ts` from its own consumer search and missed it (the same class of error as the glob; Code caught it and refused the delete). **C3 (`95cf48e`)** — the dup key was **`:96 key={id}` over `billIds`**, not the `:82` issue-code map the backlog named: **82 filings in the scoped drill blob carry duplicate bill IDs because the precomputed `readLdaTables` path has no `SELECT DISTINCT` while the live `hydrateFilings` path does — and the HO 440 distinct-filings ranking is unaffected**, because `computeBillDrill` inverts `billsByUuid` through a `Set<string>` (`lda-rollup.ts:459–464`) so a bill repeated inside one filing contributes its uuid once; the duplication only ever escaped through the `FilingSummary.billIds` display field (`:482`), which is where it was fixed (deduped both arrays). Retired the allowlist entry; `MissingSecret` kept. **C4 (`c229145`)** — `verify-deploy.ts` `process.exit` → `process.exitCode` (live-proven: `verify:deploy` exited 0), the `sync-fec.ts:4` stale "1000/hr" comment corrected to the real 60/hr tier, the dead `getSponsorStates` probe that had produced the HO 404 audit's **false SLOW: 34,656ms**. **C5 (`33dfb3e`)** — the amendments repair gate moved from `stored >= pagination.count` to full enumeration (`missing.length === 0`), matching the nominations fix at `e175fd7`; `RepairResult` now carries `distinctLive` + `duplicates`. **Falsified, so recorded as proven protection not a WATCH:** a scratch fetch monkeypatch served the real 6,975-id stored set plus one duplicate with `pagination.count=6976`; the real `repairAmendments` returned `duplicates:1, complete:true` in **pass 1**, where the old gate (`6975 >= 6976`) would have looped to `maxPasses` with `complete:false`. Harness deleted, tree clean, nothing committed from it. **C6 (`af8378e`)** — the president predicate-wait (`waitForURL(/[?&]stage=president/)` replacing the 1.5s fixed wait; the `toContain` assertion kept; the crawl settle at `:162-163` and the sites at `:275`/`:308` untouched) plus an explicit `/races` → `/electoral` landing assertion. **Code's correction, carried:** the "HO 502 pattern" the HO 570 handoff told it to extend **does not exist in the file** — `/news` and `/president` became real routes, so there was no per-route `redirectedFrom()` assertion to copy (the claim came from the backlog). **The convention problem, stated as a finding:** the ledger's header rule (a backlog line's mechanism is a claim) notes two wrong in three HOs and says a third makes it a convention problem — this arc produced **two more genuine wrong-mechanism claims** (the `.v2f-sponsor*` glob, the HO 502 redirect pattern) plus stale line numbers in three places, so it is past the threshold; promoted to a real backlog item rather than another tally mark. **No theme-% change** — cleanup on existing surfaces, no bar moves, no new surface, no migration, no cron change. **Numbering — the 568 → 570 pointer jump skips no block:** HO 569 was the doc sweep for 566–568, and docs-only sweeps are owed no roadmap block (the HO 506/536/553/558/562/565 precedent). Docs: HO 571. **Also notes now run through HO 570.**

**Also (HO 572), the `#lobby-drill` anchor restored + the schedule-trigger answer — a correctness fix on an existing surface plus a read-only check; no theme-% change.** **The fix (`290e239`, two files).** `TopicCrosswalk`'s code chips link to `/lobbying?issue=…#lobby-drill`, but the HO 486 redesign had removed the target element — so a click dropped the reader at page top. The id is restored on the **stable `.mc-content lob-content` pane container** (`app/lobbying/page.tsx:416`), **not** the scoped `.mc-ctx` block: a well-formed-but-unknown `?issue=` resolves `selectedDrill = null` and falls back to unscoped (`:117`), so the scoped block doesn't render and an id inside it would dangle again through a narrower door. A comment at the id records the placement so nobody "corrects" it onto the scoped block. **Why restore rather than drop the fragment** (the fork HO 571 filed with two options): the crosswalk renders in `.lob-secs` **below** the pane (`:493` vs `:416`, the HO 492 side-by-side layout), so a reader clicking a code chip is at the bottom of the page with ~400px of chrome (masthead + two-row nav + readout + blurb + fbar) between them and the scoped answer they asked for; dropping the fragment would have made the test green **by deleting the behavior instead of restoring it** — the same locate-the-reader's-selection principle already applied at HO 492 (`scrollIntoView({block:"nearest"})` when a bounded rail re-rendered with its selection out of sight) and HO 517 (the rail pin when a deep-linked member landed on a rail with nothing lit). **The assertion promoted soft → hard** (`e2e/fit-finish.spec.ts:470`, the HO 473 precedent: soft while a finding is open, hard once fixed). The STEP 0 diagnostic is the interesting half: the failure was **not** a soft red — `drill.evaluate(...)` ran against a zero-element locator and rejected *before* `expect.soft` was reached, which is why it surfaced as a hard failure and why the `codeChip.count()` guard above it was passing (chip present, target missing). Suite **1 failed → 0 failed (41 passed, 1 skipped)**. **Verified:** `inView` true at 1280×720 and the motivating short viewport; `?issue=ZZZZ#lobby-drill` → 200 with the scoped block absent, the anchor present, zero console errors, confirmed on prod; unscoped `/lobbying` unchanged (~1,380px against the HO 492/493 ~1,369 figure — data-dependent, an id adds no layout). **STEP 0(a) — the `schedule` trigger fires.** Two runs — 2026-07-29 (failure) and 2026-07-30 (success) — both landing **~85 minutes after the `0 15 * * *` slot** (recorded so a future reader doesn't treat a 16:2x run as anomalous). Latest green → the **Jul-23 open loop closes, and with it the whole Phase-2 Playwright-in-CI loop**, whose only surviving thread this was. Breakdown over the last 30: `deployment_status` 14 success / 12 skipped / 2 failure; `schedule` 1 success / 1 failure. **The finding — UNCHARACTERIZED.** An intermittent `pageErr=1` (uncaught page error) on **`/`** and **`/?stage=other_chamber`** in the `smoke.spec.ts` route crawl accounts for the 07-29 schedule red and both `deployment_status` reds; it sometimes recovers on retry #1 and sometimes fails both hits. **Ruled out:** the HO 473 multi-child SVG `<title>` hydration class — all four `<title>` sites (`PrimaryTimeline:145`, `BillsTimeSeries:123`, `CommitteeActivityChart:145`, `RaceDistrictModal:140`) carry a single template-literal child at `290e239`, so that fix held; and the global-`concurrency` cancellation WATCH (these are `failure`, not `cancelled`). **Untested hypothesis, flagged as such:** the two failing URLs differ only by a filter that changes the feed, so the shared dashboard chrome is the suspect surface, and its time-derived render inputs (the HO 183 cycling stamp, relative ages, the hearings three-hour `watchState` window, the tape) are the shape that mismatches only when a render straddles a boundary — **not to be written up as "probably the known #418 class"** (the grep above falsifies that specific mechanism, and scoping a probe on it would repeat the wrong-premise failure HO 570/571 spent two sweeps documenting). Its own probe HO. **No theme-% change** — a correctness fix on an existing surface plus a read-only check, no bar moves. **Numbering — the 570 → 572 pointer jump skips no block:** HO 571 was the doc sweep for 570 (docs-only sweeps are owed no roadmap block, the HO 506/536/553/558/562/565/569 precedent). Docs: HO 573. **Also notes now run through HO 572.**

**Also (HO 574), the `pageErr` probe — the artifact answered it, the design paid, and the failing set is still moving; no theme-% change.** **The design nearly didn't pay.** The probe was built **artifact-first** against a **14-day retention clock**: all four failed-run report artifacts were still live, and the captured `pageErr` message was **identical across all four runs** — a single cause. It is **React #418, `args[]=HTML`** — the hydration text-content mismatch variant. The message had been captured all along (`smoke.spec.ts:129` pushes `err.message`) but only the **count** was ever printed (`:200`), so the diagnostic had existed for weeks yet was reachable only through an expiring artifact. **Route/hit breakdown:** run `30506188415` (`/?stage=committee`, `/?stage=other_chamber`, hit-2); `30510430349` (`/`, `/?stage=other_chamber`, hit-1 + hit-2); `30470412500` (the 07-29 schedule — `/`, four `?stage=` variants, `/president`, both hits); `30572418753` (five `?stage=` variants + `/members`, hit-2). **It fires on hit-1 as well as hit-2, which rules out the HO 533 cache-serialization class.** **H1 falsified.** `/president` and `/members` fired, and both are non-tape inner pages (HO 323/326 stripped the tape; no `MarketsTape` in `HeaderBar`), so the HO 572 "tape-bearing set" observation does not hold — killed on the artifact route pattern rather than a local test, the stronger evidence. **The block's real lesson — the firing set has been re-characterised twice, from growing samples.** Two runs supported "tape-bearing"; four supported "the relative-age feed surfaces," after two non-tape pages fired. **Neither characterization was wrong on its data; both were premature** — the tell is that the set *grew* with sampling (2 routes → 6 → 6 including a non-tape inner page) rather than converging. **The current characterization is also provisional, and the fix HO must not scope around the observed route set until it stops growing.** The confound is still live: those pages are also the heaviest client-island pages, so "relative-age" and "most islands" remain perfectly confounded. **H2 supported, component unidentified.** Time-derived hydration is the shape that fits (#418 by message, both hits, sometimes recovering on retry), but it is a **new** cause, not the HO 473 `<title>` mechanism — re-confirmed falsified, all four `<title>` sites single-child at `290e239`; nearest prior art is the HO 489/490 relative-age `nowMs` arc, a candidate the fix HO *starts* from, not a conclusion this one reached. **Not reproducible locally** — two full local smoke crawls, route-crawl `pageErr=0` throughout; recorded as a **result, not a gap** (the handoff pre-authorised it — an unreproduced intermittent with the instrument shipped is a complete outcome). **C1 (`57395b9`, `e2e/smoke.spec.ts` only):** a second log line, emitted only when non-empty, carrying the actual messages whitespace-collapsed + truncated to 200 chars each, `pageErr` first. **Falsified before recorded as working** (the HO 549/553 rule): a scratch client-only throw in `CyclingTimestamp` produced a real `pageerror` through the unchanged capture and the new line printed it, console messages cut at 200; scratch reverted, nothing committed. **Handoff premise corrected (a doc error, not a code one):** the HO 574 handoff said `attachCollectors`/`logClean`/`assertClean`/`settle` are shared between the specs — **they are not.** `fit-finish.spec.ts:56` defines its own `attachCollectors` (and its own `GATE_COOKIE` `beforeEach`); it imports only `isKnownNoise` from `console-noise.ts`. The claim came from SKILL `:1913` (corrected in the HO 575 SKILL commit), so C1 is smoke-only and fit-finish is provably unaffected (re-confirmed 41/0). **No theme-% change** — a read-only probe plus test instrumentation, no user-facing surface (the HO 379 / 382–386 / 503–504 / 548 harness precedent). **Numbering — the 572 → 574 pointer jump skips no block:** HO 573 was the doc sweep for 572. Docs: HO 575. **Also notes now run through HO 574.**

**Also (HO 576–577), the primaries data arc — the member-hub primary chip corrected + the house-date self-referential clobber fixed and repaired; no theme-% change.** Two code arcs on one subject; 577 came directly out of 576's STEP 0. **HO 576 — `getPrimaryForRace` over structured columns (`e3cf396`).** The member-hub chip reconstructed the id `senate-${state}-2026-${party}` literally, which **structurally could not match a `-special-` row**, so SC's Aug 11 senate special was invisible and the chip showed a stale June 9; replaced with a query over structured columns. **STEP 0 earned itself twice.** M3a **proved the handoff's own proposed query wrong** — with no `district` restriction it leaked per-district house rows and would have "fixed" 127 nulls with arbitrary district dates. And it surfaced the design question the backlog line hid: the caller passes `district = null` for House **and** Senate members by design, so "prefer the soonest future contest" hands a House member a Senate-only special that isn't theirs. **The approval was self-contradictory, and Code named the contradiction rather than picking one and proceeding:** special-exclusion, a delta matching M3b's 2 keys, and no caller change could not all hold simultaneously for a chamber-blind function. The resolution was a **`memberChamber` argument** — named that way, not `chamber` (which reads as row-selection) or `includeSpecial` (which would put the policy in the caller) — with **no `p.chamber = ?` in the WHERE**, so no House chip was emptied. The rule, as written into the code comment: **a Senate special is a Senate-only contest, so it is never a valid proxy for a House member's primary in any state** — the senate-row-as-proxy convention works because regular Senate and House primaries share a date, and a special is exactly the case where that coincidence breaks. Derivable, not tuned to SC. Special-test is the id substring (no column marks it), isolated in one named predicate flagged as the single site to change if an `is_special` column ever lands. **Verified delta: SC's two senators only** — 532 unchanged, 0 newly populated, 0 lost chips.

**HO 577 — the house-date clobber. The defect class, not the symptom: a table must not derive its writes from an unfiltered aggregate over itself.** `calByState` was `MAX(primary_date) GROUP BY state` with no filters, so it computed "the latest date on any row this state owns," not "this state's primary date." Specials and runoffs won by construction — and once a house row absorbed the wrong date it was `election_round='primary'` and **won MAX itself on the next tick: the loop was already self-sustaining.** Measured (M1a–M1c): **3 states — LA, GA, SC — 48 house rows**, cause tally **special=2 · runoff=3 · house=48** — the house bucket dominating is the finding, the special/runoff were the injection sources but no longer the active cause. Excluding only special and runoff was therefore **provably insufficient**: **`district IS NULL` is load-bearing because it severs the self-referential class rather than narrowing it** — every row that can feed back carries a district. **M7 forced the shape:** 15 states own house rows but have no clean statewide senate row (AZ CA CT HI IN MD MO ND NV NY PA UT VT WA WI) — exactly HO 209's 35-state gate, with no stale pre-gate rows surviving — so a bare `district IS NULL` filter would have nulled their dates, and the write instead **COALESCEs to preserve** the existing value. **The consequence is recorded, not buried:** those 15 states' house dates are now **frozen with no refresh path** — strictly better than circular and the right call, but new latent staleness, which gets its own WATCH. Part 1 resolved **without calendar-threading** — the cron walks a cursor and the House slice already reads the statewide date from the DB's senate rows (`syncCalendar` seeds them from 270toWin), so the fix lives entirely inside the House sync and the scrape stays out of the per-tick path. **LA is a different failure mode entirely and its facts are recorded here because nothing in the repo can rederive them:** Governor Landry's **EO 26-038 (2026-04-30)** suspended Louisiana's U.S. House primaries only, following SCOTUS in *Louisiana v. Callais* striking the congressional map as a racial gerrymander; per the FEC, **neither the May 16 Senate primary nor the June 27 Senate runoff was affected**, so `senate-LA-2026-*` remains correct and a valid `calByState` source. The order ran "until July 15, 2026 or such time as determined by the Legislature" — **that date has passed**, the Legislature approved a new map in late May, and LA may have reverted to the jungle primary for U.S. House — so **no date is correct for the LA house rows, and "suspended" is itself a claim with an expiry.** LA modeled **postponed, not cancelled**: `primary_date`/`runoff_date` NULLed via a `HOUSE_PRIMARY_SUSPENDED={LA}` carve-out, **`primary_type` left alone** — that column is a *type* (open/closed/jungle), not a status; encoding `suspended` there would break precisely when LA needs `jungle`, and it's the same column where SC's `open` is correct. **The repair (Stage 2, `--apply`):** date-only, **GA 28 → 05-19, SC 14 → 06-09, LA 6 → NULL**, correct dates **derived from the clean statewide row, not hardcoded**, both externally verified against the public calendar — GA's clobber was the June 16 runoff, SC's was the Aug 11 special. Date-only because GA's 28 rows are settled and the tick's roster delete-rebuild would churn decided results; verified **48/48 converged, GA roster `80→80` / `28→28` untouched**. **The review caught two amendments before land:** every SELECT and UPDATE carries `id NOT LIKE '%-special-%'` so a house special can't be swept, and the GA roster check captures a fingerprint **before and after** rather than an after-only count that reads the same either way. **Ordering was the whole risk.** GA's 28 rows are settled, so a guard landing before the repair would have frozen them at the wrong date — **Part 1 → repair → falsification → guard, across two deploys because they cannot ship together.** The House `isSettled` guard (parity with the Senate path ~`:707`) is **untriggered the moment it lands**, and an untriggered guard reads identically to a broken one — so the falsification drove the **real** `syncHouseDistricts` against a settled GA-01 and proved it fires: `settledSkipped` names both ids, `primary_date` and the roster's `updated_at` both unchanged (the delete-rebuild never ran). **The interlude:** prod went down mid-arc on an **account-wide Turso rows-read block (~3h, no code fix)** — STEP 0 could not run and was committed **UNRUN** rather than validated against fixtures (fixtures would have produced numbers that looked like findings and weren't); attribution afterward put the burn on **CBT, not psephos** (psephos peaked ~1.7M and was declining; CBT stepped ~10× around Jul 25 and plateaued near 85–90M/day). Live at **`3391850`** (Part 1 `2e8cbbe` · repair `eb88959` · guard `3391850`), two-stage fast-forward through a review ref. **No theme-% change** — a correctness fix and a data repair on existing surfaces. **Numbering — the 574 → 577 jump skips no block:** HO 575 was the doc sweep for 574, and 576–577 are covered by this one block. Docs: HO 578. **Also notes now run through HO 577.**

**Also (HO 579–580), two cron-hygiene fixes on the trades + LDA paths — no theme-% change (a log-noise correction and a cron-topology fix, no user-facing surface).** **HO 579 — the FMP `page` 402 is a plan boundary, not an error (`54f0590`).** `/api/sync` had logged `[sync] trades error: FMP house-latest page 1 failed: 402` every tick; confirmed by hand against the live endpoint — page 0 returns data, page 1 returns the 402 — because the account is on FMP's **Free plan, which permits `page=0` only**. **It's an entitlement gate, not a rate limit:** the FMP usage meter read **0/250 calls and 0 MB/512 MB** while the 402s fired, so nothing was misconfigured or over quota. `lib/fmp.ts` gained `FmpPlanBoundaryError` + `isFmpPlanBoundary`, keyed on **402 AND the page-parameter text** (not 402 alone, so a payment failure on any other parameter stays a real error — proven both ways: a synthetic limit-parameter body returns false, and a forced 404 still lands in `errors` with `isFmpPlanBoundary=false`); `lib/trades-ingest.ts` records `planCappedAtPage` on the result instead of pushing to `errors`, and `/api/sync` carries `tradesPlanCap` per chamber. **The design call worth recording:** `maxPagesPerChamber` was deliberately **not** capped at the call sites (3 cron / 20 manual) — hardcoding `1` would work today and quietly cap a future paid plan, whereas detecting the boundary means an upgrade needs no code change. Nothing is lost operationally (the `-latest` endpoints return newest-first, so page 0 still captures every new disclosure; only per-tick backfill depth is forgone). **The honest gap:** the post-deploy prod tick was warm (`inserted=0` → short-circuit before page 1), so it exercised the clean stop but not the `planCappedAtPage=1` recording — proven locally against the real 402 and self-closing into a log line on the next tick that reaches page 1 (WATCH). **HO 580 — the LDA rollup and sync were competing for one budget (`e459fe9`).** The rollup started only if `msLeft >= ROLLUP_RESERVE_MS (220_000)` against a 300s ceiling, so **the sync had to finish within 80s**; the first authenticated run made **259 `lda.gov` calls at ~1s each** (~259s), starving the rollup by more than 3× the margin. **The inversion is the finding:** the rollup ran only when the sync had nothing to do and was starved precisely when new filings arrived — `route.ts:45-48` accepted "a day stale" but the real behaviour was stale indefinitely until a hand-run `npm run lda:rollup` (last refresh 114,048ms; 129,105 filings / 277,703 activities). Neither component was wrong; the coupling was. **Change 1:** a dedicated **`/api/cron/lda-rollup` at `0 22 * * *`** (the previously-banked standalone slot, now built), with the rollup block + `ROLLUP_RESERVE_MS` + `ROUTE_CEILING_MS` removed from the sync route; the new route can `revalidateTag("lda")` which `scripts/rollup-lda.ts` cannot (Next-runtime only), so the surface no longer waits on a TTL, and the manual script stays the backfill hatch. **Slot correction recorded:** the handoff proposed `0 10`, which is **taken by `/api/sync-votes`** — the instruction to verify against `vercel.json` rather than trust the handoff is what caught it; `0 22` is clear of every daily cron, markets (`0 */4`, last 20:00), the weekday `?source=fmp` pass (`0,30 13-21`, last 21:30), and kalshi (`15 */2`). **Change 2:** the deadline check at `lda-sync.ts:411` ran *before* `fetchPage` while the 429 retry sleep happened *inside* it — a check passing at 279s then a `retry-after=39` sleep landed at 318s and the function was SIGKILLed instead of exiting with `deadlineHit` (the observed 504). `fetchPage` now returns **`null`** when the sleep would cross `deadlineMs`, and the caller treats it as `deadlineHit`, **not** `fetchError` (different destinations — reporting it as an error would convert a clean pause into recorded failure); `onThrottle` still fires so `throttled429` keeps counting the real 429; backfill passes `deadlineMs: undefined`, leaving the branch inert. **Exactly one `return null` exists in the fetch path**, so no other condition can be misread as a deadline pause. **The falsification was two-sided** (why it's recorded): BAIL (`retry-after=60`, deadline ~35s out) → bailed, `deadlineHit=true`, `fetchErrors=0`, elapsed 31s against the ~91s a real sleep would have produced; CONTROL (`retry-after=2`, deadline far) → slept, proceeded, 16 pages — a guard that always bailed would pass a bail-only test and be badly wrong, and elapsed time is physical evidence rather than a reported flag. First runs after deploy: sync at 21:38 (`pages=19`, no rollup line at all), rollup at 21:39 (`rollup ok in 20219ms`). **Numbering — the 577 → 580 jump skips no block:** HO 578 was the sweep for 576–577, and 579–580 are covered by this one block. Docs: HO 581. **Also notes now run through HO 580.**

**Also (HO 582), the markets tape read burn — a query-cost fix, no theme-% change (a read-budget/infrastructure fix; the only user-facing effect is a latent double-render removed).** `getLatestMarketTicks` (the tape query) had `revalidate: 60` — **equal to `MarketsTapeClient`'s `POLL_MS`** — so the SWR TTL expired every minute and any open tab drove ~1 regeneration/min around the clock (~1,440/day). **STEP 0 (`markets-read-burn-582.ts`, `3baf98c`, read-only, measured-not-reasoned per the read-budget WATCH):** 12,240 rows/regen × ~1,440 = **~17.6M rows/day per open tab, ~20% of the ~88M/day plateau** — NOT small, so the tape is a real contributor and this ships as more than hygiene. **C1 (`cf1516d`) is the ~60× lever** — `revalidate 60 → 86400`, a bounded-staleness backstop with the single tag-flushing writer (the markets cron's `revalidateTag("markets")`) as the sole refresh path; a regen marker (`[markets] regenerating market-ticks-latest`) proves the new cadence, and the false "no per-request DB hit between writes" header comment on `/api/markets/latest` is corrected true. **C2 (`8396068`) ~1.5×** — Q2's spark window `julianday(ticked_at) >= julianday('now','-9 day')` was non-sargable (the column wrap defeated `idx_market_ticks_symbol_time`, reading every row each spark symbol owns: 5,636 read to return ~1,619); replaced with a sargable JS-ISO `ticked_at >= ?` bound (STEP 0 M1 confirmed the stored `new Date().toISOString()` format, M3a proved row-for-row equivalence, EXPLAIN is now a per-symbol range SEARCH). **C3 (`2a86c3f`) was initially SKIPPED as a no-op and that was WRONG — the reasoning error is the record, not just the outcome:** STEP 0 read Q1's `SCAN market_ticks USING COVERING INDEX` and called it "index-only, cheap," but that IS the scan branch of the gate, and index-only is a *latency* property while this HO's currency is *rows read*. After C2 the covering scan was **~6,604 of the ~8,223 residual (~80%)** and **grew with table history, not symbol count** (~100 rows/day trends toward ~43k/regen within a year). C3 replaces the MAX-GROUP-BY join with N per-symbol `WHERE symbol=? ORDER BY ticked_at DESC LIMIT 1` seeks UNION ALL'd (all N SEARCH arms, no temp b-tree, **~19 rows read against the join's ~6,604**, equal ~31ms latency — the ~1,638 figure is the *combined* post-fix per-regen total, Q1 ~19 + Q2 ~1,619, not C3's own read), bounding it at symbol count (oddities — rows-read vs milliseconds are different currencies). **C4 (`d0feb8f`)** visibility-gates the poll (skip when `document.visibilityState==="hidden"`, poll-on-return; `POLL_MS` unchanged) — defense in depth at the source. **Two behavior changes, both strict improvements (STEP 0 M3):** the 13 retired legacy symbols (DOW/GOLD/…) are no longer read (the join returned them, the loop discarded them via `if (!meta) continue`), and **CPI+WTI — which had two identical rows tied to the millisecond on `ticked_at` (a same-run double-write) and which the join therefore rendered TWICE on the tape — dedupe at read time** via `LIMIT 1` (the write-path defect is a separate P3 backlog item with its own instrument; a read-time dedupe removes the *symptom*, oddities). **Verification is PARTLY OPEN — recorded as such, not written up closed:** `/api/version`=`d0feb8f` confirmed, and local + prod `/api/markets/latest` verified the deduped 18-symbol payload with sparks intact; but **freshness-after-the-next-cron-tick, the marker cadence in runtime logs, and the 24h Turso rows-read slope were all pending at sweep time.** **The prediction, on the record before the measurement lands (append-only, so it stands either way):** the plateau was a *step* (~10× ~Jul 25, a ~79M/day jump), the tape has polled since HO 172, and `market_ticks` starts 2026-05-27 — so the tape accounts for **at most ~17.6M of the ~79M step**; C1+C2+C3 should cut the daily slope by **roughly a fifth — a level shift, not a resolution**, and if the slope barely moves the burn model is wrong and *that* is the finding. The read-budget OPEN LOOP is **partly discharged**, not closed — it stays open aimed at summarize / LDA / traffic for the residual ~80% (backlog). **Numbering — the 580 → 582 jump skips no block:** HO 581 was the doc sweep for 579–580 (docs-only sweeps are owed no roadmap block, the HO 506 precedent). Docs: HO 583. **Also notes now run through HO 582.**

**Also (HO 584), the LA House 2026 restore — the "small data fix" that was the HO 577 clobber through the front door; no theme-% change (a correctness fix on an existing surface).** The backlog item read as a one-line unlock ("drop LA from `HOUSE_PRIMARY_SUSPENDED` in the same change that writes a real date") and **it wasn't** — that precondition was **necessary and insufficient.** HO 577's non-suspended branch COALESCEs the House date from `calByState`, LA has a clean statewide **senate** row (`senate-LA-2026-*` @ 2026-05-16), so unlocking alone would have COALESCEd 05-16 onto all six `house-LA-*-open` rows — **the exact HO 577 clobber, re-entered through the mechanism 577 shipped as the fix.** STEP 0 (`la-house-restore-584.ts`, `11f7735`, read-only, non-SELECT-refusing) measured the six rows and what the unlock would write (M2 = 2026-05-16, the §0 argument confirmed). **The root cause is the convention, not Louisiana:** the senate-row-as-proxy holds only while a state's chambers **share a date AND a system**, and **Act 7 of the 2026 Regular Session broke both at once** — the House runs a jungle all-candidate primary on Nov 3 (an "open general" Dec 12 if nobody clears a majority) while the Senate runs closed party primaries on May 16. So this is the **second instance of the HO 576 rule** (*a Senate contest is not a valid proxy for the House when the coincidence breaks*), a **statute standing in for 576's special.** **The fix (`c40c447`) generalizes the mechanism:** `HOUSE_PRIMARY_SUSPENDED` (a force-NULL lock) → a per-state **`HOUSE_PRIMARY_OVERRIDES`** map carrying `{primaryDate, runoffDate, primaryType}`; an override WINS over `calByState` (no COALESCE — it is authoritative), every other state is unchanged, and **suspension is now expressible as a NULL-dated override**, so HO 577's suspended/COALESCE split collapses to one branch keyed on override presence. LA → `{2026-11-03, 2026-12-12, jungle}`. **The override covers `primary_type`, not just the dates, for a non-obvious reason:** `primaryTypeFor` DEFAULTS everything outside top_two/top_four/ranked_choice to `open`, so LA senate's stored `open` is a **fallback, not a determination** — a correct future upstream fix to its true `closed` (Act 1/640, LA's first Senate party primaries since 2010) would otherwise stamp `closed` onto six all-party House rows via the proxy; writing `jungle` is safe against that *correct* fix, not against today's value (oddities, the no-op-that-depends-on-an-upstream-bug). **External facts with their expiry (nothing in the repo can rederive them):** Act 7 + the LA SoS calendar, **verified 2026-07-31**; litigation over the suspension and the new map was still live, so "restored to jungle" is itself a claim with an expiry — re-check before treating a future discrepancy as a bug. **Terminology trap recorded so a future reader doesn't "repair" it:** LA's statute calls the Nov-3 contest the **"open primary"** and Dec 12 the **"open general,"** but CBT's `open` means *party primaries any voter may join* (what SC has) — a structurally different system — so LA is `jungle` and Dec 12 is modeled as `runoff_date`; the SoS wording must not prompt a correction to `open`. **The row shape was already correct (HO 93.5) but the reasoning under it was dead:** the six `-open` rows came from Ballotpedia voteboxes against a handoff premise (LA closed-partisan for 2026, Act 1/640) that *Louisiana v. Callais* and Act 7 killed — right answer, dead reasoning — so C2 (`6188ac8`) **re-commented the `NONPARTISAN_HOUSE_STATES` membership around Act 7 rather than re-deriving it, and inverted the tripwire** (under Act 7 there should be no D/R LA House voteboxes, so the structural guard firing now means Act 7 was superseded, not that Ballotpedia drifted). **Verification is COMPLETE here, unlike 582's partly-open close** — the falsification drove the real `syncHouseDistricts` and **all six rows are 2026-11-03 on prod.** But the method itself is the subject of a new discipline rule: **a leg proved the *defect* by writing the 05-16 clobber to the production DB and relying on the fix to undo it, rather than proving branch-and-args untouched** — survivable only because the primaries helpers are uncached (SKILL + oddities). **The caveat, in the same breath:** the fix is write-side only — `getPrimaryForRace` still resolves a LA House member's chip from the statewide **senate** row (05-16), so **the member chip and the six House rows now disagree on prod today**, a live user-visible inconsistency this HO created (the read-side of the convention it fixed write-side, same root as HO 576's `memberChamber` fork; backlog). **Numbering — the 582 → 584 jump skips no block:** HO 583 was the doc sweep for 582 (docs-only sweeps are owed no roadmap block, the HO 506 precedent). Docs: HO 585. **Also notes now run through HO 584.**

**Also (HO 586), the LA/at-large House member-chip read side — the read-side of the convention HO 584 fixed write-side; no theme-% change (a correctness fix on the member-hub primary chip, an existing surface).** Entered as "the LA chip shows May 16" — the live inconsistency HO 584 *created* (write-side corrected the six `house-LA-*-open` rows to Nov 3, but `getPrimaryForRace` still proxied the chip off the statewide `senate-LA-2026-*` row @ 05-16) — and ended as the general **district-first resolution**. **What licensed the promotion from LA-only to general is the M3 full-corpus delta:** every current member's chip computed under the shipped query and the proposed one — **534 members, (c) non-null→null = 0** (no regression, the gate), **senate changed = 0** (the proxy path byte-identical), **every (b) date-change was exactly the LA six** (May-16 proxy → their own Nov-3 jungle row), and the **160 (a) null→non-null fills were precisely the HO 576 top-two / no-2026-Senate-seat carry-forward**, closed here as a side effect (a party-keyed statewide senate proxy structurally can't serve CA/WA's `-open`-only rows or the ~21 no-Senate-seat states; the member's own `house-{ST}-{DD}` row can). **The fix is three touches, not one — the M1 finding was that it couldn't live in `queries.ts` alone:** the query (a district-first branch preferring the member's own `house-{ST}-{DD}-2026-*` row, `p.party = ? OR 'open'`, falling to the statewide proxy only on no-row-found — never on found-but-empty), the caller (`app/members/[bioguideId]/page.tsx` now passes `member.district` instead of a hardcoded null), and the render (the label reads the RESOLVED row's `party` over a three-value column — D→"Dem", R→"Rep", anything else, i.e. `open`/jungle, no party word: the honest all-party "Primary:"). **The at-large contract, made a convention:** `members.district` is NULL for the six at-large states (AK/DE/ND/SD/VT/WY) while `primaries.district` stores `"00"`, mapped at the query boundary (`district == null ? "00"`) — null-means-at-large for a House member; a stray null in a multi-district state finds no `"00"` row and degrades safely to the proxy, never a wrong match; the invariant that keeps that safe is that **no `"00"` row exists outside the six at-large states**. Five of the six at-large members sat invisible in M3's "unchanged" bucket — an **outcome gate can't see a branch that never fired** — and only M6, instrumenting WHICH branch ran, caught them (oddities). **The ORDER BY correction, attributed correctly:** the review spec said "exact party wins" without scoping it to a temporal class, and it shipped as the FIRST sort key — which would let a stale PAST exact-party row outrank a live FUTURE `open` one, defeating the chip's "next primary" semantic; the mandated re-run-after-change caught it, and the record is that the **spec was wrong, not the implementation** (fixed to future-first as the primary key, exact-party-over-`open` a tie-break WITHIN a temporal class, never across it; oddities). **The AK accounting note (so the arithmetic reads right):** the M3 delta split +2 fills / +4 same-date moves rather than the expected +3/+3 because Begich already carried a `senate-AK-2026-R` proxy — so AK is a same-date row-move, not a fill. **The countdown note (written down so it's never filed as a bug):** `primaryDays` (the 30-day amber countdown) now runs for ~430 more members than before this HO — LA countdowns appearing in early October, and CA/WA/AK ones on their own schedules, are expected behavior. **Verification COMPLETE on prod at `a1a97a8`** (the primaries helpers are uncached, so no cache convergence to wait out): four eyeballs through the shipped `getPrimaryForRace` — Carter LA-2 / Fields LA-6 → "Primary: 11-03-26" (no party word), Moran TX-1 → "Primary Rep: 03-03-26", McBride DE (NULL→"00", the at-large branch-1 path) → "Primary Dem: 09-15-26", Cornyn TX senate proxy byte-identical; no review-ref prod lag (~52s promotion). **The 77-of-560 empty-past-house-roster count M5 surfaced is pre-existing data debt** (invisible on the date-only chip), filed P3, not a 586 blocker. **Numbering — the 584 → 586 jump skips no block:** HO 585 was the doc sweep for 584 (docs-only sweeps are owed no roadmap block, the HO 506 precedent). Docs: HO 587. **Also notes now run through HO 586.**

**Also (HO 589–590), the `pageErr` #418 hydration arc — a diagnostic probe + a committed harness + two correctness fixes, and the OPEN LOOP is SHARPER but NOT closed — and explicitly NOT narrowed, since the two-cause partition is retracted below; no theme-% change (hydration-correctness fixes, no user-facing surface change).** The arc that HO 574 left open ("React #418, component unidentified"). **HO 589 (STEP 0, `259babd`) corrected two 574-era claims that had aimed every downstream hypothesis, and one is load-bearing:** (1) `args[]=HTML` is the **structural / markup** variant, **not** "text-content" — `react-dom-client.development.js:5240` fills the first `%s` with `fromText ? "text" : "HTML"` and CI captured `HTML`; a text mismatch reads `args[]=text`. (2) The "identical message ⇒ **single cause**" inference is **withdrawn** — the component-naming diff (second `%s`) is **DEV-only** (prod `react-dom` bundle carries 0 occurrences of the `hydration-mismatch` template/link), so the minified prod string is **byte-identical for every structural mismatch in the app** and message identity carries **zero** cause-count information; the unattended crawl can never self-identify a component, and raising HO 574's 200-char cap buys nothing. **The sample-attribution correction (so a future reader doesn't mis-credit it):** 589 sampled `30724601882 · 30583714867 · 30572418753 · 30595816107`; **HO 574 sampled `30506188415 · 30510430349 · 30470412500 · 30572418753`** — **only `30572418753` overlaps**, so 589's "`30595816107` is a transient Server-Components 500, not #418" applies to **589's own sample**, not to 574's record (writing it as "one of 574's four wasn't #418" would be a false correction to an append-only entry — the HO 587 misattribution class). Three near-disjoint runs is a **second** confirmation the failure is live in-window. **H4 (a static-prerender + `useSearchParams` skew) raised and FALSIFIED:** every prod-firing route builds `ƒ Dynamic`, so `useSearchParams()` gets real params server-side and cannot skew — and 590 M3b corrected the evidence table: **all five routes 589 read as `○ Static` are 3xx redirect stubs** (`/dashboard-v2`→`/`, `/committees`→`/members`, `/members/pass-rate`→`/members?sort=passrate`, `/primaries`+`/races`→`/electoral`), so "`○ Static`" meant "prerendered output" which for a redirect is the stub — **no static route serves tape HTML**, and the static-prerender concern dissolves with H4. **HO 590 rebuilt 589's scratch reproduction rig as a committed, local-only harness** (`scripts/diagnostic/hydration-clock-harness-590.ts`; needs `next dev` for React 19's DEV describeDiff, so it can't ride `e2e-prod.yml`), which manufactures the SSR-vs-hydration clock skew and captures the DEV owner tree + `args[]=HTML`/`text` variant — the guard-correction that a fix whose falsification instrument is *scratch* is protection recorded without being fired. **Two DISTINCT latent bugs found and fixed, kept distinct in the record — both are correctness hardening, NEITHER is proven to be a prod cause, and they are deliberately UNNUMBERED (see the partition retraction below):** **the markets tape (structural, `args[]=HTML`, `626c251`):** `MarketsTapeClient`'s `useState(() => Date.now())` gates the `<span class="markets-tape-arrow">` node via `showSlots`; under a stale/market-boundary skew the arrows collapse (harness: SSR 72 arrows → 0 on hydration; reconciled as `2 halves × reps(2) × 18` vs the post-mount `reps→1`, so `reps` is consistent across hydration and NOT the bug). Named via the DEV owner tree (`TickItem` → the arrow span), fixed by prop-drilling a server `nowMs` (the HO 489/490 shape). **The `lob-age` text drift (`args[]=text`, `601f54a`) — a SEPARATE defect:** `FilingRow.relAge()`'s render-time `Date.now()` drifts the "Nd ago" string across a day boundary; same prop-drill fix, proven by **string equality** (SSR text == hydrated text under skew), not the tape's node-count. **The finding that keeps the OPEN LOOP open (590 C5):** prod `/` is served **fresh per request** (`X-Vercel-Cache: MISS`, `Age: 0`, `no-store`, `Date` advancing), so the render→hydration gap is **sub-second** (harness `control(+0)` — that same gap — **never fires**) and **cannot straddle** a 26h/market boundary; `isMarketOpen` is TZ-explicit (`America/New_York`). So the tape and `lob-age` are real latent bugs but are **NOT the mechanism of prod's ~42 `args[]=HTML` fires**, which **remain unattributed** — a green crawl after these fixes is doubly ambiguous. **The "cause #1 / cause #2" partition is RETRACTED here (HO 591), because C5 destroyed the premise under it:** the two-cause split existed for exactly one reason — `/` carried the tape and the inner pages didn't, so they *had* to be different bugs — and C5 measured the tape out of prod. **The corrected statement: all ~42 prod `args[]=HTML` fires are UNATTRIBUTED — `/`, the six `?stage=` home variants, `/president`, and `/members` alike — not "one fixed, one remaining."** **Whether that is one cause or several is now UNKNOWN**, since message identity carries zero cause-count information (589 M1) and the evidence that once separated the routes is gone. **`/president` is kept named** — it fired in prod (HO 574) and is **tapeless**, and narrowing to "the members surface" would silently drop a route from a firing set whose *growth* is exactly what HO 574's scoping guard warns about. **One lead checked and RULED OUT (HO 591) — a shared client island spanning the firing set:** the repo has **no `SessionProvider` and no `useSession`** (auth resolves **server-side** in `HeaderBar` / `DashboardV2Header`, prop-drilled into `AuthButton`) and **`app/layout.tsx` is bare**. `/` and `/members` have **disjoint** directly-imported client islands (5 vs 9); the **three** shared ones come via their server headers — `AuthButton` (server-prop `user`, `useTransition` only), `CyclingTimestamp` (HO 489-verified safe), `MobileNavDrawer` (`useState(false)` constant seed) — **all deterministic at render.** Stated precisely, because the loose form is false: they **do** share three client components; the finding is that **none of the three can skew.** This is a ruled-out lead, **not** support for either partition. **`/members` remains the sharpest UNEXPLAINED observation** — it fired at `control(+0)` in the 589 sweep and prod fired `committees-redirect`→`/members` — but **37 fresh-context re-tries across 589+590 produced 0 fires** and the single-page-bleed hypothesis 589 floated **failed to reproduce** (590 C2), so those control fires stand unexplained rather than naming a second cause. **Falsification (three-sided, via the harness):** RED at HEAD (`/`@+40d FIRE, arrows 72→0) → GREEN after (no fire, arrows 36, and the full 175-scenario sweep 34+8 fires → 0) → CONTROL (a `+27h fastForward` still flips the tape to stale via the untouched 60s interval, 0 hydration errors — the fix doesn't freeze the staleness signal). **Verification on prod at `601f54a`** (`/api/version` → `601f54a9…`), **and read carefully what it does and does not cover — it covers exactly one thing: the fixes didn't break the tape.** The prop-drill's own risk is a *frozen* clock, which would surface as dead arrows or a stuck stamp, so: prod `/` (SSR, with `ct_seen`) serves **72 `markets-tape-arrow` + 60 `markets-tape-symbol` + 2 AS OF stamps**, prod `/dashboard-classic` **8 + 8**, `/api/markets/latest` **18 symbols**; and on the same SHA locally (prod withholds the *interactive* tape from headless — BotID, so the hover/hydrated read must be local), hydrated `/` settles to **36 arrows / 60 symbols / 36 change cells** — the documented `72 → reps→1` post-mount reconciliation, **not** a collapse — `/dashboard-classic` to **4 / 8**, the **AS OF stamp cycles** (CT→PT; UTC→ET), the untouched 60s poll **fires at ~63s with the arrow count unchanged at 36** (values poll, arrows survive it), and **0 hydration errors** fire on either page. **There is no cache convergence to wait out:** `nowMs` is a **per-request server value**, not a cached one — and prod `/` re-measured `X-Vercel-Cache: MISS` / `Age: 0` / `no-store`, C5's fresh-serve again. **What this expressly does NOT verify is anything in the OPEN LOOP.** Since C5 proved these fixes were **never the prod mechanism**, **a green `e2e-prod.yml` crawl is NOT verification** of this arc — not of the fixes, not of a cause, not of a close; the loop still closes only on a **named cause plus green runs**. **HO 574's block is SUPERSEDED, not retro-edited** — its "single cause" reading was correct on its evidence and wrong on a fact nobody had (the prod message carries no diff). **Numbering — the 586 → 589 gap:** HO 587 was the doc sweep for 586, **HO 588 is the parked absence-watch STEP-0 probe** (`7ce1f0e`, read-only diag) whose roadmap block is owed when that arc ships, and **HO 591 is the sweep for 588–590** (this block + the backlog retraction + oddities + SKILL). Docs: HO 591. **Also notes now run through HO 590.**

**Also (HO 589-595), the pageErr/500 arc — one hypothesis family falsified, one fault split in two, one 500 named and closed, and TWO of this arc's own conclusions retracted on measurement; no theme-% change (correctness + reliability, no user-facing surface change).** **HO 589 (STEP 0, `259babd`) corrected two documented premises that had aimed every downstream hypothesis:** `args[]=HTML` is the **structural / markup** variant, not "text-content" (`react-dom-client.development.js:5240` fills slot 1 with `fromText ? "text" : "HTML"`), and the component-naming diff in slot 2 is **DEV-only**, so the minified prod message is **byte-identical for every structural mismatch in the app** and **message identity carries zero cause-count information**. The HO 574 instrument's 200-char cap is therefore already at its ceiling — raising it buys nothing, because the prod crawl can never self-identify a component. **HO 590 fixed two DISTINCT latent bugs, and NEITHER is proven to be a prod cause:** the markets tape (`MarketsTapeClient`'s `useState(() => Date.now())` gating the arrow node, structural `args[]=HTML`, `626c251`) and `FilingRow.relAge()`'s "Nd ago" drift (`args[]=text`, a **separate** defect, `601f54a`), both fixed by prop-drilling a server `nowMs`. **The load-bearing finding is C5:** prod `/` is served **fresh per request** (`X-Vercel-Cache: MISS`, `Age: 0`, `no-store`), so the render-to-hydration gap is ~1.3s and **cannot straddle** a 26h/market boundary — the harness's `control(+0)` never fires. Both fixes are recorded as **correctness hardening, not closures**. **HO 591** was the sweep plus four fixups, including **retiring the "cause #1 / cause #2" partition** once C5 destroyed its premise (the split existed only because `/` carried the tape and the inner pages didn't). **HO 592 falsified H5** — data-dependent invalid HTML nesting, the strongest remaining *clock-independent* structural mechanism — **in one pass: 0 reparented / dropped / synthesised nodes across all 37 `ROUTES`**, with the detector self-validated before any result was trusted (6 positive fixtures incl. div-in-p, a-in-a, tr-without-tbody, multi-child SVG `<title>`; 2 negative, one pinning `<div>` inside `<a>` as **valid** HTML5) and scanner coverage checked (1460 authored = 1460 parsed on `/`, every element matched by source offset, so the clean result is a real read and not a short one). The **HO 473 SVG `<title>` class is clean at HEAD** both statically (4 sites, all single-child) and at runtime (0 multi-child titles in rendered prod HTML). **H4** (static-prerender + `useSearchParams` skew) is likewise recorded raised-and-falsified: every prod-firing route builds `ƒ Dynamic`, and the five routes 589 read as `○ Static` are 3xx redirect stubs. **HO 593 answered the question that reframed the arc: TWO FAULTS.** The `#418` fires and the intermittent 500 are separate, and the discriminator is the pageErr **message**, which the crawl already logged — no instrument change needed. Over 54 runs / 4,230 route-hit cells: **87 `#418` vs 12 "Server Components render"**, the 12 landing only on `committees-redirect` / `members-pass-rate` / `member-detail` / `stale`, while every `/`-family route **and `/president`** carries `#418` and has never 500'd; the 500 is a whole-route `__next_error__` shell, **not** a segment failing after a flushed shell, so it cannot be producing a structurally different tree. **Three record corrections carried precisely:** (1) run `30595816107` is **RECLASSIFIED** from HO 589's "transient Server-Components 500" to a **site-wide outage** — 216/216 cells non-200, which swamped every aggregate until it was detected and excluded; this is a **new statement here, not a retro-edit** of the 589 record, which was correct on its evidence. (2) The M2 stage table is recorded **WITHOUT a ranking**: the counts are **2/0/0/1/1/1** and will not carry one, because retry masking makes them too small and noisy to order. (3) Route-disjointness is phrased as **"disjoint in the crawl sample"** — M3's digest lists `/stale`, `/` **and** `/members` over 24h, so **`/` is not immune, it was unsampled.** **HO 594 shipped the covering index (`eab2fd2` migration, `8159554` rewire) AND retracted its own STEP 0 recommendation.** Steady state went **20-24s to 140ms-1.5s**, but co-located the cold first touch was still **8.2-14.1s, worst 20.003s to a TimeoutError — margin -4.1s**. The reusable lesson is the *shape* of the error: **a recommendation made on a laptop MEDIAN (208ms) was reversed by a co-located WORST CASE.** Two controls pinned the residual rather than assuming it (`?order=` moved the spike to whichever query touched the table first; `?warm=1` showed a trivial `SELECT 1` does not absorb it), giving the page-residency-under-60s finding that made indexing structurally insufficient. **HO 595 materialized it.** `member_participation` (~554 rows) is rebuilt on the only writer of `member_votes`, and **`member_votes` is now absent from the request path** — the seven remaining references are the bounded ones. Verified **query-level and co-located**, per 594's own finding that route-level counts understate this class: **GREEN worst 6.46s (margin +3.54s) vs RED 14.4s (margin -4.4s) measured in the same requests, 0 query breaches, 0 TimeoutErrors**, route-level 0/13 on cold keys disjoint from both prior runs, and the residual 2-6s proven **positional** (the first-query-in-a-cold-function tax — reordering moved it, and the same read runs 23-137ms when it follows). **Values unchanged:** fingerprints `1eedaa8d` / `21611d72` / `349972d`, row counts 536/549/536, across `/members` (both sorts), `/committee/[code]` and `/members/[bioguideId]`. The **non-destructive refresh path is PROVEN, not assumed** — all three guard branches (empty / shrink / throw) fired against the live 554-row table, which held 554 rows with `refreshed_at` unmoved each time, then a real refresh advanced the stamp with a stable row count. **595's own STEP 0 M2 was ALSO retracted:** it framed materializing as a 1h-to-24h freshness regression and defended it with a 3.02pp drift ceiling; `member_votes` has exactly one writer path (`lib/votes-sync.ts` + `lib/senate-votes-sync.ts`, both only via `/api/sync-votes`, cron `0 10 * * *`), so materializing on the writer is **freshness-NEUTRAL and marginally better** — the value publishes at sync time instead of up to an hour later, and the shipped `revalidate: 3600` was spending ~23 recomputes a day re-deriving values that could not have changed. The justification defended a cost that did not exist. **Verification of 595 on prod at `2297b98`:** `/members?sort=missed` and `?sort=passrate` both 200 rendering 7.5% / 22.5% / 1.9%, `/committee/hlig00` 200, `/members/A000055` 200; `member_participation` 554 rows, stamp `2026-08-04T01:54:03.907Z`. **What is NOT closed:** the `pageErr` #418 OPEN LOOP stands exactly as open and as unattributed as before — this arc removed one hypothesis family and one confound from it, and named nothing. **Numbering — the 586 to 589 gap:** HO 587 was the doc sweep for 586, **HO 588 is the parked absence-watch STEP-0 probe** (`7ce1f0e`) whose roadmap block is still owed when that arc ships, and **HO 596 is the sweep for 591-595** (this block + oddities x6 + SKILL + backlog); **589-590 were already swept at HO 591** and are re-narrated above only because the `30595816107` reclassification and both retractions revise them. Docs: HO 596. **Also notes now run through HO 595.**

**Also (HO 597), the `/lobbying` VOLUME sort — a materialization plus a cache-placement fix, and the arc found a WORSE defect on the way out; no theme-% change (a query-cost + correctness fix; the user-facing effect is that VOLUME returns rows again).** **STEP 0 (`baed5c4`) led with the finding that lowered its own urgency**, which is the part worth keeping: the HO 440/448 guard had fired twice in the 7-day retention window and **neither was a timeout** — both were `BLOCKED: SQL read operations are forbidden`, a Turso read-quota block during one ~93-minute incident that also took the rollup, top-firms and crosswalk. Timeout-shaped fires in real traffic: **0**. The 20s cold breach was real and reproducible on demand but production kept the query warm; that was reported plainly rather than dressed up as justification for work already scoped. **Two defects shipped.** (1) The one the WATCH named: VOLUME ordered by a request-time `COUNT(*) FROM lda_activities GROUP BY filing_uuid`, materialized to `lda_filings.activity_count` with `idx_lda_filings_activity(activity_count DESC, dt_posted DESC)` forced via `INDEXED BY` — subquery and JOIN gone, a pre-ordered single-table walk (`daf6dcc` migration · `387c56f` writer + resumable backfill · `f2f4915` rewire). HO 544 had tuned the JOIN *direction* here and was right to; the lesson that supersedes it is that **no join direction saves a request-time aggregate over a large table**. (2) Found while fixing (1) and the reason the degradation lasted: **the guard sat INSIDE the `unstable_cache` callback, so its degraded empty feed was the callback's return value and was cached for an hour** — one cold miss poisoned that key for 60 minutes and the successful retry never ran (`82eaa24`; the HO 552 class, oddities). **Falsified four-sided (`418e80c`).** RED co-located: the old shape breaches every VOLUME variant and **does not finish in 55 seconds** — worse than STEP 0 could see, since STEP 0 measured through the 10s+10s double abort and could only learn "20s". GREEN worst-of-cold-runs: volume **564ms (+9.44s)**, page-40 **1.48s (+8.52s)**, +linked **2.97s (+7.03s)**. CONTROL ran the old and new SQL against the **same live state** and diffed them (not a temporal before/after — the corpus grows daily): 1,000 visible rows identical position-for-position, clamp boundary identical in three forms, **0 of 429 zero-activity filings visible** (rank-1000 count = 11), `SUM(activity_count)` == `COUNT(lda_activities)` exactly. The degradation path was **fired**, as a matched RED/GREEN pair: RED served the stored empty in <600ms without touching the DB, GREEN re-queried and recovered. **Two residuals, both filed rather than quietly widened into.** `?sort=volume&linked=1&page=40` improved from ">55s, always" to **worst 15.62s, breached 2 of 5** — a large improvement that is **not a clear**, and rounding it to one would be the over-claim HO 594 had to retract; its mechanism is per-row `EXISTS` probing cold pages (the HO 594 residency class), its fix a second materialization beyond this HO's priced scope. And the one that matters more: **`/lobbying?q=` is breaching on prod and rendering a wrong answer** — `?q=boeing` shows "No filings match" with a 200 while 78 filings match — **not caused by this arc** (the search SQL is byte-untouched) and **found only because the new instrument names the failing cache key**. HO 547 priced that path honestly at **≤67ms** for the pager COUNT; at 129,401 filings it is **92.8s**. A cost measurement is a property of the corpus, not the query (oddities). **The instrument is proven in production**, which is what the handoff demanded of any fix here: the guard's log line now carries `sort/linked/q/page`, and it is that line — not the crawl, which is structurally blind to a 200-with-empty-body — that turned an invisible failure into a one-line diagnosis. Also notes now run through HO 597.

**Also (HO 597, record correction to the HO 594 and HO 597 blocks above — appended, not retro-edited, per the append-only rule).** **Every `20.00Xs` figure in the HO 593-597 arc is the instrument's ceiling, not a measurement.** `lib/db.ts` aborts a request at 10s and retries once, so a query slower than 10s reports **~20.00s regardless of whether it takes 21 seconds or five minutes** — the readings are RIGHT-CENSORED at the bound. So the HO 594 block's *"worst 20.003s to a TimeoutError"* should be read as **>20s**; its sub-ceiling readings (8.2-14.1s steady, 140ms-1.5s) are real durations and stand, and the **-4.1s margin stands as a lower bound** on how negative it gets. Same for HO 593's 20.005s and HO 597 STEP 0's three starred rows. **HO 597 is what exposed it:** re-running the same RED query behind a **55s** ceiling instead of a 10s one showed it does not finish in 55 seconds either — so a figure the record carried as "20s" was in truth **>55s**, a 2.75x understatement of the defect that was being argued about. The general form, and the reason this is worth a block of its own: **a timeout is a censoring instrument, and a censored reading is a lower bound, not a value.** When a measurement lands on the bound, re-run it behind a deliberately looser ceiling before quoting it — otherwise the number you defend a design decision with is just the number your timeout was set to. **No pointer advance — this block CORRECTS the HO 594 and HO 597 blocks above and closes no arc; the pointer stands at HO 597 from the block above.**

**Also (HO 598), the `/lobbying?q=` routed search — the wrong answer removed, the loop NARROWED not closed, and the arc reversed itself three times on the way; no theme-% change (correctness + reliability — the only user-facing change is that search returns right answers and fails honestly).** **The reframe that reset the arc, and the most reusable thing in it: the LIKE was never the cost — the walk is.** A `COUNT(*) FROM lda_filings WHERE registrant_name IS NOT NULL` — a name-column read with **no predicate to evaluate and nothing to match** — measured **86.3s co-located**, against the zero-match search's 91.3s. So cost tracks **bytes dragged off the table**, not the predicate and not the match count; every candidate after that was priced in bytes, and the ones that looked clever on the predicate were priced out. **FTS was re-verified and REJECTED AGAIN, on a NEW reason.** HO 546's premises still hold (name columns 25.1 / 27.6 chars; **5,326 registrants + 22,422 clients = 27,748 distinct names across 129,401 filings**); only its *number* expired (~700ms → 91.3s). But the deciding argument is neither of those: FTS5 cannot do infix, so `%oeing%` stops matching "Boeing" — and, decisively, **FTS fixes the sparse half while still needing routing for the dense half, exactly as the name table does.** Both solve the same half; the name table then wins on every remaining axis (already built and maintained, keeps substring semantics, no new index type, no trigger on the `FLUSH_AT=100`-tuned write path). **My earlier framing — "FTS solves a half that isn't broken" — is recorded here as WRONG: sparse IS the broken half.** **The `~700ms → 91.3s` delta is UNATTRIBUTED, and explicitly not a platform regression.** `oddities.md:235` records **no measurement conditions** for the 700ms — no cold/warm, no co-located/laptop, no spread — while the entry immediately beside it, same arc and same surface, records *"a TEMP B-TREE at 7.8s cold"* and even reasons that search is usually served cold. The arc distinguished cold for one number and not for the other, so the gap sits unexplained between corpus growth (112k → 129k, only **1.15×**), measurement conditions, and platform behaviour, and nothing on the record can apportion it. **Two gate failures, and the rewire was withheld both times** (round 1 FAIL, round 2 FAIL after sparse was re-measured at n=8 and routing was priced) before a third round cleared every regime except sparse. **Two retractions, each recorded as a retraction rather than quietly dropped:** the STEP 0 assertion that *"the corpus is NOT the cause"* (nothing measured supported it — the 86.3s bare-column read is what replaced it), and the **12.6s routing price** (leg 2 had been charged for the name scan leg 1 had already done; with ids passed through, leg 2 is **0–1.5s** — the correction is what made the routed path viable at all). **Shipped:** the bounded pager `COUNT` (`1afdd02`, 58.1s → **2.25s** on common terms, rendered `520+`, because any total past the clamp was already being discarded by the code that asked for it) · the honest timeout render (`79d1276` — *"Search timed out — try a narrower term"*, distinct from the genuine *"No filings match"*, count `—` not `0`; the guard always knew the difference and never said so) · `lda_names` + its two id indexes + the sync writer (`4fd9b48`, `e7fdf30`, `85a173f`) · the routed rewire (`e4f09ba` — resolve the term against `lda_names`, **empty → return without touching `lda_filings` at all**, sparse → seek by literal ids, dense → hand back to the `dt_posted` walk) · the completeness instrument (`a4e2c1c`) · the arg-binding fix (`06d03dd`). **The routing threshold `m* = √(k·N) ≈ 1,297` is DERIVED, not tuned** — a formula over `PAGE_SIZE` and the rollup blob's corpus count, so it re-derives as the corpus grows — and it was verified on a **straddling** pair (`insur` 0.86×, `hospital` 1.16×), not on comfortable extremes. **Final measurements: 24 runs, 0 breaches, median 3,291ms, worst 7,735ms.** **Prod-verified at `06d03dd` across all four regimes** — `boeing` 13 rows / count 78 · `insur` 13 / 520+ · `hospital` 13 / 520+ · a nonsense term **0 with "No filings match" in ~400ms** (was 91.3s, and it now never reaches the table). **It shipped over a failed gate, deliberately, with the three conditions stated** — strict dominance (the replaced sparse path breached on **both** `lib/db.ts` attempts at its *best* observed 19.8s; the candidate went 0-for-24), honest failure, and the loop left open — now written into SKILL as a fenced rule, together with the gate statistic it corrected: **gate on the upper tail (`worst − median`), never on the range**, because `max − min` penalises a fast outlier and a 380ms best case had failed sparse on its own good news. **HO 594's retraction was for CLOSING on a thin margin; this improves on one.** **The loop is NARROWED, not closed:** the residual is leg 1 — a ~33,620-row / ~1 MB covering scan ranging **0.3–6.6s** — which is ~1/23 the bytes of the walk this arc started from, fell roughly linearly, and **carries the same 2-15× run-to-run spread at every size**; that is a variance question, not a query-design one, and it is handed by name to the new variance OPEN LOOP. **And a bug shipped and was caught in prod within minutes — by an instrument this arc had just added.** `e4f09ba` left the bounded COUNT binding **2 args against a 17-arg routed predicate**; the guard **degraded to an empty feed instead of 500ing**, and HO 597's failing-key log line named it, fixed at `06d03dd`. What let it through is its own lesson (oddities): the page query and the COUNT run in **parallel inside one guard**, and the equivalence harness executed only the page query — **verifying one branch of a `Promise.all` is not coverage of the pair.** Both safety nets that caught it — the honest-timeout render and the keyed log line — were built in the preceding four HOs, which is the one part of this arc that worked exactly as intended. Docs: HO 599. **Also notes now run through HO 598.**

**Also (HO 600-601), the SC August special primary — a phantom contest deleted, a parser gate that inverted against itself replaced by a router, and TWO of the driving handoffs' own premises retired on measurement; no theme-% change (a correctness fix plus an ingestion path on the existing electoral pillar — Races holds at 99).** **The external facts first, because the repo cannot rederive them.** South Carolina's August 11 contest is a special **primary** under **S.C. Code §7-11-55**, held after the death of the Republican nominee for U.S. Senate, and open to **Republican candidates only** because that nominee had general-election opposition; voters who cast a June *Democratic* primary ballot are barred from it, and the winner goes onto the November 3 general ballot in the deceased nominee's place. Filing closed **2026-07-28**. Verified **2026-08-05** against the SC Election Commission notice (2026-07-13) plus concurring county-board notices and the LWV state guide, and independently corroborated by Ballotpedia, which links an August 11 **Republican** primary page and no Democratic counterpart. It is **not** a special *election*, which is why SC is absent from Ballotpedia's two-item 2026 Senate specials list (FL, OH) — and that distinction is the whole arc. **HO 600 (`36b7731`, STEP 0 `43b7ef6`) deleted a live phantom.** The seeded `senate-SC-2026-special-D` asserted a contest that does not exist by statute. It was **not dormant** — `election_round='primary'` put it into `getPrimaryCalendar` -> `PrimaryTimeline` and into `getDashboardPrimaries`, where it **inflated the Aug 11 tick from 47 to 48 contests**. Its visible footprint was a **count inflation, not a rendered label** (nothing anywhere printed the words "Democratic special primary"), which is the honest severity and is recorded here because the handoff assumed worse. Both halves shipped in one commit — `seed-special-primaries.ts` is a pure `INSERT ... ON CONFLICT` with **no delete pass**, so removing the seed entry alone leaves the row and deleting the row alone gets undone by the next seed run; proven by a full re-seed that upserted only the R row. The delete was one literal id, print-before-delete, dry-run by default, with the zero-candidate authorization gate enforced **at runtime** rather than trusted to memory. **Two HO 600 premises were retired by its own measurements.** `/race/S-SC-2026` was **never in scope** — the race hub reads `getRunoffsForRace`, which filters `election_round='runoff'` and returns **0 rows** for it. And a surface the handoff did not name **is** in scope: `getDashboardPrimaries` runs unconditionally in `CompetitiveRacesBlock` regardless of variant, so the PRIMARIES tab renders on **both** `/` and `/dashboard-classic`. **The defect HO 601 fixed, stated as its class: a container's self-reported type cannot gate its contents.** `parseCandidatesPage` kept a votebox only when the box's specialness **agreed with the page's** (`/special/i.test(h5) !== onSpecialPage -> drop`), where `onSpecialPage` is `isSpecialElectionPage()` — which reads the **`<title>`, and the `<title>` is not the URL**. Ballotpedia serves the FL/OH special-election **article** under the plain `United_States_Senate_election_in_{State},_2026` URL (HTTP 200, `redirected=false`), so those pages self-report as special and their "Special ..." boxes agreed and were kept. SC's page is a genuine regular article that **also** hosts an additional Aug 11 special primary: page-special false, box-special true, so the only box carrying a **published 10-candidate field** was silently dropped and the parser returned `no_candidates`. **Fixing the URL alone would not have worked** — that is the finding that reframed the arc. **HO 601 §1's own rule was WRONG, and its §2 gates caught it before any code shipped.** The handoff specified routing on the `<h5>` alone: any special-marked box -> `senate-{ST}-2026-special-{party}`. Measured across all 35 senate states, that produced **4 unseeded targets** and moved **FL, OH and SC**; FL's and OH's 2026 races **are** special elections, so every primary box on their pages reads "Special ..." while the base ids are their correct and only home — routing on the `<h5>` would have moved all four rosters onto unseeded ids, which the HO 561 gate refuses, leaving the base rows to take an empty roster, HO 564 to correctly refuse the delete, and **both states to freeze silently at stale values**. The shipped rule instead resolves against the registry: **a special-classified box routes to `senate-{ST}-2026-special-{party}` IFF that id is seeded, otherwise to the base id** — because the `<h5>` says a box is a special *contest* but not whether that contest is *ADDITIONAL to a regular one for the same seat*, which is the fact that decides the destination, and the seed registry is the curated encoding of exactly that. Seeded router: **0 unseeded targets, SC the only state changed, FL/OH byte-identical.** **A consequence recorded where it will be read: SEEDING IS NOT ADDITIVE.** Adding a `-special-` seed row now MOVES that state's boxes off the base ids — seed FL and its rosters migrate, the base rows freeze, and nothing errors. That is the same failure the router rejection avoided, **re-entered through a config change instead of a code change**, so the warning lives at the router, in a new `data/special-primary-seeds/README.md`, and in oddities. **Shipped (HO 601):** `5bf1176` pre-flight · `efa858f` code · `0473dc6` docs · `07dd555` the pre-flight baseline relabel · `c4a3c06` the freeze-status residual. The code also carries a **specialness-aware dedup key** (Mark Lynch appears in SC's June R roster at 28.9% **and** in the August field, so a `contest|name` key silently dropped a real second candidacy); the **C2 regular-page fallback** in `scrapeSenateSpecialCandidates`, kept for a reason §0 did not give — the standard pass is **cursor-driven** and HO 600 M2 measured three consecutive ticks stuck on `unit="house"` at cursor **439-462 of 471**, so the senate slice might not reach SC before Aug 11, and a fixed election date cannot be served by a cursor position; and, because that fallback parses a document full of regular voteboxes, the special pass now filters on `isSpecial` so it **writes only its invoked special ids** and cannot become a second writer of base rows on a different schedule. **C3 fixed an instrument that could not read FAIL.** `runSpecialPriorityPass` returned `string[]`, so `scrapeSenateSpecialState`'s `fetchFailure` was computed and then discarded: HO 600 M2 read **`fetchFailures: []` across three ticks that each 404'd**, while the backlog WATCH named that field as its evidence. It now returns `{attemptedIds, populatedIds, fetchFailures, settledSkipped, emptyRosterSkipped, sources}`, forwarded verbatim by the pass-through route. `emptyRosterSkipped` was added **pre-emptively during the fix**, because "attempted, no failure, nothing populated" would otherwise have read exactly like success — the same inversion, caught before it shipped. **Falsified in three legs against prod, twice (the re-run also proving idempotency, 10 -> 10 identical).** Leg 1: **10 rows** on `senate-SC-2026-special-R`, exact name match to the published field, **0 missing / 0 extra**, with `sources[].url` confirming the fallback fired and `candidates: 19` against 10 written confirming only special boxes were taken. Leg 3: FL/OH content identical with **both** branches exercised (FL future-dated so the write path ran and reproduced itself; OH past-dated so `settledSkipped` fired) and **0** `-special-` rows created for either. **Leg 2 is the one to read carefully, and the ship report initially overstated it.** It recorded `isSettled` **consulted and REFUSED** on `senate-SC-2026-R`, byte-identical, `updated_at` untouched. But `isSettled` short-circuits **before the roster is built**, so it refuses every settled row unconditionally and cannot discriminate a substituting roster from any other — and the payload it refused was the **correct June roster**, not a substituting one. So the HO 560 freeze is **exercised on its general case** and is now **STRUCTURALLY UNREACHABLE for substitution**: the router removes that hazard upstream, so no substituting roster can ever arrive at a settled row for the freeze to observe. HO 600 reported the freeze untested; **the honest successor state is narrower than "exercised," not broader.** The router is the first line, the freeze the second, and it stays live and useful for its general case — leg 3's OH `settledSkipped` is that case firing. The generalizable shape is now in oddities: **a fix upstream of a guard can make it unreachable, and the fix's own success report reads as the guard being exercised** — distinct from HO 549/553, where the guard never fired at all; here it fired on a different input than the one it exists for, which no "did it fire?" check can distinguish. **A third premise fell with the arc.** HO 601 §5 guarded `senateSpecialPageUrl` as "correct for FL/OH." **FL and OH never used it** — they are read from their regular URLs — and SC's special URL 404s, so combined with HO 561 S2's 0-of-35 missing standard pages, **nothing currently reaches Ballotpedia through that URL shape at all.** Filed as a loop with an explicit *do not delete on this evidence*: the measurement shows it is currently unreached, not that the case it defends against cannot occur. **Scope held:** `senateSpecialPageUrl` unmodified, no `-special-` row auto-created, `senate-SC-2026-special-D` absent throughout, no Aug 25 runoff row (filed, dated, conditional). Docs: HO 602.

**Also (HO 602), the platform read and read-budget attribution — four HOs of reasoning replaced by one measurement, and a logged burn figure corrected; docs-only, no theme-% change.** **Provenance, because this is the only record.** Every number here came off the **Turso dashboard for org `csu-j3`, read 2026-08-05**. There is **no script and it is not reproducible from the repo** — the HO 584 statutory-facts class, so the numbers, their source and their date are written together. **The platform facts the variance loop was blocked on.** Plan **Developer** ($5.99/mo, **2.5B rows read/mo**, +$1/B overage). DB primary **`aws-us-west-2`** (`libsql://cbt-csu-j3.aws-us-west-2.turso.io`), single group `default`, **no replicas, no branches**. Vercel functions are pinned **`pdx1`** (`vercel.json:2`), which is us-west-2 — so **store and compute are in the same AWS region**. **Two variance hypotheses die on that**: cross-region latency and replica routing are both out. What remains is that Turso's own Developer-plan launch post describes the tier as running on a **massive multi-tenant server**, which is the shape that leaves a median alone while fanning an upper tail. **There is no purchasable lever** — the plan comparison table carries quota, retention and governance rows and **no performance row at any tier**, so a Scaler upgrade is an unfalsifiable experiment and was **rejected rather than run**. The loop therefore closes by **BOUNDING** (a co-located worst case with the margin as a number), never by explaining, which was always the second half of its criterion. **Read-budget attribution, answered by measurement after four HOs of reasoning about it.** Per-query `rows_read` over 30 days: the `bills_agg` sponsor aggregate **110.0M / 317 calls / 347k per call (26%)** · `getParticipationStrip` **91.3M / 106 / 861k (22%)** · `getChamberParticipationContext` **85.4M / 96 / 890k (20%)** · the pre-582 markets spark window **40.7M / 8,660 (10%)** · the pre-582 markets `MAX`-`GROUP BY` join **35.5M / 8,470 (8%)**. **Top five = 363M of 418M, 87%.** The whole LDA family is **~11M, under 3%**. **None of the four standing suspects was the answer** — the loop carried the e2e crawl, summarize, the LDA precompute and real traffic; it was three aggregates. **The `/members` 500 loop and the read-budget loop were ONE fault, and nothing said so.** HO 593 named the two participation queries as the 500's cause, HO 594 indexed them, and **HO 595 materialized them into `member_participation` and removed `member_votes` from the request path — closing 42% of the read budget as a side effect**. HO 582 had taken the markets **18%** two days earlier. HO 595's tombstone says *"Not claimed: this does not touch the `pageErr` #418 OPEN LOOP"* and is **silent on the loop it did close**. Recorded here as a **new statement, not a retro-edit** — 595's block was correct on its evidence. **Append-only correction to the HO 577 burn figure.** The record says CBT "stepped ~10x around Jul 25 and plateaued near **85-90M/day**." The daily series shows a baseline of **3-5M/day** through ~Jul 22, a ramp from **Jul 23-24** steepening **Jul 27-30**, and a peak near **38M**. **The timing survives; the magnitude is roughly 2.4x overstated**, and three HOs reasoned against the higher number. **The ramp has a named cause, and it is not the one first proposed here.** A 24-hour Top Queries read showed LDA on top and was mis-read as the ramp — **a 24-hour attribution window shows what is LEFT after the fixes, not what caused the rise**. The 30-day window names the participation aggregates, which shipped **into** the ramp: `getChamberParticipationContext` HO 523 (`2b8d62b`, Jul 25), `getParticipationStrip` HO 527 (`7fa729c`, Jul 26), `participationAggCte` into the roster HO 535 (`b565c13`, Jul 27). Dates verified against `git log`. **The HO 597 prediction is OWED AND UNSCORABLE, not failed.** HO 594/595/597/598 all landed Aug 3-5 — the same 48 hours as the `activity_count` backfill (129,000 UPDATEs) and the `lda_names` back-catalogue sweep — so the fixes and their one-time cost are **confounded**, and the day-matched quiet day the prediction requires does not exist yet. Direction is not in doubt: **~60% of the burn was removed across Aug 1-4** against a logged estimate of *"roughly a fifth, a level shift not a resolution."* **Re-read Aug 8-9 on a quiet day** and score it against the stated constraints rather than letting it age out. **Cap risk is low**: 418M against 2.5B over 30 days; even sustained at the 38M peak that is ~1.1B/month. **The lesson that outlives the numbers** (oddities): **rows read and milliseconds are different currencies**, and this is the strongest instance yet — a covering-index scan documented in-code as *"index-only, 96ms"* (`lib/queries.ts:6015`) is the **single largest read-budget consumer on the database**, invisible to every latency instrument, visible only to per-query `rows_read`. Docs: HO 602. **Also notes now run through HO 602.**

**Also (HO 603–604), the layout arc's first phase — three orphaned arcs given a home, then the viewport type scale shipped in three commits that can each be reverted alone; no theme-% change (a global type-plumbing change plus doc custody — no surface gained or lost a feature).** **HO 603 was custody work and has no block above this one, which is why the range starts at 603.** Part A filed three arcs that existed only in conversation into `docs/backlog.md` — Absence Watch (its R2/R3 fork recorded UNRESOLVED, and its cost half explicitly EXPIRED because HO 594/595 rewrote the participation path it was priced against), the HO 590 dashboard declutter (whose mock **does not exist on disk**, so the backlog entry *is* the artifact), and the C1–C8 layout audit. Plus the display-orphan sweep including the dead variant fork (`1adf056`) and the `isSettled` router comment (`3bc619e`). **The lesson that entry carries: a backlog entry that points at a file is only as tracked as the file** (oddities). **HO 604 then shipped C2, the type scale, as C0/C1/C2 + a rider.** **C0 (`c943e07`)** landed `docs/design/dashboard-layout-target.html` (recovered verbatim from `~/Downloads`, SHA-256 checked) and `layout-conventions-arc.md`, the plan of record — **but its second artifact, `layout-conventions-c1-c8.html`, did not land** — absent from disk at sweep time; a copy survives in the design chat and lands as its own `docs(design)` commit if relayed. The second mock in this arc to go missing this way. The arc doc's §0 pointer to it is knowingly dangling. **C1 (`4185325`) is the load-bearing idea: substitute at IDENTITY first.** Six `:root` tokens `--fs-9..--fs-14` declared equal to the literals they replace, so the 714-site substitution — 353 in `globals.css`, 17 in `landing.module.css`, 344 across 82 TSX files — renders byte-identical and the ladder becomes a one-file change that reverts alone. Eleven regexes, not 714 edits, because **714 of ~827 type sites are six values**. Two traps were caught only because the numbers were taken twice: `landing.module.css` writes `font-size:11px` with **no space**, so a regex tuned to the `globals.css` style would have skipped all 17 of its sites and reported clean (the HO 549/553 shape — a pattern that structurally cannot match reads identical to nothing to match); and a **coverage grep must be run BEFORE as well as after** — it read **714 → 0**, and a grep pointed at the wrong path returns 0 both times and looks like a clean sweep. **C2 (`fd069b9`)** is a flat **+1px per stop at 1600/2000/2400**, chosen over the multiplicative alternative because it keeps every token an integer at every stop while still walking the mock's 12→13→14→15. **Coverage, and why the C1 gate could not have produced it: 0 missed sites, calibrated against the pre-C1 build where the SAME instrument reports 496 missed / 0 scaled — the same 496 signatures.** A computed-size diff across an identity change **cannot detect a missed site** (a site left as literal `11px` renders 11px, exactly what `--fs-11` renders), so it is a *breakage* gate that reads zero on a no-op; only the 1440→2560 delta reads differently depending on whether the work happened. At 1440 the rendered output is byte-identical to pre-arc across seven routes. **The rider (`5b9c34c`) repo-ignores `docs/handoffs/`** — Tailwind v4 honours a repo `.gitignore` but **not** `core.excludesFile`, so local builds were compiling class names out of handoff *prose* into five phantom rules that the deploy never had; the defect is **build-input parity**, not shipped dead CSS. **Prod-verified at `5b9c34c`**: three ladder stops live, `--fs-11` 11px→14px between 1440 and 2560, no horizontal overflow, three identical passes. **Deferred and named, not dropped:** C2's other half (row padding + fixed-size glyph strips stepping at the same breakpoints) — the ~68 CSS one-offs and 34 SVG `fontSize` attributes don't scale, so the hierarchy compresses at wide widths; that is recorded in the ladder comment as bounded and real, not a bug. Docs: HO 605. **Also notes now run through HO 604.**

**Also (HO 605–606), the layout arc's doc sweep and then the audit — an instrument that refused its own first crawl; docs plus one read-only diagnostic, no theme-% change (nothing user-facing moved).** **HO 605 (`de08df2` + `bbe062b`, via review ref)** swept 603–604: the roadmap block above, the `backlog.md:130` rewrite (both its claims had gone false — `NOTHING WAS BUILT`, and the `layout-audit-591.ts` filename), four oddities, the arc-md renumber, and a ride-along correction to the HO 603 oddity whose own 784 figure was short for precisely the reason that entry is about. It also split the deferred **padding half of C2** into its own backlog entry — remediation, not measurement — and caught a live self-contradiction: SKILL's Layout grid paragraph prescribed `max-w-[1200px]` on a sparse column at 2400px+, which is **exactly what C8 rejects**, so landing C1–C8 beside it would have put SKILL in contradiction with itself; it now defers to C8 and records the supersession. **HO 606 built the audit (`e794194`) and then fixed it (`94f6543`), because the falsification leg REFUSED THE FIRST CRAWL — the single most useful thing this arc has done.** Pointed at `/` before any zero was trusted, M1 reported a **10px** interior gap on the breaking-headline row whose headline visibly ends ~1,100px from its timestamp, and M2 read **zero**. **The cause, made twice: layout boxes were measured instead of ink.** A `flex: 1` child stretches its box across exactly the whitespace C1 is about, so the box-edge gap is ~0 while the visible gap is enormous; M2 had the same defect one level down, where a stretched last child hides its panel's slack. Fix: measure to the **deepest ink** — the painted box when an element has one (a bordered chip *is* its box), else its text client rects. Same row, same threshold: **10px → 1,092px**; M2 on `/` **0 → 3**. **Thresholds were never touched — only the measurement definition**, which is the distinction between correcting an instrument and tuning one. A third bug hid in the diagnostic written to *explain* an M2 zero: it evaluated on a **fresh blank page** and printed `[]`, indistinguishable from "this route has no such element" — the fourth same-as-success instance this arc has produced, and the one that landed in the place whose entire job is explaining a zero. **The numbers, with the correction the run itself forced: 27 distinct pages, not 30.** `/races` and `/primaries` 308 to `/electoral` and `/members/pass-rate` 307s to `/members?sort=passrate` — redirect aliases §3 missed when it excluded `/committees` and `/dashboard-v2` for the same reason — so site-wide M1 is **1,329 deduped**, not the 1,902 the raw table shows. Over **6,082 candidate rows**: M1a 362 raw · M1b 2,299 raw; **M2 29**; **M4 1,170 elements holding 102,450px of reserved vertical space**. Raw counts inflate because one visual gap is counted once per ancestor wrapper (2,661 → 1,902 on an innermost-of-chain dedup). **The gap distribution is bimodal with a gulf at 60–120px** — 1% of 72,913 measured gaps — so the 120px threshold sits in empty space and is **defensible rather than assumed**, which is the thing a threshold usually cannot say for itself. **M4's seed vocabulary caught 14 of ~1,170 hits**; the candidate-emitter found the rest, which is the vocabulary closing on measurement exactly as designed rather than on the `NO MEETINGS`/`—`/`NO DATA`/`NONE` guess. **The table's top three are three different problems, which is why they are worth taking together:** `/members` (565 M1b singleton wide rows, and the worst narrow-width surface by an order of magnitude — C1/C8, wide rows that should be panes), `/changes` (M3 **14.34** against a 2–5 norm, plus 42,255px reserved — C3/C4, not C1 at all), and `/lobbying` (78 of 88 are **M1a** repeated-row anchors — the canonical C1 defect and the cleanest test of whether the fix generalises). **Burn on the record: 80 page loads, 131.4s, 0 retries, 30/30 HTTP 200, zero routes unmeasurable.** Output was pasted and never committed; full per-route detail sits in the repo-ignored `docs/handoffs/606-artifacts/`. Docs: HO 607. **Also notes now run through HO 606.**

**Also (HO 608–610), P3 of the layout arc — the dashboard rebuilt against the committed mock in three slices, then the instrument that scored it rebuilt too; no theme-% change (every surface kept its features; only their arrangement moved).** **The slice shape, and the numbers each slice actually moved on `/` at 2560, all under instrument v1 so they compare (the 49 is the 606 crawl artifact's own figure; the 31 and 22 are from the rebuilt same-day crawl chain, since this HO's re-run had destroyed that artifact — in-session prints at 608 and 609 read 50 and 32, and the ±1–2 is live-data churn, not transcription: `f031673` itself measured 32 and then 31 forty minutes apart in one session):** 606 baseline **M1 49 · M2 3 · M3 8.41 · M4 11 elements / 1,190px** → **HO 608** (`2a1bd90` audit touch-up · `1d9c339` dashboard-classic removal · `5fa764b` the two-region shell) + **HO 609** (`938f0ca` compact two-line rows · `f031673` internal feed scroll + viewport-anchored popover) together took it to **M1 31 · M2 3 · M3 6.06 · M4 3 / 758px** → **HO 610** (`72d63a0` hearings interior · `730b792` packing pass · `2ca2c96` panel collapse · `a620e20` cleanup) closed at **M1 22 · M2 0 · M3 5.18 · M4 1 / 42px**. **The site-wide delta is exactly the two dashboard routes: 24 of 26 routes byte-identical across the full pre/post crawl** — M1 2,152→2,131, M2 23→17, M4 93,568→92,136px, every unit of which is `/` and `/?stage=committee`. That flatness was the control and it held perfectly. **Two of 608's §6 expectations missed, and both diagnoses are worth more than the predictions were.** It predicted **M2 3 → 0–1** from `align-items:start`; M2 stayed **3** through both 608 and 609, because the three hits were never the grid — they were the hearings week grid's empty day columns, which no shell change could reach and which only died at 610. And it set **nav one row at ≥1200px**, which the 608 HALT diagnosed as the **8×54=432** arithmetic: the nav had just moved *into* `.dash-left`, and the feed column takes ~432px, so the target was computed against a container the element no longer lives in (retracted in backlog with that shape). **HO 610's own three slices.** **The hearings interior (C4)** replaced the five-column week grid with the mock's `.hbar` / `.rib` / `.sched-wrap`: a day bar that falls through to the next day with meetings rather than ever showing an empty one, a five-cell ribbon where an empty day costs **one dash** instead of a 358px "No meetings" box, and the shown day's schedule. That single change is where **M2 3→0** and **M4 758→42px** came from, the 42px residue being the HEARINGS tab label. **It also reversed HO 282's height pin** — the box was pinned to the RACES footprint so a tab switch never resized it, but the day schedule is about a third of that height, so pinning would hold ~240px of dead space open under the DEFAULT tab, which is what C7 and C4 forbid; the cost, stated because it undoes a deliberate decision, is that a HEARINGS↔RACES click now shifts the week strip below it. `RacesBoxTabs.tsx` was untouched (CSS only), and the tab's TYPE/CHAMBER filter bar went with the grid — a glance surface, with the filters one click away at `/hearings` via `→ ALL`. `/hearings` itself is unchanged and still shares the per-meeting detail card, extracted to `HearingDetailCard.tsx` in the same commit so the two cannot drift. **The packing pass (C1)** put breaking rows, the masthead and the week strip on the mock's packed-left flex, and **deviated from the mock on the masthead**: the mock's `.mast` pins SIGN IN right with a `.sp{flex:1}` spacer, which IS the C1 pattern (SIGN IN sat ~700px right of LAST SYNC at 2560), and **SKILL owns the conventions, so the mock loses that one**. It also fixed `.race-grid`'s pre-existing 1200–1399 overflow — **not with another breakpoint, because a breakpoint cannot express it**: the feed column appears at 1400 and takes 400px, so the columns the container holds run **2 → 3 → 2 → 4** across viewports 1200/1300/1440/2560, non-monotonic in viewport and monotonic only in CONTAINER width, which is what `auto-fit` reads. Swept 700→2560: scrollWidth ≤ viewport everywhere (was +120px at 1200, +20px at 1300, and a 327px grid-over-container spill at 1440 that no document scroll revealed). **The panel collapse** gave `BillExpandPanel` a container query on its own width: inside `/`'s 400px column the panel is 372px and the 1.35fr/1fr body resolved to **99px/273px — 71px of prose, 1.4 words per line over 48 lines** — and the fr ratio was not what decided it (the metabox's ~273px min-content is a floor the 1fr track cannot go under, so the whole squeeze lands on the summary). Container, not viewport: `/bills` renders the same component at 1108–2228px and never crosses the threshold, so no route branch exists. After: **6.9 words/line over 10 lines**. **A live CSS bug surfaced on the way in and had been shipping since HO 570.** The comment above `.bxp` listed swept classes with glob stars, and the star-slash pair inside `-cmte-*` **closed the comment**; the parser then discarded the `.bxp` rule that follows, so `padding: 14px 16px 16px` was **dead in every built stylesheet for forty handoffs** — the panel measured 372px wide with a 372px body — and the only trace was one `Unexpected token Delim` warning in the build log that nobody read. It is also what silently dropped `container-type` on the first attempt at the collapse. **Two premise corrections the arc forced.** The feed rows are **`V2FeedList`, not `BillRowList compact`** — they have been since the HO 311 swap, and the handoff chain had been carrying the older name. And **HO 300's `overflow-visible` on `home-feed-panel` was a stale claim on main, in both halves**: the card it named is `SponsorHoverName`, which renders on `/bills`' `BillRow` and **has never rendered in that column**, and its "safe because the feed has no max-height" clause expired when HO 609 capped the column. Removed and re-reproduced at 1440 on a feed scrolled to its end with a row expanded — the title popover renders at the identical rect either way (it is `position:fixed` since 609) and the expanded panel's own `.v2f-sc-card` is bounded by `.v2f`, an INNER ancestor the rule never governed. **Then the instrument, because the fix broke the thing that scored it.** Removing `/`'s last stretched panel made the M2 falsification leg — whose anchor was "M2 must fire on `/`" — **refuse the crawl that would have scored the removal**: a successful remediation rendered indistinguishable from a dead detector (`2469404` re-probes fallback routes and names where it fired). **The 22 residual M1 rows then decomposed into 14 design / 5 instrument / 3 predicate**, and only the first was a real finding: 14 were stage-funnel rows, where the space between a short bar and its value **is the encoding**; 5 were packed rows that v1 scored anyway because it bucketed items on `round(centreY/4px)`, and a baseline-aligned 13px id and 17px headline fall in different buckets, so it measured id→age **with the headline sitting invisibly between them** — 873px of gap on a row with none; 3 were `.home-shell`/`.dash-page`/`.dash-left`, whose "gap" is the two-region gutter C8 asks for. **So instrument v2 (`e6bbc08`) changed four things and no threshold:** vertical-interval clustering replaces centre bucketing (under a six-token type ladder, mixed sizes on one baseline are the NORM, so v1 could not read any real row correctly); an M1b candidate with a child taller than 60px is a container, not a row; rows under `[data-viz-row]` leave M1 and are **counted** on their own M1x line, because an uncounted exemption grows silently; and the artifact is SHA-stamped and never overwritten — v1 wrote a fixed filename, and **this HO's own re-run destroyed the 606 baseline it was meant to be compared against**, which had to be rebuilt by checking out `f031673` and crawling again. **v2 was falsified with its anchors INVERTED**, because HO 610 packed the rows v1's anchor was built on: the known-GOOD rows must now read SMALL (masthead **12px**, breaking **24–50px**, against v1's 75px and 873px for the same rows — wrong in BOTH directions), the viz exemption must be VISIBLE (13 exempted, 0 funnel rows scored), and a known-BAD control must SURVIVE (`/members` holds **548**). The bound on the good rows is **derived, not tuned to pass**: the id column is a fixed 5.8em (93px at 2560), so a short id contributes basis-minus-text of alignment slack before the 10px gap — 93−53+10 = 50 for `S 5201`, 93−79+10 = 24 for `SJRES 181`. **Legs A and B both assert that v2 reports LESS than v1; alone they are satisfied by an instrument that reports nothing, and leg C is the only thing separating a correction from a silencing.** **Under v2, `/` reads M1 0 · M2 0 · M4 42px, with 13 viz-exempted** — a true zero net of a counted exemption, on the route the whole phase was about. `/?stage=committee` keeps **one** row: `.active-filter-strip` at 1,347px, conditional chrome that only renders when a filter is active and was therefore invisible to every unfiltered pass. **The v2 full crawl IS the P4 baseline, and it is a different instrument from every number above it** — site M1 **1,634** (M1a 327 · M1b 1,307), **M1x 26**, M2 17, M4 968 / 92,136px — ranking **`/members` 548 · `/changes` 471 · `/stale` 153 · `/bills` 78 · `/lobbying` 69 · `/news` 62 · `/trades` 55**. **P4's order is ruled and it revises 606's:** phase order holds, `changes`/`stale` stay P6 and lead it as a pair; within P4 **`hearings` FIRST** (the 610 hearings interior transplants nearly whole, and `/hearings` now carries **M2 9**, the largest on the site), then **`members`** (the C8 panes work), then `bills`/`news`/`trades`, with **`lobbying` last now that its generalisation-test role is spent**. **Narrow widths moved slightly the wrong way and are recorded rather than buried:** on `/`, @720 overflow 82→89 and extra-wrap +1 at both 900 and 720 — the ribbon and the now-longer masthead line wrap. P3 is a wide-viewport phase; the M5 debt is a backlog entry carrying those numbers. **Prod-verified twice** (`a620e20` for the four code slices, `e6bbc08` for v2), each `/api/version` to the served SHA plus a converged read. Docs: HO 610 (this block). **Also notes now run through HO 610.** *(superseded by the HO 611-615 block below.)*

**Also (HO 611–615), P4 of the layout arc — the feed routes packed, then the phase closed by rebuilding the instrument that scored them and moving its anchor off the product entirely; no theme-% change (every surface kept its features; only their arrangement moved).**

**READ THE ERA LABEL ON EVERY NUMBER BELOW. v1 / v2 / v3 are three rulers and they do not compare.** The P4 baseline is v2 (`e6bbc08`); P4's own before/after is v2→v2 across two DOMs; the phase-close crawl is v3 (`f40768f`) and is reported twice — once against the same DOM under v2, so the instrument delta is isolable, and once as the P5/P6 baseline. **A route whose number fell because v3 stopped lying about it is an INSTRUMENT NOTE, not a remediation claim, and this block never launders one as the other.**

**P4's remediation, v2 throughout so the routes compare.** Site M1 **1,634 → 312** (M1a 327→108 · M1b 1,307→204), M2 **17 → 8**, M4 **92,136 → 65,278px**. Per route: **`/members` 548→21** (HO 612 `c531177` — one grid line, the `.mc-row` name track `1fr`→`minmax(0,240px)`; plus `3a2b3f1`'s HeaderBar titlebar, −2 on every route, and `8a23ff9`'s ≥1750px second roster pane) · **`/lobbying` 69→64** (HO 614 `13203eb`) · **`/trades` 55→52** (HO 613 `5e272d8`) · **`/news` 62→0** and **`/bills` 78→1** (HO 613 `3ba8c29`/`5d63ab4`, HO 614 `718d2ab`) · **`/hearings` →0 with M2 9→0** (HO 611 `f2a28dc`, the week ribbon + agenda replacing the five-column grid) · plus `c42ddb1`, `ActiveFilterStrip`, the conditional chrome every unfiltered pass had been blind to. **The two largest movers were never worked: `/changes` 471→1 and `/stale` 153→51, entirely by shared-component inheritance from the feed-row packing** — the phase's best single argument that C1 is a convention defect and not a per-route one, and why `/changes` enters P6 already M1-clean.

**THE ANCHOR-EXPIRY TRILOGY, and it is the arc's most reusable lesson because it happened three times in four handoffs while the rule for it was already written down.** HO 610's oddity said a falsification anchor pinned to a specific known defect has a shelf life exactly as long as the defect, and a remediation phase is in the business of ending them. **612 planned for it** — leg C was re-anchored from `/members` to `/changes` in its own commit (`63c7cd2`) because 612 was about to take `/members` to ~20 by design, at which point a silenced instrument and a successful fix read identically. **613 was saved by the fallback list rather than by foresight** — leg C walks a list and passes on the first route that fires, so when `/changes` collapsed to 1 the leg fell through to `/stale` (51) and the crawl proceeded. **614 exhausted it**: its §2 preamble had to grep the components it touched against `/stale`'s render tree *before the first commit*, because the fallback list had no depth left. **615 ended the pattern instead of extending the list.** Leg C is now `scripts/diagnostic/fixtures/layout-legc.html` — committed, loaded over `file://`, versioned with the instrument, reachable by no route and by no remediation. Four positives, four negatives (each a preserved false positive of an earlier instrument version), one calibration block asserting M1x = 3 exactly. **Positives catch silencing, negatives catch over-firing, and both directions now hold permanently in one leg.** `/stale` retires as an anchor with thanks; the M2 product-fallback probe retires with it, since the fixture's POS-2 / NEG-4 pair proves the panel detector directly for three fewer page loads.

**THE MULTI-RECT SAGA, whose shape is that REMEDIATION PRODUCED THE ARTIFACT.** v2 pushed every client rect as its own measurement item, so a cell that wrapped onto two lines became two items and its short last line could be measured against the next cell **with its own long first line sitting between them, invisibly**. C1 packing is precisely what lets a long cell wrap instead of being pushed out of view — so the artifact grew with every fix the phase shipped. It was hit at 613 (`/bills`' filter bar, a 223px residual proven to be the search input's painted box sitting in the next band), three times inside 613, and then at 614, whose commit body records it as *"the first the wrap fix itself caused."* **v3 (`f40768f`) measures INK SPANS**: one item per child node, extent = the union bbox of its rects, present in every band it overlaps, so no gap is ever measured across it. The measurement then forced a second rule the plan had not anticipated — **the continuation-band cut**: a band whose member set is a SUBSET of another band's is not a line, holds no item not already measured somewhere richer, and can only ever report a gap across items the fuller band contains. The live case was a `/trades` row whose second band held exactly `{date, amount}` and reported the 625px between them — an area the reader sees occupied by five single-line cells. Membership, never position or count: a genuine two-line row keeps distinct items on line 2 and is still measured.

**THE REMEDIATION KIT, as it actually accreted — eight moves, each earned by a route that resisted the previous one.** **Decompose before designing** (614's 67 split into four families of which one was the commit's; 613's `/bills` bar carried TWO pins). **Derive, don't tune** — the cap is arithmetic or it is a curve, never a guess. **The knee** (`/members` 240px = 16 names clipped to buy 11 rows; `/trades` 280px, just above p75, = 2 of 50 live) — with its precondition, that **when the label-ink SPREAD exceeds the 120px threshold no cap satisfies both "no truncation" and "under threshold" at once**, which is why both routes bought their last rows with truncations and filed the trade rather than taking it. **The two-ended derivation** (614: `cap = threshold + min-ink − column-gap = 120 + 239 − 14 = 345`, taken at 340 — derived against the SHORT end, because a cap sized off the widest label leaves the shortest one `cap − ink` of slack; HO 612 measured that directly, where a widest-name+2ch cap of 340px left the shortest name 231px of gap and took 548 to 527, not to ~0). **The wrap-cap** (614's rc cell is two names joined by an arrow and can wrap, so its cap cost zero truncation where a member name or a ticker description could not). **Wrap, don't pin** — the standing narrow-width guard. **Dependency-grep the anchor** (614 §2: prove leg C's route shares nothing with the work by dependency, not by route name). **Re-anchor rides the expiring commit** (612 `63c7cd2`).

**The chip rule got its reference row, and it demonstrates both halves at once.** `FilingRow` on `/lobbying` (HO 614 `13203eb`): **31 issue codes became plain dim text with 0 borders** because they are labels, and **181 bill chips kept their `1px solid rgb(217,119,6)`** because they are links with hover. The C3 rule since HO 609 — a border survives only on an interactive affordance — now has one row that shows the line rather than describing it.

**THE PHASE-CLOSE COMMITS (HO 615).** **`f40768f` instrument v3** (above) · **`8a05ad4` the exemption extension** — `FirmsLeaderboard` and `TopicCrosswalk` marked `data-viz-row`, `/lobbying` M1 **28→1**, both curves measured before either attribute was written: TopicCrosswalk's void is **bar-end → value** (37–648px, and capping the label track clips all 23 labels at every cap tried while leaving 18–21 rows over), FirmsLeaderboard's is **name → the bar column's fixed origin** (476–699px on 25 of 25, and cap 300px clips 0 while leaving 25 over) · **`294b51a` the rail** · **`9244cb9` `HearingsList` removed** with its 8 selectors, per-selector against a positive control that fired first. **The attribute went on the TABLE and not the row list**, because a column header's geometry is not independently choosable — it has to sit over the columns it labels, and both headers were over threshold (742px, 1051px) for exactly that reason.

**Three step-0 premise corrections, all MEASURED rather than reasoned, which is the only reason they were found.** (1) **`/trades`' 52 are not multi-rect artifacts.** They were expected to drop to ~0 under ink spans; the artifact inflated the MAGNITUDE (1137→248px) while the count fell only 52→32, because the row's FIRST band reads 129–248px of real column slack against the amounts axis and is over threshold either way — a genuine C1 residual, not an instrument one. (2) **`/lobbying`'s "11 artifact rows" is a subset, not a route total** — v2 reads 64 on that DOM, decomposing to ≈48 axis + ~11 artifact + ~5 other. (3) **`/committee/[id]` has no `.mc-pane` at all**, so "possibly, check its roster embed" is answered negative rather than left open. Each of the three had a plausible reasoned answer; none of the three reasoned answers was right.

**THE 883 WAS A JOINT CONSTRAINT AND THE FILING NAMED ONLY ONE HALF OF IT.** The HO 612 entry recorded "a 312px rail that never collapses" plus "a hard 883px minimum," which reads as one cause with a restatement. Measured: at 720 the pane is 688 and the content column 374, and the element setting the document width is a `.mc-r-num` cell whose row grid needs ~546px. **Neither the rail nor the row is the defect alone** — the rail is what leaves 374 for something needing 546, and relieving either one clears it. The same filing listed `/lobbying` `/bills` `/news` as sharing the class, which is a **class-presence claim, not a failure claim**: measured at 720, `/members` 883 (+163) and all three others exactly 720. So the fix hooks a new `.mb-pane` class rather than bare `.mc-pane` — widening it would restyle three already-closed routes to fix a fourth.

**THE RAIL REVERSAL, and it is the eyeball clause earning its place in a handoff.** §3 specified the rail stacking BELOW the content, C8's inverse, with "no disclosure widget unless stacking proves unusable in the eyeball." Built as specified, **every number was green and the rail landed at y = 19,643** — the member list is unpaginated at `LIST_LIMIT` 600, so the committee filter became unreachable on precisely the screens the rule exists for. Doc-width passed, bucket (b) passed, the wide layout was byte-identical, and the feature was useless: **no metric in this audit can see "off-screen."** Above and unbounded is no better (236 committees would bury the answer), so the rail is capped at 420px with internal scroll — not a new widget but the HO 492 `.lob-rail-scroll` convention, shipped one route over for the same reason. Acceptance: `/members` doc-width @720 **883→720**, bucket (b) **0/0/0** at 1024/900/720, 1024+ byte-identical (pane 992 / rail 312 / content 678 either side, and the media query cannot reach above 900 by construction).

**"The instrument was right; the fixture was wrong."** Leg C's NEG-2 — the wrapped-cell negative — was authored with a right-aligned trailing cell, which put a genuine 135px far-right anchor inside the row that exists to isolate the phantom. v3 reported it and the leg failed. **The correct response to a failing negative control is to check whether the control is clean, not to relax the assertion**: the phantom itself was already gone (590→61px once the second defect came out). A negative must carry exactly one property — the thing it denies. The same pass caught a first `/trades` leg-A bound of 200px **sized off one sampled row's 129**, re-derived to 400, which sits in the empty space between the two measured populations (real slack 129–248, artifact 625–1137), so the gate asks whether any artifact reading survived rather than whether the number got smaller.

**Comments are hypotheses, and this phase produced a base rate for it.** Three orphan claims tested across four handoffs — **one true-but-already-actioned, one false, one stale**: `FilingRow.tsx:5`'s claim that `IssueDrill` was orphaned was TRUE, but the file had already been deleted at `0765aaf`, so the `git rm` branch could not fire and HO 614 corrected the comment instead (`cc1b32f`). HO 611 §3's claim that the hearings embedded mode still existed was FALSE, and commit 2's own falsification is what surfaced it (`42efcee`). HO 610 found HO 300's `overflow-visible` note stale in both halves. **One in three of this arc's load-bearing comments was wrong about the code beside it.**

**THE P4 GAP, owned here rather than discovered by whoever opens P5.** P4's scope list in `layout-conventions-arc.md` §1 is eight routes: `/bills` `/news` `/amendments` `/nominations` `/hearings` `/members` `/lobbying` `/trades`. The HO 610 order ruling named *"hearings FIRST, then members, then bills/news/trades, with lobbying LAST"* — six — and **`/amendments` (v3 20) and `/nominations` (v3 15) fell out of the phase without ever being declined.** No handoff dropped them; an ORDER RULING was read as a SCOPE LIST, and each subsequent HO took its route from the order. **The reusable shape: an order ruling is a permutation of a scope list and can silently be a subset of one, so a phase closes by diffing against its SCOPE LIST, not against the order it was worked in.** Neither is a P5 or P6 route; both are carried into a P4-residual HO in the backlog.

**The v3 phase-close crawl — 26 routes, 72 page loads, 77.4s, 0 retries — reported both ways so the instrument change is isolable.** **Same DOM (`cc1b32f`), two rulers:** v2 312 → v3 180, i.e. **−132, of which only 27 is product work** (the exemption). The remaining 105 is the instrument no longer lying, and it is concentrated exactly where the wrap artifact lived: `/reports` 16→1, `/trades` 52→32, `/patterns` 11→3, `/welcome` 8→1, and `/lobbying` 64→28 before the exemption took it to 1. **M2 (8) and M4 (709 elements / 65,278px) did not move at all** — a clean control that v3's changes were confined to M1 and M5. **The v3 P5/P6 baseline:** site **M1 180** (M1a 47 · M1b 133) · **M1x 151** · **M2 8** · **M4 709 / 65,278px**, ranking **`/stale` 47 · `/trades` 32 · `/committee-detail` 28 · `/amendments` 20 · `/members` 16 · `/nominations` 15 · `/member-detail` 4 · `/bill-detail` 4 · `/patterns` 3 · `/vote` 3 · `/electoral` 2**, with **`/` `/bills` `/news` `/hearings` `/changes` `/search` `/president` `/race-detail` at 0**.

**M1x IS A CANDIDATE COUNT, NOT A ROW COUNT, and the exemption extension is the first marking large enough to make that visible.** `/lobbying` reads 125: itemised, **50 are actual rows** (25 firm rows + 1 header + 23 topic buttons + 1 header) and **75 are wrappers and tracks** that pass a width-based candidate gate — a 1255px bar track and a firm-name span stretched by its `1fr` column are both ≥600px wide. The same inflation has always been inside the funnel's 13. The handoff predicted "~48 into M1x" and "M1x ≈ 13 + the new count"; neither maps, because M1 counts over-threshold rows and M1x counts candidates. **The actionable number is the M1 drop.**

**M5 gained its (a)/(b) split and immediately retired a worry this arc had been carrying for four handoffs.** M5x = the element's OWN style declares `text-overflow: ellipsis` or a line clamp — authored, permanent, not actionable; bucket (b) = real. **`/members`' "worst narrow-width surface on the site," 721/217/1301 overflowing elements at 1024/900/720, is 100% M5x.** Every one is an authored `truncate`. There was never any real overflow there. **The honest caveat, recorded rather than buried:** M5x captures ellipsis and clamp only, so `/`'s bucket (b) of 86–88 is **the markets marquee counted at six nesting levels** (`markets-tape--scroll` 7519/976, `-track` 7542/7538, `-half`, `-set`, `-item`…) plus the three ancestors carrying its +85px document overflow at 720, and `/bill-detail`'s single (b) at 720 is `pre.bdp-rawpre` (710/686). Neither is narrow-width breakage. A scroll-container-by-design third category is filed in the backlog and deliberately NOT taken here.

**Prod-verified converged** at `9244cb9` — `/api/version` to the SHA, then the served stylesheet read directly: `.mb-pane` present with its `max-height:420px` inside `max-width:900`, `hearings-band-head` and `hearings-empty` at 0, and `.hearing-row` at 7 as the positive control that the grep can still see a live rule. Docs: HO 615 (this block). **Also notes now run through HO 615.**

**Also (HO 616–620), the layout arc's close — P4-residual, P5, P6a and P6b worked to their scope lists, then the instrument rebuilt one last time so the terminal crawl could be trusted as the maintenance baseline; no theme-% change (every surface kept its features, only their arrangement moved).** Five HOs, twelve route commits, two corrective docs commits and two instrument commits, and the arc ends on an **arithmetic identity rather than a target**: site M1 equals the named residue register, site M1x equals the exemption registry, and nothing is unexplained. The terminal crawl (`62c25e4`, 26 routes, 72 page loads, 76.3s, 0 retries) is what every future layout HO reads first.

**READ THE ERA LABEL ON EVERY NUMBER BELOW. v1 / v2 / v3 / v4 are four rulers and they do not compare — and this block never launders an instrument note as a remediation claim.** The full chain, every arrow attributed: **v1 2,152 → 2,131** (product — P3, and 24 of 26 routes were byte-identical across it) **→ v2 1,634** (instrument) **→ 312** (product — P4) **→ v3 180** (−132 on the same DOM, **of which 27 is product** and 105 is the instrument no longer lying) **→ 27** (product — P4-residual at HO 616, P5 at 617, P6a at 618) **→ 17** (product — P6b at 619) **→ v4 17** (instrument, and this arrow is **flat by construction**). **The bridge control, which is why the run was done twice on one DOM:** v3 and v4 read `62c25e4` and **M1 17 · M1a 0 · M1b 17 · M1x 332 · M2 3 are byte-identical across the two rulers**, because v4 touched only M3, M4 and M5. **The M3 and M4 arrows are instrument, all of them** — v3's M4 601 / 57,458px partitions exactly into v4's **M4-true 14 / 853px + M4f 587 / 56,605px**, an identity, not a re-measurement, and no route's M3 movement between the two columns is a product claim.

**HO 616 — P4-residual, and its whole value is that four routes with the same-looking defect got three different answers.** The axis question — *does a numeric column on a shared x qualify for the `data-viz-row` exemption?* — was ruled route by route, by applied curve, never by pattern. **`/trades` 32 → 1: the attribute was BARRED and the fix was structural** (`7eba4be`). A right-aligned money column is clause 1's named non-qualifier; the reorder moved the variable-width asset name **last**, converting per-row interior slack into trailing gap, which is the target state and unthresholded — and HO 613's 280px cap **retired** rather than being re-tuned, clipping going 10 rows → 0 at 2560. **The reorder alone fixed almost nothing, which is the finding:** the row's real gap was type→amount at 129px, two fixed tracks' slack, and the repair was three column widths derived from both closed vocabularies rendered in the real cell across 1,209 trades. Two of them were invisible defects, not gaps: the **amount** track at 110px was narrower than *every* value in its vocabulary, so the money column had been wrapping to two lines on every row — M1 skips a second band holding one item and M4's 12-char limit excludes a 16-char cell, so **neither instrument could see it**; and the **date** track at 64px, verified by HO 613 at 1024/900/720, overlapped the chamber chip by 3px at 2560 because the `--fs` ladder steps every token +2px by the widest stop — *a fixed track checked only at narrow widths is unchecked at wide ones, because the type in it is not the same size there.* Fixing the wide row then **caused a narrow-width regression** (bucket (b) 0 → 105 at 720, document over by 40px) which `minmax(0,N)` and an 80px-earlier compact breakpoint closed — *a fixed track is a pin in disguise: it cannot give width back.* **`/amendments` 20 → 0 and `/nominations` 15 → 0: the attribute was SPENT, on curves** (`780da6d`). Both render `RankRow`, which the handoff called identical and which reading both files falsified — one was a local function at `1.6fr`, the other inline at `1.4fr` with an active state, i.e. **near-identical, which is worse than identical, because it is drift that already happened**; extracted to `components/RankRow.tsx` with `labelFr` deliberately left a prop. The applied curves say mark: `/nominations` live worst 1,347px and **cap 200 → 2,071px**, `/amendments` live 652px and cap 200 → 917px — every cap worse, because the bar sits in a fractional `2fr` track that absorbs whatever the label gives up, so the dominant void is bar-end → count, *which is the encoding itself: a short bar with a long run to its number IS the small number.* **`/members` 16 → 13: decomposed to accepted residue** (`c400450`), after the **seventh application of one pattern** — a `flex: 1` spacer element pinning the three strip heads' trailing items at 1,843 / 1,671 / 1,534px. **Heads only: the HO 612 fence lifted for chrome and stayed shut on the plots**, and the reason that class stayed invisible for three handoffs is worth the record — *these were carried by a scope fence, not missed by an instrument, which is a different failure mode and a slower one, because a fence looks like a decision every time it is re-read.*

**And HO 616 corrected the arc's own registry rather than defending it** (`a2ca42e` + `1b9d334`). HO 615's `FirmsLeaderboard` entry read *"cap 260px clips 2 names and leaves 25 of 25 over threshold; cap 300px clips 0 and leaves 25 over — a cap cannot reach it."* **That claim was false, and it was false because it was MODELLED** — computed from track arithmetic that subtracts each label's own excess over the cap, which reports *no change* for any cap wider than the longest label and therefore cannot see that capping a `1fr` label track **moves the fixed bar track left**. Applied in the page and re-read: **cap 180px → worst 114px, 0 of 25 over, 10 of 25 names clipped.** A cap *can* reach it. **The exemption stands on the corrected number and not the wrong one** — a reachable cap that clips 40% of firm names is not an acceptable knee beside this arc's others (3% on `/members`, 0.33% on `/trades`) — and **the false claim is quoted in place rather than deleted, because a registry whose corrections are invisible is a registry nobody re-checks.** `TopicCrosswalk` was re-measured the same way and **confirmed with its mechanism attached**: every cap makes it worse (180 → 873px), the modelled curve having landed on the right answer for the wrong reason. The rule went to SKILL as one sentence: **the curve is APPLIED in the page and re-read, never computed from track arithmetic** — *a marking with no curve is a silenced defect, and a marking with a modelled curve is a silenced defect with a number attached.* One further correction the arithmetic forced on the way: the funnel — the **oldest** marking — had only ever filed one unit, and a first draft of the site total asserted 113 visual rows from a guessed row count; measured, it is 13 candidates / 6 rows. **The unit caveat was written at HO 615 and the oldest entry had not been brought up to it.** Site M1x 151 → 310. **P4 then closed on its scope list** — eight routes, every disposition in a permitted form (worked-to-zero / exempted-with-curve / accepted-residue-decomposed), the first enforcement of the SKILL rule HO 615 wrote after `/amendments` and `/nominations` fell out of the phase silently.

**HO 617 — P5, and the two positioned axes got OPPOSITE answers from the same applied method, which is the clearest thing the registry has produced.** `/committee-detail` 28 → 0 · M2 2 → 0 · M4 11 elements / 662px → 1 / 42px (`a30f063`); `/member-detail` 4 → 0 · `/bill-detail` 4 → 0 · `/vote` 3 → 2 · `/race-detail` 0 verify-only (`cd8c3d7`). **Decomposition first, and both of the handoff's "likely" families were wrong** — not the `BillRow`/`NominationRow` lists (zero contribution) and not the two charts (SVG, never in M1 at all). What it actually was: 27 roster rows on a `grid-cols-[1fr_auto]` pin, one `ml-auto` header link at 1,803px, two grid wrappers stretching — one of them **"Recent activity" holding a single line of empty-state text stretched to 1,042px beside a 27-member roster, 954px of it blank, the largest single stretched panel left on the site** — and ten `WatchCell` spans that rendered `<span className="hearing-watch" aria-hidden />` for meetings with no video, 62px each, **620px of the route's 662px**. **The axis pair.** `.ideology-axis` was **MARKED**: applied curve (axis width → worst gap / scale / party-median spread) 2490 → 2299px / 1195px per DW-NOMINATE unit / 1878px · 1400 → 1209 / 672 / 1055 · 1000 → 809 / 480 / 753 · 700 → 509 / 336 / 527 · 500 → 309 / 240 / 376 — reaching the 120px threshold needs a ~420px axis, **a 6× loss of resolution on the one distance the rail exists to show**. `.v2f-bar` was **CAPPED**: live 2486 → 379px worst · 1200 → 165 · 1000 → 132 · 800 → 98 · 640 → 72 — a cap reaches it, and **position here encodes SEQUENCE, not magnitude**, so compressing costs the reader nothing. Same method, opposite verdicts, and the discriminator is what the position *means* — **magnitude is marked, sequence is capped**, scoped to `.bdp` because `.v2f-bar` is the shared `BillStageBar` and SKILL's HO 507 rule forbids editing it for one page's benefit. **`/vote` produced the plateau curve and the residual was filed rather than chased:** `columns-2 sm:columns-3 lg:columns-4` topped out at 4 columns ~630px wide holding 120–235px names, so a row read 511px across the gutter; `columns-[240px]` gives 9 columns at 2560, and **240/16 is the ELBOW of the applied curve, not a target** — (260,24) → 196px · (240,16) → 160 · (228,12) → 132 · (220,12) → 132 · (200,12) → **1192**. It plateaus at 132 because below ~240 the binding term stops being the column width and becomes the ragged fill of the final column, and 200 breaks it outright. **No width reaches zero, so 2 rows are filed decomposed.** And the HO 617 HALT produced a finding that bounded its own arithmetic: `/member-detail`'s 2,312px of M4 was **not** the reserved empty-state panel §2 predicted — itemised, it is 27 populated elements (a photo, 2 stat tiles, 4 tab buttons, 20 feed-row parts at 99px each because the feed row is 99px tall). **M4 counts SHORT-TEXT elements, not empty ones**, which on any feed-bearing route inflates hugely, and P6a's scope had been about to be set off M4 pixels.

**HO 618 — P6a, where the defect was C4 wearing C1's clothes and the signature was the tell.** `/stale` M1 47 → 0 (`1c1058f`). §1's live question — do the momentum rows carry bars on a shared origin, i.e. is this the registry question again? — **was answered by measuring, not by pattern**: they do not, so no exemption was spent. **All 47 rows read EXACTLY 121px, one pixel over threshold, and a uniform gap is not a content-driven stretch — it is a fixed reservation.** Across all 50 rows: 45 no-HEARD rows at 121px, 4 HEARD at 57px, 1 at 65px; inside a no-HEARD cell, `.row-support` 40px reading "—" then `.row-hslot` **52px reading nothing**. HEARD shows on 5 of 50 rows and the badge right-aligns against the age column either way, so the reservation bought nothing. **M4 could not see it, and that qualifies HO 617's conclusion rather than contradicting it: M4 keys on HEIGHT, and this reservation is 52px WIDE and content-height tall — a horizontally-reserved slot is invisible to M4 by construction.** M1 caught it only because the slot happened to sit between two other cells. **`/stale` M3 13.62 → 6.55, and the party-red age heat died at source** (`65d5eee`), splitting **the same helper by what its two modes MEAN**: `daysSinceColor` painted both staleness and desk-time overrun with `--party-republican` — the oddity filed at HO 610, forbidden by the SKILL party rule since, *because a party token carries party, and a red number reads Republican before it reads old.* **Staleness is not an alarm**: on the route whose entire premise is staleness, per-row red is noise and the number plus the furthest-first sort already carry it, so it returns no colour and inherits the row. **Desk-time is a real threshold** — past 10 days a bill has outrun the constitutional veto clock — so it keeps a hot end, moved to amber-bright, *this app's urgency colour and nobody's party.* §2 predicted `/bills` would tick down with it and **it didn't, for an exact reason**: `daysSinceColor` runs only when `daysSinceMode` is set, and plain `/bills` has no days-since column, so it never consumed the party-red and had nothing to inherit — a convention correction with zero metric movement anywhere, stated rather than left as an unexplained flat number. **The quiet pass is structural, not a list of dimmer colours, and that distinction is what actually moved M3**: M3 counts descendants whose *own* style differs from their parent's, so repainting a label a quieter shade still competes and still scores; the row's base ink moved down to dim and every label below dropped its declaration and **inherits**. **The target was ≤5 and it lands at 6.55, and the last 1.5 was not taken** — all nine survivors are exactly what C3 says keeps colour (title, stage pill, prior stage, chamber rail, sponsor link, watch button, party bracket, momentum signal, heard badge), and the handoff's own guard said hitting 2 would mean something functional had been deleted. **`/changes` closed by verification, in its own words: worked to zero by shared-component inheritance from the HO 613 `BillRow` pack** — the route that proved C1 was a *convention* defect rather than a per-route one, closing without a commit of its own. **Site M1a → 0** with `/stale`'s 47, and it stays 0 through the terminal crawl. A third commit the plan didn't have then closed a real document overflow the guard surfaced (`913bb8b`): `TopicFilter` rendered 24 chips in a `flex` row with **no `flex-wrap`** inside a `.filter-chips` section that *does* wrap, so below ~1,020px both P6a routes scrolled sideways — pre-existing, fixed at source, **one word, and it is the kit's own guard**: *a fixed-width row inside a wrapping parent is a pin wearing the parent's clothes.*

**HO 619 — P6b, the batch, and the convergence the instrument built to score it could not see.** Three commits. **The rails size to content** (`67767f1`): `/members` was paying 2,618px of dead panel and `/bills` 1,807px, and `align-items: start` capped both — **but the scoping was measured, not assumed.** All four `.mc-pane` consumers stretch their rail by default; only two waste anything by it. `/lobbying` reads **−1,958px** and `/news` **−14px** — their rails are bounded scroll containers whose ink *overflows* the box, so capping would shorten a rail that has more to show, **which is the negative-dead case and it was left alone.** And **the seam was load-bearing and could not ride the rail**: `.mc-rail`'s `border-right` is the only thing marking the split on a 2,528px pane, so it moved to the **container** (a 1px full-height background at `--mc-seam-x`) — *because either child can be the shorter one*, and a filtered `/bills` inverts which column is tall. **The chip rule converged by function, measured in the DOM rather than read out of JSX** (`8937235`): 796 topic chips across nine `BillRow` surfaces, and **zero of them are a link, a button, or a filter** — every one is a `<span>` inside a Tooltip. So both of the ruling's open questions were answered *the rule, not the pass*: `/stale`'s chips were **not** links, so HO 618's quiet pass removed no affordance and the border stays gone; `/changes`' chips do **not** filter, so the divergence was **not principled — it was the rule not yet applied, to 428 chips on one route.** Two surfaces keep their border for the opposite reason (the `/changes` and `/stale` filter rows are real click-to-filter links, hrefs measured), and `/changes` now shows both halves on one screen: **24 bordered controls above 132 rows of plain labels.** A separate finding from the same audit: HO 618's `plain` prop **never reached `TopicChips`' responsive MOBILE branch**, so `/stale` still shipped one bordered chip per row to a phone — 50 of 140 — *a prop that styles by function has to reach every branch that renders it*, and that is one of the two citations behind this sweep's second SKILL rule. **And the commit reported honestly that M3 could not score it:** M3 sampled the M1a groups (`kids >= 3`), the chip-bearing rows carry two visible element children, so **the 796-chip convergence was invisible to the instrument built to score row noise** — filed for 620 rather than presented as a null result. **The batch** (`d56608e`) took nine routes, and **seven of the eleven residual rows were ONE family**: a section head with its trailing element pinned to the far right of a 1,400–2,500px bar, by `margin-left: auto`, `justify-content: space-between`, or a `flex: 1` middle cell — *the same three spellings this phase has now removed eight times.* `/patterns` 3 → 0 · `/electoral` 2 → 0 · `/reports` `/trends` `/watchlist` `/report-detail` `/welcome` 1 → 0 each, with `/lobbying` 1 and `/trades` 1 **accepted, priced at HO 615**. `/patterns`' bars header is the arc's last new exemption and ships with an applied curve — void 781px live and **worse at every cap** (180 → 1,166px) while clipping 0 of 5 labels, the `TopicCrosswalk` shape and not the `FirmsLeaderboard` one; M1x **18 candidates / 6 rows** (5 bars + 1 header), site M1x 314 → 332. `/welcome`'s live panel is a **static replica of the pre-609 dashboard row** — which is why it held the site's worst M3 at 8.86, still carrying a boxed id, an underlined non-link sponsor and bordered chips — and the quiet pass mirrored the live reference row on `/` rather than inventing a second treatment: **8.86 → 5.00 with all five survivors named, colour-only, zero borders, zero backgrounds.** One rule fell out of it: **M3 counts a colour difference in EITHER direction, so dimming an element to quiet it scores exactly like brightening one** — the first pass left three survivors scoring only because they were *quieter* than a bright parent, and moving the row's base ink down is the honest fix rather than the metric one. **Two formal acceptances, decomposed rather than inherited:** `/electoral`'s M2 2 × 52px hero-stat band (reopening costs the align-start machinery plus two moved seams to reclaim 98px and buys three ragged-bottom cells where a stat band's job is to read as one aligned row) and `/welcome`'s 775px aside, which is **not a stretched sibling at all** — `.split` is `min-height: 100vh`, so both columns are viewport-sized and `align-items: start` would collapse the landing to a 915px band inside a 1,440px viewport. **Site M1 27 → 17, M2 5 → 3, and the 17 IS the whole of the accepted-residue register.**

**HO 620 — instrument v4, and it shipped rather than being filed because the terminal crawl is the baseline every future HO reads and v3 would have handed it two known lies** (`59a45cd`). Four measurement changes, **no threshold moved**, falsified on the fixture before any route. **(1) M4 splits into M4-true and M4f (furniture)** — the emitter (≥40px tall, ≤12 chars) cannot tell reserved emptiness from a short label doing its job in a tall row, and furniture is *inside a repeated-sibling row AND carrying non-empty text*, **both clauses**, because a genuinely empty box in a repeated row is the `/stale` reservation and must stay in M4-true. The HO 617 hand-split became the instrument's own output, on its own line, the M1x pattern. **(2) M4w — width-reserved slots**, because M4 keys on height and `/stale`'s 52px `.row-hslot` was the whole of that route's M1 while M4 read nothing. **(3) M5s — scroll-by-design**, per the backlog entry's shape, counted **once at the outermost of its chain**, since six levels of one marquee was the whole distortion; the definition deviates from that entry's literal phrasing and says so — *"…with an animated or explicitly-scrollable CHILD"* misses `pre.bdp-rawpre`, one of its own two worked examples, because a `<pre>`'s only child is a text node. **(4) M3 samples every repeated-sibling group with ≥2 element children**, retiring the `kids >= 3` gate it had inherited from M1a.

**`/changes` is the worked example of both lies at once, and it is why v4 shipped.** Its **M3 3.00 was 10 nav items at 3.00 apiece** — the route's feed rows never entered the sample at all, so a number the whole phase had read as "this route is quiet" was measuring the navigation and wearing the route's name; under v4 it reads **11.23**, and **that movement is instrument, not product**. Its **M4 267 elements / 26,523px** — the largest single M4 figure on the site, and the number that would have set P6's scope — is **267 furniture and ZERO M4-true**: 267 short labels in repeated rows. *The headline number was entirely not the thing it is named after.* M1a is untouched by the M3 change, so this is a second, wider group pass and M1 cannot move with it.

**The M4w near-miss is kept in the record because the fixture comment had already named the failure mode and the predicate committed it anyway.** M4w's first draft carried **no negative control**, passed a green fixture, and then fired **359 times on `/members`** — 201 committee-activity bars, 153 topic-mix segments, 4 tracks. **A bar is also an empty element whose width was chosen; it is the exact opposite of a reservation, because its width IS the datum.** What separates them is **paint**: a reservation is invisible by definition. The predicate now requires no paint of its own — **`/members` 359 → 0** — and the fixture carries an identically-sized **painted** segment as the control it should have had first. This is the same distinction `data-viz-row` draws for M1, drawn here by a property of the element rather than by an attribute someone has to remember to add.

**Then the crawl falsified v4 itself, which is why the bridge run is mandatory rather than optional** (`62c25e4`). `/bill/[id]`'s raw-JSON block classified correctly, but `/`'s markets marquee — **worked example #1 in the very backlog entry this column implements** — read M5s 0 and left all 86–88 in bucket (b). Two causes, both measured before either was touched. **(1) The instrument was suppressing the property it keyed on.** The audit runs its context with `reducedMotion: "reduce"`, deliberately, because a moving marquee would perturb the geometry every other measurement depends on — and `globals.css` carries `@media (prefers-reduced-motion: reduce) { .markets-tape-track { animation: none } }`. Measured both ways: no-preference reads `markets-marquee`, reduce reads `none`. **So the computed animation-name of the site's one marquee is `none` under the only conditions this audit ever looks at it.** Animation is now read from the **stylesheet** — an element is animated *by authorship* if any rule matching it declares a non-`none` animation-name, whatever the active media query says about playing it. **(2) `overflow-x: clip`, not `hidden`** — the tape computes to `clip` and the predicate checked `hidden` only; they are one family and both are accepted. **The fixture could not have caught (1), and that is the lesson rather than the excuse:** its marquee case has no reduced-motion override, so it passed under both the broken and the fixed predicate and **proved nothing about either**. Case **v4-C2** closes the gap — the same geometry plus the same `@media (prefers-reduced-motion: reduce) { animation: none }` the app carries — and it fails on the first draft and passes now. **A fixture that does not model the conditions the instrument runs under is not a trap: a preference-conditional style is part of the measured system.** That sentence is now a clause on SKILL's falsification-constraints paragraph. The fix was **sequenced after the closeout commit rather than amended into the v4 commit**, because the crawl is what surfaced it and collapsing that into "v4 shipped correct" would erase the reason the bridge run exists.

**The closeout pair, neither left for the maintenance baseline to inherit** (`25493bd`). **`/bill/[id]`'s three metabox chips are now plain** — HO 619 had left them bordered on the argument that a metabox *field* is not a repeated row and C3's noise target is per-row multiplication, which is true and **is not what the rule asks: the rule asks what the chip DOES.** Grepped, they are the sole remaining non-plain `TopicChips` consumer, a bare render with no href and no handler, i.e. labels. Same rule, no special case, and the last chip divergence on the site closes; the `plain` prop now has no false consumer, **which is not dead code — it is the mechanism the rule needs the day a chip becomes a control.** **`/welcome`'s `landing_readout` wraps** — at 900 the 44% pitch column leaves ~292px and the three stats need 331px, **the last real narrow-width item on the site and, of all places, on the front door.** Fixed by wrapping, not by shrinking the 18px stat padding to buy 39px: *a tuned number goes stale the next time a stat's digits grow, and "wrap, don't pin" is the kit's standing narrow guard.* It **predates P6b** — every changed selector in the HO 619 landing diff was `.winbar` or inside `.row`, and `.readout` is in the other column — so this was never that HO's regression; it was simply the first time `/welcome`'s narrow ladder had been run.

**The fixture reads 18 of 18 exact, and the standard it now holds is the PAIRED CONTROL.** The ten pre-existing readings are unchanged (POS-1..4, NEG-1/1b/2/3/4, CAL M1x 3), and the **eight** new cases ship as *pairs* rather than as bare "the new column is non-zero" assertions: **M4f 6 / M4-true 0** with an *empty*-box control that must stay M4-true · **M4w exactly 3** with the painted-bar control at 0 · **M5s exactly 1** (the outermost frame, inner level absorbed) with an un-animated twin that must stay in bucket (b) · **M5s 1 under a reduced-motion override** (v4-C2, the fixup's own case) · **M3 sampling 2-child rows** at a non-zero mean. A new column asserted only in the direction it was built to fire is satisfied by an instrument that fires on everything, which is legs A and B's both-direction problem in a smaller frame.

**The terminal baseline, v4 at `62c25e4`.** Site **M1 17** (M1a **0** · M1b 17) · **M1x 332** · **M2 3** · **M4-true 14 / 853px** · **M4f 587 / 56,605px** · **M4w 0** — the live width-reservation is fixed, so the fixture carries the only positive. **22 of 26 routes read M1 0**; the four that don't are `/members` 13, `/vote` 2, `/lobbying` 1, `/trades` 1, **which is the residue register exactly.** M3 spans 2.21 (`/trends`) to 11.23 (`/changes`) under the new sampler. Narrow: the marquee now lands in **M5s** at every width on `/`, `/`'s bucket (b) reads 0 / 0 / 8 at 1024 / 900 / 720 against v3's 88 / 78 / 86, `/members`' 721 / 4 / 472 is still **100% M5x authored truncation**, and `/bill/[id]`'s single 720 item is M5s. The gap distribution's 500+px bucket is 0, and **every route was measured — none skipped.**

**The two identities, checked as arithmetic and not as prose, because they are what "done" means here.** **Site M1 17 == the permanent-residue register, entry by entry:** `/members` **13** = 11 knee-marginal `a.mc-row` at 121–131px (priced at HO 612; reopening costs 16 more truncated names) + 2 `.mc-row-hdr` at 230px, a short column label over a wide data column · `/vote` **2**, the plateau residual, no column width reaches zero · `/lobbying` **1**, a column header priced at HO 615 · `/trades` **1**, the 196px amount-column header, irreducible without narrowing a column sized to stop the money wrapping. **Site M1x 332 == the `data-viz-row` registry, marking by marking:** `StageFunnel` 13 + 13 (two routes, one component, 6 visual rows each) + `/lobbying` 125 (50 rows) + `/amendments` 94 (30) + `/nominations` 65 (15) + `MemberIdeology` 4 (2) + `/patterns` `PatternBars` 18 (6) = **332 candidates across seven markings on six routes, 115 visual rows.** Both sums balanced on the first check. **The arc's one-line identity: site M1 equals named residue; site M1x equals the registry; nothing unexplained.** **Also notes now run through HO 620.**

**Also (HO 621–623), Absence Watch shipped, plus the dashboard fit-finish that followed it — no theme-% change (a new front-page surface over an existing measure, not a new data domain; 623 is arrangement only).** **HO 621 re-ran a probe written four months earlier, and the reconcile is the reusable half.** Three of its four verbatim-copied facts were unchanged (the delegate carve byte-for-byte, `PARTICIPATION_FLOOR = 50`, the HO 566/540 lag query, line moved by one). The fourth — the population predicate — had **changed mechanism at HO 595 without changing semantics**: `getParticipationStrip` stopped aggregating `member_votes` live and now reads the materialized `member_participation`, whose writer computes exactly the two integers the probe had been computing inline. Two things follow that a re-run would otherwise have assumed. **The floor did NOT migrate into the materializer** — it is still applied at read time by each consumer, so the table holds all ~554 bioguides and "floored population" is a property of the query, not of the row set. And **`member_participation` is cumulative-only** (four columns, no `vote_id`, no date, no window), so a recency rule remains a genuinely new read path that nothing materializes. The probe's M1/M1.4 moved onto the table, because the ruling turns on the probe's population being the *strip's* population; **a new M1.0 measures the one axis on which they can now diverge — staleness — and it read clean: stamp 0.44d old, uniform across rows, 0 members differing, 0 missing.** That instrument exists because a stale materialized table renders correct-looking numbers with no error and no visual tell. **The four reads.** M1's sweep: House `>=10%` names 18 members, `>=15%` seven, `>=20%` three; **the Senate is a different distribution entirely** — `>=10%` names exactly three and nothing at all sits above 15%, so no single cumulative cut serves both chambers. M2's divergence sets were the whole point and both failure modes were **real, and lopsided 6:1**: six House members carried `>=15%` cumulative while missing 0–2 of the last 30 rolls (five voted on the final pre-recess roll call), against one — Biggs, 26 of 30 missed at 9.0% — invisible to a rate. Senate divergence was 0 both ways. M3b measured the window path at **306ms / 16,028 rows**. M1.4 found **exactly one** structurally invisible member (Darline Graham, SC, 14 votes); no delegate is floor-excluded — the six are in the population and carved by rule. **Two things the output showed that nobody asked for, and both changed the build.** The **House was in recess**, so its "last 30 roll calls" was a two-day pre-recess burst and its last vote was 15.2 days old — which is *why* those six sticky members all read `window=0/30`, and why a present-tense strip would have been false about an entire chamber. And **streak and window disagree on the two shapes**: Wilson and McConnell were 30/30 with streak 30, while Biggs was 26/30 with **streak 0** — only a streak distinguishes *gone* from *spotty*. **The ruling: the trigger is the consecutive-miss streak, R1 is dead, R2's rate rides inside the row as evidence rather than as the trigger, the copy is dated, and the acute/chronic division of labour with the participation surfaces is recorded so it is not later "fixed".** **HO 622 built it in two commits.** `abd8768` is `getAbsenceWatch`: **candidate filter first, walk second** — Phase A is the probe's own window query aggregated DB-side, a *necessary* condition (a >=30-streak member necessarily has every explicit position in the last 30 rolls equal to `not_voting`) that bounds the expensive walk. Its predicate is **`nv = n AND nv > 0`, deliberately NOT `nv >= 30`**: a genuine streak member with NO-DATA rows inside the window has fewer than 30 rows there, so the tighter-looking filter would drop exactly the member the band exists to name — the loose one still returned 4 candidates across both chambers, of which 2 survived the walk. Phase B walks to a 120-roll bound in one query per chamber. `lastCastDate` is **sliced from `vote_date`, never parsed**, because the House stores a `-04:00` offset and the Senate a `Z`, and a `Date` round-trip would shift a late-evening House vote onto the next calendar day. **The degradation guard sits OUTSIDE the `unstable_cache` boundary** (HO 598): an empty array here is the good-news state that renders nothing, so caching a failure as "nobody is absent" would be a silent wrong answer with no tell — and this was not hypothetical, the guard caught the real `Invariant: incrementalCache missing` throw when the first oracle tried to call the function from `tsx`. `12b3e57` is the band, in the HO 608 slot the mock draws it in, with **two recorded deviations**: the mock drew the rule 621 ruled OUT (missed >= 20 in 21 days, six members, a sparkline and a CHRONIC chip), so the row anatomy is the ruled one and only the mock's shell carries over; and its two-column `.abw2` grid does not, because that grid existed to fit six rows and a permanently half-empty column is a reserved box under another name. **The mock's footer ships as designed and is load-bearing** — the delegate carve is population-correctness, so Radewagen at 82.4% missed is absent *by rule*, and without the disclosure that absence is mysterious rather than legible. **The probe was the oracle, and the oracle's own first cut was wrong in the instructive way.** It reads the rendered band off a running server (the cached function cannot be called outside a Next request context) and reported FAIL on all six assertions against a render that was **correct** — because it whitespace-split the row and read `R | - | KY` as three cells, not knowing React emits `<!-- -->` separators between interpolations. **The same class bit the narrow-width probe harder**: its first version bucketed rects by rounded `top`, which under the six-token `--fs` ladder splits one baseline-aligned row into three and measures a gap *across* a cell sitting invisibly between them — a phantom **221px** interior gap, precisely the layout audit's own v1 bug that instrument v2 fixed with vertical-interval clustering. Corrected, the interior gap is **8px at every width — exactly the flex `column-gap`** — and that number, not the audit's, is what proves C1 here, because **the audit's M1 does not score this band's rows at all**: two sibling rows fall between M1a's `>=3 siblings` gate and M1b's `no siblings` gate, so its 0 is true but *under-determined* on the new element. The other 622 expectation that missed: **M4-true is 1 / 42px before AND after** — that residue is the HEARINGS tab label HO 610 closed on, and the band contributed zero, which is the honest claim rather than the predicted 0. **The C4 branch was fired, not narrated** — threshold bumped to 9999 in the working tree only, rebuilt, `/` rendered **zero `abw` nodes** with nav and hearings intact as controls; reverted, tree clean, nothing committed. **HO 623 is fit-finish, and one of its two items did not exist.** `25629d9` widened the feed column **`minmax(400px,27%)` -> `minmax(480px,31%)`** (an owner ruling and a recorded mock deviation, the HO 610 masthead precedent): measured **678 -> 779px at 2560** and **400 -> 480 at the 1440 floor**, both under the ~850px C8 ceiling. **The `bxp` container query moved with it, 440 -> 520, and the handoff's stated reason for that did not survive measurement** — at 1440 the panel's inner width is **420px**, already below the old 440, so 1440 would have stayed stacked either way; the boundary that actually moves is **1680**, inner **446px**, which under 440 the *widened* column would have flipped to wide mode at a cramped 446. The transferable rule is **a container-query threshold is derived from the container it was tuned against, so widening the container obliges you to re-derive the threshold** — otherwise a width change silently changes which viewports collapse. `/bills` was checked rather than assumed: 1140px at 1440, far past either threshold. `d754db3` dimmed **past ribbon days** to the same 0.42 the schedule rows already use, so one opacity means one thing in that panel: in NEXT mode the bar reads `NEXT · THU SEP 17` while the ribbon still showed `TUE 7 · WED 9 · THU 4` at full strength, which reads as three pending days the bar is ignoring. **The counts were correct — the ribbon is a week total, not a forecast — so this was a DISPLAY contradiction, not a data one**, and that is the find worth keeping: *a ribbon of counts beside a NEXT jump is data-coherent and display-contradictory until the past dims.* It needed one JSX branch, flagged rather than taken silently, because no selector can know which day is past and the shown day can sit outside the week entirely. **The other item — the NEXT-state spacing jump — DOES NOT REPRODUCE, and was measured rather than eyeballed away:** `bar.bottom -> row.top` is **0px at 2560, 1700 and 1440**, `.hsch-wrap`'s height equals the single row's exactly, and no element occupies the band; all three of the handoff's candidate diagnoses are ruled out (`.hcal-agenda-day` is not in this component at all — that is `/hearings`' calendar, this is `.hsch-*`; there is no agenda-head to suppress; `.hsch-wrap` declares no row-gap so the empty second column at >=1700 contributes nothing). **Nothing was changed for it.** The other hearings states were **fired, not narrated** — a scratch env-read clock in `HearingsTab`, reverted and uncommitted: TODAY mode at Wed Aug 5 renders MON/TUE dim, WED amber-active, THU/FRI full; the empty week at Oct 5 renders `NOTHING SCHEDULED` + five dashes + one dim line with no `.hsch-wrap`. **The audit did not move across 623** — `--route home` at 2560 reads **123 candidates · M1 0 (M1a 0 · M1b 0) · M1x 13 · M2 0 · M4-true 1 / 42px · M4w 0**, identical either side, falsification PASS. **Narrow was confirmed rather than assumed**, and the confirmation needed a rebuilt control: at 720 the post-623 build shows a non-tape overflow (`.hsch-row` / `.hsch-title`, `docScrollW` 739) and **the pre-623 build shows the byte-same set** — bucket (b) unchanged, pre-existing, not introduced here. **Live at ship, and the expectation was the instrument: the band names exactly two — McConnell (51 consecutive, since Jun 11, 14.1%) and Wilson (34, since Jul 16, 29.3%).** **Prod-verified converged twice** — `12b3e57` for the band (four sequential reads, byte-identical content, which is also the HO 533 serialization check: plain arrays, no Map) and `d754db3` for the fit-finish (five reads on one stylesheet hash carrying `minmax(480px,31%)`, `@container bxp (max-width:520px)` and `.hsch-cell.is-past{opacity:.42}`, with the HTML's 4 dimmed cells and 2 absence rows agreeing — no stale-HTML-new-CSS cycle). Cost as shipped: **1,064 rows returned / ~516ms cold**, one regen a day on the `votes` tag. Docs: HO 623 (this block). **Also notes now run through HO 623.**

**Also (HO 624–627), the ledger ghost and the dashboard density pass — no theme-% change (624–626 are read-cost and a one-line index hint; 627 is arrangement and depth over existing measures, not a new data domain).** **HO 624 was a probe against the backlog's largest read line, and the line was a ghost.** The entry read `bills_agg` **110M rows / 26% of the 30-day budget / 317 calls at ~347k each**, and the doubt was specific: **~347k is not a `bills`-shaped number** (the corpus is ~17k) but `member_votes` was ~366k, and until HO 595 `participationAggCte()` full-scanned it **inside the very same statement text** — the per-query ledger attributes by statement, and the HO 602 window (read 2026-08-05) straddles a July fix. **The probe changed instrument, and that is what made M3 a measurement instead of an argument.** `@libsql/client` v0.14.0 does not surface per-statement `rows_read`, so it reads Turso's hrana `/v2/pipeline` directly and meters exactly what the ledger meters. **M0 calibrates it because the instrument has a trap: a bare `COUNT(*)` is answered from B-tree interior pages and under-reports by 227–416×** (`member_votes` 366,496 rows → `rows_read: 1617`; `bills` 17,463 → 42), so nothing may price a scan by bare count and every figure below uses a predicated form. **The verdict: 20,752 rows per call**, identical on both sorts, **16.7× under the ledger** — component sum 18,617 (`bills_agg` 17,463 + `part_agg` 554 + `members` 553 + `palestine` 47) plus ~2.1k of ≈4 index seeks per member row across the three LEFT JOINs. **The coincidence test is decisive and was stated as arithmetic: `|347k − member_votes 365,996| = 18,996` against `|347k − measured| = 326,248`** — the ledger figure sits within 5% of the thing HO 595 killed and 16× from the query it was billed to. **Two levers died on measurement.** The proposed `congress = 119` narrowing excludes **zero** rows (all 17,463 sponsored bills are 119, a single-congress table), and `includeCeremonial` is not one either — **both CTE variants read 17,463**, because the predicate filters rows RETURNED inside a covering scan, not rows read. Materialization was priced and declined: ~1,092 written rows to save a 17k covering scan that reads in single-digit ms. **The variant-surface inventory is the part a future ledger read needs**, because the ledger groups by statement text and there are **three**: `getMembersRanked` (filters × sort × page × pageSize — 72 combinations before state/q/page, × 56 states) · `getCommitteeRoster` (per committee, 236 × 3 × 2 = 1,416) · `searchMembers` (per query string at 600s, unbounded by construction). **And the plan check found a live defect nobody was looking for:** `searchMembers`' lean copy carried **no `INDEXED BY`** and planned onto `idx_bills_is_ceremonial` as a **non-covering MULTI-INDEX OR** — the HO 277/329 misplan class, on a request path — measured **35,129 rows / 594.6ms, 2.01× the hinted shape**, the row-fetch doubling a non-covering OR predicts. **HO 625 filed it in three hunks and one of them was a correction nobody asked for.** The QUEUED entry resolved; the **reusable rule** recorded as *date every per-query ledger read against the fixes that landed inside its window — a killed statement stays on the leaderboard for the length of the window*, which is the measured-number rule one level up (a ledger line is `f(query, corpus, window)` read as a property of the query); a **new dated OPEN LOOP** for a single dashboard glance after ~Sep 1, the first window clean of both the pre-595 statement and the HO 606–615 layout-arc crawls, carrying the standing rule **nothing gets optimized off the current ledger — its top lines are ghosts and burn events**; and a flagged ride-along correcting the HO 602 tombstone, which credited HO 595 with closing **42%** of the budget. **HO 624 makes that false: all three of the top three lines were the same ghost, so 595 actually closed ≈(110.0 + 91.3 + 85.4)/418 = 68%.** The per-call figures in that tombstone are left intact as a faithful record of the read and re-labelled as pre-fix cost inside a trailing window. **HO 626 took the hint, in one line.** Plan flipped to `SEARCH bills USING COVERING INDEX idx_bills_sponsor_agg` and the `GROUP BY` temp b-tree disappeared with it (the covering index is already ordered by `sponsor_bioguide_id`). Full-statement `rows_read` across five terms: `smith` 35,129→18,580 · `texas` 35,228→18,679 · `a` 36,594→20,045 · `johnson` 35,126→18,577 · `california` 35,268→18,719, with the hinted CTE standalone at **17,463 — exactly `billsAggCte`'s figure**, so it now lands in the hinted family. **Equivalence was proven, not assumed:** all five terms returned **identical rows, position for position**, old vs new against the same live state — a hint changes the plan and must not change the answer, and because the values are byte-identical nothing behind the 600s cache had to converge. **The currencies discipline held under temptation:** timings improved sharply (277ms → 8.8ms) and were deliberately **kept out of the commit's argument**, because the Aug 7 platform read established a dev-box timing as a known non-signal — citing it would have been the exact error the 624/625 arc was about, one HO after writing it down. **HO 627 was density, and it opened with an owner-reported defect the arc's own instruments had never been pointed at.** **Commit 0: the row is the click target, everywhere in it.** C3 had made stage the sole carrier of colour, so the amber bill id read as *the thing to click* — and clicking it left the dashboard while clicking two pixels to its right expanded. One advertised affordance, two outcomes. The id keeps its `href` (new-tab, copy-link, a11y) and its `onClick` became modifier-aware; the sponsor link had the same swallow and got the same treatment; a **leading** disclosure glyph returned (leading, because a right-anchored caret is the C1 defect HO 609 removed from this row); and the drill moved **one level in, not away** — a packed-left `→ FULL PAGE` now leads the expand panel, because the pre-627 button sat at the bottom of `.bxp-metabox` and the `@container (max-width:520px)` rule stacks the dashboard's narrow panel, pushing it below the whole summary/hearing/news/odds run. **The gate is the durable part and it amends the §1 reproduction permanently: the prior reproductions passed while the owner failed — they clicked pixels users don't. The gate now clicks the pixels users do**, taking each target's own client rect and issuing a real mouse click at its centre, from a genuinely scrolled feed, at 1440 and 2560. **It caught a real defect on its first honest run**: letting the default happen on a modified click was not enough, because the event still bubbled — ctrl-click opened the bill in a background tab **and** expanded the row behind it, the same defect mirrored (`open=true · tabs 1→2`). 16/16 after. **Two handoff premises did not survive measurement, and both were the same shape.** The handoff names the feed column `.bills` with `.feed { overflow-y: auto }`; **neither class exists** — the column is `.dash-bills` and the scroller is `.v2f` itself — and the first gate run scrolled the missing selector, scrolled nothing, and reported *"feed too short to scroll"*, which is the same-as-success shape; the harness now finds the scroller by walking up to the first genuinely-overflowing ancestor rather than trusting a class name. And a **variant-split depth looked right and was refused**: `/dashboard-classic` was removed at HO 608–610 (the route 404s) and `app/page.tsx` is the only caller of all three feed components, always `variant="v2"`, so a variant-keyed constant would have been an unreachable branch carrying a comment about a page that no longer exists. **Commits 1–3 were depth, not breadth — nothing the arc cut came back.** MOVERS and NEW 5→30, STALLS 5→60 (a **ceiling, not a target** — the stale set is 5 today, so it renders everything there is), BREAKING 5→10. **Cost was measured because the ledger discipline applies even to 25 extra feed rows:** MOVERS `rows_read` **1,018→1,094** and NEW **1,027→1,103**, +76 each — the ~1,000 baseline is the mention subquery's `news_mentions` GROUP BY, not the LIMIT, since both walks are pre-ordered index ranges that short-circuit at LIMIT either way. **Commit 3 un-tabbed the DISTRIBUTION panel to the mock**, BY STAGE over BY TOPIC, co-visible; BY TOPIC ships as a **ranked bar list** per the mock rather than the treemap, because two halves of one panel have to read as one panel and a squarified treemap beside a bar run reads as two widgets. `DashboardTopicTreemap` is unchanged and still exported with no dashboard caller (noted, not swept); `DistributionsTabs` was deleted in the commit that orphaned it; the fixed 230px `.distributions-panel-body` went with the tabs, since it existed to stop a swap jumping and would now **cap the panel at half what it renders**. Click-to-filter was verified live on **both** halves rather than assumed (`?stage=floor` → 1 selected + 5 dimmed; `?topics=healthcare` → 1 selected + 7 dimmed; `ActiveFilterStrip` in both), cross-rebase intact. **A NEW `data-viz-row` marking shipped WITH ITS CURVE, and leg B refused the crawl until it did** (live M1x 13→30). Measured void, bar-end → value: **2560 TOPIC 8–395px (median 353) against STAGE 8–488px (median 454); 1440 TOPIC 8–155px against STAGE 8–166px.** **No cap reaches it, and this is the strongest form of the exemption rather than the `FirmsLeaderboard` kind: the bar is scaled to the track, so the void is PROPORTIONAL TO THE VALUE ENCODED** — a topic at 1/8 of the max must leave 7/8 of its track empty, and capping shrinks track and bar together, preserving the proportion while making the small topics unreadable. Registry form **M1x 30 / 14 rows** (candidates, not visual rows). **The audit held where it had to:** `--route home` at 2560, falsification PASS, **M1 0 · M2 0 · M4-true 1 / 42px · M4f 0** — the P3 exit numbers unchanged; **only M1x moved, and it is decomposed above.** **The remaining left-column shortness at 2560 is recess plus C4 doing their jobs** — empty states collapsed, panels content-sized — and is explicitly not to be fixed by resurrecting anything the arc cut. **Prod-verified converged at `3d0d6d1`**: 6 stage rows, 8 topic rows, 30 feed rows, 30 disclosure glyphs, and `.bxp-head-drill` present in the served stylesheet (the HO 212 stale-`.next` trap ruled out by reading the CSS, not the HTML). Docs: HO 625 (backlog), HO 627 (this block). **Also notes now run through HO 627.**

**Also (HO 628), the feed row hairline the 609 rebuild dropped — no theme-% change (one declaration restored from the committed mock; arrangement only).** The owner reported the right-rail bill rows reading cramped, and the diagnosis was **one missing declaration, not a spacing rework**. The mock's `.mv` is three things — `padding:5px 10px`, a hover background, and **`border-top:.5px solid var(--border-soft)`** (`dashboard-layout-target.html:142`, identical in `dashboard-2col.html`); the HO 609 rebuild adopted the first two and dropped the third **without a comment recording the drop as a decision**, which is how it survived three HOs. At depth 5 the omission never read. **HO 627's raise to 30 is what exposed it**: thirty borderless two-line rows at 5px padding are one undifferentiated block. Mock wins — the standing precedent, cited at 627 for this same page's distributions panel. `04f7523` puts it on the **group wrapper** via the sibling combinator, not on the row, and three things follow from that placement. The first group carries no line against the tab strip with **no `:first-child` carve** (measured: `border-top-width` 0px on group 1, 1px `rgb(17,24,32)` on group 2). `.v2f-group.open`'s amber frame overrides the grey top edge rather than fighting it — **but only by source order**, since `.v2f-group + .v2f-group` and `.v2f-group.open` are both specificity (0,2,0), so the hairline is declared ABOVE `.open` and a tidying reorder would put a grey line on the open row's top edge; the comment says so. **The open-frame interaction was verified, not assumed, because the handoff named it as the one thing that could go wrong.** The open group's `margin:-1px 0` pulls the next sibling's new hairline into the same 1px band as the amber bottom border — **measured `gapPx = -1`, exact overlap** — and `position:relative; z-index:1` against the neighbours' `auto` is what makes amber paint over it. **No sliver at 1440 or 2560, top or bottom**, so the handoff's contingency (`.v2f-group.open + .v2f-group { border-top-color: transparent }`) was not needed. **One reading worth keeping: `getComputedStyle` returns `1px`, not `0.5px`** — Chrome rounds a sub-pixel border up to one device pixel at DPR 1, which is what all ~279 fractional borders in the file already do, so it renders as the house hairline renders; but a future diff taken against the *computed* value will not find the number the declaration contains. **Commit 2 was NOT taken.** The contingency was `.v2f-row` vertical padding 5px → 7px as a recorded mock deviation if the rows still read cramped; no such ruling was given, so padding stays `5px 12px` — the mock's density is 5px WITH the hairline, and that pairing is the approved design. Gates: the pinned HO 627 click gate re-ran **16/16** at both widths from a genuinely scrolled feed (no JSX changed, but it is the pinned requirement and it is cheap); `--route home` at 2560 read **M1 0 · M1x 30 · M4-true 1/42px**, falsification PASS — a border-top opens no gap. **Prod-verified by stylesheet on the deployed hash** (the HO 212 trap, read from the CSS and not the HTML): `.v2f-group+.v2f-group{border-top:.5px solid var(--border-soft)}` present at `04f7523`. Docs: HO 628 (this block). **Also notes now run through HO 628.**

**Also (HO 629), the bills column boundary moves left, and the instrument's live anchor moved with it — no theme-% change (arrangement only).** Owner ruling: **31% → 45%**, chosen off an interactive mock with a live fraction knob (candidate set 31/36/40/42/45) rather than estimated from stills. Not a side flip — the J-revision arrangement stands. `78f5c78`, measured on the local production build, **every figure net of a 15px scrollbar, which is why each lands slightly under the handoff's estimate**: column **779 → 1130px at 2560** (the boundary moves 351px left), 729 → 1058 at 2400, **480 → 626 at 1440** (45% now binds; the 480 floor goes dormant above ~1067 viewport and stays as-is). Left region 1372px at 2560 with two-up panels 816/544, and 756px at 1440 with 446/297 — the compression the ruling accepted. **The gain is real and partial, and it was measured on ONE build and ONE data state** by injecting the old track so the fraction is the only variable: truncated movers titles **18/30 → 14/30 at 2560**, 18/30 → 15/30 at 2400, 20/30 → 18/30 at 1440; what still truncates is genuinely long titles, not a width failure. **Breakage was checked rather than assumed** — real horizontal overflow in the left region is **0 at both widths** (authored ellipsis and scroll excluded) and `documentElement.scrollWidth` equals the viewport at both, so the handoff's HALT-if-a-panel-breaks condition did not fire. **The ~850px C8 ceiling is RETIRED, not re-justified.** C8's scope is rows carrying a right-hand element — its stated mechanism is that extra width pushes that element further away, which is C1 again. This row has none: HO 609 removed the pinned metric and caret, HO 627's disclosure glyph is leading. The title is packed-left `nowrap` + `ellipsis`, so added width converts directly into visible characters and a short title ends mid-row with nothing anchored past it. The ceiling was **never tested at 623** (both values sat under it) and is false for a truncating row. **The derived bxp check reached the handoff's conclusion and falsified its reasoning.** The threshold stays **520**, but not because "wide down to ~1290, stacked in a narrow band below that". Measured: `.bxp` carries 16px horizontal padding and an `inline-size` query resolves against the **content box**, so 1440 reads outer 598 / **content 566** (chrome **60px, re-measured rather than reused from 623**), and the flip sits at **viewport 604 STACKED / 605 WIDE** — content 520 vs 521, exactly the threshold. **The band the handoff predicted is empty**: the two-column grid only exists at `min-width:1400`, and 1400 already yields content **548 > 520**, while at 1399 the grid has collapsed and the panel is 1291 wide. **So the dashboard panel is WIDE at every viewport ≥605 and the stacked branch is now mobile-only** — a behaviour change at 1440, which used to stack. `/bills` confirmed with one measurement rather than re-derived: **1140px, WIDE**. One consequence recorded so it is not re-filed as a defect: HO 627 moved `→ FULL PAGE` into the panel head partly *because* the container query stacked the dashboard's narrow panel, and that justification is now historical at desktop widths; the change stands on its own. **The layout audit halted, the instrument was RIGHT, and the ANCHOR was wrong — `83a29fa` re-anchors it, riding the commit that invalidated it (the HO 612 discipline).** Leg B asserted `live M1x === 30`; M1x counts **candidates**, and candidacy is width-gated (`m1bMinWidth` 600), so narrowing the left region dropped the widest node under `[data-viz-row]` from **659px to 518px** and M1x fell **30 → 0 with the marking untouched**. **Checked before touching the assertion** (HO 615's rule): at 2560 on one build the attribute reaches the same **2 nodes** (`ul.stage-funnel`, `ul.topic-dist`) and the same **86 visible nodes** under them at 45% and at an injected 31%. Nothing rotted. **The sharper reason to replace it rather than retune it: the old gate had stopped discriminating** — at 45% it read FAIL whether the marking was intact or deleted, so it could no longer separate either of its two failure modes from a legitimate resize, which is the same-as-success shape arriving through a threshold. Leg B's live half now asserts what the attribute **reaches** (marked-node count and identities, width-invariant); M1x is still printed beside it as the registry figure, but as an observation, not a gate. **Both directions were fired before it shipped**, scratch only, reverted, components clean: marking stripped from `StageFunnel` → reaches `ul.topic-dist` → FAIL; marking added to `div.v2f` → reaches 3 nodes, M1x **157** → FAIL; PASS restored on the reverted build. **Both dashboard arms MEASURED, not derived** (the HO 627 precedent): `--route home` and `--route home-stage-committee` each read **M1 0 · M2 0 · M4-true 1/42px · M1x 0**, falsification PASS. The exemption registry is restated in the backlog — **site M1x 366 → 306** — with the visual-row count unchanged at 131, which is the whole point: the marking is **dormant at this width, not gone**. The click gate re-ran **16/16** at both widths (row geometry changed). **Prod-verified by stylesheet on the deployed hash**: `minmax(0,1fr) minmax(480px,45%)` and `@container bxp (max-width:520px)` in the served CSS at `83a29fa`, HO 628's hairline still present alongside. **The mock is committed here, and the trace of its absence is kept rather than tidied away: `docs/design/mock-629-bills-boundary.html` was ABSENT FROM DISK at close time on 2026-08-07** — not in `docs/design/`, not in `~/Downloads`, and named as absent rather than assumed lost — and the owner dropped it in the amendment pass before this block landed, so it ships in this commit. It is the interactive one: a `--bills-frac` knob over the 31/36/40/42/45 candidate set with a live viewport/column/inner/mode readout, which is what makes "chosen off the mock, not estimated from stills" a checkable claim rather than a recollection. **The mock is the ruling's record; this block is its reading.** Docs: HO 629 (this block) + the exemption-registry restatement (backlog). **Also notes now run through HO 629.**

**Also (HO 630), Absence Watch expands the member card in place and navigation becomes opt-in — no theme-% change (an existing card reused on an existing band; arrangement plus a prefetch).** Owner ruling, the same product logic as HO 627's: **the drill moves one level in, not away.** The band wrapped each whole row in a `<Link>`, so any click left the dashboard; now a plain click expands the member's card under the row and the member page is opt-in — modifier-click on the name, or the card's own drill. `SponsorExpandedPanel` is reused **verbatim**, no absence-specific variant. **The /members data path does not transfer, and that is the design:** that surface expands by URL state and server-renders the selected member, which IS navigation — the defect. `abd3b63` prefetches instead. `getMemberCardExpansion` is extracted from an inlined IIFE in `app/members/page.tsx` to `lib/queries` (this HO makes it two-consumer, the promotion bar) and **deliberately carries no cache of its own** — each of its five parts keeps its own key/TTL/tag, so a `bills` flush still reaches stats/topics/recent-bills and a `committees` flush still reaches the roster; a sixth entry would have an invalidation contract that is the intersection of five different tags. Assembly rides inside the cached `getAbsenceWatch`; the degradation guard stays outside the cache boundary, untouched. **The degradation rule is PER MEMBER and its failure mode is named:** a card-assembly failure degrades THAT member to identity-only (`card: null`) and the row still renders from the base query, without an expand affordance — a row that advertises an affordance it cannot honor is the 627 defect again. One try/catch around the loop would let one bad member cost every card; hoisting it further would let it cost the band, and "card data missing" collapsing into "nobody is absent" is the HO 622 silent-wrong-answer one level up, on a surface where empty is the good-news state that renders nothing. **Two structural constraints decided the shape of `e1e49e0` and both are load-bearing.** `SponsorExpandedPanel` is a SERVER component and a client island cannot import one, so the server band renders each panel and passes it into the island as a `ReactNode` — the `ActivityTabs` idiom, and what keeps `lib/queries` (and with it `next/cache`) out of the client bundle. And because the band keeps `formatSince`, **the island holds no clock at all**: the HO 574/589 constraint is satisfied by construction rather than by discipline, and the grep over `AbsenceWatchRows.tsx` returns zero lines. Row: `<Link>` → `div role=button tabIndex=0 aria-expanded` with Enter/Space; the name stays a real `<a href>` with a modifier-aware `onClick` whose `stopPropagation` is **not optional** — without it a ctrl-click opens the tab AND expands the row behind it, the HO 627 mirrored defect. Leading disclosure glyph, never far-right. **The head-drill was MEASURED into existence rather than added on principle** — the handoff made it conditional on where the card's own buttons land, and `View detail →` sits **447px down at 2560 and 402px at 1440**, ~85% of a 482–536px card, before the stack pushes it further; `→ Full page` now leads at **3px / 7px**. **Commit 3's threshold is measured, and the handoff's stated failure mode was the WRONG INSTRUMENT.** It named "committee names wrapping unreadably"; measured, they **never wrap at any width** (0/14 from 1370px down to 560px) because they truncate instead. So the curve reads truncation, anchored against the pane the card was designed for rather than a bare pixel count: **/members gives it 1073px, columns 210/518/345, 12/14 committees truncated, worst name 30% visible.** The band's wrapper: **1370 → 9/14 / 41%** (the band at 2560 — *better than the reference*) · 1200 → 11/14 / 33% · **1050 → 13/14 / 26%** (parity with the reference) · 950 → 14/14 / 21% (every name now cut) · **754 → 14/14 / 12%** (the 1440 floor). **1050px** is therefore where the band's card reaches the readability of the surface it was built for — above it the band is no worse than /members already is, below it the card degrades past any existing surface. A **container** query, not a media query, deliberately: the band's width is a function of the dashboard grid's fraction, and HO 629 moved that fraction one commit earlier, so a media threshold would have silently changed meaning with it. Result: three columns at 2560 (card 577px tall), one 704px column at 1440 (522 → 1245px tall). Scoped to the band's mount per the HO 507 rule, and `/members` **verified byte-identical rather than asserted** — card width, all three column widths, truncation count and worst-visible percentage, at both widths either side of the change. **Gates.** The click gate reads **30/30**: the 16 HO 627 feed legs pass **untouched** and 14 absence legs join them (7 per width). **No scroll leg, dropped deliberately and said so in the harness** — the band sits at the top of the page, so the client-rect-vs-page-position divergence the feed legs exist to catch cannot arise. **The ctrl-click leg was proven to fire before it shipped** (stopPropagation removed in scratch reproduced `open=true · tabs 1→2` at both widths, the exact HO 627 signature; restored, 30/30) — and it **caught a weakness in its own first draft**: it asserted only that *a* tab opened and printed "no /members tab" while passing, a leg that proved less than it printed, so it now waits for the destination URL. Audit `--route home` at 2560: **M1 0 · M2 0 · M4-true 1/42px**, falsification PASS, element count 1755 → 1780. **The handoff's guard-2 expectation of M1x 30 was already superseded by HO 629 one commit earlier** — it reads 0 and 629's block records why; the crawl does not open panels, so the card's interior is inherited from /members' P4-worked hygiene and is not re-scored here. **Cost, priced in ROWS per the ledger discipline: 236 rows per `getAbsenceWatch` regeneration** for today's 2-member band (M000355 106, W000808 128, plus one batched palestine read of 2) — paid on regeneration, not per request, and **never per expand**, which is a client toggle over prefetched props. Linear in band membership, which `ABSENCE_STREAK_MIN = 30` bounds to a handful. The real cost is PAYLOAD: both panels serialize into the RSC flight data on every dashboard render (650,209-byte document at 2 members), which is exactly what buys the zero-fetch toggle; neither is in the DOM until opened. **Reconciling the probe's verbatim SQL against the shipped helpers corrected two paraphrases that would have UNDERSTATED it** — `getSponsorRecentBills` has NO LIMIT (the 7-row cap is render-only in the panel) and `getSponsorTopTopics` counts in JS over the full raw row set rather than aggregating in SQL. **Prod-verified converged at `dc79474`**: four sequential reads with byte-identical band markup (2 rows / 2 glyphs / 2 panels) and **zero `class="abw-row" href`**, which is the positive proof the whole-row link is gone rather than an absence read as success; the served stylesheet carries `@container abwcard (max-width:1050px)`, `container:abwcard/inline-size` and `.abw-drill`. Docs: HO 630 (this block). **Also notes now run through HO 630.**

**Also (HO 631), the absence card gets a compact interior, stops reshaping the page, and its bound moves to its bottom edge — no theme-% change (an existing card re-densified on an existing band; arrangement plus a payload bound).** **Owner rulings, both made off `docs/design/mock-631-absence-card-overlay.html` — the live one, with a variant knob and a working overlay, not stills: COMPACT interior, OVERLAY over the page, click-outside-closes.** The mock rides this commit as the ruling's record (the HO 629 precedent); **its Wilson dataset is illustrative, and that mattered — see below.** **Commit 1 (`78f12ee`) made the density OPT-IN rather than hoping overrides do not leak**: `SponsorExpandedPanel` grows `density?: "full" | "compact"` defaulting to `"full"`, `/members` passes nothing, and the HO 507 rule is satisfied by the explicit prop. Compact is photo 56px inline with the name block, bills and committees capped 10, committees two-column collapsing under HO 630's container threshold (which does not move — the container width is unchanged by this HO), both columns closing with the house `[ + N MORE → ]`. **`/members` was RENDER-DIFFED, not asserted, and the diff earned its keep on a detail no reasoning would have reached:** the full branch keeps its original three-child JSX shape rather than one interpolated string, because **React emits text-segment markers between adjacent children**, so collapsing them into a template literal changes the served HTML — visually identical, no longer byte-identical, which is the gate. **The count-source fix landed HERE, one commit before the slice that needs it.** The bills see-all gated on `recentBills.length` while printing `stats.total` in the same label — two different predicates (the array is `summary IS NOT NULL`-gated, the total is not), so the panel was already incoherent; more to the point, gating on array length means commit 3's slice would have silently killed `[ + N MORE → ]` **exactly when it applies.** **Commit 2 (`56daf37`) is the overlay, and its stacking claim was reasoned and then verified rather than asserted:** the pop can only overlap the band's LATER SIBLINGS in the left region — all `z-index: auto` — so a modest z-index beats paint order, and it never reaches the sticky bills rail **by geometry, not by z-order** (`left:0; right:0` of a band that ends at the column gap gives it no width in that column to paint into). Esc closes and returns focus to the row; an outside click closes AND the clicked target's own behaviour proceeds — one click, not two. **The zero-reflow instrument discriminates, and its falsification came FREE:** rect-top of the element after the band, closed vs open, reads **0px at both 1440 and 2560**, where the same measurement read **~523px at 2560** before the change — so no scratch edit was needed to prove the leg can fail, the arc's own prior state supplied the nonzero reading. A leg that only asserted "open" was equally green either side of the change and proved nothing. **Commit 3 (`fa87cdf`) closes the QUEUED line, and its saving lands somewhere other than where the handoff looked.** The handoff asked for a served-document re-measure against HO 630's 650,209-byte baseline; **measured, the document does not move, and the reason is structural rather than a failed slice** — the card is a SERVER component, so only its RENDERED output serializes, and commit 1 already caps the render at 10. **Confirmed by ID, not by byte count** (`119-s-476`, `119-hr-5868` absent from the pre-slice document), because a byte total can coincide. What the slice actually removes is the tail of the value `getAbsenceWatch` **caches**: **16 rows, 15,481 bytes, 44% of the bills payload** — and because the Data Cache survives a deploy, that saving lands on the next regeneration, not at promotion. **The handoff's own figure for this was wrong, and the correction is the durable half.** It predicted "12→10 and 9→9 at the current band" and called the saving "small … the point is the bound, not the bytes." Live: 12→10 (cut 2) and Wilson **24**→10 (cut 14). **The 9→9 was the MOCK's illustrative Wilson dataset read as an expectation about real data** — the owner named it his error and the **second of its class**, and extended the policy accordingly: **illustrative mock data never sets a numeric expectation for a build.** The saving is not small; it is 44%. **Commit 4 (`707b94a`) is the HALT-ruled fold bound, and it exists because a viewport FRACTION is not a viewport BOUND.** `min(72vh, 760px)` bounded the card's HEIGHT while knowing nothing about where the card STARTS; the pop hangs at `top:100%` of a band sitting below the whole header stack, so 72vh of 900 put the bottom at **992.5 against a 900px fold — 92.5px below it**, reachable only by scrolling the page, which is the page-growth commit 2 removed re-entered through the bottom edge. `calc(100vh - 360px)` subtracts the origin first, so the bound is on the BOTTOM. **The constant is measured, and the first measurement was wrong in a way worth recording:** written against 1440 alone it read "345 + buffer, width-invariant". Measured across three viewports the band-bottom offset is **344.5 (1440×900) · 352 (1920×1080) · 346.5 (2560×1400)** — a 7.5px spread, because the header stack's nav wraps differently at 1920, so **the widest viewport is not the worst case; 1920 is.** 360 is sized against that worst origin (8px clearance there, 15.5px at 1440), and the comment now carries the range and the coupling rather than the one number, because a constant derived from one width silently mis-sizes at another. All three terms earn their place: the fold term wins at 1440 (540px) and 1920 (720px), the 760px cap still wins at 2560×1400 where the fold term is **idle and this commit changes nothing** — which the probe reports as N/A rather than counting as a pass, since **a green reading must distinguish "the fix works" from "the defect never reproduced here"** (it recomputes the OLD bound's bottom from the same measured origin to do so). **A stale server nearly measured the wrong build, and the catch generalises the HO 212 trap one layer.** A `next start` from the previous evening was still listening on :3000 and answered 200; its stylesheet carried `abw-card-wrap` and `abw-row` but **zero `abw-card-pop`** — a pre-commit-2 build serving the state the arc had already left, and every figure taken from it would have read plausibly. Not a 404'd stylesheet but a **200 stylesheet from a stale build**, caught by asserting the new rule's presence in the SERVED CSS before trusting a single measurement. **Gates.** Click gate **36/36** — the 16 feed legs untouched, 20 absence legs (10 per width), the three new ones being Esc-closes-with-focus-return, outside-click-closes-**and**-the-target-still-acts (both halves asserted; a leg that only checked the close would prove less than it prints), and open-A-click-B-switches. Audit `--route home` at 2560, **closed state**: **M1 0 · M2 0 · M4-true 1/42px · M4f 0 · M1x 0**, falsification PASS, elements 1780 → 1791. **Scoped honestly: the crawl does not open the pop, so the overlay's open state is not audited** — the card's interior is inherited from `/members`' P4-worked hygiene. **Prod-verified converged at `707b94a`**: the served stylesheet carries `max-height:min(72vh,760px,100vh - 360px)` — the minifier drops the redundant `calc()` inside `min()`, which is valid and is byte-identical to the build the probe measured (same CSS hash locally and on prod) — and the fold probe re-run **against prod** returns the identical origins, computed max-heights and above-fold verdicts at all three viewports. Docs: HO 631 (this block) + the QUEUED tombstone (backlog). **Also notes now run through HO 631.**

**Also (HO 632), MIA, feed day-groups, and the cards go quiet — no theme-% change (display copy, arrangement, and subtraction; no new data domain).** **Three rulings off `docs/design/mock-632-quiet-cards-and-groups.html`, revised twice in review (group headers punched to amber-bright with the tape's 2px left edge; the band renamed), and a FOURTH ruled item that turned out to need no code.** The mock rides this commit as the ruling's record. **Its arrival is itself the durable half: after the browser-to-disk hop failed three times, the mock shipped THROUGH THE HANDOFF CHANNEL as a fenced block in `632-mock-delivery.md` carrying a declared md5 and byte size, to be extracted, verified, and the wrapper deleted.** Extraction is by exact bytes between the fences and the checksum discriminates: the trailing-newline candidate read `2a49e8d8…`, the correct one `7989929c17a09b48b14c1a4c6cbcac56` at **27377 B**, confirmed independently with `md5sum` before a line of code was written. **That is now the standing mechanism for mock transport** — a mock too large or too fragile for the browser hop travels as a checksummed `.md` delivery, is verified before use, and the wrapper is deleted as transport rather than kept as record. **C1 — the band is MIA on screen, and the handoff's own caution about its grep was right: there are TWO sites, not one.** The visible title AND `aria-label="Absence watch"` on the section; renaming only the first leaves a screen reader announcing the old name over a band that reads MIA. They now differ ON PURPOSE — visible `▲ MIA` (terse chrome; the ▲ carries the alarm and C3 wants one bright element, not a restated one), accessible `MIA: absence watch` (keeping the context a sighted reader gets free from the band's position). "MIA" alone would announce an initialism stripped of what makes it legible, a downgrade aimed at the reader who can least afford one; the file says so, so a later consistency pass does not collapse the two. The FEATURE keeps its name everywhere else — roadmap, backlog, helpers, filenames. **C2 — TODAY / YESTERDAY / EARLIER THIS WEEK on MOVERS and NEW, and it REVERSES HO 609's refusal with the provenance kept rather than overwritten.** 609 refused feed groups on a sound rule — the committed mock had none (its TODAY/YESTERDAY strings belonged to the hearings day bar) and the mock wins. **What changed is the mock, not the rule**, so the same precedent now requires them; the comment asserting the refusal is replaced, because a comment stating the opposite of the shipped state is its own defect. **The bucket field is PARAMETERIZED BY MODE, because the rule "group and age from the same timestamp" has no single-field answer**: MOVERS reads `stage_changed_at`, NEW `introduced_date`, STALLS `latest_action_date` — and STALLS is ungrouped by design, since it is ordered by stall age and a TODAY header over it is a category error. **The two grouped fields do not store the same SHAPE, and this is the HO 622 trap in a new place**: `stage_changed_at` is an ISO instant in UTC (convert to ET), `introduced_date` is DATE-ONLY (slice, never parse — `new Date('2026-08-06')` is UTC midnight, i.e. 2026-08-05 20:00 ET, backdating every row of that feed by one). One rule over both silently breaks whichever loses. **ET is measurable rather than principled: 5 of 30 MOVERS rows (17%) land on a different calendar day under UTC bucketing.** ET-today is derived ONCE from the drilled `nowMs` and shared by both paths, so the two feeds cannot disagree about when today started; nothing reads a clock (every `Date` is built from an argument), so SSR and hydration bucket identically and 574/589 holds by construction. **Measured distribution at close: MOVERS TODAY 12 · YESTERDAY 18 · EARLIER 0 · OLDER 0; NEW TODAY 0 · YESTERDAY 3 · EARLIER 27 · OLDER 0.** **OLDER renders nothing today and is NOT vestigial — its population is a function of the WEEKDAY, not corpus depth**, because both feeds are 7-day rolling windows while the ISO week is Monday-anchored: late in the week nearly the whole window sits inside the current week, while on a Monday up to six of seven days fall before the week start and OLDER carries most of the feed. **A Monday screenshot showing a full OLDER group is correct behaviour, not a regression** — recorded here so it is not filed as one. Empty buckets render nothing (a header over no rows is C4's reserved box wearing a label). **The seam needed no override, verified rather than reasoned**: the 628 hairline is `.v2f-group + .v2f-group`, a header sits between two groups as their sibling, so the adjacency does not match across it and the header's own border-top takes over — measured `border-top: 0px` on every post-header group at both widths. **The mock ships a defensive `.grp + .v2f-group{border-top:none}` and it was deliberately NOT carried**: it is genuinely redundant here, and a no-op override is indistinguishable from a load-bearing one to the next reader. **C3 — the metabox and the compact card's buttons go quiet, by SUBTRACTION.** `.bxp-metabox` loses background, border, radius and padding and nothing replaces them, because `.bxp-mrow` already carried `border-bottom: 1px solid var(--border-strong)` — the hairline-separated rows the mock's quiet variant draws are what the panel had all along, wearing a box. Row padding and separator weight were out of the ruling and are untouched. **Global, not dashboard-scoped, and that is the ruling**: the panel is shared with `/bills`, so a dashboard-only fork would put two treatments on one panel showing the same rows on two surfaces — the pre-316 chip problem in reverse; `/bills` rides it deliberately and was verified, not assumed. The last boxed button becomes a plain amber `→ Congress.gov ↗` link (`.bxp-btn--secondary` had exactly one consumer since 627 moved the primary to `.bxp-head`), the two glyphs kept because they mean different things — one marks the link, the other that it leaves the site. The compact card's stacked buttons become plain amber links under `density="compact"` ONLY; `/members` renders full density, where no ruling was made, and keeps its boxes. **The arrow MOVES rather than doubling** — the boxed labels already end in a trailing →, so the compact label drops it; writing both renders "→ View detail →". **Topic chips are unchanged in both variants** (they are links; HO 619 keeps their borders). **THE FOURTH ITEM WAS RULED YES AND SHIPPED NO CODE, and the error was the review's, not the handoff's.** Stage colour on `.v2f-stage` was ruled YES — and was **already live**, landed at `938f0ca` (HO 609, the same rebuild whose comment refused feed groups): the `STAGE_COLOR` map already carried dim INTRO/CMTE, `--stage-floor`, `--stage-enacted`, **and** the `other_chamber`/`president` extension the handoff flagged as its own new generalization and offered to revert. **The verification that missed it looked at the wrong LAYER** — it searched CSS for stage-keyed classes and found none, while the shipped rule is an inline `style` off a map in JSX. **Now policy, in three clauses: a style-presence check covers CSS and inline JSX BOTH; select by FUNCTION, not by an assumed class name; and an EMPTY SELECTION FAILS rather than reporting absence.** The commit was dropped; a pure inline-to-class refactor with zero visual delta was declined as not what the ruling asked for. **The same policy earned itself twice more inside this HO's own verification, which is why it is stated as policy rather than as an anecdote.** **First:** the groups probe located tabs with an unscoped `button` locator, and `.first()` matched the WeeklyBand's "NEW BILLS" metric earlier in the DOM — the NEW tab never switched, every NEW assertion re-measured MOVERS, and it reported PASS. It was visible ONLY because the two distributions happen to differ. The probe now scopes to the tablist AND asserts `aria-current` landed before trusting a reading; NEW then read YESTERDAY:3 · EARLIER:27, matching the independent DB-side recon exactly. **Second:** the chip check selected `.topic-chip`, matched nothing, and printed `n/a` **while passing** — those chips carry no class at all, their border being an inline `border: 1px solid ${topicColor}`, the identical blind spot that hid the stage colouring. Re-selected by href it reads 3 chips at 1px, so the HO 619 constant is verified rather than eyeballed. **`/members` byte-identity held, and the diff had to be READ rather than glanced at**: the whole page is NOT identical across the change — the committee rail's activity-bar widths and member counts moved (236 rows either side, live `recent_count` drift picked up when the rebuild reset the data cache) and the stylesheet hash changed because globals.css did. Both are attributable and neither is the panel. Isolated, the sponsor panel is identical on both sampled members across a 40k-char window from `.sponsor-card-grid`, the button block character-for-character, still carrying `sponsor-card-btn--amber` and its trailing arrow with no compact marker — so the conditional label introduced none of the text-segment markers that broke byte-identity at HO 631. **Gates:** click gate **36/36** at both widths across all three commits (headers are inert — no handler, no role, no tabIndex — and row discovery was unaffected); groups probe 14/14; quiet probe 6/6, asserting COMPUTED style rather than class names, since a renamed class with the box still applied would pass a markup check and fail the eye. Audit `--route home` at 2560: **M1 0 · M2 0 · M4-true 1/42px · M4f 0 · M1x 0**, falsification PASS, **M3 unchanged at 5.18** — the handoff anticipated M3 movement from the stage-colour commit, and with that commit dropped there is none — and els 1791 → **1793**, exactly the two headers on the default tab. **Prod-verified converged at `f077a4f`**: `▲ MIA` and `aria-label="MIA: absence watch"` in the served HTML with zero occurrences of the old visible string; TODAY/YESTERDAY headers stable across three sequential reads; and in the served stylesheet `.bxp-metabox{align-self:start}` (the box gone, the two surviving `.bxp-metabox` rules being pre-existing responsive margins), `.bxp-mrow` still carrying its separators, both `.bxp-golink` and `.sponsor-card-link`, `.v2f-grp` verbatim from the mock — **weight included, and that clause is true of `f077a4f` and stops being true one commit later; C4 below moves it** — and **zero** occurrences of the seam no-op. **THE VERDICT CAME BACK AS A HALT TWICE BEFORE IT CAME BACK AS A VERDICT, and it is recorded verbatim with its provenance rather than paraphrased.** Given in the DESIGN CHAT, against the LIVE PROD SCREENSHOT of `f077a4f`: *"looks good but can be scale back the bold text."* That reads as approval of the three shipped rulings plus one adjustment, and the element it names is unambiguous — the only thing this arc made bold is `.v2f-grp`, which the mock sets to 600. **C4 (`805b2aa`) is that adjustment: 600 → 400, one declaration, nothing else touched.** **The mock is NOT edited and stays at 600.** It is the record of what was *ruled*; the adjustment was made afterwards off the *rendered surface*, which is the HO 623 precedent (the feed column widened 400→480 by an owner ruling with the mock deviation recorded rather than back-fitted). **A mock is a record of a ruling, not a claim about pixels** — so when the two disagree after the owner has seen the render, the render wins and the mock keeps the history, and the deviation lives in the CSS rule's own comment where the next reader of that rule meets it rather than in an artifact they may never open. **Amber-bright, the .18em tracking, the 8px 12px 5px padding and the 2px left edge all stay verbatim from the mock, deliberately**: one variable moved, so the next screenshot attributes what it shows to the weight and to nothing else. **Verified COMPUTED rather than authored** — a rule can be present in the stylesheet and lose the cascade — **16/16 at both widths**, the weight asserted AND each of the four invariants pinned separately, because a leg that checks only the weight is equally green on a build that also nudged the colour; an empty selection FAILS rather than printing absence, this HO's own policy applied to its own last commit. Harnesses re-run **whole**, against a fresh build on a killed-and-restarted server (the HO 631 stale-server trap, and one was in fact listening): groups **14/14**, quiet **6/6**, click gate **36/36**. Audit `--route home` at 2560, closed state, matches the main baselines exactly — **M1 0 · M2 0 · M4-true 1/42px · M4f 0 · M1x 0 · M3 5.18**, falsification PASS — while **els reads 1795 against 1793, reported rather than smoothed**: a font-weight cannot add a node, and the feed's buckets have rolled a day (MOVERS now YESTERDAY 12 · EARLIER 18), so that is live-data drift of exactly the kind this block already names for `/members`. **Prod-verified converged at `805b2aa`**: the served stylesheet is the same hash and the same md5 as the build the probe measured, carries `.v2f-grp{…font-weight:400}`, and contains **zero** `.v2f-grp` rules at 600. **THE ARIA STRING STANDS AT `MIA: absence watch` BY PRE-RULED DEFAULT, and it is filed as a default rather than as a ruling.** It was put to the owner twice and came back silent both times; the relay carried a pre-ruled fallback for exactly that case, so C1's deliberate split survives untouched — visible `▲ MIA`, accessible `MIA: absence watch`. Recorded so a later reader does not mistake an unexercised default for a positive decision, and so the divergence is not collapsed by a consistency pass on the strength of a silence. Docs: HO 632 (this block). **Also notes now run through HO 632.**

**Also (HO 633), the sans face is IBM Plex Sans, self-hosted, and the HO 257 reversal is finally on the record — no theme-% change (one token, zero consumer edits, no new data domain).** **Owner ruling off `docs/design/mock-633-sans-specimen.html`, a four-candidate face knob driven on the REAL surfaces rather than on stills**, which is why no per-route ruling was needed: the specimen showed the representative surfaces and `--sans` is the seam, so every consumer moves together by construction. The mock rides this commit as the ruling's record. **It is the SECOND use of the HO 632 checksummed-delivery mechanism and it worked first time** — extracted from `633-mock-delivery.md`, verified independently at `c9b081e06acbde420cb103e2ca363d6d` / **12,571 B** against the declared pair before a line of code was written, wrapper deleted as transport. A mechanism that survives its second use on a different mock is a mechanism rather than an anecdote. **THE CHECKSUM IS THE BLOB'S, NOT THE WORKING COPY'S, AND THAT DISTINCTION IS ALREADY LIVE ON 632's MOCK — re-verify with `git cat-file -p <ref>:<path> | md5sum`, never `md5sum` on the file.** `core.autocrlf` is `true` here and the repo carries no `.gitattributes`, so an LF blob checks out as CRLF: `mock-632-quiet-cards-and-groups.html` reads `7989929c…` as a blob — matching the checksum its own block declares — and `d2bc16c7…` on disk. **A future reader re-checksumming either mock the obvious way gets a mismatch and would reasonably conclude the record was tampered with**, which is a self-inflicted false alarm on a mechanism whose whole purpose is to make tampering detectable. Named here rather than in 632's block, which is append-only and was correct when written. **THE RULING SUPERSEDES HO 257's "CBT has no `--sans`", and the interesting part is that the rule had already been reversed SILENTLY by the 609-era title work** — so for several arcs the codebase shipped a sans while two live `globals.css` comments asserted it did not, each using the false rule as the stated justification for a mono choice that was itself correct. **Commit 2 (`2c344b5`) corrected both comments; commit 3 (`32cf791`) corrects the SKILL.** The wiring keeps the token as the seam: `variable` on `<html>`, `globals.css:106` becomes `--sans: var(--font-plex-sans), system-ui, …` with **the old system stack DEMOTED TO FALLBACK rather than deleted**, so a face that fails to load degrades to what shipped yesterday instead of to a serif. **WEIGHTS ARE 400 / 500 / 600, MEASURED — and the handoff's expectation of 400 + 700 was wrong in a way that matters more than the number.** **A static grep could not have answered it either**: 10 of the 12 rules that set `font-family: var(--sans)` set **no weight at all**, so their weight arrives by inheritance or off a Tailwind class on the element, and the authoritative reading is therefore the **COMPUTED weight of every element the browser actually paints in the sans stack, across the routes that use it** — 400 (titles, prose, sponsor names), 500 (`/bills` and `/patterns` row titles), 600 (sponsor brackets, absence-card names), with **no 700 consumer anywhere**. The asymmetry is why it was worth measuring rather than guessing: loading a weight nothing uses is payload on every page, but **missing one something uses is worse, because the browser synthesises it — and a faux-bold is precisely what a specimen sign-off cannot catch.** **THIS IS THE THIRD INSTANCE OF ITS CLASS AND THE RULE SHARPENS, AS PRE-DIRECTED.** The owner named the second at HO 631 — the mock's illustrative Wilson dataset read as an expectation about real data — and set the policy *illustrative mock data never sets a numeric expectation for a build*. HO 633's source is one step further out: the expectation came from **the specimen's own class declarations**, not its data. So the sharpened form: **a mock or specimen is evidence about ITSELF — its data and its own style declarations alike — never about the product**; and where the product's own source cannot answer either, as here, the authority is the computed value on the rendered surfaces rather than anything readable statically. **PAYLOAD, AND A FINDING THE LEDGER HABIT SURFACED: the payload does not scale with the weight count, because Google now serves Plex Sans as a VARIABLE font.** The three weight blocks share ONE identical six-file set (`font-stretch:100%`, the same woff2 across all three), so the on-disk cost is **137,108 B across 6 unicode-range subsets regardless of whether one weight is loaded or three** — of which English text fetches **exactly one, the 40,240 B latin file**, confirmed on the network log as a single same-origin request. The built CSS carries **19 `@font-face` blocks: 18 real (3 × 6 subsets) plus a metric-adjusted `IBM Plex Sans Fallback`** (local Arial, `ascent-override: 101.32%`, `size-adjust: 101.17%`) ahead of the system stack, so the `display: swap` flash is metric-matched rather than a reflow. **C1b RESTORED NOTHING, AND THE CORRECTED STORY IS THE DURABLE HALF.** C1b was scoped to restore a preload that next/font "emitted no preload" for, with a candidate cause of variable-class-only wiring skipping the injection. **The candidate dies on the first artifact** — the loader emits the latin subset as `*.p.woff2` at 40,240 B regardless of `variable` vs `className`, so the request was honoured and the break is downstream of it. **Prod carried the preload all along.** The break is in Next, is **WINDOWS-ONLY**, and is mechanical: `NextFontManifestPlugin` finds the font module with `mod.request.includes('/next-font-loader/index.js?')` — a forward-slash literal — while the loader path is assembled with `path.join`, which is backslash-separated on Windows; the predicate cannot match, `next-font-manifest.json` ships as `{"pages":{},"app":{}}`, `getPreloadableFonts` returns null, nothing preloads. Vercel builds on Linux. **Both fallback numbers, 400 kbps / 150 ms, 3 runs each, identical source both sides:** local build — woff2 starts **3240 / 3278 / 4070 ms**, *after* the CSS that discovers it, swap window **+2798 / +2783 / +2776 ms**; prod — woff2 starts **360 / 422 / 387 ms**, ~1.9 s *before* the CSS finishes, swap window **+477 / +543 / +700 ms**. **So the expectation that the 2,797 ms would shrink toward the unthrottled case was already true, and prod was never the slow case: 2,797 ms was a local-build artifact.** Unthrottled there is no flash either way — the font completes before FCP (local −11 / −13 / −15 ms, prod −74 / −76 / −142 ms). **The confound reads the right way round**, which is what makes the comparison usable across two different targets: prod's FCP is *later* than local's while its font starts ~2.9 s *earlier*, so the gain is the preload and not a faster link. **Reproducing the recorded 2,797 to within a millisecond is what separated instrument-sound from target-wrong** — had the re-measurement disagreed with the record, the instrument would have been the suspect and the real finding would have stayed buried. Landed as `18f6e68`: a comment in `app/layout.tsx` carrying the mechanism, the numbers with their date, and an explicit **fence against hand-rolling a `<link rel="preload">`**, which would double the preload on prod to paper over a build that is not the product. **THE LAYER-CHECK POLICY GAINS A FOURTH CLAUSE, from this arc rather than from 632's:** *presence of a runtime-injected element is a RENDERED-DOM question* — React streaming inserts the font preload from a flight `HL` hint, so **the served HTML reads identical whether the build emitted the preload or cannot emit it at all**, and a `curl` of prod would have confirmed the wrong conclusion on both sides. Only reading the `<link>` out of the rendered DOM distinguishes them. It sits alongside the three from HO 632 (CSS and inline JSX both; select by function; an empty selection fails) as the same failure in a fourth layer: the check looked where the thing is *authored* rather than where it *lands*. **Zero-external re-checked in both directions after C1b**, local and prod: exactly one font request, same-origin `/_next/static/media`, **zero off-origin requests of any kind and zero font-CDN requests** — the mock's CDN load was transport only. **RESIDUE MOVED, AND THE HANDOFF PRE-NAMED THAT IT COULD.** `/members` **13 → 9**, restated in the permanent-residue register at `ba65d5a` with the decomposition measured off `m1.rows` rather than the summary count: **7 knee-marginal `a.mc-row` at 121 / 123 / 123 / 124 / 124 / 126 / 129px + the 2 `.mc-row-hdr` at 230 / 230px** — four of the eleven knee rows crossed under threshold, the band narrowed 121–131 → 121–129, and **the header pair HELD, unmoved**. That split is the finding: the register's own claim was that the knee and the header-axis class differ in kind, and a text-metric change moving the first while unable to touch the second is that claim **arriving as a prediction that held**. **The face swap is named as CANDIDATE, not established cause** — no single-build before/after isolated it — and **the four crossed rows buy nothing forward**, having been priced as accepted residue at HO 612; whatever carried them under can carry them back over on the next face, weight or cap change. Site M1 restated **17 → 13**. **`/lobbying` is filed as a WATCH and deliberately NOT restated**, an incidental find of this arc rather than a product of it: the register carries 1, a crawl reads **3 at 215 / 179 / 129px**, and **the divergence was verified PRE-Plex, so it predates HO 633** — which is the only thing this arc is in a position to say about it. **The class attribution is the substantive half**: 215px is the `mc-row-hdr` the register already prices, while **179px and 129px are `lob-filing-row` DATA rows — a class the register never priced**, so this is not the accepted line growing and must not be folded into it. **The reviewer's candidate window (HO 597–598) is recorded as the reviewer's and as WRONG**: the 1 was priced at HO 615, re-affirmed at HO 619, and read 1 again in the terminal v4 crawl at HO 620, which **bounds the change to after 620** and outside that window. Site M1 is therefore stated as **13 with up to 2 unexplained — 15 if the WATCH attributes** — **cited, never subtracted**, so the closing identity is open on exactly one named line rather than balanced by dropping the rows that would unbalance it. **Gates:** audit `--route home` at 2560, closed state, matching the HO 632 close baselines exactly — **M1 0 · M2 0 · M4-true 1 / 42px · M4f 0 · M1x 0 · M3 5.18 · els 1795**, falsification **PASS**, so a whole-app face swap moved nothing on `/`; `--route members` run per the handoff's pre-naming, **fixture legs green FIRST at 18 of 18 exact** before any zero or any moved number was trusted; typecheck clean per commit; code, docs and SKILL never mixed. **Prod-verified live at `18f6e68`** by served SHA. **One upstream item is left as the owner's call with zero repo impact**: the Windows manifest bug is a clean `vercel/next.js` issue — mechanism, predicate line, repro all in hand — and **the layout comment protects CBT whether or not it is ever filed**. **HALT 1 ANSWERED, AND THE PRE-RULED DEFAULT WENT UNEXERCISED.** Recorded verbatim with both halves of its provenance, per the mechanism the previous arc established: given in the **DESIGN CHAT**, against a **live prod screenshot of `ba65d5a`** posted alongside — ***"good"***. **That is a live confirmation of the specimen ruling on the rendered surface, which is the one thing a specimen sign-off cannot supply**: the four-candidate knob ruled the face in the abstract, and this reads the shipped face on the real dashboard. **The relay carried a pre-ruled default for the no-verdict case** — that HALT 1 produced none, the specimen ruling (*"ibm"*) standing as the arc's only owner ruling and live confirmation declined — **and it is filed as UNEXERCISED rather than dropped**, so a later reader mistakes neither the presence of a fallback for its use nor its absence from the record for its never having existed. **The channel took one halt to get here, and that is the rule from `73d1273` working in both of its sentences**: the relay came back carrying a verdict SLOT rather than a verdict, the session halted instead of reading silence as assent, and the verdict then arrived as a quotation with named provenance. **The provenance also departed from the template it was requested under** — the relay anticipated *"typed into the Code session"* and the verdict was in fact given in the design chat against a screenshot — **so what is recorded is where it actually happened, not where it was expected to**; a provenance line back-fitted to its own template is worth nothing to the reader who later wants to check the reading rather than inherit it. Docs: HO 633 (this block). **Also notes now run through HO 633.**

**Also (HO 634), the `/lobbying` M1 divergence is attributed, priced and closed — and the closing identity balances again at site M1 15, with nothing unexplained.** The register carried `/lobbying` **1** while a crawl read **3**; the two extra rows are `div.mc-row.lob-filing-row` **data** rows, a different class from the column header the register already held. **The attribution is not a commit: the data crossed under a constant.** Neither candidate window contains the change, and the evidence is **byte identity rather than commit absence**: the `.lob-filing-row` block hashes to **md5 `bc49313d103615a3f976173dd7791770` at `62c25e4` and at `1e6435a`** — the same bytes across the whole window — with `git log 62c25e4..HEAD -- app/lobbying components/FilingRow.tsx` returning **ZERO** commits as corroboration, and the governing grid untouched since HO 614 `13203eb`. So **both windows (HO 597–598, and after-620) are retired AS RETIRED**, which is the reconciliation the WATCH asked for rather than an assumption that one of them applied. The mechanism is HO 614's cap: 340px, fitted to a then-current **minimum** rc ink of 239, with `gap ≈ 340 − rendered_ink + 12` — and a minimum over a growing corpus can only move one way, so every filing shorter than anything in that sample pays the difference straight into the gap. **Disposition (a), accepted residue. `(c′)` — per-row content sizing bounded at today's 340, `fit-content(340px)` — was MEASURED and DECLINED, and it is priced on the register so reopening is a decision rather than a rediscovery:** it buys over 5 → 1, header 215 → 12 and **zero vertical cost** (feed +0px, wrapped 63 → 63), and it spends the ISSUES column — x-origin spread **0 → 167px at 2560 and 0 → 201px at 1440**, worst where the surface is tightest — leaving a **partial** column, 78% of rows holding the old x at the bound while 22% pull left. **`.lob-filing-row` ships unchanged; no code, no CSS, no component was touched by this HO.** **FILED AS ADOPTED-UNVIEWED, and that is recorded rather than glossed: the eight paired renders were never opened, and the ISSUES-column loss — the thing the pass HALTed for — was ruled on the written recommendation, not by eye.** The shots stay on disk as the **unexercised record**, so a later reader knows which kind of decision this was. **Three things ship with it.** (1) The **header-axis class closes EXACT on all three routes** against a corrected **four-term** form, `gap = track_A − ink_A + column_gap + inset_B`, **valid only where `inset_A = 0`** — and the precondition is written into the register line, not a footnote, because `.mc-r-num` and AMOUNT are live counterexamples where the form gives 52 against a surface reading 32. `/trades` is the exhibit: `inset_B` **139px**, so the old three-term form predicts 57 against a measured **196**, off by more than the threshold itself. (2) **Two of the four terms are text metrics**, which corrects — append-only, HO 633's block untouched — its claim that the class "cannot be moved by type at all": the prediction held, the stated reason was over-strong, and it held because those metrics are short fixed labels rather than corpus data. (3) The **admission standard is amended twice**: a register line prices the row's **worst** gap, not the row (`/members` carries 4 over-threshold gaps across 2 header rows and the register carries 2), and a line may be **class + bound + statistic instead of a px constant** where the class genuinely has no constant — `/lobbying` M1 varies **1–3 per crawl**, measured twice on different days, 7 of 81 rows pooled at 121–179px. **Site M1 restated, cited and not subtracted: 9 (`/members`) + 2 (`/vote`) + 3 (`/lobbying`) + 1 (`/trades`) = 15**, where `/lobbying`'s 3 is 1 header row + 2 data rows and is itself a sample under that bound. **Two items found off the shots ride as backlog lines only because they verified**, and neither is 634 scope: the dashboard renders **243** and **138** twice each from **one predicate** each (`getStageChangesCount`, `getNewBillsThisWeekCount` — same cached call at both sites, diverging only under an active filter, week strip recommended to survive since it carries the deltas), and **`bills.stage_changed_at` records when the SYNC OBSERVED the advance, not when the stage changed** (`new Date().toISOString()` at both write sites; lag positive on 40 of 40 recent movers, median 3.76d, 40 rows across 2 minute-stamps) — which matters only because HO 632's day-grouping reads that field, so **"group and age from the same timestamp" holds with both off the observation clock**: internally consistent, externally wrong. **Also notes now run through HO 634.**

**Also (HO 635–636), the stage clock renamed and ENACTED re-sourced — a schema rename that fixes a CLASS of defect and exactly one instance of it; no theme-% change (one column renamed, one reader re-sourced, no new data domain).** The HO 634 block filed `bills.stage_changed_at` as a WATCH: both write sites stamp `new Date().toISOString()`, so the field records **when the sync OBSERVED an advance, not when the stage changed**, and HO 632's day-grouping reads it. **The census is what made the size legible — fifteen consumers read that name as occurrence, NINE of them windowing COUNTS on it** — and the disposition follows from that split: **rename, so every reader confronts what it is getting, plus re-source the one reader whose replacement anchor is exact.** `stage_changed_at` → **`stage_observed_at`** (`stage_transitions.changed_at` → `observed_at` folded in), across both writers, every production reader, all 14 `INDEXED BY` hints and the 17 diagnostics under `scripts/`. **THE RENAME FIXES NO INSTANCE, AND THAT IS THE POINT** — it converts every remaining class-(C) reader from an unexamined default into a deliberate choice. **ENACTED is the one instance, and it moved first and alone** (`9e95b21`), because for an enacted bill the latest action IS the enactment, so `latest_action_date` is exact rather than a proxy; **both halves of its WoW delta moved together**, since subtracting an occurrence count from an observation count is the same defect one level up. It changed a live number — the strip read `● 0 enacted ▼3` and now reads `±0`, because the shipped ▼3 was three enactments we *noticed* last week that had happened a month earlier. **THE FIGURES, WITH THEIR GRANULARITIES, BECAUSE THE NUMBERS ALONE MISREAD** (n=40 throughout, coverage **100%** — 40 resolved, 0 unresolved): the shipped clock bins a row into the right **week** **60.0% (24/40)**; `latest_action_date` bins it into the right **week** **85.0% (34/40)**; and `latest_action_date` **equals** the true advancing-action date exactly, at **DAY** granularity, **82.5% (33/40)**. **The last two are ONE PROXY AT TWO GRANULARITIES, not a proxy scored against a truth** — the ground-truth date carries no score, it is the yardstick — and **34 ⊇ 33 by construction**, since an exact date match is necessarily a same-week match. Disposition (4) therefore rests on **85.0 against 60.0**, both at the granularity the counting readers actually window on. A later reader who sees 85 beside 82.5 and reaches to reopen this should read that sentence first; the failure mode this paragraph exists to prevent is a closed decision reopened on a granularity mismatch. **WHAT DID NOT SHIP, STATED SO IT READS AS A DECISION AND NOT AN OVERSIGHT: eight class-(C) counting readers still window on the observation clock** — `getStageChanges` / `getStageChangesCount` (the `243 ▲15` strip, read at three sites), `getWeeklyBandPriorWeek`, `getWeeklyBandTransitionLadder`, `gatherLeadData`'s two transition reads, and `gatherReportData`'s stage-movements + first-tracked reads — carried forward as a QUEUED backlog entry with disposition (4) already priced. The six class-(B) display-age readers and three class-(A) ordering readers are **deliberately unmoved**: nothing in the probe says they must move, and observation time is arguably the right clock for ordering a terminal. **The census classes OVERLAP** (`getStageChanges` both windows and orders), so those class counts do not partition the fifteen. **Disposition (3) — a new occurrence column — is DISQUALIFIED ON POPULATION, not on effort:** a ~29h API backfill reaching **1,758 of 17,520** rows, so the corrected counts would be a partial-population figure with no honest way to signal that to a reader. **The measured stakes for whoever picks (4) up:** `TRANSITIONS` reads **243** on the shipped clock against **163** on occurrence — 49% high — and the four-week shape **INVERTS**, 112→230→228→243 versus 180→288→114→163, so the *delta* diverges threefold rather than the level merely shifting; **zero missing cron days across 28**, so this is structural to polling cadence and not an outage artifact. **THE MIGRATION SHAPE IS THE REUSABLE PART: expand → ONE cutover commit → an idempotent top-up run AFTER the deploy is confirmed live → contract**, at zero exposure and the cost of one extra `migrate` run. The expand adds the column, backfills it, and creates the **correctly-named** index before any hint can name it — a missing named index is a hard SQL error, not a silent fallback. **The contract is the FALSIFICATION LEG, not cleanup:** while the old column still exists a missed reference is *silent*, reading a column nobody writes and going stale invisibly; dropping it is what makes a missed reference fail loudly, so a clean run after it IS the proof that the cutover was complete. Verified either side — expand read gap 0 / mismatch 0 on both tables, and the pre-drop gate re-run against prod *after* the cutover was confirmed live read gap 0 on both, so no in-flight lambda from the old build beat the top-up. **`stage_transitions` DUAL-WROTE for exactly one release** because its `changed_at` is `NOT NULL` with no default — which is precisely why its zero readers did NOT license the bare `RENAME COLUMN` a reader-only analysis suggests. **A stale verbatim mirror was reconciled as part of the cutover:** `cold-start-audit-332.ts` pins copies of production SQL, and its `queryEnactedThisWeek` copy still windowed on the old clock after `9e95b21` moved it — so re-running unreconciled would have EXPLAINed a statement that no longer exists in the product and passed it green. Second citation of the HO 554→557 rule, this time inside the standing instrument built to catch mis-planned queries. **TWO CORRECTIONS TO THE HO 634 BLOCK ABOVE, appended rather than retro-edited:** `getStaleBills` windows on `latest_action_date` and was **never exposed** to this field (the 634 write-up misattributed it), and the legacy-NULL cohort is **15,631**, not the `~14k` `components/StagePillStrip.tsx` still documents (comment fix filed as QUEUED; HO 636 was docs-only). **And one correction to my own reading of the observation, worth more than either:** I read the three percentages as accuracy scores against a shared yardstick and concluded 82.5% must therefore be a coverage figure. Coverage was 100%. **A trio of percentages measured against one ground truth invites exactly that misreading unless each one carries its granularity** — which is why the figures above carry theirs. A second inference stated as if measured is corrected in the same spirit: the observed stale-value flip was seen against a **local `next start` and its `.next/cache`**, not against prod — no stale value was ever observed on prod, and the first prod read after `bb54c22` already served the corrected one. **Docs (HO 636):** this block · the WATCH closed with its remainder carried forward as a priced QUEUED entry · the 32-reference rename sweep · two new oddities entries and two ride-along clauses · the SKILL schema/index/query contract plus three durable clauses (the migration pattern, the `ensureColumn` resurrection trap, the index-name lie). **The sweep left every CLOSED roadmap, backlog and oddities block alone by rule** — an entry naming `stage_changed_at` was correct when it was written, and a sweep that silently rewrites history is worse than one that leaves a stale name in a 2026-07 block; where a reader would be misled, this block carries the correction. **Also notes now run through HO 636.**

**Also (HO 637), disposition (4) survives falsification and is HELD anyway — the anchor is right and the maturity is not; no theme-% change (a read-only probe and a ruling, no code).** The HO 636 QUEUED entry recorded (4) — re-source the eight class-(C) counting readers from `stage_observed_at` onto `latest_action_date` — as **priced and proven viable** off an 85.0%-vs-60.0% week-binning figure. **That figure measured BINNING ACCURACY FOR ROWS ALREADY IN THE SET; it never measured SET MEMBERSHIP**, and moving the window changes which rows are in the set at all. So this HO measured membership, and then measured the thing neither the handoff nor the entry had thought to ask. **On membership, (4) SURVIVES:** across the current week and three prior (cohort **1,556** stamped rows, 2026-08-11), the rows (4) admits that the shipped clock does not are **overwhelmingly late notice, not staleness** — 0/57/144/157 per window observed *after* the window closed, against only 3/2/6/12 observed before it opened, and the share of set (2) stale beyond the p90 observation lag (16.01d) is **0.6% · 0.0% · 0.9% · 5.1%**. Corpus-wide only **52 of 1,556** rows (3.3%) have any action postdating their observed advance, **28** by more than a week. The feared membership error is real and small. **Its measured size is a FLOOR, not a steady state** — both sets require the stamp, which begins 2026-05-11, so the staleness ceiling is 92.1 days and the rate can only grow as the stamped population ages.

**WHAT HELD IT IS A DEFECT THE ARC HAD NOT LOOKED FOR: MATURATION.** Under (4) the predicate still requires `stage_observed_at IS NOT NULL`, so a week's count **keeps growing as late observations arrive** — 57 of w1's eventual 106 rows landed after w1 closed. The trailing-7d window is therefore always missing its unobserved tail while the comparison week has matured, and **the WoW delta is biased negative by construction**. The settling curve, pooled over seven closed windows: **f(0)=32.7% · f(3)=63.2% · f(7)=79.7% · f(14)=92.1% · f(21)=95.0%**. The bias floor is `f(N+7) − f(N)` — **at N=0 it is 47.0pp, meaning a perfectly flat week renders −47% WoW.** Live confirmation of direction: w0 reads 155 against w1's 106 for a naive **+46.2%**, but at equal maturity (both at m=0) it is 155 against **49**, **+216%** — understated ~4.7x. **This is the ENACTED lesson one level over: same clock on both halves, different MATURITIES.** The shipped clock has no maturation term at all — its window and its stamp are the same event, so f(0)=100% by construction, which is the property being given up.

**Three ways out were priced and all three were refused, so the entry records refusals rather than an open question.** **(a) A SETTLING LAG of N=14** brings the bias to 3.0pp with 92.1% in hand — and buys a 3pp residual on a strip then labelled for a week that ended a fortnight ago. **The dashboard's claim is that it answers what is happening NOW; a 14-day-stale live signal is a different product, not a tuned one — owner ruling, and it is not a parameter.** N=21 reaches −0.7pp for another week of staleness. **(b) (4d) DISCLOSE-DON'T-DELAY** — keep the window current and mark the week as still settling, the way a tape marks an unsettled close — **PRICED BADLY AND DECLINED ON THE MEASUREMENT.** Completeness at close ranges **3.9% to 70.3%, an 18.0x spread, median 34.2%**, so *one* caveat cannot describe the number: the same mark would sit over a reading 70% complete one week and 4% the next, and w4 would have rendered **3** against an eventual **77**. The cause is that observation is **batched, not continuous** — over 56 days, 45 days carry any observation and 11 carry none, per-observing-day median 12 against a max of 137 — so the close-day reading is a function of whether a batch landed before or after the boundary, i.e. of sync cadence rather than of Congress. That is not an incomplete number, it is a differently wrong one, and the trade this project keeps choosing is wrong-for-incomplete. **The scheme's "settled prior-week delta" is not settled either:** at any week's close its two halves sit at f(7) and f(14) by construction, a −12.4pp floor, and simulated across w0–w5 the shown delta sign-flips against the settled truth twice (−4% shown vs +2% true; −15% vs +89%). A genuinely settled delta needs both halves at m≥14 — the 14-day staleness relocated into the delta, not removed. **(c) So GROUP A IS HELD**, pending an owner ruling on the label, not on a parameter.

**The eight are ruled per reader, and the handoff's own groupings were corrected twice by the code.** **Group A — four readers that move together or not at all:** `getStageChangesCount` · `getStageChanges` · `getWeeklyBandTransitionLadder` · `getWeeklyBandPriorWeek`. **The handoff's "`getStageChanges` is separable, a (C)+(A) hybrid" was WRONG**: the feed and its count are co-rendered on three surfaces (`app/changes/page.tsx:57`, `app/welcome/page.tsx:138`, `components/ActivityTicker.tsx:56`) and are **arithmetically coupled** — `ActivityTicker.tsx:63`'s `remaining = counts.total - bills.length` drives the `[ + N more → ]` footer, so moving the count alone makes that number silently wrong, clamped at 0 when the count drops below the rendered rows. The ladder coupling was verified rather than inherited (same `buildChangesWhere({},7)` + `GROUP BY stage`; w0 sums 220+23+3 = 246 = the headline). **APPROVED IN PRINCIPLE, HELD on maturity.** When it moves: hint to `idx_bills_latest_action` **in the same edit**, the forced-hint justification comment at **all four sites** (`queries.ts:6793`, `queries.ts:6835`, `dashboard-lead.ts:51-57`, `report-generation.ts:669-676`) — not the one the handoff named — and `cold-start-audit-332.ts:205,207`'s pinned copies reconciled in the same edit. **Group B — `gatherLeadData`'s two reads: DECLINED**, recorded as declined rather than left open; the dashboard lead has been orphaned since HO 244 and moving a clock inside an unrendered generator is churn. **Group C — the handoff's "two reads" ARE NOT A PAIR, and the split is the arc's best catch:** `report-generation.ts:676` is a count over a named calendar week, already `date(stage_observed_at) BETWEEN ? AND ?`, while `:720`'s `MIN(date(stage_observed_at))` is `stageTrackingStart`, the provenance guard that lets a report say "this week predates stage tracking" instead of "Congress was idle" — **a statement ABOUT the observation clock, which must never move.** Moving `:676` alone would also make that guard insufficient: **118 cohort rows (7.6%), action dates spanning 2025-01-23..2026-05-07**, would count as transitions in weeks that predate tracking while the guard still said the week predates tracking. **`:676` NEVERTHELESS STAYS, and the reason is maturation, not the guard** — the report generates Monday 09:30 UTC for a week that closed ~9.5 hours earlier, maturity ≈0.4d, so under (4) it would **freeze ~33–43% of the week's movements into `reports.content_md` and never revisit them**, where a live reader at least self-corrects as observations arrive. Under the shipped clock the report's count is complete-as-defined at week close. **The split stands as analysis; the move does not happen.**

**THE LANDED HO 635–636 FIGURES NEED THIS CORRECTION, AND THE DECOMPOSITION SHOWS BOTH PRIOR FRAMINGS WERE WRONG.** The four-week occurrence column `163 / 114 / 288 / 180` and the claims built on it — **"243 against 163, 49% high"** and a four-week shape that **INVERTS** — read the observation count as an inflation of a true occurrence count. **"49% high" is not supported.** The two anchors do not differ by spurious membership (0.6% this week); they differ by **BINNING**, and **neither figure is the true count**: 243 is *moves we noticed this week*, 155/163 is *bills we ever saw advance whose latest action of any kind fell this week*, and advances that actually **occurred** this week remain unmeasured — disposition (3), a new occurrence column, stays disqualified on population (~1,758 of 17,520 reachable). **The shape inversion is real but is a RE-BINNING, not the correction of an error.** **Do not write the gap as 111** — that is a net (806 in (1) over the 28-day span against 695 in (2)) and it describes neither population. The gross is **126 rows (1) counts that (4) does not, against 15 (4) counts that (1) does not**, and 126 − 15 = 111. **Of the 126, both mechanisms are present and the earlier one-sided readings were each half right:** **34 sit at >30d lag** — genuine phantoms that (1) counts as recent transitions and (4) correctly rebins away, the exhibit being **`119-s-3674`, latest action 2026-01-26, first observed advancing 2026-08-07**, counted by the shipped clock as a transition in the last four weeks — while the remaining ~92 sit at ordinary observation lag straddling the span boundary, which (4) pushes backward out of view rather than removing. Median lag 16d, p90 58d, max 193d.

**Docs (HO 637):** this block · the QUEUED entry rewritten to carry the settling curve and the per-reader rulings, so a later reader inherits the refusals instead of rediscovering the bias · a new backlog line for the orphaned lead-generation path. **Finding: the shipped clock's own window compares a 24-char ISO instant against `datetime('now', …)`'s space-separated form, and `'T'` (0x54) > `' '` (0x20), so the boundary calendar day is admitted whole at the lower bound and excluded whole at `getWeeklyBandPriorWeek`'s upper bound — measured at −5..+6 rows/week against a true-instant compare.** It **self-resolves for Group A** the moment both bounds sit on a date-only column, so it is not fixed twice; its only residue in product code is `lib/dashboard-lead.ts:64` and `:103`, the orphaned generator — **retiring that path closes this finding outright**, which is why the backlog line carries it. Older probes pinning the same shape (`cold-start-246`, `filtered-view-500-382`, `sync-trailing-cost-479`, `stage-clock-635`) are a sweep line, not a gate. **Also notes now run through HO 637.**

**Also (HO 638–639), the ME Senate card named a field the seat no longer had, and `nominee` enters the `race_candidates` vocabulary as a distinct status handled identically to `won_primary` — no theme-% change (one value added to an existing vocabulary plus a three-row roster correction; no new data domain).** `/race/S-ME-2026` and the dashboard race card showed **Mills and Platner** as the active field while the seat's actual general-election candidate was **Troy Jackson**, nominated at a **2026-07-25 party convention** after Platner won the June 9 primary and **withdrew on July 10**. **Three mechanisms independently guaranteed it would never self-correct, and the third is not a variant of the second.** (1) The `races-seed.json` entry was frozen — a hand-curated roster nothing re-derives. (2) `backfill:race-challengers`' `source_url='harvest:primary_winner'` **sentinel guard structurally skips any race that already carries a non-sentinel row**, which is exactly what a hand-seeded strip race is — so the harvest could never reach S-ME by construction, not by accident. (3) Found in Phase 1 and independent of both: ME's `primary_candidates` carry **no `status='winner'` row at all**, so even with the guard lifted the harvest is **inert** for that seat — it derives challengers from primary winners and ME has none recorded. Removing any one bar leaves the other two standing.

**Two corrections to the handoff, recorded rather than retro-fitted.** Its §1 claimed the seed was frozen at HO 171 with `last_verified` 2026-06-01. **Wrong: `seed:races` RAN on 2026-07-03 and it touched ME.** The JSON *entry* was frozen, not the run — the write happened, the content didn't change, and the stamp moved anyway. That is the finding, not a footnote: it is the entire mechanism behind the `last_verified` oddity filed this arc. Second, the **"NJ-07 shape precedent"** cited when the seed diff was approved is **cosmetic**. JSON array order feeds nothing (the loader upserts row by row) and display order is the SQL `CASE` ladder alone — so a correct NJ-07 render is **not evidence** that the file's candidate ordering did anything at all, and it must not be cited as one again.

**The vocabulary, and why the expedient thing was refused.** `nominee` ships as a **distinct status handled identically to `won_primary` everywhere** — a different HOW of becoming the party's general-election candidate, not a different WHAT. Seeding Jackson as `won_primary` would have rendered correctly today, which is precisely the trap: it writes a **false claim into a status column that nothing downstream re-reads** (he won no primary; Platner did), and a false-but-correctly-rendering value is the HO 577 defect class carrying the HO 420 cost. **Five hard sites, not the four the handoff counted.** The missed one is **`buildRoster`'s `wonPrimary` flag**, which feeds `favoredMember`'s party-market tiebreak — a nominee who didn't set that flag would lose the tiebreak to a `withdrew` row. `RosterMember.wonPrimary` was **renamed `isNominee` in the same commit**: once `nominee` sets that flag, the old field name is the same status-column lie relocated one layer inward.

**The falsification, and the two sites ME cannot reach.** With Mills and Platner both `withdrew`, `active.length === 1` and `ofParty.length === 1` **short-circuit before either flag is read** — so `buildRoster` and `deriveMatchup` are **unreachable from ME's own data** and are proven by the scratch third-candidate fixture **alone**. Measured, not asserted: without the fixture, `nominee` and a garbage status are **indistinguishable** on those two paths. A green ME card is **not** a fire of them, and the provenance is stated here so a later reader doesn't inherit the wrong one.

**Two soft sites got comments, not code.** `RaceMapCard` and `RaceDistrictCard` render the raw status generically, so `nominee` degrades correctly with **no edit** — that is **luck, not design**, and it is the opposite failure mode from `statusLabel`'s `default:` rung, which silently swallows an unknown. The comments mark the generic path as **load-bearing** so a later tidy toward an explicit switch doesn't manufacture a third silent-default site.

**The ordering fire, and why it could not run from `tsx`.** Both ladders are `unstable_cache`-wrapped, and calling one outside a Next request context throws `Invariant: incrementalCache missing` — this codebase's own HO 622. So `getRaceCandidates` was fired through **`GET /api/race/S-ME-2026/hub`** and `getRaceCandidatesForCycle` through a **rendered `/electoral`**, its only consumer. Both returned **Jackson 0, Platner 1, Mills 2**; the failing signature would have been **Jackson at 3+**, sorted below the withdrawn rows. The raw status column was read back as the literal string `nominee` **first**, before either ordering was trusted — because `seed:races` does not validate status and a typo would have produced a **byte-identical failure signature** at the seed step.

**Two things the fire turned up that nobody asked for.** `buildRacesCartogram` had to be **read** before `/electoral`'s DOM order could count as a SQL fire — it is a pure grouping pass with no sort, so the array order is the query's; had it sorted, the reading would have been **JS measured and called SQL**. And the locator **failed loudly on its first run** rather than returning an empty selection: `kalshiOdds` carries a nested `"raceId":"S-ME-2026"` that tripped the boundary guard, so the rewrite selects by **roster content** and then back-tracks to the enclosing `raceId` to verify provenance (57 challenger arrays in the payload, **exactly one** matched).

**One thing that looks like evidence and isn't.** The live ME card renders `Jackson  D · nominee`. That string is **`RaceCard:206`, a hardcoded label for the `challenger.kind === "nominee"` SHAPE** (HO 305) — unrelated to the status column, and it would read identically if Jackson were `won_primary`. The status-column evidence is the **two orderings** plus `/race/S-ME-2026`'s title-case **`Nominee`**. This is the arc's own instance of the layer-check policy: the check has to look where the value **lands**, not where a similar-looking string is **authored**. Filed to oddities.

**The instrument that already knew, and it is the sharpest finding of the arc.** **Kalshi (65%) and Polymarket (69%) both named Troy Jackson as favorite** while the roster printed beside them on the same card named Platner and Mills. `favoredMember`'s name-market branch does `roster.find(m => nameKey(m.name) === nameKey(fav.favoriteLabel)) ?? null` — so it **computed "this market names someone who is not on this roster" on every render for roughly two and a half weeks** and **discarded it as a null**, falling back to a party label. Two independent feeds disagreed with the curated data beside them and **nothing surfaced it**. Meanwhile `races.last_verified` read **green on the defect**: `/race/S-ME-2026` printed "last verified 2026-07-03" beneath a roster **two candidate-states** behind, because the stamp records when `seed:races` last ran and stamps whether or not the entry's content changed — the **same-as-success shape** (HO 503/506/637) reaching a **live user-facing surface**, with a live exhibit. Both are filed: the market-vs-roster disagreement as a queued detector, the stamp as an oddity.

**What the fix did NOT do, stated as a decision.** Mills and Platner **stay as rows**: `seed:races` is insert/upsert with **no delete pass**, so removing them from the JSON is a no-op and both would render as `running` forever — they had to be **transitioned by status**, and `withdrew` is accurate for both. And ride-along B left the **39-race `last_verified` re-stamp unpatched** (2026-07-03 → 2026-08-11, of which exactly one was actually verified): a hand-restore of the other 38 is **reversed by the next run with nobody watching**, which is worse than a uniformly meaningless stamp. The stamp is not trustworthy either way; the honest move is to record why, not to launder 38 dates into looking deliberate.

**Docs (HO 639):** this block · three backlog QUEUED lines (the name-market stale-roster detector, the 11 shares-without-a-winner contests split into their two classes, the absent convention-nominee ingestion path) and two WATCH lines (the twice-transcribed ordering ladder, `seed:races` validating ratings but not status) · two oddities entries (the `RaceCard:206` shape label, the `last_verified` run-stamp) · SKILL's three stale status enumerations corrected with a vocabulary note, the harvest passage's `won_primary` deliberately left alone. **Also notes now run through HO 639.**

**Also (HO 640–641), the two-class split was a story and the measurement killed it — and underneath it sat a settled-guard that freezes a contest the instant it is scored; no theme-% change (a read-only probe and its docs).** HO 640 was scoped to characterize the 2 contests HO 639 left uncharacterized inside the 11 shares-without-a-winner contests. **It started by reproducing the whole partition rather than starting from the 11, and that is what licensed everything after it:** on the predicate copied verbatim from the HO 638 probe (`primary_date IS NOT NULL AND primary_date < today AND election_round = 'primary'`), it read **718 / 506 / 212 → 11 / 123 / 78, every delta zero**. A drifting denominator would have made every number below it a measurement of something else; it didn't drift, so the characterization stands on the same ground the original claim did.

**The category-error half of the split is FALSIFIED, with numbers.** HO 639's backlog line asserted the 8 top-two/jungle contests were a **model** error — two candidates advance by design, so a single `status='winner'` row is a category error. Measured across completed contest-party `open` contests: **CA 52 total / 48 with winners / 48 carrying exactly TWO winner rows. WA 10 / 3 / 3. Zero contests anywhere carry exactly one.** Fifty-one top-two contests are marked correctly with two advancers each — the schema, the ingester and HO 207's two-★ rule all handle top-two properly. So the 8 are not a category error; **they failed the same way ME and NJ did**, and the 11 collapse to **one class, not two**.

**The count was wrong in its inputs too, and the correction names where the phantom came from.** **ME is two contests, not one** — `senate-ME-2026-D` and `senate-ME-2026-R` are separate `primaries` rows — so the real split is **8 / 2 / 1**, and the single genuinely unclassified row is **`house-NJ-09-2026-R`**: an ordinary partisan primary, two candidates, Pino at 51.5%, a derivable winner, no top-two or RCV excuse. Noticing that 8 + ME didn't reach 11 was right; **inferring "2 uncharacterized contests" from the gap was not** — one of the two was an undercount, not a contest. A fourth shape rides inside ME: **`senate-ME-2026-R` carries one candidate, Collins at 100.0%**, uncontested, so there is no votebox contest for anyone to mark — which is exactly why the obvious guard fix is refused below.

**THE FINDING THAT OUTRANKS THE ONE THE PROBE WAS SENT FOR.** `isSettled` (`primaries-sync.ts:551-565`) is **past-dated AND carries a recorded share — with no winner term** — evaluated at all three sync write paths (`:646`, `:905`, `:1311`) **before the incoming roster is built**, while `status` and `vote_pct` are written in the **same INSERT** (`:685`/`:948`/`:1380`). A contest scraped in the window after Ballotpedia posts results and before it marks a winner therefore takes shares + `running` and **settles in that same instant**; every later tick refuses it. It is a race against Ballotpedia's call, and it keeps catching contests every cycle. **516 of 718 completed contests are settled — and most froze CORRECTLY, having been marked before they settled. The damage is the 11 that froze mid-write. The mechanism is the defect, not the count.** The arithmetic closes exactly rather than being cited as a residue: 506 with-winner + 11 = 517 against 516 with-shares, the one-row difference **measured, not inferred**, as `senate-MI-2026-R` — one candidate, winner marked, no share recorded.

**Two dispositions, and the second one already ships.** **Stopping new losses is a guard fix and NOT a one-liner:** adding a winner term un-freezes every contest that legitimately has no winner — the uncontested seats — and those re-scrape forever, so a correct fix needs a bounded re-check (eligible N days past `primary_date`, then settle regardless) or an attempt counter, with **N measured against how long Ballotpedia actually takes to call**. Probe, then build, under its own number. **Recovering the existing 11 needs no build at all:** `isSettled` is module-private and guards only the three sync paths, so **`npm run backfill:primary-results`** (and `reingest:primary-slate`) bypass it and write both columns — gated on one measurement, whether Ballotpedia has marked those voteboxes since, which is a live fetch and therefore not a docs-HO action. **"Permanent" was the review's word and it is struck**: the freeze is **unreachable by any automatic path, not irreversible**, and calling it permanent would have justified building a repair that already exists. What is true is the reporting — the cron reports success every tick while never touching these rows, the same-as-success shape reaching the ingestion side.

**The WA verdict is `structurally missing`, and the evidence is a same-tick contrast rather than a timestamp argument.** All ten completed WA contests carry `pc.updated_at` at or after their own 2026-08-04 date, so "un-swept" is retired outright. In the **same 2026-08-06 tick**, WA-04/06/10 each took **two** winner rows while WA-05/07/08/09 took none — same date, same run, same code path. `isWinner` originates at `primary-candidates-scrape.ts:212` off Ballotpedia's votebox `winner` CSS class, so **no mark means no winner**, and the discriminator was entirely whether that particular race had been called.

**A self-correction inside the probe, kept because the wrong number was the plausible one.** Reachability was first read off `primaries.race_id`, found NULL on all eleven, and reported **all 11 unreachable** — a finding that would have quietly closed the product question. That column is the **runoff** join key (4 of 876 rows, 0 of the 718 completed; `getRunoffsForRace` matches on it and is correct), **not** the primary→race link, which is the shape join `state` + `chamber` + `CAST(district AS INTEGER)` gated by `EXISTS(race_ratings)`. Re-measured: **product impact is 5, not 11** — NJ-09, WA-05, WA-08, ME-D, ME-R sit on rated seats; the other 6 are unrated and the harvest never reaches them regardless. Filed to oddities, with the wrong first pass as the exhibit. Ride-along noted there: the density comment at `queries.ts:1684` still reads **3/907** against a measured **4/876**.

**`primary_type` is NULL on all four WA rows, and the handoff's proposed attribution was wrong.** This is **not** an instance of the HO 584 fallback-default WATCH: `primaryTypeFor` (`primary-calendar-scrape.ts:62-67`) falls back to `return "open"` and **cannot emit NULL**, so citing 584 would file a live gap under a cause that reads as understood. It takes its own WATCH line with **mechanism unknown stated in those words** — **91 NULL of 876, 75 of the 718 completed**, across 12 groups, with **WA the only special-system state left NULL** (CA `top_two` 52, AK `top_four` 4, ME `ranked_choice` 6, LA `jungle` 6). The consequence is the reason it is carried: **a branch on `primary_type = 'top_two'` catches 52 CA and 0 WA**, and the miss looks like absent data rather than a bug.

**SKILL was checked and NOT edited, which is a result.** Three anchored greps — the `primary_candidates.status` enumeration (`'running' | 'winner'`), the `/primaries` ★-advancer passage, and the `race_id` schema comment ("loose link to races.id") — all read correct. The ★ passage's *"runoffs advance two, so two ★s is valid and expected"* is now **corroborated** by 48/48 CA and 3/3 WA rather than merely un-contradicted. **There is no Commit B**; manufacturing an edit to make the sweep look complete was refused.

**THE PATTERN, NAMED BECAUSE IT IS THREE FOR THREE AND ALL THREE ARE THE OWNER'S.** §1 of the HO 638 handoff said the seed was frozen at HO 171 with `last_verified` 2026-06-01 — the run had happened, on 2026-07-03, and touched ME. The **NJ-07 "shape precedent"** cited when the seed diff was approved was cosmetic — JSON array order feeds nothing. And now the **category-error split**, reasoned from what top-two *ought* to imply rather than from what the corpus *does*. Each was tidier than the truth, each survived until something measured it, and each was specified by the owner and implemented faithfully by Code. **A framing that makes the backlog tidier is a hypothesis, not a finding.** The counter-discipline is the one this probe ran by accident of instruction: reproduce the whole before characterizing the part, and re-derive a split from data rather than restating it from the report that proposed it.

**Cost:** 11 statements, **38,864 `rows_read`**, both table counts predicated (`WHERE id IS NOT NULL`) so no figure is quoted off a bare `COUNT(*)` — the HO 624 M0 trap, where a bare count is answered from B-tree interior pages and under-reports by 227–416×. `primaries` 876 rows, `primary_candidates` 2,556.

**Docs (HO 641):** this block · backlog QUEUED line 2 **rewritten** (its two-class premise is dead, so it was replaced rather than amended — an amendment would leave the falsified sentence sitting above its own correction) · a new QUEUED **freeze parent** carrying both dispositions · a new WATCH line for the NULL `primary_type` · two oddities entries (the subset-predicate guard, the `race_id` name) · no SKILL edit. Probe `ffde734`, read-only, pushed separately. **Also notes now run through HO 641.**
**Also (HO 642–643), a dashboard chrome batch, a chamber selector, and four rulings taken on measurement rather than on the exhibit that prompted them — no theme-% change (presentational edits, one filter on an existing panel, and three read-only probes; no new data domain).** **The build first, because it is the smallest part.** The HO 608 polarization chip is **cut from `/`** on an owner ruling that ideology belongs to `/members` only — `getPolarizationBand` stays (the members page reads it and `PolarizationOverTime` documents the registration-vs-caucus seam against it), but `/` no longer calls it at all, and the five `.weekly-band-polchip` rules went with it because `WeeklyBand.tsx` was their only consumer. **The breaking-row hover lost both of the things that put a shade over the bills:** the `:has()` cross-column dim that dropped the whole feed to 40%, and the overlay's drop shadow. The opaque `--bg-row-hover` fill plus a 1px `--border-strong` carry the overlay alone — verified by eye on the furthest-overrunning row, **351px past the panel edge and over the distributions**, which is the property that had to hold once the dim it leaned on was gone. **`.v2f-title` was never bold.** Measured before editing: computed weight **400**, IBM Plex Sans, 14px, `rgb(229,231,235)`. The perceived heaviness was **contrast, not weight**, so the lever was `--text-primary` → `--text-secondary`; the weight is now written explicitly so the next reader need not walk the chain to learn it was never set. C3 still holds — line 2 is `--text-dim` and the id chip amber, so the title stays the row's brightest element.

**`.abw-name` is the one whose MECHANISM IS AN UNFALSIFIED HYPOTHESIS, and it is recorded that way deliberately.** The MIA member names carried an unfamilied `font-weight: 600`, so they inherited `--font-mono` and asked **Consolas** — the Windows resolution of that stack — for a weight it does not ship; the browser synthesized a faux-bold, and synthesized Consolas bold is what reads as Comic Sans. That chain is **unfalsified, not proven**: it depends on which mono resolves on the owner's machine, and a rendered-font read here measures this box. The fix (`var(--sans)` at **500**, a real loaded Plex face since HO 633) is correct on every platform regardless, because it removes the synthesis rather than the symptom — which is why it shipped on an unproven mechanism. **The falsification is the owner's eye;** if it still reads wrong the next step is `CSS.getPlatformFontsForNode` on the owner's browser, not another guess. `.abw-title`/`-claim`/`-ev`/`-party` stay mono — chrome, and HO 297's split is exactly that line.

**`PER_CHAMBER` 2 → 3, and the handoff's stated mechanism was a reading of one viewport presented as the general case.** `.race-grid` is `repeat(auto-fit, minmax(min(400px,100%), 1fr))`, so the column count is whatever the container buys. Measured on the shipped dashboard: `.dash-left` **756px → 1 column** (viewport 1440), **1020px → 2** (1920), **1372px → 3** (2560). The handoff put "`.dash-left` of ~1370px → 3 columns" at the default; **1372px is the 2560 reading alone**, and at 1440 the grid is a single column. So the orphan the ask was pointed at is a **≥~2350px phenomenon** — four cards wrapping 3+1 — while at 1440 and 1920 four cards already filled their rows. The fix is unchanged and now covers more: **6 divides by 1, 2 and 3; 4 divides none of them**, so every column count this panel resolves to fills completely (measured row shapes `[1×6]` / `[2,2,2]` / `[3,3]`). It is the card count and not the grid: widening to a flat 2×2 would trade the hole for four ~680px cards, C8's failure mode. **The unpriced cost, stated because nobody priced it:** at 1440 the grid is one column, so six cards add roughly **500px of panel height at exactly the width that never had an orphan** — the fix is free only at the width that had the defect.

**The chamber selector was mostly already built, and the one real correction was in a count nobody would have checked.** `FeedFilters.chamber` and `buildFeedWhere`'s `bill_type IN (…)` already existed, and `buildChangesWhere`/`buildStaleWhere` compose on it, so MOVERS and TOP STALLS needed only a populated first argument; only `getNewBillsThisWeek` / `getNewBillsThisWeekCount` took signatures, and they moved in the same edit because that helper's own comment says its predicate is exactly the count's so the tab label cannot drift from the rows. **The load-bearing correction: `getStageChangesCount` computes `total` from `buildChangesWhere({}, days, dashboard)` — it DROPS its own `filters` argument by construction**, so a chamber passed in could never have reached `.total` and the badge would have stayed corpus-wide while the rows narrowed. Both call sites read **`.filtered`**, the arm that reads the filters; with no chamber the two arms are byte-identical, so the unfiltered default is unchanged. Verified by exact partition: MOVERS **335 → 116 / 219**, NEW THIS WEEK **173 → 44 / 129**, both summing back. `chamber` is feed-panel scope only — not in `DashboardFilters`, not touching `WeeklyBand`, BREAKING, the distributions or the masthead — and that is written at the derivation site, because a URL param reshaping exactly one panel is surprising and the next reader will otherwise "fix" it.

**The count coupling was PINNED, not merely imprecise, and that is the reason it got its own commit.** `ActivityTicker`'s `[ + N more → ]` took the chamber but not the dashboard stage/topic filters. Measured against prod running the pre-fix code as a control: under `?topics=healthcare` the footer read **305** — the corpus value — while the badge directly above it read **57** and thirty rows were rendered. Not drift; the filter reached the rows and not the count at all. Passing `filters` to the count makes it exact and costs nothing: it is the identical call `app/page.tsx` already makes for the badge, so it shares that `unstable_cache` entry — no new query, no new key. The zero branch is exercised and correct (`[ VIEW ALL CHANGES → ]`, never `+ 0`).

**R1 — `challenger.kind === "empty"` renders a finding it has never once earned, and the fix is AUTHORISED.** The S-MI exhibit (`no challenger filed` printed beside a Kalshi favourite, a Polymarket favourite and an AIPAC PAC line all naming people) turned out to be a class: across **186 rated seats**, **129 reach `kind === "empty"` and 0 of them are true-empty**. Every instance is roster-**absent**. That is sharper than a conflation — it is not a mixed population needing disambiguation, so a split sized for a mix would have been sized wrong. **`/electoral` already renders the same underlying state correctly**, as `RaceMapCard`'s *"Challenger field not yet available"* — an absence. Two surfaces, one state, two contradicting claims, and **the hedged one is the one nobody questioned**. The fix takes the number this sweep establishes and its shape is recorded here: the caller can distinguish on `hubs[i].candidates.length`, so `empty` splits into *roster absent → hedge* and *roster present, uncontested → the finding*. **The second branch has ZERO members in the live corpus**, so it must be **hand-fired on HEAD before it is ever recorded as working** — an unexercised guard is unproven, not protection (HO 552). Scratch only, never a checkout of pre-fix files, confirm it fires, revert, `git status` clean, commit nothing.

**R2 — the harvest is scoped separately, and the render fix does NOT wait for it.** `backfill:race-challengers`' reach stops at `primary_date` **2026-06-02** while winners exist through **2026-08-06**, and its sentinel rows sit at **82 across 53 seats — matching SKILL's recorded first-run figure exactly**, so it has effectively never advanced; the rated set growing **137 → 186** is a second, independent contributor to the 0-row count. Of the 129, only **62 are derivable** — running the harvest closes **under half** the class and leaves the false claim standing on the other **67** (the HO 638 ME shape, where no winner is recorded and the harvest is inert). **The HO 642 report's "hasn't been re-run since 2026-08-04" is CORRECTED here:** that was an inference, the measurement is roughly two months, and it matters because the guess made the staleness look like a week's lag. `race_candidates` carries **no `updated_at` at all**, so the bracket is behavioural — newest primary date reached versus newest reachable — which is **stronger than a timestamp column would have been**, not a workaround for its absence.

**R3 — the HO 639 market-vs-roster detector is NOT built, and the trigger is a re-measurement rather than a date.** M3 counted **88 of 186** seats where a card element names a person while the roster names nobody. An alarm firing on **47% of the rated set** reads the harvest's staleness, not the defect it was conceived for, and calibrating a threshold against a stale harvest bakes that staleness into the baseline permanently. **Trigger: re-measure M3 after the harvest is current.** Build only if it collapses to the ME-shape handful it was designed for.

**R4 — the at-risk lower bound is W = 8, and the PLATEAU is the finding rather than the rung.** Chosen by the rule fixed before the numbers existed: smallest W in [6,20] with pooled max ≤ 6 and median ≥ 1. Pooled max falls 7, 7, **5** and then **sits at 5 for every W from 8 through 20** while distinct members drain 24 → 7 — so raising the bound past 8 buys **no reduction in the peak at all**, only thinner transient traffic. The ceiling is set by roughly **five persistent long House absences that no bound in range excludes**; 5 of them are inside the band at W=20 exactly as at W=8. The band at 8 still spans 22 roll calls before promotion, well past the 10-roll product floor. **WATCH:** selected on a window whose House newest roll is **20 days old** and which lies entirely pre-recess, so it measures voting days rather than the gap but cannot see the return. Re-read after the House comes back, alongside the still-open S=30 review — which stays **unswept**, because moving both boundaries in one pass would make neither attributable. The tier itself remains **unbuilt** and belongs to the deferred MIA redesign.

**R5 — scope, stated so a later reader cannot over-read the 129.** It is **population-at-risk, not cards on screen**. The string lives in `components/RaceCard.tsx`, rendered only by `CompetitiveRacesStrip variant="v2"` — the dashboard's featured seats — so **1 of the 6 featured cards prints it today** (S-MI). PA-10, the other seat commit B newly featured and which nobody had looked at, is **clean**: one hand-curated Ballotpedia row (Janelle Stelson, `won_primary`) whose non-sentinel `source_url` makes the harvest's `NOT EXISTS` guard skip the seat entirely — the HO 213 seed-safe guard working as designed.

**What this arc REFUSED, each naming what would have closed a question prematurely.** The at-risk tier was **not declined off the one-cursor snapshot** — the snapshot read 0 and generalised to "any WARN in 5..29 selects zero", and the 60-cursor replay refuted it outright (median 4, 31 distinct members). The **ceiling of 6 was not moved to fit a W**, and the sweep was not extended past 20 to rescue the tier, because below that the band spans fewer than 10 rolls and warns nobody about anything. And the **harvest was not run to make the S-MI exhibit go green before the class was measured** — which would have turned a 129-seat render defect into a one-seat data chore and closed the wrong question.

**Cost:** three read-only probes. `absence-atrisk-642.ts` 21 statements / **148,545 `rows_read`** (the 60-cursor replay and the 15-rung W-sweep add **zero** queries — both are further passes over the control walk's in-memory matrix, and the statement count is byte-identical before and after them). `roster-coverage-642.ts` 8 statements / **851,974 `rows_read`**, the bulk being M5's could-reach join across `primaries × primary_candidates`. Every figure is a real per-statement `rows_read` via hrana `/v2/pipeline`; nothing is sized by a bare `COUNT(*)` (the HO 624 M0 trap).

**Docs (HO 643):** this block · four oddities entries filed as **one named family plus one outlier** (an index is not a clock; an element rect is not rendered ink; `grep -c '^-[^-]'` is not a deletion count; and separately a verdict string that does not read its own numbers) plus the transient-sampling lesson · four backlog lines (the `--numstat` diff instrument owed from 639–641, harvest currency, the M3 re-measure as R3's build trigger, and the `.home-grid` display orphan filed as a claim) · SKILL's rated-set figure corrected in both places it appears. **Also notes now run through HO 643.**

**Also (HO 644–646), a false negative that had never once been true, a band that became cards and grew a second tier, and the sweep that closed a three-hop pointer drift — no theme-% change (one render fork, one presentational rework plus a widened window on an existing query, and their docs).**

**HO 644 — the card stopped rendering an unknown as a finding.** `challenger.kind === "empty"` printed *"no challenger filed"* on **129 of 186 rated seats, 0 of them true-empty** — the branch had never once been true. `/electoral`'s `RaceMapCard` already rendered the same state correctly as an absence, so the fix was a **copy, not a design decision**: `deriveMatchup` now discriminates on `candidates.length` (roster absent → `unknown` → the hedge) rather than `active.length`, and the string is verbatim from `RaceMapCard`'s placeholder with an instruction to shorten both sites together if it ever has to move. **The sub-case named and NOT built for:** `activeChallengers` also filters withdrawn, so a roster whose challengers have all withdrawn lands on `empty` and renders the negative finding — arguably wrong for a field that *was* filed and then vacated. Zero members today, and a third variant on zero evidence is the mistake the change existed to fix.

**The arc's instrument lesson is HO 644's `/electoral` control, and it is the sharper of the two this arc produced.** The obvious check read **0 vs 0** on both strings — equality by absence, indistinguishable from *"neither surface renders it"*, and it would have passed while proving nothing. It was rescued by working out that `RaceMapCard` only mounts on `pinnedCell`, that `onStatePick` routes to the district modal instead, and that `onSearchSubmit` still pins — then pinning Michigan by search to force the state into existence. Same discipline on the payload: slice before the first `self.__next_f` push rather than accepting a raw `hedge=2`.

**HO 645 — the absence band becomes cards, and gains a second tier.** The band's rows are member cards now, ruled off `docs/design/mock-645-absence-cards-v6.html` (committed before the build, `0e4ab80`). Anatomy: a **mat** around the photo — the Topps border, retoned; it is what makes the thing read as a card rather than a tile, and collapsing it into a thick border loses the face's inner frame with it — the streak as a **corner disc** with its 6px mitre, a position chip, a name plate on a scrim over the photo's foot, and a two-cell stat line. The back is a new **330px `AbsenceCardBack`** with ruled stat rows. **`SponsorExpandedPanel` is NOT modified** — `/members` is its other consumer — and with it went the container query, both caps and the measured `calc(100vh - 360px)` fold bound that existed only to fit a ~1000px three-column card into a band-width overlay.

**Positioning: vertical in CSS, horizontal measured.** `top: calc(100% - 12px + 8px)` off the card `<ul>`, where `12px` is the rack's bottom padding and **`8px` is exactly how far a 12px square rotated 45° pokes above its box**, so the notch's tip meets the card rather than nearly meeting it — measured live at **−0.49px** tip-to-card-edge at both 1440 and 2560. Horizontal is read off the open card's `offsetLeft`/`offsetWidth` and clamped into the rack. It anchors to the **lowest** card, not the open one: with one row they are the same card and the notch touches, and if the list ever wraps the panel drops below both rows instead of covering the second — the HO 631 ruling surviving the rework. **Accepted cost, stated so it is not rediscovered as a defect:** the open panel covers the band's footnote and its delegate-carve disclosure.

**The at-risk tier is W = 8**, from HO 642 P2's pre-fixed criterion. Phase A's window widens from 30 to 8 and Phase B's walk is byte-identical; the bucket is `>= ABSENCE_STREAK_MIN` (30) → MIA, `[8, 30)` → at-risk. `ABSENCE_WARN_MIN` and the Phase A window are now **one constant**, because they are the same number by construction rather than coincidence — Phase A's `nv = n` over the last W rolls is a *necessary* condition for a streak of W, so a window wider than the smallest reported streak filters that member out before Phase B ever sees them. **`ABSENCE_STREAK_MIN` was NOT swept:** the S=30 review is a separate loop still gated on the House returning, and moving both boundaries in one pass makes neither attributable.

**Re-measured, not inherited** (`scripts/diagnostic/absence-payload-645.ts`, prod, 2026-08-11): Phase A **house 26,120 → 6,922** `rows_read`, **senate 6,060 → 1,616**, Phase B unchanged at **240 per chamber**. Narrowing W makes Phase A *cheaper* because the window IS the scan — an `IN (…)` over vote ids, so a wider one reads more rows to return fewer candidates. HO 642 read 8,674 for the house leg; today's corpus reads 6,922, which is why it was re-read rather than carried.

**Two branches fired rather than argued.** The amber tier is **empty in the live corpus** (MIA 2 / AT RISK 0) exactly as the occupancy replay predicted, so it was falsified by temporarily raising `ABSENCE_STREAK_MIN` to 40 — moving Wilson at streak 34 through the amber path on real HEAD with real data. The footnote rendered `MIA = 40+`, **which is itself the proof that the disclosure is interpolated and not hardcoded**. And deviation 5 retired HO 630's identity-only degrade on a structural argument; that argument was then **run**: `card === null` forced at the assembly site gives a complete back (three ruled rows of base-query data, head, sub-line, footnote), **zero** block elements rather than empty shells, and a live affordance (`role="button"`, `aria-expanded` `false→true`, one panel). **The control leg is what makes it evidence** — 0 blocks on a nulled member is trivially true, so the discriminating reading is the control's **2 blocks / 0 empty-shelled in the same page load**. Both reverted, nothing committed. **Named and NOT covered by the PASS:** the *cause* was not exercised — the forced null sits downstream of `queryAbsenceWatch`'s per-member `try/catch`, so that catch's `console.error` and its `Promise.all` isolation are still unfired, and they are carried as a WATCH rather than folded into the green.

**Eleven deviations from the handoff, all disclosed. The three load-bearing ones:** the card is `div[role="button"]` rather than the mock's `<button>`, because §1a keeps the name an `<a>` and an anchor inside a button is invalid HTML; the disc reads `60+` at the walk bound, because there the streak is a **floor, not a count**; and the front stat cell says **MISSED**, not the handoff's `119TH` — `missedPct` is a missed rate and **the mock's label named its complement**. That last one is the HO 631 rule earning its keep on a new axis: illustrative mock data set no *numeric* expectation there, and here it nearly set a **semantic** one, on the surface whose entire claim is a member's voting record. **The rule is extended: an illustrative mock sets neither a figure nor a field's meaning.**

**Also recorded, because a substituted proof should read as a substitution:** §5.7's byte diff on `/members` was **not run** — a stash-and-rebuild is unsafe on a shared working tree — and was replaced by a structural proof that names each way the page could have changed and closes it (`SponsorExpandedPanel` and `app/members/page.tsx` untouched; the `globals.css` diff confined to the `.abw` block; zero `.abw-*` classes served), ending at the served string `class="aspect-[3/4] shrink-0 object-cover" style="width:96px;…"`, preserved character for character by reordering the class interpolation. **`SponsorPhoto` *was* modified; its emitted output was not.** Its fallback branch has no instance on that page, so that leg is proven by inspection, not live.

**HO 646 — this sweep, and the drift it exists to prevent.** The pointer had gone **three hops**: 643's block authorised 644, 645 was derived from 644, 646 from 645, with nothing writing a block in between. **The numbering convention reads from the roadmap**, so a doc sweep is not optional bookkeeping — three hops of derivation is exactly the drift it exists to prevent, and each hop was a number nobody could check against the record.

**Two premises in the HO 646 handoff were wrong on inspection, and correcting them here is what keeps this block from asserting them.** (1) §4 instructs *"read SKILL's Absence Watch passage first"* and the Docs line calls for it **rewritten**. **There is no such passage.** Measured at `8852478`: zero occurrences of `MIA`, `streak`, `ABSENCE_`, `getAbsenceWatch`, `AbsenceWatch`, or HO 622/630/631 anywhere in `SKILL.md`; the single `Absence` hit is *"Absence of badge is the correct signal"* on the member hub. So commit 4 **ADDS** the passage — the band shipped at HO 622 and was reworked twice without ever reaching SKILL, which is a three-HO documentation gap this sweep closes rather than a rewrite it performs. (2) The `/` page entry's **body order is stale beyond the band**: it lists a three-item body (`CompetitiveRacesBlock` → `WeeklyBand` → two-column) that predates HO 608's region split and HO 270's `RacesBoxTabs`, so the band could not simply be slotted into a correct list. The band's slot is named in that entry; **the rest of its staleness is left standing and recorded here rather than silently half-corrected** — a partial rewrite of a stale enumeration reads as a current one.

**One process note, from HO 645's own commits, because the pattern is worth a clause rather than three.** Each of its three commits mixed kinds. `0e4ab80` is labelled `docs(...)` and carries the `AbsenceWatchRows → AbsenceWatchCards` rename — splitting a pure rename from its content change is the **right** technique, and it is what let git detect the rename and keep the file's history, but the vehicle was the docs commit. `6a92bbe` carries a diagnostic edit inside a build. And `8852478` carries the backlog re-price alongside code — **that one is the handoff's fault, not the build's**: HO 645 §4 mandated the re-price as a condition of shipping B, which contradicted the standing rule that docs never ride with code. **Recorded as an instruction defect: when a handoff mandates a doc edit as a condition of shipping, it must name it as its own commit in the commit table.**

**Docs (HO 646):** this block · three oddities entries (a threshold expressed in the wrong variable; `__name is not defined` as a build artifact; a lesson encoded in one instrument is not inherited by the next) plus two live confirmations noted against the existing same-as-success rule rather than re-filed · four backlog lines (the payload entry amended with its by-id breakdown, and three new: the unfired `card === null` cause, the 119TH denominator at zero query cost, the `SponsorPhoto` fallback residue) · **SKILL's absence-band passage ADDED, not rewritten** — see the premise correction above. **Also notes now run through HO 646.**
**Banked / unbuilt:** **strong VIA-markup report attribution** (blocked, not deferred — `stage_observed_at` history (renamed from `stage_changed_at` at HO 635) began 2026-05-11, 52 committee→floor moves all-time, no markup-date alignment → 0 clean joins; HO 268 Gate A; revisit once the history matures) · **MARKUPS column on the reports index stat strip** (queued from 268 — needs a new persisted column + migration + backfill) · **MOVERS from→to display + hop-count sort** (gated on `stage_transitions` accrual, planted 2026-06-11) · ~~metro-zoom panels (spec-3 Phase 2)~~ (**SHIPPED HO 236/237** — dense-state CA/TX metro insets + leader lines; see backlog DONE. Extending the per-state config to more dense states is a fresh scoped item, not this banked one.) · ~~**race→news / member→news surface**~~ (**both halves SHIPPED** — race→news hub HO 398, member→news hub HO 414, off the HO 394 observation layer's `observation_entities` entity join; residual drawer/challengers + the dark HO 272 `NEW` badge tracked in backlog QUEUED) · **observation inert fields** (grading / cluster / lineage — activate when a second collector or a real consumer arrives) · **the_hill budget-starvation soak** (watch a few news ticks that politico runs long — confirm the_hill isn't systematically skipped; fix is per-feed budget carving, not a timeout raise) · primaries-map results-coloring (recency-only today) · rating-history sparkline (logging live since HO 220; chart awaits weeks of data).

**Owed eyeball:** **hearings LIVE state** — the ● LIVE badge (v2 band callout + `/hearings` list/calendar glyph) was never exercised against a real streaming meeting (0 live in every build sample); confirm it renders correctly the next time a committee meeting is actually live.

**Also (HO 654), a count stopped claiming to be about Congress, and the delta beside it stopped being rendered at all.** `getStageChangesCount` takes **disposition (4c)** — decline to re-source, disclose instead, stay on the observation clock — and with it **Group A closes as a set** (`getStageChanges` · `getWeeklyBandTransitionLadder` · `getWeeklyBandPriorWeek`). The group's own rule was *move together or not at all*, which governs PREDICATE moves; this HO moves no predicate, so the coupling is honoured rather than broken, and there was no case for disposing one reader out of a coupled four. The HO 636 entry's stated close criterion was an owner ruling on the strip's LABEL, and that ruling is this one.

**The strip reads `TRANSITIONS OBSERVED` and carries no week-over-week delta.** The reason is NOT that observation is fine — it is that **neither available clock yields a correct delta and no third anchor exists in the database.** On the observation clock a delta partly measures sync cadence: `stage_observed_at` is stamped with the wall clock of the run, so an outage dips and the catch-up spikes, and both read on the dashboard as activity in Congress. On occurrence it is biased negative by construction, since 54% of a week's final count arrives after the week closes. HO 635 measured **243 observed against 163 occurrence** with the four-week shape inverting; HO 637 then disqualified the 163 as well, because windowing on `latest_action_date` admits a May referral that picked up a cosponsor yesterday. The LEVEL survives all of that — it is a real count of a real thing — so the count stays and the label now names it.

**Removed in both renderings, which is the part that took a ruling.** The inline `▲N` and the hover card's `▲N vs last wk` + `last wk N · MON DD` are both gone. Disclosure does not rescue a delta: the lag copy explains why the LEVEL is inflated and says nothing about whether the delta's SIGN is trustworthy, and copy that did say so would be a number rendered beside an instruction not to believe it. The hover reader is the one who asked for detail, so serving them a dated prior-week figure is worse than serving them nothing. `prior`/`priorDate` are now optional on `WeeklyBandMetricCard` and their **absence is the claim**. The disclosure sits in that card rather than a Tooltip — the band has none, and the card records why (the primitive does label+body only; the dashboard's stacking would occlude an in-flow card) — so the copy is hover-only and **the label carries the honesty at rest.**

**Figures recorded, not characterised.** At the edit the strip read **361**, prior **176**, delta **▲185**, priorDate 2026-08-05. The ladder's rungs were **committee 335 + floor 24 + other_chamber 2 = 361**, summing to the headline exactly — recorded BEFORE the edit so that a pre-existing gap could not later be misread as this HO breaking the sum; the `introduced`/NULL case that could open one is absent from this window. The delta figure is left as a figure: **▲15 at HO 653's writing and ▲185 here** is consistent with the volatility this ruling assumes and equally consistent with a genuinely busy fortnight, and the data does not separate those.

**Two surfacing facts worth having on record.** The count is read at **five** sites, not the three HO 635 named — `WeeklyBand`, `app/page.tsx`, `ActivityTicker` on `/`, plus `app/changes/page.tsx` and `app/welcome/page.tsx`. Only `WeeklyBand` renders a label or a delta, so the relabel touched one component; but **`/welcome` renders the number as `MOVERS(361)` with no hover surface, so no disclosure reaches it.** `MOVERS` makes no timing claim, so no relabel is owed there — it is simply a surface where the figure appears unqualified. And the ENACTED cross-clock defect HO 654 was told to look for **does not exist**: `queryEnactedThisWeek` and `queryEnactedPriorWeekCount` both window on `latest_action_date` (`lib/enacted-this-week.ts:67-71` and `:97-101`), moved in lockstep by HO 635.

**Docs (HO 654):** this block · the HO 636 class-(C) entry struck (all eight readers dispositioned: Group A closed here, Group B declined at 637, Group C ruled to stay) · one new backlog entry for a stale e2e fixture found in passing · SKILL's strip and `getStageChangesCount` entries state that the count is of observations and that the delta was deliberately removed. **Notes now run through HO 654.**

**Also (HO 656), four open loops closed or advanced by RUNNING things that already shipped — and the one that needed new code needed two fields.** No migration, no cron wiring, no `isSettled` edit. **§A — the freeze recovery is PROVEN on real data.** The gate was one live Ballotpedia fetch of `house-NJ-09-2026-R`: **Pino is now marked `isWinner=true` at 51.3%**, so the freeze is what the entry said it was — **unreachable by the automatic paths, NOT irreversible**. `npm run backfill:primary-results` (which bypasses `isSettled` entirely) then ran off-tick: **398 seats, 370 ok / 28 empty-or-failed, 1,926 candidate rows updated, `vote_pct` 1,663 → 2,077 of 2,527**. **The frozen set went 11 → 6, with 7 of the original 11 recovered** (CA-04/14/16/43, NJ-09, WA-07, WA-09) and **4 residual** (WA-05, WA-08, ME-D, ME-R). The residuals were live-checked rather than counted: **all three pages parse `ok` and mark ZERO winners**, so the residue is **upstream-unmarked, not a parse failure** — `senate-ME-2026-R` (Collins, uncontested, 100.0%) being the expected-and-correct case the entry already named.

**Two things the run showed that no amount of reading would have.** First, **the population is a MOVING SET, not a fixed 11** — two rows entered the frozen class *during* the run (`house-AZ-03-2026-R`, 1 candidate, uncontested; `house-VA-10-2026-R`, 2 candidates, Aug 4) because writing shares ahead of Ballotpedia's call is exactly what settles a row. The freeze is **ongoing and recoverable**, which is a different and better fact than “11 rows are stuck.” Second, **N cannot be measured from our data and the honest answer is to say so.** We know only that Ballotpedia *had* marked the recovered 7 by today, never *when* — upper bounds only. The live residuals give the other end: **WA unmarked at 9 days, ME unmarked at 65**. So the recommendation is a **bounded re-check window with a GENEROUSLY CHOSEN N (30 days past `primary_date`, then settle regardless)**, not a fitted one; the cost is N wasted fetches on an uncontested seat, and at N=30 the recent contests stay eligible while ME settles. The attempt-counter shape needs a column, so it is a migration and out of scope by construction. **Filed QUEUED; the build takes its own number.**

**§B — the harvest had never advanced, and running it moved 94 seats.** Before-state re-derived rather than trusted: **sentinel 82 rows / 53 seats — byte-identical to SKILL's FIRST-RUN figure**, which is the evidence it had never re-run. After: **206 rows / 147 seats**, and **zero-roster rated seats 130 → 36**. Those 36 are classified into buckets that SUM, with no unnamed remainder: **28 future primary** (not a defect) · **2 winners exist but are all the incumbent**, correctly excluded (NC-07, S-SD) · **2 the §A freeze class** (WA-05, WA-08) · **3 past-dated with an EMPTY ROSTER** (ME-02, NY-02, NY-22 — `cands=0`, the HO 564 territory) · **1 too recent to be called** (WI-06, voted Aug 11, two days before the run). The rated set itself re-measured **187** against the entry's 186, and completed contests **768** against 718 — both drift, both re-derived.

**§C — the absence card's denominator, two fields and no new statement.** `queryAbsenceWatch`'s population pass was already SELECTing `member_participation.total` and `.not_voting` and throwing both away after dividing, so the row now reads **`14.9% · 133/890`**. **Zero new SQL was the premise AND the gate** (added lines matching `db.execute|sql:` = 0). It stays **missed/total, not the mock's cast/total**: the band's claim is absence, so the numerator must be the same quantity the percentage is a rate of. The overflow stop-condition was **measured, not eyeballed** — 83px on one line live, 93px at the mock's `29.3% · 370/1,262` scale, inside a 330px back.

**§D — the `pageErr` rate has NOT step-changed, and that is the finding.** Eleven scheduled prod crawls, 2026-08-02 (HO 590's deploy) → 08-12; **one day's log is simply gone** (`gh`: `log not found`, retention), so the observable span is 10 days and is reported as such rather than extrapolated. **Fires on 7 of 10 days**, events per day **2 · 1 · 4 · 1 · — · 0 · 5 · 10 · 0 · 0 · 1**. The three clean days are scattered, not a step: **08-07 clean is followed by the two heaviest days (5, then 10)**, and the 08-10/08-11 pair is broken by a fire on 08-12. **No deploy in the 604–655 train moved the rate in either direction.** Affected routes are the age-rendering feed set throughout (`home`, `home-stage-*`, `dashboard-v2`, `bills`, `stale`, `committees-redirect`, `member-detail`) — recorded as an observation, not a hypothesis, per the entry's exhausted-families rule. **The counts come from the LOGS, not from run conclusions:** `expect.soft` fails the test, `--retries=2` then reports it flaky, and the workflow still concludes `success` — so conclusions under-report fires and a green run list would have read as a fixed bug.

**Docs (HO 656):** this block · backlog — the 119TH-denominator and harvest-currency entries struck with their numbers, the `isSettled` freeze parent and its 11-contests symptom updated with the recovery and the residue, the `pageErr` entry given §D's table and conclusion, and three new QUEUED entries (the bounded-re-check guard build, the harvest's schedule, `race_candidates.updated_at`). **No SKILL commit:** §B.5 checked the 82/53 line and it presents as **“First run:”** — a dated record, which the instruction says to leave alone. **Notes now run through HO 656.**

**Also (HO 657), the races panel merged to one body — and both of the handoff's load-bearing premises turned out to be false on measurement.** The ruling was variant B of `mock-657-races-merge.html`: the COMPETITIVE|PRIMARIES sub-tab row, the 6-month tick timeline, the 2×2 date grid and the primaries count badge all die; the competitive body survives byte-identical under a new **NEXT PRIMARIES** line with per-date click-open detail. `RacesPanelTabs` (62 lines) and `DashboardPrimaries` (158) are **deleted**, and the shell is server-rendered — the island existed only to hold the toggle. **`getDashboardPrimaries` (the query) is untouched and re-pointed at the line**, consuming the same `cards`/`strip` payload the grid did: no SQL moved for a display merge.

**PREMISE 1, corrected: the hover-popover confinement check could not be run at all.** The handoff called it “the number-one regression risk in the whole HO.” The popover renders only on the **`default` variant**, and **`/dashboard-classic` no longer exists in the tree** — `CompetitiveRacesBlock` has exactly ONE consumer, `app/page.tsx` at `variant="v2"`. So the HO 230 containment box **currently confines nothing on any live route**. The check was **demoted on measurement, not skipped**: replaced by the structural assertion (same `<section>` tag, same `dashboard-pane home-races-pane` classes, one `.home-races-pane` in the DOM) plus a filing. The classes stay regardless, because `.dv2-racesbox .home-races-pane` is live styling on this route.

**PREMISE 2, corrected: `.races-panel-body`'s min-height does not steady the HEARINGS↔RACES flip.** §2.3 asked to “keep whatever prevents the box from jumping.” Nothing does: measured, the box goes **980px on RACES to 102px on HEARINGS**, and the body renders **879px against a 240px floor**, so the rule is **inert at current content**. It was SUB-tab parity, and the sub-tabs are gone. **The handoff's framing had already been written into two comments before the measurement**; both now state what is true, and the min-height is kept only as a floor for a shrunken body. The lesson is HO 654's, one turn later: a comment that overstates its scope is the next defect's nursery, and inheriting one from a handoff is the easiest way to mint another.

**What the payload check bought, which the sub-tab structure had been hiding.** Both tab bodies were `ReactNode` props, so **both serialized on every request even though one rendered** — `dash-prim*` appeared **44 times in the payload** of a page that showed none of it. Deleting the file rather than un-toggling a branch takes it to **0**, and the `/` response drops **555,593 → 549,687 bytes**. Recorded as a figure, uncharacterised. The same property is why “sub-tab row absent” is asserted **in the rendered DOM** and never as a payload string count: a string count conflates DOM with RSC payload, and on this page it did.

**Empty-window behaviour is proven, not assumed.** Forcing `cards: []` renders **nothing at all** — no line, and deliberately **no “no upcoming primaries” notice** — while the competitive body holds. In the merged panel the battlefield IS the body, so an empty notice is noise; the seasonal death is commented so nobody adds an empty state back. Perturbation reverted before the commit.

**Docs (HO 657):** this block · backlog — a QUEUED entry for the now-unreachable `default` variant (its deletion drags the popover, `RaceHubBody`'s preview mode, the `pacByRace` gating and their CSS, plus a SKILL phantom sweep, so it is its own enumerate-first HO per the HO 650 precedent), the parallel-glob flake WATCH gaining its second route, and the prose-that-outlived-its-code entry gaining two diagnostics · SKILL, alone behind the self-mod guard, re-pointing `getDashboardPrimaries` and turning the PRIMARIES-tab and `RacesPanelTabs` passages into RECORD. **Notes now run through HO 657.**

**Also (HO 658), the `default` variant of the races strip is deleted — and the insurance three comments called load-bearing was measured to hold nothing up.** The enumerate-first HO the HO 657 entry filed: `CompetitiveRacesStrip`'s 2×2 branch, HO 178's hover popover, the HO 230 `[data-col]`/`[data-row]` position keys, `RaceHubBody`'s `preview` mode and its preview-only IN THE PRESS block, the Block's three `variant === "v2"` ternaries, and the `.competitive-race-*` CSS all go. **Figures: 578 deletions against 234 insertions across 7 files; `competitive-race` occurrences in the SERVED CSS bundles 34 → 0** (33 in `globals.css` source over 23 lines), **bundle 172,518 → 169,887 bytes local, and 0 on prod against live neighbours still present** (`race-grid` 2, `dv2-racesbox` 9, `home-races-pane` 1, `rc-stat` 4, `hsch-row` 5 — the neighbours are what make the zero discriminating rather than a fetch that missed).

**Two prefetches died with the surface they fed, and they are the reason this was enumerate-first.** `getMember` and `getRunoffsForRace` ran per seat in the Block's loop for `RaceHubBody`'s preview only: **12 reads per dashboard render (6 seats × 2)** for a surface no route rendered. `incumbent`/`runoffs` leave `RaceHubData` with them. **`getRaceCandidates` and `getRaceNews` STAY** — the roster resolves candidate-named markets to party lean (HO 274) and `news[0].observedAt` feeds the NEW badge (HO 432) — and both were confirmed against **pre-fixed baselines**, because the roster's failure mode is same-as-success: kill the threading and the K cell renders a **neutral surname with no error**. Baseline and post-deletion read identically: **6 cards on the same 6 `data-seat` values**, **`New 4` + 4 `.rc-new` pips** and `Moves 3` under a `2000-01-01` last-view stamp, and the five discriminating KALSHI cells **all still party-colored** — `S-ME` Jackson 67%, `S-MI` El-Sayed 59%, `IA-03` Garriott 74%, `OH-07` Poindexter 77%, `PA-08` Cognetti 63%, every one `rgb(59,130,246)` = `--party-democrat`. **S-GA and the three POLYMARKET cells are named false-pass cells and prove nothing**, so they are not cited as evidence.

**All three HO 230 containment sites delete: leg 1 fired on every one.** `.home-races-pane`'s `position:relative`+`overflow:hidden`, its `overflow:visible` override in the 1200px block, and `.dv2-racesbox-panels`'s `position:relative`. The third was gated on one more measurement, because the earlier instrument had exercised **only** the RACES pane while that declaration wraps BOTH. Run with **HEARINGS active**: the pane holds **26 nodes in 66px**, has **zero** absolute/fixed visible descendants, and — the premise correction — **has no hover popovers at all**. The gate's own wording assumed them; `.hcal-stat-tip` belongs to `HearingsCalendar`, which **HO 610 replaced on `/` with the day schedule**, so hovering all 8 hoverable elements produced no popover and no DOM growth. Dropping `position:relative` moved **0 of 22 visible nodes**; the padding-injection control moved **22 of 22**. The RACES leg was then **re-run rather than inherited from the approved table**: **79 visible positioned descendants, 0 whose `offsetParent` is `.dv2-racesbox-panels`** (they anchor to `.ctl` and `.cm-agg`), **0 of 338 moved** on removal against **317 of 338** on the control. Deleting a positioning context is exactly where a remembered pass is worth the two minutes to redo.

**A third instrument trap, on top of the two the approved table already carried** (a perturbation aimed at a `display:none` pane reads "nothing moved" vacuously; a hover aimed at a non-hit-target reads the same). **The first RACES run reported 338 of 338 nodes moved — a total dependency that did not exist.** `getBoundingClientRect()` is viewport-relative and Playwright's `hover()` scrolls its target into view, so re-hovering between snapshots shifted every rect by one uniform delta. **The tell was the uniformity**, and the fix is document-relative coordinates (`+ window.scrollX/Y`). A perturbation instrument that moves the CAMERA between readings reports the whole scene as moving; both a 0 and a 100% deserve a control leg before either is believed.

**Deviations and things left standing, named rather than absorbed.** (1) **The absence figure is 34, not the 37 the approved instrument recorded** — same direction, different count method; both sides here are counted identically (substring occurrences across the served bundles), which is what makes the delta an instrument rather than a number. (2) **The prod dashboard DOM is not reachable to verify the two live-consumer comparators**: `/` 307s to the landing for a signed-out client (the HO 355 auth gate), and the comparators are client-computed from a signed-in render. Prod verification is therefore **SHA `c1b08cf` + the discriminating CSS absence + the public `/race/[id]` payload** (Incumbent · Candidates · News · last-verified all present, `In the press` **0**, `competitive-race` **0**, stable across two reads); **the badge and party-color comparisons were run locally on the identical commit and are reported as local.** (3) The approved ride-along named `useSingleOpenPanel.ts:10-11`; **`:21` carried the same phantom** (it counted CompetitiveRacesStrip as one of "four consumers" of the Esc handler — it stopped using the hook at HO 178) and both lines are corrected. (4) `RaceCard.tsx:35` named the `default` variant as a live sibling and is corrected under the phantom sweep. (5) **Left standing, recorded not rewritten:** the two dated diagnostic comments (`roster-coverage-642`, `sc-special-premise-600`) are artifacts of their probe time; `app/page.tsx:164` still describes "COMPETITIVE|PRIMARIES sub-tabs" and `RaceCard.tsx:102` still says "all four cards" — both are HO 657/642 residue, neither claims a deleted thing is reachable, and widening a deletion into someone else's stale prose is how a scoped commit stops being reviewable.

**`GET /api/race/[id]/hub` is confirmed orphaned and FILED, not deleted.** Zero callers in `app/`, `components/`, `lib/`, `e2e/`, `scripts/` — every `/race/` hit is the page route. SKILL has recorded it unused since HO 178, so it is **pre-existing, not dragged here**; deleting it would have widened this commit past its own boundary. Its header comment still describes the drawer that this HO deleted, and that staleness rides with the filing.

**Docs (HO 658):** this block · backlog — the QUEUED default-variant entry **struck with its figures**, plus one new QUEUED line for the `/api/race/[id]/hub` orphan · oddities — one entry, that a perturbation instrument reports a vacuous "nothing moved" against a collapsed pane **and a spurious "everything moved" when the camera moves between readings**, so falsify the instrument in both directions before believing either · SKILL, alone behind the self-mod guard: the HO 178 popover and HO 230 containment passages become RECORD, the `getRace`/`getRaceCandidates`/`getMostCompetitiveRaces` entries stop describing the 2×2 hover-popover grid, the `/` entry's "4 competitive seats" is corrected to 6 (HO 642), and any `nowMs`/#418 consumer list naming the Strip or Block is corrected. **Notes now run through HO 658.**

**Also (HO 659), `race_candidates` gained one TEXT column and stopped being a table you can only ask questions about by diffing runs.** `updated_at`, added by `ensureColumn` in `main()` — never the CREATE array, per the `activity_count` (HO 597) rule the `migrate.ts:840`/`:918` comments state. **The semantics were pinned before any code, because the helpful deviation is the defect here.** It is **last write (reach)**, not last content change: the harvest DELETEs and re-derives, so an unchanged roster still re-stamps, and *what a run touched* is exactly the quantity HO 642 and HO 656 were approximating behaviourally. One `runStamp` per invocation is bound into every write of that run, so "which run touched this row" is a **GROUP BY, not a range guess**. **NULL means pre-instrumentation and is never backfilled with a fabricated time** — the 11 curated HO 171/174/182 rows read NULL today, and the behavioural bracket stays the record for pre-column history.

**Writers re-derived rather than trusted, and the count held: exactly two.** `backfill-race-challengers.ts`'s `INSERT OR IGNORE` and `seed-races.ts`'s upsert — the latter's `ON CONFLICT DO UPDATE` sets the stamp too, since a re-seed IS a write under the reach semantic. Every other `race_candidates` reference across `scripts/` and `lib/` is a SELECT; `backfill-races.ts` names the table only in a comment about what it does not touch. **No reads were added anywhere** — `lib/queries.ts` is untouched, and the first consumer is HO 660's staleness check.

**Both falsification legs ran, with figures.** Before: **217 rows / 151 seats**, sentinel **206 / 147**, curated **11**, sentinel content checksum `127aeac…`. **Post-migration non-NULL = 0** — the migration stamps nothing, proven rather than assumed. **Post-harvest: non-NULL 206, exactly the sentinel row count; ONE distinct stamp** across 206 rows / 147 seats; **checksum byte-identical** to the pre-migration capture with zero row-count drift, so nothing needed explaining as real-world movement. **Negative leg: the 11 curated rows are still NULL, count unchanged** — one reading that proves two things, that the migration did not stamp and that the harvest's skip-curated guard still holds with the new column in the statement. `POST /api/revalidate?tag=races` returned `{"ok":true}`; nothing reads the column yet and no app surface changed, so no further prod verification is owed.

**An incidental proof of the reach semantic, recorded because it was luck rather than design.** The harvest was run **twice** (the second time to capture its summary lines), and the pair is the semantic stated as data: **the stamp advanced to the second run's ISO while the content checksum did not move at all**. That is precisely "an idempotent rewrite stamps" — reach measured, content unchanged — and it also means the stamp now in the DB is the second run's, not the first's. The instrument is `scripts/diagnostic/race-candidates-stamp-659.ts`, left untracked beside its siblings; it prints the before-census, the checksum and the stamp census in one pass, so the three legs are diffs of one instrument rather than three hand-written queries.

**The cadence ruling goes onto the record here: the challenger harvest gets slot (b), its own daily cron slot.** HO 656 priced both shapes and recommended (b); this HO adopts it unchanged and for its stated reasons — the harvest is pure DB-to-DB with no Ballotpedia fetch, it is idempotent under its sentinel, and it therefore buys freshness without spending the primaries cron's 50s deadline or the per-unit cursor that makes a killed tick resumable. **The wiring is HO 660 and the backlog entry stays OPEN until it lands**, because that entry's close criterion is *chosen AND wired*: a ruling with no cron line is still a harvest that runs when a human remembers.

**Docs (HO 659):** this block · backlog — the `updated_at` entry **struck with its figures**, the harvest-schedule entry gaining the slot-(b) ruling while staying open, and the M3 re-measure entry **restated** (the 642-era 88/186 and 129/62/67 figures are superseded by HO 656's post-run 36, bucketed and summing; the trigger now waits for HO 660's first *scheduled* fire, and the detector is built only if the remainder past those buckets holds the ME shape) · **no oddities entry — nothing odd emerged**; the column behaved exactly as specified on both legs · SKILL, alone behind the self-mod guard: the `race_candidates` schema block gains the column with its semantic, and the `backfill:race-challengers` entry gains the per-run shared-stamp sentence. **Notes now run through HO 659.**

**Also (HO 660), the challenger harvest got a clock — and the one thing that would actually prove it cannot be forced.** `/api/cron/race-challengers`, daily at `30 12 * * *`, wiring the slot (b) ruled at HO 659. **The slot is derived, not picked:** the harvest's ENTIRE input is what `/api/cron/primaries` writes, that cron fires `0 0,12 * * *`, and US primary results post in the 00:00–06:00 UTC band — so the 12:00 tick is the one that reads them settled, and 12:30 harvests what it just wrote. **A winner marked by the 00:00 tick waits at most 12.5h**, which is the stated price of holding the ruling at ONE daily fire rather than quietly over-provisioning to two. 12:30 is clear of the weekday fmp half-hourlies (`0,30 13-21`) and collides with nothing in a table that went **16 crons → 17** (functions 15 → 16). **No cron-lock, stated as a decision so it doesn't read as a gap:** the primaries tick is deadline-bounded at 50s inside a 60s ceiling so it is finished by ~12:01, and nothing else writes `race_candidates` on a schedule — the seed is manual.

**The refactor shipped alone and its equivalence was MEASURED, not read.** A route cannot import a script's `main()`, so the harvest moved to `lib/harvest-challengers.ts` — the `runStamp` (inside the function, so one stamp per invocation regardless of caller), the sentinel DELETE, the `INSERT OR IGNORE … SELECT DISTINCT`, and the fill census, returning figures instead of printing them. **The SQL moved byte-identical, and the HO 659 stamp instrument is what checked that claim**: after the refactored run the sentinel content checksum was **`127aeac…`, identical to the pre-refactor reading**, with exactly **one fresh distinct stamp across all 206 sentinel rows** and the **11 curated rows still NULL**. That is the second HO in a row where the instrument built for one purpose paid for itself in another — a refactor that passes a content checksum plus a stamp census touched behavior not at all. The npm script survives as the manual path with what a scheduled fire has no use for (before-census, sample eyeball, flush reminder); wiring a clock did not retire the hand-run.

**Verified on prod at `5b08bdd`, with the guard fired rather than assumed.** Two negative controls first — **no header → 401** and **wrong secret → 401** — then the authorized fire: **200 in 643ms**, payload `cleared 206 · inserted 206 · rows 206 · races 147 · of 187 rated`. Read back three ways: `cron_runs` carries **exactly one row for the path, `success`, 643ms**, with the figures in its payload; the 659 instrument shows **one distinct stamp `2026-08-14T02:36:51.750Z` across 206 rows / 147 seats** — the route's own stamp, which is how we know the write went through the shared path and not some other — and curated **11 still NULL**; `/api/health` lists the route with `maxStaleMs 93600000` (26h, mirrored from the existing daily entries rather than invented), `lastStatus success`, `healthy true`, and the whole health payload green.

**Two tails, both open on purpose.** (1) **The `schedule` trigger is UNPROVEN and is not claimed.** The HO 504 precedent governs: a manual POST and a deploy-adjacent invocation prove the ROUTE, never the schedule. **WATCH: confirm the ~12:30 UTC `cron_runs` row tomorrow.** (2) **That row — not today's curl — arms the M3 re-measure**, exactly as the HO 659 restatement specifies, because the detector must calibrate against the maintained regime and a hand-fired route is still a harvest that runs when a human remembers. The two tails are one dependency: until the schedule proves itself, M3 stays unarmed.

**One drift observed and deliberately not fixed:** `CRON_ROUTES`'s primaries entry records `schedule: "0 12 * * *"` while `vercel.json` has `0 0,12 * * *`. The field is **display-only** — it is echoed into the health payload and plays no part in evaluation, which keys on `maxStaleMs` — so this is a cosmetic staleness in a neighbouring line, not a health bug, and correcting someone else's row inside a wiring commit is how a scoped diff stops being reviewable. Recorded here; it belongs to whoever next touches that table.

**Docs (HO 660):** this block · backlog — the harvest-schedule entry **struck**, both halves of its *chosen AND wired* criterion now true, carrying the slot, the route path, the verification figures and **one WATCH line for the first scheduled fire** (and the note that this fire is what arms M3) · the M3 restated entry **untouched**, its trigger text already naming HO 660's first scheduled fire · **no oddities entry — nothing odd emerged** · SKILL, alone behind the self-mod guard: the cron topology gains the slot, and the `backfill:race-challengers` entry records that both callers now share `lib/harvest-challengers.ts`. **Notes now run through HO 660.**

**Also (HO 661), `isSettled` learned the difference between SCORED and DECIDED — and the recon corrected the cost model before a line was written.** The predicate was `primary_date < today AND EXISTS(vote_pct NOT NULL)`, with **no winner term**: a contest scraped in the window after Ballotpedia posts shares but before it marks a winner took shares and froze in that same instant, and every later tick refused the write that would have landed the call (HO 656 watched two rows join that class mid-run). It now reads **past-dated AND (a candidate carries `status='winner'` OR the date is older than `today − SETTLE_WINDOW_DAYS`)**, N = 30, generous rather than fitted — recovery timings give upper bounds only, and the live residuals sat unmarked at 9 days WA / 65 days ME. The `vote_pct` term is **gone**, so "settled" and "decided or expired" agree for the first time: the old rule froze rows that were not decided and retried forever rows that were. Shipped `c0d2376` (code) + `2d188f2` (diag), prod live-verified on `2d188f2`.

**THE REFRAME: `isSettled` is a WRITE-freeze, not a fetch-skip — so the window's price is not what the parent entry said it was.** All three call sites (`:646` special pass, `:905` Senate, `:1311` House) evaluate **after** the page has been fetched and parsed, and `buildScrapeUnits`/`readCursor` carry no settled term whatsoever — the cursor fetches its unit either way. **The "N wasted fetches per uncontested seat" cost the backlog told the build to state plainly does not exist in this architecture**; what N buys is N days of *rewrite-eligibility* on decided-but-uncalled rows, which is precisely the healing being purchased, and it is self-correcting because the source is unchanged. The router (HO 601) still keeps substitution off these ids and the HO 564 non-empty gate still refuses erasure, so widening the window widens neither hazard. (Filed as an oddity in its own right: a cost stated in units the architecture does not spend.)

**The five classes, measured 2026-08-14 against 876 `primaries` rows — settled 658 → 732, delta +74, predicted `+|C| +|E| −|U|` = +74, arithmetic reconciles.** **(1) UNFREEZES — 4:** `house-VA-10-2026-R`, `house-WA-05-2026-open`, `house-WA-08-2026-open` (Aug 4), `house-AZ-03-2026-R` (Jul 21). The handoff's conditional fifth, **WI-06, resolves NO** — it voted Aug 11 and has taken no shares, so it never entered the class and sits in class 5, open and unchanged. **(2) stays settled via EXPIRY — 2:** `senate-ME-2026-D` / `senate-ME-2026-R` (Jun 9), no churn and no re-hammering of the uncontested Collins page. **(3) called-but-shareless — 0, and the handoff's named member is FALSIFIED:** `senate-MI-2026-R` carries **one candidate with BOTH a winner row and a recorded share**, so it was already settled under the old predicate and is unmoved by this change; the class is empty corpus-wide today, which means the accepted trade (a later share-add reaches a called row only via the backfill) currently has no live members. **(4) NEWLY SETTLES via EXPIRY — 78**, of which **3 carry a roster** (`senate-GA-2026-R-runoff`, `senate-LA-2026-R-runoff`, `senate-LA-2026-D-runoff` — results never ingested) and **75 never had a page at all**. **(5) everything else — open, unchanged.**

**The tick proves the new predicate is what RAN, and it proves it in the freezing direction.** Manual fire 15:30:07Z, `success`, cursor 192→204 (house NC-04 → OK-01), 24 `settledSkipped`. **Six of those 24 are uncalled AND shareless** (`house-NC-04-2026-R`, `NC-06-R` and four siblings, all 2026-03-03) — rows the OLD predicate would have written, skipped now only because they are past the floor. Eighteen would have skipped either way. A payload of 24 skips is not itself evidence; the 6 are, because they are the only rows in that slice whose behaviour the two predicates disagree about.

**And the unfreeze was exercised end-to-end, not merely at the predicate level — on one district that carried its own control.** The cursor sat 100+ units from any class-1 id, so `house-AZ-03-2026-R` was driven through the real `syncHouseDistricts` path directly (a deviation from the handoff's cron-fire-only ops, named rather than absorbed — it is the same mechanism the 577 verifier uses, and it only accelerates "the next tick that reaches them"). **The two contests in that one district split correctly:** `-D` is called (Ansari, `winner`) and was **refused**; `-R` is scored-but-uncalled and **took the write**, its candidate stamp moving `2026-08-13T01:18:41Z → 2026-08-14T15:31:53Z`. **Content is byte-identical** — Ballotpedia still has not marked the R winner — which is the honest outcome: the row is now *eligible* for a call it was structurally barred from receiving, and a re-parse of an unchanged source correctly changes nothing. Date unchanged, `rosterDeletesRefused` empty, no clobber.

**Clobber protection re-verified under the new definition, and the instrument was reconciled in the code commit.** `scripts/verify-house-settled-guard-577.ts` carried the predicate **inline**; an instrument selecting exemplars by the retired definition would pick rows the guard no longer freezes and read FAIL as though the guard broke — the lied-twice family. Its precondition now asserts **CALLED**, printing `expired` beside it as context, so a row settled only by age can never satisfy it. **GA-01 survives on its own merits** (D carries 2 winner rows, R carries 1), so the targets did not move and the test stays non-vacuous: all three assertions PASS — guard skipped both ids, date unchanged, roster untouched.

**Tick duration: 19,835ms against the prior nine runs at 21.9–33.3s.** At or below the low end, i.e. no meaningful move — **the null the write-freeze reading predicted, stated in advance so that it confirms rather than merely fails to alarm.** Downstream (3.5): no class-1 winner landed, so nothing flowed to `race_candidates` at the 12:30 harvest and there is nothing to report there.

**Two further deviations, named.** (1) **`windowFloor` is derived at THREE sites, not the one the handoff specified** — `syncSenateCandidates`, `syncHouseDistricts` and `runPrimariesCronTick` each compute their own `today`, so each needed the floor beside it; one exported `settleWindowFloor(now)` keeps the derivation single while the threading is triple. (2) **Commit 1 touches three files, not two:** `runSpecialPriorityPass` gained the argument and `scripts/diagnostic/special-router-falsify-601.ts` calls it, and `scripts/` is in `tsconfig`, so leaving it would have failed the Vercel build (the HO 485 rule).

**Two windows now running, and they close without ceremony.** `house-AZ-03-2026-R` expires **2026-08-20**; the three Aug-4 rows expire **2026-09-03**. If Ballotpedia never marks them the rows settle unhealed and `backfill:primary-results` / `reingest:primary-slate` remain the recovery — which is already today's reality for that class, since the sync's eternal retries never healed them either.

**Docs (HO 661):** this block · backlog — the bounded-re-check QUEUED entry **struck with its figures**, the `isSettled` freeze parent's disposition (1) restated as **BUILT** (with the surviving line that the backfill remains maintenance past N and that the population was always a moving set), and the 11-contests symptom given one line that the class now self-heals within N going forward · oddities — one entry, that a cost stated in units the architecture does not spend survives review because it is unfalsifiable rather than because it is true · SKILL, alone behind the self-mod guard: the settled-guard passages rewritten to the new predicate with the old semantics preserved as dated RECORD. **Notes now run through HO 661.**

**Also (HO 662), the pre-migration sweep — the method layer flushed out of memory and into the repo, because project memory does not follow the repo.** CBT moves into a new maintenance project, and project-scoped memory and chat history do not move with it. The test applied was whether a fresh session with empty memory, given nothing but `git clone`, could reconstitute how this project is worked. `SKILL.md` documents the *product* comprehensively; the *method* — how a session establishes ground truth, how an HO number is derived, how handoffs are sized, what the product refuses to become — was almost entirely memory-resident. Enumerate-first per the HO 650/658 precedent: STEP 0 produced a coverage table and halted before a file was touched.

**The table ran 20 rows and needed a fourth verdict.** STATED / NARRATED / ABSENT split **7 / 3 / 6**, with **4 rows PARTIAL** — half the rule already stated, half absent — a bucket added rather than rounded, because forcing those either way would have licensed either a duplicate of SKILL text or the loss of real coverage. Row 20 (the concurrent-session git discipline) was added during the grep: `SKILL.md` **cites** "the concurrent-session rule" twice, at `:2314` and `:2329`, and defines it nowhere.

**Two of the recon's own verdicts were wrong, and both failed the way the handoff warned about.** The read-budget rule was filed ABSENT off a grep for `rows-read`; Platform facts has carried it since HO 602 under `rows read` and `rows_read` — the quota at `:2247`, the instrument of record at `:2263`, *price a query in rows when the budget is rows* at `:2275`. And the `unstable_cache` serialization rule was filed ABSENT while `:1594` states it in full — inside one query's catalogue entry, where a session adding a *different* cached query has no reason to look. **Read the sentence, not the match** was the handoff's own instruction to the sweep, and the sweep's premises had already broken it twice.

**Three rulings closed the open questions.** (1) The declined-pillars row was **false as carried** — FRED is a live feed (tape: TNX/VIX/WTI/NATGAS/BTC), so what was declined is *macro as its own theme*, not FRED; the pillar-vs-feed distinction is now the sentence's point, alongside FARA (its own spine) and standalone exec-branch tracking (`/president` exists because bills reach the desk — the spine attaching, not an exec pillar). (2) The relay-swallow threshold tightened from two strikes to **ONE**, on the evidence in `:2331`'s own history: HO 508 swallowed three, HO 537 twice more, and **no second paste on record ever bought delivery**. (3) §2 shrank to what was genuinely absent once the two mis-verdicts were corrected.

**`docs/method.md` (`98e929a`) — eight sections, born with the cross-reference discipline rather than acquiring it.** Roles · Session start · Handoff discipline · Relay · Doc authority and conventions · Product scope · Environment · Memory hygiene. It names SKILL sections in nine places (review-ref route, owner verdicts, external data sources, pre-flight verification, process cleanup, information architecture, query helpers) and restates none of them, because a rule written twice is a rule that will drift and nothing in the toolchain reports it when they do. **SKILL wins ties on implementation**, stated in both files. HO numbering moves here as canonical — `max(roadmap pointer, highest HO in commit subjects) + 1`, never a filename, with the corollary that the roadmap is authoritative only while current. Environment is stated **per machine** (the Windows box, where WSL is present but distros will not run; the MacBook, where none of that applies) with the instruction to establish which one the session is on first.

**`SKILL.md` alone behind the self-mod guard (`463f675`, +31/−1) — four rules, each placed where its neighbourhood already is.** The `unstable_cache` JSON-only rule as an `AUTHORING RULE` above the query catalogue it governs (`:1594`'s instance note untouched and cited from it as the worked case) · `EXPLAIN` is a plan instrument, not a latency one, so a new cold query is probed and timed before anything is built on it · the append-only deletion gate with **both** alternatives named as wrong rather than weaker (`grep -c '^-[^-]'` excludes deleted markdown bullets by construction; a normal-format diff against `git show HEAD:file` is invalid under `core.autocrlf`), plus binary-mode reads and `git cat-file -p <ref>:<path> | md5sum` for checksums · and the `:2331` tightening, with the two-strike form preserved as dated RECORD. Plus a header pointer naming `docs/method.md`. **The read-budget residue was measured and deliberately not written:** Platform facts already names the instrument, the source and the rule, so a line ordering them would have been padding — the skip is on the record with its condition.

**Two root artifacts dropped (`1fd6d83`, −108 lines):** `-w` (14,531 bytes of Vercel SSO HTML, the artifact of a mis-parsed `curl -w`) and `claude-code-handoff-2.md` (5,188). **0 references** to either across `app/ lib/ scripts/ e2e/ docs/ .github/` and the README — measured, because a zero you assumed is not evidence.

**The handoff archive shipped as one tracked gzip, and its scope was wrong by 53 files until the tree was read (`docs/handoffs-archive-362-661.tar.gz`, 829,609 bytes).** 267 `.md` files, **HO 362 through 661**, raw 2,313,136 → 829,609 compressed (2.79×), entries prefixed `handoffs/`; `gzip -t` clean, 267 entries out, all 267 md5s identical to the working copies. The directory ignore is **untouched** — a gzip is opaque to Tailwind's class scanner, so build-input parity (HO 604) holds and the trade is reversible in either direction. The 20 `*-artifacts/` directories are excluded: **56.9 MB of the tree's 61.7 MB**, the exact build-input class the ignore exists for. **The scope correction is the finding:** tracked handoffs do stop at 429, but the untracked set starts at **362** — 362–429 is partly tracked and partly not — so the specified 430–661 would have archived 214 files and left 53 behind to save 122 KB. What produced the wrong scope is in oddities.

**One open loop closed on measurement rather than on prose: HO 660's first SCHEDULED fire happened.** `/api/health` reads `/api/cron/race-challengers` `lastRunAt` **2026-08-14T12:30:49.466Z**, `success`, against schedule `30 12 * * *` — the row the HO 660 WATCH asked for, on its own stated terms (HO 504: a manual POST proves the route, never the schedule). That row is also what **arms the M3 re-measure**, which now has a live trigger and no memory carrying it.

**Docs (HO 662):** this block · **`docs/method.md`, new** · README — `docs/handoffs/` annotated as repo-ignored and local-only, the archive listed under it, and lines added for the roadmap, backlog, oddities and method docs, which were absent from Project structure entirely · backlog — the HO 660 scheduled-fire WATCH **struck with its figure** and the M3 trigger recorded as armed, one new WATCH line for the two HO 661 settle windows (2026-08-20 and 2026-09-03, both crossing the migration boundary), the `max(roadmap pointer, …)` WATCH entry reduced to **a one-line pointer at method.md** with its own "belongs in SKILL eventually" clause noted as superseded by method.md existing, one new line for the README cron paragraph being three topologies stale, and the 662 tombstone · oddities — one entry, that a tracked-count-versus-disk-count equality check run in a fresh clone is same-as-success by construction · SKILL, alone behind the self-mod guard, as above. **Notes now run through HO 662.**

**Also (HO 663), the M3 re-measure came back NO-GO — and the useful result is that the remainder the detector was conceived for is EMPTY, not small.** The trigger the HO 659 restatement set — re-measure after the challenger harvest's first *scheduled* fire, because a detector must calibrate against the maintained regime and not a decaying one-off — fired on 2026-08-14 and was confirmed here from `cron_runs` directly rather than inherited: `started_at` **2026-08-14T12:30:49.466Z**, `success`, 2003ms, on `30 12 * * *`, with the harvest's own `updated_at` reach stamp one second later at **12:30:50.466Z** across **206 rows / 147 seats**, one distinct stamp. The only other row in that table is HO 660's manual prod verification (02:36:51.676Z, 643ms — the figure that HO recorded), so the two runs reconcile and there is no unexplained fire.

**Exhibits 88 → 8, and all 8 bucket with nothing left over.** Rated set **187** · zero-roster **36** · M3 exhibits **8**. Bucketed on the HO 656 discipline (buckets that SUM, no unnamed remainder): **1** future primary (`S-AK-2026`, votes 2026-08-18, 16-candidate open field, no winner) · **2** winners exist but all are the incumbent, correctly excluded by the harvest (`NC-07-2026` Rouzer, `S-SD-2026` Rounds) · **1** past-dated, fully scored, uncalled upstream (`WA-05-2026`, voted 2026-08-04, 12/12 scored, 0 winners) · **3** past-dated with an empty roster (`ME-02-2026`, `NY-02-2026`, `NY-22-2026`, `cands=0` on both party rows — HO 564 territory) · **1** too recent to be called (`WI-06-2026`, voted 2026-08-11, three days before the run) · **0** ME-shape remainder. **1+2+1+3+1+0 = 8.** The recorded decision rule builds only if the remainder beyond the buckets holds the ME shape, so **zero is a cleaner NO-GO than a large number would have been** — it is not the "borderline" case that escalates, and it is not the "finding is the data" case either: every exhibit is ordinary pipeline state, and a detector calibrated on this population would fire exclusively on contests that have not voted, on incumbents who won their own primaries, and on rosters waiting for an upstream call. **The entry stays OPEN as a WATCH rather than closing**, because the population is a moving set — 28 of the 36 zero-roster seats are future-dated and 21 of those vote on 2026-08-18 (18 FL House plus `S-AK`/`S-FL`/`S-WY`) — so re-arm after the September primaries close, the last being `S-DE-2026` on 2026-09-15. **Re-arm predicts no zero read, and the entry says so rather than leaving it to be inferred:** buckets 2 and 4 are standing populations, not transients — an incumbent who wins their own primary stays correctly excluded for the rest of the cycle, and the past-dated empty rosters are tracked in the backlog's HO 586 M5 entry (77 of 560 past-dated house rows at 0 candidates; `ME-02-2026` named among its 8 both-party-empty holes, `NY-02-2026`/`NY-22-2026` the same population) — the HO 564 class, not drained by a re-measure — while bucket 1 converts rather than vanishing. **The re-arm question is the SHAPE of the remainder, never the size of the total: re-bucket and read bucket 6 alone.** And a gated build **inherits the bucket-2 spec decision, which is ROSTER-STATE-INDEPENDENT — an empty roster is not the trigger.** The detector must resolve the favorite label against **incumbent + challengers**, the set `resolveKalshiFavoriteParty` takes (`lib/race-colors.ts`), never against the `race_candidates`-derived roster `favoredMember` resolves within (`lib/race-matchup.ts`): **the incumbent lives on the race row** (`races.incumbent_bioguide_id` → `members`), **not in `race_candidates`**, because the harvest excludes an incumbent-winner by design (`pc.bioguide_id <> r.incumbent_bioguide_id`), so a resolution keyed on that table alone cannot see the person the market named **at any roster size**. `NC-07-2026` and `S-SD-2026` are the worked empty-roster cases, but a populated seat whose market names its incumbent is the identical false positive — and a detector without the carve-out reproduces HO 642's baseline on a smaller corpus.

**One bucket's NAME went stale under HO 661, and it is relabelled WINDOW-OPEN rather than left as a freeze.** HO 656 called bucket 3 the `isSettled` freeze class; that label is not carried forward, because under the HO 661 predicate `WA-05-2026` is past-dated with no winner and inside the 30-day window (floor 2026-07-15), so it is write-**eligible** and will take the call when Ballotpedia marks one; it expires into the frozen state on **2026-09-03**, which is exactly the later of the two HO 661 settle-window WATCH dates. That WATCH and this bucket are the same row, and the handoff's instruction to check `status` live rather than assume is what surfaced it.

**STEP 0 reconciled the instrument before the run, and the SQL turned out clean while two prose claims in it had gone false.** All four pinned copies in `scripts/diagnostic/roster-coverage-642.ts` were diffed term-by-term against their live sources — the harvest join and sentinel against `lib/harvest-challengers.ts`, the rated-set predicate against `getRacesIndex`, the `WITHDRAWN` set and active-challenger filter against `lib/race-matchup.ts`, the Kalshi close-time gate against `kalshiActive` — and **HO 660's byte-identical claim is verified rather than inherited**: zero drift in meaning, so no HALT. What had rotted was the surrounding prose. The provenance line still pointed at `scripts/backfill-race-challengers.ts … at 3de13e6`, a file that has only wrapped the harvest since HO 660 moved it; and M5's comment asserted that `race_candidates` **has no timestamp column**, which HO 659 falsified by adding `updated_at` — the very column that answers M5's question. Both corrected under STEP 0.3 as their own commit, and M5 now prints the stamp census beside the behavioural reach bracket, keeping both because they measure different things: the stamp says when a run touched a row, the bracket says how far its derivation reaches, and a run that fires on schedule and derives nothing new stamps fresh while reaching no further. The reach reads **2026-08-11 = 2026-08-11**, matched, so staleness explains none of the 36.

**Two OPEN LOOPS struck as session-open reconciliation — both had met their close-criterion and neither had been struck.** `/dashboard-classic` sunset: the route does not exist in the tree at `f83f439`, deleted across HO 608–610 (`1d9c339`), and its HO 335 note about 3 ungated reads died with it. v2 click-to-filter decision: superseded at HO 320 — `app/page.tsx` awaits `searchParams` and threads `DashboardFilters` into the distributions, MOVERS and BREAKING, so there is no `staticMode` to keep and no classic route to compare against. **Neither is new work; both are the ledger's own reconcile-at-open rule doing what it exists for**, and the gap between "criterion met" and "line struck" ran roughly 50 handoffs in one case and 340 in the other.

**Docs (HO 663):** this block · backlog — the M3 entry given its measurement, bucket table and NO-GO disposition with the re-arm condition and the corrected bucket-3 reading, plus the two OPEN LOOPS lines **struck with their pointers, not deleted** · oddities — one entry, that a diagnostic's *negative* schema claim goes false when a later HO adds the column, with no source to diff it against and no reason for the adding HO to grep for prose asserting the absence · `scripts/diagnostic/roster-coverage-642.ts` reconciled in its own commit ahead of the docs, kinds unmixed. **No SKILL edit** — nothing measured here changed a product rule. **Notes now run through HO 663.**

**Also (HO 664), the display-orphans chore closed all three members — and the handoff's own “verify, don't inherit” instruction is what stopped it shipping half the excision.** The entry's remaining members were the dead non-v2 feed arms and the `.home-grid` dim rule; both landed at `33a4e78`, alongside a carried strike. **The mechanism in the backlog entry was wrong and was corrected before executing on it**, per that file's own header rule: the “non-v2 fallback renderer arm” is **not in `V2FeedList`**, which has no fallback. It was the else-arm of each of the **three server wrappers** that pick the renderer — `ActivityTicker` (falling back to `<BillRowList compact>`), `TopStalls` and `NewThisWeek` (both to `TopStallsList`). The half of the claim that held is that `app/page.tsx` is their sole caller and always passed `variant="v2"`. `ActivityTicker`'s own header had recorded the arm as “a separate cleanup, noted not done” since HO 627; this was it.
**The reconcile at session open killed the queue head, which had shipped a week earlier.** The prior close state named the `searchMembers` `INDEXED BY` hint as the cheap head of QUEUED. The tree said otherwise: **HO 626 `e9d2aad`, 2026-08-07**, `lib/queries.ts` +14/−1, an ancestor of `main`, the hint mirroring `billsAggCte` and sitting inside `searchMembers` under a comment header naming HO 626 and the HO 624 measurement. **The record existed in two places and was missing from the third** — the roadmap “Also (HO 624–627)” block carried the plan flip and all five `rows_read` pairs, the code comment carried the rationale, and the ledger line still read STILL OPEN. Struck here as session-open reconciliation, the HO 663 form. **Third instance of the never-struck class in two sessions** (663 struck two), which is now less a coincidence than a rate: the gap is not that work goes unrecorded but that the *ledger* is the copy nobody updates at ship time.
**The chore, and the part nobody predicted.** Stripped: three else-arms, three `variant` props, three `variant="v2"` call-site props, and `getWatchedBillIds()` from `ActivityTicker` (sole consumer was the dead arm — one DB read per dashboard render, gone). Deleted: `components/TopStallsList.tsx`, zero callers post-strip. **CSS scope came in far wider than the handoff scoped it.** STEP 0 was told to expect `TopStallsList` to render “Tailwind utilities only, no `globals.css` excision — verify, don't inherit”. Verifying falsified it: the island rendered **seven** globals.css selectors, so its deletion orphaned `.top-stalls-row*` (the HO 126 row plus its HO 164 accordion rules) and `.bill-chip*` (the HO 125-derived chamber-tinted pill), **~69 lines across two blocks**, none of it in the plan. The adjacent `.home-col-stack` rode the same excision (same HO 178 block as `.home-grid`, 0 consumers — the `.pdc` adjacent-orphan shape). Kept: `.home-quadrant.home-breaking-panel` (the worked negative control, both selectors live at 7 and 1) and `.row-chevron`'s base rule (still live on `BillRow`; only its `.top-stalls-row` descendants died). Final counts — source 9 files, **+98 / −299**, `TopStallsList` 117 lines deleted; built-CSS **0** for `home-grid` / `home-col-right` / `home-col-stack` / `top-stalls-row` / bare `bill-chip`, against controls `breaking-row` 11 · `home-quadrant` 6 · `home-breaking-panel` 1 · `row-chevron` 3.
**`.bill-chip` got a tombstone rather than a silent delete, because the prior decision deserved to close on the record.** It was written as a reusable primitive — “available as a primitive for any future surface that wants a chamber-tinted bill-id pill” — and never gained a second consumer. **Zero renderers is the criterion and a comment is not a caller**, so it went, with the reasoning recorded at the site and in the ledger; the chamber-tint concept survives in `--rail-house` / `--rail-senate` via `BillIdRail`, and the pill is recoverable from git history.
**The positive control caught the instrument, not the code — twice, in two different disguises.** The handoff mandated a positive control before any zero was trusted, and the first pass read `.breaking-row` = **0**, which is a failing control and not a finding: the selectors were grepped in their **leading-dot CSS form**, and TSX carries bare class names. Re-run bare, the control read **7** and the real zeros stood. The same shape recurred at the built-CSS gate, where `grep -oF` prints only the matched substring, so a `grep -v lob-bill-chip` filter chained after it **could not fire** and `bill-chip` read 2; inspected with context, both hits were the live, unrelated `/lobbying` class. **A control that only ever confirms is not a control** — both readings were indistinguishable from success until something known-live was required to be non-zero.
**A runtime gate passed against another session's server, and the tell was a stylesheet.** STEP 3's `npm run start` was backgrounded, hit **`EADDRINUSE`**, and never bound — while `curl` returned a clean **200** with the feed intact, because a **concurrent session's server already held port 3000**. The gate “passed” on a build that was not this one. What exposed it was a **400 on one of the two referenced stylesheets**: that process had booted against an older `.next` and its manifest named a CSS file this branch's rebuild had deleted (“up after 1s” for a cold Next start was the other tell). The gate was **discarded, not reinterpreted**, and re-run on a private instance on port 3100 — 200, `v2f` 426, every removed selector 0 in the served HTML, and **both** stylesheets 200. All three tabs were confirmed rendering via each wrapper's unique footer link in the flight payload, after a first check looking for uppercase tab labels read 0 (they are Title Case; the uppercase is `text-transform`) and a `"/stale"` grep read 0 (RSC escapes the quotes). The other session's process was **left alive** and this one's killed port-scoped. **A verification server that fails to start is indistinguishable from one that started, if something else is already answering on its port.**
**Docs (HO 664):** this block · backlog — the display-orphans entry **struck with the corrected mechanism, the widened CSS scope and the `.bill-chip` tombstone**, the `searchMembers` residual **struck as DONE at HO 626 with its figures**, and one new QUEUED entry for `BillRowList`'s `compact` passthrough, which this HO **created rather than found** — filed, not chased, because `BillRowList` is live on 6+ routes (HO 507) and the branch is the last consumer of `BillExpandedPanel`; the entry pins that `BillRow`'s own `compact` mode stays live on three surfaces that never route through it. Both strikes are in place, **1 deletion each by numstat**, text preserved inside the markers — and the arc's own instrument note applies: `grep -c '^-[^-]'` read **1** against numstat's **2**, because the struck bullet begins `- **Display…` and renders as `--`, exactly the blind spot the gate names. · oddities — one entry, on the port-collision false green. **Notes now run through HO 664.**

**Also (HO 665), the two paired filings closed in one HO because their own text said they had to — and the sweep's set GREW on measurement, which is the close criterion working, not a miss.** The hub-route entry and the `/dashboard-classic` prose entry sat adjacent in `docs/backlog.md`, and the second carried the instruction that decided this HO's shape: **“Whichever entry is picked up first takes the other with it, so the two ages never diverge.”** Both are struck here, kinds unmixed across three commits — one chore, one docs, one SKILL — so the pairing was honored without a single mixed-kind commit. Enumerate-first per HO 650/658/662: STEP 0 produced a closure table with **instrument controls run before any zero was trusted** (the HO 664 lesson, where four instruments failed and no code did), and it halted before a file was touched. Three of its rows came back differently from the handoff's sketch, and all three were ruled on before execution rather than improvised past.
**The chore was one route and nothing else.** `app/api/race/[id]/hub/route.ts` — 34 lines, a **pre-existing orphan since HO 178**, deliberately not dragged into HO 658's scoped deletion — went at `80dd987`, taking `app/api/race/[id]/` and `app/api/race/` with it; `app/api/races/` (plural, district-geo) is a different live tree and was untouched. Two files, **−34 / +3−2**. Its HO 166 header comment, which still described the competitive-races drawer and its per-raceId client cache as live, **died with the file** rather than being maintained into accuracy. `lib/queries.ts` was not edited: all four imported symbols keep consumers, and the one that narrowed — **`getRunoffsForRace`, down to `app/race/[id]/page.tsx:69` alone** — is named in the close note rather than chased, per the HO 507 shared-file rule. **Gates:** typecheck + build green; the build route manifest no longer lists `/api/race/[id]/hub`, with `/api/bill/[id]/panel` and `/api/races/district-geo/[state]` present in the same list as controls.
**The freshness probe was designed so it could fail, which is the only reason its pass means anything.** STEP 3's 404 check used a **real race id** (`S-MI-2026`), not a made-up one: `/race/S-MI-2026` returned **200**, proving the id resolves against the live DB, while `/api/race/S-MI-2026/hub` returned **404**. A made-up id 404s on a stale build and a fresh one alike and would have proven nothing; a real id would have **200**'d on a server still carrying the route, so the 404 discriminates fresh-build-without-route from stale-build-with-route. The bind check that HO 664 turned into procedure ran first and passed on its own terms: the listener on :3100 mapped to PID 23320, and that PID was confirmed to be `node.exe` running `next start -p 3100` out of **this** repo's `node_modules`, created 21 seconds earlier — my process, not a neighbour's. Killed by PID afterward, port-scoped; three foreign node processes were left alive.
**The census is the deliverable, because sizing the residual IS what that entry asked for.** Fresh greps read **12 `/dashboard-classic` occurrences across 9 lines** plus **3 `api/race` lines**. **Seven lines were edited** — `:1592`, `:1741`, `:1974`, `:1996`, `:1999`, `:2002`, `:2006` — and the remainder needed nothing: `:1278–1280` is a claim+RECORD pair, `:2194` already reads “RESOLVED AT HO 658”, and `:1767`/`:1776` are `api/races/district-geo` (plural, unrelated). The MarketsTape passage lost its false **“renders on two surfaces”**; `:1592`'s live “the endpoint has zero callers” clause gained the deletion. **The rider closed:** `:1741`'s `<CompetitiveRacesBlock showBattlefield variant="v2" />` lost the prop — the component reads **0** for `variant` against a control that finds it in five sibling components, and the live render at `app/page.tsx:169` is `<CompetitiveRacesBlock showBattlefield />`. **The set grew by two on measurement:** `:2002` and `:2006` both assert things about the static bare `/dashboard-classic` tape in the present tense and were adopted into the sweep rather than left for a later HO to re-find. Two figures were corrected in passing — `:1741` carries **four** classic occurrences, not two (all RECORD, verdict unchanged), and `:1974`'s clause was LIVE, so **only its parenthetical** was RECORD-phrased with a cross-ref to `:1930`, leaving the compact-passthrough disposition to backlog's own entry for it.
**The handoff's changelog target was wrong, and was dropped rather than followed.** STEP 5 inherited HO 664's shape and pointed the changelog append at `:2087` — but that block is the **client-islands membership history** inside § Server / client split, and it fits HO 664 only because `TopStallsList` genuinely was an island. An API route never was one, and a prose sweep is not a membership change. SKILL has no general changelog and no API-route inventory; the precedent is that **HO 608–610 and HO 658 recorded their route deletions in place**, as RECORD phrasing at the site that described the thing. So no changelog line was written and the deletion fact lands at `:1592`, where the route was already described. The narrative lives here instead.
**The sweep found an orphan underneath the prose it was correcting, and filed it instead of chasing it.** Checking whether `:1999`'s bare-tape bullet was merely stale prose or still had code behind it produced a mount census: the **only** two `<MarketsTape …>` render sites are `components/DashboardV2Header.tsx:146` and `:147`, **both prop-driven**, and `components/HomeHeader.tsx` is **absent from the tree** — it died with `/dashboard-classic` at HO 608–610, and it was the bare tape's only mount. So `BARE_TAPE_EXCLUDED` (`components/MarketsTape.tsx:27`) and the no-props default path are a **pre-existing zero-mount branch**, unreachable since 608–610. It is now a QUEUED entry with that census pinned as its evidence, not a line in a chat log: `MarketsTape` renders on `/` every time, so a shared-component edit does not ride a prose sweep.
**Both gates that failed during this HO failed in ways that looked like the code and were not — filed to oddities.** `npm run typecheck` went red immediately after the deletion, on three errors naming the deleted route, all of them inside stale `.next/types` that only a build regenerates; and the project's own **stage-by-explicit-pathspec rule** collided with `git rm` to abort an add and ship a half-commit that `git show --stat` then reported as complete. Neither touched the product. The entry records both, because each one hands a plausible false reading to whoever meets it next.
**Docs (HO 665):** this block · backlog — the hub-route entry and the SKILL-staleness entry **both struck in place with their close notes**, the pairing clause honored and named, the census stated exactly as executed, and **one new QUEUED entry** for the zero-mount bare-tape branch, which this HO **found rather than created**; three in-place changes, text preserved inside the markers · oddities — one entry, on the two gates that lied · SKILL — its own commit, seven in-place edits, no changelog append. **Notes now run through HO 665.**

**Also (HO 666), the head of QUEUED closed — a dead prop, the 444-line component behind it, and 270 lines of CSS that only that component rendered.** Backlog `:75` was opened at HO 664 by the strip that created it, and deliberately not chased there: `BillRowList` is live on six routes, so removing a prop threaded through it is its own change behind its own gates (HO 507), and it cascaded into `components/BillExpandedPanel.tsx`, which that branch was the last consumer of. All of it landed at `532324f` — **8 files, +104 / −766** — with the entry's alternative close (record a keep reason) never becoming live: no second render site, no type consumer beyond the three importers, no `stage-pipeline` hit outside the dead file.
**The footgun was two filenames one letter apart, and the control for it ran before anything else.** `components/BillExpandPanel.tsx` is the LIVE shared panel (HO 317); `components/BillExpandedPanel.tsx` was the target. The pair was proven separable before a single edit — a grep for the live name returns **0** inside the dead file, so neither name leaks into the other's search — and their class namespaces turned out to be **disjoint except `tabular-nums`**: the live panel renders `bxp-*` exclusively, the dead one rendered `bill-expanded-*` plus `stage-pipeline*`. That disjointness is what made a 270-line CSS excision safe to reason about at all, and it was measured rather than assumed. The relocation target was chosen on the same logic: the four payload types moved **verbatim** to a types-only `components/bill-panel-types.ts` instead of folding into the live component, which left `BillExpandPanel.tsx` untouched apart from one import line and **ended the name collision in every import line in the tree** rather than preserving it past the deletion.
**A handoff premise was falsified by the enumeration and corrected before it was executed — the second time in three HOs.** The handoff instructed that `SponsorHoverName`'s header comment be reconciled so the surviving sharer reads `BillExpandPanel`. It does not: **`BillExpandPanel` never imported `SponsorHoverName`** — it renders its own `bxp-sponsor` / `bxp-sponsor-name` / `bxp-sponsor-bracket` markup — so the component's only two consumers were the dead panel and `BillRow`, and post-deletion `BillRow` is the **sole** consumer. The same false claim was sitting in SKILL `:1973` ("used by both the expanded panel's SPONSOR row and the collapsed `BillRow`"), which the deletion falsified outright, and it was swept in the same pass. This is the HO 664 mechanism-correction form: the enumeration is worth running precisely because it catches the specification, not only the code.
**Three SKILL lines were added to the sweep and one was taken out of it, which is what minimal-edits looks like in both directions.** Added: `:1973` (above) and `:1979`, the `compact`-variant bullet that described the deleted path in the present tense. Removed: `:1972`, which the handoff inherited from HO 664's hunk list — its live content is `BillRow`'s `onToggle` behavior plus an existing RECORD, and this HO falsifies neither. **Flagged and deliberately left:** `:1976`–`:1978`, including `:1977`'s mention of `.bill-expanded-news-empty`, a class this HO excised — those bullets sit inside the frame `:1974` already sets, "the **superseded** pre-317 full layout (kept for history)", and **a history-framed name is a record, not a claim**. Three code comments naming the deleted file were left for the same reason (`NewThisWeek.tsx:14`, `BillExpandPanel.tsx:5`, `app/globals.css:2096`).
**The `:2087` changelog question came back the opposite way from HO 665, on the same criterion.** One handoff ago the append was DROPPED, because `:2087` is the client-islands membership history and an API route was never an island. Here it is CORRECT: `components/BillExpandedPanel.tsx` was a client island, the islands set genuinely changed, and the entry records the change rather than a commit kind. The criterion did not move; the facts did — which is the point of having one.
**The gates were run in the order the last HO's oddity dictated, and one of them was built to fail.** Build first, then typecheck — the HO 665 finding that a deletion leaves stale `.next/types` shims that fail `tsc` while naming the deleted file. Both green. The CSS excision was checked per selector against built CSS with three in-family controls held live (`.bill-expanded-link` 2, `bxp-metabox` 3, `bxp-summary` 1) and the shared `--stage-*` variables at 19, so a zero meant "excised" and not "instrument broken". All six routes returned 200 behind a PID-verified bind with per-route content assertions, plus a **negative control** — zero occurrences of the excised markup in any served page — and the live expand path `GET /api/bill/119-hr-10083/panel` returned the exact `{committees,news,meetings}` shape the relocated types describe. **`/watchlist` is reported honestly as the one partial gate:** unauthenticated it serves its sign-in state and does not render `BillRowList` at all, so it proves the route serves, not that the panel renders there.
**Nothing odd emerged, so `docs/oddities.md` gains no entry this HO.** The one thing worth naming is a figure, not a gotcha: the recon's "52 lines" of CSS was a count of *matching lines*, and the real excision was **270 lines / 47 rule blocks / 33 selectors** — the block extents. That correction lives in the backlog close note where the sizing belongs.
**Docs (HO 666):** this block · backlog — `:75` **struck in place** with the close note carrying the strip, the cascade, the 2-named-plus-2-structural reading of "four types", the corrected CSS sizing, the falsified premise, the deliberately-untouched records and every gate figure including the `/watchlist` limitation; one in-place strike, text preserved inside the markers · oddities — **no entry, nothing odd** · SKILL — its own commit: `:1930`, `:1973`, `:1974`, `:1975`, `:1979`, `:2068` edited and `:2087` appended. **Notes now run through HO 666.**

**Also (HO 667), the orphaned dashboard-lead path retired — a product call executed, HO 637's finding 1 closed with it, and a diagnostic caught mid-lie.** Backlog `:91` was owned by Corey because retire-or-restore is a product question, and Corey ruled **RETIRE** in chat on 2026-08-15. Nothing had rendered the lead since HO 244; `getDashboardLead` had zero consumers outside `lib/queries.ts`; the step cost one Gemini call and two windowed `bills` reads on every one of four daily ticks, on the route whose budget HO 116/135 spent an arc defending. The chore (`ea4fa31`) is **+89 / −377 across 12 files**: `lib/dashboard-lead.ts` (−182), `scripts/generate-lead.ts` (−51), `scripts/diagnostic/check-lead-134.ts` (−12), the `package.json` `"lead"` script, `getDashboardLead` and its header out of `lib/queries.ts`, and the lead block plus `timings.lead` out of `app/api/sync/route.ts`. The `revalidateTag("bills")` after `runSync` stays; only the lead block's second one went.

**`lib/queries.ts` was in scope, and the principle is what makes that not a contradiction.** HO 664–666 each ran under a "queries.ts untouched" rule. This HO touched it — because **the backlog entry itself named `getDashboardLead` for removal**, so the symbol was owned by the entry rather than merely adjacent to it. The distinction is ownership, not size: the file was opened for one function, its header comment and the one `:6975` clause that cited the deleted cron read, and for nothing else. A future HO should read the rule the same way — the question is whether the entry owns the symbol, not whether the file is shared.

**HO 637 finding 1 closes outright, which is half the reason this was worth doing.** `lib/dashboard-lead.ts:64` and `:103` were the last non-Group-A `datetime('now')` comparisons against `stage_observed_at`, so the mixed-format `'T' > ' '` boundary compare now has **no product-code residue at all**. It closes by deletion rather than by fix — the cheapest possible resolution, and available only because the code was already dead.

**THE FINDING: a diagnostic that would have printed a confirmation off an absent measurement, and it was caught by enumerating the readers rather than by grepping the name.** The handoff's recon classified `scripts/diagnostic/sync-trades-margin-482.ts` as **prose-only** — three comment lines, RECORD-and-stay. It was wrong, and the recon's own instrument is why: a name-grep finds mentions, and `timings.lead` was not merely mentioned there but **structurally consumed** — a required field on the `SyncRow["timings"]` type, a term in `residualOf()`, and the whole subject of §1's "lead regression guard". Left alone, §1 would post-667 have filtered to an **empty** `leadVals`, taken `(leadD.max ?? 0)` → `0`, fallen through **both** thresholds and printed *"✓ All post-fix leads < 5000ms — hint holding, HO 480/481 confirmed."* A pass rendered by the absence of data. That is the same-as-success shape this project keeps re-meeting, in a new place: **a threshold guard written as `(agg.max ?? 0) >= FLAG` treats "no sample" as "best possible sample"** — filed to oddities as its own mechanism. §1 now short-circuits on `n === 0` and names the retirement; `residualOf()` makes `lead` optional so post-667 rows stay in the residual analysis instead of silently dropping out of a cohort that had quietly stopped growing.

**Two more instruments reconciled on the same principle — an instrument that measures a retired step is not neutral, it is a future false reading.** `sync-timeout-verify-480.ts` reads `payload.timings.lead` at `:44`, which the recon also missed; post-667 every row reads `no-timing` and its verdict prints *"NOT YET: only 0 in-band success tick(s)"* — a false **negative** implying the HO 480/481 fix regressed. It takes a header line dating it to that window rather than a rewrite, because the loop it verified is genuinely closed and the instrument is a record of how. `sync-trailing-cost-479.ts` had its lead arm removed outright (the `gatherLeadData` read replica and the timed `generateDashboardLead` call), header line recording it; the trades and sync-tail arms are untouched and still meaningful.

**Six recon deviations were surfaced at the HALT and all six adopted — the enumeration, not the handoff, was the source of truth.** Beyond the two diagnostics: SKILL `:1048` (the cron-topology bullet naming "dashboard lead" as a live step) and `:1565` (the `getEnactedThisWeek` entry asserting "two consumers, deliberately split") were both live claims the handoff's STEP 5 list omitted, and both went false on this change; `lib/enacted-this-week.ts` needed its whole `:3–9` rationale block reconciled rather than the single line the recon named, and it is paired with `:1565` so the code comment and SKILL tell one story — the "a cron-side reader must NOT consume the cached wrapper" rule stays **true as history**, it simply has no consumer now. `.claude/settings.local.json`'s stale `npm run lead` permission is untracked and was left alone.

**The stale news-cron prose was FILED, not flagged, and the reason is the other strike in this commit.** SKILL `:1029` and `:2169` describe RSS ingestion as running "after sync + summarize + lead generation" — stale since HO 117 split news to its own cron, and stale a second way now. It went to QUEUED with an instrument instead of into chat, because this HO also struck backlog `:100` — the primaries display-orphan sweep, **done at HO 603 (`1adf056`, −854 across 9 files) and never recorded**, the **fourth never-struck item in four sessions**. A chat-only flag is that class's feeder. The strike was verified against the tree at `ebea5de` rather than taken from the roadmap: ancestry true, only `PrimaryTimeline.tsx` surviving, `getPastPrimaries` at 2 comment hits and 0 definitions.

**The `dashboard_state` row delete was priced before it was run.** A live census read **9 keys in code and 9 rows in the DB**, matching exactly — `weekly_lead` plus the committees cursor, the primaries cursor, the summarize cron-lock, `kalshi_chamber_control` and the four LDA rollup blobs — so `WHERE key = 'weekly_lead'` was provably the only casualty, never a table drop and never a prefix. Pre-state: 1 row, `updated_at=2026-08-15T18:01:07.929Z`, stamped by that day's 18:00 UTC tick, which is also the evidence the cron was still writing a value nobody read. The delete ran **after** `verify:deploy` matched, so no deployed cron could rewrite the row.

**Gates.** `npm run build` then `npm run typecheck`, both green in that order (the HO 665 stale-`.next/types` ordering), with `ƒ /api/sync` still in the route manifest as the positive control that the route compiles after the edit. Greps ran controls first — `queryEnactedThisWeek` at 10 hits and `getEnactedThisWeek` with live consumers, against **0 live / 10 RECORD** for the retired names and 0 for `package.json`'s `"lead"`. STEP 3 ran a local prod render with the mandated bind check (`:3107` → PID 36088, `node.exe`), `/` at 200 with both stylesheets 200 and the masthead reading `17,203 tracked · 98 enacted` — which proves the dashboard never depended on the lead, not that the lead is gone. `/api/sync` was deliberately **not** hit locally: it writes bills to the live DB and calls Gemini.

**One drift observed, not taken, and — on Corey's instruction — FILED rather than left as a clause here.** STEP 3's content assertion recorded the live masthead as `17,203 tracked · 98 enacted`, while SKILL `:1937` describes the HO 244 readout as `n bills tracked · n in committee · n on the floor · n other chamber · n enacted` (with `:1579` and `:1952` pointing at the same description): three segments absent, the first reworded, and the literal string `bills tracked` returning **0** hits in the rendered HTML. Pre-existing, unrelated to the lead retirement, so not fixed inside a retirement diff — same reasoning as the news-cron prose above. **The correction that matters is procedural:** the first draft of this block named it here and nowhere else, and *a named-but-unfiled drift is how the never-struck class starts* — the class this very HO struck its fourth instance of. It went to QUEUED with a `grep` instrument before landing.

**Docs (HO 667):** this block · backlog — `:91` and `:100` **both struck in place** with close notes, plus **three insertions** (the post-deploy WATCH, whose failure shapes are stated separately because a still-`lead`-carrying payload and an unchanged total mean different things; and two QUEUED comment-only entries — the news-cron prose and the masthead-readout drift, each with a `grep` instrument) · oddities — **one entry**, the empty-sample threshold inversion · SKILL — six sites, its own commit and its own approval.

**Also (HO 668), the dead `.pdc*` block excised — the smallest HO of the run, and the one where the gate nearly passed by measuring the old build against itself.** Backlog `:424` was sized at HO 603 and then named out-of-scope in 664, 665, 666 and 667 in turn: live CSS with no consumer, the HO 570 C1 class. The chore (`5bc03aa`) is one contiguous cut — `app/globals.css:8075–8215`, **−141 lines, 0 insertions, one file** — taking the section header, `.prim-scrubber`, `.prim-scrub-seg` and its two variants, `.rdm-scrubrow` and the descendant `.rdm-scrubrow .prim-scrubber`, the `PrimaryDistrictCard` comment, and the whole `.pdc*` family.

**A pure-CSS deletion still earned a STEP 0, because the entry carried its own trap and the trap is only worth anything if it is re-run.** The entry warns that a quote-anchored grep for `"rdm-action--disabled"` reads dead while the selector is LIVE at `RaceDistrictModal.tsx:272` (applied as the second class in `className="rdm-action rdm-action--disabled"`). Re-run as a **demonstration rather than quoted**: anchored **0**, bare-substring **1**. That is what licenses the nineteen zeros — they come from the instrument shown to work, not the one shown to lie. Four positive controls (`rdm-action` 5, `rdm-title` 1, `hearing-row` 3, `races-panel-body` 3) were read **before** any zero and on the same invocation.

**The entry's own selector list was incomplete, and the reason generalizes.** `.prim-scrub-seg` is a distinct class carrying three rules, folded into `.prim-scrubber` in the entry — and **a `prim-scrubber` substring grep does not match `prim-scrub-seg`**. So the substring instrument prescribed to defeat *false positives* can simultaneously **under-enumerate siblings**: it is sensitive on the question "is this exact name used?" and silent on "what other names live in this block?". The census is therefore driven off the block's own enumerated selectors, not off the entry's list — **19 distinct names across 26 selector rules**, all 0 in `app/ components/ lib/`, `e2e/` and `scripts/`, with no dying name referenced anywhere else in `globals.css`. The `.pdc*` sub-block came to **106 lines, byte-identical to the entry's sizing**, which is the corroboration that the rest of the arithmetic holds.

**The prefix-danger census reads 26/25 against the entry's 26/24, and that is the entry correcting itself rather than a selector going live.** 26 distinct `.rdm-` names, **25 live, 1 dead** — and the one dead is `rdm-scrubrow`, precisely what this HO removed. The entry counted two apparent zeros and then documented one of them, `rdm-action--disabled`, as a false positive; applying its own finding gives 24 + 1 = 25. A `.rdm-` prefix sweep would still delete twenty-five live selectors, which is why the rule is per-selector and never prefix.

**A guard clause in the entry was found MOOT, verified rather than inherited.** Its "NOT dead, do not touch" protected `.dash-prim-grid` / `.dash-prim-card` as live in `DashboardPrimaries` — but **HO 657 retired the entire `.dash-prim-*` block** (`globals.css:4496` records it), the component is absent, and `dash-prim` in TSX/TS reads 0. The thing being protected had stopped existing eleven handoffs earlier. Backlog premises are claims, not facts; this one was checked before it was obeyed.

**THE FINDING: the byte-identical gate nearly passed by construction, because a port-scoped kill failed silently and the "post" render came from the pre-change server.** The pre-render server was killed with `taskkill … | tail -1` — **piped, so its exit status was discarded** — and the follow-up check was `netstat | grep LISTENING` returning nothing, which was read as *released*. It was an absence-of-evidence reading, and it was wrong: the process was alive, and the next `next start` died with `EADDRINUSE` into a log nobody had to read. **The tell was that the new "server" reported the same PID as the old one.** Had that gone unnoticed, `curl` would have hit the pre-change build, `cmp` would have reported byte-identical, and the gate would have certified the excision by comparing the old build with itself — a green produced by the instrument measuring the wrong thing, which is this project's recurring shape arriving through process management rather than through a query or a grep. Two corrections shipped with it: the kill is now verified **positively and three ways** (exit status read unpiped, `tasklist` reporting no such PID, and a `curl` that must fail with exit 7), and the gate carries a **build discriminator** — the served stylesheet hash must move (`4e8dbc70…` → `788dc503…`) and the served bundle must carry 0 `pdc` against 5 `rdm-action`, so a stale server cannot masquerade as a fresh one. Filed to oddities.

**Gate result, with its own control.** Pre-render taken from the unmodified tree at `52145c1`, post-render from the excised build: both **200 / 821,768 bytes**, normalized identically (scripts and the hashed stylesheet `<link>` stripped) to **252,214 bytes** each, `cmp` silent — **byte-identical, no dynamic-field exception needed**, so the STEP 3.5 classification path was never entered. Because normalization removes ~70% of the payload, it was itself controlled: the compared markup still carries real page content (`cart-` 19, `race` 24), so the `cmp` is not comparing two emptied shells. Built CSS after the cut reads 0 for all four excised names against controls `rdm-action` 5, `rdm-action--disabled` 1, `hearing-row` 7, `rdm-title` 1 — the trap selector surviving in the served bundle being the neatest single proof the cut was per-selector.

**No SKILL commit, as predicted, and the check was run rather than assumed** — `pdc` / `prim-scrubber` / `prim-scrub-seg` / `rdm-scrubrow` all read 0 in `SKILL.md`, and the `PrimaryDistrictCard` / `PrimaryScrubber` hits at `:1765` and `:1772` are already phrased as retired `/primaries` history. Nothing here is a client island, so no `:2087` append.

**A rider was authorized on the HO 667 sync WATCH even though it cannot close, because a WATCH whose failure shape fires on correct behaviour is wrong on the record now rather than later.** No post-deploy `/api/sync` tick had landed (DB clock 21:53 UTC, cron `0 */6` next at 00:00), so the entry stays open and still closes on the observation. But the four pre-deploy ticks were read — **#10014 4,612ms total / 2,148ms lead · #9957 5,706 / 1,703 · #9898 6,270 / 2,098 · #9841 7,327 / 2,357** — and they retire the yardstick: the lead step cost **~1.7–2.4s of a ~4.6–7.3s run**, not the ~25.3s of a 40.4s run the WATCH quotes from HO 139, because HO 480/481 collapsed its DB half. The entry's second failure shape would have fired on a correct post-deploy tick. **Item 9 scoped riders to an in-hand closing observation; correcting a yardstick is a different act** — the measurement that makes the WATCH readable was in hand, the one that closes it was not.

**Docs (HO 668):** this block · backlog — `:424` **struck in place** with the close note carrying the 19/26/141 correction, both censuses, the demonstrated trap, the moot guard clause and the gate result; plus the **rider** on the HO 667 WATCH (two in-place edits, 2 insertions / 2 deletions, both deletions being those lines re-emitted) · oddities — **one entry**, the silently-failed kill that makes a byte-identical gate pass by construction · **no SKILL commit**, no live claim named the block.

**Also (HO 669), the bare `<MarketsTape />` branch stripped and `lib/markets`' `bareTape` tag retired — the entry's floated keep reason answered by the tree, and a gate that passes by construction saying so out loud.** Backlog `:422` was opened at HO 665's prose sweep, which RECORD-phrased the SKILL claims and then measured the code underneath them; it was deliberately not chased there because `MarketsTape` renders on `/` on every load and a shared component does not ride a prose sweep (HO 507). The entry's close was two-branched — strip behind gates, **or** record a keep reason — and **Corey ruled STRIP in chat on 2026-08-17**. The keep branch was not merely declined; it was **answered on the evidence**: the shape a keep reason would have taken is "the bare tape is wanted for a future non-dashboard mount," and `/welcome` already **is** a non-dashboard tape that never used this branch — it carries its own `TAPE_SLOTS`, its own `TapeItem` and its own `landing.module.css` `.tape`, imports nothing from `lib/markets`, and reads `getLatestMarketTicks` directly. The one surface that wanted a bare eight-symbol tape after HO 608–610 built its own rather than mounting this one. The chore (`46b7ffc`) is **2 files, +48 / −71**.

**The `group` prop rode along as an adjacent orphan, and it is the reason the excision is worth more than its line count.** `group` had zero callers — nothing in the tree writes `<MarketsTape group=` — and its filter arm died by the same read as the bare arm. Taking both is what dropped the **entire `lib/markets` import**: `MARKET_SYMBOLS` was used inside the file only by `BARE_TAPE_EXCLUDED` and the `placeholderSymbols` else-arm, and `MarketGroup` only by that prop. Both survivor checks were run per-symbol in the HO 665 form before the import was touched, and the distinction that keeps this from being a data change is written into the close note: **`MarketGroup` at `lib/markets.ts:86`/`:102` and `lib/queries.ts:48`/`:9290` is the tick's `group` COLUMN, not the prop** — the type survives, the data field survives, only the component's filter goes.

**`lib/markets.ts` was touched under a stated scope exception, not by drift.** HO 664–666's "shared file untouched" rule would have kept a config module the sync pipeline reads out of a component chore. What overrides it is the **HO 667 `getDashboardLead` precedent**: an entry that names a symbol inside a shared file may touch **that symbol and nothing else**. `:422` names `bareTape` for reconcile-or-keep in its own close text, so the field, its doc block, its 7 markers and the three comment clauses that explained it moved — and every other `MarketSymbol` field, `MARKET_SYMBOLS` itself, and the whole sync/fetch path did not. Sole-reader status was **proven with a control**, not assumed: `bareTape`'s only reader was `MarketsTape.tsx:29`, while the in-family control `cadence` read >0 on the same sweep.

**The roster gate passed by construction, and the honest framing is the finding.** 60 symbol spans pre, 60 post, `cmp` silent. That is not a discovery about the tape: both live mounts pass `symbols`, which short-circuits at `symbolSet` before any arm this HO touched, so **the strip could not have changed the render**. The gate proves the right arm was cut. What it needed to be worth anything was a discriminator — and **the one STEP 3 specced was void here.** It asked for the served stylesheet hash to *differ* between the two builds, treating a match as the HALT condition that caught HO 668's survived server. But this HO touches no CSS, so the content hash **must** match (`788dc503…` both sides): the specced trap-detector cannot fire on a change that is CSS-neutral, and a literal reading would have HALTed a correct run. Freshness was proved directly against the served artifact instead — **`.next/server` holds 0 files containing `BARE_TAPE_EXCLUDED` and 0 containing `bareTape`, with the control token `placeholderSymbols` reading 1** — on a distinct port, with both binds PID-verified and the STEP 1 port confirmed free before the post-run. **A discriminator that cannot vary is not a weaker discriminator; it is not one, and it has to be replaced rather than reported as passing.**

**Three additions to the approved table, each flagged rather than absorbed.** (i) Making `symbols` required **forces the `= {}` parameter default off** — an empty object cannot satisfy a required prop — which the handoff did not name; it is compelled by the approved change, not a widening. (ii) `groupTicks` was renamed `tapeTicks`, a two-line consequence of the `group` strip inside the file the entry owns. (iii) The recon's claim that **`bareTape` / `BARE_TAPE_EXCLUDED` "appear nowhere in SKILL" is false at HEAD** — both tokens sit inside `:1997`, the one line already earmarked as the sole edit, so scope was unchanged but the edit had to retire them rather than leave them naming a deleted const.

**SKILL took three edits where the handoff predicted one, and the extra two were ruled on before they were made.** `:1997` as specced. But `:1994` carried **three** clauses the strip falsifies — "the bare shape is therefore a **zero-mount code branch**" (there is no branch now), the parenthetical "(filed QUEUED at HO 665 — strip the branch or record a keep reason)" (this HO closes it), and "**bare = no props** → the default set" (`symbols` is required) — while only its "renders on **ONE** surface" clause stayed true. `:2000`'s B2-restyle bullet ended "so the scope now guards a zero-mount branch." Both were presented at the STEP 0 HALT as departures from STEP 5's "Nothing else" and **approved as an amendment** — but `:2000`'s first cut was then **held and cut back, and that exchange is the most useful thing in this HO.** It had been rewritten to "the scope now guards nothing: both surviving mounts pass `scroll`" — an inference wearing a verified fact's clothes. The fact is verified; "guards nothing" additionally needs the scope's *predicate*, which nothing here had read. Corey named the shape exactly: **writing an unverified SKILL claim while closing an entry about unverified SKILL claims is the wrong way to close it.** Measuring it then **falsified** the claim rather than merely failing to support it — `globals.css:6678`/`:6691`/`:6970–6971` scope real overrides over the tight static defaults, and `globals.css:6674–6675` already says why the scoping stays: "`scroll` is still an opt-in prop on `MarketsTape`," which HO 669 did not change (`scroll` still defaults `false`, so the unscoped path stays reachable by the component's API and merely has no caller). `:2000` was cut to the verified half — it records the strip and states nothing about what the scope guards. **The generalizable bit is the offered fork:** an under-evidenced claim gets *either* the measurement that carries it *or* a retreat to the half already proved — never a confident sentence spanning the gap. **No `:2087` changelog append:** `MarketsTape` is a server component and `MarketsTapeClient` is untouched, so client-island membership does not change. That is HO 665's criterion applied — and citing HO 666, the inverse case, is the point: the criterion is being *applied*, not habitually skipped.

**The `showMeta` finding was filed, not chased, and the filing says which kind it is.** `showMeta` defaults `true`, neither live mount passes it, so `MarketsTapeClient`'s false-arms are unreachable. It is a **new QUEUED entry — found by this HO, not created by it**, the explicit inverse of HO 664's `BillRowList` filing, and the distinction is why both entries name it. The prop and its default were left exactly as found: the client is the live island on `/` and runs past 1,000 lines, so it is its own change behind its own gates (HO 507).

**The carried HO 667 sync WATCH CLOSES, and the read cost two instrument failures before it was worth anything.** 8 post-deploy `/api/sync` ticks, **0 carrying `lead`**, all reading `timings = {reportCatchup, sync, trades}`; six of the eight total **831–4,900ms**, at or under the pre-deploy floor. Judged against the HO 668 rider's measured **~1.7–2.4s off a ~4.6–7.3s run**, never the HO 139 ~25s headline. The two long runs are volume, not lead, and that was checked rather than waved at: #10475's own `timings.sync` is 25,739ms of its 26,765ms total. **Both instrument failures would have closed the WATCH on nothing.** `timings` is not at the payload root — the envelope nests it one level down — so the first reader returned "no timings" for **every** row, pre- and post-deploy alike, which is indistinguishable from success. And string-comparing an ISO instant to a space-separated boundary is not a date comparison (`"…T18:01Z" > "… 21:44"` because `T` > space), which flipped all four rider-baseline pre-deploy ticks to the post side. The fix is the control the entry now carries: **a pre-deploy tick must print a numeric lead on the same invocation before any post-deploy zero counts** — 15 of 16 did. This is the same lesson HO 664–668 have now paid for repeatedly: **the instruments fail, the code does not.**

**The `grep -c '^-[^-]'` deletion trap fired LIVE on this HO's own backlog diff, which is better evidence for the rule than the rule's text** — the HO 668 form, where a trap is re-run as a demonstration rather than cited. HO 643 recorded that the pattern is not a deletion count; HO 639 named why (a deleted markdown bullet renders as `-- ` in unified diff and evades `^-[^-]`). Run against the HO 669 docs commit it returns **0** while `--numstat`'s deletion column returns **2** — and the two deletions are real, both being struck ledger lines re-emitted with `~~…~~` and a close note. A rule that has been written down twice and still reads clean on a live diff is worth re-demonstrating, because **0 is exactly the answer a genuinely deletion-free diff gives**, and nothing in the reading distinguishes the two.

**A gate in the handoff was internally inconsistent, and it is recorded as a gate defect rather than quietly satisfied.** STEP 3 required `BARE_TAPE_EXCLUDED` to read a hard **0** while granting its sibling `bareTape` the "0 live, N RECORD" form **in the same list** — two tokens retired by the same edit, held to two different standards. Read in the RECORD form for both (0 live / 1 RECORD each, the survivor being the rewritten `MarketsTape.tsx` header naming both as struck), because the alternative is gutting a RECORD comment to move a substring count, which is the precise failure the "not gutted" clause exists to prevent: **a RECORD names the dead thing — that is what makes it a record and not a silence**, the `queries.ts:1071` house form. Corey ratified the reading and the gate as the thing that was wrong.

**Oddities (HO 669) — two entries, and both are instrument lessons rather than product ones.** The first is the direct successor to HO 668's: **a freshness discriminator that cannot vary is not a weak check, it is not a check** — and read literally it HALTs a correct run, since this HO's own claim (CSS untouched) guarantees the stylesheet hash matches. The rule it yields: choose the discriminator **against the change**, not from the previous incident, and ask what would make it differ before trusting it. The second is the `cron_runs` envelope — `payload` contains a `payload` key, so `p.timings` reads undefined on **every** row, indistinguishable from the success it is looking for — carrying with it the `'T' > ' '` boundary-compare hazard HO 637 tracked in product code, here reappearing in a throwaway diagnostic where nothing typechecks the semantics.

**Two findings were routed to the ledger rather than to oddities, because they are product observations and one of them is user-visible.** `lib/queries.ts:9455` claims `MarketsTape` partitions the tick list by `group`, which was **already false at `5549d94`** — false since HO 234, on top of a roster 13 symbols out of date since HO 251 — so it is a comment-only QUEUED entry that rides the next touch of that region, deliberately not edited inside a scoped chore (`lib/queries.ts` is shared, and the exception this HO earned covers only `bareTape` in `lib/markets.ts`). And the live ODDS strip renders **both** Fed-cut pairs as "FED CUT SEP" — observed in the gate capture, identical pre and post, untouched by this HO — filed as a **WATCH** rather than a fix because the disposition forks: if `FEDCUT`'s discovery has rolled onto the September event the two symbols are duplicates and one should go, and only if it hasn't is `showMonth` the thing to fix. Relabelling before establishing which would make the tape confidently wrong instead of visibly redundant.

**A third finding went to the ledger as its own QUEUED entry, and it is an instrument-coverage gap rather than a product one.** The 14 **untracked** files in `scripts/diagnostic/` sit inside `tsconfig.json`'s `include` (`scripts/**/*.ts`, with `exclude` only `node_modules`), so **local `typecheck` runs over a different file set than CI does** — CI clones the repo and sees 129 tracked diagnostics, never the 14 uncommitted ones. HO 669 demonstrated it rather than reasoned it: the WATCH probe, never staged, red-lit both `npm run build` and `npm run typecheck` with two `TS2532`s under `noUncheckedIndexedAccess`, failing a gate on a chore the file is not part of. **The direction is stated in the entry because it decides the severity** — this is a **false RED locally**, not a false green in CI, so the cost is misattributed debugging on a clean chore, not a defect reaching prod. SKILL `:2332` (HO 485) already covers the tracked half, where a *committed* probe breaks the Vercel build; this is the untracked half, and it cuts against the parity implied by SKILL § Continuous integration (HO 488). HO 667 hit the tracked side of the same seam when the 479 diagnostic coupled a deletion to typecheck.

**Docs (HO 669):** this block · backlog — `:422` **struck in place** with the close note carrying the controlled mount census, the forced `= {}` consequence, the `group` cascade and its data-field distinction, the 7 named markers with the sole-reader control, the answered keep reason, the pass-by-construction gate and its **replaced** discriminator, the structural reason `app/globals.css` is untouched, the **internally-inconsistent grep gate** read in the RECORD form, and the `:2000` cut-back with the measurement that falsified it; the **HO 667 sync WATCH struck** on the observation with both instrument traps recorded; plus **four new entries** — QUEUED for `showMeta`, QUEUED for the `tsconfig`-include exposure, QUEUED for the `queries.ts:9455` comment, and a WATCH for the duplicated FED CUT label carrying its trigger and both fork branches · oddities — **two entries**, now also carrying the `.next/server` **replacement** instrument and the running **13 instrument failures / 0 code failures** tally · SKILL — **three minimal edits** (`:1994`, `:1997`, `:2000`), its own commit behind the self-mod guard.

**Also (HO 670), `/welcome` rebuilt as the board — the first page many people see, priced before it was built, and the HO where the eyeball caught what every green gate missed.** Corey ruled the redesign in chat on **2026-08-17** against an interactive mock iterated to sign-off; this was the Code half of a settled design, not a fork. The page it replaced was the HO 361 split layout ported from `docs/design/landing.html`. The new shape, top to bottom: **top rail** (the `Congressional Terminal` prompt · `[ IN BETA ]` · a ticking clock) · **BREAKING** · **banner** (a 68px headline, the lead, and one CTA grid) · **MARKETS** tape · **ODDS** tape · **three cycling demo panels** · **bottom rail**. The chore is **5 files, +1,257 / −365**, and touches no other route, no shared component (with one named exception below), and no `lib/`.

**The read budget was the gate that decided what shipped, and it was measured twice — before the build and after it.** The instrument (`scripts/diagnostic/welcome-read-budget-670.ts`) calls the **real** `lib/queries.ts` functions through the real `lib/db.ts` client and reads `rows_read` off Turso's own pipeline response via a `globalThis.fetch` shim, so nothing reproduces SQL and the measurement cannot drift from the shipped query. Two shims make App-Router code run under `tsx`: `globalThis.AsyncLocalStorage`, and an **always-miss** `__incrementalCache` stub — the second matters, because a real cache would report 0 on the second call and flatter the budget. Controls ran first: a no-DB call read **0**, `getCorpusStats` read **17,704**. The arithmetic: baseline (the old page's five reads) **57,669**; ceiling **173,007**; rebuilt **94,240** = **1.63×**. Kept baseline reads **56,547** (the stat readout's three, plus **one** `getLatestMarketTicks` serving **both** tapes) + nine datasets **37,693**. **No dataset was cut for cost, and none needed a new query.** The most expensive single dataset is MEMBERS at **20,995**; the cheapest is LOBBYING at **1** (the LDA rollup is an O(1) `dashboard_state` blob parse, HO 437/442), and `getTopicDistribution` was priced for the Patterns panel and rejected at **84,546 reads alone** — 1.5× the entire old page, for 24 rows.

**WARM IS ZERO, and the premise that predicted otherwise is worth recording because it is a natural mistake.** The 94,240 above is the **cold** figure — the cost of *generating* the page with an always-miss cache stub. The review expected warm to land near cold, on the reading that `force-dynamic` leaves the expensive reads uncached. **`force-dynamic` disables the full-route cache, not the Data Cache**, and all **14** of the page's calls are `unstable_cache`-wrapped: `revalidate 600` (breaking news) · `3600` (nine of them, including the three the premise named as uncached — `getStageDistribution` 35,419, `getCorpusStats` 17,704, `getStaleBills` 7,247, `getClusterStats` 4,477) · `86400` (`getFeedBills`, `getMembersRanked` 20,995, `getMostCompetitiveRaces`, `getLatestMarketTicks`). Read out of the source per call, with a negative control (`getRecentFilings`, a plain function, correctly reported as un-wrapped) — `scripts/diagnostic/cache-options-670.ts`. **Measured against a real running server rather than a harness:** a `NODE_OPTIONS=--require` tap on the server process patches the global `fetch` that `lib/db.ts` calls and totals `rows_read` per request (`scripts/diagnostic/rows-tap-670.cjs`). Cold request **94,494** — a 0.3% independent cross-check of the probe's 94,240 — then requests 2 through 7 at **0 rows, 0 statements, ~65ms** against 1.4s cold. **Two controls, because a zero from an instrument that has gone quiet looks identical to a zero from a cache hit:** (A) a **fresh server process** with the Data Cache left on disk still read **0**, proving the cache is on disk and not process memory; (B) deleting `.next/cache/fetch-cache` and restarting drove the very next request back to **94,494** and the one after it to **0** — the instrument can produce a non-zero, so the zeros are readings. **What bounds the cost is the TAG, not the TTL — and the first framing of this block said otherwise, so the correction is recorded rather than swapped in silently.** The review asked for the per-TTL ceiling and this block first carried one (~69.7k rows/hour, 1.27×). Measuring it killed it: **the TTL is a backstop the tag almost never reaches.** With the same server tap, a warm visit costs **0** → `POST /api/revalidate?tag=bills` → the next visit costs **89,090** → the one after it **0** again; `news-breaking` costs **10,185** the same way. The 89,090 matches the sum of the page's eight `bills`-tagged entries (**88,987**) to within 103 rows, which is what identifies the mechanism instead of merely observing a number. **And the flush cadence is not the 4×/day the review assumed:** `/api/sync` fires `revalidateTag("bills")` 4×/day, but `/api/cron/summarize` fires it **unconditionally on `*/10`** (`route.ts:88`) — ~**148 flushes/day**. So the steady-state multiple is the same **1.63×** as the per-render one (89,090 against the old page's 54,665 for the same flush), because ~94% of the page's cost is `bills`-tagged; the per-render figure was never an over-estimate of steady state, it *is* steady state. The real bill is `min(visits, flushes)`: continuously visited ≈ **13.2M rows/day** (old page ≈ 8.1M), one visit an hour ≈ 2.1M/day, an unvisited window free. **Two 86400s entries are amplified ~148× by carrying `bills`** — `getMembersRanked` (`["members","bills","votes"]`, 20,995 → ~3.1M/day, not the ~21k/day its timer implies) and `getFeedBills` — while `getMostCompetitiveRaces` (`race-ratings`, weekly) and `getLatestMarketTicks` (`markets`, ~24×/weekday) are not. **This also falsifies a cell in the approved STEP 0 table**: its cache column was left blank for six rows and a blank reads as "uncached"; the `86400 · tags` reading is right, confirmed three ways — the source lines, the 0-row warm visits (impossible uncached), and the flush measurement naming which entries the tag governs. Caveat stated rather than buried: measured against `next start`'s filesystem incremental cache; prod is Vercel's Data Cache, same `unstable_cache` surface, and it survives deploys (oddities HO 312) — the prod confirmation is the filed entry's close criterion.

**Three datasets are not what the spec named, and the surface says so rather than the docs alone.** ENACTED reads the enacted-stage feed slice because `getEnactedThisWeek()` returns **0 rows** in a week with no enactments; HEARINGS adds `getRecentMeetings(14)` because the entire forward calendar is **3 rows** in the August recess, and its label reads **SCHEDULED + RECENT**; MEMBERS reads this-Congress sponsorship because **no 30-day member-activity query exists**, and its label reads **MOST BILLS SPONSORED**. A panel that renders a header over no rows is the failure mode STEP 3 was written to catch, and a track shorter than its own viewport breaks the scroll loop besides — so these are data findings, not preferences. All three are filed with their numbers.

**Four STEP 0 rows came back other than the handoff expected, and each changed the work rather than being forced.** (1) **There is no font drift**: all **10** `font-family` declarations in `landing.module.css` were already on-token at `4ae76a8`, control grep zero. What had drifted was **size** — `23px` / `11.5px` / `10.5px` / `clamp(28px,3.5vw,46px)` off the HO 604 ladder — which is what the rebuild actually fixed, so "the welcome page doesn't match the app" was a type-scale problem wearing a font-token costume. (2) **`MARKETS_TAPE` / `ODDS_TAPE` / `ODDS_PAIRS` are not in `lib/markets`** — they are module-private consts in `components/DashboardV2Header.tsx:28/:46/:56` — so importing them would have meant editing either a forbidden file or a shared `/` component; the rosters are instead **derived from `MARKET_SYMBOLS`**, with MARKETS = every `fmp`/`fred` symbol, verified **set-identical** to `MARKETS_TAPE`, and ODDS = every `kalshi` symbol. `TAPE_SLOTS` and `TapeItem` are gone, `lib/markets.ts` is untouched, and the divergence HO 669 recorded is closed at the config level. (3) **SKILL carries no `TAPE_SLOTS` claim at all** (0 hits) — that claim lives in `docs/backlog.md:434`, inside an entry already struck CLOSED at HO 669, so nothing was owed there and no struck entry was retro-edited. (4) **The clock premise was wrong twice**: the markets tape does not cycle its zone on click (`useZoneCycle` is a 4s timer, MT-pinned under reduced motion), and `formatInZone` cannot render the spec'd string at all (no date, no seconds). **The clock's zone is PINNED to MT and the hook is deliberately not imported** — ruled on the HO 670 review after the first cut wired `useZoneCycle` up to it. The distinction is the keeper: **cycling a zone is right where the stamped moment is FIXED** (the tape's AS OF, the masthead LAST SYNC — only the projection rotates, which reads as a world clock) **and wrong under a running clock**, where the hour would jump every 4s while the seconds ran on, which is indistinguishable from a bug to anyone who watches it. Every time on the page — the clock, both AS OF stamps, LAST SYNC — is now MT.

**Market state is derived, never hardcoded, and the derivation was reachable after all.** `isMarketOpen` lives in `lib/market-hours.ts:78` — pure, client-safe, and *not* owned by `MarketsTapeClient`, which merely imports it — so `/welcome` computes CLOSED/OPEN server-side per render (the page is `force-dynamic`) and pins ODDS to LIVE the way the live tape treats a "signals" strip. The handoff's fallback (stamp without the suffix, file it) was not needed.

**One file outside `app/welcome/` changed, and it is named rather than absorbed.** The `SIGN IN → LOGIN` relabel lives in `components/LandingCTAs.tsx`, which STEP 2's file gate reads as forbidden. The mount census — with `BreakingTicker` as its companion — shows **exactly one** render site for each, `app/welcome/page.tsx:188/:214`: both are page-exclusive components, not shared surfaces. Flagged in the commit message and here, per the standing rule that a defensible departure still gets said out loud (HO 623/629/639). The same census corrected a comment wrong since HO 361: `LandingCTAs` claimed to be "the ONLY client JS on /welcome" while `BreakingTicker` sat beside it. The page now carries **three** islands, the clock being the one HO 670 added — which is why the SKILL changelog append at `:2087` is correct here on the HO 665 criterion, cutting the way it cut at HO 666 rather than the way it cut at HO 669, where no island membership moved.

**The gates were all green and the layout was wrong three times.** Build, typecheck, a 200, both stylesheets 200, `TAPE_SLOTS`/`TapeItem` at 0 with `getLatestMarketTicks` reading **11** as the control, the dropped module selectors reading **0** in the built CSS against ten surviving ones reading 1 each, and — the STEP 3 gate that mattered most — **all nine datasets mounted with every row present** (12/12/12 · 12/14/16 · 9/12/5 titles matched, each appearing 4× from the marquee duplication and the flight payload, with a known-present and a known-absent control on the same run). On that evidence the page was shippable. A screenshot showed a ~300px dead zone under every panel, an empty right third of the ODDS strip, and a reduced-motion render whose top fade permanently half-erased the first row. All three were fixed before the commit; all three are invisible to a content assertion, because **a string is in the markup whether or not it is in the visible box**. The instrument-failure tally HO 664–669 ran to 13-against-0 therefore **inverts** here: sound instruments, wrong code, caught by looking.

**Fade and frost are two separate jobs, and the distinction is the design fact worth keeping.** The rows fade by **alpha**, via a mask on the scrolling viewport, so glyphs stay sharp all the way out. The frost is a **separate overlay on the panel surface** — a 120px bottom band and a 34px top strip, `backdrop-filter: blur(5px) saturate(.85)`, each itself masked so it engages only past the point where the text has already gone to nothing. A `backdrop-filter` placed over live text smears the type; that is the bug the split exists to avoid. Under `prefers-reduced-motion` **both are removed entirely**, because with the scroll stopped they no longer hand rows off at the edges and simply damage the first and last row — measured, not assumed: 32 animating elements normally against **0** under `reduce`, 3 dataset layers visible and 6 `display:none`.

**The mock was in the tree under a different name, and `landing.html` had never been committed at all.** The handoff expected `docs/design/welcome-670.html`; the signed-off file is `docs/design/welcome-formats-mock.html` (141KB, three formats, format 1 ruled), and it is committed here **at the name Corey gave it** rather than renamed to match a filename guess — the HO wins over the mock on design values, not on file names. `docs/design/landing.html` was present on disk but **untracked**, so its one-line supersession note arrives with the file's first commit; SKILL `:1741` cites it by path, which is reason enough to have it in the repo rather than on one laptop.

**Docs (HO 670):** this block · backlog — **five new QUEUED entries**, each with its close criterion: the `HeaderBar` `IN BETA` + clock across all routes (Corey-ruled, deliberately not taken, with the reduced-motion guard named as the reusable half and the clock flagged **mount-don't-reimplement**); the `--font-mono` → Plex Mono token swap, carrying the falsified drift premise so the swap is understood as a brand change and not a repair; the `/welcome` tape unification with `components/MarketsTape`, carrying the corrected location of the three roster consts; the three data-forced dataset substitutions with their measured costs and the missing 30-day member query; `lib/zone-cycle.ts`'s pure formatter being unreachable from server components because a hook shares the module, — ruled on the review — a **`docs/method.md` visual-inspection gate for render-touching work**, filed as its own small HO because a method change does not ride a feature handoff, the **absolute-cost entry reframed on measurement** (`bills`-tag-driven at ~89,090 rows per flush and ~148 flushes/day, superseding both the per-render framing and the per-TTL one), and — ruled on the review — the **site-wide finding that HO 670 only surfaced**: `/api/cron/summarize` flushes the `bills` tag unconditionally every 10 minutes across **37** tagged cache entries, and **296 of 300 consecutive ticks (98.7%) wrote nothing and flushed anyway**, so the lever on the absolute number is the flush cadence rather than the dataset count — a conditional flush is one line and its own HO · oddities — **three entries**: the CSS-only marquee's unwritten minimum content size on both axes (with the live tape's JS measurement as the contrast), the all-green-gates/wrong-layout inversion and its tally, and two throwaway-instrument traps (esbuild `keepNames` breaking `page.evaluate`, and `next build` typechecking untracked probes) · `docs/design/` — the signed-off mock committed and `landing.html` committed with its supersession note · SKILL — its own commit behind the self-mod guard.

**Also (HO 671), the `bills` flush made conditional on a write — a one-line guard that removed ~96% of the site's cache invalidations, and the HO where producing the control cost one authorised production write.** HO 670 surfaced it while pricing `/welcome` and could not take it: `app/api/cron/summarize/route.ts` called `revalidateTag("bills")` unconditionally on a `*/10` schedule, and a change to invalidation for **37** tagged cache entries across every route is not a landing-page chore. The measurement that motivated it, re-taken at HEAD over 300 consecutive ticks (2026-08-16T14:00Z → 2026-08-18T15:50Z): **296 (98.7%) summarized ZERO bills and flushed anyway**, 4 wrote, 0 lock-skipped, 0 errored, 0 unreadable — with the raw envelope printed and the control (`ok > 0` reads 4, not 0) ruling out the HO 669 payload-nesting trap. That ratio is **HO 406 working, not failing**: the queue is drained, inflow is ~30/day, a tick clears 100–120, so nearly every tick correctly finds nothing to do. **The waste was the flush, never the schedule** — the rule worth keeping is that **an unconditional flush on a scheduled tick is a cost multiplier, not a safety margin: flush on the write, not on the schedule.**

**`stats.ok` carries the guard because of a code boundary, not a convention, and that is what made the guard defensible rather than plausible.** Both content-write paths — the advance branch's `UPDATE bills` + `stage_transitions` insert, and the no-move branch's `UPDATE` — fall through to `stats.ok++` with no branch between, so `ok > 0` **iff** a summary landed. The only other write, `markBillFailed`, touches `summarize_failed_at` and `summarize_attempts`, and **0 of the 37 tagged queries read either column** — measured with a control showing the grep finds those columns in four other files, so the zero is a reading. That is why a **failed-only tick deliberately does not flush**, and because the observed window held **zero** `(ok = 0, failed > 0)` ticks, that branch is argued from column visibility rather than counts — the guard's own comment is where the argument lives, since it is the half a future reader would otherwise "fix".

**`/api/sync` KEPT its unconditional flush, and the discriminator is better than the analogy that would have replaced it.** Superficially it is the same defect at 4×/day; it is not the same case. Its `upsertBill` sits **inside** the loop's `try/catch`, so a throw after the row commits — the conditional `stage_transitions` insert is the concrete candidate, and nothing wraps the pair in a transaction — is **caught**, and `upserted` stays 0 with the write landed. An invisible write means the return value cannot carry a guard, so its 4 flushes/day stay as cheap insurance. Summarize's writes sit **outside** its `catch`, so the same partial write cannot reach the flush at all. Same shape, opposite consequence, opposite verdict.

**The four STEP 3 readings, each able to fail, all against a real `next start` with the HO 670 rows tap (`NODE_OPTIONS=--require`), bind-checked by PID before any curl was trusted:** **A** warm visit **0** · **B** idle tick then a visit **0** — the guard took · **C** a real writing tick (`summarized: 1`, 1,800 prompt / 122 output tokens) then a visit **89,090**, and the visit after it **0** · **D** direct `POST /api/revalidate?tag=bills` then a visit **89,090**. **C and D agree to the row**, which is what makes the pair a control rather than two anecdotes, and B's zero is a reading rather than silence precisely because D produced a non-zero on the same tap minutes later.

**Reading C cost one authorised production write, and the cheapest honest option was not the most obvious one.** The queue was not merely drained but empty — 17,716 bills, 17,716 summarized, 0 eligible — so no tick could write without being given something to write. Two alternatives were rejected on the record: an **embedded-replica copy** (zero prod writes, but a `file:` DB does no HTTP, so the rows tap sees nothing and C would have been measured by a different, qualitative instrument than its own control D), and **waiting for natural inflow** (unbounded — prod's still-unguarded `*/10` eats new bills within ten minutes). Corey authorised the reset under conditions: `119-hr-3370` named and its full row captured first, picked to be invisible on every surface this HO measures (introduced stage, last action 2026-02-11, `stage_observed_at` NULL, **not** among the 12 oldest by `latest_action_date` — the stalls panel's exact slice — 0 news mentions, no cluster), a two-attempt budget, and the post-C settle reading taken. It landed on the first attempt and healed on the same tick: corpus back to **17,716/17,716**, **0** `stage_transitions` rows appended (the predicted `noop` from a settled stage), `stage`/`text_length` identical — and `topics` back as **`["government_operations","housing"]`** where it had been **`["other"]`**, a real content change from the regenerated classification, recorded rather than smoothed over. **The cron-lock side effect is named rather than left in the margin:** `claimCronLock` is shared state, so the local tick took the slot prod's `*/10` would have claimed and prod's tick no-op'd for that window — the HO 432 design working, harmless, but a local action with a production consequence.

**The new steady state, which is the number this HO existed to produce, and it is site-wide rather than per-page.** Flushes fall from ~**148/day** (144 unconditional + 4 sync) to ~**6/day** (~2 writing summarize ticks — 4 in 300 ticks over 49.8h — plus sync's unchanged 4): a **~25× reduction**, landing on every consumer of all 37 `bills`-tagged entries. For the page that found it, ~**13.2M rows/day** if continuously visited becomes ~**0.53M**. **Nothing was traded for it** — no query changed, no panel dropped, no schedule moved, one file touched, +38/−5. The per-flush cost is untouched at 89,090 by construction: the guard changes *how often* the tag is invalidated, never *what* it invalidates (`grep -c '"bills"' lib/queries.ts` unmoved at 53; bills-tagged tag groups unmoved at 37; the two executable call sites still two — wrapped, not deleted).

**A claim from the STEP 0 table was pulled before it could land in the docs, and the correction is the entry's most useful residue.** Item 7 had ended "no surface can serve a written-but-unflushed summary" — and item 1's own tracing falsifies it: the write pair `:315`/`:341` is not atomic, so a throw between them leaves the `bills` UPDATE committed with `ok++` never reached and the flush never fired, stale until TTL. HO 671 neither creates nor widens that path (before the guard the throw escaped the route; after it, it escapes one line earlier), but the claim as drafted would have been a live guarantee the code does not make — the class the HO 670 SKILL guard caught, appearing this time in a *table* rather than in SKILL. Filed as its own HO with three named closes.

**Docs (HO 671):** this block · backlog — the HO 670 summarize-flush entry **struck in place** with all four readings, the guard shape's basis, the `/api/sync` verdict, the authorised write and how the row healed; the `/welcome` absolute-cost entry **amended in place** rather than duplicated (its cadence half closed, the prod confirmation still open, the `/api/revalidate` default-to-`bills` note kept); **one new entry** for the non-atomic write pair; and the `tsconfig`-include entry **appended** with its third live firing — the first to red-light `next build` and not only `typecheck`, plus the observation that HO 670's tree went red *after* its final gate run because an untracked probe was written afterwards, invisible because CI only ever sees tracked files, with the close criterion extended to require that such an error become impossible or visible rather than merely tolerable · oddities — **one entry**, an instrument lesson about `tail`-ing a command whose failure mode is a stack trace · SKILL — its own commit behind the self-mod guard.


**Also (HO 672), three closes: the flush guard confirmed in production, the `tsconfig` `scripts`-include exposure shut, and the gate entry that generalises both.** Ordered so the one that could fail went first.

**STEP 1 — the guard measured in prod, and the criterion it was written against turned out to bundle two questions.** HO 671 shipped `if (stats.ok > 0) revalidateTag("bills")` and proved it under `next start`; Vercel's Data Cache is a different surface (HO 312), so the guard sat in production as a falsifiable prediction. The `/welcome` entry's close read *"flat across idle ticks, with steps only at writing ticks"* — which mixes a **mechanism** claim with a **cadence** claim, and only the first is a latency question. **Mechanism: closed.** Baseline **p25 333 · p50 353 · p75 380 ms**; sensitivity PASS at the six-cron 00:00Z slot (**2251 ms, 6.4×**); one clean positive — the `00:10:20` tick wrote (`summarized=21`), guard fired, next sample **1304 ms (3.7×)**, then straight back to 391/350/368; one clean negative — the `00:20:20` tick idle, following five samples 334/348/345/330/307, **inside the baseline IQR**. `/api/version`, sampled in the same cycle as an untagged control, held **91–115 ms through the 2251 ms cycle**, so the excursions are server-side regeneration and not network. **Cadence: not a latency question, and already answered better by `cron_runs`.**

**The experiment is THIN, and the reason is self-inflicted rather than environmental.** `TaskStop` reported success twice without killing the detached children, so three samplers ran concurrently against prod for 45 minutes; the load changes at **00:40:01** and **00:50:02** fell on two of the four planned negatives, which were discarded as unattributable. **One positive, one negative, one sensitivity reading** — and the word is *thin*, not *sufficient*. **The variance cuts both ways and this hour ran HOT:** two writing ticks inside it (`00:01:15`, `00:10:20`, both `summarized=21`) against a 3.3/day mean. A second window at 06:10Z was **declined** as the wrong instrument aimed at the better-answered half. One unplanned corroboration, in a single two-minute stretch: the idle `00:31:19` tick produced nothing (321 ms) while the news cron flushing `news-breaking` fifteen seconds later produced **977 ms** — the discriminator firing on one tag and staying silent on the other.

**The prediction's arithmetic was low, and why it was low is worth more than the number.** Measured over 8 days of `cron_runs`: **3.3 writing ticks/day, mean, range 0–5** — so ~**7.3 flushes/day, range 4–9**, not the ~6 the record carried. HO 671's "4 in 300 ticks" sampled a 49.8h stretch containing 08-16, which wrote nothing at all, and 08-17, which wrote once. **A point estimate reported without its range invites exactly this correction.** The conclusion is untouched: ~148/day to single digits.

**The best structural finding: writing ticks are sync-coupled, not scattered.** **25 of 26** fell within ten minutes of an `/api/sync` tick (`0 */6`) — sync ingests and nulls `summary`, the next summarize tick drains the queue — so the guard's remaining flushes **cluster behind sync's four rather than arriving independently**, and the next writing tick is predictable rather than something to wait out. **Coalescing the pair was considered and DECLINED**, recorded here so the option is on the record as weighed: merging them would halve a cadence already at ~7/day, and filing work not worth doing costs more than the work saves.

**One measurement was not obtainable from the repo and is named rather than worked around.** The specified effect series — the Turso account rows-read slope — is dashboard-only from this box: no `turso` CLI, no platform API token. SKILL already records the identical limit under *Platform facts*. The latency series is a substitute that carries its own calibration inside the same window. `/api/version` returning `builtAt: null` is why establishing the deploy boundary needed the Vercel deployment list (`2026-08-18T18:55:07Z`, production, READY) — filed as a cheap entry riding that route's next touch.

**STEP 2 — the `tsconfig` `scripts`-include exposure, shut by tracking plus a scratch class (`bb746c6`, 34 files).** `include` carried `scripts/**/*.ts` under `strict` + `noUncheckedIndexedAccess` with `exclude` of `["node_modules"]` only, so CI — which checks out a fresh clone — typechecked **185** `scripts/` files while this box typechecked **214**. That gap was the bug. **It fired three times (HO 669/670/671) and was never visible between firings**, which is why three sessions each read it as a one-off. **Both premises in the filing were wrong and the corrections chose the fix:** 27 pre-existing untracked probes, not ~14; and untracked was **never the house pattern** — 126 of 154 diagnostics were already tracked, the directory is in no `.gitignore`, and `git check-ignore` returns nothing, so the 29 were untracked because nobody ran `git add`. That inverts the option from *require probes to typecheck* into *finish the convention already in force*. Probes now default to **tracked**; `scripts/diagnostic/scratch/` is the gitignored **and** `tsconfig`-excluded opt-in for work known disposable when written. Excluding the directory outright was refused with the file list as evidence (**126 tracked** files blinded, every standing instrument SKILL names among them); a second looser config was refused because **the defect is timing, not strictness** — a probe written after the gate is unseen by whichever config would have checked it. Criterion met **per class: invisible to destined-for-CI** for the tracked location, **impossible** for `scratch/`. The reproduction ran both with byte-identical `TS2532`: tracked gave `BUILD EXIT=1`, `TYPECHECK EXIT=2`, file named; scratch gave both exit 0 **while the identical defect sat on disk**, which is what proves the exclusion rather than a missing file is doing the work.

**A second finding closed for free, and it is the more durable half.** Six untracked probes were cited as the instrument behind a recorded finding while existing on one machine — SKILL named `welcome-read-budget-670.ts` as the source of the 94,240-row `/welcome` price, and a fresh clone could not run it, against a method that says *clone fresh, the repo is ground truth*. Three further citations (`check-lead-134`, `drop-stale-index-335`, `recover-fts-336`) point at files **committed then deleted** and recoverable from history — noted so a future reader looks there rather than concluding the instrument never existed.

**The chore deliberately bundled the `--write` inversion with the tracking, because making the write instrument safe was a precondition of tracking it rather than a separate concern** — a mutation script becomes materially more dangerous the moment it starts arriving in every clone. All 29 were audited; exactly one writes (`reset-bill-671.ts`, `UPDATE bills SET summary = NULL`), and it gained a header naming the mutation plus a required `--write`, the default inverted on the reasoning that *a flag you must remember in order NOT to write is a flag that will be forgotten exactly once*. `rows-tap-670.cjs`, a 30th untracked diagnostic, was tracked too and **flagged as outside the approved scope of 29** — leaving it would have left the convention violated by the commit establishing it.

**STEP 3 — `docs/method.md` § Gates, widened past render output.** *A gate you cannot fail is not a gate*, with the executable form beneath it: **before running a check, say what it reads if the work was never done; if that is the same as success, it is not the check you think it is.** Six instances — HO 670's green gates over three wrong layouts; HO 671's `| tail -3` showing frames instead of the message; HO 671's same one-liner being a write whose failure mode was *unclear whether it landed*; HO 672's `git check-ignore -v` **exiting 0 on a negated pattern**; HO 672's `TaskStop`, **the first where a tool lied about an action rather than about a state**; and HO 672's **`--word-diff=color` written to a file**, an export carrying **0 change markers in 38,468 bytes** because in that mode the markers are ANSI escapes — *an empty word-diff and a diff with no changes are the same bytes*, and it happened inside the delivery step of the commit closing this very entry. Two requirements go beyond the filing: *a gate binds to the state at the paste, not to the state when it ran* (HO 670's tree went red **after** its final gate, so the report was true when made and false when read), and the append-only deletion gate now carries **its reproduction** — HO 639 filed `grep -c '^-[^-]'` as blind on reasoning, and this HO caught it red-handed **on the commit closing the gate entry about it**: struck backlog lines begin `- **`, the removed side reads `-- **`, `^-[^-]` excludes it by construction, and the grep read **0** where `--numstat` read **2**. One line is adopted from outside the project and marked as such: *a check built from the same expression as the thing it checks is a tautology wearing a verdict.*

**The `TaskStop` contamination earns its place because the tidier design would have hidden it.** One shared output file meant 540 lines for 240 cycles and the mismatch was unmissable; per-run filenames — the obvious tidy-up — would have produced three inflated, internally consistent, entirely clean-looking series, and the analysis would have run on one believing it was the only client. **Prefer the output arrangement in which contamination is visible over the one that is merely neater.**

**Docs (HO 672):** this block · backlog — the `tsconfig` entry, the `method.md` gate entry and the `/welcome` absolute-cost entry **all three struck in place**, text preserved inside the markers, **3 deletions by numstat** and each one a line edited rather than removed; the `/welcome` close carries the mechanism/cadence split, the corrected 3.3/day range 0–5, the thinness and the contamination that caused it · oddities — **three entries**, `git check-ignore -v` exiting 0 on a negation, `TaskStop` reporting success without killing the child, and `--word-diff=color` exporting a diff with no change information in it · method.md — the new § Gates section · SKILL — its own commit behind the self-mod guard. **Notes now run through HO 672.**

**Also (HO 673), the sponsor portrait always on in the bill panel — one of the three design rulings shipped, the other two blocked on data that does not exist.** Corey ruled the panel redesign on 2026-08-18 against three mocks; **only ruling 1 ships.** Rulings 2 (cosponsor faces, party-split at six slots) and 3 (related bills, format R2 with the companion promoted) are **not part-built** — no markup, no CSS, no stub. The corpus carries a cosponsor **count** (`bills.cosponsor_count`, from `$.cosponsors.count`) and nothing else: 50 `CREATE TABLE`s in `scripts/migrate.ts`, none a roster, and `scripts/backfill-cosponsors.ts` is pure SQL over `raw_json`. Related bills measured the same shape — `$.relatedBills` is present on **6,773 of 17,722 bills**, always as `{count, url}`, identities absent, behind the per-bill `/relatedbills` sub-endpoint exactly as cosponsors sit behind `/cosponsors`. Both are filed as **one ingestion entry**, since it is one data-domain problem and not two. Building either against stand-ins would have been the `.pdc*` class HO 668 spent a handoff excising.

**The ruling record is two handoffs and they conflict; the older one was right.** `673-sponsor-portrait-always-on.md` (2026-08-18, base `dc10f72`) had already measured the missing roster and ruled *"only ruling 1 ships; ruling 2 must not be part-built."* `673-bill-panel-sponsor-related.md` (2026-08-19, base `ff535c8`) was written from the remembered design rulings rather than from that handoff and the repo, and reopened the settled question as if undecided. **The newer file was followed** — it was authored against the just-pushed SHA and derived the same number — **and where they conflict the older one wins.** Named here because the defect is in a handoff's content, not in the choice of which to follow.

**The correction that matters more than the excision: the hover card was retired for a stated reason that was wrong.** The justification on record was that `.v2f-sc-card` *held no field that is not already visible*. It held one. The card rendered name, bracket and `cardMeta`; the first two already appeared beside it, but `cardMeta` is **full state name + chamber** (`Illinois · House`) where the bracket spells only `[D-IL-10]` — an abbreviation and an implicit chamber. **The excision is safe because the mock re-homes that field as `.spmeta` (shipped as `.bxp-sponsor-meta`), not because it was duplicated.** Excising on the stated premise would have dropped the field silently. A correct action taken for a wrong reason is a defect that only surfaces the next time someone reasons from the same premise.

**What shipped** (`8cf2400`): the portrait always on at **80×94** — the size `.v2f-sc-photo` already used — beside a stacked name / party-bracket / state-chamber column, per `docs/design/mock-673-sponsor-cosponsor.html` (`sha256 13563ab275ad140268aa5e47d96c2ac1c47cc39c6eff3046045f53d1ff65a0f9`); `mock-673-related-bills.html` (`sha256 d36055713cdd85380d2e2b8735b2765385997477a57bdc20073d4bf92974524f`) is committed alongside it as the **blocked** work's ruling record. **Zero new reads and zero new queries** — `sponsor_depiction_url` already arrives on every one of these surfaces through the existing `SPONSOR_ENRICH_JOIN` (a 1:1 `members` PK join) and was hidden behind `display:none`. The card is **excised, not hidden**: 7 `.v2f-sc-*` rules, the 2 hover-gate selectors, and the markup.

**The change is deliberately unscoped across a shared surface.** `BillExpandPanel` renders on **seven routes** — `/`, `/bills`, `/changes`, `/stale`, `/watchlist`, `/president`, `/members/[bioguideId]` — and a prop to make the portrait conditional was **declined**: two behaviours for one panel is worse than one behaviour everywhere. (`app/api/bill/[id]/panel/route.ts` was in the STEP 0 census and is **not** a render surface — it returns JSON, so it supplies the panel rather than drawing it.) The consequence was captured rather than discovered: **metabox +76px on every surface and every viewport** (+75 at 1920, rounding), which reconciles as the 94px portrait less the single text line it replaces; **stage bar byte-identical** in width and height at every reading; retired-card count **0** everywhere.

**Captures (HO 670 gate, first render-touching HO since § Gates was written):** before/after at **1920 / 1440 / 1100 / 430** plus a **separate `prefers-reduced-motion` pass**, across all seven surfaces, retained in the repo-ignored `docs/handoffs/673-artifacts/` with a machine-readable `readings.json` per side. Console clean (0 errors) on every reading. **Both** corpus bills whose sponsor has no portrait — `119-s-4944` (Armstrong, R-OK) and `119-s-5025` (Graham, R-SC) — render the initials fallback, found by probe rather than by browsing: exactly **2 of 17,722**, against a control of **551 of 553** members carrying a `depiction_url`. `/watchlist` opened no panel on either side (empty for an anonymous session) — the one surface not visually exercised, and unchanged between runs.

**The e2e test was replaced, not edited, and the reason is the session's sharpest finding.** `fit-finish.spec.ts` carried a test *named for the thing this HO deletes* — "sponsor hover card renders inside the expand panel" — guarding its only assertion with `if (await card.count())`. Remove the card and the count is 0, the assertion never runs, and the test **passes while annotating itself "not a finding."** It is § Gates' operational question failing on its own terms: what does this read if the work was never done? The same as success. The replacement asserts the portrait is visible **without hover**, asserts the retired card is absent from the DOM, and carries **no skip-on-empty guard** — a run finding no resolved-sponsor row now fails. **It was proven able to fail**: run against pre-change markup it went red on `.bxp-sponsor-photo` not found; against post-change, green. A test written after the fact and never seen failing is the same instrument class as the one it replaces.

**Docs (HO 673):** this block · `docs/design/` — the two `mock-673-*` files committed as the ruling record with their checksums above, on the HO 670 precedent and **not** the start of the filed `docs/design/` tracking fix · backlog — **five** entries: the cosponsor+related ingestion (one domain, both sub-endpoints named), the 9 remaining `if (await X.count())` e2e guards of which 4 wrap `expect.soft` directly, the two `SponsorPhoto` implementations with both hosts named, the pre-existing 169px horizontal overflow on `/` at 430, and the pre-existing duplicated party bracket on the raw-name sponsor path · oddities — **two** entries. **Notes now run through HO 673.**

**Also (HO 674), the cosponsor and related-bill rosters ingested — two tables, one sub-resource pattern, priced before it was built and the price checked afterwards.** The data half of HO 673's blocked rulings 2 and 3. **No UI, no query, no route** — `bills.cosponsor_count` deliberately untouched, and STEP 0's accidental-render check confirmed with a control that no existing surface changes behaviour when the tables gain rows (zero references to either table name anywhere in `components/`, `app/`, `lib/`; control: `amendment_votes`, a table that does exist, returns 12 hits in `lib/queries.ts`).

**Four STEP 0 premises were wrong and each was corrected with a reading rather than an argument.** The handoff's 402-cosponsor bill **does not exist** — the corpus maximum is **338** (`119-hr-842`) and only 10 bills need a second page. The rate limit is **20,000/hour, not the 5,000** SKILL had recorded, read off a live `x-ratelimit-limit` header. The per-bill sub-resource pattern **already exists** in `lib/summarize.ts` (`fetchBillText` / `fetchBillContext`), so this HO follows one rather than establishing one, which lowers its stakes. And `$.relatedBills.count` **counts relationship entries, not bills** — proven, not inferred: across 40 sampled bills the summed `pagination.count` (1,078) equals the summed `relationshipDetails` entries (1,078) against **1,004 distinct listed bills**. That last one is why `relationship_type` sits **inside** the primary key; without it a backfill dies on a unique-constraint failure at row ~90,000 rather than at its start.

**The sampling that was ordered before STEP 1 paid for itself twice, and the control paid more than the sample.** 52 NULL-`cosponsor_count` bills across all 8 bill types: **94.2% genuine zeroes**, 3 not — the API omits the `cosponsors` key entirely rather than returning 0, so NULL *is* the no-cosponsors case (`scripts/backfill-cosponsors.ts` asserted the inverse and its comment was corrected in this HO's chore commit, the same ruling as `SponsorPhoto.tsx` at HO 673). But one of the three **control** bills read `db=5 api=10`, and chasing that produced the finding that matters: **`bills.cosponsor_count` is systematically stale and always low.** Corpus-wide after the backfill, **1,915 of 13,922 bills disagree (13.8%)** — **1,911 roster-higher against 4 roster-lower**, net **+4,887** cosponsors the column does not know about. Cosponsors accrue without reliably moving `updateDate`, so no upsert fires. *(A 40-bill sample had read 30%; the corpus supersedes it at 13.8%, and the ~220 NULL-with-cosponsors extrapolation likewise overshot the true 111.)*

**Schema, with the two decisions settled by measurement rather than by preference.** `bill_cosponsors(bill_id, bioguide_id, …)` and `bill_related_bills(bill_id, related_bill_id, relationship_type, …)`, loose FK per HO 447. **Ordering** needs no sequence column — `sponsorshipDate` plus `isOriginalCosponsor` covers it. **Withdrawals are stored and marked**, because the API *returns* them: `119-hr-452` reports `count: 299` and `countIncludingWithdrawnCosponsors: 300`, and **the list yields 300**, the extra carrying `sponsorshipWithdrawnDate`. That asymmetry is written into the schema comment, not merely the entry, because it is a trap with a live hit rate: **`pagination.count` excludes withdrawals while the list includes them**, and `cosponsor_count` is sourced from the former, so any integrity check must filter to the active subset or it disagrees on exactly the bills that have a withdrawal. `identified_by` is **deliberately lossy** and its comment says so with the rate — where CRS and the House both assert the same relationship the second write replaces the first, **measured at 1 in 167 (0.6%)** on the slice (`119-s-1318` → `119-hr-1919`, "Related bill", identified by both).

**The backfill is read-only by default and that was demonstrated, not asserted:** a full 50-bill dry run fetched 80 requests, shaped 1,889 rows, and the read-back showed **0 rows in both tables**. Idempotency likewise — re-running `--write` over the same 50 re-wrote 1,722 + 167 rows and left the table counts **identical**, while `--resume` skipped 45 of 50 and spent **5 requests against the cold run's 80**, that 80 being the control proving the counter can move.

**STEP 3, full corpus:** 17,677 bills fetched, **24,429 API requests against a STEP 0 estimate of 24,505 — 0.3% off** — 2 transient errors (`119-hr-8539`, `119-sres-269`), both recovered on an explicit retry, leaving zero outstanding. Result: **`bill_cosponsors` 168,610 rows across 14,033 bills** (77 withdrawn, 90,746 original) and **`bill_related_bills` 10,254 rows across 6,768 bills**, 6,934 distinct related ids. **Wall clock was ~4h against a predicted ~1.5h, and the mis-estimate is worth recording: it was priced against the rate limit when the binding constraint is per-request latency.** The job ran at ~2.1 req/s ≈ 7,500/hr, never approaching the 20,000 ceiling it had been costed against — the request count was accurate to 0.3% precisely because that was measured, and the wall clock was wrong by 2.7× precisely because it was not.

**The relationship-type value set is SIX, not the four a 40-bill sample showed, and ruling 3 now has its numbers.** Corpus-wide: `Related bill` 6,187 · **`Identical bill` 3,454** · `Procedurally related` 375 · `Public law contains the text` 114 · **`Contained in public law` 100** · **`Identical Bill (Became Law)` 24** — the last two invisible at sample scale, and the last one carrying a **capital B** so that `= 'Identical bill'` silently excludes it (oddities). **There is no "Companion" value.** `Identical bill` covers 3,237 bills, is **89.3% cross-chamber** (3,083 of 3,454, against 371 same-chamber), and **69 bills carry more than one**, so "promote *the* companion" has no unique referent on those. That is an **open question for Corey**, recorded in backlog and not decided here: promote `Identical bill` as-is, promote it only where the chamber differs, or something else. The UI HO cannot be written until it is answered.

**Integrity at full scale, both directions.** Cosponsor rows resolve to `members.bioguide_id` at **100.00%** (168,610/168,610), which is the number the party-split UI depends on. Related rows resolve to `bills.id` at **99.20%** — and the 82 that do not are a **finding about the corpus, not about this ingestion**: **74 of them are 119th-Congress** ordinary-type bills the sync has not reached, only 8 prior-Congress. Filed. The first attempt to characterise them grouped by an unqualified alias and reported all 82 as 117th-Congress, which would have dismissed the whole thing as a prior-Congress artifact; the corrected query is what produced the real split (oddities).

**Docs (HO 674):** this block · backlog — the HO 673 ingestion entry **struck in place** with its text preserved and the close criterion shown rather than claimed (`119-hr-452` read from the local DB with no network call), plus **four** new entries: the ruling-3 open question, the 13.8% stale `cosponsor_count`, the resume watermark and its ~3,800-request tax, and the 74 missing 119th-Congress bills · oddities — **three** entries · SKILL — its own commit behind the self-mod guard. **Notes now run through HO 674.**

**Also (HO 675), the bill panel's roster half — related bills R2 with cross-chamber identicals promoted, and cosponsor faces party-split.** The render HO 673 ruled and HO 674 unblocked, shipped on the shared `BillExpandPanel` and therefore on all **seven** routes that mount it (`5caa4ec`). Two new queries on the **lazy** `/api/bill/[id]/panel` fetch under a new **`bill-rosters`** tag, plus `lib/bill-rosters-view.ts` for the transforms and 245 lines of CSS.

**The apportionment rule, as arithmetic, and the correction that produced it.** Six face slots. Equal split among the parties **present**, floored; a party that cannot fill its share hands the remainder back and the loop runs again over the groups that still have room; tie-break, when the remainder is smaller than the number of open groups, is largest party count first. Worked: `R×4 → R4` · `R×40 → R6` · `D6/R2/I1 → D3/R2/I1` · `D1/R1 → D1/R1` · `D3/R3 → D3/R3` · `D21/R1 → D5/R1` · `D218/R184 → D3/R3` · zero → the row does not render. **The handoff's prose pointed the other way and was wrong** — "by the actual party breakdown, not fixed 3/3" reads as *proportional*, and that was the architect's summary rather than Corey's ruling. The committed `allocate()` in `mock-673-sponsor-cosponsor.html` is the ruling, and the summary had lost a property the code had: **proportional allocates a PRESENT party zero faces on 718 bills** — `119-hconres-31` D1/R11 → **D0/R6**, erasing the lone Democrat — where this rule does it on **none**. The two disagree on **1,944 bills (13.85%)**. And the thing the prose was trying to forbid, the rule already does not do: it lands on a flat 3/3 on only **35.2%** of two-party rosters. **The port is verified against the record rather than against itself** — all 12 named cases reproduce, three invariants hold across 6,724 party combinations, and a deliberately wrong rule run through the same harness fails 4 cases and produces 1,400 erasures, so the harness can detect a bad rule.

**Withdrawn cosponsors are excluded in SQL, not downstream, and that is load-bearing.** HO 674 stored them (`sponsorship_withdrawn_date`, 77 rows corpus-wide) so this could be a decision. Filtering in the component would leave the count and the faces disagreeing on exactly the bills that have a withdrawal — the asymmetry HO 674 wrote into the schema comment. Captured on `119-hr-452`: **300 stored, 299 active**, and the panel reads `299 cosponsors` above groups summing 107 + 191 + 1 = **299**.

**The `identical` predicate is case-insensitive CONTAINMENT, never equality, and the ruling's own headline figure was a casualty of the equality it forbids.** `LOWER(relationship_type) LIKE '%identical%'` matches `Identical bill` (3,454) and `Identical Bill (Became Law)` (24) out of six corpus values; `= 'Identical bill'` **drops those 24**, which are the most interesting rows in the set because the twin became law and this bill did not — the promoted line now says so. Controls: 6,562 `Related bill` / `Procedurally related` rows exist and the predicate matches **0** of them. **HO 674's "69 bills carry more than one identical" was produced by that equality; under the ruled predicate it is 91.**

**Cross-chamber, and the number the ruling assumed: the design is unchanged.** Chamber comes from the id's type segment, and **0 of 3,478** identical rows fail to parse on either side. **3,107 rows (89.33%) are cross-chamber**, against 371 same-chamber that stay in the list below — promoted nowhere and dropped nowhere (217 bills carry one). Counted by **distinct target**, which is what the block draws, only **25 of 3,060 bills (0.82%)** promote more than one, so the promoted block is a single row **99.2%** of the time. **Counted by ROW it looks like 47, and the gap is HO 674's "counts relationship entries, not bills" trap firing one layer up** — twice in two HOs now, so the rule that survives both is *count what the UI draws, not what the table holds*. **The dedupe by target is therefore forced rather than chosen:** 22 pairs carry more than one identical-matching type — `119-hjres-124 → 119-sjres-80` arrives as `Identical bill`, `Identical Bill (Became Law)` **and** `Contained in public law` — so a row-wise promotion draws the same twin three times. **16 of the 82 unresolved related ids sit on rows this block promotes**, and those render as id-only with no title and **no link**, because `app/bill/[id]/page.tsx:126` calls `notFound()` and a link would 404.

**The cost decision, priced twice and wrong once in the obvious direction.** The rosters ride the **lazy panel route**, not the row payload: on the row payload they cost **+504 rows per 50-row page render** on a `bills`-tagged entry that is re-read after every flush (89,090 rows, ~7.3/day), where the lazy route costs **~63 rows** and only for bills someone expands. Measured, current panel open **12.3 rows** → **75.3** on a 40-bill ordered sample (6.1×). The tag is **`bill-rosters`, not `bills`** — nothing writes these tables on a schedule, so joining `bills` would discard the entries ~7.3 times a day for data that had not changed; executable `bills`-tagged entries are **37 before and 37 after**. **And the query SHAPE was priced rather than reasoned, which is the half worth keeping:** one full join reads **1,015** rows on `119-hr-842`; fetching the party counts and then a `LIMIT 6` per party reads **3,384** — **3.3× worse** — because the `ORDER BY` scans the whole party group whether or not six rows come back, and doing it per party pays that scan twice. The intuitive optimisation was the expensive one.

**`bills.cosponsor_count` is deliberately untouched, so the panel now shows the drift.** The total line stays on the stale column while the group headers come from the roster, and on **1,919 of 13,926 bills (13.78%)** they disagree — net **+4,880**, median **+1**, max **+50**, 56 bills off by more than 10. Ruled visible rather than repaired: sourcing the total from the roster is the quiet fix this HO was forbidden to make, and the column is HO 674's entry to close. Both ends captured (`119-hr-8591` 10 vs 11, `119-hr-1628` 202 vs 252).

**Captures (§ Gates), and the run that mattered most is the one that failed.** 75 readings at **1920 / 1440 / 1100 / 430** plus a separate `prefers-reduced-motion` pass across 15 probe-found bills; **75/75 panels opened, 0 console errors, 0 non-200 stylesheets, 0 horizontal overflow**. **The first run reported 0 errors / 0 CSS failures / 0 overflow having never opened a single panel** — an unscoped `[role="button"][aria-expanded]` matched the header's mobile-nav toggle, and `/bills`' `?q` runs through `bills_fts MATCH` rather than the id `LIKE` the other feeds use, so `?q=119-hr-842` returned an empty list. Three greens from an instrument that never reached the thing under test; the harness now exits non-zero unless every case opens. **Layout, predicted then measured:** metabox **+67 / +134 / +201** by party count against a STEP 0 prediction of +66 / +133 / +199; panel **+0 to +202** at desktop widths and up to **+401** at 430 where the container query stacks the columns. **Stage bar and sponsor block byte-identical**, asserted as a comparison rather than an assumption — the delta is measured by removing exactly the added nodes and re-reading, and both read unmoved in all 75. The sponsor photo class strings are **`bxp-sponsor-photo`** and **`bxp-sponsor-photo bxp-sponsor-photo--fb`**, unchanged, with the fallback path exercised on `119-s-4944`.

**Three defects the readings could not see and the screenshots could.** The initials-fallback face **lost its party edge**: `.bxp-face-photo--fb`'s `border` shorthand reset all four sides after the shared `border-bottom`, and the computed value read `rgb(31,41,55)` where a real photo read `rgb(59,130,246)` — two faces in the corpus have no portrait, so this would have shipped as a party-coloured strip with an uncoloured gap that nothing in the markup would have shown. The list rows' **relationship labels stranded ~500px from their rows**: the mock's `auto 1fr auto` grid reads as a tidy ledger in its ~560px isolation box and falls apart in the real ~880px left column, so `.bxp-rb` became flex with a non-growing title, matching `.bxp-comprow`, which was already flex and already read correctly in the same capture. And the hover tip **covered the group header in every shot**, because the harness hovered before photographing — the resting layout, the thing the gate exists to look at, was never photographed at all; hover now gets its own image. The tip reveal is separately proven: 49 cases, resting `none` and hovered `block` with real 146–171×47–49px boxes.

**Scope held.** Hover names are CSS-only and `BillExpandPanel` is already `"use client"`, so island membership is unchanged and **no changelog entry is owed** (HO 665 criterion). The `+N` chip and `+N more` render as **plain text**, not links: the mock draws both as `<a href="#">` and neither destination exists, so the affordance ships without the promise. The promoted block is **not** deduped against the hearing agenda — the list is (76 bills overlap; control 34,958 sibling pairs) — because suppressing a twin that happens to sit on today's agenda would erase the one fact the block reports; flagged for Corey rather than taken silently. The faces reach **five of the seven** routes, since `getWatchlistBills` and `getMemberBills` omit `SPONSOR_ENRICH_SELECT` and the row is gated on `cosponsor_count` — inherited, not widened, and filed.

**Docs (HO 675):** this block · backlog — the ruling-3 open question **struck with its close criterion shown and three of its own figures corrected** (69→91, the by-row 47 vs the by-target 25, and the cross-chamber share), the `cosponsor_count` drift entry given its UI-visible consequence with the distribution and both captured ends, and **four** new QUEUED entries (the two destination-less `+N` affordances, the two routes the faces do not reach, the fourth party-bracket implementation, and the promoted-block agenda exemption addressed to Corey). *There was no separate ruling-2 entry to strike — ruling 2's blocker was the ingestion entry, already closed at HO 674.* · oddities — **five** entries · SKILL — its own commit behind the self-mod guard. **Notes now run through HO 675.**
**Also (HO 676), the rosters given a maintenance path — and the finding is that a trigger compared against a value you may not correct has to fire on that value MOVING, not on it disagreeing with you.** The number that decides the design comes first: of **1,089 bills that gained a LATER cosponsor in 30 days, only 479 (44%) carry an `update_date` at or after that cosponsor's date.** The other **56% move no `updateDate`, fire no re-upsert, and are invisible to any count-based trigger whatsoever** — which is why the workhorse here is an oldest-checked-first sweep rather than a clever predicate, and why a design that leaned only on counts would have looked correct and silently missed the majority of accrual. The sweep is also the **only** path that reaches the **3,799 bills storing `cosponsor_count` NULL, 109 of which carry a roster anyway**, so NULL is not reliably zero and a `count > 0` trigger structurally cannot see them.

**The trigger, in three measured steps, because the first two were wrong.** HO 674 STEP 0.4 specified `active roster <> cosponsor_count`. Measured at HEAD that selects **1,910 bills of which 1,906 are ones where the ROSTER is the fresher side** — fetching them writes nothing and selects them again next tick, forever, because the stale half is `bills.cosponsor_count`, a column this HO is scoped out of repairing. Adding **DIRECTION** (`count > stored`) took the priority set to **10 + 73**. The STEP 3 bounded slice then caught direction alone re-firing forever on **5 bills whose comparand is stale ABOVE the truth** — four where a cosponsor withdrew after the count was captured, dropping `pagination.count` while the stored column kept the old value, and one where `$.relatedBills.count` reads 1 against an API returning 0 entries. **A fetch can lower the roster toward reality but can never raise a wrong count to meet it**, so disagreement is permanent there while movement is not. Adding **MOVEMENT** (`count <> count_at_check`) took the residual to **0**, falsified on the same rows under both predicates in one probe: **5 → 0**, the detector proved able to fire on the data it reports zero for. **Direction and movement are one finding in mirror** — direction closes the case where the roster is behind, movement closes the case where the comparand is stale above the truth — which is why the second half landed here rather than being filed as a follow-up that would have left this block claiming a half-made fix and handed the next reader *"directional is the rule"*, a statement that is wrong in general.

**The three alternatives to the two new columns were rejected on inspection, not taste**, and the middle one is the sharpest: `active_count` holds the API's number, and the entire defect is that the comparand differs from the API's number, so **using one to stand for the other is the tautology § Gates already warns about**. Reading it off `checked_at`/`changed_at` fails because a timestamp records *when*, never *what*. Backing off on recency instead would suppress the five stale-high bills **and** genuine growth arriving the day after a check — buying the fix by blinding the trigger.

**`bill_roster_state` is a table, not a column on `bills`, for two reasons and the cost one is measured.** Evaluating the trigger against the roster tables means aggregating 168,610 cosponsor rows: **228,223 `rows_read` / 98ms**. The same selection against a per-bill table is a bills scan plus one index probe — the identically-shaped never-checked selection measured **31,652 `rows_read` / 61ms**. The blast-radius reason is the stronger one long-term: a column on `bills` would put a new invariant on the sync's `ON CONFLICT DO UPDATE SET`, which would have to omit it forever (the HO 459 `committee_hydrated_at` rule), while **a separate table cannot be clobbered by a re-sync at all.** Seeded once from the backfill's own coverage — that run considered 17,722 bills, fetched 17,677 and skipped 45 as covered, so a bill left with zero rows was checked and found empty rather than missed. **The seed's one inaccuracy is declared rather than hidden:** the ≤7 bills that entered `bills` after that candidate list get a `checked_at` they did not earn, costing at most one sweep cycle and costing nothing for the six that mattered, all of which the directional trigger selected on the count anyway.

**Cadence, with both self-corrections carried rather than quietly fixed.** `/api/cron/bill-rosters`, `40 */3` (8 ticks/day, colliding with nothing at `:40`), `maxDuration 300`, a 240s carve and a 120-bill cap, its own route rather than a step inside `/api/sync` — which has hit **53s of its 60s ceiling** and is not a place to add fetch-heavy work. **(i) The request price is ~1,920/day, not the ~1,325 first stated.** That estimate used the backfill's 1.38 requests/bill, which skipped the related fetch when the count was 0 — and **gating the refresh on a possibly-stale count would reintroduce precisely the blindness the sweep exists to backstop**, so the refresh always fetches both and pays 2 requests per bill. The reason is worth more than the number. **(ii) Wall clock landed under model, with the cap binding before the deadline** — 120-bill ticks measured **86s / 107s / 129s / 146s** on bills with real rosters and **37s / 38s** on settled ones, against a 240s carve. A deadline that never binds means the tick size is the control, which is the parameter that can be reasoned about. Full corpus recycles in **~18.5 days**; the 44% that moves `updateDate` lands within one tick of the next sync upsert.

**The flush guard is on `changedBills`, never on rows written, and it was seen firing both ways.** `INSERT OR REPLACE` rewrites every row of an unchanged roster, so a row count is non-zero on essentially every tick and the flush would become exactly the multiplier HO 671 removed from summarize. `changedBills` is incremented inside the branch that performs the write, selected by the diff being non-empty, so it cannot be truthy while nothing was written. **The first attempt to observe it was unreadable and the reason is worth keeping:** the first request after any idle period costs ~42ms whether or not anything flushed, so latency alone cannot separate a flush from a warm-up. Adding an untagged `/api/version` control read in the same pair resolved it — **wrote: panel 96.9ms / control 4.1ms; wrote nothing: panel 7.7ms / control 4.3ms; the authorised tick, 4 changed: panel 47.6ms / control 5.4ms**, against steady hits of ~5ms. The control is flat across every idle period, which is what rules out the confound; the panel's magnitude varies with the known query-cost spread and only its separation from the control is load-bearing.

**A bound stated in units the instrument cannot produce is not a bound.** The handoff required a **30-bill** bounded slice *and* a two-direction flush-guard proof. The guard lives in the route, and the route's unit is a **120-bill tick** — so the two requirements are incompatible as written and the executing session could not satisfy both. It ran the slice as specified and then five route ticks, and **stated the overage rather than leaving it to be inferred from addition**: 5 ticks, 600 bill-slots, **390 distinct bills** (360 from three oldest-first ticks plus the 30-bill slice; two ticks under a scratch newest-first ordering re-checked bills already inside that set), all against the shared prod Turso with a local server and no second database. Same family as everything else in § Gates, one level up: **not a mis-measurement, a mis-specification** — the bound was expressed in bills while the only instrument that can exercise the guard is denominated in ticks.

**Closed with a bill nobody chose in advance.** `119-hr-9641` arrived rosterless **while this HO was being built** — the corpus grew 17,728 → 17,729 between STEP 0 and the migration — carrying `cosponsor_count` 6 against an empty roster, and the trigger selected it. It now reads 6 roster rows. **Two of `backlog:80`'s three close legs were mis-specified by the entry itself and are recorded as such rather than reinterpreted.** Leg 1 said *introduced after the backfill*; `119-hr-9641` was **introduced 2026-07-13**, before it, and what postdates the backfill is its **ingestion** (`update_date 2026-08-20T18:50:39Z` against the backfill's last write `2026-08-20T01:02:19.943Z`). **The class that matters is ingested-after, not introduced-after** — `/api/sync` reaches a bill weeks after introduction, `119-hres-1400` having been introduced 2026-06-30 and ingested 08-19 — which is the same gap `backlog:471` files, noted here and deliberately not chased. Leg 3 demanded the count-vs-roster disagreement move off 1,910/13,929; it reads **1,905 of 13,931**, and the criterion was measuring the wrong quantity: that fraction is `:81`'s stale **column**, not roster freshness. The instruments that answer what the leg was reaching for are **bills with `cosponsor_count > 0` and zero roster rows, 5 → 0**, and the priority band **draining to 0 after every tick**.

**Residual, stated so the block is not read as complete:** **510 of 17,729** bills were re-checked in the shipping session, and the cron's first autonomous fire on prod happens after this lands. Four of the bills the authorised tick changed were reached **by the sweep alone** — their `$.relatedBills.count` is absent from `raw_json`, so no count signal exists for them at all, which is the sweep's second justification arriving as evidence rather than as an argument.

**Docs (HO 676):** this block · backlog — `:80` **struck in place with its text preserved**, all three legs answered and the two mis-specified ones named as such, plus `:81` **amended in place** to record that its premise (*once the rosters exist, the roster IS the better source*) now holds, since a roster nothing maintained was not a better source than a stale column but a differently-stale one, and to correct its *always low* framing with the 4 stale-above-the-truth bills the trigger had to be built around · **one** new QUEUED entry, `/api/cron/lda-rollup` absent from the `CRON_ROUTES` health registry (found while adding the roster route, filed rather than fixed; the registry's own header said *14* while listing 15 and was corrected in passing) · method — the parked § Gates clause on close criteria that cannot become false · oddities — **three** entries · SKILL — its own commit behind the self-mod guard, both halves of the `bill-rosters` tag entry rewritten together rather than half-corrected, which is the third time this arc a clause has needed rewriting whole. **Notes now run through HO 676.**
**Also (HO 677), `bills.cosponsor_count` given an owner — and the finding is that a measurement is about the table it was taken on.** The deciding argument is the failure mode, so it goes first: under the option taken, a broken refresh means new bills carry NULL and the panel **hides** the COSPONSORS row rather than printing a total above groups that do not sum to it. Absence over a wrong number, visible rather than silent, and `/api/health` already watches the route at a 7h threshold. The rejected alternative fails the other way — remove the refresh's write and the column silently reverts to a frozen value with nothing rendering differently, which is exactly how `backlog:81` came to exist.

**TWO OF THE HANDOFF'S OWN PREMISES WERE WRONG, and the second is the one worth keeping.** (1) *"Sync re-staleifies on the next upsert"* describes a mechanism that does not exist: `/api/sync` writes the column from the detail endpoint's `$.cosponsors.count`, the **live** count at upsert time. Measured — **136 of 136** bills upserted within a day agreed with their roster, **501 of 501** within a week, and **0 of the 1,905** disagreeing bills had been upserted in that window (oldest `update_date` 2025-02-28). The defect was **frequency, not correctness-at-write**, so the fix that mattered was not a better writer but a single owner. (2) The handoff instructed that option C — drop the column, derive from the roster — be disqualified using HO 675's **+504 rows per 50-row page**. That figure is `bill_cosponsors`: **168,629 rows at ~12 per bill.** C would join `bill_roster_state`, which is **1:1 and already materialized** by HO 676. Measured on the real feed shape: **110 `rows_read` against 75, +35 on a 25-row page.** **THE RULE: a measurement is about the table it was taken on — reusing a cost figure across tables is citing, not measuring.** It is worth writing down because the reach was so natural: an existing, authoritative, arc-local number, invoked in the very sentence instructing the executor to *price rather than reason*, which is what made it look like the question was already settled. C was rejected instead on **blast radius** — six-plus queries gaining a JOIN, several carrying forced `INDEXED BY` hints that must EXPLAIN clean, `getMemberStats`' `AVG` moving to a joined table, and an HO 635 expand/contract for the drop — to replace one materialized count with a join to another.

**Option B, in two parts, because ownership alone cannot move a number.** `lib/bill-rosters-refresh.ts` owns the column and writes it from the same `pagination.count` the HO 676 watermark caches, so the column and `bill_roster_state.active_count` cannot drift apart by construction. `/api/sync` omits it from **both** the INSERT list and the `ON CONFLICT DO UPDATE SET`, with the omission stated there (the HO 459 rule: an unexplained omission gets helpfully re-added). But only **510 of 17,729** bills had been refresh-checked, so a one-shot reconcile shipped in the same commit — **2,013 rows, pure DB, zero API calls**, since the derived count was already materialized. **Disagreement 1,905 of 13,931 → 0; NULL-despite-a-roster 108 → 0.** Of the 2,013: 1,901 up, 108 NULL→value, and **4 DOWN — `119-hr-1238` 2→1, `119-s-1326` 8→7, `119-s-2904` 24→23, `119-s-3966` 21→20** — the column sitting above the truth after a cosponsor withdrew. Right, and a decrease; named in advance so it surprises nobody.

**The NULL rule, ruled rather than left implicit, and its first reason is a measurement.** Write `active_count` when > 0, **NULL when 0**. **No bill in this corpus has ever stored `0`** (NULL 3,798 · `=0` **0** · `>0` 13,931), so NULL already IS the zero; writing `0` would invent a third state and flip the panel's `cosponsor_count != null` gate true on **3,690 bills**, rendering an empty COSPONSORS row nobody asked for. So NULL's meaning changes from *the API omitted the key* to *the refresh saw zero active cosponsors* — same values, a stated rule instead of an artifact — and the ambiguity it used to carry now lives in the data model, where `active_count = 0` beside a `checked_at` is a **verified** zero. It held under a real 2,013-row write: still zero rows storing `0`.

**What this does not claim.** The column is **bounded-stale, not correct.** Its freshness is each bill's last check, and at close **97.1% of `bill_roster_state` rows still carried the HO 676 seed stamp** rather than a refresh check — so *the corrections this HO writes are corrections to 2026-08-19/20 truth, not to now*, bounded going forward by the ~18.5-day sweep. Better than unbounded, which is what the column had. **This is the third time in this arc a fix has been better rather than correct, and each time saying so has been worth more than the fix.** No `revalidateTag("bills")` from the cron either: the corrected value reaches the panel through the `bills`-tagged row payload, so it surfaces on the next `bills` flush (~7.3/day, mean gap ~3.3h) — flushing on a schedule for a handful of bills would re-create the HO 671 multiplier. The one-shot flushes once.

**The before was HO 675's own artifacts, which is what makes the pair a measurement.** Its captures were still on disk, taken by the same harness over the same 15 bills **before this HO existed**, so the baseline could not have been shaped by knowing what the fix would do; `roster-shots-675.ts` was then re-run **unmodified** for the after. `119-hr-1628` **202/252 → 252/252**, `119-hr-8591` **10/11 → 11/11**, eight agreeing bills unmoved, four genuine-zero bills still hiding the row. Two of the 108 (`119-hr-8411`, `119-hres-1334`) now render the row **and their faces**, which they had hidden entirely — the clearest evidence that NULL was never reliably zero. The gate control, `/members/[bioguideId]` with a row expanded, shows the row **still absent** at all five viewports: not widened.

**Three findings from the instruments rather than the code.** The gate harness read the panel on `.bxp` mounting, which races the lazy `/api/bill/[id]/panel` fetch — it reported `groups: []` at the first viewport of every context, which reads exactly like *"this bill has no groups"*; **§ Gates' own subject, appearing inside an instrument built to satisfy § Gates, for the third time this arc.** The reconcile **exited non-zero after 2,013 committed and read-back rows**, because `REVALIDATE_URL` pointed at a localhost that was not running — a run that succeeded and reported failure, which teaches a reader to distrust the one signal that has to stay trustworthy. And `/members/[bioguideId]` was found **overflowing 10px at 430**, pre-existing and filed: like HO 673's 169px overflow on `/`, it was found by a capture harness aimed at something else entirely, which is the argument for the viewport set being **wide rather than representative**.

**Docs (HO 677):** this block · backlog — `:81` **struck in place with its text preserved**, its close criterion answered as a fraction (**1,905 → 0**) and **its own stated mechanism corrected**, since the sync's write was never incorrect, merely infrequent · **two** new QUEUED entries: the two refresh branches that have never fired (with the awkward note that a good outcome — the reconcile and the seed — is what hides them) and the 430px overflow · method — **the second parked § Gates clause, riding alone as ruled** · oddities — **two** entries · SKILL — its own commit behind the self-mod guard, the COSPONSORS row entry's two halves rewritten together. **Notes now run through HO 677.**

**Also (HO 678), the API-key rotation audit — and the finding is that the emission path had already fired, into the one store nobody was looking at.** A read-only audit of every secret the app consumes, on the rule that a public repo (`CSU-J3/cbt`, `private: false`, confirmed) burns any value that ever reached any commit. History is clean: gitleaks 8.30.1 over all refs (**1128 commits, 0 findings**) and a second exact-value instrument over `git log --all -p` (**1141 commits, 321,623 lines**) plus the **134 reflog-only commits `--all` never reaches** — **0 hits on any of the eight secrets**, in full or by 8-char prefix. The tracked tree (974 files), the untracked set, the 1,536 ignored handoff files, the client bundle and all 331 build artifacts are likewise clean. **What was not clean was the database.** `wrapCronRoute` (`lib/cron-log.ts:165`) takes a raw `err.message` into **both** the JSON response and `cron_runs.error_message`, and seven `lib/` fetch helpers throw messages containing the full request URL — so `CONGRESS_API_KEY` sits in cleartext in **three production rows** (`/api/sync`, 2026-06-17, 06-18, 08-08), in `error_message` **and** `payload`. On the success path `chronicErr` persists the same strings on a **200**. A sweep of **348 text columns across all 58 tables** found it there and nowhere else, and found no other secret in the database at all.

**The verdict table had no cell for what was found, and saying so was the point.** ROTATE NOW triggered on history, tracked file, client bundle, or a runtime log inside retention — a permanent DB column is none of those and is strictly worse than the log case, whose retention is one day. Rather than stretch the rule silently, the audit called the row ROTATE NOW **on stated reasoning** and put the gap to the owner, who **extended the trigger to "any durable store the app writes (DB, KV, blob)"**. `CRON_SECRET` was ruled ROTATE NOW on a different mechanism: the value had been pasted as a **literal into the commands Code ran**, so it travelled through the API inside those tool calls — the six `.claude/settings.local.json` allowlist entries (deleted here, 541 → 535) being **residue, not cause**. That ruling is now a `method.md` § Environment line: a command references a secret by env name, never by value.

**Five of the pre-read's premises were wrong, and the instruments — not the reasoning — are what corrected them.** The 13-commit gap between `rev-list` (1141) and gitleaks (1128) was blamed on `package-lock.json` path-allowlisting; there are **0** lockfile-only commits. Purpose-built control repos established the real rule — **gitleaks counts a commit only if it has ≥1 added text line** — and the four zero-add commits it counts anyway are exactly the four also containing a Modified file, since a removals-only modification still presents a post-image and a pure deletion does not. **1141 − 13 = 1128, exact**, and none of the 13 can carry a secret. The emission set was named as four `lib/` sites; it is **nine, of which seven carry a key** — while **two the pre-read flagged were already safe** (`summarize.ts:79` passes a keyless payload URL; `markets.ts:251` is deliberately key-stripped to `keyLen`). `K0Ue` still matches the live `FEC_API_KEY` and sits in **five** tracked files, not three. Consumed distinct is **41**, not 35. And `.env.example` is missing **seven** names, not six — `REVALIDATE_URL` joins the list, which is recorded here rather than by amending `:37`.

**Two instruments failed their own controls, and one of them had already printed a clean result.** The targeted grep reported `hits=0` over all of history while firing **7 of 8** patterns on synthetic bait: a quoted heredoc had collapsed `\\` → `\`, so `"\s*"` reached Node as literal `s*` and the regex **still compiled**, asking a different question — and the pattern it destroyed was the only one matching an api.data.gov key, the exact shape gitleaks has no rule for. Rewritten with regex literals the control went 8/8. Vercel's runtime-log surface failed differently: `level=["warning"]` returns nothing for a window in which `statusCode=500` returns a line labelled `[warn/serverless]`, and the text-path control times out at every window size — so instrument D's log half was recorded **UNAVAILABLE, not passed**. It changed no verdict, because the durable evidence had already decided the row; an audit that had rested on the log query would have reported the key as never emitted. **Both are now § Gates instances seven and eight.**

**The rest of the table came back quiet, which is worth stating as a result rather than an absence.** `vercel env ls` across all three environments: **12 variables held, all 12 consumed — no dead secrets**; Development holds **nothing**; the HO 579–581 `LDA_API_KEY` gap is not recurring (Production **and** Preview, 31d, the youngest entry). Maximum age is **120 days**, so the audit's proposed 12-month threshold **was struck** — nothing turned on it — and the two api.data.gov keys, which have no provider-side age instrument, took an **annual calendar line** instead. Six entries reach **Preview**, so both ROTATE rows ship to every review-ref build and the rotation must cover Preview as well as Production. `TURSO_AUTH_TOKEN` is **non-expiring** (`EdDSA`, `iat` 2026-05-02, no `exp`) and filed as a WATCH; its JWT age (121d) and Vercel's `created` (120d) agree within a day, the one provider-independent cross-check available. **Rotation order is fixed and not negotiable: sink → sources → deploy → rotate → scrub → GSA disable**, because rotating before the sink fix writes the new key into the next failed run.

**A third oddity came from the session's own conduct, not the audit.** `vercel login` is a device flow whose CLI process **stays alive waiting** for the browser approval; a blanket kill of every `node.exe` matching `vc.js` removed that listener, so the owner saw a browser success page while `auth.json` on disk was **3 bytes — `{}`, no token** — its sibling temp file naming the killed pid. The browser's success page is not the signal; the CLI printing the account is. Filed with the rule that **cleanup stays port-scoped and never runs while an auth flow is open** — an interactive flow's waiting process is application state, not debris.

**Docs (HO 678):** this block · backlog — **seven** new OPEN LOOPS lines (the two ROTATE NOW rotations carrying their mechanisms and the extended durable-store trigger; the emission-path fix **naming the sink first** with the seven sources and four diagnostics under it; the annual api.data.gov calendar; the CLI-stays-external constraint; the postcss advisory that **rides a Next 16 HO rather than driving one**; and Node v25 outside the engine ranges) plus **one** WATCH line for the non-expiring Turso token · oddities — **three** dated entries (the heredoc that rewrote a regex in transit; the log filter that denies what another displays; the blanket kill that ate a device-auth listener) · method — § Gates gains instances **seven and eight** from the first two of those, and § Environment gains the by-name-never-by-value rule from ruling 2 · **no SKILL edit** · `docs/handoffs/678-pasteback.md` holds the filled closure table, repo-ignored. The audit itself wrote nothing: STEP 0 was read-only and halted for the ruling, and no code, env, DB or rotation change rides this commit. **Notes now run through HO 678.**

**Also (HO 679), the emission sink — redaction at the throw, and the four sources the enumeration missed that the RULE caught.** The HO 678 audit found `CONGRESS_API_KEY` in cleartext in three production `cron_runs` rows; this closes the path that put it there. `lib/redact.ts` carries two functions applied at three layers: the **row sink** in `finishCronRun` (the single writer of `payload` and `error_message`, so the catch path, the success path's `chronicErr` and all four handler-level messages are covered **with no change at those call sites**), the **body sink** on both `wrapCronRoute` return paths, and the **sources**, fixed as well because a script printing to a terminal never reaches the sink and that terminal is a transcript. **The two passes are not peers and the SKILL entry says so:** the **value pass is the primary defence** (literal env values, any casing, any length, URL or not), the **header pass is defence in depth** (case-sensitive `Bearer`/`Token` with a 12-character tail floor) and exists only for a third-party token echoed back to us — a value in no env var we hold. The query-parameter pass **removes** the parameter rather than substituting a placeholder, because the scrub predicate HO 680 will run is `api_key=` **with the `=`** and a placeholder would match it forever.

**The ruling that did the real work was a RULE, not a list, and the difference is measurable.** STEP 0 enumerated the ledger's eleven sources and confirmed every line number at HEAD; Corey ruled the general form — *every throw that interpolates a URL goes through `fetchError`; a throw that does not is untouched* — which took in five keyless-today sites for uniformity. Applying it then surfaced **four more the enumeration had not seen** (`kalshi-218` ×2, `sync-career-votes`, `voteview-source`), because the STEP 0 discovery grep was scoped to `lib/` while the four diagnostics had come from the ledger rather than from a scan. **Twenty sites, and a repo-wide sweep now returns zero residual URL-interpolating throws.** Three status-only throws stay untouched by construction — `committees-sync:72`, `markets:261`, `meetings-sync:49` — the second of which corrects the ledger's own citation (`:251` is the URL build, `:261` is the throw) and the third of which the ledger never named at all.

**Row 5 closed at NO CHANGE on a structural argument rather than a judgement call.** Fourteen `console.*` sites log an `Error.message`; every one is fed either by a throw now built through `fetchError` or by a status-only throw. Redacting at the **throw** redacts every downstream consumer for free, so the handoff's contingency (*if more than ~10, HALT for a scope ruling*) was never reached — the fix had moved upstream of the consumers.

**CLOSE CRITERION MET WITH ITS CONTROL, on the ruled vehicle.** `/api/cron/nominations` was chosen over `/api/sync` at the HALT because `/api/sync` fails the test the handoff itself set: `runReportCatchup()` precedes the keyed fetch and can write a `reports` row, while the nominations handler's first statement is the keyed fetch whose catch re-throws, so a 403 aborts with **zero writes** outside `cron_runs`. Control on `842a705` wrote row **14171** carrying the tracer **and** `api_key=` in `error_message`, `payload` and the response body (**1/1/1**) — the vehicle is right and the mechanism is what the audit said. The fixed build wrote row **14172** at **0/0/0**, the two assertions checked **separately** so a broken pattern pass could not read green behind the value pass. The surviving `error_message` keeps the full path, all five non-key parameters, the 403 and the upstream body. Both rows deleted with read-backs (**0**, and **0** tracer anywhere in the table), `MAX(id)` back to **14170**, baseline still **3** (ids 322/344/8348). The control ran in an **isolated worktree** with a `node_modules` junction so the shared working tree was never checked out to an old commit; the junction was unlinked before the worktree was removed, since deleting through it would have taken the real install.

**Review caught what eleven green fixtures did not, and that is the durable half.** `BEARER_TOKEN` shipped as `gi` with an unbounded tail and rewrote the prose `token expired` to `token [REDACTED]` — a mutation with no security value on a string carrying no secret — while the gate read **11/11**. The fix-up narrowed it to case-sensitive with a 12-character token-shaped tail and added **two negatives**: `token expired` and `is the row there?` (the latter guarding the trailing-separator repair's `[/=]` condition). The gate is now **thirteen fixtures, nine positives and four negatives**, reading **4/13, exit 1** under an identity stub — every positive fails, every negative passes by construction. **A redaction gate built only from positives measures whether it redacts, never whether it over-redacts**, and the narrowing's cost is stated in SKILL rather than hidden: `Bearer abc123` is no longer matched by the pattern pass, which is acceptable only because the value pass is the one doing the work.

**A process slip, recorded as a recurrence rather than as a new rule, because the rule already exists.** The STEP 1 and fix-up commits were made while standing on local `main` — left there by the previous session's fast-forward — so local `main` briefly carried unapproved work that `origin/main` did not. This is the **HO 655** lesson arriving from the other direction: that entry is about `git branch -f main` being refused while you stand on the branch, and the habit it prescribes (`checkout -b <ho>-review-local` **first**, then move `main`) is exactly what was skipped here. Corrected the same way it prescribes — branch created at the tip, `main` forced back to `842a705`, and the result verified by **reading `git rev-parse main`**, never the exit code of the move. Nothing unapproved reached the remote: `origin/main` sat at `842a705` throughout and the work lived on `679-review`.

**Docs (HO 679):** this block · backlog — **two** OPEN LOOPS lines **struck in place with their text preserved**, the emission-path line (`:74`) and `.env.example` (`:37`), each recording what its own numbers got wrong: seven sources became twenty, six missing env names were seven, and the ledger's `markets.ts:251` citation pointed at the URL build rather than the throw · SKILL — its own commit behind the self-mod guard, § Environment variables gaining the six names it never carried plus a new **Redaction sink (HO 679)** section · **no oddities entry** — nothing failed a control in this HO, and the one candidate (a gate whose positives all passed while the helper mutated prose) is recorded here and flagged to Corey rather than filed unilaterally. **Nothing rotates here; HO 680 rotates both keys from the baseline above, because rotating before the sink fix writes the new key into the next failed run.** **Notes now run through HO 679.**
