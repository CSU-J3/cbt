# HO 426 — Doc sweep: the ideology surfaces (424 band, 425 dotplot)

> Self-claim the next free number: `ls docs/handoffs/ | sort -V | tail`. Body assumes 426 (425 = the /members dotplot, live at `910f5c4`; 424 = the dashboard band, live at `127b798`). If HEAD moved, renumber.
>
> **Docs-only.** `git add` only the doc files by explicit pathspec, ancestry re-checked immediately before push against `910f5c4`, one docs commit. `SKILL.md` is in scope and trips its self-mod guard: **HALT and re-approve** the SKILL edits. No `verify:deploy` (docs don't touch runtime). Show me the diff before committing, no auto-commit.

## Gate — what's already reconciled (do this first, HALT if double-logging)

424 and 425 shipped code with docs deferred into this sweep. Before writing, confirm current state:

- `git log --oneline` since `910f5c4` for any docs commit already tombstoning 424/425.
- `grep -n "PolarizationBand\|getPolarizationBand\|IdeologyStrip\|getIdeologyStrip\|MemberProductivityScatter\|424\|425" docs/*.md SKILL.md` for what's already present.
- `git status` for which handoff `.md` files (424, 425, and this one) are untracked and need folding into the docs commit.
- Read the roadmap Also-pointer tail live (the HO 422 sweep left it at "run through HO 422"). Move it to 426; report what it read.

Reconcile in place, skip anything already done, report present-vs-applied.

## What the arc did (grounding — don't re-derive)

Two ideology surfaces on top of `member_ideology` (HO 419), both live in prod, no new tables:

- **HO 424, dashboard chamber polarization band** (`127b798`). New query `getPolarizationBand()` (all four chamber-party medians independent of any member; `getMemberIdeology` is member-chamber-scoped and couldn't be reused). `PolarizationBand` server component, `.pol-band-*` CSS: two chamber rails (HOUSE/SENATE) on a fixed −1..+1 dim1 domain (the HO 421 2% inset), D and R median ticks, an amber gap line spanning them, the gap magnitude as the hero. Divs + CSS (the 421 positioned-tick rail at aggregate scale), not SVG. Placed full-width between `RacesBoxTabs` and `WeeklyBand` (the battlefield moved inside `RacesBoxTabs`' non-default tab at HO 270/271, so "below the battlefield" resolved to this always-visible slot). Strict D/R medians via the shared `median()` helper, independents scored but in neither median.
- **HO 425, /members polarization dotplot strip** (`910f5c4`). New query `getIdeologyStrip()` (the full scored population, one cache entry). `IdeologyStrip` client-island SVG Wilkinson dotplot (`binw 0.02`, party-colored, strict-D/R median ticks pinned to the band's method inline, empty-center annotation, hover name + dim1 + hub, click-through). `.ideo-strip-*` CSS, ~180px at the top of `/members`, server-scoped to the chamber toggle only (party/state/sort/search don't rescope). The third live hand-rolled SVG chart.
- **The finding:** both chamber gaps read **0.92** on dim1 (House and Senate level). Cross-checked band-vs-strip, and confirmed 0 `member_ideology.chamber`-vs-`members.chamber` mismatches across 552 rows. Coverage 551 scored / 1 unscored (the known single NULL-dim1 member).

## `roadmap.md`

- **Add an HO 426 housekeeping note documenting both surfaces** (band on the dashboard, dotplot on `/members`), following the per-note stamp convention (each note ends with its own "run through HO N"). **No bar move.** Ideology surfacing rides as an Also, consistent with the HO 422 decision that held Member depth at 99 and reserved 100 for the scorecard. Read the theme percentages live; don't move any.
- **Move the Also-pointer to 426** (single current pointer).
- If a sequencing/queue section frames ideology surfaces, mark the band and dotplot shipped and note the over-time chart remains, data-gated (below).

## `backlog.md`

- **DONE tombstone for 424** (`127b798`, live: the band, `getPolarizationBand`, both gaps 0.92, placement below `RacesBoxTabs`).
- **DONE tombstone for 425** (`910f5c4`, live: the dotplot, `getIdeologyStrip`, `IdeologyStrip`, the band-vs-strip cross-check).
- **Refine the ship-3 (over-time) queued item** with its two prerequisites: (1) a data-layer handoff to sync per-Congress D/R chamber medians from `HSall_members.csv` into a new table (a few hundred median rows, not every member every Congress; liveness + format verified the HO 419 way); (2) the build handoff reads the band's real current-Congress gaps (0.92/0.92 via `getPolarizationBand`) for the right-edge dots, not the mockup's illustrative endpoints, and it's the **fourth** live SVG chart, so the shared scatter scaffold gets extracted then (the standing SKILL trigger).

## `oddities.md`

- **The Senate is now as polarized as the House on dim1 (both gaps 0.92, 119th).** The common "Senate is the calmer chamber" assumption and the over-time mockup's illustrative caption (Senate 0.81 / House 0.88) don't hold on real data. Don't hardcode the mockup endpoints; read `getPolarizationBand`.
- **A client-island chart can't import `lib/queries`' `median()` or server helpers** without dragging `next/cache` into the client bundle. `IdeologyStrip` pins the identical strict-D/R method inline instead. The next client-island chart does the same, keeping the method identical to the server version so the numbers agree.

## `SKILL.md` (self-mod guard — HALT and re-approve)

- **Query registry:** `getPolarizationBand()` (all four chamber-party medians + scored/unscored counts, app-side via `median()`, small-table no index, `member-ideology` daily-TTL tag) and `getIdeologyStrip()` (the full scored population, one cache entry, same tag).
- **Design-system / chart idioms:**
  - The band extends the HO 421 positioned-tick rail (**divs + CSS**, `--ideology-axis` / `.pol-band-*`) to chamber-aggregate scale: two median ticks per rail plus an amber gap line, no per-member marker, the 2% inset applies. It is **not** an SVG chart.
  - The dotplot is a **full-width hand-rolled SVG** Wilkinson dotplot (`BillsTimeSeries` family, `.ideo-strip-*`), no inset (the inset is the rail rule).
- **Fix the SVG-chart inventory (the carried drift):** strike `MemberProductivityScatter` (HO 328 deleted it) and add `IdeologyStrip`. The live count is then accurate at **three** hand-rolled SVG charts (`BillsTimeSeries`, `CommitteeActivityChart`, `IdeologyStrip`). This count is load-bearing: the over-time chart (ship 3) is the fourth, the point where the shared scatter scaffold gets extracted.

Don't narrate the fix history; that's the handoffs' job.

## Commit discipline

One docs-only commit: `git add docs/roadmap.md docs/backlog.md docs/oddities.md` + the SKILL path after approval + the untracked handoffs from `git status`. Ancestry re-checked immediately before push against `910f5c4`. Reconcile in place, not append-a-duplicate. No `verify:deploy`. Show me the diff before committing, no auto-commit.

## Report back

Which gate items were already present vs applied, what the Also-pointer read before the move, the theme percentages left untouched, the diff, and confirmation SKILL was edited only under approval.
