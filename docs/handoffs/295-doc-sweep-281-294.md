# HO 295 — Doc sweep: 281–294

No code. Fold the B-series tweaks and the B2 tape arc into the four living docs. Read each doc's current contents first and reconcile: update in place, don't duplicate entries that already exist, and edit the roadmap STATUS block rather than appending.

Covers HOs 281–294: header type-scale (281), B4 box (282), B5 deltas + HEARINGS + popovers (283, 286), chip-family additive (287), and the B2 tape build (289 equities, 290 ODDS/recession, 291 motion, 292 hook, 293–294 hover box).

## SKILL.md

- Chip family: the 3-tier vocabulary and size ladder. Tier 1 bordered chips (ID amber 11px, topic color@45% 10px); Tier 2 stage pills 10px `LABEL · age`; Tier 3 text tags (party dim, source K/P brand-color 10px, micro-tag EOD/MO 9px dim bordered). Ladder 11/10/9. Tokens `--kalshi #4de4b2` / `--poly #2e5cff`. Shared `<SourceTag>` and `<MicroTag>`. Note the existing id/topic/stage/party chips are NOT yet on this system (BillIdRail is a vertical chamber block, TopicTags 14px no-border, .stage-pill full-color border); migration is queued into B6. Fold the chip-family spec in here as the canonical reference, since chip-family.html was never committed to the repo.
- v2 tape architecture: two stacked marquees (MARKETS / ODDS), pinned labels outside the scrolling track, counter-scroll (MARKETS R→L, ODDS L→R via animation-direction reverse), edge-fade via widened opaque-edge gradients (label::after/meta::before, 72px on the scroll tape / 28px on the bare tape), and the bareTape-excluded set (equities + poly halves ride the v2 scroll only; the bare / tape stays at 8 symbols). MARKETS roster: SPX, NDQ, the five equities (NVDA/AAPL/MSFT/GOOGL/LMT), 10Y, WTI, CPI/UNEMP (MO). ODDS: shutdown, Fed-cut, recession, all dual-source K/P.
- Tape hover box: portaled to body, position:fixed, z-1000, opaque, anchored bottom ~4px above the ticker block's bottom and grown upward (translateY(-100%)) so it pops over the ticker for items on either strip, clear of the nav. Content: title / detail line (name · change · date · freshness) / policy hook. Hooks live in lib/policy-hooks.ts, keyed by roster symbol.
- Freshness derivation: eod = source==='fred' && cadence==='daily' (10Y/WTI); MO = FRED monthly (CPI/UNEMP); intraday indices/equities (SPX/NDQ/the five) carry no freshness badge. Never tag an intraday print EOD.
- Shared-component reach: the WeeklyBand (B5) and the masthead/header type scale (281) render on both / and /dashboard-v2; changes to either land on both.
- Header type scale (281): desktop title 26px, nav 16px, active 2px amber underline, scoped @media(min-width:701px); mobile (HO 244) sizes untouched.

## docs/oddities.md

- Hover-box paint-over: a :hover box nested in a strip sharing a z-index with a sibling strip gets painted over by the later-in-DOM sibling; only the part outside the sibling shows. Fix = portal to body + position:fixed + high z-index + opaque bg. (293)
- A box taller than the thin tape strips spills onto the nav if dropped downward; anchor its bottom near the ticker bottom and grow upward to keep it over the ticker. (294)
- parseTickerDate year-only fallback: some Kalshi markets (recession KXRECSSNBER) carry no strike_date/close_time and no month in the ticker; soonest-event selection needs a year-only fallback. Dated markets (shutdown/fed) resolve first. (290)
- Kalshi rate limit: each symbol fires events+markets; 3+ symbols in parallel trip the public-API limit (429). Retry-on-429/503 with jittered backoff in the Kalshi fetch. (290)
- CPI/UNEMP (FRED monthly) don't show the market-CLOSED wash intraday equities do; correct monthly behavior. (290)
- `_`-prefixed App Router folders are private/non-routable in Next.js; a probe route under /_… 404s. (288)
- 1366px header wrap: the 281 type bump moved the one-line threshold to ~1420px, so 1366 reflows counts/nav to a clean 2nd line. Copy-trimming the counts readout is the only clean lever if 1366 density matters. (281)
- B4 box: RACES in normal flow as the height anchor, HEARINGS overlaid absolute (desktop) / display:none-but-mounted (mobile, preserves MOVES badges); 468px across tabs. (282)
- Dev-server guard: kill lingering node dev servers before a clean `.next` restart (zombies accumulate across sessions serving stale bundles), or you hit the HO 212 stale-CSS trap. (282)

## docs/backlog.md

- Mark DONE: 281–294.
- QUEUED, folded into the B6 arc: existing-chip migration (id/topic/stage/party → chip-family spec).
- BANKED (already logged in 292): hover policy-hook → live CBT bill/topic link, part of the news-linkage arc.
- WATCH (still open): getSponsorsRanked, searchBills mis-plan rewrites.
- Note: debt ceiling has no clean prediction market on Kalshi or Polymarket (288); dropped from ODDS, revisit if one appears.

## docs/roadmap.md

- Read the STATUS block live and edit in place. The B-series tweaks and the B2 tape arc are v2 polish under Home; Home's residual 1% is the unshipped v2 → / swap, which this doesn't change. Update the notes to reflect the tape arc + B-series complete, % held pending the swap. Don't invent numbers; reconcile against what the block currently says.

## Ship

Docs only. Commit (named `git add`). `git push`, `npm run verify:deploy`, served SHA === HEAD (the version endpoint moves even on a docs commit).
