# HO 235 — doc sweep for the 228–234 arc (SKILL + roadmap STATUS + backlog)

Confirm the next free number before saving: `ls docs/handoffs/ | sort | tail`. Body assumes 235.

## What this is

The deferred sweep for the whole HO 228–234 group, now that the 10-item dashboard design pass is complete. Doc-only — **no code changes anywhere.** Two commits: (1) SKILL.md sweep, (2) roadmap STATUS bump + backlog ground-truth.

Standing sweep rule: **re-grep every claim live before writing it.** The list below is the planning chat's ledger of what shipped; treat each entry as a pointer to verify, not text to transcribe. Where the ledger and the live code disagree, the code wins and the report says so.

## Commit 1 — SKILL.md sweep

### New / changed since the last sweep

- **FRED re-source (HO 228):** markets cron sources FRED series via the proper API (`api.stlouisfed.org`), not `fredgraph.csv` (cloud-IP-blocked from Vercel egress — record that as the reason). `FRED_API_KEY` and `FMP_API_KEY` live in **Vercel Production env, not the repo** — document both, plus the env-parity lesson (local `.env` ≠ prod).
- **Nav restyle + BILLS rename (HO 230):** icons dropped, `\`-prefix, bracketed active item, 3-group dividers; the BILLS|NEWS label is gone.
- **Masthead cursor (HO 230):** cursor trails the lead-in prose end, not the title.
- **Races popover re-confinement (HO 230):** restored the HO 170 containment that HO 178 regressed.
- **Topic treemap (HO 231):** area-honest squarified treemap REPLACES the HO 128/132 d3-pack bubble cluster; GOV-anchored, half-height, `?topics=` filter intact; colors from `lib/topic-colors.ts`.
- **MOVERS tab (HO 232):** the middle-column ACTIVITY tab renamed MOVERS in place (label/identifier/aria only; props unchanged). Predicate unchanged: `getStageChanges`, 7d, recency. The from→to display + hop-count sort are **deferred, gated on `stage_transitions` accrual** — record that gate explicitly.
- **`stage_transitions` table (HO 232):** write-only log, `(id, bill_id, from_stage NULLABLE, to_stage, changed_at)` + two indexes, written from `lib/summarize-runner.ts`'s transitioned branch sharing the slot's condition and timestamp. Plant anchor `2026-06-11T18:38:18Z`. No reads anywhere. Validator: `scripts/diagnostic/validate-stage-transitions-232.ts` (tracked) — document its verdict semantics (PASS / PASS-with-note on B>A double-hops / INCONCLUSIVE on quiet ticks / FAIL on missing row, slot mismatch, or orphan).
- **Enacted-this-week (HO 232):** predicate extracted to `lib/enacted-this-week.ts` with the two-consumer split — the cron lead reads it **raw** (post-summarize, pre-revalidate), the banner reads the **cached** `getEnactedThisWeek` (tag `bills`). `EnactedBanner` sits between the report snapshot and the grid; enacted green via existing `--stage-enacted`; empty state renders visibly; +N overflow links `/bills?stage=enacted`.
- **RacesPanelTabs (HO 233):** COMPETITIVE · PRIMARIES tabs on the races panel, ActivityTabs idiom. **The panel chrome (`.home-races-pane`, the popover containment box) moved from `CompetitiveRacesStrip` into the tab wrapper** — record this loudly; anyone touching popover confinement later needs it. PRIMARIES view: 6-month timeline strip + 2×2 soonest-date cards + ALL PRIMARIES → expander; `getDashboardPrimaries` rollup is **uncached**, matching the primaries-helper convention.
- **Single global tape (HO 234):** `DualMarketsTape` DELETED (lifecycle: HO 168 split → 185/202 made it global → retired 234). One `<MarketsTape />` on every page, single AS OF, `.markets-tape-block` z-rules removed, `.markets-tape { z-index: 30 }` carries the stacking. The `group` field remains in the data layer, unused by rendering.
- **Closed-state (HO 234):** `--ticker-closed: #9b3d3d` (the arc's only new token); `lib/market-hours.ts` client-side NYSE signal (9:30–16:00 ET Mon–Fri, Intl-based, hardcoded 2026 holidays, half-days out of scope); wash + `· CLOSED` in the AS OF meta; **precedence `closed = !isMarketOpen && !stale` — STALE wins.**

### Stale content to fix (don't just append — correct it)

- **`showStageTransition` / the transition-render idiom no longer exists** (removed around HO 125/148/191); `/changes` renders plain rows. The SKILL doc still describes the old idiom — rewrite that passage to the live truth.
- **Lead-hide breakpoint is `<700`, not `<1024`** (caught live in HO 230).
- Any remaining dual-tape or bubble-cluster description supplanted by the entries above.

**Commit:** `docs: SKILL sweep for HO 228-234 arc`

## Commit 2 — roadmap STATUS + backlog ground-truth

- **`docs/roadmap.md` STATUS block:** bump theme percentages per shipped state (the design pass mainly moves home-page-redesign and visualizations). Read the current block from the repo, propose old→new per changed theme **in the ship report** so the deltas get an eyeball — the block is the canonical source, so the edit lands here, not in anyone's memory.
- **`docs/backlog.md`:**
  - Close the design-pass block entries (the "dashboard audit design pass (post-HO-226)" group): items 1–4, 6–10 shipped (HOs 230–234), item 5 absorbed-in-place (HO 232).
  - Add the **deferred residue entry:** "MOVERS from→to display + hop-count sort — gated on `stage_transitions` accrual (planted 2026-06-11)." This must not evaporate; it's the back half of design item 5.
  - Add: "`primaries.race_id` backfill-or-drop — dead link (3/907 populated), live pattern is the derived seatId → `race_ratings` join (HO 233)."
- Anything else the live grep shows as closed-but-still-listed gets closed with a one-line note, same as the HO bed693b cleanup did.

**Commit:** `docs: roadmap STATUS bump + backlog ground-truth post design pass`

## Constraints

- Doc-only. No source, CSS, schema, or script changes — if the sweep finds a code problem, report it, don't fix it.
- Named `git add` per commit (the three doc files only). Report the roadmap deltas before pushing so the percentages get a look.
