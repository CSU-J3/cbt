# HO 357 — Doc sweep (post 346–351 arc) + Filler strip prose reconcile

> Claim the next free HO number. Parallel sessions are live on this tree and the
> numbering-collision hazard is real right now — handoff files 352–356 already
> exist untracked. Run `ls docs/handoffs/ | sort -V | tail` and take the next
> free number; if 357 is taken, rename to match.

346–351 shipped and prod-verified (SHA 339f8fc). Reconcile the four living docs
**in place** — find the existing entries/sections and extend or update them; only
add new where there's no home, and don't clobber what's there. Then a small
verify-and-fix on the Filler Watch strip's hardcoded prose numbers (Part B,
folded in per request). Summarize what changed in each doc in your report.

## Part A — the four living docs

### oddities.md

**1. Parallel-agent git race.** Find where the existing "named git add" discipline
lives (oddities or SKILL) and extend it in place — don't duplicate. Add the WHY
and the stronger pattern: the staging index is shared across parallel sessions on
one working tree, so `git add` then `git commit` is racy (another session's commit
can sweep up your staged files — it nearly cost HO 350 this arc). Commit by
explicit pathspec (`git commit <files>`) to bypass the shared index, and re-check
HEAD (`git log --oneline`) before push to confirm you shipped what you staged.

**2. /stale 73s cold query** (HO 350 follow-up, fix 339f8fc). Place alongside the
existing Turso planner-misplan entries. Mechanism: a CASE-based stage sort
defeated the date-index short-circuit, forcing a temp b-tree over a full
date-range scan (73,492ms). Fix: drive single-stage / default-group views off the
selective `idx_bills_summary_stage` (~1.6s) and drop the degenerate CASE sort when
the view is a single stage (43ms). Generalize the lesson: a CASE / computed
`ORDER BY` can defeat an index short-circuit; for single-value-filtered views,
drop the CASE and let the selective index carry the sort.

### backlog.md (OPEN LOOPS)

- **CLOSE** Filler Watch "awaiting Design mock" — shipped (Patterns strip 348 +
  dashboard weekly bar 351).
- **CLOSE** Stale reframe "awaiting HO 345 results" — stage-led base shipped (350,
  fix 339f8fc).
- **ADD** Stale momentum layer — scoped and wired (HO 345 #5–6: current-cosponsor
  rank and the hearing-then-silent `meeting_bills` join are both live), blocked on
  an enriched-row design mock before it can become a build handoff. Back in the
  design chat's court.
- **ADD** `cosponsor_history` table — needed for the dropped support-delta
  ("backed it, then it died"). Append-only, accrues from now on the
  `stage_transitions` / `rating_history` precedent, no backfill possible. Shelved
  until the table exists.
- Leave untouched (none touched this arc): HO 341 watch-star wiring,
  `/dashboard-classic` sunset, HO 157 font-table desktop lifts, news cron 60s
  flakiness.

### roadmap.md (STATUS block — source of truth, don't invent numbers)

- Mark 346–351 shipped. Read the block to see which themes the Patterns/Trends viz
  surfaces and the Stale/bills surface map to, and bump those percentages and the
  overall figure by your own read of remaining work. Do not import percentages
  from outside the block.
- Bump the "through HO N" marker to the highest shipped HO you can verify in git
  log (≥351; 352 reports ladder also shipped per the log — include it if you
  confirm it).

### SKILL.md (conventions — durable only, not a changelog)

- **Filler framing.** Record that the cumulative ceremonial / "filler" read is
  form-based (the four ceremonial clusters), and the per-bill `is_ceremonial`
  classifier's "% ceremonial" stat was dropped from the Patterns detail strip
  because it contradicted the form-based framing. The dashboard weekly bar still
  uses the broad `is_ceremonial = 1 OR cluster IN (four)` definition. Note it so
  future work doesn't reintroduce % ceremonial on Patterns.
- **/stale behavior.** The surface is stage-led now: default PAST COMMITTEE =
  `stage IN ('floor','other_chamber','president')`, enacted excluded, sorted
  stage-desc in legislative order then furthest-first. The old "oldest first /
  60+ days" definition is gone. Update the /stale description in place if one
  exists.
- **Procedural filter.** The /stale procedural-housekeeping filter is a curated
  hybrid: a frozen ID list (13 opening-week items, including the 3 joint-session
  resolutions) + the `electing members%` live title pattern + a blocklist
  (`hres-966`, `sres-6`), with the boundary that `hres-5` (Rules package) and
  "Amending the Rules" resolutions are never filtered. Record where the list lives
  and how to extend it, so it stays maintainable.
- **PatternBubbleSVG retired** in favor of ranked bars on /patterns — update any
  architecture/component note that referenced the bubble. Skip if SKILL doesn't
  catalog it.

## Part B — Filler Watch strip prose reconcile (folded in)

The strip (348) shipped with live headline numbers (now ~1,227 filed) but two
hardcoded prose numbers that didn't rebuild: the register line ("1,097 of the
1,225") and the attrition row's 1,225. The live total drifted +2 since the
diagnostic, so the prose and the computed headline now disagree.

1. Confirm the current live filler total (four-cluster union). Is it 1,227?
2. Find where the +2 landed — facility-naming (the only can-become-law cluster) or
   one of the three non-binding clusters. That decides whether the non-binding
   subset is still 1,097 or moved.
3. Fix durably: wire the register line and attrition row live, same compute the
   headline already uses, so they self-reconcile and never drift again. The
   non-binding subset is the live sum of awareness + honoring + sense-of-congress;
   the structure takes interpolation cleanly — "... non-binding by nature,
   {nonBinding} of the {total}. Only facility namings can become law, and
   {enacted} did." Attrition row interpolates {total} → {pastCommittee} →
   {enacted} the same way.
4. If the prose genuinely can't take live interpolation, correct the static values
   to match live and flag in your report that they need re-checking each sync —
   but prefer live.

This changes the live /patterns page, so it ships like code.

## Commit / report

- Commit doc changes by explicit pathspec (per the hazard above, parallel sessions
  are live). Re-check HEAD before push.
- Part B pushes + `npm run verify:deploy` until the served SHA matches HEAD;
  confirm /patterns renders styled (stylesheet 200, not just route 200).
- Report: a short summary of what changed in each of the four docs, the Part B
  finding (live total, where the +2 landed, what you wired), and the final SHA.
