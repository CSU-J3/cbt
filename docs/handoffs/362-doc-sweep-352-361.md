# HO 362 — Doc sweep (post 352–361) + BILLS→LEGISLATION rename

> Claim the next free HO number. Parallel sessions are live on this tree and the
> collision hazard is real — run `ls docs/handoffs/ | sort -V | tail` and take the
> next free number; if 362 is taken, rename this file to match.

Two parts. **Part A** reconciles the four living docs **in place** for everything
shipped since the 357 sweep. **Part B** is the BILLS→LEGISLATION label rename, which
ships like code. Do Part A first (doc-only, low risk), then Part B.

## What shipped since 357

357 swept 346–351 (and folded in 352). Since then:

- **Reports arc — 352, 358, 359, 360.** Stage-movements ladder (352), floor-votes
  section (358), reports sub-nav kept in-surface (359), floor-votes color rule +
  refinements (360). **None of these four carried a doc section** — they're the main
  gap this sweep closes.
- **Multi-user arc — 354, 355, 356, 361.** 354 was a read-only probe (no footprint).
  355 (auth) and 356 (watchlist per-user) **baked doc edits into themselves**; 361
  (landing) did not. For 355/356 the sweep mostly **confirms those edits actually
  landed** — precedent says baked-in doc edits can fail to commit (356 itself found
  HO 342's doc was never tracked). For 361 the sweep adds the landing entries.
- **353 was a read-only content probe — no doc footprint by design.**

---

# Part A — the four living docs

Reconcile in place: find the existing entry/section and extend or correct it. Only
add new where there's no home. Don't clobber. Summarize per-doc changes in your
report, and call out explicitly whether each 355/356 baked-in edit was already
present or had to be (re-)added.

## SKILL.md (conventions — durable only, not a changelog)

**1. Kill the two stale single-user contradictions.** These almost certainly
survived the 355/356 edits (those added new sections but didn't hunt the old prose):

- "What this is" still reads *"Built for one user (no auth, no accounts)."*
- "What not to do" still reads *"Don't add user accounts or auth. This is
  single-user."*

Both are now false. Correct them: auth shipped (Google OAuth, friends-tier; **logged-
out is the demo, auth gates only the watchlist**). Find and fix any other "single-
user"/"no auth" phrasing in the same pass.

**2. Auth + watchlist (confirm 355/356 landed; add if thin).**
- Architecture line: NextAuth v5 (Auth.js), Google OAuth, JWT session strategy, our
  own minimal `users` table — **no Auth.js adapter**.
- `users` table in the schema block (`id` UUID PK, `google_sub` UNIQUE, email/name/
  image, created_at).
- `watchlist` is now per-user: composite PK `(user_id, bill_id)`, FK `users.id`. The
  schema block in SKILL still shows the old `bill_id`-only PK — fix it.
- `/api/watchlist` 401s when anonymous; read helpers (`getWatchlistBills`,
  `getWatchedBillIds`, `isInWatchlist`) read `auth()` internally and are **uncached**;
  write helpers take an explicit `userId`.

**3. Reports — section anatomy (new; the Reports arc never wrote this).**
- **Stage movements** renders as a **destination-stage ladder** (one rung per stage
  in canonical order, sqrt-scaled bars reusing the dashboard stage-distribution bar,
  zero-count rungs kept dimmed). Committee rung is count-only; rungs past committee
  name their bills on sub-lines. This replaced the old per-transition row list.
- **Floor votes** section sits **after** Stage movements. Named votes one line each,
  outcome verbs `PASSED / FAILED / ADVANCED / BLOCKED / CONFIRMED / REJECTED`; chamber
  (S/H); bill ref is a **bare ID run through `linkifyBillIds` → `/bill/119-…`** (NOT
  `/bills/slug`); margin `yea–nay`; party split shown only on contested/divergent
  votes. Procedural/amendment/UC-nomination votes collapse to a `+ N more` line.
  Classifier reads live `result` / `question` / `amendment_designation` — no invented
  strings. Recess states handled (one-chamber-dark line; both-in-session-no-votes;
  both-in-recess).
- **content_md is one pure-markdown string.** Web render and `.md` download are
  identical **except** one deliberate web-only divergence: `ReportMarkdown` colors the
  outcome verb by exact-text match on the bolded token (`PASSED/CONFIRMED` →
  `--stage-enacted`; `ADVANCED` → `--stage-floor`; `FAILED/BLOCKED/REJECTED` →
  `--party-republican`). The stored markdown carries no color/HTML. (Fuller "why" note
  goes in oddities, item 1.)

**4. Reports sub-nav.** CHANGES / PRESIDENT / REPORTS all render **inside the reports
surface** (masthead + persistent sub-nav, breadcrumb `…\Reports\Changes|President`).
PRESIDENT no longer exits to a bare `/bills` page. Update any note that described it
as a link-out.

**5. Landing.** `app/welcome/page.tsx` owns the landing (no masthead). Routing is a
**cookie redirect from `/`, no middleware** (`ct_seen` cookie + anonymous → redirect
to `/welcome`; "Enter terminal" sets the cookie and routes to `/`). The landing rows/
tape/breaking-strip are **static presentational replicas, not the live `BillRow` /
`MarketsTape`** — the only thing sourced live is chip colors from `topic-colors.ts`.

**6. Brand note (minimal — do NOT do the brand sweep here).** The user-facing brand
is moving to **"Congressional Terminal"** (masthead reads `Congressional Terminal:\…`).
The full CBT→Congressional Terminal rename is a separate deferred sweep (backlog item
below). For now add **one line** noting the brand is in transition so the doc doesn't
fight that loop. Leave the existing "CBT / Congress Bill Terminal" strings for the
brand sweep.

## oddities.md

**1. Report web/download color divergence (HO 360).** `ReportMarkdown`'s exact-text
outcome-verb coloring is the report's **single deliberate web-vs-file divergence** —
color is render-layer only; the `.md` is plain bold. Record it next to the existing
report/markdown-parity note (the HO 352 lesson) so a future "make `.md` match web"
instinct doesn't try to push color into `content_md` (it can't). Note the same rule
could later re-color the 352 ladder's stage labels.

**2. Report prose undercount (HO 352 lesson; add if not present).** Feed the prose-
generation prompt the **true structured counts + the full named list**, never a
sample — the section lead miscounts off a sample.

**3. Confirm 356's drive-order edit landed.** `/watchlist` should be in the drive-
order trap note (drive from the small user-filtered side, force with `INDEXED BY` on
the watchlist PK), with HO 342 recorded as never-shipped / absorbed into 356. If 356's
oddities edit didn't land, add it.

**4. The uncacheable-helper trap.** `getWatchlistBills` / `getWatchedBillIds` /
`isInWatchlist` **cannot** be `unstable_cache`-wrapped once they read `auth()` —
`unstable_cache` has no request-scoped cookie access, and a global cache leaks one
user's stars to another. Uncached is required, not a regression. Record where this
lives if 356 already put it in SKILL; don't duplicate across both docs — oddities is
the better home for the "why you can't cache these" trap.

## backlog.md (OPEN LOOPS)

- **CLOSE** landing B1 — shipped (361).
- **CONFIRM 356's closes landed** (re-add if not): `/watchlist` 500, `getSponsorStates`
  deletion, HO 341 `getWatchedBillIds` loop (now per-user), HO 342 tombstoned as
  absorbed-into-356.
- **ADD** brand/URL rename — CBT → Congressional Terminal across global `<title>` /
  metadata, README, manifest, `metadataBase`, the `verify:deploy` host string
  (`/api/version` poll target), and in-app "CBT" strings. Flagged out-of-scope by 361.
  *(BILLS→LEGISLATION is NOT an open loop — it ships in Part B of this handoff.)*
- **BANK** (not scheduled): HO 352 ladder stage-colors could ride the 360
  `ReportMarkdown` color rule (extend the exact-text match to stage labels); the
  CHANGES/PRESIDENT-as-peers-of-REPORTS regroup question (359 deferred).
- **LEAVE OPEN, untouched this arc:** `/dashboard-classic` sunset; HO 157 font-table
  ≥701px desktop lifts; news cron 60s flakiness; Stale momentum layer (awaiting an
  enriched-row design mock); `cosponsor_history` table (shelved until it exists).

## roadmap.md (STATUS block — source of truth, don't invent numbers)

- Mark the Reports arc shipped (352 ladder, 358 floor votes, 359 sub-nav chrome, 360
  color rule). Bump the **Reports** theme % and the overall figure by your own read of
  remaining work. Do not import percentages from outside the block. **352 may already
  be in 357's bump — confirm and don't double-count.**
- Bump the "through HO N" marker to the highest shipped HO you can verify in `git log`
  (≥361).
- The multi-user/auth arc (355/356/361) is **net-new direction, not one of the seven
  tracked themes** — leave the theme bars unchanged for it (356 already said this). Do
  not mint a new theme bar.

---

# Part B — BILLS → LEGISLATION rename (ships like code)

Rename the user-facing **display label** "BILLS" to "LEGISLATION". **Label only — the
route stays `/bills`.**

**Grep first, then change everything you find.** Locate every user-facing occurrence
of the BILLS/Bills label before editing: top-nav item, breadcrumb/masthead segment,
page headings (`<h1>`/masthead strings), any nav array, aria-labels/title attributes.
Likely homes: the header/breadcrumb chrome components (HeaderBar / BreadcrumbMasthead /
the HO 185 / 323 / 325 chrome) and any NAV config array.

Rules:
- Swap the **display string** to the spelled-out word: `LEGISLATION` where the surface
  is all-caps nav, `Legislation` where the breadcrumb is title-case. **Match each
  surface's existing case. Do NOT abbreviate to "LEG."**
- **Do NOT touch the route or any identifier.** `/bills`, `/bills?q=` (HO 338 FTS),
  `/bills?stage=…`, every internal `href`, the `bills` table, query helpers, file/
  component names — all unchanged. This is a string swap, nothing else. (If a `/bills`
  → `/legislation` route move is ever wanted, that's a separate heavier change — flag
  it, don't do it here.)
- **Layout check:** "LEGISLATION" is 11 chars vs "BILLS" 5. Eyeball the top-nav after
  the swap for overflow/crowding at desktop **and** the <700px mobile nav. If it
  overflows, **flag it** — don't silently shrink type or abbreviate.

**Capture in SKILL.md:** wherever the nav/IA is described, the bills surface's nav
label is "LEGISLATION" (route stays `/bills`). One line.

## Small auth-arc verify (fold into your 355/356 confirmation)

HO 361's body said `signIn("github", …)` for the landing "Sign in" CTA, but 355 built
**Google only**. Confirm `components/LandingCTAs.tsx` (and any `AuthButton` sign-in
call) calls `signIn("google")`. If any still says `"github"`, it targets a provider
that doesn't exist — fix to `"google"`.

---

# Commit / report

- **Part A** commits doc changes by **explicit pathspec** (parallel sessions are live
  on this tree — `git commit <files>`, not `git add` then bare commit). Re-check HEAD
  (`git log --oneline`) before push.
- **Part B** is a separate commit. Named `git add <file>`, explicit pathspec, re-check
  HEAD, `git push`, then `npm run verify:deploy` until the served SHA === HEAD; confirm
  the nav renders styled (stylesheet 200, not just route 200).
- **Report:** per-doc summary of what changed; for each 355/356 baked-in edit, whether
  it was already present or you had to add it; the github→google finding; the rename's
  grep hit-list + any nav-overflow note; and the final SHA(s).
