# HO 165 — SKILL.md update for shipped dashboard work (162/163/164)

## Why

SKILL.md was reconciled by the HO 161 sweep, but three handoffs shipped since (162 header redesign, 163 races strip, 164 activity drawer) and the doc now lags them. This is normal drift — closing it now keeps the doc accurate before the next handoffs (166 color-key relocation) ground against it. 166 in particular edits elements 162 moved (footer legend, ColorKeyStrip, LegendBadge), so the doc must describe the post-162 state or the next handoff starts from a stale premise.

This is a **factual update of three known changes**, not a sweep. We have full context on all three — they shipped this session. No diagnostic needed; just bring the affected sections current.

## Scope — update these sections to match shipped reality

**1. HO 162 — header redesign (the `/` entry + the `--home-header-height` note + the `HomeHeader` component entry).**
What shipped:
- `.home-header-top` is now **single-column** (`flex-direction: column`), not a two-child flex row. The `ColorKeyStrip` legend is no longer in the masthead.
- Brand prompt (`.home-header-prompt-row .terminal-prompt`) is **36px** on desktop (`@media min-width:1024px`), scoped so the `<700` 24px grow and the 700–1023 18px base are untouched. The brand is now the dashboard anchor.
- The lead-in prose stacks **below** the brand (was baseline-aligned beside it). Lead `max-width` bumped **1200px → 1400px**.
- A **header `?` badge** (`LegendBadge`, extracted from ColorKeyStrip) sits near the brand — text-only popover (PARTIES / BILL TYPES / ACCENT).
- The legend (`ColorKeyStrip`, STAGES + TOPICS) moved to a new **`.home-footer`** region (`.color-key-footer`) after `</main>` inside `.home-shell`.
- `--home-header-height` is now fully dead (the void fix removed its last structural relevance) — note it as safe-to-delete, same as before but confirm the masthead no longer references it at all.

Note for 166's sake: the footer legend and ColorKeyStrip described here are about to move again in HO 166 — but document the current (post-162) state accurately so 166 has a correct starting point.

**2. HO 163 — competitive-races strip (the `/` entry layout stack + the race-ratings rendering note at ~line 1006).**
What shipped:
- `CompetitiveRacesBlock` is now **mounted** on the dashboard, between the `.home-snapshot-slot` (ReportSnapshot) and `.home-grid`. Update the `/` layout stack to include it in order.
- It renders a **4-across card strip** (`.competitive-races-grid`, `repeat(4,1fr)` → `repeat(2,1fr)` ≤1023 → `1fr` ≤700), not the old vertical `.competitive-race-row` list. The `.competitive-race-row` rules were replaced by `.competitive-race-card`.
- **Chamber mix, Senate-led:** pulls a competitiveness-ordered pool from `getMostCompetitiveRaces`, partitions by chamber, takes top 2 each, renders `[...senate, ...house]`. Senate-control seats read first.
- Card content: seat label (carries HOUSE/SENATE — no separate chamber chip), incumbent line (party dot + name, "OPEN SEAT" when null), multi-source `RatingChip size="sm"` chips. Links to `/race/[id]`.
- The "(handoff 72)" attribution at ~line 1006 should be updated/extended to reflect that the block is now live with the Senate-led mix (HO 163), not just "uses the same RatingChip mapping."

**3. HO 164 — Activity + Top Stalls expand drawers (the `/` entry ActivityTabs description + the client-island set + the accordion section).**
What shipped:
- ACTIVITY and TOP STALLS rows now **expand to the full `BillExpandedPanel`** (summary + meta + committees/news on fetch + action chips), single-open within each tab, **reset-on-switch**.
- New component **`TopStallsList`** (client) — Path B accordion wrapper keeping the `[chip][title][Xd]` row layout, adding expand via the shared hook. New file.
- New hook **`useSingleOpenPanel`** (`{ expandedId, toggle, panelCache, handleLoaded }`) — extracted from BillRowList, now used by both BillRowList and TopStallsList.
- `BillRow` line 63 relaxed: `expandable = typeof onToggle === "function"` (dropped `!compact`). `BillRowList` gained a `compact?` passthrough. `ActivityTicker` now renders `<BillRowList compact bills watchedIds />` (was a hand-rolled `<ul>`).
- **Client-island count update:** add `TopStallsList` to the canonical set (was 24 → now 25; confirm by grepping `^"use client"` under `components/`). Update both the count and the enumerated list at the `### Server / client split` section and any other place that states the count.

## Guardrails

- Factual update only. Don't rewrite untouched sections. Preserve voice and structure.
- Don't touch source files — SKILL.md (`.claude/skills/cbt/SKILL.md`) only.
- Where a section already partially describes something (e.g. the line-1006 race note), edit in place rather than appending a duplicate.
- Re-grep the `"use client"` count rather than trusting "24+1" — confirm the real number.

## Verification

- Show the diff.
- Confirm only SKILL.md changed.
- Confirm the client-island count matches a live grep.
- Re-read the `/` entry top to bottom for internal consistency (no leftover reference to the legend being in the masthead, no contradiction between the layout stack and the component entries).
