# HO 318 — Doc sweep: 315–317 + correct the stale SKILL premises

Confirm the next free number before saving: `ls docs/handoffs/ | sort -V | tail`. Body assumes 318.

The bill-row standardization arc is done: the collapsed row (sponsor line, sans title, shared `TopicChips`) and the expanded panel (shared `BillExpandPanel` containing the shared `BillStageBar`) are unified across the dashboard feed and /bills, differing only in trigger. Three SKILL premises were stale enough to bite mid-arc — cosponsors, sponsor district, and the /bills expand state model — and each cost a correction round. This sweep reconciles the living docs to what actually shipped, with priority on those stale facts so the next handoff doesn't inherit them.

**Docs only. No code, no CSS deletion** (the dead-CSS cleanup is banked separately). Grep SKILL for each premise below; correct where present; if a premise isn't literally in the doc, skip it.

## SKILL.md — corrections

1. **Cosponsors.** Anything saying cosponsors are skipped / not stored is wrong. `bills.cosponsor_count` is stored (in `SPONSOR_ENRICH_SELECT`) and rendered in the expanded panel's meta card.
2. **Sponsor district.** Any "state stored, not district" framing is wrong. District is available via the `SPONSOR_ENRICH` members-join (`msp.district`) and renders in the `[D-NV-4]` bracket on both surfaces.
3. **/bills expand state.** Replace "?expanded= URL state" with the real model: React click state via `useSingleOpenPanel`, single-open, panel mounted as a sibling below the row. (The sibling mount was correct; the URL-state claim was not.)
4. **All-mono rule.** "All UI stays strictly monospace" now has a carve-out: bill **titles** are system sans (the `--sans` token exists), everything else stays mono. Document the carve-out and the token.
5. **Topic chip.** Now the shared `TopicChips` component — bordered, topic color from `lib/topic-colors`, tooltip via the shared primitive. Note the one remaining borderless `TopicTags` holdout: `/bill/[id]`.
6. **Expanded panel + stage bar.** Now shared: `BillExpandPanel` (which contains the one `BillStageBar`), imported by both `V2FeedList` and `BillRowList`. Document the interaction split — dashboard expands on hover (`:hover`/`:focus-within`, nested in the row, box grows inline, data lazy-loads on first hover); /bills expands on click (single-open, sibling). Single-open both ways; inner links `stopPropagation`.
7. **Topic overflow.** Kill any "fit-to-width + N" language. The shipped behavior is the responsive path: all chips on desktop, first + `+N` on mobile. Measured fit-to-width was never built — leaving the claim in is why it keeps getting re-asserted.

## backlog.md — OPEN LOOPS reconciliation

Confirm these are banked (Code banked some during 317):
- **Rich HEARING slot** — narrow scope: `getMeetingsForBill` already returns the full meeting; it's a panel-route projection-widen + a meeting-selection rule (nearest upcoming vs most recent vs live-first), no new pipeline.
- **Touch expand** — the dashboard's hover-only expand doesn't open on tap (no hover on touch); keyboard works via focus-within. Lands with the mobile redesign.
- **Dead-CSS sweep** — the old `.v2f-exp` / `.v2f-side` / etc. expand classes (now unused after 317) plus the older `.hill-band` and unused `lib/hearings` exports from 306. One cleanup pass; grep each class repo-wide before deleting.
- **/bill/[id] topic-chip migration** — the last borderless `TopicTags` holdout. Scale decision: match the 9.5px row chip, or keep the bordered treatment sized up for a page with room.

Add these owed eyeballs (both are quick "just look" checks):
- **Stage-node states** — only the moving/pulse state was visually confirmed this pass (HR 9352, cyan COMMITTEE node). The parked (stalled ≥60d) and enacted (solid green, no pulse) states are verbatim from the shipped 298 bar, so low risk, but unconfirmed in the unified panel. Confirm when such a bill is in the feed.
- **/bills open-panel parity** — the /bills expanded state couldn't be headless-captured (React click state, not URL state). Click a /bills row and confirm the panel renders identically to the dashboard's.

## roadmap.md — STATUS block

The bill-row standardization arc (collapsed + expanded, both surfaces) is complete. If it moves a theme percentage (Home / Viz, whichever tracks the feed/row work), bump it — **read the current % from the repo STATUS block, don't assert a number**. If it doesn't map cleanly to one theme, leave the bars and just record the arc as done.

## oddities.md

If oddities tracks process gotchas (not only code quirks), add the recurring lesson from this arc: SKILL's mechanism facts (what's stored, how state works) lag the live repo and have caused premise errors — verify schema and state-model claims against a live grep before scoping, don't trust the frozen notes. Skip if oddities is code-gotchas only.

## Constraints

- Docs only. No code, no CSS deletion.
- Reconcile **in place** — correct existing lines, don't append duplicate sections.
- Named `git add` on the doc files. No deploy needed (docs don't affect the build); standard push.

## Ship report

- Each SKILL correction: show the old line and the replacement.
- Backlog: the four banked items + the two owed eyeballs present.
- Roadmap: whether a theme moved and to what (read from the repo, not asserted).
- Confirm docs-only — no code or CSS touched.
