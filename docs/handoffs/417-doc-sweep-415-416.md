# HO 417 — Doc sweep: the FEC/member arc (415–416) + Member depth 98→99

> Self-claim the next free number: `ls docs/handoffs/ | sort -V | tail`. Body assumes 417. If HEAD has moved past 416, renumber.
>
> **Docs-only.** No code, no schema, no sync, no `sync-fec.ts` edit. `git add` only the doc files listed — explicit pathspec, ancestry re-checked immediately before push, one docs commit separate from any code. `SKILL.md` is in scope and trips its self-mod guard: **HALT and re-approve** the SKILL edits, don't route around the guard. No `verify:deploy` — docs don't touch runtime.

## Gate — what's already reconciled (do this first, HALT if double-logging)

HO 414's doc sweep already landed (`dea073d`: member→news loop half-close, roadmap Also note). HO 415's code shipped (`c36f0a2`) but **never got a doc sweep** — it deferred into this one. HO 416 shipped (`62b7f8e`); its handoff `.md` is untracked. Before writing, confirm the current state so this doesn't duplicate:

- `git log --oneline` since `dea073d` — is there a docs commit already tombstoning 415 or 416?
- `grep -n` backlog.md for `415` / `416` and for the by_size loop — is the loop already closed and are the tombstones present?
- `git status` — which handoff `.md` files (415, 416, and this one) are untracked and need folding into the docs commit?

Reconcile **in place**, skip anything already done, report which items were already present vs applied.

## What this arc did (grounding — don't re-derive)

Three handoffs, one theme (member FEC fundraising surface), commits confirmed:

- **HO 414** (`3e7d1f1` code / `dea073d` docs) — member→news on `/members/[bioguideId]`, second consumer of the observation layer. Already swept; leave its tombstone.
- **HO 415** (`c36f0a2`) — the by_size backfill. The real finding: `fetchFecBySize` hard-coded `cycle=2026`, but `schedule_a/by_size/by_candidate` keys to the **candidate's election cycle**, so off-cycle senators (Class 1/3, up 2028/2030) returned empty at 2026. Fixed with `nextElectionYear ?? CYCLE` (HO 411's specials-aware column, one source of truth). by_size **464 → 530**, `bysize_empty=0`. This answered the standing HO 390 question: **there is no genuine by_size floor** — the entire "empty-for-valid-ID" bucket was the cycle-key bug, 100% recovered. Also carried the rate-hardening (cap-55 / 1100ms / `skipped_fresh` cursor) forced by the key being 60/hr, not the 1,000/hr the code comment claimed.
- **HO 416** (`62b7f8e`) — the `totals_failed` `candidate_id` sweep. 2 departed (Grijalva, Greene) dropped via an `is_current=1` filter on `fetchPendingMembers`; 8 House→Senate 2026 switchers re-resolved to live Senate ids via a new `data/fec-candidate-overrides.json`; Marshall re-resolved (scoring-tie case, added to the same override); Zinke and Barrasso left as named no-action residuals. A "Senate 2026 campaign" display tag derives from resolved id being S-prefix while `chamber='house'`. by_size **530 → 539**.

## `roadmap.md` STATUS block (read it live, edit in place — don't trust a remembered %)

- **Bump Member depth to 99.** Both HO 404 holds are discharged: member→news shipped (414), by_size floor is 0 (415, 539 populated). Read the current value (expected 98); if it's not what's expected, surface it rather than overwriting blind. Edit the metric and the bar.
- **Add an Also (HO 414–416) note** recording the arc → Member depth 98→99, following the append-a-superseding-note convention (don't rewrite the dated HO 404 anchor). Update the "Also notes run through HO NNN" pointer to 416.
- **Don't bump past 99.** If the block documents a 99→100 hold, leave it — that's the deferred member scorecard arc (career vote participation + external scorecards), still banked and unbuilt. Surface it, don't close it.
- Foundation (96) and Races (99) don't move — this arc is Member-depth only.

## `backlog.md`

- **Close the by_size OPEN LOOP** — it was the second Member-depth-98 hold, discharged by 415 (floor 0). Strike-and-close in place, DONE pointer.
- **DONE tombstones** for 415 (`c36f0a2` — cycle-key fix, 464→530, floor 0, HO 390 question answered, rate-hardening) and 416 (`62b7f8e` — 2 departed filtered, 8 switchers + Marshall via override, 530→539, Zinke/Barrasso residuals). 414's tombstone already exists — leave it.
- **Open loops to carry (these are real, don't drop them):**
  - **Vercel Production scope key** — the registered `K0Ue` key needs adding to Vercel Production env, or the deployed app's live FEC calls stay on the 60/hr tier. Corey's action (dashboard), not code.
  - **Empty `FEC_API_KEY=` line in `.env.production.local`** — resolve or delete; a zero-length value can shadow a real one. Corey's action.
  - **Stale `sync-fec.ts` comment** claiming "well under 1000/hr" — false (the key was 60/hr throughout). Correct on the next `sync-fec.ts` code touch; not worth a commit of its own.
  - **Zinke residual** — `candidate_id` correct, `totals@2026=0` is genuinely-unfiled/$0. Re-check when he files. Logged by the `totals empty @2026` line each run.
  - **Barrasso residual** — id correct, upstream FEC totals gap (empty at both 2026 and 2030, so not cycle-keyed). Clears only when FEC re-serves his totals. `530→531` does not fall out of any code fix.
  - **Switcher override expiry** — `data/fec-candidate-overrides.json` entries are time-bound; remove a switcher when they drop the Senate bid or leave office.
  - **Empty-retry drip** — a chronic empty (null result → `small_dollar` unset → not skipped) is retried every window at 1 call each. Current floor is 0 so it's inert; revisit only if `bysize_empty` goes non-zero.

## `oddities.md` (field notes — the traps a future author re-trips)

- **FEC by_size is election-cycle-keyed; totals are reporting-cycle-keyed.** `schedule_a/by_size/by_candidate` returns data at the candidate's election cycle, so off-cycle senators (Class 1/3) are empty at the current year and populated at their election year — query it via `nextElectionYear ?? CYCLE`. Totals are the inverse: they resolve at the reporting cycle regardless of class (Alsobrooks, Class 1/2030, has `totals@2026`). **Do not extend the cycle logic to the totals path** — it's a fix for a bug that isn't there. Barrasso empty at both 2026 and 2030 confirmed totals aren't cycle-keyed.
- **No totals ⇒ no by_size (structural gate).** The sync gates the by_size fetch behind a fresh/successful totals call, so a member with an upstream totals gap can't get by_size populated even if it's independently available. Barrasso is the worked example — id valid, by_size unreachable because totals fails first.
- **Member FEC identity overrides live in `data/fec-candidate-overrides.json`**, consulted by the resolver before chamber-derived lookup — the second such `data/` override after `senate-special-elections.json`. Two reasons a member lands there: a House→Senate switcher (resolver keys office off `chamber='house'` and re-finds the dormant House id forever), or a scoring tie between a stale and live id (Marshall — his House-era and Senate ids tie at 6, first-wins picks stale). Entries are time-bound (drop on bid-exit / departure).
- **Switcher display tag is computed, not flagged.** The "Senate 2026 campaign" tag on the fundraising line derives from resolved `fec_candidate_id` being S-prefix while `members.chamber='house'`. Correctly-chambered senators (Marshall) render plain — the tag keys off the mismatch, not off override-file membership.
- **Departed members drop out of FEC sync via `is_current=1`** on `fetchPendingMembers` (mirrors HO 411's `backfill-races` guard). Verified `sync:members` flips departed members to `is_current=0` correctly (Grijalva, Greene both read 0), so the filter is the whole fix — no upstream staleness bug.

## `SKILL.md` (self-mod guard — HALT and re-approve before editing)

Additions, only after approval, kept to structural conventions:

- FEC cycle rule: by_size queries at election cycle (`nextElectionYear ?? CYCLE`), totals at reporting cycle. One line, load-bearing.
- Member FEC identity overrides: `data/fec-candidate-overrides.json`, consulted before chamber-derived resolution (name it alongside the specials file as the override-consulted-by-resolver pattern).
- Standing rule: FEC sync filters `is_current=1` (departed members excluded).

Don't narrate the fix history — that's the handoffs' job.

## Commit discipline

One docs-only commit: `git add docs/roadmap.md docs/backlog.md docs/oddities.md` + the SKILL path only after approval + any untracked handoff `.md` files from the gate (`git status`). Ancestry re-checked immediately before push against `62b7f8e`. Reconcile in place, not append. No `verify:deploy`. Show me the diff before committing — no auto-commit.

## Report back

Which gate items were already present vs applied, the Member depth value found (confirm it read 98 before the bump), the diff, and confirmation SKILL was either untouched or edited under approval. If the roadmap block carries a 99→100 hold, name it rather than closing it.
