# HO 322 — Doc sweep: 319–321

Confirm the next free number before saving: `ls docs/handoffs/ | sort -V | tail`. Body assumes 322.

The dashboard-row arc — 319 (expand reverted to click), 320 (distributions click-to-filter restored), 321 (two-line collapsed row) — is done and live (eff94c0). Three SKILL lines lag it. **Docs only** — the dead-CSS cleanup stays banked separately. Grep each line below; fix in place.

## SKILL.md — corrections

1. **Dashboard expand is CLICK, not hover (319).** The §"Dashboard v2 feed rows" HO 317 line says the dashboard `/` expands on HOVER (`:hover` / `:focus-within`, panel nested in the row, lazy-load on first hover). It was reverted: `/` expands on **click** via `useSingleOpenPanel`, the same pattern `/bills` uses — single-open both surfaces, no hover-reveal CSS left, tap = click on touch. (The `/bills` click behavior in that same line is unchanged; only the dashboard half was hover and is now click.)
2. **Collapsed feed row is TWO lines (321).** The §"Dashboard v2 feed rows" collapsed-rowhead bullet still reads "`TopicChips` after the title" (one line). It's now two: line 1 = title only (`--sans`, full `main` width, chips removed); line 2 = sponsor `LASTNAME` (→ the member page, the same route the expand resolves) + party-colored `[party-state]` bracket + the relocated shared `TopicChips`. Grid `[ id | main(1fr) | metric | caret ]`, `align-items: start`. **No feed-query change** — the HO 300 `SPONSOR_ENRICH` already carried the fields.
3. **Distributions click-to-filter is restored on `/` (320).** The §"/" entry says the distributions render in `staticMode` and "porting it into this composition is a future handoff (open loop)." Done: `/` awaits `searchParams`, `?stage=` / `?topics=` → `DashboardFilters`, funnel/treemap go interactive, MOVERS + BREAKING rebase, `ActiveFilterStrip` returns. TOP STALLS / NEW THIS WEEK stay unfiltered (their queries don't take `DashboardFilters` — classic parity, reported not built). **Close the open loop.** `/dashboard-classic` untouched.

## backlog.md

- **Un-bank touch-expand** — 319 fixed it (tap opens on the dashboard now). Mark done / remove from OPEN LOOPS.
- Confirm still banked: rich HEARING slot, `/bill/[id]` chip migration, and the dead-CSS sweep — note the dead-CSS set likely shrank after 319 dropped the hover-reveal CSS, so its cleanup pass should re-grep before deleting.
- Confirm still on WATCH (neither was exercised by 319–321, so they stay): the two owed eyeballs from 318 — stage-node parked/enacted states, and `/bills` open-panel parity.

## roadmap.md — STATUS block

The dashboard-row arc (319/320/321) is complete. If it moves a theme percentage, bump it from the repo STATUS block — **read it, don't assert**. Date to HO 322.

## Constraints

- Docs only. No code, no CSS.
- Reconcile **in place** — don't append duplicate sections.
- Named `git add` on the doc files. No deploy needed; standard push.

## Ship report

- Each SKILL correction: old line → new.
- Backlog: touch-expand removed/done; the rest confirmed banked.
- Roadmap: whether a theme moved (read from the repo).
- Docs-only confirmed — no code touched.
