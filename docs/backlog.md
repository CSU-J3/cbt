# Backlog

Raw ideas not yet ready for a handoff. Graduate entries to `docs/handoffs/` with a number when the shape sharpens.

- **✅ Markets cron failing — FIXED (HO 227).** Root cause: Stooq retired the `/q/l/` light-quote CSV endpoint (404s every symbol; the surviving `/q/d/l/` path is behind a JS browser-verification wall, unusable headless), so all 15 Stooq symbols went stale 2026-06-05 while the route kept reporting `success`. **Resolution:** (1) re-sourced — indices SPX/NDQ/DOW → **FMP** `/stable/quote` (intraday, reuses the existing `FMP_API_KEY`); WTI/NATGAS/TNX/VIX/BTC → **FRED** (EOD, labeled `EOD` on the tape); dropped the 6 sector ETFs + SILVER + GOLD + DXY (no free programmatic source — tape 17→8). (2) **Failure honesty** — a tick that fetches 0/N symbols now `throw`s → `wrapCronRoute` finalizes `error` (HTTP 500), so a future source death shows up in `cron_runs.status` within the 30-min retention window instead of rotting as a buried-`error_message` `success`. Verified locally 2026-06-10: FMP indices ticked fresh (mdate today, real intraday prices); forced total-failure recorded `cron_runs` status `error` (#202), not `success`; EOD tag renders on FRED symbols only. **The failure-honesty fix is fully validated** (locally + in prod — see below).

  **PROD VERIFICATION 2026-06-10 — two infra blockers found, BOTH NOW RESOLVED.** Pushed + deployed (sha `4a45aee`, Vercel READY) then triggered the deployed route. The HO 227 code is confirmed running (error strings match the new throw), and the failure-honesty fix worked perfectly — 4 consecutive `cron_runs` `error` rows (#203/#204 + retries) instead of buried-success. Two blockers surfaced, both since cleared:
  1. **✅ `FMP_API_KEY` was absent from Vercel's Production env** (reported `FMP_API_KEY not configured` for SPX/NDQ/DOW; the trades pipeline reads the same var but is best-effort so it never surfaced). **Resolved** — key added to Vercel Production; indices SPX/NDQ/DOW now tick live from FMP egress, dated today.
  2. **✅ FRED-egress — FIXED (HO 228).** All 5 FRED symbols (WTI/NATGAS/TNX/VIX/BTC) timed out from Vercel egress — **including TNX/VIX**, which the HO 227 premise wrongly assumed "already tick fine in prod" (their last rows were local-run artifacts). Root cause: the `fredgraph.csv` graph endpoint is bot-walled from cloud egress IPs (hangs to timeout from Vercel; reachable only from the laptop — the local-vs-egress trap). **Resolution:** re-sourced `fetchFred` off `fredgraph.csv` → the official JSON API `api.stlouisfed.org/fred/series/observations` (a distinct host-path that DOES reach egress — verified by an egress probe: clean `400` in ~200ms keyless for all 5 series; `fredgraph.csv` timed out from the same probe), keyed by a free `FRED_API_KEY` in Vercel **Production** env. All 5 series confirmed live + fresh (incl. CBBTCUSD "Coinbase Bitcoin"); **none dropped, tape stays 8.** EOD labeling + the 227 honesty path untouched.
- **Substack data post.** Dashboard has enough surface area to support a piece pointing readers at specific bills. Hooks include total tracked, enacted count, presidential-desk count (once `/president` ships). Angle: what's actually moving in this Congress.
- **Mobile redesign of dashboard surfaces.** Desktop ships first across HO 126/131/133. Consolidates HO 123 (mobile touch tooltip equivalents), HO 125 (BillRow mobile-responsive audit), HO 127 (WatchStar mobile tooltip parity), HO 131 (mobile-first redesign), HO 133 (mobile redesign for new header + grid). Trigger when desktop layout stabilizes.
- **/search v2/v3 enhancements.** Per-tab filter rail (v2), type-ahead in SearchBox (v2), LLM-synthesized answer at top (v3). All deferred from HO 129.
- **~~Breaking-news marquee ticker~~ — CLOSED.** Superseded by the BREAKING panel (`BreakingNewsBlock` / `BreakingRow`, HO 114/164/178): the dashboard surfaces recent `news_mentions` as a row panel, not a scrolling marquee. Kept as a tombstone, not silently deleted — the only marquee in the app is the markets tape.
- **Member stock-trades dashboard ticker.** The PTR pipeline already shipped (HO 70 — `lib/trades-ingest.ts`, `npm run sync:trades`, `getMemberTrades`, the member-hub trades block). Residual is a **dashboard-level trades ticker UI** only; the pipeline + per-member surface exist. (Dropped the "parked until a revenue story" framing — the data layer got built.)
- **SVG primitive extraction across chart components** (HO 100).
- **`revalidateTag('primaries')` wiring** (HO 103) — **only if** primaries queries ever get cached. They currently use plain `db.execute` (no `unstable_cache`), so the tag isn't needed; this is conditional, not pending work.
- **Backfill ~26 bills that lost a topic tag during the HO 120 drain.** Trigger: analyst view depends on full tag coverage.
- **Topic taxonomy expansion** if unmapped category counts exceed a useful threshold (HO 121).
- **Cross-Congress historical members** (HO 124); 119th only today.
- **External member scoring sources** (DW-NOMINATE etc.) (HO 124).
- **`/bill/[id]` page redesign** (HO 125). (Note: HO 191 redesigned the bill *expanded panel* in the feed, not the detail page; the page has only had incremental chrome — HO 145 committees, 154.5 nav, 184/185 masthead.)
- **Per-source weighting in media-attention count** (HO 130).
- **Window selection on the per-bill media-attention column** (HO 130). `MediaAttentionCell` is a fixed 7-day count; add a window selector to the per-bill column. (The `/bills` NEWS view already ships 24/72/168/720h windows — this is the per-bill column specifically, which is still 7d.)
- **Filter/sort within Breaking/Top Stalls tab strip** (HO 133).
- **Intraday VIX source.** End-of-day VIX shipped (FRED `VIXCLS`, HO 178); an *intraday* source is still unsourced — Stooq doesn't carry it, Yahoo's unauthed endpoints are too unstable for a refresh contract. Revisit when a free intraday source emerges or a paid plan covers it incidentally.
- **News-as-retirement-research feed.** A loose "possible retirement mentions this week" surface scanning `news_mentions` for "won't seek re-election"-type language near a member name, as a research assistant to flag candidates for the hand-curated retirement seed — NOT a rendered signal (news mining is too unreliable to be source-of-truth). A convenience layer on top of the seed flag, only worth building if the OPEN-seat tag proves valuable first.
- **Relative cash-thin signal on race cards.** Incumbent cash-on-hand ships dim (HO 212); an amber "thin-cash" highlight was cut because the distribution is a smooth gradient with no natural absolute threshold (House $0→$10.8M continuous, Senate N=6). If revisited it must be RELATIVE (bottom-quartile or rank-within-chamber), never an absolute dollar cutoff.
- **S-OK-2026 incumbent miscoded.** Shows Lankford (Class-III / 2028); the 2026 OK Senate seat is the Class-II Mullin seat, now held by appointee Armstrong who can't run — genuinely an open seat, but we have the wrong incumbent and are missing the appointee. Needs a member-data correction, then the OPEN tag (HO 221, held out of the seed until fixed). *(Verified live 2026-06-09: `S-OK-2026.incumbent_bioguide_id = L000575` (Lankford), `incumbent_running` NULL, not in `races-seed.json` — still accurate.)*
- **district-geo (HO 224):** Census cd119 TopoJSON + `lib/district-geo.ts` projection helper + `districtSeatId` join, all 119th-keyed. **Wired into the `/races` district modal (HO 225) AND the `/primaries` district modal (HO 226)** via the cached `GET /api/races/district-geo/[state]` route. `DISPLAY_STALE_STATES` flag is opt-in, default draws 119th everywhere (the modals do not yet act on it).
- **district-geo banked — metro-zoom panels (spec-3 Phase 2).** Dense-state fix (CA/TX cramped at overview zoom). `getStateDistrictGeometry(abbr, subset?)` already takes an optional district-ID subset, so a metro panel is config + a subset-render — **no new code path**. The cleanest unblocked next build off the HO 224–226 surface. *(Also tracked in `docs/roadmap.md` STATUS.)*
- **district-geo banked — race→news linkage.** No race→news join exists (`news_mentions` is bill-keyed; a race has no bills) — the wall every modal / list / card hit across HO 222/225/226. A real pipeline build (a race↔news mapping), not a UI pass. *(Also in roadmap STATUS.)*
- **district-geo banked — primaries-map results-coloring.** A later layer over the same `primary_candidates.vote_pct` (self-filling May→September); the HO 226 primaries map is recency-only by design (HO 205 verdict). *(Also in roadmap STATUS.)*
- **district-geo banked — rating-history sparkline.** Gated on `rating_history` accruing weeks of inflection data (change-detect logging live since HO 220); the chart is the remaining build once the series has depth. *(Also in roadmap STATUS.)*
